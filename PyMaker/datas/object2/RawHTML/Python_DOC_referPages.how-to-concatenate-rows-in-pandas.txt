<div class="post-text" itemprop="text">
<p>I want to concatenate two rows in a DataFrame to one row.</p>
<p>Here is the code:</p>
<p>import pandas as pd</p>
<pre><code>df = pd.DataFrame(columns = ['string1', 'string2'])
df.loc[len(df), :] = ['Hello', 'This is Sam']
df.loc[len(df), :] = ['how are you?', 'from Canada']

#create the next row: ['Hello how are you?', 'This is Sam from Canada']
</code></pre>
<p>How to do it?</p>
<p>You can test the code <a href="https://repl.it/@Chan2019/MurkyRingedExternalcommand" rel="nofollow noreferrer">here</a>.</p>
</div>
<div class="post-text" itemprop="text">
<p>Use <code>agg</code> with <code>append</code>:</p>
<pre><code>df = df.append(df.agg(' '.join), ignore_index=True)
df

              string1                  string2
0               Hello              This is Sam
1        how are you?              from Canada
2  Hello how are you?  This is Sam from Canada
</code></pre>
</div>
<span class="comment-copy">Hi, please post some example data and what the result should look like</span>
