<div class="post-text" itemprop="text">
<p>How A can be changed into B in python?</p>
<pre><code>A = ['a.png', 'b.png', 'c.png', 'd.png', 'e.png']
B = 'a.png;b.png;c.png;d.png;e.png'
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>there's a function called join which might come to your rescue now:</p>
<p>try this:</p>
<pre><code>b = ';'.join(a)`
</code></pre>
<p>in your case:</p>
<pre><code>A = ['a.png', 'b.png', 'c.png', 'd.png', 'e.png']
B = ';'.join(A)
print b #this will return -&gt; a.png;b.png;c.png;d.png;e.png
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>Simple, use <a href="http://docs.python.org/3/library/stdtypes.html#str.join" rel="noreferrer"><code>str.join()</code></a>:</p>
<pre><code>&gt;&gt;&gt; A = ['a.png', 'b.png', 'c.png', 'd.png', 'e.png']
&gt;&gt;&gt; B = ';'.join(A)
&gt;&gt;&gt; print(B)
a.png;b.png;c.png;d.png;e.png
</code></pre>
</div>
<span class="comment-copy"><code>B = ';'.join(A)</code></span>
<span class="comment-copy">please show some effort when you ask questions here. Otherwise nobody will like you...</span>
