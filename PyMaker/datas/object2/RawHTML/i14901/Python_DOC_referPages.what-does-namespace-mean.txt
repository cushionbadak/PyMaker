<div class="post-text" itemprop="text">
<pre><code>&gt;&gt;&gt; parser = argparse.ArgumentParser(prog='PROG')
&gt;&gt;&gt; parser.add_argument('-f', '--foo')
&gt;&gt;&gt; parser.add_argument('bar')
&gt;&gt;&gt; parser.parse_args(['BAR'])
Namespace(bar='BAR', foo=None)
&gt;&gt;&gt; parser.parse_args(['BAR', '--foo', 'FOO'])
Namespace(bar='BAR', foo='FOO')
&gt;&gt;&gt; parser.parse_args(['--foo', 'FOO'])
usage: PROG [-h] [-f FOO] bar
PROG: error: too few arguments
</code></pre>
<p>Is <code>Namespace</code> some keyword in Python? </p>
<p>Is it also used in other cases?</p>
<p>Thanks.</p>
</div>
<div class="post-text" itemprop="text">
<p>It's referring to the <a href="https://docs.python.org/3/library/argparse.html#argparse.Namespace" rel="nofollow"><code>argparse.Namespace</code></a> class. There's nothing special about it; it's not a keyword or anything.</p>
</div>
<span class="comment-copy"><a href="http://en.wikipedia.org/wiki/Namespace" rel="nofollow noreferrer">en.wikipedia.org/wiki/Namespace</a></span>
<span class="comment-copy">@Syn: Here <code>Namespace</code> is capitalized in the first letter. I assume it is different from <code>namespace</code>,and is a keyword?</span>
<span class="comment-copy">No it's not a keyword, it's the name of a class</span>
<span class="comment-copy">@SyntacticFructose you should consider deleting your comment, its a bit rude but also plainly incorrect.</span>
