<div class="post-text" itemprop="text">
<pre><code>Password = subprocess.call(["openssl",
    "enc",
    "-d", "-aes-256-cbc",
    "-a",
    "-in", "/home/xxx/xxx/pvcsadminPWwin.enc",
    "-pass", "pass:password"])
</code></pre>
<p>During the execution of this Python script, I want the password to be extracted.</p>
<p>And when the script executes the password gets printed by default on the screen.</p>
<p>But when I give</p>
<pre><code>print Password
</code></pre>
<p>it prints <code>0</code>.</p>
</div>
<div class="post-text" itemprop="text">
<p><a href="https://docs.python.org/3/library/subprocess.html#subprocess.call" rel="nofollow"><code>subprocess.call</code></a> returns the return code of the called command.</p>
<blockquote>
<p>Run the command described by args. Wait for command to complete, then return the returncode attribute.</p>
</blockquote>
<p>Use <a href="https://docs.python.org/3/library/subprocess.html#subprocess.check_output" rel="nofollow"><code>subprocess.check_output</code></a>.</p>
</div>
<div class="post-text" itemprop="text">
<p>Thanks to Lutz for giving me a track to proceed, and after trying a lot many things, i observed the below things.</p>
<p><code>Password = subprocess.call(["openssl",
    "enc",
    "-d", "-aes-256-cbc",
    "-a",
    "-in", "/home/xxx/xxx/pvcsadminPWwin.enc",
    "-pass", "pass:password"],stdout=subprocess.PIPE)
    stdout, stderr = Password.communicate()
    print stdout[:-1]</code></p>
<p>stdout is a string having endline character in the end. so i picked the whole string except the last character.
and then it returns the correct password.</p>
</div>
<span class="comment-copy">I am using python 2.6.6 and used subprocess.Popen() But it gives output like:          <code>&lt;subprocess.Popen object at 0x2b0c9e0c25d0&gt;</code></span>
<span class="comment-copy">Then take a look at <a href="https://docs.python.org/2.6/library/subprocess.html" rel="nofollow noreferrer">docs.python.org/2.6/library/subprocess.html</a></span>
