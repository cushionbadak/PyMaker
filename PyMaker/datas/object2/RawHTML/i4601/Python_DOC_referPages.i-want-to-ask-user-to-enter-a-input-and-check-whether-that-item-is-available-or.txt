<div class="post-text" itemprop="text">
<div class="question-status question-originals-of-duplicate">
<p>This question already has an answer here:</p>
<ul>
<li>
<a dir="ltr" href="/questions/70797/user-input-and-command-line-arguments">User input and command line arguments</a>
<span class="question-originals-answer-count">
                    12 answers
                </span>
</li>
</ul>
</div>
<p>I want to modify this Program so it can ask the user that for which color Hat he/she wants.</p>
<pre><code>    def hat_available(color):
    hat_color='red,green,blue,yellow'
    return(color.lower() in hat_color)

have_hat = hat_available("red")
print('hat available is', have_hat)
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>Here is your code:</p>
<pre><code>def hat_available(color):
    hat_color=['red','green','blue','yellow']
    if color in hat_color:
        return color

have_hat = hat_available(input('Choose a hat color: ')) 
print('hat available is', have_hat)
</code></pre>
<p>For Python 2 use <code>raw_input()</code> instead of <code>input()</code></p>
</div>
<span class="comment-copy">Which part of the explanation of <a href="https://docs.python.org/3/library/functions.html#input" rel="nofollow noreferrer"><code>input</code></a> in the documentation do you have trouble with?</span>
<span class="comment-copy">Use <code>input</code> instead of <code>raw_input</code> if using python3</span>
<span class="comment-copy">also hat_color should be an array <code>[]</code> (list) instead. But you got a nice start here!</span>
<span class="comment-copy">M having an error in this. raw_input is not defined</span>
<span class="comment-copy">You are right. Didnt want to make it more confusing but i should have changed that.</span>
<span class="comment-copy">Cool. You can mark it as such. Good luck.</span>
