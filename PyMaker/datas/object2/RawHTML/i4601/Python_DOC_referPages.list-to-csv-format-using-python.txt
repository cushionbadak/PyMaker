<div class="post-text" itemprop="text">
<pre><code>a = [{'year': 222, 'title': 'abc'}, {'year': 111, 'title': 'ddd'}]
</code></pre>
<p>how can i convert to csv format like <br/></p>
<pre><code>b =     year, title
        222, abc
        111, ddd
</code></pre>
<p>I use aws lambda that I can't import pandas</p>
</div>
<div class="post-text" itemprop="text">
<p>Use <a href="https://docs.python.org/3/library/csv.html#csv.DictWriter" rel="nofollow noreferrer">csv.dictwriter</a></p>
<pre><code>import csv
import io


a = [{'year': 222, 'title': 'abc'}, {'year': 111, 'title': 'ddd'}]
f = io.StringIO()
fieldnames = ['year', 'title']
writer = csv.DictWriter(f, fieldnames=fieldnames)
writer.writeheader()
for d in a:
    writer.writerow(d)

print(f.getvalue())

out:
year,title
222,abc
111,ddd
</code></pre>
</div>
<span class="comment-copy">Why do you need pandas? <code>import csv</code>...</span>
<span class="comment-copy">Also, this is basically  a dupe of your last question. <a href="https://stackoverflow.com/questions/49423634/json-to-csv-using-python" title="json to csv using python">stackoverflow.com/questions/49423634/json-to-csv-using-python</a></span>
<span class="comment-copy">what is your "a dupe" meaning? I'm a dupe of python using I admit. Just I want to learn more about python and ask too simple question?</span>
<span class="comment-copy">"I use aws lambda that I can't import pandas", means I don't want import pandas. Why you still ask me"Why do you need pandas?", So Funny?</span>
<span class="comment-copy"><b>Dup</b>-licate. And what I meant was why you you think you <b>need</b> pandas? When there are other ways to write a CSV without external libraries.</span>
<span class="comment-copy">Sorry, I using aws lambda cann't use"with open('names.csv', 'w', newline='') as csvfile:", Can you help me ?</span>
<span class="comment-copy">@KennyChan: Please see update.</span>
<span class="comment-copy">obj = s3.get_object(Bucket='excelfile23987', Key='ttttt.json')&lt;br&gt;     body = obj['Body']&lt;br&gt;     a = body.read().decode('utf-8')&lt;br&gt;     # [{'year': 222, 'title': 'abc'}, {'year': 111, 'title': 'ddd'}]</span>
<span class="comment-copy">my original "a"resource is show above. I have an error in"writer.writerow(d)".   "errorMessage": "'str' object has no attribute 'keys'", "errorType": "AttributeError",Please help me</span>
<span class="comment-copy">Don't ask multiple question. I have answered your question. then you told you cant use file. I changed my answer. Now you want another answer.</span>
