<div class="post-text" itemprop="text">
<p>Can we use del function in print statement like this:</p>
<pre><code>var=[11,22,33,44,55,66]
print (del var[2:4])
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p><code>del</code> is a <a href="https://docs.python.org/3/reference/lexical_analysis.html#keywords" rel="nofollow noreferrer">Python keyword</a>. It can't be used in <code>print</code>.</p>
<p>Even if it would be a function and could be used, it would not return anything. So usage in <code>print</code> would not make much sense.</p>
</div>
<div class="post-text" itemprop="text">
<p>From the documentation
<a href="https://docs.python.org/3/reference/simple_stmts.html#the-del-statement" rel="nofollow noreferrer">del is a statement</a>, it does not return anything.</p>
</div>
<div class="post-text" itemprop="text">
<p>you can use del keyword to delete the slice [2:4] like this:</p>
<pre><code>for i in (2,2):
     ... del var[i]
</code></pre>
<p>but it is not the best way to delete a slice; And you can't use del to delete<br/>
a slice in one call. </p>
</div>
<span class="comment-copy">What are you trying to achieve?</span>
<span class="comment-copy">a) <code>del</code> is not a function b) What stops you from testing it?</span>
<span class="comment-copy">Use the Python interpreter. <a href="https://docs.python.org/2/tutorial/interpreter.html" rel="nofollow noreferrer">(Using the Python interpreter - Python Docs)</a></span>
<span class="comment-copy">This question makes no sense to me.</span>
<span class="comment-copy">Next time, before asking: ask yourself "why do I want to do this?".</span>
<span class="comment-copy">How does this answer the question?</span>
