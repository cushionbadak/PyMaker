<div class="post-text" itemprop="text">
<div class="question-status question-originals-of-duplicate">
<p>This question already has an answer here:</p>
<ul>
<li>
<a dir="ltr" href="/questions/20180777/inverting-a-dictionary-when-some-of-the-original-values-are-identical">Inverting a dictionary when some of the original values are identical</a>
<span class="question-originals-answer-count">
                    5 answers
                </span>
</li>
</ul>
</div>
<p>I want the function inverse to return this output:</p>
<pre><code>{3: ['I', 'love'], 2: ['python']}
</code></pre>
<p>the function will replace the key and the value, but if there's the same value twice, it'll be one key. how can I do that?
// here's the code:</p>
<pre><code>def inverse(dict1):

   def main():
        dict1 = {'I': 3, 'love': 3, 'python': 2}
        print(inverse(dict1))

    if __name__ == "__main__": 
        main()
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>You can do that as follows:     </p>
<pre><code>dict1={'I': 3, 'love': 3, 'python': 2}
res_dict={}
for key,val in dict1.items():
    if res_dict.has_key(val):
        res_dict[val].append(key)
    else:
        res_dict[val]=[key]
</code></pre>
</div>
<span class="comment-copy">Any attempt from you?</span>
<span class="comment-copy">I really have no idea how can I do that. I'm not sure how to replace value and key without knowing exactly what they are.</span>
<span class="comment-copy"><a href="https://docs.python.org/3/tutorial/datastructures.html#dictionaries" rel="nofollow noreferrer">Here you go: a description of python dictionaries</a></span>
