<div class="post-text" itemprop="text">
<p>I am writing a python script for crawling emails </p>
<pre><code>from selenium import webdriver
import re

driver = webdriver.Chrome("C:/Users/Sam/Downloads/chromedriver_win32/chromedriver.exe")
driver.get('https://stuactonline.tamu.edu/app/organization/profile/public/id/1488')

doc = driver.page_source

emails = re.findall(r'[\w\.-]+@[\w\.-]+',doc)

print(emails)
</code></pre>
<p>if you notice the end of the link is the number 1488. I am trying to break it up where I can have a for loop that loops from 50 to about 5000 because there are multiple emails I want to get from the different websites ended in numbers from 1000 to 5000</p>
</div>
<div class="post-text" itemprop="text">
<p>You can easily parametrize the url:</p>
<pre><code>for i in range(1000, 5000):
    driver.get('https://stuactonline.tamu.edu/app/organization/profile/public/id/' + str(i))

    #Your stuff...
</code></pre>
</div>
<span class="comment-copy">do you know a way I can avoid repetition, some emails are being repeated and i dont want that</span>
<span class="comment-copy">You can do something like <code>emails = list(set(emails))</code>.</span>
<span class="comment-copy">thanks it worked. What exactly does <code>list()</code> and <code>set()</code> do in this case tho</span>
<span class="comment-copy"><code>set()</code> converts the input list to a Python <a href="https://docs.python.org/3/tutorial/datastructures.html#sets" rel="nofollow noreferrer">Set</a> which has no duplicates by construction. <code>list()</code> is needed to convert the result back to a list and depending on the use case may not be mandatory.</span>
