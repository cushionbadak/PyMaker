<div class="post-text" itemprop="text">
<div class="question-status question-originals-of-duplicate">
<p>This question already has an answer here:</p>
<ul>
<li>
<a dir="ltr" href="/questions/30383659/matching-strings-with-re-match-doesnt-work">Matching strings with re.match doesn't work</a>
<span class="question-originals-answer-count">
                    2 answers
                </span>
</li>
</ul>
</div>
<p>I cannot figure out why this is not matching. I would really appreciate if somebody can help me here.</p>
<pre><code>print(re.match(r"Newton", "Isaac Newton , physicist"))
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p><code>re.match</code> matches at the beginning of the string only, use <code>re.search</code> to find the first match anywhere in the string (see the <a href="https://docs.python.org/3/library/re.html" rel="nofollow noreferrer">documentation</a>).</p>
</div>
<span class="comment-copy"><code>re.match</code> only matches the <i>start</i> of the string.</span>
<span class="comment-copy">Because your string doesn't start with <code>Newton</code>. Perhaps you wanted <code>re.search</code></span>
<span class="comment-copy">Documentation for the re module for python 2.7 is here <a href="https://docs.python.org/2/library/re.html" rel="nofollow noreferrer">docs.python.org/2/library/re.html</a> - scroll down to find <code>match</code></span>
<span class="comment-copy">At least link to the Python documentation...?</span>
<span class="comment-copy">Fair enough added it</span>
