<div class="post-text" itemprop="text">
<p>My functions return variables but after "return" code does not show on console anything unlike Jupiter notebook.</p>
<pre><code>dollar = 1.0
quarter = dollar / 4
dime = dollar / 10
nickel = dollar / 20
penny = dollar / 100


def quarter_returns(change):
    counter_quarter = 0
    while change &gt;= quarter:
        change = change - quarter
        counter_quarter = counter_quarter + 1
    return "Your quarter change is {}, {}".format(counter_quarter,round(change,2))

quarter_returns(0.56)
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>Because that is not the <a href="https://docs.python.org/3/tutorial/interpreter.html#interactive-mode" rel="nofollow noreferrer">interactive mode</a>. Use <a href="https://docs.python.org/3/library/functions.html#print" rel="nofollow noreferrer">print</a> to see the results:</p>
<pre><code>print(quarter_returns(0.56))
</code></pre>
<p>Output:</p>
<pre><code>Your quarter change is 2, 0.06
</code></pre>
</div>
<span class="comment-copy">Don't wanna do that. I googled it but I do not saw anything</span>
<span class="comment-copy">@CeylanBAĞCI Excuse me, what do you not want to do? Display the results? What is the problem then?</span>
<span class="comment-copy">a func  should show string statements with return code on console. am I wrong</span>
<span class="comment-copy">Don't think to get functionalities like Jupyter on terminal if you will try to run file using python command. Jupyter has been implemented in such a way that it automatically displays the value contained in variables. So it is on us whether we'll use print() in jupyter notebook or not. It is for saving the time of user. @Ayxan is correct as I see.</span>
