<div class="post-text" itemprop="text">
<p>I have the following object field:</p>
<pre><code>is_vendor = models.BooleanField(default=False)
</code></pre>
<p>I have the following if statement:</p>
<pre><code>print(customer.is_vendor) //This prints False
if customer.is_vendor:
     print('im a vendor') //This prints even the value above is false

else:
     print('im not a vendor')
</code></pre>
<p>Why is this occurring?</p>
</div>
<div class="post-text" itemprop="text">
<p>You have a string in the field  -- "False" as opposed to False. Which is cool for Django but not python.  Try <code>customer.is_vendor.to_python()</code> instead. That will wrangle it into a boolean for you. </p>
<pre><code>if "False": print 'True!' # is True
if False: print 'True!'   # Nope.

if customer.is_vendor.to_python():
   print "Is a vendor!"
</code></pre>
</div>
<span class="comment-copy">Just a guess (hence a comment), does <code>models.BooleanField</code> implement the <a href="https://docs.python.org/3/reference/datamodel.html?highlight=__bool__#object.__bool__" rel="nofollow noreferrer"><code>__bool__</code></a> method? What happens when you run <code>bool(is_vendor)</code>? If it doesn't implicitly have a truthy value you might need to be explicit in <code>if customer.is_vendor ==...</code> Again, just an educated guess.</span>
<span class="comment-copy">You have a string in the field. Which is cool for django but not python.  Try <code>customer.is_vendor.to_python()</code> instead?</span>
<span class="comment-copy">but it is a django boolean field, why would it be a string?</span>
<span class="comment-copy">Check the source -- The boolean field has a lose definition of boolean and does not wrangle the value by default.  <a href="https://docs.djangoproject.com/en/2.1/_modules/django/db/models/fields/#BooleanField" rel="nofollow noreferrer">docs.djangoproject.com/en/2.1/_modules/django/db/models/fields/…</a></span>
<span class="comment-copy">That's sure loose.</span>
