<div class="post-text" itemprop="text">
<p>Sorry, I'm troubled, I have the sequent suggestion!</p>
<pre><code>lista_yi_da_attiv.append (1+lista_ratio.index(min(x for in lista_ratio if x is not 0)
</code></pre>
<p>returns a sequent </p>
<blockquote>
<p>ValueError: min() arg is an empty sequence</p>
</blockquote>
</div>
<div class="post-text" itemprop="text">
<p>In your case, <code>min(x for in lista_ratio if x is not 0)</code> might equals to <code>min([])</code> (the argument is an empty sequence) that raises <code>ValueError</code> </p>
<pre><code>&gt;&gt;&gt; min([])
Traceback (most recent call last):
  File "&lt;stdin&gt;", line 1, in &lt;module&gt;
ValueError: min() arg is an empty sequence
</code></pre>
<hr/>
<p><strong>Specify a default value for <a href="https://docs.python.org/2/library/functions.html#max" rel="nofollow"><code>max</code></a> and <a href="https://docs.python.org/2/library/functions.html#min" rel="nofollow"><code>min</code></a> to avoid such exceptions caused by an empty sequence</strong>. For instance,</p>
<pre><code>min(list or [0]) # default vaule is 0
</code></pre>
<p>Python3.4 adds a default keyword argument to <a href="https://docs.python.org/3/library/functions.html#max" rel="nofollow"><code>max</code></a> and <a href="https://docs.python.org/3/library/functions.html#min" rel="nofollow"><code>min</code></a>. For instance, </p>
<pre><code>&gt;&gt;&gt; min([], default=0)
0
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>May be </p>
<pre><code>lista_yi_da_attiv.append(1+lista_ratio.index(min([x for x in lista_ratio if x != 0])
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>When your <code>x=0</code> you don't have an index and can't add.</p>
</div>
<span class="comment-copy">Best solution, instead of using Try Except we can follow this.</span>
<span class="comment-copy">It has not solved anything unfortunately</span>
