<div class="post-text" itemprop="text">
<p>I have the following string:</p>
<pre><code>text = "i eat salad"
</code></pre>
<p>I would like to change <code>eat</code> to <code>ate</code> to get:</p>
<pre><code>text = "i ate salad"
</code></pre>
<p>Do i have to use join or split?</p>
<pre><code>text.split()[1] = 'ate'
print(text)
</code></pre>
<p>I've tried this before unsuccessfully.</p>
</div>
<div class="post-text" itemprop="text">
<p>just split and <em>store</em> the result.</p>
<p>Then change the second word, and join again:</p>
<pre><code>text = "i eat salad"
words = text.split()
words[1] = 'ate'
print(" ".join(words))
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>Use replace() to change it. </p>
<pre><code>text = "i eat salad"
text = text.replace('eat', 'ate')
print(text)
</code></pre>
</div>
<span class="comment-copy">you mean <code>l = text.split(); l[1] = 'ate'</code> then <code>print(" ".join(l))</code></span>
<span class="comment-copy">Alternatively <a href="https://docs.python.org/3/library/stdtypes.html#str.replace" rel="nofollow noreferrer"><code>str.replace()</code></a> if you're looking to blanket match all occurrences.</span>
<span class="comment-copy">if OP needs word boundary that's not going to cut it.</span>
<span class="comment-copy">@Jean-FrançoisFabre I've edited my comment to give a brief note, but yes that is true.</span>
<span class="comment-copy">really thank you!!</span>
<span class="comment-copy">note: I obviously don't care about rep since this is CW. Can someone explain the downvote? I checked for proper duplicates but could not find a proper one.</span>
<span class="comment-copy">thank you so much :D</span>
