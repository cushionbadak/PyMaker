<div class="post-text" itemprop="text">
<p>I have python file <code>gui1.py</code>. I want to execute it without terminal, By using an icon to launch the script <code>gui1.py</code>, which will prompt a <a href="https://docs.python.org/3/library/tk.html" rel="nofollow noreferrer"><code>tkinter</code></a> window on the screen.</p>
</div>
<div class="post-text" itemprop="text">
<p>Make your file executable using below command</p>
<pre><code>chmod +x gui1.py
</code></pre>
<p>Or Create a Bash file</p>
<pre><code>vi rungui1.sh

#!/bin/bash
python gui1.py
</code></pre>
<p>then save it and make it executable</p>
<pre><code>chmod +x rungui1.sh
</code></pre>
<p>Then follow these steps:</p>
<blockquote>
<p>Hit Alt+F2, type dconf-editor and hit Enter.</p>
<p>In dconfg-editor goto: org ➤ gnome ➤ nautilus ➤ preferences</p>
<p>Click on executable-text-activation and from drop down menu select:</p>
<p>launch: to launch scripts as programs.</p>
</blockquote>
</div>
<span class="comment-copy">This is not working. I have already done that, it is still opening the file in gedit.</span>
<span class="comment-copy">Check now i added one more method</span>
<span class="comment-copy">No it's still not working.</span>
