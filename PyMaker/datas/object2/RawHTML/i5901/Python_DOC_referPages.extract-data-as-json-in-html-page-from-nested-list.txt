<div class="post-text" itemprop="text">
<p>My data is to find here: <a href="https://paste2.org/72LmCn2P" rel="nofollow noreferrer">https://paste2.org/72LmCn2P</a></p>
<p>How is it possible to extract data from this kind of JSON?</p>
<p><a href="https://i.stack.imgur.com/l6hCo.png" rel="nofollow noreferrer"><img alt="enter image description here" src="https://i.stack.imgur.com/l6hCo.png"/></a></p>
<p>I can see from the View in Firefox, that Firefox can transform this list to a working JSON format. Snippet Code:</p>
<pre><code>    with urllib.request.urlopen("url") as url:
        soup = BeautifulSoup(url, "html.parser")
        output_file.write(str(json.loads(soup.get_text())))
</code></pre>
<p>How Can I transform this kind of data in a useful transformed data?</p>
</div>
<div class="post-text" itemprop="text">
<p>It looks like a <em>JSON respons</em>e you are getting - that means that you don't need an HTML parser to parse it - load it with <a href="https://docs.python.org/3/library/json.html#json.load" rel="nofollow noreferrer"><code>json.load()</code></a> directly:</p>
<pre><code>import json

with urllib.request.urlopen("url") as response:
    data = json.load(response)
    print(data)
</code></pre>
</div>
<span class="comment-copy">Thank you for your suggestion. I get the Error: <code>UnicodeDecodeError: 'utf-8' codec can't decode byte 0xf6 in position 68: invalid start byte</code></span>
<span class="comment-copy">@madik_atma ah, you need to specify the encoding - this might be relevant: <a href="https://stackoverflow.com/a/7146199/771848">stackoverflow.com/a/7146199/771848</a>.</span>
<span class="comment-copy">In your code you have use .load, in the link he explained it for .loads? You mean .load accept the keyword encoding?</span>
<span class="comment-copy">@madik_atma you can do <code>json.loads(response.read(), encoding="your_encoding")</code>.</span>
<span class="comment-copy">Its work now under his flow: <code>data = json.loads(response.read().decode('utf-8', 'ignore')) output_json.write(str(data))</code> . But its still not in the useful formation. How can I extract or load the data from this response data? Thank you!</span>
