<div class="post-text" itemprop="text">
<p>I have a string like this <code>1:sometext:othertext</code> and I have to copy the part after the second colon and paste it at the end of it, <code>1:sometext:othertext othertext</code> </p>
<p>can someone help me?</p>
</div>
<div class="post-text" itemprop="text">
<p>Try like this  </p>
<pre><code>str1 = "hello:hi:world"
x, y, str2 = str1.split(":")
str1 = str1 + str2 # if you need space then str1 = str1+" "+str2
print(str1)
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>You can use string built in <code>split</code> function to do this.</p>
<pre><code>text = '1:sometext:othertext'
text + ' ' + text.split(':')[2]
</code></pre>
</div>
<div class="post-text" itemprop="text">
<pre><code>lines = """1:some text:othertext
2:sometext:othertext
3:sometext:othertext"""

lines = lines.split('\n')

for line in lines:
    key, some, other = line.split(':')
    print '%s  %s' % [line, other]
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>This will produce output as required</p>
<pre><code>text = '1:sometext:othertext'

text = " ".join(text.split(":")[2:])
</code></pre>
<p>'1:sometext:othertext'  --&gt;  othertext</p>
<p>'1:sometext:othertext:abc:jkl'  --&gt;  othertext abc jkl</p>
</div>
<span class="comment-copy">Can you show what you tried that didn't work?</span>
<span class="comment-copy">actually i have 400000+ lines of this, so i need to automate this. How can that be done?</span>
<span class="comment-copy">You are reading it from file ? or you just have those lines in your script ?</span>
<span class="comment-copy">@Deepak: Just iterate over the data. If you don't know what I'm talking about please reread the <a href="https://docs.python.org/3/tutorial/index.html" rel="nofollow noreferrer">tutorial</a>.</span>
