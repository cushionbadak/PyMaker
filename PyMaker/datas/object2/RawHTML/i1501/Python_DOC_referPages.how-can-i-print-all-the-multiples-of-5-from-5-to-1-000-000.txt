<div class="post-text" itemprop="text">
<p>New to Python and having a hard time creating a <code>for</code> loop or <code>while</code> loop that'll give me an output of <code>5, 10 ,15, 20, ...</code> all the way to <code>1,000,000</code>.</p>
</div>
<div class="post-text" itemprop="text">
<p>Use <code>range</code> (note your doing to a million which is gonna take a while):</p>
<pre><code>print(list(range(5,1000005,5)))
</code></pre>
</div>
<div class="post-text" itemprop="text">
<pre><code>total = 5
while total &lt;= 1000000:
    print(total)
    total = total + 5
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>Try this</p>
<p><code>for i in range(0,1000000,5):
  print(i)</code></p>
<p>This will print every time it goes up by 5.
The <code>range()</code> function works like <code>range(start, stop, step)</code></p>
</div>
<span class="comment-copy">I think the python <a href="https://docs.python.org/3/library/functions.html#func-range" rel="nofollow noreferrer"><code>range()</code></a> object is what you want.</span>
<span class="comment-copy">This doesn't answer the "question" which explicitly "asks" for a <code>for</code> or <code>while</code> loop...</span>
