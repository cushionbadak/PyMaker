<div class="post-text" itemprop="text">
<p>I am trying to apply validation for the items in a list. I have managed to open it but am struggling trying to compare if the two words are anagrams. </p>
<p>This is my result in the terminal.</p>
<pre><code>anagram:  ['word,word', 'stiff,schtiff', 'word,word', 'stiff,schtiff', 'word,word', 'stiff,schtiff', 'word,word', 'stiff,schtiff']

Anagram
</code></pre>
<p>In this sample, it is clear I am doing it wrong with the two identical variables A, B but am not sure what to do.</p>
<pre><code>word1 = open('a.txt', 'r').read().split()
word2 = open('a.txt', 'r').read().split()
count = {}
validation = True
if len(a) == len(b):
    for i in range(len(a)):
        if a[i] in count:
            count[a[i]] += 1
        else:
            count[a[i]] = 1  
        if b[i] in count:
            count[b[i]] += 1
        else:
            count[b[i]] = 1     
    for i in count:
        if count[i] % 2 == 0:
            validation = "Anagram"
        else:
            validation = "Not Anagram"
            break
else:
    validation = "Not Anagram"            
print(validation)
</code></pre>
<p>What the hell am I doing?</p>
<p>I want to achieve this in the terminal.</p>
<pre><code>anagram:  ['word,word', 'stiff,schtiff', 'word,word', 'stiff,schtiff', 'word,word', 'stiff,schtiff', 'word,word', 'stiff,schtiff']

anagram, not anagram, anagram, not anagram, anagram, not anagram, anagram, not anagram
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>You can try using <a href="https://docs.python.org/3/library/stdtypes.html#set" rel="nofollow noreferrer"><code>sets</code></a> to to that:</p>
<pre><code>anagram = ['word,word', 'stiff,schtiff', 'word,word', 'stiff,schtiff', 'word,word', 'stiff,schtiff', 'word,word', 'stiff,schtiff']
for elem in anagram:
    items = elem.split(",")
    firstLetters = set(items[0])
    secondLetters = set(items[1])
    if firstLetters == secondLetters:
        print("Anagram")
    else:
        print("Not anagram")
</code></pre>
<p>Output:</p>
<pre><code>Anagram
Not anagram
Anagram
Not anagram
Anagram
Not anagram
Anagram
Not anagram
</code></pre>
<p><strong>Edit:</strong> Here is how you can read them from file and perform comparisons:</p>
<pre><code>with open("anagram.txt","r") as inFile:
    words = [line for line in inFile]
    words = words[0].strip().split(",")
    first = []
    second = []
    for i in range(len(words)):
        if i%2 == 0:
            first.append(words[i])
        else:
            second.append(words[i])
    for f,s in zip(first,second):
        if set(f) == set(s):
            print("Anagram")
        else:
            print("Not anagram")
</code></pre>
</div>
<span class="comment-copy">Perhaps provide what you want to achieve with an expected result.</span>
<span class="comment-copy">Done. See above ^</span>
<span class="comment-copy">The data set is being read from a text file. This is what I am finding difficult.</span>
<span class="comment-copy">So how do you get the elements for <code>anagram</code>? If you could provide some sample data for both files then I could be able to reproduce your problem.</span>
<span class="comment-copy">I retrieve them by opening the file in the text editor. The code reads the list which is inside the text file: a = open('anagram.txt', 'r').read().split()</span>
<span class="comment-copy">Sorry. I have a separate part that prints out the list: anagramlist = open('anagram.txt', 'r').read().split() print(anagramlist)</span>
<span class="comment-copy">As you can see, I have duplicated (which I know is wrong), but don't know how to do this properly.</span>
