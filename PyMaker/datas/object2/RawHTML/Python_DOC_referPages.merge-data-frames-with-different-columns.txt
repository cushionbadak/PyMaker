<div class="post-text" itemprop="text">
<div class="question-status question-originals-of-duplicate">
<p>This question already has an answer here:</p>
<ul>
<li>
<a dir="ltr" href="/questions/53645882/pandas-merging-101">Pandas Merging 101</a>
<span class="question-originals-answer-count">
                    1 answer
                </span>
</li>
</ul>
</div>
<p>I need to merge/join two dataframes that have some column names in common, but also have different columns names. For example:</p>
<pre><code>df1 = {A: hello, B: bye, C:good morning, F: nice}
df2 = {A: hola, D: buenos dias, C: adiós, G: bad}
</code></pre>
<p>So the resulting dataframe I want is like:</p>
<pre><code>  A   |B     |C            |D           |F    |G  
----------------------------------------------------
hello |bye   |good morning |None        |nice |None
hola  |None  |adiós        |buenos dias |None |bad
</code></pre>
<p>I've been trying to merge the dataframes using the 'merge' pandas' function as follows:</p>
<pre><code>res = pandas.merge(df1, df2, on='A')
</code></pre>
<p>But I don't get the results I want.
Any suggestions?</p>
<p>Thanks!</p>
</div>
<div class="post-text" itemprop="text">
<p>First, create your <code>pd.DataFrame</code> objects</p>
<pre><code>df1 = pd.DataFrame({'A': 'hello', 'B': 'bye', 'C':'good morning', 'F': 'nice'}, index=[0])
df2 = pd.DataFrame({'A': 'hola', "D":' buenos dias', 'C':' adiós', 'G': 'bad'}, index=[0])
</code></pre>
<p>Then, just use <a href="https://pandas.pydata.org/pandas-docs/stable/reference/api/pandas.DataFrame.merge.html" rel="nofollow noreferrer"><code>how='outer'</code></a></p>
<pre><code>pd.merge(df1, df2, how='outer')
</code></pre>
<p>which yields</p>
<pre><code>    A       B   C               F       D           G
0   hello   bye good morning    nice    NaN         NaN
1   hola    NaN adiós           NaN     buenos dias bad
</code></pre>
</div>
<span class="comment-copy">Initially, you have two data frames or two dictionaries ?</span>
<span class="comment-copy">@RafaelC well, yeah, I have to dictionaries</span>
<span class="comment-copy">I'm getting the following error:     "type object argument after * must be an iterable, not itertools.imap"</span>
<span class="comment-copy">I solved it as follows:  <code>res = pd.concat([df1, df2], join='outer', sort=False)</code></span>
<span class="comment-copy">@sooaran Your error is not a consequence of my code, because I didn't use <code>itertools</code> anywhere. But glad you could solve it</span>
<span class="comment-copy">I know, I really appreciate your help!! Thanks!!</span>
