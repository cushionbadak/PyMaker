<div class="post-text" itemprop="text">
<p>I have the following statement that I can't seem to find the problem:</p>
<pre><code>Analysis = 'Trythis'
TestName = 'ThisOne'
NumberIteration = 25

for num in range(NumberIteration):
    x= np.loadtxt("%s/results/data_%s/Outputs/$s/%sLCOE.txt" % (Analysis, num , TestName, TestName))   
</code></pre>
<p>I keep receiving the following error:</p>
<pre><code>  np.loadtxt("%s/results/data_%s/Outputs/$s/%sLCOE.txt" % (Analysis, TestName, TestName, TestName)) 
TypeError: not all arguments converted during string formatting
</code></pre>
<p>I have tried to use <code>%d</code> and <code>%c</code>  instead of <code>%s</code>.  Does numpy have a problem using '_' before using a string?</p>
</div>
<div class="post-text" itemprop="text">
<p>Python is complaining that you gave four arguments to <code>%</code> but only have three <code>%s</code> in your format string.  </p>
<p>I think the <code>$s</code> is meant to be a <code>%s</code>:</p>
<pre><code>x= np.loadtxt("%s/results/data_%s/Outputs/%s/%sLCOE.txt" % (Analysis, num, TestName, TestName))
#                                         ^^
</code></pre>
<p>Note however that in modern Python code, you should be using <a href="https://docs.python.org/3/library/stdtypes.html#str.format" rel="nofollow"><code>str.format</code></a> instead:</p>
<pre><code>x= np.loadtxt("{}/results/data_{}/Outputs/{}/{}LCOE.txt".format(Analysis, num, TestName, TestName))
</code></pre>
</div>
