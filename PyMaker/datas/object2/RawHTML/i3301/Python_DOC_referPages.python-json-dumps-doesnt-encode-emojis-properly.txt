<div class="post-text" itemprop="text">
<p>Why does json.dumps() encode emojis into unicode? See code and output below:</p>
<pre><code>import json
obj = {"key": "hello 😀"}
print(obj)
</code></pre>
<blockquote>
<p>{'key': 'hello 😀'}</p>
</blockquote>
<pre><code>print(json.dumps(obj))
</code></pre>
<blockquote>
<p>'{"key": "hello \ud83d\ude00"}'</p>
</blockquote>
<p>I have tried <code>print(json.dumps(obj)).encode('utf-8')</code> and some variants (<code>.decode()</code>...) but it didn't change the output much. Im working on Python 3.6.1</p>
</div>
<div class="post-text" itemprop="text">
<pre><code>print(json.dumps(obj, ensure_ascii=False))
</code></pre>
<p>However, the ASCII variant is more portable, since you are almost guaranteed you won't have encoding problems. <a href="https://docs.python.org/3/library/json.html#character-encodings" rel="nofollow noreferrer">Docs</a></p>
</div>
<span class="comment-copy">have a look at <a href="https://stackoverflow.com/questions/25707222/print-python-emoji-as-unicode-string" title="print python emoji as unicode string">stackoverflow.com/questions/25707222/…</a></span>
<span class="comment-copy">@ChetanAmeta Thanks but this shows how to go from smiley to \ud83... not the other way around</span>
<span class="comment-copy">@Thomas do you want to load from \ud83 to smiley ? is this your requirement ?</span>
<span class="comment-copy">@Murali yes indeed</span>
<span class="comment-copy">@Thomas In that case dump it normally to a file, but when you load it mention the  encoding as "utf-8". doesn't it solve your problem?</span>
