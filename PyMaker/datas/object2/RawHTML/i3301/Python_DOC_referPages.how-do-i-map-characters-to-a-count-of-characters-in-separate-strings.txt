<div class="post-text" itemprop="text">
<p>There will be 2 inputs, <code>person1</code> and <code>person2</code></p>
<p>I need to check if the 2 inputs have the letters t,r,u,e,l,o,v,e (yes, the value of e will be duplicate).</p>
<p>example: putting in john smith and jane doe will result to:</p>
<p>T - 1</p>
<p>R - 0</p>
<p>U - 0</p>
<p>E - 2</p>
<p>L - 0</p>
<p>O - 2</p>
<p>V - 0</p>
<p>E - 2</p>
<p>I hope this makes sense. I am still a python rookie.</p>
<p>EDIT:</p>
<p>I came up with the code below, thanks to your help. Now, I need help. Why doesn't <code>print(tr)</code> generate a value?</p>
<pre><code>person1 = input("Enter your name:")
person2 = input("Enter your lover's name:")
container = []

def makeList(person1, person2):
    from collections import Counter
    c = Counter(person1) + Counter(person2)
    for letter in "truelove":
        container.append(c[letter])

makeList(person1, person2)

print("Just a moment while we calculate...")

def add(a,b):
    sum = a + b
    if sum &gt; 10:
        return sum-10
    else:
        return

tr = add(int(container[0]), int(container[1]))
print(tr)
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>Feed each name to a <a href="https://docs.python.org/3/library/collections.html#collections.Counter" rel="nofollow noreferrer"><code>Counter</code></a>, combine the two <code>Counter</code>s, then look up each character of the third string in the <code>Counter</code>.</p>
<pre><code>from collections import Counter

name1 = "John Smith"
name2 = "Jane Doe"
final = "TRUELOVE"

c = Counter(name1.upper()) + Counter(name2.upper())
for letter in final:
    print(letter, c[letter])
</code></pre>
</div>
<span class="comment-copy"><code>foreach letter in input do findings[letter] += 1 end</code></span>
<span class="comment-copy">The first task should be to break the problem down into simpler steps. Start by writing out how to compare two characters to see if they're the same. Then wrap that in a loop to compare a character to every character in a string. Then loop that over all the characters you need to compare.</span>
