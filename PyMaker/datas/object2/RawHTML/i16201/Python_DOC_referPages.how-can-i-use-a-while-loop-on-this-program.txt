<div class="post-text" itemprop="text">
<p>I need to know how to use a while loop on the output so it keeps outputting "Line: ".
What i am trying to do is reverse each word in a string but it stops once i use one input, i need to use a while loop to keep getting an output and the while loop has to stop on an empty input.</p>
<p>My workaround:</p>
<pre><code>a =input("Line: ")
b=a.split()
ans=''
while a != '':
  for word in b:
    word = word[::-1]
    ans=ans+word+' '
    a =input("Line: ")
    print(ans.rstrip())
</code></pre>
<p>The output i want:</p>
<pre><code>Line: hello world
olleh dlrow
Line: extra
artxe
Line:
</code></pre>
<p>The output I'm getting:</p>
<pre><code>Line: hello world
olleh
Line: extra
olleh dlrow
Line: 
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>you can try in this way    </p>
<pre><code>a=input('input:')
while a.strip()!='': 

    b=a.split()
    ans=''
    while len(b) &gt;0 :
       word=b[0][::-1]
       ans+=word+" "
       b.pop(0)
    print ans
    a=input('input:')

#output i ma hsejar !aloH
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>The easiest way to do this is with a <code>while True</code> loop and <code>break</code>:</p>
<pre><code>while True:
    line = input("Line: ")
    if not line:
        break
    ...
</code></pre>
<p>This works because empty strings <code>""</code> evaluate <code>False</code>-y. You can also make your code more efficient with lists:</p>
<pre><code>words = []
while True:
    line = input("Line: ")
    if not line:
        break
    for word in line.split():
        words.append(word[::-1])
print(" ".join(words))
</code></pre>
<p>This avoids the constant string concatenation with <code>+</code>. </p>
</div>
<span class="comment-copy">Did you try to write <code>while</code> above this?</span>
<span class="comment-copy"><a href="https://docs.python.org/3.4/tutorial/introduction.html#first-steps-towards-programming" rel="nofollow noreferrer">docs.python.org/3.4/tutorial/…</a></span>
<span class="comment-copy">I still don't know how to do it? I tried to do a while loop and stuff but i get an incorrect output :(</span>
<span class="comment-copy">@jonrsharpe think the <a href="https://docs.python.org/3/tutorial/" rel="nofollow noreferrer">Python 3</a> tutorial's more appropriate here.</span>
<span class="comment-copy">So where is your code with a while loop, and <b>what exactly is wrong with it?</b></span>
<span class="comment-copy">Thanks for your reply! I need to print out each time they input something reversed. Your program prints out all their inputs reversed when no input is detected. Can you please print out the input at each line for me?</span>
<span class="comment-copy">You can alter the code for yourself: move the assignment of an empty list to <code>words</code> and the <code>print</code> call inside the <code>while</code> loop.</span>
