<div class="post-text" itemprop="text">
<p>User providing me with search expression, e.g. "^.*/bin/run.sh"<br/>
Need to scan a file and replace search expression and replace with value also provided by user.</p>
<p><code>
search = "^.*/bin/run.sh"<br/>
replace = "/path/to/bin/new_run.sh"<br/>
</code>
how do I search and replace in python 3?</p>
</div>
<div class="post-text" itemprop="text">
<p>Using the <a href="https://docs.python.org/3/library/re.html#re.sub" rel="nofollow noreferrer">re</a> module's <code>re.sub</code> method.</p>
<pre><code>oline = re.sub(search, replace, iline)
</code></pre>
<p>Where you are looping through line by line.</p>
<p>Look at the <a href="https://docs.python.org/3/library/fileinput.html" rel="nofollow noreferrer">fileinput</a> and <a href="https://docs.python.org/3/library/argparse.html" rel="nofollow noreferrer">argparse</a> modules for how to wrap this up into an application if that is what you want.</p>
</div>
<span class="comment-copy"><code>str.replace()</code>?</span>
<span class="comment-copy">You <i>are</i> using regex, right ?</span>
<span class="comment-copy">Yes using regex</span>
