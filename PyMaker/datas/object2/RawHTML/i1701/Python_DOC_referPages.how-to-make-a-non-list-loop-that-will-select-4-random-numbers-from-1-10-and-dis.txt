<div class="post-text" itemprop="text">
<pre><code>import random
counter = 0
count = 0
counting = 0
valueOne = 0
valueTwo = 0
while counter ==0:
    playerOne = random.randint(1,10)
    number = random.randint(1,10)
</code></pre>
<p>About right here is where I get confused. It will run the loop, but every time it runs, the random values get reset, and even if the first time it prints a random number, the second time it might print the same and so forth.</p>
<pre><code>    if number == playerOne:
        count = count + 1
    if number != playerOne:
        valueOne = playerOne

        if number != valueOne:

            print("lotto number",number)
            counting = counting + 1
        if counting &gt;= 4:
            print("it took you this many trys",count)
            input('play again?')
            counting = 0
            count = 0
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>Should list the numbers in 1-10 (inclusive) then randomly sample them.</p>
<pre><code>random.sample(range(1,11), 4)
</code></pre>
<p><a href="https://docs.python.org/3/library/random.html" rel="nofollow noreferrer">random.sample docs</a></p>
<p><a href="https://docs.python.org/3/library/functions.html#func-range" rel="nofollow noreferrer">range docs</a></p>
</div>
<span class="comment-copy">Thankyou for the example. I will look more into those docs and give an update after i experiment a little.</span>
<span class="comment-copy">@Huynh if this helped, please accept the answer.</span>
