<div class="post-text" itemprop="text">
<p>I'm building a flask application and I want to remove the redundancy on importing modules. So, on runtime I want to print all the imported modules.</p>
<p>Is there a way to do that?</p>
</div>
<div class="post-text" itemprop="text">
<p>You can use <code>sys.modules.keys()</code> but you will need to import <code>sys</code> to use it.</p>
</div>
<div class="post-text" itemprop="text">
<pre><code>import sys
print(sys.modules.keys())
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p><code>sys.modules</code></p>
<p><a href="https://docs.python.org/3/library/sys.html#sys.modules" rel="nofollow">docs</a></p>
<p>"This is a dictionary that maps module names to modules which have already been loaded."</p>
</div>
<span class="comment-copy">what exactly are you trying to reduce? you can get a list of imported modules by examining <code>sys.modules</code>...</span>
<span class="comment-copy">I moved some code between modules, but I haven't removed the the imports. So, I want to remove all the uneeded. I just don't need the import calls.</span>
<span class="comment-copy">@malta fixed, thanks</span>
