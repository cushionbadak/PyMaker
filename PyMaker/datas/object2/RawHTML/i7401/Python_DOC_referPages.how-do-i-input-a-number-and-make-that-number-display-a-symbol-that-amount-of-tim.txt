<div class="post-text" itemprop="text">
<p>I am trying to make a program which takes a number as input, and the outputs this many as # symbols.
I got as far as - </p>
<pre><code>Hash = input(“Enter a number? “)
</code></pre>
<p>But I dont know how to use that input to display the # that many times.
E.g.</p>
<pre><code>Enter a number? 6
(printed)######
</code></pre>
<p>Can anyone tell me how I can do this?</p>
</div>
<div class="post-text" itemprop="text">
<p>If you need to print <code>n</code> times the symbol <code>#</code>:</p>
<pre><code>print('#'*n)
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>This simple code will give the result you want.
<code>n=int(input(“Enter a number? “))
print('#'*n)</code></p>
</div>
<div class="post-text" itemprop="text">
<p>Remember to indent this properly
You can use:</p>
<pre><code>Hash=int(input("Please enter a number: "))  
for each in hash:  
    print("#")
</code></pre>
</div>
<span class="comment-copy">You can do: <code>'#' * number</code></span>
<span class="comment-copy">A good start would be to start reading the excellent <a href="https://docs.python.org/3/" rel="nofollow noreferrer"><i>documentation</i></a></span>
<span class="comment-copy">@Someprogrammerdude why not <a href="https://docs.python.org/2.7/" rel="nofollow noreferrer">this documentation</a>?</span>
<span class="comment-copy">I'm guessing this is a homework assignment to teach you loops. Go read about loops in Python, as SPD said, the documentation is a good place to start reading about loops.</span>
<span class="comment-copy">maybe you mean <code>for each in xrange(hash)</code> or simply <code>range(hash)</code> in python 3.x, but for this type of approach is better to use <code>_</code> which is a disposable variable like <code>for _ in range(hash)</code></span>
