<div class="post-text" itemprop="text">
<p>I have a date of format : <code>13 July 2017 22:00</code> that I would like to convert to an <code>ISO</code> date format of type <code>2017-07-13T22:00:00+0100</code>.</p>
<p>So far I have managed to do this: </p>
<p><code>datetime_object = datetime.strptime(date_start, '%d %B %Y %H:%M')</code></p>
<p>where <code>date_start = '13 July 2017 22:00'</code></p>
<p>this returns <code>2017-07-13 22:00:00</code> ... close but not the same.</p>
<p>How can I convert it to the type I need ?</p>
</div>
<div class="post-text" itemprop="text">
<p>Use <a href="https://docs.python.org/3/library/datetime.html#datetime.datetime.isoformat" rel="nofollow noreferrer"><code>datetime.isoformat()</code></a>:</p>
<pre><code>&gt;&gt;&gt; datetime_object = datetime.strptime('13 July 2017 22:00', '%d %B %Y %H:%M')
&gt;&gt;&gt; datetime_object.isoformat()
'2017-07-13T22:00:00'
</code></pre>
<p>Note that this produces a timestamp in a "local time zone" (since your initial date has no time zone information attached).</p>
</div>
