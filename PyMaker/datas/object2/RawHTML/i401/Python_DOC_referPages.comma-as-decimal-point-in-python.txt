<div class="post-text" itemprop="text">
<p>I am new to python. 
In python, I wish to convert float variables to string variable with 2 decimal places and decimal comma. </p>
<p>For example, 3.1415 --&gt; 3,14</p>
<p>it is all right. But when it comes across 1.20, it becomes 
1.20--&gt; 1,2 
but my expected output is like 1,20 
What can I do or what should I type? And how can I improve my current code? Thank You Guys</p>
<p>My code is in the following:</p>
<pre><code>s=float(input())
a=round(s,2)
x=str(a)
y=x.replace('.',','))
print(y)
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>Try using this:</p>
<pre><code>&gt;&gt;&gt; num = 1.201020
&gt;&gt;&gt; '{:.2f}'.format(num).replace('.', ',')
'1,20'
</code></pre>
</div>
<span class="comment-copy">Hi, could you post the code you are currently using for the conversion?</span>
<span class="comment-copy">Possible duplicate of <a href="https://stackoverflow.com/questions/19986662/rounding-a-number-in-python-but-keeping-ending-zeros">Rounding a number in Python but keeping ending zeros</a></span>
<span class="comment-copy">are you expecting to round the decimal or just strip the remaining decimals ?</span>
<span class="comment-copy">round to 2decimals</span>
<span class="comment-copy">Could you explain how it works?  is {:#.2f} treating the variable as float or string? Thank you</span>
<span class="comment-copy">It is string formatting. <a href="https://docs.python.org/3/library/string.html#formatstrings" rel="nofollow noreferrer">The docs</a>.</span>
