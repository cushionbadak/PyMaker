<div class="post-text" itemprop="text">
<p>I wrote the whole code, just in the place of <code>x</code> and <code>y</code>, this has to take input from user:</p>
<pre><code>import numpy as np

A= np.array([[1,1],[2,4]])

print (A)

b = np.array([x,y])

z=np.linalg.solve(A,b)

print(z)
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>Since you already know the shape of matrix and only need two elements, you can input them individually.
You have to typecast accordingly, if they are integers.</p>
<pre><code>x = int(input("Enter x: "))
y = int(input("Enter y: "))
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>You can take the input value from the user in python as ,</p>
<pre><code>x=input("Enter value of x :")
</code></pre>
<p>similarly for y ,</p>
<pre><code>y=input("Enter value of y")
</code></pre>
<p>But this takes the value of x &amp; y as string . so if you want to take the input in integer you must typecast the input as,</p>
<pre><code>x=int(input("Enter value of x :"))
</code></pre>
<p>similarly for y ,</p>
<pre><code>y=int(input("Enter value of y :"))
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>Why not do something like this :-</p>
<pre><code>import numpy as np
x = int(input("input number of rows: "))
y = int(input("input number of cols: "))
arr = np.array([ [ input() for i in range(y) ] for j in range(x) ])
print(arr)
</code></pre>
<p>Where (x,y) is the shape of the matrix, needed to be initiated before this.</p>
</div>
<span class="comment-copy">See <a href="https://docs.python.org/3/library/functions.html#input" rel="nofollow noreferrer"><code>input</code></a></span>
