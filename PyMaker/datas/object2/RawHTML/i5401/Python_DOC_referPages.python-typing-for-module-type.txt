<div class="post-text" itemprop="text">
<p>I am dynamically loading a Python module using <code>importlib.import_module</code> as follows</p>
<pre><code>    def load_module(mod_name: str) -&gt; ???:
        return importlib.import_module(mod_name)
</code></pre>
<p>Can somebody tell me what is the correct type annotation for a module type. The <code>typing</code> module does not contain one and I could not find an answer elsewhere.</p>
</div>
<div class="post-text" itemprop="text">
<p>You're looking for <a href="https://docs.python.org/3/library/types.html#types.ModuleType" rel="noreferrer"><code>types.ModuleType</code></a>.</p>
</div>
