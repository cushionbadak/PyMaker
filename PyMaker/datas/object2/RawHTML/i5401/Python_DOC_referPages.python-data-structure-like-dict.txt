<div class="post-text" itemprop="text">
<p>Online I found some code that used the variable <code>explored</code>. 
This variable is implemented like a dictionary: <code>explored = {rootId}</code>, but it doesn't have key-value association, moreover this variabile uses the function <code>.add(elem)</code> while the dict doesn't have this function.
eg. <code>explored.add(elem)</code>.</p>
<p>Which data structure is that? (I'm using python 3.6.3)</p>
</div>
<div class="post-text" itemprop="text">
<p>It's a <a href="https://docs.python.org/3/tutorial/datastructures.html#sets" rel="nofollow noreferrer"><code>set</code></a>. There are also online <a href="https://www.programiz.com/python-programming/set#change" rel="nofollow noreferrer">examples</a> that use the methods you mentioned.</p>
<p>Next time you wonder, I suggest you just print the type, like this:</p>
<pre><code>print(type(explored));
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>Might be a set, it has a method "add", but you could simply print the type, such as:</p>
<pre><code>print(type(explored))
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>It's the <code>set</code> datastructure.
Here's a link to it on the official docs: <a href="https://docs.python.org/3/tutorial/datastructures.html#sets" rel="nofollow noreferrer">https://docs.python.org/3/tutorial/datastructures.html#sets</a></p>
</div>
<span class="comment-copy">Could you share the code that you found online?</span>
<span class="comment-copy">print(type(THE_UNKNOWN THINGY))</span>
<span class="comment-copy"><a href="https://docs.python.org/3.5/tutorial/datastructures.html#sets" rel="nofollow noreferrer">You mean a <code>set</code>?</a></span>
<span class="comment-copy">Looks like a <code>set</code>.</span>
