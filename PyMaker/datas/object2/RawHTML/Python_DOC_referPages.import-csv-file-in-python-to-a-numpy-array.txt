<div class="post-text" itemprop="text">
<p>I'm trying to import some values from a csv-file to a numpy array in python. 
So far I've read the CSV-file with pandas but I can't succeed with creating a numpy array with the values from the csv columns.</p>
</div>
<div class="post-text" itemprop="text">
<p>Just found the answer. Just had to use DataFrame.values</p>
</div>
<span class="comment-copy">Do you have some code? What have you tried? Have you searched? <a href="https://stackoverflow.com/a/3519314/4146962">stackoverflow.com/a/3519314/4146962</a></span>
<span class="comment-copy">Dataframe <code>.values</code> or <code>to_numpy()</code> is the usual way.</span>
<span class="comment-copy"><code>.values</code> not <code>.values()</code>..  It's a property that returns an array, not a method.</span>
