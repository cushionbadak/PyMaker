<div class="post-text" itemprop="text">
<div class="question-status question-originals-of-duplicate">
<p>This question already has an answer here:</p>
<ul>
<li>
<a dir="ltr" href="/questions/8899905/count-number-of-occurrences-of-a-given-substring-in-a-string">Count number of occurrences of a given substring in a string</a>
<span class="question-originals-answer-count">
                    22 answers
                </span>
</li>
</ul>
</div>
<p>I have a string "ABChi hi" and I would like to write a program to determine the number of times the substring "hi" occurs in the main string. So far, my code looks like this:</p>
<pre><code>    the_word = "ABChi hi"
    new_word = the_word.split()
    count = 0
    for i in new_word:
        if i == "hi":
            count += 1
</code></pre>
<p>The problem is that when I split the string, I get </p>
<pre><code>  ["ABChi", "hi"]
</code></pre>
<p>which will only help me count one of the two hi's that occur, although the correct answer is 2. Does anyone have any ideas on how to obtain the correct answer?</p>
</div>
<div class="post-text" itemprop="text">
<p>eh, <a href="https://docs.python.org/3/library/stdtypes.html#str.count" rel="nofollow noreferrer">count</a>...</p>
<pre><code>&gt;&gt;&gt; "ABChi hi".count("hi")
2
&gt;&gt;&gt; 
</code></pre>
</div>
<span class="comment-copy"><code>the_word.count("hi")</code></span>
<span class="comment-copy">You can do: <code>"ABChi hi".count("hi")</code> ..</span>
