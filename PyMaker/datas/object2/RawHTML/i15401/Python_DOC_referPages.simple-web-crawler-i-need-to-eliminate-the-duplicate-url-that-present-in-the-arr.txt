<div class="post-text" itemprop="text">
<p>i am using the array to store the URL  and i need to eliminate the URL the are present more than once in array because i don't need to crawl the same URL again:</p>
<pre><code>self.level = []  # array where the URL are present 
for link in self.soup.find_all('a'):
    self.level.append(link.get('href'))
    print(self.level)
</code></pre>
<p>i need to eliminate duplicate URL before crawling this URL.</p>
</div>
<div class="post-text" itemprop="text">
<p>Maintain a <a href="https://docs.python.org/2/library/sets.html"><code>set</code></a> of urls:</p>
<pre><code>self.level = set()
for link in self.soup.find_all('a'):
    self.level.add(link.get('href'))
</code></pre>
</div>
<span class="comment-copy">can u explain this]</span>
<span class="comment-copy">@mans it is just that, by definition, a set is a collection of unique elements.</span>
<span class="comment-copy">can you explain set() method what it is ..?</span>
<span class="comment-copy">@mans it is a way of initializing an empty set, see <a href="http://stackoverflow.com/questions/6130374/empty-set-literal-in-python" title="empty set literal in python">stackoverflow.com/questions/6130374/empty-set-literal-in-python</a>.</span>
<span class="comment-copy">@mans: Please see the Python docs about sets: <a href="https://docs.python.org/3/tutorial/datastructures.html#sets" rel="nofollow noreferrer">docs.python.org/3/tutorial/datastructures.html#sets</a> and <a href="https://docs.python.org/3/library/stdtypes.html#set-types-set-frozenset" rel="nofollow noreferrer">docs.python.org/3/library/stdtypes.html#set-types-set-frozenset</a> (thanks to P_O_I_S_O_N for suggesting the 1st link).</span>
