<div class="post-text" itemprop="text">
<p>I need to read all images from a folder <code>test_image</code>, to do so, all the images have to have same prefixes in name. </p>
<pre><code>PATH_TO_TEST_IMAGES_DIR = 'test_images'
TEST_IMAGE_PATHS = [ os.path.join(PATH_TO_TEST_IMAGES_DIR, 'image{}.jpg'.format(i)) for i in range(1, 21) ]
</code></pre>
<p>I want to choose another folder. How can I do this without changing all the names into that format?</p>
</div>
<div class="post-text" itemprop="text">
<pre><code>import glob, os

PATH_TO_TEST_IMAGES_DIR = 'dir'
searchstr = os.path.join(PATH_TO_TEST_IMAGES_DIR, '*.jpg')
list_of_images = glob.glob(searchstr)
</code></pre>
<p>using glob, assuming that you're just looking for all the .jpg files.</p>
</div>
<span class="comment-copy">You may use <code>os.listdir()</code>  to get the list of all the file names in a given directory.</span>
<span class="comment-copy">Use <a href="https://docs.python.org/3/library/glob.html" rel="nofollow noreferrer"><code>glob</code></a></span>
