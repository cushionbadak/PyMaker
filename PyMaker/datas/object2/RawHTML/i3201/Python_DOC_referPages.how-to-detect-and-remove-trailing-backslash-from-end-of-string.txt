<div class="post-text" itemprop="text">
<p>An entry of any file has a trailing backslash</p>
<pre><code>cache[path] = file.read()
for file_name, multi_line_content in cache.items():
   for line in multi_line_content.splitlines():
      // this line may or maynot have trailing backslash
</code></pre>
<p>Ex:</p>
<p><code>dsf  dff  dfdf \</code></p>
<p><code>cvcs dsd sdds\</code></p>
<hr/>
<p>How to process this line, detect trailing backslash and remove it, before the copying the complete line as string?</p>
</div>
<div class="post-text" itemprop="text">
<p><a href="https://docs.python.org/3/library/stdtypes.html#str.rstrip" rel="nofollow noreferrer"><code>rstrip</code></a> is just what you need:</p>
<pre><code>s = s.rstrip('\\')
</code></pre>
</div>
<span class="comment-copy">Is this a text file you are reading from a python program, or a line in the python?</span>
<span class="comment-copy">@MartinBonner  This is a multiline string <code>cache[path] = file.read()</code></span>
<span class="comment-copy">That will remove <i>all</i> trailing backslashes (which may or may not be what the OP wants).</span>
<span class="comment-copy">Removing looks obvious.. detecting is the problem</span>
<span class="comment-copy">@overexchange they aren't separate problems. <code>rstrip</code> will remove trailing backslahes if they exist. If they don't, the string will be returned unchanged.</span>
<span class="comment-copy">Are you saying that <code>for line in multi_line_content.splitlines():</code> is safe?</span>
<span class="comment-copy">@overexchange should be, yes.</span>
