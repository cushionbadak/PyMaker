<div class="post-text" itemprop="text">
<p>So I have this inventory dict and an empty items list that I want to fill it with the keys in the inventory dict. Any clues on how do I fill the list?</p>
<pre><code># Inventory Dict
inventory = {}
inventory["item a"] = 12
inventory["item b"] = 10
inventory["item x"] = 25
print(inventory)

# Item List
items = []
</code></pre>
</div>
<div class="post-text" itemprop="text">
<pre><code>items = list(inventory.keys())
</code></pre>
<p>or in a loop (as requested)</p>
<pre><code>for key in inventory:
    items.append(key)
</code></pre>
<p>though as is there's no need for the loop. Perhaps there would be in another situation</p>
</div>
<span class="comment-copy"><code>items = list(inventory.keys())</code></span>
<span class="comment-copy">have a read of <a href="https://docs.python.org/3/library/stdtypes.html#typesmapping" rel="nofollow noreferrer">docs.python.org/3/library/stdtypes.html#typesmapping</a></span>
<span class="comment-copy">How if I want to in a loop that fills the list with dict's keys?</span>
<span class="comment-copy">probably duplicate of <a href="https://stackoverflow.com/questions/3294889/iterating-over-dictionaries-using-for-loops/3294899#3294899" title="iterating over dictionaries using for loops">stackoverflow.com/questions/3294889/…</a></span>
<span class="comment-copy">Possible duplicate of <a href="https://stackoverflow.com/questions/3294889/iterating-over-dictionaries-using-for-loops">Iterating over dictionaries using 'for' loops</a></span>
