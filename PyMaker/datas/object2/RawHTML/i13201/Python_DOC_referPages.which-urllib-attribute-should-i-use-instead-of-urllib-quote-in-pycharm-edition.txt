<div class="post-text" itemprop="text">
<p>I used this in pycharm 4.5.4:</p>
<pre><code>url_base += urllib.quote()
</code></pre>
<p>It gave an error:</p>
<blockquote>
<p>AttributeError: module 'urllib' has no attribute 'quote'.</p>
</blockquote>
<p>Which attribute should I use here?</p>
</div>
<div class="post-text" itemprop="text">
<p>I take it you are using Python 3, so you should change it to <code>urllib.parse.quote</code></p>
<p>Here is a  <a href="https://docs.python.org/3/library/urllib.parse.html#urllib.parse.quote" rel="nofollow">link to the documentation</a></p>
</div>
<span class="comment-copy">pycharm is irrelevant, what version of python you are using is.</span>
