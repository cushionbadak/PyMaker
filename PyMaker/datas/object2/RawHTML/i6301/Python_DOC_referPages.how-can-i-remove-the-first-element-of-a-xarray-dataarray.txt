<div class="post-text" itemprop="text">
<p>I have a xarray.DataArray:</p>
<pre><code>array([  5.437614e+01,   2.090367e+02,   3.687384e+02, ...,   5.121467e-02,
         5.116013e-02,   5.110552e-02])
Coordinates:
  * time     (time) datetime64[ns] 1999-12-01 2010-06-01T06:00:00 ...
</code></pre>
<p>I found several answers for lists, but I am not able to do it for an array.</p>
<h1>Tried</h1>
<pre><code>a1.pop[0]

AttributeError: 'DataArray' object has no attribute 'pop'
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>try <code>a1 = a1[1:]</code> because that should work perfectly</p>
</div>
<div class="post-text" itemprop="text">
<p>Try other brackets (the round ones):</p>
<pre><code>a1.pop(0)
</code></pre>
<p>Edit: I found the second answer very useful, but it's about list since there are no arrays in Python?</p>
<p><a href="https://stackoverflow.com/questions/9754729/remove-object-from-a-list-of-objects-in-python?answertab=votes#tab-top">Remove object from a list of objects in python</a></p>
</div>
<span class="comment-copy">This kind of array? <a href="https://docs.python.org/3/library/array.html" rel="nofollow noreferrer">docs.python.org/3/library/array.html</a> If so: <a href="https://docs.python.org/3/library/array.html#array.array.pop" rel="nofollow noreferrer">docs.python.org/3/library/array.html#array.array.pop</a></span>
<span class="comment-copy">@FelixKling An xarray.DataArray to be more precise.</span>
<span class="comment-copy">I had tried it. Please see edit.</span>
<span class="comment-copy">Anyway, it should have been <code>a1.pop(0)</code>. I suspect <code>a1 = a1[1:]</code> will work.</span>
<span class="comment-copy">Please provide the <i>full</i> <a href="http://stackoverflow.com/help/mcve">Minimal, complete, verifiable example</a>.  We should be able to paste your posted code into a text file and reproduce the problem you described.</span>
<span class="comment-copy">Tried with python3 and xarray.DataArray and indeed <b>it does work</b></span>
<span class="comment-copy">That won't work either. <code>pop</code> doesn't exist: <code>AttributeError: 'DataArray' object has no attribute 'pop'</code></span>
<span class="comment-copy">Have you tried using a normal list?</span>
<span class="comment-copy">I'm not the person who asked the question, so no. I just pointed out that your answer cannot work.</span>
<span class="comment-copy">@Chipsvernichter the question isn't <i>about <code>list</code> objects</i>, it is about <code>xarray.DataArray</code> objects.</span>
