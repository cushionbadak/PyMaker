<div class="post-text" itemprop="text">
<p>I have some tuples from an sql query that are a list of album names. However, they output in unicode giving it u' before each name, which I would like to remove. It prints out like this: </p>
<pre><code>((u'test',), (u'album test',), (u'test!',), (u'',), (u'album1',), (u'album2',), (u'album3',), (u'testalbum',))
</code></pre>
<p>but i'm looking for just the names such as: test, test, album1, album2 etc. </p>
<p>I've tried using a for loop to encode each album name indivudally, but then i get: </p>
<pre><code>AttributeError: 'tuple' object has no attribute 'encode'
</code></pre>
<p>Any suggestions? 
thank you for your feedback! </p>
</div>
<div class="post-text" itemprop="text">
<p>Looks like you are calling <code>encode()</code> on the tuple. You really should be calling <code>encode()</code> on the (Unicode) string, which is the first element of the tuple.</p>
<hr/>
<p>For example,</p>
<pre><code>&gt;&gt;&gt; t = (u'hello', u'world')
&gt;&gt;&gt; t.encode()
Traceback (most recent call last):
  File "&lt;stdin&gt;", line 1, in &lt;module&gt;
AttributeError: 'tuple' object has no attribute 'encode'
&gt;&gt;&gt; t[0].encode()
'hello'
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>If you don't want the default output of tuples and Unicode strings, print the strings:</p>
<pre><code>&gt;&gt;&gt; t=(u'test',),(u'album test',),(u'test!',),(u'',),(u'album1',),(u'album2',),(u'album3',),(u'testalbum',)
&gt;&gt;&gt; for item in t:
...   print item[0]
...
test
album test
test!

album1
album2
album3
testalbum
</code></pre>
</div>
<span class="comment-copy">The tuples look weird. Why are their second elements missing? A string like <code>u'something'</code> is known as a unicode string. The rules for converting a Unicode string into the ASCII encoding, for example, are simple; for each code point: If the code point is &lt; 128, each byte is the same as the value of the code point. If the code point is 128 or greater, the Unicode string can't be represented in this encoding. <a href="https://docs.python.org/3/howto/unicode.html" rel="nofollow noreferrer">docs.python.org/3/howto/unicode.html</a></span>
<span class="comment-copy">Also, please provide your code.</span>
<span class="comment-copy">Also don't post images.  Hard to cut-n-paste to formulate an answer.</span>
<span class="comment-copy">@sam <code>(u'test')</code> isn't a tuple; it is a string with parentheses around it.  <code>(u'test',)</code> is a one-element tuple.</span>
<span class="comment-copy">@sam Also FYI, parentheses for the tuple aren't required either, but they are shown for display.  <code>t = u'test',</code> is also a one-element tuple.</span>
