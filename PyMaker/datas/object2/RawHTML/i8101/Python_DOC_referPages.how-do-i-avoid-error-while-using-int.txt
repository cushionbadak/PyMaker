<div class="post-text" itemprop="text">
<p>I have a question concerning <code>int()</code>. Part of my Python codes looks like this</p>
<pre><code>string = input('Enter your number:')
n = int(string)
print n
</code></pre>
<p>So if the input string of <code>int()</code> is not a number, Python will report ValueError and stop running the remaining codes.</p>
<p>I wonder if there is a way to make the program re-ask for a valid string? So that the program won't just stop there.</p>
<p>Thanks!</p>
</div>
<div class="post-text" itemprop="text">
<p>What you're looking for is<h1>Try / Except</h1></p>
<p><strong>How it works:</strong></p>
<pre><code>try:
    # Code to "try".
except:
    # If there's an error, trap the exception and continue.
    continue
</code></pre>
<p><strong>For your scenario:</strong></p>
<pre><code>def GetInput():
    try:
        string = input('Enter your number:') 
        n = int(string) 
        print n
    except:
        # Try to get input again.
        GetInput()
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>You can use <code>try except</code></p>
<pre><code>while True:
    try:
        string = input('Enter your number:')
        n = int(string)
        print n
        break
    except ValueError:
        pass
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>Put the whole thing in an infinite loop. You should catch and ignore <code>ValueError</code>s but break out of the loop when you get a valid integer. </p>
</div>
<div class="post-text" itemprop="text">
<pre><code>n = None
while not isinstance(n, int):
    try:
        n = int(input('Enter your number:'))
    except:
        print('NAN')
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>While the others have mentioned that you can use the following method,</p>
<pre><code>try :
except :
</code></pre>
<p>This is another way to do the same thing.  </p>
<pre><code>while True : 
    string = input('Enter your number:')
    if string.isdigit() : 
        n = int(string)
        print n
        break
     else : 
        print("You have not entered a valid number. Re-enter the number")
</code></pre>
<p>You can learn more about 
  <a href="https://docs.python.org/3/library/stdtypes.html" rel="nofollow noreferrer">Built-in String Functions</a> from here. </p>
</div>
<span class="comment-copy">Possible duplicate:  <a href="http://stackoverflow.com/questions/5424716/how-to-check-if-string-input-is-a-number" title="how to check if string input is a number">stackoverflow.com/questions/5424716/…</a></span>
<span class="comment-copy">Possible duplicate of <a href="http://stackoverflow.com/questions/5424716/how-to-check-if-string-input-is-a-number">How to check if string input is a number?</a></span>
<span class="comment-copy">This <code>GetInput</code> function does not return anything useful.</span>
<span class="comment-copy">You are missing a <code>break</code> somewhere :p</span>
<span class="comment-copy">You are missing a <code>:</code> too. Also, you should try to avoid infinite while loops and give them a clever condition instead.</span>
<span class="comment-copy">Updated. Thank you both :)</span>
<span class="comment-copy">You are catching a very broad exception. Somehow input console is corrupted that will stuck in a infinite loop.</span>
