<div class="post-text" itemprop="text">
<div class="question-status question-originals-of-duplicate">
<p>This question already has an answer here:</p>
<ul>
<li>
<a dir="ltr" href="/questions/8986179/python-the-hard-way-exercise-6-r-versus-s">Python the Hard Way - exercise 6 - %r versus %s</a>
<span class="question-originals-answer-count">
                    7 answers
                </span>
</li>
</ul>
</div>
<p>Well, i just always use %r on python, but I don't know when I have to use these other formats...</p>
</div>
<div class="post-text" itemprop="text">
<p>This is explained in <a href="http://docs.python.org/3/library/stdtypes.html#old-string-formatting" rel="noreferrer">the Python documentation</a>.  In short,</p>
<ul>
<li><code>%d</code> will format a number for display.</li>
<li><code>%s</code> will insert the presentation string representation of the object (i.e. <code>str(o)</code>)</li>
<li><code>%r</code> will insert the canonical string representation of the object (i.e. <code>repr(o)</code>)</li>
</ul>
<p>If you are formatting an integer, then these are equivalent.  For most objects this is not the case.</p>
</div>
<div class="post-text" itemprop="text">
<p>Here is an example to supplement James Henstridge's answer:</p>
<pre><code>class Cheese(float):
    def __str__(self):
        return 'Muenster'
    def __repr__(self):
        return 'Stilton'

chunk = Cheese(-123.4)

print(str(chunk))
# Muenster
print(repr(chunk))
# Stilton
print(int(chunk))
# -123
print('%s\t%r\t%d'%(chunk, chunk, chunk))
# Muenster  Stilton -123
</code></pre>
</div>
<span class="comment-copy"><a href="http://docs.python.org/2/library/string.html#format-examples" rel="nofollow noreferrer">docs.python.org/2/library/string.html#format-examples</a></span>
<span class="comment-copy">That certainly exists on the Internet somewhere</span>
<span class="comment-copy"><a href="http://stackoverflow.com/questions/8986179/python-the-hard-way-exercise-6-r-versus-s" title="python the hard way exercise 6 r versus s">stackoverflow.com/questions/8986179/…</a></span>
<span class="comment-copy">Well, at least <code>str.format</code> is marginally clearer about these.</span>
