<div class="post-text" itemprop="text">
<p>It can be done with termios under Unix, but not under Windows.</p>
<p>I do it easily in perl with TERM::Readkey, or in Windows Batch files.</p>
<p>If an answer has been given here, I have been unable to find it.</p>
<p>I find it hard to believe that python omits such a basic operation available in essentially all other languages.</p>
</div>
<div class="post-text" itemprop="text">
<p>The answer above returns a byte, not a character, so it needs to be decoded.</p>
<p>Here is a solution that returns a single character without having to hit Enter. It works in python 3.4, on Windows</p>
<pre><code>def getChar():
    import msvcrt 
    return msvcrt.getch().decode('utf-8')
</code></pre>
</div>
<span class="comment-copy">You mean like: <a href="https://stackoverflow.com/questions/510357/python-read-a-single-character-from-the-user" title="python read a single character from the user">stackoverflow.com/questions/510357/…</a></span>
<span class="comment-copy">Possible duplicate of <a href="https://stackoverflow.com/questions/510357/python-read-a-single-character-from-the-user">Python read a single character from the user</a></span>
<span class="comment-copy"><a href="https://docs.python.org/3/library/msvcrt.html#msvcrt.getch" rel="nofollow noreferrer"><code>msvcrt.getch()</code></a> works fine for me.  Python 3.5.2, Windows 10.</span>
