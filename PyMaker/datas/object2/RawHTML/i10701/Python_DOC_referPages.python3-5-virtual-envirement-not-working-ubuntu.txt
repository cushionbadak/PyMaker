<div class="post-text" itemprop="text">
<p>I've installed Python 3.5 on my VPS Ubuntu.</p>
<p>Command:</p>
<pre><code>python3.5 --version
</code></pre>
<p>Gives:</p>
<pre><code>3.5.0+
</code></pre>
<p>and then I install simple Flask application and install virtual environment and activate it:</p>
<pre><code>virtualenv -p python3.5 envname
source envname/bin/activate 
</code></pre>
<p>But if I print Python version, it returns 3.4.3</p>
<pre><code>from flask import Flask
import sys
app = Flask(__name__)
@app.route("/")
def hello():
    return sys.version
if __name__ == "__main__":
    app.run()
</code></pre>
<p>This part: </p>
<pre><code>sys.executable
</code></pre>
<p>Returns:</p>
<pre><code>/usr/bin/python3
</code></pre>
<p>Not 3.5.</p>
</div>
<div class="post-text" itemprop="text">
<p>Kind of workaround, but the simplest solution that comes to my mind:</p>
<pre><code>cp envname/bin/python3.5 envname/bin/python
</code></pre>
<p>That way even if Python scripts are executed with <code>python</code> command it will use Python 3.5.</p>
</div>
<span class="comment-copy">You create an environment called <code>envname</code> and activate one called <code>venv</code>.</span>
<span class="comment-copy">im recreate virtualenv, but version still 3.4.3+ (default, Oct 14 2015, 16:03:50) [GCC 5.2.1 20151010]</span>
<span class="comment-copy">im run  virtualenv -p python3.5 envname and run source envname/bin/activate</span>
<span class="comment-copy">but version still 3.4.3+ (default, Oct 14 2015, 16:03:50) [GCC 5.2.1 20151010]</span>
<span class="comment-copy">When using Python &gt;= 3.3 you don't need virtualenv module anymore and actually should use <a href="https://docs.python.org/3/library/venv.html" rel="nofollow noreferrer">pyvenv</a></span>
<span class="comment-copy">where i can run this?</span>
<span class="comment-copy">In the terminal obviously. Just like you have ran <code>source envname/bin/activate</code> to activate your virtualenv.</span>
