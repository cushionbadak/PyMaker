<div class="post-text" itemprop="text">
<div class="question-status question-originals-of-duplicate">
<p>This question already has an answer here:</p>
<ul>
<li>
<a dir="ltr" href="/questions/23294658/asking-the-user-for-input-until-they-give-a-valid-response">Asking the user for input until they give a valid response</a>
<span class="question-originals-answer-count">
                    17 answers
                </span>
</li>
</ul>
</div>
<p>How to code a loop that asked user to input several student name and print out the name. </p>
<p>1st the use is requested to input how many student name to enter and it loops the number enter asking for the student name. </p>
<p>Please see the code below. My challenge is to include in the loop.</p>
<pre><code>n = eval(input("How many student do you have to enter: "))
a = (input("Enter the student name: "))

print (a)
</code></pre>
</div>
<div class="post-text" itemprop="text">
<pre><code>students = []
n = int(input('How many student do you have to enter:'))
for x in range(n):
    name = input('Enter the student name:')
    dob = input('Enter the student dob:')
    students.append({'name': name, 'dob': dob})
print('\n'.join(['name: {0}\ndob: {1}'.format(x.get('name'), x.get('dob')) for x in students]))
</code></pre>
</div>
<span class="comment-copy"><a href="https://docs.python.org/3/tutorial/controlflow.html#for-statements" rel="nofollow noreferrer">docs.python.org/3/tutorial/controlflow.html#for-statements</a></span>
<span class="comment-copy">Why not use a <code>for</code> loop? Manually incrementing a counter in a <code>while</code> is more likely to attract typos or off-by-one errors.</span>
<span class="comment-copy">Good idea :) updated</span>
<span class="comment-copy">how do you add another DOB input?</span>
<span class="comment-copy">updated my answer</span>
<span class="comment-copy">This is great! thank you!</span>
