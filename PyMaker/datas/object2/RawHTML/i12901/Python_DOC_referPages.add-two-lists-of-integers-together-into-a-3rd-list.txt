<div class="post-text" itemprop="text">
<p>for example</p>
<pre><code>list1=[1,2,3,4,5]
list2=[1,2,3,4,5]
..
list3=[2,4,6,8,10]
</code></pre>
<p>I've already tried something, but it pops me a message that says: out of range". here is my code:</p>
<pre><code>for i in range(mikos):
    lista3[i]=lista1[i]+lista2[i]

print(lista3)

#'mikos' is the number of elements in each list
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>One way is to do:</p>
<pre><code>from operator import add
map(add, list1, list2)
</code></pre>
<p>Another would be:</p>
<pre><code>[x + y for x, y in zip(list1, list2)]
</code></pre>
<p>And yet another one is </p>
<pre><code>[sum(x) for x in zip(list1, list2)]
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>You can do this without a loop:</p>
<pre><code>lista3 = [x+y for x,y in zip(lista1, lista2)]
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>Very simple approach is:</p>
<pre><code>list1=[1,2,3,4,5]
list2=[1,2,3,4,5]    
list3 = []    
mikos = len(list1)

for i in range(mikos):
    list3.append(list1[i]+list2[i])

print(list3)
</code></pre>
</div>
<span class="comment-copy">How have you defined lista3 before the <code>for</code> loop?  It seems like it probably doesn't have 5 elements.</span>
<span class="comment-copy">Might want to use <code>izip_longest()</code> with a <code>fillvalue=0</code> argument to handle lists of differing lengths.</span>
<span class="comment-copy"><code>mikos</code> means something like <code>length</code> in English.. for those who wonder as I did.</span>
<span class="comment-copy">Why the downvote?  I think that the OP looks rather for a simple solution than a comprehension expression that is very nice but somewhat harder to use for a beginner. (<a href="https://docs.python.org/3/reference/expressions.html#displays-for-lists-sets-and-dictionaries" rel="nofollow noreferrer">docs.python.org/3/reference/…</a>)</span>
