<div class="post-text" itemprop="text">
<p>I'm making a turn based game in Python. At one point, I need to generate a random number out of a hundred and see if it is in-between two numbers. It needs to be in the if statement. </p>
<pre><code>elif random.randint(0,100) #is equal a number between 20 and 60
</code></pre>
<p>Is there a way to do this?</p>
</div>
<div class="post-text" itemprop="text">
<p><code>if low &lt; random.randint(0,100) &lt; high</code> does the job well, unless you want to store the random value in a variable</p>
</div>
<div class="post-text" itemprop="text">
<p>You can do </p>
<pre><code>elif random.randint(0,100) in range(20, 61)
</code></pre>
</div>
<span class="comment-copy">elif 20 &lt; random.randint(0, 100) &lt; 60:</span>
<span class="comment-copy">Really inefficient way to write <code>20 &lt;= random.randint(0,100) &lt;= 60</code>....</span>
<span class="comment-copy">True. Should I delete the answer?</span>
<span class="comment-copy">but <code>range</code> in python3 is a lazy expression right?</span>
<span class="comment-copy">Actually, from <a href="https://docs.python.org/3/library/stdtypes.html#ranges" rel="nofollow noreferrer">docs.python.org/3/library/stdtypes.html#ranges</a>: "Changed in version 3.2: Implement the Sequence ABC. Support slicing and negative indices. Test int objects for membership in constant time instead of iterating through all items.". So it's good.</span>
<span class="comment-copy">@CătălinMatei depends on the Python version... so you may want to at least mention that your solution is inefficient on Python &lt; 3.2 ;)</span>
