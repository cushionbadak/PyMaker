<div class="post-text" itemprop="text">
<p>I have a list consisting of tuples like the following:</p>
<pre><code>m = [('a', 'b', 'c', 'd'), (1, 2, 3, 4), ('alpha', 'beta', 'gamma', 'eta')]
</code></pre>
<p>how can i get the following output:</p>
<pre><code>['c', 3, 'gamma']
</code></pre>
<p>need only for 'c' and not for rest of the elements a,b,d</p>
</div>
<div class="post-text" itemprop="text">
<p>Variable 'm' contains list of tuples, there are 3 tuples considered as 3 items in list. </p>
<p>Variable 'n' takes each element (tuple) in the list and prints its value according to its index number.</p>
<pre><code>m = [('a', 'b', 'c', 'd'), (1, 2, 3, 4), ('alpha', 'beta', 'gamma', 'eta')]
for n in m:
    print (n[2])
</code></pre>
</div>
<div class="post-text" itemprop="text">
<pre><code>m=[(1,2),('a','b')]
for t in m:
    print n[1]
</code></pre>
</div>
<span class="comment-copy">You can transpose and index with <code>zip</code>: <code>list(zip(*m))[2]</code></span>
<span class="comment-copy">A bit more verbose: <code>[elm[2] for elm in m]</code>.</span>
<span class="comment-copy">Thank you. Didnt know about zip function in .py</span>
<span class="comment-copy">@f4l Read more <a href="https://docs.python.org/3/library/functions.html#zip" rel="nofollow noreferrer">here</a></span>
<span class="comment-copy">Could you add some commentary to explain your code and how it answers the question.</span>
<span class="comment-copy">variable 'm' contains list of tuples,there are 3 tuples considered as 3 items in list. variable 'n' takes each element (tuple) in the list and prints its value according to its index number.</span>
<span class="comment-copy">Where is <code>n</code> defined?</span>
