<div class="post-text" itemprop="text">
<pre><code>import random
r1 = (random.randint(0,9))
r2 = (random.randint(0,9))
r3 = (random.randint(0,9))
r4 = (random.randint(0,9))
r5 = (random.randint(0,9))
r6 = (random.randint(0,9))
r7 = (random.randint(0,9))
print ("your item barcode number is", r1,r2,r3,r4,r5,r6,r7)
r8 = (r1*3+r2*1+r3*3+r4*1+r5*3+r6*1+r7*3)
roundup = round(r8, -1)
print (r8)
print(roundup)
  GTIN = (roundup-r8)
if GTIN&lt;0:
GTIN = (r8-roundup) 
print("the GTIN number is", GTIN)
print(r1,r2,r3,r4,r5,r6,r7,GTIN)
</code></pre>
<p>i can't get it to round up to the higher ten somebody help so basically i want it to be like 44 rounds up to 50 unless the number is a mutiple of 10 somebody help me do it</p>
</div>
<div class="post-text" itemprop="text">
<pre><code>def round_up_by_ten(num):
    return num if not num%10 else ((num//10)+1)*10
</code></pre>
</div>
<span class="comment-copy">where do i put that in my code and what do i get rid of</span>
<span class="comment-copy">This is a function, like <code>round</code>.  You would put this at the top of your code, under the <code>import</code>s.  Instead of calling <code>round(r8)</code> call <code>round_up_by_ten(r8)</code>.  If you're a little lost, I suggest the official python tutorial: <a href="https://docs.python.org/3/tutorial/index.html" rel="nofollow noreferrer">docs.python.org/3/tutorial/index.html</a></span>
<span class="comment-copy">thank you very much it worked</span>
