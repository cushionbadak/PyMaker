<div class="post-text" itemprop="text">
<p>Could you help me understand why <code>print(truth(prog.match(text, 0, 6)))</code> equals true?</p>
<pre><code>import re
from operator import truth

prog = re.compile(r'&lt;HTML&gt;$')
text = "&lt;HTML&gt;            "
print("Last symbol: {}".format(len('&lt;HTML&gt;')-1))
print(truth(prog.match(text, 0, 6)))
print(truth(prog.match(text)))
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>If you use the <code>match(text, startpos, endpos)</code> method of a compiled regex, it will act as if you've passed <code>match(text[startpos:endpos])</code> (well, <a href="https://docs.python.org/3/library/re.html#re.regex.search" rel="nofollow">not exactly</a>, but for the purposes of <code>$</code>, it is). This means that it'll think <code>&lt;HTML&gt;</code> is at the end of the input (which is what <code>$</code> matches).</p>
<p>However, when this is not the case the extra whitespace at the end of <code>text</code> will prevent <code>$</code> from matching, so no match is found.</p>
</div>
<span class="comment-copy">What is <code>truth</code>? Did you mean <code>bool</code>?</span>
<span class="comment-copy"><code>truth</code> is not a built-in: either there is code missing or this does not equal <code>True</code>...</span>
<span class="comment-copy">@brianpck Oops.</span>
<span class="comment-copy">Hope this link will be helped to you <a href="https://www.dotnetperls.com/if-python" rel="nofollow noreferrer">dotnetperls.com/if-python</a></span>
<span class="comment-copy">Edited the code. Pardon for the "truth".</span>
<span class="comment-copy">Hm, it explains the situation well. This feature is not documented, is it?</span>
<span class="comment-copy">@Michael <a href="https://docs.python.org/3/library/re.html#re.regex.match" rel="nofollow noreferrer">It is</a>.</span>
<span class="comment-copy">In the documentation: pos and endpos parameters limit the search region. I would say that it is not written that it works like slicing. By the way, if it worked like slicing, couldn''t we see the same effect with the beginning? text = " &lt;HTML&gt;            " print(truth(prog.match(text, 1)))</span>
<span class="comment-copy">@Michael The documentation covers <code>^</code> as well.</span>
<span class="comment-copy">Have you trued it with r'^&lt;HTML&gt;', text = " &lt;HTML&gt;" and pos=1?</span>
