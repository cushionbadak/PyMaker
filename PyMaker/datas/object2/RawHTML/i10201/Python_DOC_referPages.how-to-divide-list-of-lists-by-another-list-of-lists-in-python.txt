<div class="post-text" itemprop="text">
<p>I have <code>q = [[7,2,3],[4,5,6]]</code> and <code>r=[[6,1,2],[3,4,5]]</code>. I need to divide q by the corresponding elements in r. (i.e. <code>[[7/6,2/1,3/2],[4/3,5/4,6/5]]</code>)</p>
<p>Output needed B = [[1.16,2,1.5],[1.33,1.25,1.2]]</p>
<p>Code:</p>
<p><code>B= [[float(j)/float(i) for j in q] for i in r]</code>. </p>
<p>However, I keep getting an error : TypeError: float() argument must be a string or a number. I have imported division from future. Any suggestions? </p>
</div>
<div class="post-text" itemprop="text">
<p>Use <a href="https://docs.python.org/3/library/functions.html#zip"><em>zip</em></a> for bring together the sublists pairwise and then use it again to bring together the corresponding numerators and denominators:</p>
<pre><code>&gt;&gt;&gt; q = [[7,2,3],[4,5,6]]
&gt;&gt;&gt; r = [[6,1,2],[3,4,5]]
&gt;&gt;&gt; [[n/d for n, d in zip(subq, subr)] for subq, subr in zip(q, r)]
[[1.1666666666666667, 2.0, 1.5], [1.3333333333333333, 1.25, 1.2]]
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>You can do:</p>
<pre><code>&gt;&gt;&gt; out=[]
&gt;&gt;&gt; for s1, s2 in zip(q, r):
...    inner=[]
...    for n, d in zip(s1, s2):
...       inner.append(float(n)/d)
...    out.append(inner)
... 
&gt;&gt;&gt; out
[[1.1666666666666667, 2.0, 1.5], [1.3333333333333333, 1.25, 1.2]]
</code></pre>
<p>Or, use numpy:</p>
<pre><code>&gt;&gt;&gt; q=[[7.,2.,3.],[4.,5.,6.]]
&gt;&gt;&gt; r=[[6.,1.,2.],[3.,4.,5.]]
&gt;&gt;&gt; np.array(q)/np.array(r)
array([[ 1.16666667,  2.        ,  1.5       ],
       [ 1.33333333,  1.25      ,  1.2       ]])
</code></pre>
<p>Or, if you have int literals:</p>
<pre><code>&gt;&gt;&gt; q=[[7,2,3],[4,5,6]]
&gt;&gt;&gt; r=[[6,1,2],[3,4,5]]
&gt;&gt;&gt; np.array(q, dtype=float)/np.array(r)
array([[ 1.16666667,  2.        ,  1.5       ],
       [ 1.33333333,  1.25      ,  1.2       ]])
</code></pre>
</div>
<span class="comment-copy">You need to iterate over the <i>values in the sublists</i>, not the sublists themselves. Right now you're trying to convert lists into floats...</span>
<span class="comment-copy">numpy will do that for you!</span>
<span class="comment-copy">I am not comfortable using numphy, Any suggestions for this code?</span>
<span class="comment-copy">Thanks. How does this work for an integer divided by one element in a list of lists? I tried doing it in this (<a href="http://stackoverflow.com/q/39969884/6820344">stackoverflow.com/q/39969884/6820344</a>) question I posted. Under "Failed Output", you can see that it does not allow one to do this type of division.</span>
<span class="comment-copy">If Python 2, you need <code>[[float(n)/d for n, d in zip(subq, subr)] for subq, subr in zip(q, r)]</code></span>
<span class="comment-copy">For Python 2, add <code>from __future__ import division</code>.</span>
