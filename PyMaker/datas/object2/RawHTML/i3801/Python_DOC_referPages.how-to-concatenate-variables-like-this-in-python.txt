<div class="post-text" itemprop="text">
<p>eg. correct sample -</p>
<pre><code>f.write("\t" +connection_name+ "\t =" + qs.connection_name + "\n")
</code></pre>
<p>what I'm trying to do - </p>
<pre><code>f.write("\t" +current+ "\t=" +qs.current+ "\n")
</code></pre>
<p>//here <code>current = 'connection_name'</code></p>
<p>How to concatenate in this kind - <code>qs.current</code> so that it prints as qs.<code>connection_name</code>'s value</p>
</div>
<div class="post-text" itemprop="text">
<p>You can use <a href="https://docs.python.org/3/library/functions.html#getattr" rel="nofollow noreferrer"><code>getattr</code></a></p>
<pre><code>getattr(qs, current)
</code></pre>
<p>So in the context of your code</p>
<pre><code>f.write("\t" + current + "\t=" + getattr(qs, current) + "\n")
</code></pre>
</div>
<span class="comment-copy">what is the error you are receiving?</span>
<span class="comment-copy">Actually was using it to print the value of object so it was showing - object has no attribute 'current'</span>
