<div class="post-text" itemprop="text">
<p>I am trying to split a string, but keep all separators bundled together in a separate list.</p>
<pre><code>s = "This is a test     for \n a string"
</code></pre>
<p>should results in</p>
<pre><code>a = ["This", "is", "a", "test", "for", "a", "string"]
b = [" ", " ", " ", "     ", " \n ", " "]
</code></pre>
<p>Any idea on how to handle that?</p>
</div>
<div class="post-text" itemprop="text">
<p><a href="https://docs.python.org/3/library/re.html#re.split" rel="nofollow noreferrer"><code>re.split</code></a> is your friend:</p>
<pre><code>split = re.split(r'(\s+)', s)   
x = split[::2]
y = split[1::2]
</code></pre>
<p></p>
<pre><code>&gt;&gt;&gt; x
['This', 'is', 'a', 'test', 'for', 'a', 'string']
&gt;&gt;&gt; y
[' ', ' ', ' ', '     ', ' \n ', ' ']
</code></pre>
</div>
<span class="comment-copy">Downvoters, it isn't <i>that</i> bad a question. The problem statement is clear and the question is answerable.</span>
<span class="comment-copy">How do you define <i>separators</i>? Is it everything that is not a word? How would the result look like if your text string ended with <code>.</code>?</span>
<span class="comment-copy">Possibly duplicate <a href="https://stackoverflow.com/questions/2136556/in-python-how-do-i-split-a-string-and-keep-the-separators/34004805?utm_medium=organic&amp;utm_source=google_rich_qa&amp;utm_campaign=google_rich_qa" title="in python how do i split a string and keep the separators">stackoverflow.com/questions/2136556/…</a></span>
