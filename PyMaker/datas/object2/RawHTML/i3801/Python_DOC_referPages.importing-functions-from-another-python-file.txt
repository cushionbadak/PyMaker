<div class="post-text" itemprop="text">
<p>my_main_file.py:</p>
<pre><code>from bfile import bfunc
def fna():
  bfunc(sth)

if __name__ == "__main__":
  fna()
</code></pre>
<p>bfile.py:</p>
<pre><code>def bfunc(sth):
  #bla bla..
</code></pre>
<p>Error:</p>
<blockquote>
<p>name 'bfunc' is not defined</p>
</blockquote>
<p>Both files are under same directory</p>
<p>PS:
I have tried everythig here <a href="https://stackoverflow.com/questions/20309456/call-a-function-from-another-file-in-python">Call a function from another file in Python</a></p>
</div>
<div class="post-text" itemprop="text">
<p>Add <code>__init__.py</code> python file to your current working directory.So python will treat directory as containing package.You can see documentation <a href="https://docs.python.org/3/tutorial/modules.html#packages" rel="nofollow noreferrer">here</a></p>
</div>
<div class="post-text" itemprop="text">
<p>Try </p>
<pre><code>import bfile
def fna():
    bfile.bfunc(sth)
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>adding <code>export PYTHONPATH="."</code>
to <code>bash_profile</code> solved the issue</p>
</div>
<span class="comment-copy">where that <code>sth</code> come from ?</span>
<span class="comment-copy">Are you running in that directory? Is <code>.</code> in your PYTHONPATH, or alternatively in <code>sys.path</code>?</span>
<span class="comment-copy">created within funa()</span>
<span class="comment-copy">sys.path doesn't contain current directory, PYTHONPATH is blank</span>
<span class="comment-copy">Possible duplicate of <a href="https://stackoverflow.com/questions/14132789/relative-imports-for-the-billionth-time">Relative imports for the billionth time</a></span>
