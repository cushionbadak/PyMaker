<div class="post-text" itemprop="text">
<p>os module contains function <code>chflags</code> that allows user to modify UNIX file flags: <a href="https://docs.python.org/3/library/os.html" rel="nofollow">https://docs.python.org/3/library/os.html</a></p>
<p>Is there a function that would let me see which flags the file currently has?</p>
<p>I am asking about file flags (equivalent of unix command lsattr or chattr) not permissions (chmod etc)</p>
</div>
<div class="post-text" itemprop="text">
<p>File flags are made available via <code>os.stat()</code>.</p>
<p>The POSIX implementation of python's os module returns a <code>stat_result</code> object that has an <code>st_flags</code> field:</p>
<pre><code>import os
st = os.stat(filename)
print(st.st_flags)
</code></pre>
<p>The possible flag bits are available in the <code>stat</code> module as</p>
<pre><code>SF_*
UF_*
</code></pre>
<p>constants.</p>
</div>
<span class="comment-copy">duplicate: <a href="http://stackoverflow.com/questions/1861836/checking-file-permissions-in-linux-with-python" title="checking file permissions in linux with python">stackoverflow.com/questions/1861836/…</a></span>
<span class="comment-copy">No dup, OP is asking for <i>flags</i>, not mode bits.</span>
<span class="comment-copy">I tried this and get <code>AttributeError: 'os.stat_result' object has no attribute 'st_flags'</code></span>
<span class="comment-copy">According to the documentation this attribute may not be available on some Unix systems. Could you please describe your setup?</span>
<span class="comment-copy">It's ubuntu 14.04 LTS, but now I figured out that my os module doesn't even have chflags function :( maybe it's not in my python? I got python 3.4.0</span>
<span class="comment-copy">Linux does not have a <code>chflags()</code> system call. It is simply not supported by the operating system.</span>
<span class="comment-copy">However Linux has a <a href="http://en.wikipedia.org/wiki/Chattr" rel="nofollow noreferrer"><code>chattr</code></a> function and this should/could be available through <code>os.chflags()</code>.</span>
