<div class="post-text" itemprop="text">
<p>I have a list of strings that I want to apply a method (<code>.split</code>). I know this can be done by a <code>for</code> loop but knowing the mentality of python I assume there is a better way, like the <code>map</code> function</p>
<p>Below is the thing I want written using <code>for</code> loop</p>
<pre><code>config = ['a b', 'c d']

configSplit = [None] * len(config)
for x in range(len(config)):
    configSplit[x] = config[x].split()

configSplit
&gt; [['a', 'b'], ['c', 'd']]
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>You can use a simple <a href="https://docs.python.org/3/tutorial/datastructures.html#list-comprehensions" rel="nofollow">list comprehension</a>, like this</p>
<pre><code>&gt;&gt;&gt; config = ['a b','c d']
&gt;&gt;&gt; [item.split() for item in config]
[['a', 'b'], ['c', 'd']]
</code></pre>
<p>If you want to use <a href="https://docs.python.org/3/library/functions.html#map" rel="nofollow"><code>map</code></a>, you can pass <a href="https://docs.python.org/3/library/stdtypes.html#str.split" rel="nofollow"><code>str.split</code></a> function to it. But, Python 3.x's <code>map</code> returns an iterable map object.</p>
<pre><code>&gt;&gt;&gt; map(str.split, config)
&lt;map object at 0x7f9843a64a90&gt;
</code></pre>
<p>So, you need to explicitly convert that to a list, with the <a href="https://docs.python.org/3/library/functions.html#func-list" rel="nofollow"><code>list</code></a> function, like this</p>
<pre><code>&gt;&gt;&gt; list(map(str.split, config))
[['a', 'b'], ['c', 'd']]
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>As an alternative to anwser by @thefourtheye you can use <code>map</code>:</p>
<pre><code>config = ['a b','c d']

new_config = list(map(lambda x: x.split(), config))

print(new_config)
# [['a', 'b'], ['c', 'd']]
</code></pre>
</div>
<div class="post-text" itemprop="text">
<pre><code>config = ['a b','c d']
</code></pre>
<p>Create an empty list</p>
<pre><code>configSplit = list()
</code></pre>
<p>Iterate over each item, split, add to new list.</p>
<pre><code>for item in config:
    configSplit.append(item.split())
</code></pre>
</div>
<span class="comment-copy">Nope, <code>lambda</code> is not necessary. You can directly pass <code>str.split</code> like in my answer.</span>
<span class="comment-copy">@thefourtheye yep, you right. But as more general case, its good to know about lambda if some other operation on the input list is required.</span>
<span class="comment-copy">In Python 2 <code>str.split()</code> is going to be more specific and won't work on unicode strings. Better to use <code>string.split</code>, or use <code>lambda</code> as per this answer.</span>
