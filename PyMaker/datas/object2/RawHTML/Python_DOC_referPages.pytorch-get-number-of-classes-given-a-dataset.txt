<div class="post-text" itemprop="text">
<p>If I have a dataset like:</p>
<pre><code>image_datasets['train'] = datasets.ImageFolder(train_dir, transform=train_transforms)
</code></pre>
<p>How do I determine programatically the number of classes or unique labels in the dataset?</p>
</div>
<div class="post-text" itemprop="text">
<p>Use:</p>
<pre><code>len(image_datasets['train'].classes)
</code></pre>
<p><code>.classes</code> returns a list.</p>
</div>
<span class="comment-copy">Wait, so you asked a question and abruptly answered it yourself?</span>
<span class="comment-copy">@DirtyBit Yes, this is encouraged on meta. There is a "Answer your own question" button after typing the question, so both are posted at once.</span>
