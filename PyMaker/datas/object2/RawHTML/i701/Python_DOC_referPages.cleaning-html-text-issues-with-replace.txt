<div class="post-text" itemprop="text">
<p>I have an editor that ads:</p>
<pre><code>&lt;p&gt;&lt;br&gt;&lt;/p&gt; or empty `p`, and I want to rplace or remove them.
</code></pre>
<p>I use:</p>
<pre><code>  value = value.replace('&lt;p&gt;&lt;br&gt;&lt;/p&gt;', '&lt;br&gt;').replace('&lt;p&gt;&lt;/p&gt;','').strip('&lt;br&gt;')
</code></pre>
<p>The problem is that sometimes remove everything, an in all cases for the first paragraph I always get: <code>p&gt;</code>﻿(removes the first chracter in tag).</p>
</div>
<div class="post-text" itemprop="text">
<p>Your error is in how you use the <a href="https://docs.python.org/3/library/stdtypes.html#str.strip" rel="nofollow noreferrer">strip</a> method, which removes any leading or trailing  sequence of the <code>'&lt;br&gt;'</code> characters. So <code>&lt;b&gt;hello&lt;/b&gt;</code> would be stripped to <code>hello&lt;/</code>, for example.</p>
<p>If you want to remove any <code>&lt;br&gt;</code> in the beginning and in the end of the <code>value</code> string, you can do it like so:</p>
<pre><code>if value.startswith('&lt;br&gt;'):
    value = value[4:]
if value.endswith('&lt;br&gt;'):
    value = value[:-4]
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>Base on you solution, why not just do ?</p>
<pre><code>value = value.replace("&lt;p&gt;", '').replace("&lt;/p&gt;", '')
</code></pre>
<p>Shouldn't that be enough ?<br/>
All <code>&lt;p&gt;</code> and <code>&lt;/p&gt;</code> would get replaced and the rest of string will remain untouched.</p>
<p>For <code>value = "&lt;p&gt;&lt;br&gt;&lt;/p&gt;"</code> you will get <code>"&lt;br&gt;"</code>.<br/>
For <code>value = "&lt;p&gt;&lt;/p&gt;"</code> you will get <code>''</code>.<br/>
For <code>value = "&lt;p&gt;&lt;/p&gt;oueo&lt;p&gt;54&lt;br&gt;65&lt;/p&gt;eoue&lt;p&gt;&lt;/p&gt;"</code> you will get <code>"'oueo54&lt;br&gt;65eoue'"</code>.</p>
</div>
<span class="comment-copy">Have you included everything to reproduce this?</span>
<span class="comment-copy">Because <code>str.strip(str)</code> removes <i>any</i> of the characters in the str, not the precise string given as argument: <code>"asdf".strip("fdsa")</code> will yield an empty string.</span>
<span class="comment-copy">@9769953 I understand, so how can I remove from start and end, keeping the string order</span>
