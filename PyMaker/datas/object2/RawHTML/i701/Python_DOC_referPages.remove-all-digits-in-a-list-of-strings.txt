<div class="post-text" itemprop="text">
<pre><code>words = ['Duration12', 'Noun1', 'Adjective7']
result = [word[:-2] for word in words]
</code></pre>
<p>gives me </p>
<blockquote>
<p>['Duration', 'Nou', 'Adjectiv']</p>
</blockquote>
<p>But I want to get </p>
<blockquote>
<p>['Duration', 'Noun', 'Adjective']</p>
</blockquote>
<p>Is there any library function to implement it or should I explicitly detect the digits available and then remove them?</p>
</div>
<div class="post-text" itemprop="text">
<p>You could use a list comprehension and remove all digits using <code>re.sub</code>:</p>
<pre><code>import re
words = ['Duration12', 'Noun1', 'Adjective7']
[re.sub(r'[0-9]', '', w) for w in words]
</code></pre>
<p><b> Result </b></p>
<pre><code>['Duration', 'Noun', 'Adjective']
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>Try This One:</p>
<pre><code>In [8]: from string import digits

In [9]: remove_digits = str.maketrans('', '', digits)

In [10]: [word.translate(remove_digits) for word in words]
Out[10]: ['Duration', 'Noun', 'Adjective']
</code></pre>
<p>For more info refer to the link: <a href="https://docs.python.org/2/library/string.html" rel="nofollow noreferrer">Link</a></p>
</div>
<div class="post-text" itemprop="text">
<p>You can filter out the numbers, using <a href="https://docs.python.org/3/library/stdtypes.html#str.isalpha" rel="nofollow noreferrer">isalpha</a>:</p>
<pre><code>words = ['Duration12', 'Noun1', 'Adjective7']

result = [''.join(c for c in w if c.isalpha()) for w in words]
print(result)
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>If you are sure all digits are at end of words, you might use <code>.strip</code> following way:</p>
<pre><code>words = ['Duration12', 'Noun1', 'Adjective7']
results = [i.strip('0123456789') for i in words]
print(results) #give ['Duration', 'Noun', 'Adjective']
</code></pre>
<p>but keep in mind that this method would not work if digits are inside rather than at end</p>
</div>
<span class="comment-copy">Will the digits always be at the end of the words?</span>
<span class="comment-copy">@learnerPy Check answer.</span>
<span class="comment-copy">@Olivier Melançon ya. And got the answer already marked as accepted below</span>
<span class="comment-copy">Great to know. From curiosity i am now searching in re if  [ 'Duration[12]', 'Noun[1]', 'Adjective[7]' ] is also possible</span>
<span class="comment-copy">You mean to remove also the brackets?</span>
<span class="comment-copy">no, putting the numbers into brackets</span>
<span class="comment-copy">Use <code>[re.sub(r'[0-9]+', r'[\g&lt;0&gt;]', w) for w in words]</code></span>
<span class="comment-copy">for words = ['Duration12:1', 'Noun', 'Adjective7:8'] i am getting ['Duration[12]:[1]', 'Noun', 'Adjective[7]:[8]'] But like to have ['Duration[12]:1', 'Noun', 'Adjective[7]:8']</span>
