<div class="post-text" itemprop="text">
<p>I have this assignment:</p>
<p>context.options |= ssl.OP_NO_TLSv1_1</p>
<p>taken from ssl python <a href="https://docs.python.org/3/library/ssl.html#ssl.OP_NO_SSLv3" rel="nofollow noreferrer">here</a>. Since the value after the assignemnt, can take different values: </p>
<pre><code>ssl.OP_NO_TLSv1_1
ssl.OP_NO_TLSv1
ssl.OP_NO_SSLv3
</code></pre>
<p>I thought of adding all the values in an array to make my code more readable. So I did the following:</p>
<pre><code>noTLS = ['ssl.OP_NO_SSLv2','ssl.OP_NO_SSLv3','ssl.OP_NO_TLSv1','ssl.OP_NO_TLSv1_1']
</code></pre>
<p>Then:</p>
<pre><code>context.options |= noTLS[3]
</code></pre>
<p>But I got an error: </p>
<pre><code>TypeError: unsupported operand type(s) for |=: 'Options' and 'str'
</code></pre>
<p>Is there any way where I can put these non-string values in one place and call them by reference like an array? </p>
</div>
<div class="post-text" itemprop="text">
<p>Maybe you mean this:</p>
<pre><code>noTLS = [ssl.OP_NO_SSLv2, ssl.OP_NO_SSLv3, ssl.OP_NO_TLSv1, ssl.OP_NO_TLSv1_1]
</code></pre>
<p>why did you convert them to <code>str</code> anyway?</p>
<p>I guess this code </p>
<pre><code>context.options |= noTLS[3] 
</code></pre>
<p>will work then just fine.</p>
</div>
<div class="post-text" itemprop="text">
<p>Instead of putting strings in your <code>noTLS</code> list, you can put in the actual values.</p>
<pre><code>noTLS = [ssl.OP_NO_SSLv2, ssl.OP_NO_SSLv3, ssl.OP_NO_TLSv1, ssl.OP_NO_TLSv1_1]
</code></pre>
<p>Then </p>
<pre><code>context.options |= noTLS[3]
</code></pre>
<p>should work.</p>
</div>
