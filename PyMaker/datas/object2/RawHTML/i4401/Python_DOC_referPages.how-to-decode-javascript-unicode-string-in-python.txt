<div class="post-text" itemprop="text">
<p>I found that it is a character of <code>TWO HEARTS</code> <a href="http://www.charbase.com/1f495-unicode-two-hearts" rel="nofollow noreferrer">here</a>. I tried to decode it:</p>
<pre><code>a = '\ud83d\udc95'
a.encode('utf-8').decode('utf-8')
UnicodeEncodeError: 'utf-8' codec can't encode characters in position 0-1: surrogates not allowed
</code></pre>
<p>How to get the right symbol?</p>
<p>Thank you.</p>
</div>
<div class="post-text" itemprop="text">
<p>Use <a href="https://docs.python.org/3/library/json.html" rel="nofollow noreferrer"><strong>json</strong></a> module to help you solve different <code>Unicode</code>.</p>
<pre><code>import json

a = '\ud83d\udc95'
m = json.dumps({"k": a})
print(json.loads(m)["k"])  # 💕
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>The page you link to tells you the</p>
<blockquote>
<p>Python Escape u'\U0001f495'</p>
</blockquote>
<p>Use it:</p>
<pre><code>&gt;&gt;&gt; u'\U0001f495'
'💕'
</code></pre>
</div>
<span class="comment-copy">Have you tried searching for the error message?</span>
<span class="comment-copy">Your link shows <code>u'\U0001f495'</code> as the Python escape; instead, here you use the Java/JavaScript escape. Why?</span>
<span class="comment-copy">This is my received data.</span>
<span class="comment-copy">Thank you Lutz. However, how to transform from <code>\ud83d\udc95</code> to <code>\U0001f495</code>? How to do the mapping?</span>
