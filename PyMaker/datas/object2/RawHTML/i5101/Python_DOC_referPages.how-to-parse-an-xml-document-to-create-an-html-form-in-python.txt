<div class="post-text" itemprop="text">
<p>I have an XML document like this:</p>
<pre><code>    &lt;section name="Personal_details"&gt;
        &lt;parameter name="Name" value="String" limit="50" multiple_values="no"&gt;
        &lt;parameter name="Phone_no" value="Long" limit="10" multiple_values="yes"&gt;
        &lt;parameter name="Address" value="String" limit="100" multiple="yes"&gt;
    &lt;/section&gt;
    &lt;section name="Academic_details"&gt;
        &lt;parameter name="Graduation_marks" value="Integer" max="100" min="0" multiple_values="no"&gt;
        &lt;parameter name="PostGraduation_marks" value="Integer" max="100" min="0" multiple_values="no"&gt;
    &lt;/section&gt;
</code></pre>
<p>I want to convert this into an HTML form. Is it possible to do this in Python? If yes then how?</p>
</div>
<div class="post-text" itemprop="text">
<p>You can use the module ElementTree that usually comes with Python:</p>
<pre><code>import xml.etree.ElementTree as ET
</code></pre>
<p><a href="https://docs.python.org/3/library/xml.etree.elementtree.html" rel="nofollow noreferrer">https://docs.python.org/3/library/xml.etree.elementtree.html</a></p>
<p>Look for the method <code>fromstring</code>.</p>
</div>
<span class="comment-copy">Of course it's possible, but we don't do the work for you.  Please visit the <a href="https://stackoverflow.com/help">help center</a> and read <a href="https://stackoverflow.com/questions/how-to-ask">How to Ask</a> to learn how to use this site effectively.</span>
