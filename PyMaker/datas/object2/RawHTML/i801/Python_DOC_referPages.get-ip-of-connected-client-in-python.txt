<div class="post-text" itemprop="text">
<p>is there a way to get the IP of the connected client? (And if possible the port it uses). 
I tried <code>client_socket.getsockname()</code> but it gave me my IP address.</p>
</div>
<div class="post-text" itemprop="text">
<p>You have to use the <code>socket.getpeername()</code> method.</p>
</div>
<span class="comment-copy"><a href="https://docs.python.org/3/library/socket.html#socket.socket.getpeername" rel="nofollow noreferrer"><code>getpeername()</code></a></span>
<span class="comment-copy">@spectras thank you!</span>
