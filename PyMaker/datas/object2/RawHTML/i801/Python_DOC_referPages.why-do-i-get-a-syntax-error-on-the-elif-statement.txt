<div class="post-text" itemprop="text">
<p>I'm pretty sure I have indented the code properly and left no parentheses open.</p>
<pre><code>a,b = [int(x) for x in input().split()]
N = int(input())
C = [int(x) for x in input().split()]
for i in range(0,len(C)-1):
    if (a+C[i] == b) or (b + C[i] == a):
        print(C[i])
        break
    elif:
        for j in range(0,len(C)-1):
            if (a+C[i]) == (b+C[j]):
                print(C[j],C[i])
                break
    else:
        print("NOT POSSIBLE")
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>If you're going to use an <code>elif</code> you need a condition just like on your <code>if</code>.  Example:</p>
<pre><code>if a == b:
    do_something()
elif a == c:
    do_something_else()
</code></pre>
</div>
<span class="comment-copy">Your <code>elif</code> has no conditional. <a href="https://docs.python.org/3/tutorial/controlflow.html#if-statements" rel="nofollow noreferrer">docs.python.org/3/tutorial/controlflow.html#if-statements</a></span>
<span class="comment-copy">You need to test for a condition in the elif statement. Elif(something):</span>
<span class="comment-copy">When do you expect your <code>elif</code> to run?</span>
<span class="comment-copy">To explain other comments: <code>elif</code> is a shortcut for <code>else if</code></span>
