<div class="post-text" itemprop="text">
<div class="question-status question-originals-of-duplicate">
<p>This question already has an answer here:</p>
<ul>
<li>
<a dir="ltr" href="/questions/954834/how-do-i-use-raw-input-in-python-3">How do I use raw_input in Python 3</a>
<span class="question-originals-answer-count">
                    8 answers
                </span>
</li>
</ul>
</div>
<p>I'm getting an error while running the code</p>
<pre><code>#script to calculate the BMI of a person

print ("Enter your heights in meters")
height = float(raw_input())
print ("Enter your Weight in Kilograms")
weight = float(raw_input())

BMI = weight/(height*height)

print (BMI)
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p><a href="https://docs.python.org/2/library/functions.html#raw_input" rel="nofollow noreferrer"><code>raw_input</code></a> only exists in Python 2.x. You should use <a href="https://docs.python.org/3/library/functions.html#input" rel="nofollow noreferrer"><code>input()</code></a> in Python 3.</p>
</div>
<span class="comment-copy">What is the error?</span>
<span class="comment-copy">Enter your heights in meters Traceback (most recent call last):   File "E:\Project 64\Working With Python\BMI Calculator.py", line 4, in &lt;module&gt;     height = float(raw_input()) NameError: name 'raw_input' is not defined</span>
<span class="comment-copy">Because you're running in Python 3, not 2.7. <code>raw_input</code> no longer exists. Change to <code>input()</code></span>
<span class="comment-copy">I have used <code>input()</code> also but still, it shows error</span>
<span class="comment-copy">Please get into the habit of actually telling us what the errors <i>are</i> rather than us having to ask you for them</span>
<span class="comment-copy">I have used 'input()' also but still it shows error</span>
