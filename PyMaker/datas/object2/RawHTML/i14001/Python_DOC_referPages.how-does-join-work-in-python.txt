<div class="post-text" itemprop="text">
<p>I want to display each row of a SQL query result on a webpage.  I found some code, but I don't understand what this line does.</p>
<pre><code>u"&lt;br&gt;".join([u"{0}".format(row.combination) for row in salt])
</code></pre>
<p>How does the <code>.join</code> method give me the desired result?</p>
</div>
<div class="post-text" itemprop="text">
<p>Best to run a python shell and test it, such as:</p>
<pre><code>&gt;&gt;&gt; "x".join (["a", "b", "c"])
'axbxc'
&gt;&gt;&gt; "abc".join (["M"])
'M'
</code></pre>
<p>The "x" was used as the thing in between the values in the iterable.  Next time "abc" was used in between, except there was only one value, so there was no in between needed.</p>
<p>Then refer to the docs as per CoryKramer comment.</p>
</div>
<span class="comment-copy">Have you <a href="https://docs.python.org/3/library/stdtypes.html?highlight=join#str.join" rel="nofollow noreferrer">read the docs</a>?</span>
<span class="comment-copy">Moderately related: <a href="http://stackoverflow.com/questions/493819/python-join-why-is-it-string-joinlist-instead-of-list-joinstring" title="python join why is it string joinlist instead of list joinstring">stackoverflow.com/questions/493819/…</a></span>
<span class="comment-copy">Also, uh <code>u"{0}".format(row.combination)</code> ....isn't that just <code>unicode(row.combination)</code>?</span>
<span class="comment-copy">"x".join(iterable) join all items of iterable spacing it by x. It put "x" between each pair of items of iterable</span>
<span class="comment-copy">You're wetting your feet with Django and you don't understand strings?</span>
<span class="comment-copy">Thanks a lot for clarifying my doubt ...</span>
