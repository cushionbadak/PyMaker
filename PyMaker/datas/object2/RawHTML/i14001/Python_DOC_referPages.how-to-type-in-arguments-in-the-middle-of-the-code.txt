<div class="post-text" itemprop="text">
<p>We can do this thing to type in any arguments:</p>
<pre><code>from sys import argv
script, argument_1, argument_2 = argv
</code></pre>
<p>Is it possible to type in arguments in the middle of the code, not in the beginning?</p>
<p>I want to do smth the same:</p>
<pre><code>print "La-la-la"
print "Any string"
# and here i want the program will take a file name as an argument to work with the file then
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>Have a look at <a href="https://docs.python.org/2/howto/argparse.html" rel="nofollow">argparse</a> a python class for command line parsing</p>
</div>
<span class="comment-copy">This question is completely undecipherable. I have no idea what you want to accomplish.</span>
<span class="comment-copy">You can look at sys.argv whenever the heck you want to.</span>
<span class="comment-copy">Stab into the dark: do you mean <a href="https://docs.python.org/3/library/functions.html#input" rel="nofollow noreferrer"><code>input()</code></a>?</span>
<span class="comment-copy">deceze, maybe. Does it work in Python 2.7.10?</span>
<span class="comment-copy">There's a selector on that manual page in the top left which lets you choose another Python version... see for yourself.</span>
