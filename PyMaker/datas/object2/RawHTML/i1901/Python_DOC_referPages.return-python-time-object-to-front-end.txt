<div class="post-text" itemprop="text">
<p>In python, the <code>timedate</code> library contains a <code>datetime</code> (timestamp<code>, a</code>date<code>and a</code>time` class. Is there a <em>time-only</em> (not corresponding to a specific date) concept in </p>
<ul>
<li>javascript</li>
<li>iso 8601</li>
</ul>
<p>so, that I can return the <code>datetime.time</code> object to the front end in a standardized manner? </p>
</div>
<div class="post-text" itemprop="text">
<p>Is this what you are looking for ? </p>
<pre><code>&gt;&gt;&gt; import datetime
&gt;&gt;&gt; now = datetime.datetime.now()
&gt;&gt;&gt; now.time()
datetime.time(11, 39, 33, 932976)
&gt;&gt;&gt; now.time().isoformat()
'11:39:33.932976'
&gt;&gt;&gt;
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>Use <a href="https://docs.python.org/3/library/datetime.html#datetime.date.isoformat" rel="nofollow noreferrer"><code>.isoformat</code></a> to return an ISO8601 formatted string from a <code>datetime</code> object.</p>
</div>
<span class="comment-copy">What is "a standardized manner"?</span>
<span class="comment-copy">I meant an ISO8601 representation, which is directly deserialized by javascript. Just like you can do with timestamps</span>
<span class="comment-copy">there's only Date object in js, not just standalone time <a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString" rel="nofollow noreferrer">developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/…</a></span>
<span class="comment-copy">Are you talking about a duration (8 hrs 30 mins) or a repeating interval (every day at noon)?  <a href="https://momentjs.com/docs/#/durations/" rel="nofollow noreferrer">moment.js</a> has some techniques to parse iso8601 durations.</span>
<span class="comment-copy">@James: a repeating time. regardless of day, eg "schools open at 9:00".</span>
