<div class="post-text" itemprop="text">
<p>I use <code>progressbar</code>, but when I execute, the code returns the <strong>error</strong> </p>
<blockquote>
<p>Traceback (most recent call last):   File "progress_bar.py", line 3,
  in 
      from progressbar import Bar ModuleNotFoundError: No module named 'progressbar'</p>
</blockquote>
<ul>
<li>I had tried <code>pip install progressbar</code> and <code>pip install progressbar2</code>
but the same error still exists </li>
</ul>
<p>I use Python 3.6.6</p>
</div>
<div class="post-text" itemprop="text">
<p>Just type this following in your command line:</p>
<pre><code>pip install progressbar
</code></pre>
</div>
<span class="comment-copy">Use <code>python3.6 -m pip install ...</code></span>
<span class="comment-copy">What's your code? Are you coding in an ide? Is your python interpreted pointing to the one you installed progressbar?</span>
<span class="comment-copy">problem solved after  python3.6 -m pip install progressbar</span>
<span class="comment-copy">In the future it might be useful to look into <a href="https://docs.python.org/3/tutorial/venv.html" rel="nofollow noreferrer">virtual environments</a>. It's a simple <code>python3.6 -m venv &lt;some-dir&gt;</code> to set up, and it will save you a lot of time to debug dependencies versioning, pip versions, different features between Python2 and Python3.x etc.</span>
