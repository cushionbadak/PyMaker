<div class="post-text" itemprop="text">
<p>See the pics below：
<a href="https://i.stack.imgur.com/JHBqe." rel="nofollow noreferrer">html code</a>
<a href="https://i.stack.imgur.com/fXB5v.png" rel="nofollow noreferrer">python output and code</a></p>
<p>The table/array format can be easily changed when export, is there any solution to keep the table/array format intact?</p>
<p>Part of the python code:</p>
<pre><code>Content = driver.find_elements_by_id('data')
    for Table in Content:
        print(Table.text)
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>First, read data in from the table. You can use <a href="https://docs.python.org/3/library/re.html" rel="nofollow noreferrer">regular expressions</a> or <code>string.split()</code> or some other method to separate <code>Table.text</code> into its component parts (e.g. a python dict or 2D list).</p>
<p>Then, you can use <a href="https://docs.python.org/3/library/csv.html" rel="nofollow noreferrer">python's <code>csv</code> library</a> to output that 2D list as a csv file, or you can look into <a href="http://www.python-excel.org/" rel="nofollow noreferrer">the several python libraries that interface with MS Excel</a> to output an Excel spreadsheet with that same data.</p>
</div>
