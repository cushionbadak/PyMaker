<div class="post-text" itemprop="text">
<p>I need to created a randomly ordered list of numbers from 0 to 9999
So here is my approach to do this:</p>
<pre><code>order = list(range(10000));
rorder = [];
while (len(order) &gt; 0):
    index_to_pop = random.randint(0,len(order));   
    print(str(index_to_pop) + " from list of size " + str(len(order)));
    rorder.append(order.pop(index_to_pop));
</code></pre>
<p>However this fails with the error </p>
<blockquote>
<p>pop index out of range</p>
</blockquote>
<p>This is because the print statemente at a given point in the iteration gives:</p>
<blockquote>
<p>7288 from list of size 7288</p>
</blockquote>
<p>So... How can this be possible? According to the documentations randint should give me a number between 0 and len(order) -1.</p>
<p>What am I doing wrong?</p>
</div>
<div class="post-text" itemprop="text">
<p>You are wrong here: "According to the documentations randint should give me a number between 0 and len(order) -1."</p>
<p>The doc says: "random.randint(a, b). Return a random integer N such that a &lt;= N &lt;= b. Alias for randrange(a, b+1)."</p>
<p>See it here: <a href="https://docs.python.org/3/library/random.html#random.randint" rel="nofollow noreferrer">https://docs.python.org/3/library/random.html#random.randint</a></p>
</div>
<div class="post-text" itemprop="text">
<p>I recommend using <em>shuffle</em> from module <em>random</em>. I think this is what you need:</p>
<pre><code>import random

ordered = list(range(10000))
random.shuffle(ordered)
print(ordered)
</code></pre>
<p>Keep in mind that shuffle <strong>does not return the shuffled list</strong>. It is shuffeling the list you gave to it. </p>
</div>
<span class="comment-copy">Whatnow? <a href="https://docs.python.org/3/library/random.html#random.randint" rel="nofollow noreferrer"><i>"<code>random.randint(a, b)</code> - Return a random integer N such that a &lt;= N &lt;= b."</i></a> If you want the range to be <i>exclusive</i>, you need to subtract 1 yourself.</span>
<span class="comment-copy">You're reading the docs wrong. <a href="https://docs.python.org/3/library/random.html#random.randint" rel="nofollow noreferrer"><code>randint</code></a> includes the endpoint. As opposed to <a href="https://docs.python.org/3/library/random.html#random.randrange" rel="nofollow noreferrer"><code>randrange</code></a>.</span>
<span class="comment-copy">Why should you reinvent the wheel? Use <code>random.sample(range(1, 10000), 10000)</code>.</span>
<span class="comment-copy">why not use numpy for this? <code>order = np.arange(10000), rorder = np.random.shuffle(order)</code></span>
<span class="comment-copy">Got my docs from scypy. <a href="https://docs.scipy.org/doc/numpy-1.15.1/reference/generated/numpy.random.randint.html" rel="nofollow noreferrer">docs.scipy.org/doc/numpy-1.15.1/reference/generated/…</a>. Did not see that this was a different randint function. So it behaves differently. Thanks</span>
<span class="comment-copy">Thanks!! I'll use this instead! Still, I can't mark this answer as correct because it does not address my original question. But thank you anyways</span>
<span class="comment-copy">you are welcome :) Your right! But i thought why not solving the main problem ;)</span>
