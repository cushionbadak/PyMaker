<div class="post-text" itemprop="text">
<p>Keep getting an error when I run my code</p>
<p>I have changed this many different times trying different things. I had this working a couple of days ago. I lost the program I was using and can't remember how it was put together. </p>
<pre><code>class Gui(QtGui.QMainWindow, ui_hayfeeder.Ui_MainWindow):    
    def __init__(self):        
        super(self.__class__, self).__init__()  
        self.exiting = True
        self.setupUi(self) 
        self.startup()
        #self.pushButton_c_update.clicked.connect(self.update_clients)
        self.pushbutton_bale1.clicked.connect(self.bale1)

    def self.pushbutton_bale1 #getting error here


        print "I am in here"
</code></pre>
<p>Print "I am in here"</p>
</div>
<div class="post-text" itemprop="text">
<p>It should be something like this:</p>
<pre><code>class Gui(QtGui.QMainWindow, ui_hayfeeder.Ui_MainWindow):    
    def __init__(self):        
        super(self.__class__, self).__init__()  
        self.exiting = True
        self.setupUi(self) 
        self.startup()
        #self.pushButton_c_update.clicked.connect(self.update_clients)
        self.pushbutton_bale1.clicked.connect(self.bale1)

    def bale1(self):
        print "I am in here"
</code></pre>
</div>
<span class="comment-copy">Please <a href="https://docs.python.org/3/tutorial/index.html" rel="nofollow noreferrer">learn</a> the basics of Python before attempting to write such relatively complex code</span>
