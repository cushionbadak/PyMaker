<div class="post-text" itemprop="text">
<p>When I attempt to run a basic shell script with the <code>subprocess</code> module, I cannot seem to get it running without specifying the absolute path to my script. That is:</p>
<pre><code>proc = run(['classify.sh', twitter_handle],
           cwd='/Users/kkworden/Code/school/Twirole',
           stdout=PIPE)
</code></pre>
<p>Does not work, but:</p>
<pre><code>proc = run(['/Users/kkworden/Code/school/Twirole/classify.sh', twitter_handle],
           stdout=PIPE)
</code></pre>
<p>Does work. Why? Shouldn't the two be the same? </p>
</div>
<div class="post-text" itemprop="text">
<p>Nevermind, I forgot to specify the script relatively:</p>
<pre><code>proc = run(['./classify.sh', twitter_handle],
           cwd='/Users/kkworden/Code/school/Twirole',
           stdout=PIPE)
</code></pre>
<p>Notice the leading <code>./</code> before my script.</p>
</div>
<span class="comment-copy">Hmm, <a href="https://docs.python.org/3/library/subprocess.html#popen-constructor" rel="nofollow noreferrer">the documentation</a> states that "In particular, the function looks for <i>executable</i> (or for the first item in <i>args</i>) relative to <i>cwd</i> if the executable path is a relative path." and yet it clearly doesn't do that. I guess it's a bug.</span>
