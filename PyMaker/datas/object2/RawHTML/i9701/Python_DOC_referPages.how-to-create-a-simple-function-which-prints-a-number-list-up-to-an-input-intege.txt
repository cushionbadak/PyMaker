<div class="post-text" itemprop="text">
<p>Given an input number n, what would be the simplest function to output a list up to this value.</p>
<pre><code>n = int(raw_input("Enter a number:"))
x = [1:n+1]
def work(n):
    print range(x)
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>Just use <a href="https://docs.python.org/2/library/functions.html#range" rel="nofollow noreferrer"><code>range</code></a>,</p>
<pre><code>n = int(raw_input("Enter a number:"))

def work(n):
    print(range(1, n+1))

work(n)
</code></pre>
</div>
<span class="comment-copy">You can do it using <code>range</code> i guess. <a href="https://docs.python.org/3/library/stdtypes.html#range" rel="nofollow noreferrer">Doc link</a></span>
<span class="comment-copy">This is invalid python syntax</span>
<span class="comment-copy"><code>list = [1,2,3,4,5]</code> , print list[1:3]</span>
<span class="comment-copy">@Akshay output [2,3]</span>
<span class="comment-copy">@sparkandshine did you test this, it doesn't work for me</span>
<span class="comment-copy">@AmyRose, yes, I do. It works for me. Which Python version do u use?</span>
<span class="comment-copy">@AmyRose, you should call the function <code>work(n)</code> as my updated answer.</span>
<span class="comment-copy">thanks for your help, is there a simpler way to achieve this?</span>
<span class="comment-copy">How much simpler do you wanna get...?</span>
