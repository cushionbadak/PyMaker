<div class="post-text" itemprop="text">
<p>So, I'm not really sure how to explain this so I'll give an example.</p>
<p>ex.
If I input: "Iowa"
I want to be able to look up the number 3</p>
<p>This is what I have so far, but it returns 0</p>
<pre><code>school=input('Select your school: ')

Illinois=['Illinois',2]
Indiana=['Indiana',2]
Iowa=['Iowa',3]

print(school[1])
</code></pre>
</div>
<div class="post-text" itemprop="text">
<pre><code>lookup = {'Illinois':2,'Indiana':2,'Iowa':3}
print(lookup.get('Iowa'))
</code></pre>
<p>use <code>dictionary</code> for this</p>
</div>
<div class="post-text" itemprop="text">
<p>First of all you need to Save all your variables in a <strong>lookup dictionary</strong> so that we can find it with its <strong>key-value</strong></p>
<pre><code>lookup = {'Illinois':2,'Indiana':2,'Iowa':3}
</code></pre>
<p>then if necessary you can ask user to input proper key/name of the school</p>
<pre><code>while True:
    try:
        school=raw_input('Select your school: ')
        if school in lookup:
            print(lookup.get(school))
            break
        else:
             school=raw_input('Select proper your school: ')
    except ValueError:
        print "Error: Invalid school."
</code></pre>
</div>
<span class="comment-copy">use dictionary  for this</span>
<span class="comment-copy"><a href="https://docs.python.org/3/library/stdtypes.html#typesmapping" rel="nofollow noreferrer">docs.python.org/3/library/stdtypes.html#typesmapping</a></span>
<span class="comment-copy">This does not provide an answer to the question. To critique or request clarification from an author, leave a comment below their post. - <a href="/review/low-quality-posts/14496614">From Review</a></span>
<span class="comment-copy">@Mack, Can you let me know why this will not return 3. or why this does not solve this "If I input: "Iowa" I want to be able to look up the number 3"</span>
