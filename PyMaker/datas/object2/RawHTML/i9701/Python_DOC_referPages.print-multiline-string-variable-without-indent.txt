<div class="post-text" itemprop="text">
<div class="question-status question-originals-of-duplicate">
<p>This question already has an answer here:</p>
<ul>
<li>
<a dir="ltr" href="/questions/11157043/does-python-have-a-built-in-function-for-unindenting-a-multiline-string">Does Python have a built-in function for unindenting a multiline string?</a>
<span class="question-originals-answer-count">
                    3 answers
                </span>
</li>
</ul>
</div>
<p>How do I print a multiline string variable to the console without the indent?</p>
<p>At the moment I tried this without success:</p>
<pre><code># Grid.
grid = """rnbqkbnr
          pppppppp
          ********
          ********
          ********
          PPPPPPPP
          RNBQKBNR"""

# Show grid.
print(grid)
</code></pre>
<p>The output is the following:</p>
<pre><code>rnbqkbnr
             pppppppp
             ********
             ********
             ********
             PPPPPPPP
             RNBQKBNR
</code></pre>
<p>This is the output where I am looking for:</p>
<pre><code>rnbqkbnr
pppppppp
********
********
********
PPPPPPPP
RNBQKBNR
</code></pre>
</div>
<div class="post-text" itemprop="text">
<pre><code>print('\n'.join(grid.split()))
</code></pre>
<p>if you have inner spaces in your lines which you want to keep:</p>
<pre><code>for line in grid.splitlines():
    print(line.strip())
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>Use <a href="https://docs.python.org/3/library/textwrap.html#textwrap.dedent" rel="nofollow noreferrer"><code>textwrap.dedent(text)</code></a>.</p>
<pre><code>&gt;&gt;&gt; from textwrap import dedent
&gt;&gt;&gt; dedent("""\
    rnbqkbnr
    pppppppp
    ********
    ********
    ********
    PPPPPPPP
    RNBQKBNR""")
'rnbqkbnr\npppppppp\n********\n********\n********\nPPPPPPPP\nRNBQKBNR'
&gt;&gt;&gt; print(_)
rnbqkbnr
pppppppp
********
********
********
PPPPPPPP
RNBQKBNR
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>Try this:</p>
<pre><code># Grid.
grid = """\
rnbqkbnr
pppppppp
********
********
********
PPPPPPPP
RNBQKBNR"""

# Show grid.
print(grid)
</code></pre>
<p>Putting <code>\</code> at the end of a line removes unnecessary newline character.</p>
</div>
<div class="post-text" itemprop="text">
<p>Simply don't indent inside of <code>"""</code>:</p>
<pre><code># Grid.
grid = """rnbqkbnr
pppppppp
********
********
********
PPPPPPPP
RNBQKBNR"""

# Show grid.
print(grid)
</code></pre>
<p>Works as expected.</p>
</div>
<div class="post-text" itemprop="text">
<pre><code>def unindent(string):
    return ''.join(map(str.lstrip, string.splitlines(1)))

# Grid.
grid = """rnbqkbnr
          pppppppp
          ********
          ********
          ********
          PPPPPPPP
          RNBQKBNR"""

# Show grid.
print(unindent(grid))
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>Could also try the <code>re</code> module and remove leading spaces for all lines:</p>
<pre><code>grid = """rnbqkbnr
          pppppppp
          ********
          ********
          ********
          PPPPPPPP
          RNBQKBNR"""
​
import re
print(re.sub('^\s+', '', grid, flags=re.MULTILINE))

rnbqkbnr
pppppppp
********
********
********
PPPPPPPP
RNBQKBNR
</code></pre>
</div>
<span class="comment-copy">Cross-site dupe: <a href="http://codereview.stackexchange.com/q/60366">codereview.stackexchange.com/q/60366</a></span>
<span class="comment-copy">This is very similar to doc strings, for which there exists a <a href="https://www.python.org/dev/peps/pep-0257/#handling-docstring-indentation" rel="nofollow noreferrer">general algorithm</a> as for how to parse the identation. You can reuse the code from there.</span>
<span class="comment-copy">This is a satisfactory solution for the OP's specific example, but take care: if the text grid contains internal spaces, those will get split as well. Ex. <code>"foo bar \n aaaaaaa \n bbbbbbb"</code> will put "foo" and "bar" on separate lines.</span>
<span class="comment-copy">Thanks, nice one.</span>
<span class="comment-copy">Note that this will also get rid of nested indentation which might not be desireable.</span>
<span class="comment-copy">Thanks, also a nice suitable solution.</span>
<span class="comment-copy">Yep, this is the most straightforward solution. And if you want the "rnb..." line to align nicely with the rest of the lines in your source, you can do <code>grid = """\ </code> and then put <code>rnb...</code> on the next line.</span>
<span class="comment-copy">Thanks for your answer and time. But I want to avoid this solution.</span>
