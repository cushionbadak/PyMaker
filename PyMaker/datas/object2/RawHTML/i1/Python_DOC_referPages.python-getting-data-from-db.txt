<div class="post-text" itemprop="text">
<div class="question-status question-originals-of-duplicate">
<p>This question already has an answer here:</p>
<ul>
<li>
<a dir="ltr" href="/questions/12661999/get-raw-decimal-value-from-mysqldb-query">get raw decimal value from mysqldb query</a>
<span class="question-originals-answer-count">
                    4 answers
                </span>
</li>
</ul>
</div>
<p>im trying to get data from DB and add it to the list. When i try to get data and make append it gets data type. 
The db structure:  </p>
<p><a href="https://i.stack.imgur.com/nSLbU.png" rel="nofollow noreferrer"><img alt="DB" src="https://i.stack.imgur.com/nSLbU.png"/></a></p>
<p>And that my code:</p>
<pre><code>import pymysql as sql
conn = sql.Connect(host='localhost', unix_socket='', user='root', passwd='', db='agd')
cursor = conn.cursor()
stat = 'SELECT * FROM stuff'
cursor.execute(stat)+
lista = list(cursor.fetchall())
test_names = []
test_prices = []
test_weights = []
for row in lista:
    test_names.append(row[1])
    test_prices.append(row[2])
    test_weights.append(row[3])
print(test_names)
print(test_prices)
print(test_weights)
</code></pre>
<p>And thats output:</p>
<pre><code>['Marcia', 'Daniel', 'Corey', 'Morgan', 'Kayla', 'Christopher', 'Heather', 'Jessica', 'Ralph', 'Wanda']
[Decimal('5558.06'), Decimal('4145.11'), Decimal('4452.92'), Decimal('7921.65'), Decimal('5578.65'), Decimal('851.20'), Decimal('2260.02'), Decimal('5388.26'), Decimal('2020.39'), Decimal('6850.01')]
[Decimal('56.47'), Decimal('75.56'), Decimal('89.83'), Decimal('12.32'), Decimal('4.74'), Decimal('36.99'), Decimal('47.88'), Decimal('84.42'), Decimal('93.63'), Decimal('53.12')]
</code></pre>
<p>I want to print this all data without "Decimal" at the beggining of every number.</p>
</div>
<div class="post-text" itemprop="text">
<p>You can explicitly convert <code>Decimal</code>s to <code>float</code>s with</p>
<pre><code>test_prices.append(float(row[2]))
test_weights.append(float(row[3]))
</code></pre>
</div>
