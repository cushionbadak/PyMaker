<div class="post-text" itemprop="text">
<div class="question-status question-originals-of-duplicate">
<p>This question already has an answer here:</p>
<ul>
<li>
<a dir="ltr" href="/questions/14390224/reshape-of-pandas-series">Reshape of pandas series?</a>
<span class="question-originals-answer-count">
                    4 answers
                </span>
</li>
</ul>
</div>
<p>I have a pandas dataframe where its column has a length of 256. How can I extract a column in order to create a 16*16 image and show it?
My problem is I don't know to choose a column a create a 16*16 object</p>
<p>I tried with pivot but this doesn't solve my problem</p>
</div>
<div class="post-text" itemprop="text">
<p>Select column, convert to numpy array and <code>reshape</code>:</p>
<pre><code>arr = df['col'].values.reshape(16, 16)
</code></pre>
</div>
