<div class="post-text" itemprop="text">
<p>I'm trying to make this code work with sys.argv but it's not showing
anything on the output. The user enters a value and then it prints the key.</p>
<pre><code>from __future__ import print_function
import sys

v = sys.argv[1:]

d={1:'a', 2:'b', 3:'c', 4:'a', 5:'d', 6:'e', 7:'a', 8:'b'}

for key, value in d.items():
    if value == v: 
        print(key, '', end='')
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>you're having a <code>type</code> issue<br/></p>
<pre><code>v = sys.argv[1:] #  v is a list here, even if its a single item or empty
</code></pre>
<p>the values in <code>d</code> are not lists so they don't match<br/>
if you change this line:</p>
<pre><code>if value == v:
</code></pre>
<p>into:</p>
<pre><code>if value in v:
</code></pre>
<p>it will work. what it does is check if your value is contained in the list</p>
</div>
<div class="post-text" itemprop="text">
<pre><code>v = sys.argv[1:]
</code></pre>
<p>sets <code>v</code> to a list of all the command line arguments. If you just want the first argument, it should be:</p>
<pre><code>v = sys.argv[1]
</code></pre>
</div>
<span class="comment-copy">And how did you start this script? I'm guessing you did it without passing any command line arguments...?</span>
<span class="comment-copy">from the cmd, just entered the file name and the argument.</span>
<span class="comment-copy"><code>v</code> is a list, <code>value</code> is a string. <code>if value == v:</code> will never be true.</span>
<span class="comment-copy">Thank you, can you help me again?  <code>word = sys.argv[1:] print(Counter(word).most_common(len(word)))</code> Why is there no output when I run this code? I can't get this..</span>
<span class="comment-copy">without knowing the contents of word that would be difficult, try printing <code>word</code> before you call Counter...  also, dont forget that <code>word</code> is a list here so passing <code>len(word)</code> to most_common is that same as passing nothing</span>
<span class="comment-copy">The <code>word</code> is just a string. The output should print all the letters and how often they are in the string.     <code>word = "Hello"</code> - when passed at sys.argv. Output should be: <code>[('l', 2), ('h', 1), ('e', 1), ('o', 1)]</code></span>
<span class="comment-copy">if you are doing <code>word = sys.argv[1:] </code> then word is most definitely a <code>list</code> and not a <code>string</code> if you want the first parameter you passwed the program use <code>word = sys.argv[1]</code></span>
<span class="comment-copy">@okdenyou code should still work though, are you SURE <code>word</code> isn't empty? also, what do you mean by "no output"? it should at least print <code>[]</code></span>
