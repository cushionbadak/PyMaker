<div class="post-text" itemprop="text">
<p>I need to output CSV files in a loop. Using pandas  </p>
<pre><code>for i in range(1,int(len(feed_file_order)/2000)+2): 
    print(i)

    final_frame = final_frame.append(feed_file_order[u:(2000*i)])

    u=u+2000
    final_frame.drop(final_frame.index, inplace=True)

    final_frame.to_csv('C:\\Users\\7005243\\Desktop\\trail_i.csv') 
</code></pre>
<p>I want my output csv files as trail_1, trail_2 etc.</p>
</div>
<div class="post-text" itemprop="text">
<p>Just change the last line of the loop to:</p>
<pre><code>final_frame.to_csv('C:\\Users\\7005243\\Desktop\\trail_{}.csv'.format(i))
</code></pre>
<p>The <code>format</code> function will take care of putting the current value of <code>i</code> where the <code>{}</code> is.</p>
</div>
<div class="post-text" itemprop="text">
<pre><code>final_frame.to_csv('C:\\Users\\7005243\\Desktop\\trail_'+str(i)+'.csv')
</code></pre>
<p>You are adding <code>i</code> inside the string which is wrong. It's a variable which should change, so isolate it.</p>
</div>
<span class="comment-copy">dont understant what is your problem, to build the file name?</span>
<span class="comment-copy">change <code>'C:\\Users\\7005243\\Desktop\\trail_i.csv'</code> to <code>f'C:\\Users\\7005243\\Desktop\\trail_{i}.csv'</code>, it should work. The problem was that <code>i</code> in your case was the character i and not the variable <code>i</code>i</span>
<span class="comment-copy">@Tacratis f-strings are great, but they available only from Python 3.6 onwards.</span>
<span class="comment-copy">Thanks for pointing this out, you are of course correct.</span>
<span class="comment-copy">That was silly me. Thanks Venkat!</span>
