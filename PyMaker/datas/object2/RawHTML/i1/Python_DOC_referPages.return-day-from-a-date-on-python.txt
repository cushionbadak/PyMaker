<div class="post-text" itemprop="text">
<p>I'm looking for a method to get the day from a certain date. I have files named with the pattern "yyyymmdd.mp3" (e.g. 20190301.mp3). In my country, that day would be a Friday. </p>
<p>I would like to obtain the information "Friday" from the string shown above and add it to the name of the file like "friday-20190301.mp3". Is there some way to do this with Python? Thanks in advance!</p>
</div>
<div class="post-text" itemprop="text">
<p>Pretty similar to @ALFA's answer, but this is a full solution:</p>
<pre><code>from datetime import datetime

example_filename = '20190301.mp3'

def convert(filename):
    day_of_week = datetime.strptime(filename, '%Y%m%d.mp3').strftime('%A').lower()
    return '{}_{}'.format(day_of_week, filename)

result = convert(example_filename)
# --&gt; 'friday_20190301.mp3'
</code></pre>
</div>
<div class="post-text" itemprop="text">
<pre><code>import datetime
import calendar

def new_name(val):
    my_date = datetime.datetime.strptime(val.split('.')[0], "%Y%m%d").date()
    day = calendar.day_name[my_date.weekday()] 
    return ('{}-{}'.format(day,val))


print (new_name('20190301.mp3'))
</code></pre>
<p>Output: </p>
<p>Friday-20190301.mp3</p>
</div>
<div class="post-text" itemprop="text">
<p>Use <a href="https://docs.python.org/3/library/datetime.html" rel="nofollow noreferrer"><code>datetime</code></a> and <a href="https://docs.python.org/3/library/datetime.html#datetime.date.strftime" rel="nofollow noreferrer"><code>strftime</code></a>:</p>
<pre><code>from datetime import datetime

datetime_obj = datetime.strptime('20190301', '%Y%m%d')
print(datetime_obj.strftime('%A'))
</code></pre>
<p>Once you obtained the weekday, you can change the filename using the <a href="https://www.tutorialspoint.com/python/string_replace.htm" rel="nofollow noreferrer"><code>replace</code></a> function.</p>
</div>
<span class="comment-copy">Have a look at the <a href="https://docs.python.org/3/library/datetime.html" rel="nofollow noreferrer"><code>datetime</code></a> module.</span>
<span class="comment-copy">1. String to datetime : <a href="https://stackoverflow.com/questions/466345/converting-string-into-datetime" title="converting string into datetime">stackoverflow.com/questions/466345/…</a> 2. Get the day of the week : <a href="https://stackoverflow.com/questions/9847213/how-do-i-get-the-day-of-week-given-a-date-in-python" title="how do i get the day of week given a date in python">stackoverflow.com/questions/9847213/…</a> 3. Rename file : <a href="https://stackoverflow.com/questions/2491222/how-to-rename-a-file-using-python" title="how to rename a file using python">stackoverflow.com/questions/2491222/…</a> 3. Use the search bar before post...</span>
<span class="comment-copy">Possible duplicate of <a href="https://stackoverflow.com/questions/9847213/how-do-i-get-the-day-of-week-given-a-date-in-python">How do I get the day of week given a date in Python?</a></span>
<span class="comment-copy">You could use <a href="https://docs.python.org/3/library/datetime.html#datetime.date.strftime" rel="nofollow noreferrer"><code>date.strftime</code></a> to get the name of the weekday.</span>
<span class="comment-copy">If you must use an integer weekday, definitely use <a href="https://docs.python.org/3/library/datetime.html#datetime.date.isoweekday" rel="nofollow noreferrer"><code>isoweekday()</code></a> which is the international standard. <code>weekday()</code> is c legacy.</span>
<span class="comment-copy">Edited using <code>strftime</code> to get the name of the weekday. Thanks @Matthias</span>
