<div class="post-text" itemprop="text">
<p>I trying to run sql query in python to update quantity but i am getting error . </p>
<pre><code>cur.execute(
  "UPDATE Products SET Price =? , Quantity =? , RackLocation =? WHERE ID =?",
   [self.new_price.get(), self.new_quantity.get(), self.new_racklocation.get(), self.id]
)
</code></pre>
<p>i am getting this error:</p>
<pre><code>[int(self.new_quantity.get()) ,  int(self.id)])    
pyodbc.ProgrammingError: ('42000', "[42000] [Microsoft][ODBC SQL Server Driver]
[SQL Server]Incorrect syntax near '='. (102) (SQLExecDirectW); [42000] 
[Microsoft][ODBC SQL Server Driver][SQL Server]Statement(s) could not be 
prepared. (8180)")
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>I was doing wrong . Query should look like this </p>
<p>cur.execute("UPDATE Products SET  Quantity =  Quantity - 1   WHERE ID=?",
                    [self.id])</p>
</div>
<span class="comment-copy">What are your get methods returning if you simply print them to console?</span>
<span class="comment-copy"><a href="https://stackoverflow.com/posts/55206024/edit">Edit</a> your Question and explain the meaning of <i>"subtracting columns"</i>?</span>
<span class="comment-copy">hi thanks for your reply  actually my query was wrong . I changed my query to this it works .</span>
<span class="comment-copy">cur.execute("UPDATE Products SET  Quantity =  Quantity - 1   WHERE ID=?",                     [self.id])</span>
