<div class="post-text" itemprop="text">
<p>I'm trying to upload a file in binary form to PostgreSQL via python</p>
<pre><code>cur.execute("INSERT INTO resultdata(data) VALUES(%s)", (psycopg2.Binary(data),))
</code></pre>
<p>So it uploads it in strange way: when i try to view it in db it just spaces</p>
</div>
<div class="post-text" itemprop="text">
<p>Just I figured that. Try this code:</p>
<pre><code> cur.execute('INSERT INTO resultdata(data) VALUES (%s)', (data,))
 cur.fetchall()
</code></pre>
<p>Actually, there no use psycopg2.Binary(data) and there already given an imported with psycopg2.</p>
</div>
<span class="comment-copy">What type is <code>data</code> in the database?</span>
<span class="comment-copy">Aside: Putting files into your database probably isn't the best approach for whatever you're trying to do.</span>
<span class="comment-copy">@Mureinik data = f.read()</span>
<span class="comment-copy">@Maxim_CH, Mureinik is asking what the column type of the <code>data</code> column in the <code>resultdata</code> table is.</span>
<span class="comment-copy">Hi, what do you mean by <i>view in the db</i>? view it through some client like dbeaver or datagrip?</span>
