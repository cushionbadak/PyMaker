<div class="post-text" itemprop="text">
<p>Flask app it's ok,but when I use Gunicorn command:</p>
<pre><code>gunicorn -w 4 -b 127.0.0.1:8004 app:app
</code></pre>
<p>or</p>
<pre><code>gunicorn -w 4 -b 127.0.0.1:8004 route:app
</code></pre>
<p>it seems  ImportError: No module named 'app'</p>
<p>my structure</p>
<pre><code>app
    │  config.py
    │  data.db
    │  forms.py
    │  models.py
    │  mulu.txt
    │  route.py
    │  __init__.py
    │  templates
    |  static
</code></pre>
<p>app is defined in <strong>init</strong>.py</p>
<pre><code>from flask import Flask
app = Flask(__name__)
</code></pre>
<p>route.py</p>
<pre><code>from app import app
@app.route('/')
def hello_world():
    return 'hello world'

if __name__ == '__main__':
    app.run()
</code></pre>
<p>Why?Thanks!</p>
</div>
<div class="post-text" itemprop="text">
<p>Your app instance is defined in <code>__init__.py</code>, so you should do </p>
<pre><code>gunicorn -w 4 -b 127.0.0.1:8004 __init__:app
</code></pre>
</div>
