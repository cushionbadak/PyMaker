<div class="post-text" itemprop="text">
<p>I cannot get the version of Kibana (from python as an example or command-line)
,all i have is the link of Kibana.</p>
<p>is there any way to do that ?</p>
</div>
<div class="post-text" itemprop="text">
<p>Try</p>
<pre><code>HTTP GET http://&lt;host&gt;:&lt;port&gt;/api/status
</code></pre>
<p>Something like</p>
<pre><code>import requests

version = requests.get('http://&lt;host&gt;:&lt;port&gt;/api/status').json()['version']['number']   
</code></pre>
</div>
<span class="comment-copy">Your question is a bit unclear. Please review <a href="https://stackoverflow.com/help/how-to-ask">how to ask a question</a> for ideas to clarify your question. Welcome to Stack Overflow!</span>
<span class="comment-copy">i mean when i don't have the port number, just link to Kibana</span>
<span class="comment-copy">Try 5601 which is the default port</span>
<span class="comment-copy">that means that there is no way to do that if i didn't have the port number ?</span>
