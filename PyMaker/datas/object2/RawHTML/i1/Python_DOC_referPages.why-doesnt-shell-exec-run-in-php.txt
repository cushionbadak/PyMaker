<div class="post-text" itemprop="text">
<p>I wanna run python code in PHP and the PHP code is bellow:</p>
<pre><code>&lt;?php
$command = escapeshellcmd('python3 /usr/C:/xampp/htdocs/scripts/a.py');
$output = shell_exec($command);
print $output;
</code></pre>
<p>But, when I run this PHP code, nothing happens in the screen!</p>
<p>How can I fix it?</p>
</div>
<div class="post-text" itemprop="text">
<p>If you say it works on the terminal and not on apache then apache's <code>php.ini</code> file may be disabling the use of <code>shell_exec()</code>.</p>
<p>See <a href="http://www.php.net/manual/en/ini.core.php#ini.disable-functions" rel="nofollow noreferrer">http://www.php.net/manual/en/ini.core.php#ini.disable-functions</a></p>
<p>Your apache's <code>php.ini</code> file may look something like</p>
<pre><code>disable_functions=exec,passthru,shell_exec,system,proc_open,popen
</code></pre>
<p>Remove <code>shell_exec</code> from this list and restart the web server, although this is a security risk and I don't recommend it.</p>
</div>
<div class="post-text" itemprop="text">
<p>The problem wasn't because of <code>disable_functions</code> and the above code doesn't have any problem in Linux.</p>
<p>So, I think the problem is because of windows or the directory!</p>
</div>
<span class="comment-copy"><code>shell_exec()</code> is usually disabled of servers by web hosting providers. Are you sure is enabled? Also, can you add after <code>&lt;?php</code> this line <code>error_reporting(E_ALL);</code>?</span>
<span class="comment-copy">Are you sure the path is correct? Did you tried to execute the command in terminal? It works?</span>
<span class="comment-copy">@GasKa How can I make sure whether it's enabled or not?</span>
<span class="comment-copy">@Cosmin Yeah. It's ok</span>
<span class="comment-copy">@Parisa using <code>function_exists</code>: <a href="http://php.net/manual/en/function.function-exists.php" rel="nofollow noreferrer">php.net/manual/en/function.function-exists.php</a></span>
<span class="comment-copy"><code>shell_exec</code> should run on windows, more than likely the path is incorrect. See: <a href="https://stackoverflow.com/questions/13783649/shell-exec-with-windows-path-not-running" title="shell exec with windows path not running">stackoverflow.com/questions/13783649/…</a></span>
<span class="comment-copy">@nickl- The problem didn't solved! :(</span>
<span class="comment-copy">Try running something simple like: <code>echo this is a test</code>.</span>
<span class="comment-copy">@nickl- There is no problem! I think the problem is with <code>shell_exec</code>!</span>
<span class="comment-copy">Then we better fix it! What do you say... =)</span>
