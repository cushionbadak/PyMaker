<div class="post-text" itemprop="text">
<p>Why is <code>self.request.user.is_authenticated()</code> not working in this view?</p>
<pre><code>class ArticleDetailView(DetailView, CategoryListMixin):
    model = Article
    template_name = 'mainapp/article_detail.html'

    def get_context_data(self, *args, **kwargs):
        context = super(ArticleDetailView, self).get_context_data(**kwargs)
        context['articles'] = self.model.objects.all()
        context['article'] = self.get_object()
        context['comments'] = Comments.objects.filter(article=context['article']).order_by('-pub_date')
        if self.request.user.is_authenticated():
            context['current_user'] = PersonalAccount.objects.get(user=self.request.user)
        return context
</code></pre>
<p>I got an error: </p>
<blockquote>
<p>'bool' object is not callable</p>
</blockquote>
</div>
<div class="post-text" itemprop="text">
<p><strong>For Django 1.10 +</strong></p>
<p>is_authenticated is now an attribute.</p>
<pre><code>if self.request.user.is_authenticated:
</code></pre>
</div>
<span class="comment-copy">Really?, omg, thank you so much!!</span>
<span class="comment-copy">Let me know if you have any issue. if works then accept the answer</span>
