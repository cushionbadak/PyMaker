<div class="post-text" itemprop="text">
<p>if heaptify this <code>[(10,'Mike'),(20,'Jack'),(10,'Bob')]</code></p>
<p>And return the min of the heap, will it guaranteed to return <code>(10,'Mike') and not (10,'Bob')</code> ?</p>
</div>
<div class="post-text" itemprop="text">
<p>no <code>heapq</code> is not stable. as stated in the <a href="https://docs.python.org/3/library/heapq.html?highlight=heapq#basic-examples" rel="nofollow noreferrer">examples of the documentation</a></p>
<blockquote>
<p>This is similar to sorted(iterable), but unlike sorted(), this
  implementation is not stable.</p>
</blockquote>
</div>
