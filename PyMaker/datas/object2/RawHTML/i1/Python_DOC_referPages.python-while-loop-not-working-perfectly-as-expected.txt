<div class="post-text" itemprop="text">
<pre><code>choice = " "
while choice not 'Y' or choice not 'N' :
    choice = input("Y for Yes or N for No")
</code></pre>
<p>What is the mistake i have made in this while loop?</p>
</div>
<div class="post-text" itemprop="text">
<pre><code> choice = " "
 while not (choice  == 'Y' or choice ==  'N') :
      choice = input("Y for Yes or N for No")
</code></pre>
<p>Please Try with this code</p>
</div>
<div class="post-text" itemprop="text">
<p>this might help (python3.x is confusing, when comes to syntax): </p>
<pre><code> choice = " "
 while choice != 'Y' or choice != 'N' :
      choice = input("Y for Yes or N for No")
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>"not" is different than "not equal to". Use this instead,</p>
<pre><code>choice = " "
while choice!='Y' or choice!='N':
    choice = input("Y for Yes or N for No")
</code></pre>
</div>
<span class="comment-copy">Hi ! You did not mentionned what do you expect from your code :-) Do you have also an error message or an answer given that you do not expect here ?</span>
<span class="comment-copy">no error but condition is not working perfectly</span>
