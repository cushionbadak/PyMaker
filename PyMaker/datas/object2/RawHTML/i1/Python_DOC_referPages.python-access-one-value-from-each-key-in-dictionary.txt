<div class="post-text" itemprop="text">
<p>So I have a file like this one:</p>
<pre><code>5.1,3.5,1.4,0.2,Iris-setosa
4.9,3.0,1.4,0.2,Iris-setosa
4.7,3.2,1.3,0.2,Iris-setosa
4.6,3.1,1.5,0.2,Iris-setosa
5.0,3.6,1.4,0.2,Iris-setosa
5.4,3.9,1.7,0.4,Iris-setosa
4.6,3.4,1.4,0.3,Iris-setosa
</code></pre>
<p>And I have put it from file to a dictionary with keys being next positive ints:</p>
<pre><code>{1: ['5.1', '3.5', '1.4', '0.2', 'Iris-setosa'], 2: ['4.9', '3.0', '1.4', '0.2', 'Iris-setosa'], 3: ['4.7', '3.2', '1.3', '0.2', 'Iris-setosa'], 4: ['4.6', '3.1', '1.5', '0.2', 'Iris-setosa'], 5: ['5.0', '3.6', '1.4', '0.2', 'Iris-setosa'], 6: ['5.4', '3.9', '1.7', '0.4', 'Iris-setosa'], 7: ['4.6', '3.4', '1.4', '0.3', 'Iris-setosa']}
</code></pre>
<p>All values that belong to one key represent something different. Now what I need to do is access first values of each key and sum them, for example:</p>
<pre><code>5.1+4.9+4.7+4.6+5.0+5.4+4.6
</code></pre>
<p>I know that's total basics of Python, but I'm a beginner. Can someone recommend the easiest way of iterating through this dictionary to make the equation beneath possible? </p>
</div>
<div class="post-text" itemprop="text">
<p>list.txt:</p>
<pre><code>5.1,3.5,1.4,0.2,Iris-setosa
4.9,3.0,1.4,0.2,Iris-setosa
4.7,3.2,1.3,0.2,Iris-setosa
4.6,3.1,1.5,0.2,Iris-setosa
5.0,3.6,1.4,0.2,Iris-setosa
5.4,3.9,1.7,0.4,Iris-setosa
4.6,3.4,1.4,0.3,Iris-setosa
</code></pre>
<p>Since you haven't mentioned the dict part, I gave it a shot:</p>
<pre><code>logFile = "list.txt"

with open(logFile) as f:
    content = f.readlines()

# you may also want to remove empty lines
content = [l.strip() for l in content if l.strip()]
dict_ = {}
line_num = 1
for line in content:
    dict_[line_num] = line
    line_num += 1
print(dict_)
</code></pre>
<p>and then the sum part:</p>
<pre><code>sum = 0
for e in dict_:
    sum += float(dict_[e].split(",")[0])
    print(float(dict_[e].split(",")[0]))

print("The sum: {}".format(sum))
</code></pre>
<p><strong>OUTPUT</strong>:</p>
<pre><code>{1: '5.1,3.5,1.4,0.2,Iris-setosa', 2: '4.9,3.0,1.4,0.2,Iris-setosa', 3: '4.7,3.2,1.3,0.2,Iris-setosa', 4: '4.6,3.1,1.5,0.2,Iris-setosa', 5: '5.0,3.6,1.4,0.2,Iris-setosa', 6: '5.4,3.9,1.7,0.4,Iris-setosa', 7: '4.6,3.4,1.4,0.3,Iris-setosa'}
5.1
4.9
4.7
4.6
5.0
5.4
4.6
The sum: 34.3
</code></pre>
<p><strong>OR (one liner)</strong>:</p>
<pre><code>print(sum([float(dict_[e].split(",")[0]) for e in dict_]))
</code></pre>
<p><strong>EDIT 2</strong>:</p>
<p>For the given <code>dict</code>:</p>
<pre><code>dict_ = {1: ['5.1', '3.5', '1.4', '0.2', 'Iris-setosa'], 2: ['4.9', '3.0', '1.4', '0.2', 'Iris-setosa'], 3: ['4.7', '3.2', '1.3', '0.2', 'Iris-setosa'], 4: ['4.6', '3.1', '1.5', '0.2', 'Iris-setosa'], 5: ['5.0', '3.6', '1.4', '0.2', 'Iris-setosa'], 6: ['5.4', '3.9', '1.7', '0.4', 'Iris-setosa'], 7: ['4.6', '3.4', '1.4', '0.3', 'Iris-setosa']}


print(sum([float(dict_[e][0]) for e in dict_]))
</code></pre>
<p><strong>OUTPUT</strong>:</p>
<pre><code>34.3
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>I can't edit my question due to low reputation, so here is the code I have:</p>
<pre><code>my_dict = {}
i=1
with open("iris.txt") as f:
    for line in f:
        items = line.strip('\n').split(',')
        key, values = i, items[0:]
        my_dict.setdefault(key, []).extend(values)
        i+=1

print(my_dict)

sum([float(my_dict[i][0]) for i in my_dict])
</code></pre>
<p>what now generates an error: "    sum([float(my_dict[i][0]) for i in my_dict])
ValueError: could not convert string to float: "
Where may be the problem?</p>
</div>
<div class="post-text" itemprop="text">
<h1>First read file and make dictionary</h1>
<pre><code>data_dict = dict()
with open("file_path") as ff:
    data = ff.readlines()
    for i, d in enumerate(data):
        print i+1, d
        d = d.replace("\n", "")
        data_dict[i+1] = d.split(',')
print data_dict
</code></pre>
<h1>Sum of keys</h1>
<pre><code>print sum([float(data_dict[k][0]) for k in data_dict])
</code></pre>
<h1>Output</h1>
<p>1 5.1,3.5,1.4,0.2,Iris-setosa</p>
<p>2 4.9,3.0,1.4,0.2,Iris-setosa</p>
<p>3 4.7,3.2,1.3,0.2,Iris-setosa</p>
<p>4 4.6,3.1,1.5,0.2,Iris-setosa</p>
<p>5 5.0,3.6,1.4,0.2,Iris-setosa</p>
<p>6 5.4,3.9,1.7,0.4,Iris-setosa</p>
<p>7 4.6,3.4,1.4,0.3,Iris-setosa</p>
<p>{1: ['5.1', '3.5', '1.4', '0.2', 'Iris-setosa'], 2: ['4.9', '3.0', '1.4', '0.2', 'Iris-setosa'], 3: ['4.7', '3.2', '1.3', '0.2', 'Iris-setosa'], 4: ['4.6', '3.1', '1.5', '0.2', 'Iris-setosa'], 5: ['5.0', '3.6', '1.4', '0.2', 'Iris-setosa'], 6: ['5.4', '3.9', '1.7', '0.4', 'Iris-setosa'], 7: ['4.6', '3.4', '1.4', '0.3', 'Iris-setosa']}</p>
<p>34.3</p>
</div>
<span class="comment-copy">Where is your code that does what you mentioned?</span>
<span class="comment-copy">The way I enter the values into a dictionary just wasn't relevant to the question. But if you need it, it's:  <code>my_dict = {}  i=1 with open("iris.txt") as f:     for line in f:         items = line.strip('\n').split(',')         key, values = i, items[0:]         my_dict.setdefault(key, []).extend(values)         i+=1</code></span>
<span class="comment-copy">see if any of the answers posted below helps?</span>
<span class="comment-copy">you may consider accepting an answer that helped you: <a href="https://meta.stackexchange.com/questions/5234/how-does-accepting-an-answer-work" title="how does accepting an answer work">meta.stackexchange.com/questions/5234/…</a> cheers</span>
<span class="comment-copy">This does not produces any errors and works as expected. Please do not post an answer for an edit, you can always edit your question. PS. Kindly see the answer I posted (Edit 2), it is tested and works as expected.</span>
<span class="comment-copy">While what you have written may answer the question, however it does seem a little lacking in explanation and may illicit confusion to other users. Can you please expand upon your answer so that it is clearer and more accessible? This will make for better answers and help future users understand how the problem was solved.</span>
