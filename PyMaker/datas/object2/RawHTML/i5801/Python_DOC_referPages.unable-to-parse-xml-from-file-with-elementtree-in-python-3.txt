<div class="post-text" itemprop="text">
<p>I'm trying the module ElementTree of Python 3 to parse XML but I can't make it work.</p>
<p>I have a XML file:</p>
<pre><code>&lt;?xml version="1.0" encoding="UTF-8"?&gt;
&lt;root&gt;
    &lt;show&gt;
        &lt;name&gt;Name 1&lt;/name&gt;
        &lt;number&gt;79&lt;/number&gt;
    &lt;/show&gt;
    &lt;show&gt;
        &lt;name&gt;Name 2&lt;/name&gt;
        &lt;number&gt;382&lt;/number&gt;
    &lt;/show&gt;
&lt;/root&gt;
</code></pre>
<p>And I want to check, given a name, if it exist on the XML and get the number. So when I write the code, I do:</p>
<pre><code>    show_input = show.lower()
    file_to_parse = findXMLshowFile(show_input)
    file_path = os.path.join(LOCATION_URL, file_to_parse)
    root = etree.parse(file_path)
    shows = root.findall('show')
    for element in shows:
        name = element.find('name').text
        if name.lower() == show:
            show_name = name
            show_number = int(element.find('number').text)
            break
    return [show_name, show_number]
</code></pre>
<p>I get empty name and number. Debugging, when I do <code>shows = root.findall('show')</code> it has 2 objects but they are empty.</p>
<p>I'm reading the <a href="https://docs.python.org/3/library/xml.etree.elementtree.html#finding-interesting-elements" rel="nofollow noreferrer">documentation</a> and I don't know what I'm doing wrong. Someone can tell me where it's my mistake?</p>
<p>Regards</p>
</div>
<div class="post-text" itemprop="text">
<p>Thanks to @RomanPerekhrest, I was doing everything right but I was checking the wrong variable.</p>
<p>Replacing <code>name.lower() == show:</code> with <code>name.lower() == show_input</code> was the solution.</p>
</div>
<span class="comment-copy">at least, do <code>name.lower() == show_input</code> instead of <code>name.lower() == show</code></span>
<span class="comment-copy">@RomanPerekhrest I was so obfuscated that I did not realize, that was the mistaque. Thank you very much.</span>
