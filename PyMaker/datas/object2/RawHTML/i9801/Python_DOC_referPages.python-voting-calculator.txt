<div class="post-text" itemprop="text">
<p>I am trying to make a calculator that determines whether the user can vote after entering their date of birth. I have tried using some other posts to get where I am currently but I am now stuck.</p>
<p><strong>I have this error:</strong> </p>
<pre><code>TypeError: unorderable types: datetime.timedelta() &gt;= int()
</code></pre>
<p><strong>And my code is:</strong></p>
<pre><code>from datetime import date

print("Please enter your birthday")
bd_y = input("Year: ")
bd_m = input("Month (1-12): ")
bd_d = input("Date: ")

now = date.today()

birthdate = date(int(bd_y), int(bd_m), int(bd_d))


age =  now-birthdate

print("Your age is", age)

if age &gt;= 6570:
    print("You can vote!")
else:
    print("Your not old enough! Sorry!")
</code></pre>
<p>Any help you can give would be great, thanks!</p>
</div>
<div class="post-text" itemprop="text">
<p><code>age</code> is a Python <a href="https://docs.python.org/3/library/datetime.html#datetime.timedelta" rel="nofollow noreferrer"><code>datetime.timedelta</code></a> and cannot be compared with an int directly.</p>
<p><a href="https://docs.python.org/3/library/datetime.html#datetime.timedelta.resolution" rel="nofollow noreferrer"><code>age.days</code></a> is the integer representation of the number of days. This can be compared to another integer.</p>
<p>It's worth noting that this does not include nor account for leap years, nor does it actually determine whether you can vote, as it takes the current time, rather than your age at a certain cutoff date. Additional avenues for improvement.</p>
</div>
<div class="post-text" itemprop="text">
<p>Try this:
    from datetime import date</p>
<pre><code>print("Please enter your birthday")
bd_y = input("Year: ")
bd_m = input("Month (1-12): ")
bd_d = input("Date: ")

now = date.today()

birthdate = date(int(bd_y), int(bd_m), int(bd_d))


age = now-birthdate

print("Your age is", age)

if age.days &gt;= 6570:
    print("You can vote!")
else:
    print("Your not old enough! Sorry!")
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p><code>age</code> is an object:</p>
<pre><code>In [256]: age
Out[256]: datetime.timedelta(10188)
</code></pre>
<p>To get the total seconds, for example:</p>
<pre><code>In [264]: age.total_seconds()
Out[264]: 880243200.0
</code></pre>
<p>To get the days:</p>
<pre><code>In [269]: age.days
Out[269]: 10188
</code></pre>
<p>To see the attributes and methods of an object like <code>age</code> just do <code>dir(age)</code></p>
</div>
<div class="post-text" itemprop="text">
<p>Why dont you make things simple and just subtract the age(year only) from the current year? See below how to get current year only:</p>
<pre><code>import datetime

print("Please enter your birthday")
bd_y = input("Year: ")
bd_m = input("Month (1-12): ")
bd_d = input("Date: ")

currentyear = datetime.date.today().year

birthdate = bd_y


age =  currentyear - int(bd_y)

print("Your age is", age)

if age &gt;= 6570:
    print("You can vote!")
else:
    print("Your not old enough! Sorry!")
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>The issue is your variable <code>age</code> is not simply the integer number of days. Please refer to <a href="https://docs.python.org/2/library/datetime.html" rel="nofollow noreferrer">here</a> to learn more about the datatype.</p>
<p>The solution to your problem is choosing the correct attribute. Long story short:</p>
<p><code>if age.days &gt;= 6570:</code></p>
<p>should resolve your immediate issue. Also it would be good practice to use variables to represent 6570 or at least make it clear what that number is (18 years * 365 days) in some way.</p>
</div>
<span class="comment-copy"><code>type(age.days) = &lt;class 'int'&gt;</code>, so typecasting is not necessary.</span>
