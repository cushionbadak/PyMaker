<div class="post-text" itemprop="text">
<p>I would like to check if string "cute" matches part of the string in line. Currently, it does, but it prints it even the line if "cute" is up against other characters.</p>
<p>This is my code:</p>
<pre><code>if "cute" in line:
    print(line)
</code></pre>
<p>But it may print lines such as
"cuteffi" or "cutei934". I have tried checking for "cute" only like this:</p>
<pre><code>if "cute" == line:
    print(line)
</code></pre>
<p>But then this only compares the entire string, even though the string may have contents like
"cute i934", in which case I would want it to print the entire string because "cute" is by itself and spaced out from other characters.</p>
</div>
<div class="post-text" itemprop="text">
<p>You can iterate the lines of the file, <a href="https://docs.python.org/3/library/stdtypes.html#str.split" rel="nofollow noreferrer">split the line based on whitespace characters</a>, and check if any of the split strings equal to the word of interest.</p>
<pre><code>with open("input.txt", "r") as f:
    for line in f:
        split_words = line.split()
        if "cute" in split_words:
            print(line)
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>Easiest, use <a href="https://docs.python.org/3/library/stdtypes.html#str.split" rel="nofollow noreferrer"><code>split</code></a>:</p>
<pre><code>if 'cute' in line.split():
     # ...
</code></pre>
<p>Or use <a href="https://docs.python.org/3/library/re.html" rel="nofollow noreferrer">regular expressions</a>:</p>
<pre><code>import re

if re.search(r'(\s|^)cute(\s|$)', line):
    # ...
</code></pre>
<p>this regex makes sure 'cute' is surrounded by whitespace or the beginning/end of the line. Using word boundaries alone would still match with surrounding punctuation.</p>
</div>
<div class="post-text" itemprop="text">
<p>You need to regex in this case. By placing string <code>cute</code> in between two <code>\b</code> makes strict match of <code>cute</code></p>
<pre><code>&gt;&gt;&gt; line="i am cutei934"
&gt;&gt;&gt; 
&gt;&gt;&gt; if (re.findall(r'\bcute\b',line)):
    print line


&gt;&gt;&gt; line="i am cute"
&gt;&gt;&gt; if (re.findall(r'\bcute\b',line)):
    print line


i am cute
&gt;&gt;&gt; line="i am 123cute"
&gt;&gt;&gt; if (re.findall(r'\bcute\b',line)):
    print line


&gt;&gt;&gt; line="i am cute right?"
&gt;&gt;&gt; if (re.findall(r'\bcute\b',line)):
    print line


i am cute right?
&gt;&gt;&gt; 
</code></pre>
</div>
<span class="comment-copy">Did you try splitting the line with space and checking if the individual split elements equal to <code>cute</code>?</span>
<span class="comment-copy">Second thefourtheye.  You can even use "if 'cute' in the_line.split()"</span>
<span class="comment-copy">Even if we split the line <code>cutei934</code> this concern is remain right?? @SamanthaAtkins</span>
<span class="comment-copy">Thank you! This works. Is there a way I can make it case-insensitive easily?</span>
<span class="comment-copy">Convert them to lowercase and verify, use <code>lower()</code> method.</span>
<span class="comment-copy">Can anyone solve my doubt ,  <code>line.split()</code> will have word <code>cuteiffi</code> then above code also works , is it the way you wanted @ally</span>
<span class="comment-copy">@Sanket : If <code>line.split()</code>  has <code>cuteiffi</code>  if condition will not match since <code>if "cute" in split_words:</code> try to find <code>cute</code> in the list and will skip if condition since condition doesn't satisfy</span>
<span class="comment-copy">Thank you @SandeepLade, now I got it, where I was wrong.</span>
<span class="comment-copy">@kvk30 : method <code>find</code> is not available in <code>re</code> module. Instead we can use method <code>search</code></span>
<span class="comment-copy"><code>re</code> module does not have <code>find()</code> @kvk30</span>
<span class="comment-copy">The regex solution with word boundaries will match lines like <code>'foo-cute'</code> however.</span>
<span class="comment-copy">I think it would be better, if we use <code>find</code> in <code>string</code> method instead of <code>findall</code> in <code>re</code> . Correct me, if I am wrong. Updated comment please look in to this :)</span>
