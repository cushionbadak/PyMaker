<div class="post-text" itemprop="text">
<p>I'm trying to think of a way to use a certain index of a list as a condition to trigger something else in python.</p>
<p>For example:</p>
<pre><code>for val in mylist:
   do something with val (the actual list entry)
   if len(mylist)&gt;= 4 and val has position 2 or bigger in mylist:
       do something else
</code></pre>
<p>It's important that the if loop remains valid from position 2 until the end of mylist.</p>
<p>what would be a pythonic way to do this?</p>
</div>
<div class="post-text" itemprop="text">
<pre><code>for i, val in enumerate(my_list):
    do_something # will do this always
    if i &gt;= 2:
        do_also_something_else # will be executed only when index is 2 or greater
</code></pre>
</div>
<span class="comment-copy">Something like this ? <code>mylist.index(val) &gt;= 2</code></span>
<span class="comment-copy">that's great and simple! However, I need <code>val</code> as the actual value in the list for <code>do something</code></span>
<span class="comment-copy">@Shaun the above code will make sure <code>val</code> is the actual list value</span>
<span class="comment-copy">@RobinZigmond ah I see! in <code>do_something_else</code> how can I use the values starting from position 2 upwards? <code>my_list[i]</code>?</span>
<span class="comment-copy">@Shaun - the <code>if (i &gt;= 2)</code> will ensure that automatically. Perhaps you should look up the documentation for <code>enumerate</code> so you understand this better: <a href="https://docs.python.org/3/library/functions.html#enumerate" rel="nofollow noreferrer">docs.python.org/3/library/functions.html#enumerate</a></span>
