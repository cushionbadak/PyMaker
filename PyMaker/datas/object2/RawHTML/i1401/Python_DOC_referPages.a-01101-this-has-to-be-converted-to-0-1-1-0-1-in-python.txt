<div class="post-text" itemprop="text">
<blockquote>
<p>convert a <em>binary string</em> into list in <strong>python</strong><br/>
  example '10011' is to be converted as <strong>list</strong> ['1','0','0','1','1'] ?</p>
</blockquote>
</div>
<div class="post-text" itemprop="text">
<p>There is a function called list to do this directly.This can convert any string into list of characters.</p>
<pre><code>list('01101')
</code></pre>
<p>will return </p>
<pre><code>['0', '1', '1', '0', '1']
</code></pre>
<p>One more way is</p>
<pre><code>a = '01101'
a_list=[]
for item in a:
    a_list.append(item)
print(a_list)
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p><a href="https://docs.python.org/3/library/stdtypes.html#list" rel="nofollow noreferrer">list</a> does exactly that:</p>
<blockquote>
<p>The constructor builds a list whose items are the same and in the same
  order as iterable’s items</p>
</blockquote>
<pre><code>a = '01101'
print(list(a))
</code></pre>
<p>Output:</p>
<pre><code>['0', '1', '1', '0', '1']
</code></pre>
</div>
<span class="comment-copy">did you try <code>list(a)</code> ?</span>
<span class="comment-copy">Possible duplicate of <a href="https://stackoverflow.com/questions/4978787/how-to-split-a-string-into-array-of-characters">How to split a string into array of characters?</a></span>
<span class="comment-copy">For the second example, why do you need <code>i</code>? <code>for item in a: list.append(item)</code> would be fine. Also naming a list <code>list</code> just shadows the builtin function <a href="https://docs.python.org/3/library/functions.html#func-list" rel="nofollow noreferrer"><code>list()</code></a>, which is never a good idea.</span>
<span class="comment-copy">@RoadRunner just wondering if <code>list()</code> is considered a function. The documentation says <i>"Rather than being a function, list is actually a mutable sequence type"</i>, but also includes it in <a href="https://docs.python.org/3/library/functions.html" rel="nofollow noreferrer">Built-in functions list</a></span>
<span class="comment-copy">@Ayxan I guess it can be looked that way. This might be a good question to ask honestly, I'm curious to see what others have to say.</span>
<span class="comment-copy">@RoadRunner asked it. Here is the <a href="https://stackoverflow.com/q/53649958/10147399">question</a></span>
<span class="comment-copy">@RoadRunner I didn't give much time to it yesterday.. I taken your inputs.Thank you</span>
