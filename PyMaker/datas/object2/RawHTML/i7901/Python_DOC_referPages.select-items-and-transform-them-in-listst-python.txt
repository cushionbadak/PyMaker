<div class="post-text" itemprop="text">
<p>I have a list like this: </p>
<pre><code>a = [1,2,1,2,1,2,1,2]
</code></pre>
<p>and I want to get a list </p>
<pre><code>b= a = [1,1,1,1,2,2,2,2]
</code></pre>
<p>I TRIED THIS CODE</p>
<pre><code>b = []
for j in range(4):
    for i in xrange(0,len(a),4):
        b.append(a[j])
</code></pre>
<p>This is the result <code>[1, 1, 2, 2, 1, 1, 2, 2]</code></p>
<p>what shoul id do</p>
</div>
<div class="post-text" itemprop="text">
<pre><code>a = [1.0, 1.1, 2.0, 2.1, 3.0, 3.1, 4.0, 4.1]
b = a[::2] + a[1::2]
</code></pre>
<p>gives</p>
<pre><code>[1.0, 2.0, 3.0, 4.0, 1.1, 2.1, 3.1, 4.1]
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>Here's the code, iterate over all even elements and then over all odd elements:</p>
<pre><code>a = ['x1', 'y1', 'x2', 'y2', 'x3', 'y3', 'x4', 'y4']
b = []

for j in range(2):
    for i in range(j,len(a),2):
        b.append(a[i])

print(b)
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>Perhaps you want to sort the list?</p>
<pre><code>a = [1,2,1,2,1,2,1,2]
sorted(a)
</code></pre>
<p>See: <a href="https://docs.python.org/3/library/functions.html#sorted" rel="nofollow noreferrer">https://docs.python.org/3/library/functions.html#sorted</a></p>
</div>
<span class="comment-copy">what is the logic behind this transformation? is it sorting or is it something else? You can get to this result in many, many ways.</span>
<span class="comment-copy">... you want <code>[x1 y1 x2 y2 x3 y3 x4 y4] -&gt; [x1 x2 x3 x4 y1 y2 y3 y4]</code>?</span>
<span class="comment-copy">Thank you i want x1y1x3y3x2y2x4y4</span>
