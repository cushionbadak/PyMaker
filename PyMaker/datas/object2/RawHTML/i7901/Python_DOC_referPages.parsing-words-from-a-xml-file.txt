<div class="post-text" itemprop="text">
<p>I have a xml file called "interview.xml" that looks like this:</p>
<pre><code>&lt;SpeechSegment spkid="S0"&gt;
    &lt;Word dur="0.22" stime="0.44"&gt;oh&lt;/Word&gt;
    &lt;Word dur="0.27" stime="1.67"&gt;bedankt&lt;/Word&gt;
    &lt;Word dur="0.3" stime="2.03"&gt;voor&lt;/Word&gt;
    &lt;Word dur="0.53" stime="2.61"&gt;deelname&lt;/Word&gt;
    &lt;Word dur="0.22" stime="3.15"&gt;aan&lt;/Word&gt;
    &lt;Word dur="0.23" stime="3.39"&gt;de&lt;/Word&gt;
    &lt;Word dur="0.14" stime="6.15"&gt;want&lt;/Word&gt;
    &lt;Word dur="0.07" stime="6.29"&gt;ik&lt;/Word&gt;
    &lt;Word dur="0.09" stime="6.36"&gt;wil&lt;/Word&gt;
    &lt;Word dur="0.06" stime="6.45"&gt;je&lt;/Word&gt;
    &lt;Word dur="0.42" stime="6.51"&gt;graag&lt;/Word&gt;
    &lt;Word dur="0.2" stime="7.52"&gt;en&lt;/Word&gt;
&lt;/SpeechSegment&gt;
</code></pre>
<p>What I would like to do now is parse all the words from this segment, so I want to create a list like <code>["oh", "bedankt", "voor", etc...]</code></p>
<p>I tried this:</p>
<pre><code>import xml.etree.ElementTree
e = xml.etree.ElementTree.parse('Interview_short.xml').getroot()

for atype in e.findall('type'):
    print(atype.get('word'))
</code></pre>
<p>But this does not give me the output I am looking for. Any thoughts what adjustments I should make?</p>
</div>
<div class="post-text" itemprop="text">
<p>Use <a href="https://docs.python.org/3/library/xml.etree.elementtree.html" rel="nofollow noreferrer"><code>ElementTree</code></a>.</p>
<p>Solution:</p>
<pre><code>import xml.etree.ElementTree as ET
root = ET.fromstring(xml_string)
required_list = [child.text for child in root]
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>I have no idea why <code>findall('type')</code> while the XML doesn't contain any <code>&lt;type&gt;</code> element. According to the XML posted, it should've been <code>findall('Word')</code>. Here is a minimal but complete codes for demo :</p>
<pre><code>raw = '''&lt;SpeechSegment spkid="S0"&gt;
    &lt;Word dur="0.22" stime="0.44"&gt;oh&lt;/Word&gt;
    &lt;Word dur="0.27" stime="1.67"&gt;bedankt&lt;/Word&gt;
    &lt;Word dur="0.3" stime="2.03"&gt;voor&lt;/Word&gt;
    &lt;Word dur="0.53" stime="2.61"&gt;deelname&lt;/Word&gt;
    &lt;Word dur="0.22" stime="3.15"&gt;aan&lt;/Word&gt;
    &lt;Word dur="0.23" stime="3.39"&gt;de&lt;/Word&gt;
    &lt;Word dur="0.14" stime="6.15"&gt;want&lt;/Word&gt;
    &lt;Word dur="0.07" stime="6.29"&gt;ik&lt;/Word&gt;
    &lt;Word dur="0.09" stime="6.36"&gt;wil&lt;/Word&gt;
    &lt;Word dur="0.06" stime="6.45"&gt;je&lt;/Word&gt;
    &lt;Word dur="0.42" stime="6.51"&gt;graag&lt;/Word&gt;
    &lt;Word dur="0.2" stime="7.52"&gt;en&lt;/Word&gt;
&lt;/SpeechSegment&gt;'''

from xml.etree import ElementTree as ET
root = ET.fromstring(raw)
result = [word.text for word in root.findall('Word')]
print result
</code></pre>
<p><strong><kbd><a href="https://eval.in/805771" rel="nofollow noreferrer"><code>eval.in demo</code></a></kbd></strong></p>
<p><strong>output :</strong></p>
<pre><code>['oh', 'bedankt', 'voor', 'deelname', 'aan', 'de', 'want', 'ik', 'wil', 'je', 'graag', 'en']
</code></pre>
</div>
<span class="comment-copy">You have to access the xml tag content with elements.text</span>
<span class="comment-copy">So when you have your SpeechSegment element, with e.findall('SpeechSegment') for example and than comparing against the attribute with element.attrib["spkid"] == "S0", you do something like wordlist=[ word.text for word in SS]</span>
<span class="comment-copy">Many thanks for your elaborated answer. The last issue I have is replicating the data you have from loading it from an actual xml file. Now if have the exact same data in an xml file which i try to import by: import xml.etree.ElementTree as ET raw2 = ET.parse('Interview_short.xml') but this gives me: &lt;xml.etree.ElementTree.ElementTree object at 0x0000000001EC7DD8&gt; ... Any thoughts?</span>
<span class="comment-copy"><code>root = raw2.getroot()</code> and the rest should be the same</span>
