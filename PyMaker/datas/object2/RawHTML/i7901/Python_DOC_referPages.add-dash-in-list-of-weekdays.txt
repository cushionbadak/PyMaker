<div class="post-text" itemprop="text">
<p>I have a sequence:</p>
<pre><code>WEEKDAYS = [
    'Sun',
    'Mon',
    'Tue',
    'Wed',
    'Thu',
    'Fri',
    'Sat',
]
</code></pre>
<p>There are inputs like:</p>
<pre><code>['Sun', 'Thu', 'Fri', 'Sat']
</code></pre>
<p>And I want output like:</p>
<pre><code>Sun, Thu - Sat
</code></pre>
<p>I am very confused how to do it.</p>
</div>
<div class="post-text" itemprop="text">
<p>Here's one way to do it with <a href="https://docs.python.org/3/library/itertools.html#itertools.groupby" rel="nofollow noreferrer"><code>itertools.groupby</code></a>. The items from the input list are clustered based on their distribution in the <code>WEEKDAYS</code> by grouping those with an index difference of 1:</p>
<pre><code>from itertools import count, groupby

lst = ['Sun', 'Thu', 'Fri', 'Sat']
c = count()
new_lst = []
for _, g in groupby(lst, lambda x: WEEKDAYS.index(x)-next(c)):
    g = list(g)
    if len(g) &gt; 2:
        new_lst.append(g[0] + '-' + g[-1])
    else:
        new_lst.extend(g)
print(new_lst)
# ['Sun', 'Thu-Sat']
</code></pre>
<p>You can make list <code>WEEKDAYS</code> a dictionary if you need to avoid multiple <code>list.index</code> calls as in the current approach.</p>
</div>
<span class="comment-copy">please explain how inputs are derived</span>
<span class="comment-copy">More simply, I mean maybe: i have <code>mylist = [0, 5, 6, 7, 8]</code>. 5 to 8 is a sequential number whereas 0 with 6 is not, so the output is like <code>0, 5-8</code>.</span>
<span class="comment-copy">Do you want the ranges to wrap around?  E.g., should the input <code>['Fri', 'Sat', 'Sun']</code> produce the output <code>Fri - Sun</code>?</span>
