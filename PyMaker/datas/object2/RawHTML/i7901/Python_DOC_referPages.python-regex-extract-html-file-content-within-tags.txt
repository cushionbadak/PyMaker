<div class="post-text" itemprop="text">
<p>I have lots of html format files in a folder. I need to check whether each of them contains this tag: </p>
<pre><code>&lt;strong&gt;QQ&lt;/strong&gt;
</code></pre>
<p>and need to extract only "QQ" and its content.
I first read one of the files to test, but it seems my regex does not match.
If I replaced fo_read as the tag </p>
<pre><code>&lt;strong&gt;QQ&lt;/strong&gt; 
</code></pre>
<p>it will match though.</p>
<pre><code>fo = open('4251-fu.html', "r")
fo_read = fo.read()
m = re.search('&lt;strong&gt;(QQ)&lt;/strong&gt;', fo_read)
if m:
    print 'Match found: ', m.group(1)
else:
    print 'No match'
fo.close()
</code></pre>
</div>
<div class="post-text" itemprop="text">
<pre><code>result = soup.find("strong", string=re.compile("Question-and-Answer Session"))
if result:
    print("Question-and-Answer Session")
    # for the rest of text in the parent
    rest = result.parent.text.split("Question-and-Answer Session")[-1].strip()
    print(rest)
else:
    print("no match")
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>You can try with BeautifulSoup:</p>
<pre><code>from bs4 import BeautifulSoup
f = open('4251-fu.html',mode = 'r')
soup = BeautifulSoup(f, 'lxml')
search_result = [str(e) for e in soup.find_all('strong')]
print search_result
if '&lt;strong&gt;Question-and-Answer Session&lt;/strong&gt;' in search_result:
    print 'Match found'
else:
    print 'No match'
f.close()
</code></pre>
<p>Output:</p>
<pre><code>['&lt;strong&gt;Question-and-Answer Session1&lt;/strong&gt;', '&lt;strong&gt;Question-and-Answer Session&lt;/strong&gt;', '&lt;strong&gt;Question-and-Answer Session3&lt;/strong&gt;']
Match found
</code></pre>
</div>
<span class="comment-copy">Have you considered using an html parser instead? <a href="https://stackoverflow.com/a/1732454/5067311">Using regex to parse HTML is scary</a>.</span>
<span class="comment-copy">I have beautifulsoup, but there are several strong tags in the html though. How can it work?</span>
<span class="comment-copy">If you have multiple tags than that's another reason to use an HTML parser instead. I'm not familiar with the subject, but either the BS4 docs or <a href="https://docs.python.org/3/library/html.parser.html" rel="nofollow noreferrer">the standard html module</a> (oops: <a href="https://docs.python.org/2/library/htmlparser.html" rel="nofollow noreferrer">python2 for you</a>) docs and some targeted googling should be helpful.</span>
<span class="comment-copy">Do you need extract &lt;strong&gt;Question-and-Answer Session&lt;/strong&gt; or the text the following it? If former, one paragraph, few, a section ..until next enclosing div etc...?</span>
<span class="comment-copy">I need the text following it. But I just think maybe need to confirm if the file has this tag first? because not all of them has this tag though</span>
<span class="comment-copy">it returns [u'\n    Question-and-Answer Session\n   '], how can I only get Question-and-Answer Session?</span>
<span class="comment-copy">you could add a <code>.strip()</code> to the end of <code>result.parent.text.split(...)[-1]</code></span>
<span class="comment-copy">splitting is a bit hacky, try <code>next_sibling</code> for any serious project.... <a href="https://www.crummy.com/software/BeautifulSoup/bs4/doc/#going-sideways" rel="nofollow noreferrer">crummy.com/software/BeautifulSoup/bs4/doc/#going-sideways</a></span>
<span class="comment-copy">There are several strong tags, but I only want the one with Question-and-Answer Session</span>
<span class="comment-copy">But strong tags are in different places, not always in the beginning.</span>
<span class="comment-copy">it will find all strong tag in html file, no matter where it is.</span>
