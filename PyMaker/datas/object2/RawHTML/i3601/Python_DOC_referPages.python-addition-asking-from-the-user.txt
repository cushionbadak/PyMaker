<div class="post-text" itemprop="text">
<p>The math statement is "add x1 and x2"</p>
<p>The output of python is:<br/>
Enter the statement or "E" to exit the program: </p>
<p>Then user inputs:<br/>
add 35 and 5</p>
<p>The output is:<br/>
Answer: add 35 and 5 = 40</p>
<p>How to use python to code this output?<br/>
Enter the statement or "E" to exit the program: add 35 and 5<br/>
Answer: add 35 and 5 = 40</p>
</div>
<div class="post-text" itemprop="text">
<p>You'ĺl need to use the .split() to separate the user inputs contained in the string.
(<a href="https://docs.python.org/3/library/stdtypes.html#str.split" rel="nofollow noreferrer">https://docs.python.org/3/library/stdtypes.html#str.split</a>)</p>
<pre><code>user_input=input("enter the statement:")
x1=int(user_input.split()[0])
x2=int(user_input.split()[1])
print("Answer: Add {} and {} = {}".format(x1,x2,x1+x2))
</code></pre>
<p>But you should check that the user used "add" and "and" using if, to make sure the user statement was as you desired.</p>
</div>
<div class="post-text" itemprop="text">
<pre><code>action = input("Enter the statement [add] or E to exit the program: ")
if action == "add":
    x1 = int(input("enter the first number: "))
    x2 = int(input("enter the second number: "))
    print("Answer: Add {} and {} = {}". format(x1,x2,x1+x2))
elif action == "E" or action == "e":
    # exit action 
    exit("Goodbye")
else:
    # unexpected action
    exit("Error: invaild action {}".format(action))
</code></pre>
</div>
<span class="comment-copy">What's your question?</span>
<span class="comment-copy">What is <code>print:</code> - it isn't python?</span>
<span class="comment-copy">Welcome to SO, check out <a href="https://stackoverflow.com/help/how-to-ask">how to ask good questions</a></span>
