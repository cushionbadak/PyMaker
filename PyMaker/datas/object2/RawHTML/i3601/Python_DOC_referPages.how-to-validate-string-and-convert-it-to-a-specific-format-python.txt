<div class="post-text" itemprop="text">
<p>I'm trying to create code that will take text input, eliminate all whitespace characters (left and right, not those between characters) and then validate if the string has following format: 'XXX XXX' (where X is letter or digit). If it doesn't (say that it has XXX-XXX or XXXXXX format) it would convert it to such format. </p>
<p>I know that I can use <code>strip</code> to remove whitespace from a string. I also know how to check the length of a string and how to convert it, but I don't know how to validate a string after using <code>.strip()</code>.</p>
</div>
<div class="post-text" itemprop="text">
<p>Use regular expressions.</p>
<pre><code>&gt;&gt;&gt; import re
&gt;&gt;&gt; inp.strip() 
aB8-9uG
&gt;&gt;&gt; match = re.match(r"([0-9a-zA-Z]{3})(?:\-| |)([0-9a-zA-Z]{3})", _)
&gt;&gt;&gt; match
&lt;_sre.SRE_Match object; span=(0, 7), match='aB8-9uG'&gt;
&gt;&gt;&gt; match.group(1) + " " + match.group(2)
aB8 9uG
</code></pre>
</div>
<span class="comment-copy">You should use regular expressions (<code>re</code>).</span>
<span class="comment-copy">Please provide code examples of what you've tried. As DyZ said, you can use <a href="https://docs.python.org/3/library/re.html" rel="nofollow noreferrer">regular expressions</a>.</span>
<span class="comment-copy">Works just perfectly. Great thx Ivan!</span>
