<div class="post-text" itemprop="text">
<p>How to use random numbers and concatenating a string to another string random_number amount of times in python 3 ????</p>
<pre><code>import random
fave_word = 'hello' + 'world'

def randomword(fave_word : str) -&gt; str:
    '''Return something'''
    rand_num = random.randint(0,3)
    for i in range(rand_num):
        fave_word += 'Bob'
print(fave_word)
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>You need to return the string you've created. Or you could use <a href="https://docs.python.org/3/reference/simple_stmts.html#global" rel="nofollow noreferrer">global</a>, but I'd <a href="https://stackoverflow.com/questions/19158339/why-are-global-variables-evil/19158418#19158418">not recommend that approach</a>:</p>
<pre><code>&gt;&gt;&gt; import random
&gt;&gt;&gt; fave_word = 'hello' + 'world'
&gt;&gt;&gt;
&gt;&gt;&gt; def random_word(word : str) -&gt; str:
...     '''Return something'''
...     rand_num = random.randint(0,3)
...     for i in range(rand_num):
...         word += 'Bob'
...     return word
...
&gt;&gt;&gt; print(random_word(fave_word))
helloworldBobBobBob
&gt;&gt;&gt; print(random_word(fave_word))
helloworld
&gt;&gt;&gt; print(random_word(fave_word))
helloworldBobBob
</code></pre>
</div>
<span class="comment-copy">Not sure what your question is here.</span>
<span class="comment-copy">@M.S The little I can understand from the code is that you are generating a random number, then concatenating it with the string. But the question is still unclear! Kindly rephrase. Thanks.</span>
<span class="comment-copy">Your function is not returning anything and you have not called it.</span>
<span class="comment-copy">One '?' is enough. Try adding explanations / clarifications rather than extra ?'s.</span>
