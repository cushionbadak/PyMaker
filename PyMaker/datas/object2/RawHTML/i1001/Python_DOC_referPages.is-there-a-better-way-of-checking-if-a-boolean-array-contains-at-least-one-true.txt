<div class="post-text" itemprop="text">
<p>For example:</p>
<pre><code>boolarr = [True, True, False, False, True, True]
atleastonetrue = bool(sum(boolarr))
</code></pre>
<p>Is there a more elegant or conventional way of doing this?</p>
</div>
<div class="post-text" itemprop="text">
<p>Following the comments, <code>any(arr)</code> works.</p>
</div>
<span class="comment-copy">One solution is to use <a href="https://docs.python.org/3/library/functions.html#any" rel="nofollow noreferrer">any</a>. Not only is more <i>pythonic</i> is also faster, it stops once it finds the <code>True</code> value.</span>
<span class="comment-copy"><code>one = any(arr)</code></span>
<span class="comment-copy">Related to: <a href="https://stackoverflow.com/questions/31099561/test-if-all-elements-of-a-python-list-are-false" title="test if all elements of a python list are false">stackoverflow.com/questions/31099561/…</a></span>
