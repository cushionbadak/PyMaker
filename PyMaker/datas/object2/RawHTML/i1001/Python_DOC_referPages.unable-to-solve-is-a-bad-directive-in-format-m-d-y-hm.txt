<div class="post-text" itemprop="text">
<p>My code:</p>
<pre><code>crime = pd.read_csv("DATA/OAKLAND.csv", parse_dates=True)
crime['DATETIME'] = pd.to_datetime(crime['DATETIME'], format=time_format)
time_format = '%-m/%-d/%Y %H:%M'
</code></pre>
<p>Error that occurs:</p>
<pre><code>'-' is a bad directive in format '%-m/%-d/%Y %H:%M'
</code></pre>
<p>Some sample dates:</p>
<pre><code>10/10/2018 20:00
11/19/2018 14:15
12/10/2018 18:51
10/29/2018 7:30
10/7/2018 14:30
1/2/2019 0:01
</code></pre>
<p>Does anyone know what the right time_format is?  So far I am unable to get the correct code..
Thanks</p>
</div>
<div class="post-text" itemprop="text">
<p>The error tells you specifically the issue. Bad directive. </p>
<p>You should read the documentation here: <a href="https://docs.python.org/3/library/datetime.html#strftime-strptime-behavior" rel="nofollow noreferrer">https://docs.python.org/3/library/datetime.html#strftime-strptime-behavior</a></p>
<p>The directive is <code>%m</code> and not <code>%-m</code>. Removing the dashes will fix it.  </p>
</div>
<span class="comment-copy">does your time look like they have dashes in them?</span>
<span class="comment-copy">To be honest I'm not sure what you are asking for. But if I add the code errors='coerce', then dates are printed. Can I conclude from this that there are some errors in the dataset that caused the error to pop up?</span>
