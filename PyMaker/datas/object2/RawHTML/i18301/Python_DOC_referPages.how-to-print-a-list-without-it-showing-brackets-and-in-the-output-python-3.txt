<div class="post-text" itemprop="text">
<div class="question-status question-originals-of-duplicate">
<p>This question already has an answer here:</p>
<ul>
<li>
<a dir="ltr" href="/questions/18816813/adding-all-the-numbers-from-a-list-togethernot-summing-python-3-3-2">Adding all the numbers from a list together(not summing). Python 3.3.2 [duplicate]</a>
<span class="question-originals-answer-count">
                    4 answers
                </span>
</li>
</ul>
</div>
<p>So say I have a list named <code>myList</code> and it looks something like this :</p>
<pre><code>myList = ["a", "b", "c"]
</code></pre>
<p>How would you print it to the screen so it prints :</p>
<pre><code>abc 
</code></pre>
<p>(yes, no space inbetween)</p>
<p>If I use <code>print(myList)</code>
It prints the following:</p>
<pre><code>['a', 'b', 'c']
</code></pre>
<p>Help would be much appreciated.</p>
</div>
<div class="post-text" itemprop="text">
<p>With Python 3, you can pass a separator to <a href="http://docs.python.org/3/library/functions.html#print" rel="noreferrer"><code>print</code></a>. <a href="http://docs.python.org/3/tutorial/controlflow.html#unpacking-argument-lists" rel="noreferrer"><code>*</code> in front of <code>myList</code></a> causes <code>myList</code> to be unpacked into items:</p>
<pre><code>&gt;&gt;&gt; print(*myList, sep='')
abc
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>Use <a href="http://docs.python.org/3/library/stdtypes.html#str.join" rel="noreferrer"><code>str.join()</code></a>:</p>
<pre><code>''.join(myList)
</code></pre>
<p>Example:</p>
<pre><code>&gt;&gt;&gt; myList = ["a", "b", "c"]
&gt;&gt;&gt; print(''.join(myList))
abc
</code></pre>
<p>This <em>joins</em> every item listed separated by the string given.</p>
</div>
<span class="comment-copy">-2 wow so much hate.</span>
<span class="comment-copy">And now you have three upvotes :)</span>
<span class="comment-copy">It's always hard to read other people's minds, but I'm guessing they downvoted you because your question doesn't show any attempt to solve the problem yourself. I think that's unfair, because this isn't exactly an easy thing to search for. (Everything that seems like a good search turns up <code>repr</code>-vs.<code>-str</code> differences and other things that are just going to confuse you.) But some people are unfair and quick to downvote. In the future it might help to explain how you tried to search, and they'll be more sympathetic. Or you could just ignore them; it's not like the downvotes really hurt you…</span>
<span class="comment-copy">This is basically identical to one of your previous questions.</span>
<span class="comment-copy">I guess that is what earned the -1s?</span>
<span class="comment-copy">I don't know if it works, but +1 for making me go read the Python 3 spec :)</span>
<span class="comment-copy">@icedwater, added a wee bit of explanation!</span>
<span class="comment-copy">Wow, I can +1 the "same" answer twice if it's edited! Nice, but... seems like a bug to me. Then again, not all edits are this small...</span>
<span class="comment-copy">I could be wrong, but it looks like the OP wanted Python 3, so I added parens around your <code>print</code> and changed the docs link to 3.x. Otherwise, perfect answer.</span>
<span class="comment-copy">@abarnert Yep, thanks a lot. Title says 3.3.2</span>
<span class="comment-copy">Funny how we both missed that. If it's not in the tag, we either assume 2.x, or try to figure it out heuristically from the text, without even looking at the giant bold letters. :)</span>
<span class="comment-copy">@abarnert Heh, I'm a 2.7 user, so I always assume 2.x :D (kinda bad that I do that, but meh :3)</span>
