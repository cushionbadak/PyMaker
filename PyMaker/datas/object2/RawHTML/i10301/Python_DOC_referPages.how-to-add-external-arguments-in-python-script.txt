<div class="post-text" itemprop="text">
<p>I want the user to provide some external arguments while calling a python script from command line, what changes I suppose to make inside my python script to add this functionality?</p>
</div>
<div class="post-text" itemprop="text">
<p>I think this is what you are looking for</p>
<p>pass arguments in command line.</p>
<pre><code>$ python test.py arg1 arg2 arg3
</code></pre>
<p>access it using sys.argv which gives list of arguments that you entered above</p>
<pre><code>import sys

print 'Number of arguments:', len(sys.argv), 'arguments.'
print 'Argument List:', str(sys.argv)
</code></pre>
</div>
<span class="comment-copy">Maybe you should show your python script that is in question?</span>
<span class="comment-copy">Did you do some preliminary research first? Go through this doc, perhaps it'll help. <a href="https://docs.python.org/3/library/argparse.html" rel="nofollow noreferrer">docs.python.org/3/library/argparse.html</a></span>
<span class="comment-copy">Either use <code>sys.argv</code> or <code>argparse</code></span>
