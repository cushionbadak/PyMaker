<div class="post-text" itemprop="text">
<p>I have two columns in a dataframe df:</p>
<pre><code>   A    B
0  NaN  NaN
1  3.14 NaN
2  NaN  4.20
3  3.65 0.68
</code></pre>
<p>Intended result for df:</p>
<pre><code>   A    B    C
0  NaN  NaN  NaN
1  3.14 NaN  3.14
2  NaN  4.20 4.20
3  3.65 0.68 3.65
</code></pre>
<p>What's the pandas equivalent for? </p>
<pre><code>if(A == np.nan):
    if(B == np.nan):
        C = np.nan
    else: 
        C == B
else:
    C = A
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>Check with <code>bfill</code></p>
<pre><code>df['C']=df.bfill(1).iloc[:,0]

df
      A     B     C
0   NaN   NaN   NaN
1  3.14   NaN  3.14
2   NaN  4.20  4.20
3  3.65  0.68  3.65
</code></pre>
</div>
<span class="comment-copy">Thanks for this! Suppose I had other columns 'D', 'E', and so on in my original df... how would I create 'C' from only columns 'A' and 'B'?</span>
<span class="comment-copy">@plummms df[[‘A’,’B’]].bfill(1).iloc[,0]</span>
