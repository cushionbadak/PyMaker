<div class="post-text" itemprop="text">
<p>I want to make a list which contains all possible 4-digit combinations of digits 1-6</p>
<p>e.g.<code>&gt;&gt;&gt;list=(1111,1112,1113,.......,6666)</code></p>
<p>how am I going to do it? I run out of ideas</p>
</div>
<div class="post-text" itemprop="text">
<pre><code>result = []
for i in range(1, 7):
   for j in range(1, 7):
       for k in range(1, 7): 
           for z in range (1, 7):
               result.append(1000 * i + 100 * j + 10 * k + z)
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>This will do the job:</p>
<pre><code>import itertools

for i in itertools.product(range(1, 7), repeat=4):
    print(i)
</code></pre>
<p>Keep in mind that <code>i</code> is a Tuple</p>
</div>
<div class="post-text" itemprop="text">
<p>Make a list with all possible 1 digit numbers.</p>
<p>l1 = ['1', '2', '3', '4', '5', '6']</p>
<p>for each string in the above list, concatenate all possible digits, i.e.</p>
<p>l2 = []</p>
<p><code>
for l in l1:
    for i in range(6): 
        l2.append(l + str(i+1))
</code></p>
<p>repeat this two more times</p>
<p>l3 = []</p>
<p><code>
for l in l2: 
    for i in range(6): 
        l3.append(l + str(i+1))
</code></p>
<p>l4 = []</p>
<p><code>
for l in l3: 
    for i in range(6): 
        l4.append(l + str(i+1))
</code></p>
</div>
<span class="comment-copy">Please show what you have tried. What have you thought about doing or tried? Have you also thought about the fact that you're literally just generating a list of all numbers from <code>1111</code> to <code>6666</code>? You can achieve this with a simple loop and removing certain ones (anything containing 7-9). That isn't a <i>great</i> solution but it's something to get started with.</span>
<span class="comment-copy">Check out <a href="https://docs.python.org/2/library/itertools.html#itertools.combinations_with_replacement" rel="nofollow noreferrer"><code>itertools.combinations_with_replacement</code></a></span>
<span class="comment-copy">@martineau The answers in that duplicate question don't answer this one, as far as I've seen. <code>itertools.combinations</code> (or <code>combinations_with_replacement</code>, or <code>powerset</code>) won't do it, you really need <code>product</code>, as in M. Matt's answer here.</span>
<span class="comment-copy">Sounds like you want <a href="https://en.wikipedia.org/wiki/Permutation" rel="nofollow noreferrer">permutations</a> of the 4 digits. See the <a href="https://docs.python.org/3/library/itertools.html#itertools.permutations" rel="nofollow noreferrer">documentation</a> for <code>itertools.permutations</code>,</span>
<span class="comment-copy">You had a few syntax problems, and in Python it has to be <code>range(1, 7)</code> to include 6.</span>
<span class="comment-copy">Oh sorry you are right It is a simple mistake</span>
