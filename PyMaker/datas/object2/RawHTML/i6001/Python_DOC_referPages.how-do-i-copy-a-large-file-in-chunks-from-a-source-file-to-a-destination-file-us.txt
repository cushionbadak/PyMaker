<div class="post-text" itemprop="text">
<p>The file can be say 20GB. The traditional approach of importing,reading a file and then writing to another file is not feasible. </p>
</div>
<div class="post-text" itemprop="text">
<p>The <a href="https://docs.python.org/3/library/shutil.html" rel="nofollow noreferrer"><code>shutil</code></a> module has a function designed for this purpose:</p>
<pre><code>import shutil
shutil.copy(src, dst)
</code></pre>
</div>
<span class="comment-copy">anything wrong with standard library module <code>shutil</code> ? <a href="https://docs.python.org/3/library/shutil.html#shutil.copy" rel="nofollow noreferrer">docs.python.org/3/library/shutil.html#shutil.copy</a></span>
<span class="comment-copy">Well I am new to python and was trying out copying smaller files which got me thinking if I can do the same with larger files without using shutil.</span>
<span class="comment-copy">Down vote for what?</span>
<span class="comment-copy">Your answer is correct. shutil.copy is the best way to deal with files with large size. As per <a href="http://www.pythoncentral.io/how-to-copy-a-file-in-python-with-shutil/" rel="nofollow noreferrer">pythoncentral.io/how-to-copy-a-file-in-python-with-shutil</a>, the metrics shows that shutil is faster for larger files. +1 vote.</span>
<span class="comment-copy">Well I am new to python and was trying out copying smaller files which got me thinking if I can do the same with larger files without using shutil.</span>
<span class="comment-copy">Why bother? shutil is meant for this type of thing.</span>
