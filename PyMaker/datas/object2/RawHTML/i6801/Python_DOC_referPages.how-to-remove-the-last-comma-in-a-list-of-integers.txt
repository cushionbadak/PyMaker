<div class="post-text" itemprop="text">
<p>The list is supposed to get the square of each number. I've managed to do that but I need to remove the last comma in the sequence.</p>
<p>When I use this code:</p>
<pre><code>def multiplicator():
      for a in range(3, 20):
          b = (a*a)
          print(b, end=",")

multiplicator()
</code></pre>
<p>I get:</p>
<pre class="lang-none prettyprint-override"><code>9,16,25,36,49,64,81,100,121,144,169,196,225,256,289,324,361,
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>You can keep your loop and add a condition:</p>
<pre><code>def multiplicator():
    for a in range(3, 20):
        b = (a*a)
        print(b, end="")
        if a&lt;19: # if not the last element
          print(end=",") # print ","
    print() # print new line after everything

multiplicator() # =&gt; 9,16,25,36,49,64,81,100,121,144,169,196,225,256,289,324,361
</code></pre>
<p>You can also use a ternary condition to shorten the code:</p>
<pre><code>def multiplicator():
    for a in range(3, 20):
        b = (a*a)
        print(b, end="," if a&lt;19 else "")

    print() # print new line after everything
multiplicator() # =&gt; 9,16,25,36,49,64,81,100,121,144,169,196,225,256,289,324,361
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>You can use <code>str.join</code> to add a delimiter between strings which will handle not adding an extra one to the end.</p>
<pre><code>&gt;&gt;&gt; ','.join(str(a*a) for a in range(3, 20))
'9,16,25,36,49,64,81,100,121,144,169,196,225,256,289,324,361'
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>A simple option would be:</p>
<pre><code>def multiplicator():
    for a in range(3, 19):
        print(a*a, end=',')  # directly a*a, no need for an intermediate variable
    print(19*19)
</code></pre>
<p>A generic solution would be:</p>
<pre><code>def multiplicator(n):
    for a in range(3, n-1):
        print(a*a, end=',')
    print((n-1)*(n-1))
</code></pre>
<p><strong>Output:</strong></p>
<pre><code>&gt;&gt;&gt; multiplicator(20)
9,16,25,36,49,64,81,100,121,144,169,196,225,256,289,324,361
</code></pre>
<p><strong>Note:</strong></p>
<p>This is a simple and similar approach to what you are trying to do, but you should definitely go for <a href="https://stackoverflow.com/a/46141494/4575071">@CoryKramer's</a> answer using <a href="https://docs.python.org/3/library/stdtypes.html#str.join" rel="nofollow noreferrer">str.join()</a></p>
</div>
<span class="comment-copy">Thank you people for down-voting a correct answer without even commenting why, again.</span>
<span class="comment-copy">I didn't down-vote, but if you look at the other answers you should be able to figure it out all by yourself...just because an answer "works" doesn't automatically make it good.</span>
<span class="comment-copy">@martineau I could've made it better, I know how to use join, I intentionally left the code as-is because I know that the OP is a beginner and wants his code to be as close to his as possible, that's why he accepted my answer and not the others. And this is probably an assignement so they won't even allow him to use join.</span>
<span class="comment-copy">Rather than dumb-down your answers, in the future perhaps you should consider putting both ways in your answer—otherwise it makes it look like your own understanding of the language is also very elementary (which could raise suspicions that the advice you're giving isn't the best). In this particular instance, since the OP accepted your answer, you may actually be hindering OP's understanding of things by making them think they have to do what's in your answer, which is clearly sub-optimal, to accomplish their goal.</span>
