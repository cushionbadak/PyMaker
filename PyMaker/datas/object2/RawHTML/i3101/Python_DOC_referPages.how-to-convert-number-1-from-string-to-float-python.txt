<div class="post-text" itemprop="text">
<div class="question-status question-originals-of-duplicate">
<p>This question already has an answer here:</p>
<ul>
<li>
<a dir="ltr" href="/questions/6633523/how-can-i-convert-a-string-with-dot-and-comma-into-a-float-in-python">How can I convert a string with dot and comma into a float in Python</a>
<span class="question-originals-answer-count">
                    9 answers
                </span>
</li>
</ul>
</div>
<p>my question is simple.</p>
<p>I got my string :</p>
<pre><code>a = '0,0127'
</code></pre>
<p>I want to convert it to a number but when i compile </p>
<pre><code>float(a)
</code></pre>
<p>i got the following message error :</p>
<blockquote>
<p>ValueError: could not convert string to float: '0,0127'</p>
</blockquote>
<p>Is there another way to convert it to a number ?</p>
</div>
<div class="post-text" itemprop="text">
<p>Using <code>str.replace</code></p>
<p><strong>Ex:</strong></p>
<pre><code>a = '0,0127'
print(float(a.replace(",", ".")))
</code></pre>
<p><strong>Output:</strong></p>
<pre><code>0.0127
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>The reason this isn't working is because the decimal type only recognizes periods (<code>.</code>) for the decimal delimiter as this is what is common in, e.g., english. You could manually change the string or do</p>
<pre><code>a = a.replace(",", ".")
float(a)
</code></pre>
<p>Which should work.</p>
</div>
<span class="comment-copy"><code>print(float(a.replace(",", ".")))</code></span>
<span class="comment-copy">Ah the problem is the coma ? perfect</span>
<span class="comment-copy">Take a look at <a href="https://docs.python.org/3/library/functions.html#float" rel="nofollow noreferrer">the documentation of <code>float</code></a> which describes the syntax that can be parsed.</span>
<span class="comment-copy">duplicate of <a href="https://stackoverflow.com/questions/6633523/how-can-i-convert-a-string-with-dot-and-comma-into-a-float-in-python" title="how can i convert a string with dot and comma into a float in python">stackoverflow.com/questions/6633523/…</a></span>
<span class="comment-copy">Thanks didn't pay any attention that i get a coma</span>
<span class="comment-copy">Beat me by 21 seconds. =)</span>
