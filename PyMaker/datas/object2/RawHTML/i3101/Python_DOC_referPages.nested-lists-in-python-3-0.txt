<div class="post-text" itemprop="text">
<p>explain how it works marks=[marks for name, marks in marksheet]</p>
<pre><code>marksheet = []
for i in range(0, int(input())):
    marksheet.append([raw_input(), float(input())])
marks = [marks for name, marks in marksheet]
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>That is similar to:</p>
<pre><code> mark=[]
 for name,marks in marksheet:
    mark.append(marks)
</code></pre>
</div>
<span class="comment-copy">Welcome to SO! Answer is <a href="https://docs.python.org/3/tutorial/datastructures.html#list-comprehensions" rel="nofollow noreferrer">here</a> but read <a href="https://stackoverflow.com/help/how-to-ask">this</a> and <a href="https://stackoverflow.com/help/mcve">that</a> first!</span>
<span class="comment-copy">Why are you using Python 3.0 instead of 3.7, or at least something like 3.3? Besides being a decade out of date, 3.0 was never all that usable.</span>
