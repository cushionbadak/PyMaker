<div class="post-text" itemprop="text">
<p>I am trying to match a set of complex numbers with re.match(). I am pretty sure the regex should work but still Python doesn't return anything.</p>
<pre><code>pattern = '\.\d+\.\d+\w\.'
value = str(complex(17, 80))
string = '' + value
print(re.match(pattern, string).group())
</code></pre>
<p>The output should be something like:</p>
<pre><code>(17+80j) 
</code></pre>
<p>But instead it is:</p>
<pre><code>AttributeError: 'NoneType' object has no attribute 'group'
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>Try this:</p>
<pre><code>pattern = r'.\d+.\d+\w.'
</code></pre>
</div>
<span class="comment-copy">Try to see (debugger or print) what do you have in re.match(pattern, string) i suspect it's None</span>
<span class="comment-copy"><a href="http://docs.python.org/3/library/re.html#re.match" rel="nofollow noreferrer">match</a> returns <code>None</code> if the string does not match the pattern.</span>
<span class="comment-copy">So eventually I was wrong about the pattern :))) Thanks Alfe!</span>
