<div class="post-text" itemprop="text">
<p>I want use try catch in Python but python give me an error what's can I do?</p>
<p>this is my code</p>
<pre><code>try:
    input('enter your number');
catch:
    print('please give integer');
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>try/catch in python is try/except.</p>
<p>you can use try/except like this:</p>
<pre><code>try:
   input('enter your number');
except expression:
   print('please give integer');
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p><strong>You can use following alternative for your use case :</strong></p>
<pre><code>try:
    input_ = int(input('enter your number'))
except:
    print('please give integer')
    exit() #if you want to exit if exception raised
#If no exception raised, execution continues from here
print(input_)
</code></pre>
</div>
<div class="post-text" itemprop="text">
<pre><code>try: 
    int(input('enter your number')); 
except ValueError: 
    print('please give integer');
</code></pre>
<p>Use <a href="https://docs.python.org/3/tutorial/errors.html#handling-exceptions" rel="nofollow noreferrer">handling-exceptions</a></p>
</div>
<span class="comment-copy">use <code>except</code> instead of <code>catch</code></span>
<span class="comment-copy">Try <a href="https://www.pythonforbeginners.com/error-handling/python-try-and-except" rel="nofollow noreferrer">here</a></span>
<span class="comment-copy"><code>except</code> is mostly explained using the words "catch exceptions" in the docs and hence I guess you used the word <code>catch</code> ;)</span>
<span class="comment-copy">What is the expression here?</span>
<span class="comment-copy">in this code nothing</span>
