<div class="post-text" itemprop="text">
<p>I have been searching for how to do this, but it seems the only solutions I could find didn't work.</p>
</div>
<div class="post-text" itemprop="text">
<p>Use <a href="https://discordpy.readthedocs.io/en/rewrite/api.html#discord.ClientUser.edit" rel="nofollow noreferrer"><code>ClientUser.edit</code></a> on the <a href="https://discordpy.readthedocs.io/en/rewrite/api.html#discord.Client.user" rel="nofollow noreferrer"><code>Client.user</code></a> object that represents your bot:</p>
<pre><code>@bot.command()
async def rename(ctx, name):
    await bot.user.edit(username=name)
</code></pre>
</div>
<span class="comment-copy">could you provide some more details?</span>
<span class="comment-copy">How would you change the avatar?</span>
<span class="comment-copy"><code>await bot.user.edit(avatar=b)</code> where <code>b</code> is some <a href="https://docs.python.org/3/glossary.html#term-bytes-like-object" rel="nofollow noreferrer"><code>bytes</code>-like object</a>.</span>
