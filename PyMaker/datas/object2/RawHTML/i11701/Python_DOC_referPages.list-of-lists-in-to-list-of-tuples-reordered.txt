<div class="post-text" itemprop="text">
<p>How 'pythonic-ly', do I turn this:</p>
<pre><code>[[x1,y1], [x2,y2]]
</code></pre>
<p>Into:</p>
<pre><code>[(x1,x2),(y1,y2)]
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>Use a <a href="https://docs.python.org/3/library/functions.html#zip" rel="noreferrer"><code>zip</code></a> and unpacking operator. </p>
<pre><code>&gt;&gt;&gt; l = [['x1','y1'], ['x2','y2']]
&gt;&gt;&gt; zip(*l)
[('x1', 'x2'), ('y1', 'y2')]
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>Handled more case in give test case.</p>
<p>If there are items in list which have different length. </p>
<pre><code>In [19]: a
Out[19]: [[1, 2], [3, 4], [5, 6], [7, 8, 9]]

In [20]: import itertools

In [21]: b = itertools.izip_longest(*a)

In [22]: list(b)
Out[22]: [(1, 3, 5, 7), (2, 4, 6, 8), (None, None, None, 9)]
</code></pre>
</div>
<span class="comment-copy">@Bhargav Rao: Can u explain how <code>*</code> is working or give some links which explain <code>*</code>?</span>
<span class="comment-copy">@Vivek You can learn more about the <code>*</code> operator here <a href="https://docs.python.org/3/tutorial/controlflow.html#unpacking-argument-lists" rel="nofollow noreferrer">docs.python.org/3/tutorial/…</a></span>
<span class="comment-copy">Aside the fact that this question benefits from <code>zip</code> specifically returning a list of tuples, more generally <code>zip(*X)</code> can be thought of as the matrix transpose of <code>X</code>. Which is useful to people who've done lots of linear algebra and therefore would naturally think of the questioner's required re-ordering as transposition.</span>
<span class="comment-copy">@BhargavRao: Updated answer, and who down voted answer can also check and let me know if it is there is any issue :)</span>
