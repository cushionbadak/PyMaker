<div class="post-text" itemprop="text">
<p>Mac + pycharm</p>
<p>code like this:</p>
<pre><code>img = cv2.imread(images[0])
cv2.imshow('Image',img)
cv2.waitKey(0)
cv2.destroyAllWindows()
</code></pre>
<p><strong>No error, Pycharm just keeps running it, after running a few minutes I have to stop by myself.</strong></p>
<p>BTW the image is only 16KB</p>
<p>try matplotlib and it works.</p>
<pre><code>plt.imshow(img)
plt.show()
</code></pre>
<p>So no file error</p>
</div>
<div class="post-text" itemprop="text">
<p>Change your image display code to this:</p>
<pre><code>img = cv2.imread(images[0])
cv2.imshow('Image',img)
if cv2.waitKey(0) == 27   #waits for the ESC key to be pressed
   cv2.destroyAllWindows()
</code></pre>
</div>
<span class="comment-copy">Weird.  This actually looks like it should work.  Try creating a window explicitely and seeing if it changes anything:  <code>cv2.namedWindow('Image'); cv2.imshow('Image',img)</code></span>
<span class="comment-copy">@Jeremiah   Hi, thanks for applying. Have tried, nothing happened but it ends running pretty quick.... at least I dont have to wait hahaha</span>
<span class="comment-copy">thanks man. it's working!</span>
