<div class="post-text" itemprop="text">
<p>I am looking at an older version of python code </p>
<pre><code>from itertools import izip
</code></pre>
<p>but when I run it, it says</p>
<pre><code>ImportError: cannot import name 'izip'
</code></pre>
<p>Anyone know how should I import?</p>
</div>
<div class="post-text" itemprop="text">
<p>In Python 3+ you just use <code>zip</code> instead. It's builtin, so you don't have to import it. Try using the <a href="https://docs.python.org/3/library/2to3.html?highlight=2to3" rel="nofollow noreferrer">2to3 tool</a> to update the code automatically.</p>
</div>
