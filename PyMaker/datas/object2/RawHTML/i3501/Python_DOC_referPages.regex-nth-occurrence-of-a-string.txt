<div class="post-text" itemprop="text">
<p>I'm trying to get the 3rd string between the 3rd and 4th occurrence of the / character using regex</p>
<p>the list of strings is something like this:</p>
<pre><code>...
/restaurants/garnish/گارنیش/
/restaurants/burger1358/برگر-۱۳۵۸/
/restaurants/delion-saadat/صبحانه--و-میان-وعده-دلیون-شعبه-سعادت-آباد/
/restaurants/kamran/کامران--اسکای-فال-سابق-/
...
</code></pre>
<p>the desired output for the list above is something like this:</p>
<pre><code>...
گارنیش
برگر-۱۳۵۸
صبحانه--و-میان-وعده-دلیون-شعبه-سعادت-آباد
کامران--اسکای-فال-سابق-
...
</code></pre>
<p>as the desired part of the string is not ASCII (English) and there is no pattern (names being different), I cannot figure out the proper regex syntax.</p>
</div>
<div class="post-text" itemprop="text">
<p>If you know the structure doesn't change, use <code>str.split</code>. In case the structure of the url changes and you only want to get the Persian characters, you can use the range of unicode characters for Persian to get the result you want.</p>
<pre><code>import re 
line = "/restaurants/garnish/گارنیش/"

re.findall("([\u0600-\u06FF\s]+)", line)
['گارنیش']
</code></pre>
<p>Edit: if you want to find for every url in your list, the following is what you need:</p>
<pre><code>lines = ['/restaurants/garnish/گارنیش/',
 '/restaurants/burger1358/برگر-۱۳۵۸/',
 '/restaurants/delion-saadat/صبحانه--و-میان-وعده-دلیون-شعبه-سعادت-آباد/',
 '/restaurants/kamran/کامران--اسکای-فال-سابق-/']
[''.join(re.findall("([\u0600-\u06FF\s]+)", n)) for n in lines]

['گارنیش','برگر۱۳۵۸','صبحانهومیانوعدهدلیونشعبهسعادتآباد','کامراناسکایفالسابق']
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>A possible regex would be:</p>
<pre><code>/.+?/.+?/(.+?)/
</code></pre>
<p><a href="https://regex101.com/r/zEte2I/1" rel="nofollow noreferrer">Demo</a></p>
<hr/>
<p>However, as pointed out in the comments, you shouldn't use a regex for something that simple, it's overkill and not very performant (and also less readable). You should use <a href="https://docs.python.org/3/library/stdtypes.html#str.split" rel="nofollow noreferrer">str.split</a>:</p>
<pre><code> your_string.split('/')[3]
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>You probably dont need regexp for this</p>
<pre><code>res = [w.split('/', 4)[3] for w in s.splitlines() if '/' in w]
</code></pre>
</div>
<span class="comment-copy">Use <code>str.split</code> instead.  <code>your_string.split('/')[2]</code> should be the string you want.</span>
<span class="comment-copy">@PatrickHaugh well, I didn't think about it. thank you so much.</span>
<span class="comment-copy">thank you for the response. Performance wise I thought regex would be a faster solution.</span>
