<div class="post-text" itemprop="text">
<p>I am using and importing ConfigParaser </p>
<pre><code>import ConfigParser
config = ConfigParser.RawConfigParser()
config.read('Config.properties')
timeout_val=config.get('Section', 'commandtimeout') 
</code></pre>
<p>and install it using,</p>
<pre><code>pip install ConfigParser
</code></pre>
<p>While running the python script getting below mention error. </p>
<pre><code>Traceback (most recent call last):
  File "system_offline.py", line 41, in &lt;module&gt;
    import ConfigParser
ImportError: No module named 'ConfigParser'
</code></pre>
<p>Now, my question is if run the same program using python 2.7 at the same system, above import statement works with out issue. </p>
<p>Wondering what needs to be done for this program to run with python3?</p>
<p>Edit: While using python 2.7 it is working but with python3, i am getting above mention error.</p>
</div>
<div class="post-text" itemprop="text">
<p>The <code>ConfigParser</code> module was <a href="https://docs.python.org/3.0/whatsnew/3.0.html#library-changes" rel="nofollow noreferrer">renamed to <code>configparser</code></a> in Python 3.0.</p>
</div>
<span class="comment-copy">Are you using Python 2 or Python 3?</span>
<span class="comment-copy">python3 --version Python 3.5.2</span>
<span class="comment-copy">I edited the question. On the path i was using with python, now getting error with python3.</span>
<span class="comment-copy">and what to used for RawConfigParser? as shown above in question?</span>
<span class="comment-copy"><a href="https://docs.python.org/3/library/configparser.html#rawconfigparser-objects" rel="nofollow noreferrer">The <code>configparser</code> module still contains a <code>RawConfigParser</code> class</a>.</span>
