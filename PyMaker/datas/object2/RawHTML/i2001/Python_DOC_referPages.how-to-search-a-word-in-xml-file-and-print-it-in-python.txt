<div class="post-text" itemprop="text">
<p>i want to search a specific word(which is entered by user) in .xml file. This is my xml file.</p>
<pre><code>&lt;?xml version="1.0" encoding="UTF-8"?&gt;
&lt;words&gt;
&lt;entry&gt;
&lt;word&gt;John&lt;/word&gt;
&lt;pron&gt;()&lt;/pron&gt;
&lt;gram&gt;[Noun]&lt;/gram&gt;
&lt;poem&gt;&lt;/poem&gt;
&lt;meanings&gt;
&lt;meaning&gt;name&lt;/meaning&gt;
&lt;/meanings&gt;
&lt;/entry&gt;
&lt;/words&gt;
</code></pre>
<p>here is my Code</p>
<pre><code>import nltk
from nltk.tokenize import word_tokenize
import os
import xml.etree.ElementTree as etree


sen = input("Enter Your sentence - ")

print(sen)
print("\n")
print(word_tokenize(sen)[0])

tree = etree.parse('roman.xml')
node=etree.fromstring(tree)

#node=etree.fromstring('&lt;a&gt;&lt;word&gt;waya&lt;/word&gt;&lt;gram&gt;[Noun]&lt;/gram&gt; 
&lt;meaning&gt;talking&lt;/meaning&gt;&lt;/a&gt;')
s = node.findtext(word_tokenize(sen)[0])
print(s)
</code></pre>
<p>i have tried everything but still its giving me error </p>
<blockquote>
<p>a bytes-like object is required, not 'ElementTree'</p>
</blockquote>
<p>i really don't know how to solve it.</p>
</div>
<div class="post-text" itemprop="text">
<p>the error happens because you are passing an elementtree object to the fromstring () methods. Do like this:</p>
<pre><code>&gt;&gt;&gt; import os
&gt;&gt;&gt; import xml.etree.ElementTree as etree
&gt;&gt;&gt; a = etree.parse('a.xml')
&gt;&gt;&gt; a
&lt;xml.etree.ElementTree.ElementTree object at 0x10fcabeb8&gt;
&gt;&gt;&gt; b = a.getroot()
&gt;&gt;&gt; b
&lt;Element 'words' at 0x10fb21f48&gt;
&gt;&gt;&gt; b[0][0].text
'John'
</code></pre>
<p>Use find() and findall() methods to search.</p>
<p>for more info, check lib: <a href="https://docs.python.org/3/library/xml.etree.elementtree.html" rel="nofollow noreferrer">https://docs.python.org/3/library/xml.etree.elementtree.html</a></p>
<p>Simple example:</p>
<p><strong>test.xml</strong></p>
<pre><code>&lt;?xml version="1.0" encoding="UTF-8"?&gt;
&lt;words&gt;
  &lt;word value="John"&gt;&lt;/word&gt;
  &lt;word value="Mike"&gt;&lt;/word&gt;
  &lt;word value="Scott"&gt;&lt;/word&gt;
&lt;/words&gt;
</code></pre>
<p><strong>example.py</strong></p>
<pre><code>root = ET.parse("test.xml")
&gt;&gt;&gt; search = root.findall(".//word/.[@value='John']")
&gt;&gt;&gt; search
[&lt;Element 'word' at 0x10be9c868&gt;]
&gt;&gt;&gt; search[0].attrib
{'value': 'John'}
&gt;&gt;&gt; search[0].tag
'word'
</code></pre>
</div>
<span class="comment-copy">I'm not sure why you're calling <code>etree.fromstring(tree)</code>, since in the previous line you've already parsed your source document into an <code>ElementTree</code> object. <code>tree</code> is not a string, and that's why you're getting that error.</span>
<span class="comment-copy">I have modified my code, its working but i guess i'm doing something wrong in it. It isn't giving me the desired output. My xml file contains the first token name 'John' but still when my program searches for this it returns 'None'.</span>
<span class="comment-copy">Here's my code     import nltk from nltk.tokenize      import word_tokenize      import os      import xml.etree.ElementTree as etree      sen = input("Enter Your sentence - ")      print(sen)      print(word_tokenize(sen))      print("\n")     print(word_tokenize(sen)[0])     print("\n")     roman = etree.parse('roman.xml')     s = roman.findtext(word_tokenize(sen)[0]) print(s)</span>
<span class="comment-copy">I updated the answer with a simple example. Check out this page, it will be useful: <a href="https://docs.python.org/2/library/xml.etree.elementtree.html#xpath-support" rel="nofollow noreferrer">docs.python.org/2/library/…</a>. Hope this helps</span>
