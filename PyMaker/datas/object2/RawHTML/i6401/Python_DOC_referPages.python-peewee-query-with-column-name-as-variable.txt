<div class="post-text" itemprop="text">
<p>Peewee query is as following:</p>
<pre><code>q = AccountTab.get(AccountTab.id == 12345)
print(q.name)
</code></pre>
<p>What if <code>name</code> here is stored in a variable, say <code>user_name</code>. Is there something like this?</p>
<pre><code>q = AccountTab.get(AccountTab.id == 12345)
print(q.#{user_name})
</code></pre>
<p>Thanks for your time!</p>
</div>
<div class="post-text" itemprop="text">
<p><a href="https://docs.python.org/3/library/functions.html#getattr" rel="nofollow noreferrer"><code>getattr</code></a> will work:</p>
<pre><code>user_name = 'name'
print(getattr(q, user_name))
</code></pre>
</div>
