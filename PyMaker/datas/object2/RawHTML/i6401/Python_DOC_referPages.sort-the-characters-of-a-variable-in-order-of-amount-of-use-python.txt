<div class="post-text" itemprop="text">
<p>Example:</p>
<p>Enter data to sort: "Hello World"
Sorted: ["l", 3, "o", 2, " ", 1, "H", 1, "W", 1, "d", 1, "e", 1, "r", 1]
In ASCII order
So I want it to go into a table in order of the amount of times it comes up with the amount of times that character comes up.</p>
<pre><code>#Like this
uncompressedInput = input("Enter data to compress: ")
# user inputted "abc"
print("Analysing...")
#sorted = str.sort(uncompressedInput)
#print(sorted)
# ["a", 1, "b", 1, "c", 1]
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>Use <a href="https://docs.python.org/3/library/collections.html" rel="nofollow noreferrer"><code>Counter</code></a></p>
<pre><code>&gt;&gt;&gt; import collections
&gt;&gt;&gt; collections.Counter(uncompressedInput).most_common()
=&gt; [('l', 3), ('o', 2), ('H', 1), ('e', 1), (' ', 1), ('W', 1), ('r', 1), ('d', 1)]
</code></pre>
<p>If you want the structure exactly like as you said,</p>
<blockquote>
<p>Sorted: ["l", 3, "o", 2, " ", 1, "H", 1, "W", 1, "d", 1, "e", 1, "r",
  1]</p>
</blockquote>
<pre><code>&gt;&gt;&gt; l = []
&gt;&gt;&gt; [ l.extend([key,val]) for key,val in collections.Counter(s).most_common() ]
&gt;&gt;&gt; l
=&gt; ['l', 3, 'o', 2, 'H', 1, 'e', 1, ' ', 1, 'W', 1, 'r', 1, 'd', 1]
</code></pre>
<p>Though, I would not recommend doing that since it will make further computation on this more confusing.</p>
</div>
<div class="post-text" itemprop="text">
<pre><code>import collections
letters = collections.Counter(uncompressedInput)
print(letters)
</code></pre>
</div>
<div class="post-text" itemprop="text">
<pre><code>from collections import Counter
[e for x in sorted(Counter("hello world").items(), key=lambda t: t[1], reverse=True) for e in x]
</code></pre>
</div>
<span class="comment-copy">Duplicate of <a href="https://stackoverflow.com/questions/2600191/how-can-i-count-the-occurrences-of-a-list-item-in-python" title="how can i count the occurrences of a list item in python">stackoverflow.com/questions/2600191/…</a></span>
<span class="comment-copy">Possible duplicate of <a href="https://stackoverflow.com/questions/2600191/how-can-i-count-the-occurrences-of-a-list-item-in-python">How can I count the occurrences of a list item in Python?</a></span>
<span class="comment-copy">A <code>Counter</code> isn't sorted you need to call <code>.most_common()</code></span>
<span class="comment-copy">Forgot to add that. Thanks @Chris_Rands</span>
<span class="comment-copy"><code>collections.Counter</code> are not ordered</span>
<span class="comment-copy">Check <code>collections.Counter.most_common()</code>: <a href="https://docs.python.org/3.6/library/collections.html#collections.Counter.most_common" rel="nofollow noreferrer">docs.python.org/3.6/library/…</a></span>
<span class="comment-copy">Code-only answers are discouraged. Can you say something as to why this works as desired?</span>
