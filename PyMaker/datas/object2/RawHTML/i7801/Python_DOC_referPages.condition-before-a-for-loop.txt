<div class="post-text" itemprop="text">
<pre><code>def uncompress(xs):
  zs = []
  b = True
  for k in xs:
  zs.extend([b for i in range(k)])
  b = not b
  return zs
</code></pre>
<p>Just wanted to know what having a variable or a condition before the for loop does in any case, so in this case what does 'b' for i in range do.</p>
</div>
<div class="post-text" itemprop="text">
<p>Since <code>b = True</code>, <code>zs.extend([b for i in range(k)])</code> is effectively <code>zs.extend([True for i in range(k)])</code> or <code>zs.extend([True, True, ..., True (k times)])</code>.</p>
<p>Update. I'm assuming that the intended (indented) code is the following:</p>
<pre><code>def uncompress(xs):
  zs = []
  b = True
  for k in xs:
    zs.extend([b for i in range(k)])
    b = not b
  return zs
</code></pre>
<p>Then if <code>xs</code> is, say, <code>[2, 1, 3]</code>, then:</p>
<ul>
<li><p>after the first for-loop execution (so called "iteration"), <code>zs = [] + [True, True] = [True, True]</code> and <code>b</code> becomes <code>False</code>,</p></li>
<li><p>then <code>zs = [True, True] + [False] = [True, True, False]</code> and <code>b</code> becomes <code>True</code>, </p></li>
<li><p>finally <code>zs = [True, True, False, True, True, True]</code> and <code>b</code> becomes <code>False</code>.</p></li>
</ul>
<p>So if <code>xs == [s, t, u, v, ...]</code> then <code>zs</code> is <code>True</code> <code>s</code> times, <code>False</code> <code>t</code> times, <code>True</code> <code>u</code> times, <code>False</code> <code>v</code> times and so on.</p>
</div>
<span class="comment-copy">The <code>[b for i in range(k)]</code> isn't a loop, it's a <a href="https://docs.python.org/3/tutorial/datastructures.html#list-comprehensions" rel="nofollow noreferrer">list comprehension</a>.</span>
<span class="comment-copy">Sorry, I'm extremely new to python so what would it do in a general sense, having that condition there?</span>
<span class="comment-copy">@JackJock I've updated the answer.</span>
<span class="comment-copy">I think I've asked a bad question I'm focused more on what 'b' is doing in a general sense not the line. @KirillBulygin</span>
<span class="comment-copy">@JackJock It's just a variable, it contains a value. I think you need a basic introduction, see this: <a href="https://docs.python.org/3.6/tutorial/introduction.html" rel="nofollow noreferrer">docs.python.org/3.6/tutorial/introduction.html</a> (for list comprehensions: <a href="https://docs.python.org/3.6/tutorial/datastructures.html?highlight=comprehension#list-comprehensions" rel="nofollow noreferrer">docs.python.org/3.6/tutorial/…</a>).</span>
