<div class="post-text" itemprop="text">
<pre><code>def test():
     print "printing from test func"

dic = {'a':1,'b':2,'c':test}
if len(dic) == 3:
     print (dic['c'])

output:
&lt;function test at 0x106887578&gt;
</code></pre>
<p>This code prints the function object as output, how to get the print statement from test function as output.</p>
</div>
<div class="post-text" itemprop="text">
<p>Simply call <code>dic['c']()</code> to run your function. Hope I helped!</p>
</div>
<span class="comment-copy">Have you tried calling the function? And you probably want to return in the function instead of printing.</span>
<span class="comment-copy">You need to using the <a href="https://docs.python.org/3/reference/expressions.html#calls" rel="nofollow noreferrer">calling syntax</a>. I.e. <code>f()</code></span>
