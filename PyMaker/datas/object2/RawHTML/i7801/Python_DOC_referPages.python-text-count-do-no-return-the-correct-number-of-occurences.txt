<div class="post-text" itemprop="text">
<div class="question-status question-originals-of-duplicate">
<p>This question already has an answer here:</p>
<ul>
<li>
<a dir="ltr" href="/questions/2970520/string-count-with-overlapping-occurrences">string count with overlapping occurrences</a>
<span class="question-originals-answer-count">
                    21 answers
                </span>
</li>
</ul>
</div>
<p>I have a question regarding the text.count() function in python.
Suppose I have the following text and I want to return the number of occurrences of "CCC":</p>
<pre><code>text = "ACCCGTTGCCCC"
print text.count("CCC")
</code></pre>
<p>why it returns 2 and not 3?</p>
</div>
<div class="post-text" itemprop="text">
<p>According to the documentation of the <code>str.count</code> method:</p>
<pre><code>&gt;&gt;&gt; help(str.count)
Help on method_descriptor:

count(...)
    S.count(sub[, start[, end]]) -&gt; int

    Return the number of non-overlapping occurrences of substring sub in
    string S[start:end].  Optional arguments start and end are interpreted
    as in slice notation.
</code></pre>
<p>So in your case there are two non-overlapping occurences of <code>CCC</code> in the string <code>"ACCCGTTGCCCC"</code>.</p>
<p>Hope it helps.</p>
</div>
<div class="post-text" itemprop="text">
<pre><code>Text.count() 
</code></pre>
<p>returns the number of occurrences of the substring sub.
In this case: "A<strong>CCC</strong>GTTG<strong>CCCC</strong>" 
Gives you 2 because your substring is "CCC" collectively which occurs only twice in the string.(i.e. where "C" is placed next to each other at least 3 times.) </p>
</div>
<div class="post-text" itemprop="text">
<p>Best would be to use the already available <code>Counter</code>.</p>
<pre><code>from collections import Counter

c = Counter('test')
print (c)
&gt;&gt;&gt; Counter({'t': 2, 'e': 1, 's': 1})
</code></pre>
</div>
<span class="comment-copy">"Return the number of <b>non-overlapping</b> occurrences of substring sub in the range [start, end]." <a href="https://docs.python.org/3/library/stdtypes.html#str.count" rel="nofollow noreferrer">docs.python.org/3/library/stdtypes.html#str.count</a></span>
<span class="comment-copy">You might be interested in this: <a href="https://stackoverflow.com/questions/2970520/string-count-with-overlapping-occurrences" title="string count with overlapping occurrences">stackoverflow.com/questions/2970520/…</a></span>
<span class="comment-copy">thanks, in case of overlapping string should I have to implement my own function or there are some built-in one?</span>
<span class="comment-copy">@simalps you can use the Counter from collections, it will output a frequency dictionary</span>
