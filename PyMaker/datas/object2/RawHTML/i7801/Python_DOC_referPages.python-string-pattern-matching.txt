<div class="post-text" itemprop="text">
<p>two string expression： <code>@RequestMapping(value = "/list/base/info")</code> or <code>@RequestMapping("/list/base/info")</code></p>
<p>How do I get uri <code>/list/base/info</code> value by String Pattern Matching?</p>
</div>
<div class="post-text" itemprop="text">
<p>In this case you can try getting it by split:</p>
<pre><code>expression.split("\"")[1]
</code></pre>
</div>
<div class="post-text" itemprop="text">
<pre class="lang-py prettyprint-override"><code>import re

re.match(
    '@RequestMapping\((value\s*=\s*)?"([^"]+)"\)',
    '@RequestMapping(value = "/list/base/info")'
).group(2)
</code></pre>
<p>That would output:</p>
<pre><code>'/list/base/info'
</code></pre>
</div>
<span class="comment-copy">Hello and welcome to SO. Please read <a href="https://stackoverflow.com/questions/how-to-ask">How to Ask</a> and show your own effort. As a first hint, have a look at regular expressions and the <a href="https://docs.python.org/3/library/re.html#module-re" rel="nofollow noreferrer">re module</a>.</span>
<span class="comment-copy"><code>print "/list/base/info"</code></span>
