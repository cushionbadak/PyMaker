<div class="post-text" itemprop="text">
<p>What are the uses for . in Django? One use I know is to indicate a directory like 'directory.file'.  Can somebody give a complete use case?</p>
</div>
<div class="post-text" itemprop="text">
<p>its not just in Django, its in <a href="https://docs.python.org/3/tutorial/modules.html#packages" rel="nofollow noreferrer">Python</a></p>
</div>
<span class="comment-copy">If you mean attribute access, that's not just just Django or even just Python; dozens of languages allow you to access the attribute <code>bar</code> of the object <code>foo</code> with <code>foo.bar</code>. I'd recommend you run through introductory Python tutorials before starting on Django.</span>
<span class="comment-copy">I found the answer, and it's specific to Django, just need a way to give it here in stackoverflow.</span>
<span class="comment-copy">It's still not clear what you're trying to ask. Please <a href="https://stackoverflow.com/posts/41070700/edit">edit</a> the question to clarify.</span>
<span class="comment-copy">the keywords is 'use case in Django' and I just found out that there very specific use cases for . in Django.</span>
