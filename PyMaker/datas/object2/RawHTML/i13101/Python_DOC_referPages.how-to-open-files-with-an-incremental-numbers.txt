<div class="post-text" itemprop="text">
<p>Right now I'm doing this:</p>
<pre><code>filenames = ['ch01.md', 'ch02.md', 'ch03.md', 'ch04.md', 'ch05.md', 'ch06.md']
with open('chall.txt', 'w') as outfile:
   for fname in filenames:
</code></pre>
<p>But I have many files written as chxx.md (until <code>ch24.md</code>). Is there any what to modify the script using ranges? So I don't have to type in all the files names?</p>
</div>
<div class="post-text" itemprop="text">
<p>You can probably use <code>glob</code>. It's an easy way to collect files with the use of wildcard characters.</p>
<pre><code>import glob
filenames = glob.glob('/yourDirectory/ch*.md') # Will give you a list of all the file names
with open('chall.txt', 'w') as outfile:
   for fname in filenames:
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>You can also use <code>for x in</code> syntax, as follows:</p>
<pre><code>filenames = ['ch0'+str(x)+'.md' for x in range(7)]
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>Just do :</p>
<pre><code>with open('chall.txt', 'w') as outfile:
    for index in range(1, 7):
        filename = 'ch' + index.zfill(2)  + '.md'
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p><a href="https://docs.python.org/2/library/stdtypes.html#string-formatting-operations" rel="nofollow">python 2 string formating</a></p>
<pre><code>filename = 'ch%02d.md' % file_index
</code></pre>
<p><a href="https://docs.python.org/3/library/string.html#format-string-syntax" rel="nofollow">python 3 string formating</a></p>
<pre><code>filename = 'ch{0:02}.md'.format(file_index)
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>My attempt with <code>o.walk</code> and <code>re</code> pattern is customizable.Better is you have nested directory, in this case just get the path of mother folder as <code>seacrh_folder</code>.</p>
<p><strong>In this approach you have more control over the file name then any other methods.</strong></p>
<pre><code>import os,re
pattern = re.compile(r'ch[0-9]+(?=.md)')

seacrh_folder = r"C:\Users\USER_NAME\Desktop"

for root,dir,files in os.walk(seacrh_folder):
    for file in files:
        if pattern.match(file):
            print file
            #do something
        else:
            pass
</code></pre>
<p>Prints-</p>
<pre><code>ch01.md
ch012983928338.md
ch0344.md
</code></pre>
</div>
<span class="comment-copy">replace first line by <code>filenames = glob.glob('ch*.md')</code></span>
<span class="comment-copy">@itzmeontv It works but weird. The concatenation stops on <code>ch14.md</code> and I have files until <code>ch24.md</code></span>
<span class="comment-copy">to sort  == &gt; <code>sorted(glob.glob('ch*.md'))</code></span>
<span class="comment-copy">Weird. I have until <code>ch24.md</code> and the files concatenate but they stop on <code>ch14.md</code>.</span>
<span class="comment-copy">They're all from the same directory? There's nothing that differentiates them from the ones that work? <code>glob</code> should picked those up.</span>
<span class="comment-copy">Oh, what happened is, they were concadenated in wrong order: <code>['ch03.md', 'ch16.md', 'ch09.md', 'ch20.md', 'ch24.md', 'ch11.md', 'ch23.md', 'ch22.md', 'ch02.md', 'ch12.md', 'chall.md', 'ch19.md', 'ch06.md', 'ch18.md', 'ch05.md', 'ch13.md', 'ch08.md', 'ch15.md', 'ch10.md', 'ch07.md', 'ch21.md', 'ch01.md', 'ch17.md', 'ch04.md', 'ch14.md']</code> How can I make them concatenate in order?</span>
<span class="comment-copy">Ah, you can wrap <code>sorted(filenames)</code> to get them in the right order.</span>
<span class="comment-copy">@itzmeontv Thanks a lot. I'm a Python beginner. One question, why do I need this <code>key=lambda x:x[2:]</code>?</span>
<span class="comment-copy">Oh, I like this. But some files read like <code>ch12.md.</code>. How to deal with them?</span>
<span class="comment-copy">@alexchenco in that case, you can use <code>.zfill()</code>:  <code>filenames = ['ch'+str(x).zfill(2)+'.md' for x in range(7)]</code></span>
