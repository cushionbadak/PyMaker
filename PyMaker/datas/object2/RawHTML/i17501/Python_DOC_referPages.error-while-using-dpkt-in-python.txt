<div class="post-text" itemprop="text">
<p>I'm writing some code to parse a pcap file in python as follows:</p>
<pre><code>#!/usr/bin/env python
import socket
import dpkt
import sys
import pcap
pcapReader = dpkt.pcap.Reader(file("clients.pcap", "rb"))
for ts, data in pcapReader:
    ether = dpkt.ethernet.Ethernet(data)
    if ether.type != dpkt.ethernet.ETH_TYPE_IP: raise
    ip = ether.data
    src = socket.inet_ntoa(ip.src)
    dst = socket.inet_ntoa(ip.dst)
    print "%s -&gt; %s" % (src, dst)
</code></pre>
<p>While compiling, I’m getting the following error message::</p>
<pre><code>Traceback (most recent call last):
  File "test.py", line 3, in &lt;module&gt;
    import dpkt
ImportError: No module named dpkt
</code></pre>
<p>Any help is appreciated.</p>
</div>
<div class="post-text" itemprop="text">
<p>On Ubuntu, just type:</p>
<pre><code>sudo apt-get install python-dpkt
</code></pre>
</div>
<span class="comment-copy">Are you sure dpkt was properly installed? If you have more than one Python installation, are you running your script with the correct one?</span>
<span class="comment-copy">You can also set <a href="http://docs.python.org/3/using/cmdline.html#envvar-PYTHONPATH" rel="nofollow noreferrer">PYTHONPATH</a> to include the directory it's in.</span>
