<div class="post-text" itemprop="text">
<p>I am getting this error when trying to read from a <code>.csv</code> file with fields in both integer and string format. 
Error message is as follows,</p>
<blockquote>
<p>ValueError                                Traceback (most recent
  call last)
       in ()
            2 FN0 = 'uci-news-aggregator'
            3 with open('%s.csv'%FN0, 'rb') as fp:
      ----&gt; 4     id, title, url, publisher, category, story, hostname,timestamp = 
      pickle.load(fp)</p>
<pre><code>ValueError: could not convert string to int
</code></pre>
</blockquote>
</div>
<div class="post-text" itemprop="text">
<p>Hmmm, there's a pretty good <code>csv</code> package in the stdlib that will help you for that job.</p>
<p><a href="https://docs.python.org/3/library/csv.html#module-csv" rel="nofollow noreferrer">https://docs.python.org/3/library/csv.html#module-csv</a></p>
<p>Why are you trying to use <code>pickle</code>. It cannot work.</p>
</div>
<span class="comment-copy">Why are you using <code>pickle</code> if it's a CSV file?</span>
