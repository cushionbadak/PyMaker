<div class="post-text" itemprop="text">
<p>I am trying to add an invisible character (<code>CAN</code> - ASCII Code 24) to a <code>str</code>, but it only adds the ASCII code of that character (obviously because I am using <code>str(24)</code>).</p>
<p>Is there some way to add the character 24 to a string object?</p>
</div>
<div class="post-text" itemprop="text">
<p>Use <a href="https://docs.python.org/3/library/functions.html#chr" rel="nofollow"><code>chr</code></a> to get the character corresponding to an ASCII ordinal value.</p>
<pre><code>myString += chr(24)
</code></pre>
</div>
<span class="comment-copy"><code>str()</code> converts the integer to a string representation.. Where you looking for <code>chr()</code> perhaps?</span>
