<div class="post-text" itemprop="text">
<p>I have a datetime object which looks like the following:</p>
<pre><code>2020-04-08 11:23:19-05:00
</code></pre>
<p>How can I read just the date part from here? For example: 2020-04-08</p>
</div>
<div class="post-text" itemprop="text">
<p>If you have a string of this format you can just split on whitespace</p>
<p><code>dateString.split(" ")[0]</code></p>
<p>where <code>dateString</code> is your input string</p>
</div>
<div class="post-text" itemprop="text">
<pre><code>datetime_object.date().isoformat()
</code></pre>
<p>This creates a string that looks like you want it.</p>
</div>
<div class="post-text" itemprop="text">
<pre><code>myDateTime = datetime.now() #you get a datetime object
myDate = myDateTime.date() #you get a date object
print myDate               #print your date object
2016-05-11
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>If you have that date set to a variable, such as <code>time_and_date</code>, you would use <code>time_and_date[0, 9] = date</code> to select the first 9 characters which are the date. the first '2' is the 0, and the '8' is the 9. You can also change this to select the time.</p>
</div>
<span class="comment-copy">See <a href="https://docs.python.org/3/library/datetime.html#datetime.datetime.strptime" rel="nofollow noreferrer">docs.python.org/3/library/…</a></span>
<span class="comment-copy">Thanks! I could read the date using this.</span>
