<div class="post-text" itemprop="text">
<p>I try the gmail reader using Python from this link <code>https://github.com/abhishekchhibber/Gmail-Api-through-Python</code> and now i want to read every email from specific data.
For Example like this :</p>
<pre><code>Name : John Doe
Address : Las Vegas
City : New York
</code></pre>
<p>I want only print for the output is <code>City : New York</code> .. how to solve this ?
thank you!</p>
</div>
<div class="post-text" itemprop="text">
<p>As having been mentioned in the Github repository, the output will be a dictionary like that.</p>
<pre><code>output = {
              'Name' : 'John Doe',
              'Address' : 'Las Vegas',
              'City' : 'New York'
         }
</code></pre>
<p>Now you just need to print out what you want, for e.g:</p>
<pre><code>print("City: " + output.get("City"))
</code></pre>
<p><code>output.get("City")</code> will return you the value of the key <code>City</code>.</p>
</div>
<span class="comment-copy">I am sorry , i am newer in Python. Can you explain the full detail how i can use this script ?</span>
<span class="comment-copy">@Rai I just edit my answer. You can read more information about <code>dictionary</code> <a href="https://docs.python.org/3/tutorial/datastructures.html#dictionaries" rel="nofollow noreferrer">here</a>.</span>
<span class="comment-copy">I got error if i paste <code>print("City: " + output.get("City"))</code> the error like this  <code>('Total unread messages in inbox: ', '10') Traceback (most recent call last):   File "gmail_read.py", line 56, in &lt;module&gt;     print("City: " + output.get("City")) NameError: name 'output' is not defined</code> . I have paste it under the <code>print ("Total unread messages in inbox: ", str(len(mssg_list)))</code> Can you help me where i must paste it ?</span>
<span class="comment-copy"><code>output</code> is just the name of the variable here, you should change it to make it same as your code.</span>
