<div class="post-text" itemprop="text">
<p>I need to extract the <code>value</code> of all instances of pattern match of <code>ssid</code> in a string. </p>
<pre><code>interface_info = '''Interface wlan1-cabin-2  
                ifindex 37  
                wdev 0x300000003  
                addr 06:53:1a:4e:07:02  
                ssid SSID3  
                type AP  
                channel 6 (2437 MHz), width: 20 MHz, center1: 2437 MHz  
        Interface wlan1-cabin-1  
                ifindex 36  
                wdev 0x300000002  
                addr 06:53:1a:4e:07:01  
                ssid SSIDTEST2  
                type AP  
                channel 6 (2437 MHz), width: 20 MHz, center1: 2437 MHz  
        Interface wlan1  
                ifindex 7
                wdev 0x300000001  
                addr 06:53:1a:4e:07:00  
                ssid SSID1 
                type AP  
                channel 6 (2437 MHz), width: 20 MHz, center1: 2437 MHz  '''

ssid_regex = re.compile('ssid (\w+)')              
ssid_extract = re.search(ssid_regex,interface_info)

print (ssid_extract)
</code></pre>
<p>returns a value for first match only. I need to extract the value of every <code>ssid</code> match [SSID3, SSIDTEST2, SSID1}.
SSID3</p>
<p>Actual SSID3<br/>
expected [SSID3, SSIDTEST2, SSID1]</p>
</div>
<div class="post-text" itemprop="text">
<h1>!/usr/bin/env python</h1>
<p>import re
interface_info = '''<br/>
phy#3<br/>
        Interface wlan1-cabin-1<br/>
                ifindex 36<br/>
                wdev 0x300000002<br/>
                addr 06:53:1a:4e:07:01<br/>
                ssid SSIDTEST3<br/>
                type AP<br/>
                channel 6 (2437 MHz), width: 20 MHz, center1: 2437 MHz<br/>
        Interface wlan1<br/>
                ifindex 7<br/>
                wdev 0x300000001<br/>
                addr 06:53:1a:4e:07:00<br/>
                ssid SSIDTEST2<br/>
                type AP<br/>
                channel 6 (2437 MHz), width: 20 MHz, center1: 2437 MHz<br/>
phy#2<br/>
        Interface wlan0<br/>
                ifindex 6<br/>
                wdev 0x200000001<br/>
                addr 02:ac:1a:4e:07:00<br/>
                ssid SSID1<br/>
                type AP<br/>
                channel 149 (5745 MHz), width: 80 MHz, center1: 5775 MHz  </p>
<p>'''
interface_info = re.sub(re.compile('^\s+', re.MULTILINE), '', interface_info)
ssid_regex = re.compile('ssid (\w+)')
ssid_extract = re.findall(ssid_regex, interface_info[interface_info.index("Interface wlan1"):])</p>
<p>print (ssid_extract)
print (type(ssid_extract))</p>
<h1>print(str(interface_info))</h1>
</div>
<span class="comment-copy">Try <code>ssid_regex.findall(interface_info)</code>? <a href="https://docs.python.org/3/library/re.html#re.findall" rel="nofollow noreferrer">docs.python.org/3/library/re.html#re.findall</a></span>
<span class="comment-copy">Nice. Worked for me. Thanks for prompt response.</span>
<span class="comment-copy">How can findall be applied starting with a specific line in the file? Say search for all <code>ssid</code> match starting from <code>Interface wlan1-cabin-1</code> till end of file.</span>
<span class="comment-copy">You could pass <code>findall()</code> the part of the string starting with that string.  <code>interface_info = interface_info[interface_info.index("Interface wlan1-cabin-1"):]</code></span>
<span class="comment-copy">In the string var <code>interface_info</code>, I would like to remove all leading edge spaces from each line before processing/parsing the string. I have tried <code>strip() and lstrip()</code> - did not work. Any suggestions?</span>
<span class="comment-copy">That's a good candidate for a new question ( stringing questions together in comments to answers from previous questions makes it impossible for others to learn from questions) I think you're asking, how to remove spaces preceding newlines in a string.</span>
