<div class="post-text" itemprop="text">
<p>I have a list containing the following:</p>
<pre><code>lst = ['abcd', 'efgh', 'ijkl'] 
</code></pre>
<p>and in addition    </p>
<pre><code>n=2
</code></pre>
<p>I want it to look like this (the string will be splitted according to n):    </p>
<pre><code>['ab', 'bc' ,'ef' ,'gh','ij' , 'kl'] 
</code></pre>
</div>
<div class="post-text" itemprop="text">
<pre><code>n=2
lst = ['abcd', 'efgh', 'ijkl']
newlist=[x[start:start+2] for x in lst for start in range(0,len(x),n) ]
print(newlist)
</code></pre>
<p>Output</p>
<pre><code>['ab', 'cd', 'ef', 'gh', 'ij', 'kl']
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>If you join your list to generate a string, you can create a new list using textwrap.wrap:</p>
<pre><code>from textwrap import wrap
pairs = wrap("Hello, world!", 2)
</code></pre>
</div>
<span class="comment-copy">Have you tried anything to implement this?</span>
<span class="comment-copy">Or even tried looking for existing answers on SO?</span>
<span class="comment-copy">for i in range(len(lst)):             final = [lst[j: j+n] for j in range(0, len(lst[i]), n)]</span>
<span class="comment-copy">Include your attempt in your code instead of posting it as a comment</span>
<span class="comment-copy">edit your question to include the code, and try to fix its formatting using the code markdown.</span>
<span class="comment-copy">Thank you! it works</span>
<span class="comment-copy">@sig Consider reading this <a href="https://docs.python.org/3/tutorial/datastructures.html" rel="nofollow noreferrer">docs.python.org/3/tutorial/datastructures.html</a></span>
