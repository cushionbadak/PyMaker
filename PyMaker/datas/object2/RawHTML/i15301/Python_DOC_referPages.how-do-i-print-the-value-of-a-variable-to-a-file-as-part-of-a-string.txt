<div class="post-text" itemprop="text">
<p>If I have:</p>
<pre><code>a=10
b = 20
</code></pre>
<p>how do I write <code>10</code> and <code>20</code> separated by a tab to the file?  Doing:</p>
<pre><code>myfile.write('a\tb')
</code></pre>
<p>just prints the literal string to the file.  How to substitute the values of <code>a</code> and <code>b</code> into the string?</p>
</div>
<div class="post-text" itemprop="text">
<p>Python does not recognize variable names inside a string literal.  Instead, it treats them as normal text. </p>
<p>In order to do what you want, you can use <a href="https://docs.python.org/3/library/stdtypes.html#str.format" rel="nofollow"><code>str.format</code></a>:</p>
<pre><code>myfile.write('{}\t{}'.format(a, b))
</code></pre>
<p>This will take the values of <code>a</code> and <code>b</code>, convert them into strings, and then insert them into the string <code>'{}\t{}'</code>.  The curly braces <code>{}</code> denote where the values will be inserted.</p>
</div>
<div class="post-text" itemprop="text">
<p>They need to be outside the '' </p>
<pre><code>myfile.write(str(a)+'\t'+str(b))
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>This is a pythonic way of doing it</p>
<pre><code>with open('Filename.fileformat', 'w') as f:
    a="10"
    b="20"
    lst = [a,b]
    for i in lst:
        print ''.join(i)
        f.write(str(''.join(i)) +'\n')
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>you can easily use formatted string for that</p>
<pre><code>myfile.write('%d\t%d' % (a, b))
</code></pre>
</div>
<span class="comment-copy">I agree with the use of <code>with</code> here, but the <code>for</code> loop is unnecessary, unless the problem becomes more complex..</span>
<span class="comment-copy">I just love adding loops to everything, the list is in case he wanted to add more variables.</span>
