<div class="post-text" itemprop="text">
<pre><code>    if not [True]*3 == [isinstance(i, int) for i in [days, months, years]]:
        raise TypeError('days, months, and years must be int type not %s %s %s'
                        % (type(days), type(months), type(years)))
</code></pre>
<p>Basically want to check if days, months, and years is an int, the most compact way I could thing of is above but I'm not sure it's the best but I believe it would be better than multiple if statements for each int.</p>
</div>
<div class="post-text" itemprop="text">
<p>You can use <a href="https://docs.python.org/3/library/functions.html#all"><code>all</code></a> and a <a href="https://docs.python.org/3/reference/expressions.html#grammar-token-generator_expression">generator expression</a>:</p>
<pre><code>if not all(isinstance(i, int) for i in [days, months, years]):
</code></pre>
</div>
<div class="post-text" itemprop="text">
<pre><code>if not all(isinstance(i, int) for i in [days, months, years])
</code></pre>
</div>
<span class="comment-copy">Perfect! Thanks.</span>
