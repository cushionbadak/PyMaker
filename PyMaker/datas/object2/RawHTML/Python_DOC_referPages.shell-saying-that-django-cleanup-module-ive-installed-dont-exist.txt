<div class="post-text" itemprop="text">
<p>Like in title I've installed django_cleanup by using:</p>
<pre><code>pip install django-cleanup
</code></pre>
<p>and put in INSTALLED_APPS:</p>
<pre><code>INSTALLED_APPS = [
'django_cleanup',
'django.contrib.admin',
'django.contrib.auth',
'django.contrib.contenttypes',
'django.contrib.sessions',
'django.contrib.messages',
'django.contrib.staticfiles',
'django.contrib.sites',
'django.contrib.flatpages',
'shop',
'account',
'rest_framework',
]
</code></pre>
<p>but now when I'm trying to run test or server in shell then i get: </p>
<pre><code>ModuleNotFoundError: No module named 'django_cleanup'
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p><a href="https://github.com/un1t/django-cleanup#configuration" rel="nofollow noreferrer">https://github.com/un1t/django-cleanup#configuration</a></p>
<p>The directions in the configuration section state this is the correct method to add to <code>INSTALLED_APPS</code>:</p>
<pre><code>INSTALLED_APPS = [
    ...
   'django_cleanup.apps.CleanupConfig',
]
</code></pre>
<p>And I would add it at the end of the installed apps list, not at the head</p>
</div>
<span class="comment-copy">I tried also this, nothing changed</span>
