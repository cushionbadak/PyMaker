<div class="post-text" itemprop="text">
<p>I made a small discord bot in python. On windows it works perfectly fine, but when I try to run it on raspbain, it says invalid syntax (with the command "python3 Bot.py")
Here's the code:</p>
<pre><code>import feedparser
from yaml import load, dump
from json import dumps as jdump
from requests import post
import xml.etree.ElementTree as ET
BASE_URL = "https://discordapp.com/api"


def get_from_summary(summary):
    root = ET.fromstring(f"&lt;element&gt;{summary}&lt;/element&gt;")
    d = f"{root[1].text}\n\n{root[2].text}"
    i = root[0].attrib["src"]
    return (d, i)
</code></pre>
<p>The syntax is at <code>root = ET.fromstring(f"&lt;element&gt;{summary}&lt;/element&gt;")</code> with the "</p>
</div>
<div class="post-text" itemprop="text">
<p>The code uses <a href="https://docs.python.org/3/reference/lexical_analysis.html#f-strings" rel="nofollow noreferrer">formatted string literals</a> (the <code>f"&lt;element&gt;{summary}&lt;/element&gt;"</code>), which were only introduced in Python 3.6, so you need to use at least that version of Python.</p>
</div>
<span class="comment-copy">Hello. Could you paste the error output, please?</span>
<span class="comment-copy">What is the <code>f"..."</code> notation? My Python 3.5 doesn’t recognize it.</span>
<span class="comment-copy">please show the exact error.</span>
<span class="comment-copy">@Boldewyn It comes with Python 3.6; see <a href="https://docs.python.org/3/reference/lexical_analysis.html#f-strings" rel="nofollow noreferrer">docs.python.org/3/reference/lexical_analysis.html#f-strings</a></span>
<span class="comment-copy">thx fixed the problem</span>
