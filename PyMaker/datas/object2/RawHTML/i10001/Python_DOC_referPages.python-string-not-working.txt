<div class="post-text" itemprop="text">
<p>Here is my syntax:</p>
<pre><code>input("Has the phone got wet?")

if input is "yes".strip('\n'):
    print("that's the problem")
else:
    print("okay")
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>Try to use == instead of is:</p>
<pre><code>answer = input("Has the phone got wet?")
if answer == "yes":
    print("that's the problem")
else:
    print("okay")
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>When you use <code>input</code>, pass the values at the terminal in quotes. Like 
<code>
Has the phone got wet?"yes"
</code></p>
<p>and not like this:
<code>
Has the phone got wet?yes
</code></p>
<p>You can use <code>raw_input</code> if you would rather not type quotes. </p>
</div>
<span class="comment-copy">But your example does not contain ==. Also, do tag your question correctly; many answerers only listen on certain tags.</span>
<span class="comment-copy"><code>is</code> is not same as <code>==</code>.</span>
<span class="comment-copy"><code>"yes".strip('\n')</code> is just <code>"yes"</code></span>
<span class="comment-copy">You need to assign to variable <code>user_input  = input("Has the phone got wet?")</code> ... <code>if user_input == "yes":</code></span>
<span class="comment-copy">What exactly does <i>" not working"</i> mean? Give a <a href="https://stackoverflow.com/help/mcve">Minimal, Complete, and Verifiable example</a>. If nothing else, you're applying <code>str.strip</code> to the wrong side of the comparison, comparing to a function not its return value and comparing by identity not equality.</span>
<span class="comment-copy">btw, works for python3</span>
<span class="comment-copy">for python2 use raw_input instead of just input</span>
<span class="comment-copy">You are testing if <code>input</code> the &lt;built-in function input&gt; equals <code>"yes"</code></span>
<span class="comment-copy">No in fact - "If the prompt argument is present, it is written to standard output without a trailing newline. The function then reads a line from input, converts it to a string (stripping a trailing newline), and returns that." - <a href="https://docs.python.org/3/library/functions.html#input" rel="nofollow noreferrer">docs.python.org/3/library/functions.html#input</a></span>
<span class="comment-copy">I'm afraid you are completely wrong, you need to store the returned value as a variable and then compare this to <code>"yes"</code> (see my comment on the question)</span>
