<div class="post-text" itemprop="text">
<p>I have 2 arrays one is mask and the other is the labels:</p>
<p>Both arrays have the same shape:</p>
<pre><code>(Pdb) L.shape
(178, 201, 101)
(Pdb) MASK.shape
(178, 201, 101)
</code></pre>
<p>when it reaches to this line:</p>
<pre><code>L[~MASK] = 0 
IndexError: 'index 255 is out of bounds for axis 0 with size 178'
</code></pre>
<p>it shows an error that I could not find any reason for that. Could you please help me with this?</p>
</div>
<div class="post-text" itemprop="text">
<p>Try:</p>
<pre><code>L[np.logical_not(MASK)]
</code></pre>
<p>The ~ (tilde) operator you are using is a bitwise complement operator, not a logical negation operator.</p>
</div>
