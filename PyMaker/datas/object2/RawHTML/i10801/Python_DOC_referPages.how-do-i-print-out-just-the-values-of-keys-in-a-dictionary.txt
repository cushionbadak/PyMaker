<div class="post-text" itemprop="text">
<p>I'm trying to create a function that prints out just the value of the key given in the parameter. But it isn't quite doing what I want it to do. Please have a look at my code and see where I'm wrong? Thanks</p>
<pre><code>list = {
    "David": 42,
    "John": 11,
    "Jack": 278
}

def get_marks(student_name):
    for marks in list.items():
        print(marks)

get_marks("David")
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>You are trying this:</p>
<pre><code>def get_marks(student_name):
    print(list.get(student_name))
</code></pre>
</div>
<div class="post-text" itemprop="text">
<pre><code>list = {
"David": 42,
"John": 11,
"Jack": 278
}

def get_marks(student_name):
    print list[student_name]

get_marks("David")
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>You can access the value at a key using  <code>my_dict[student_name]</code> but using the <code>get</code> method of the dictionary is much safer as it returns <code>None</code> when the key is not found:</p>
<pre><code>def get_marks(student_name):
    print my_dict.get(student_name)
</code></pre>
<p>I have renamed your dictionary from <code>list</code> to <code>my_dict</code> to avoid shadowing the builtin list class.</p>
</div>
<div class="post-text" itemprop="text">
<p>You can either use the <a href="http://www.tutorialspoint.com/python/dictionary_get.htm" rel="nofollow">get()</a> method of the dictionary like so:</p>
<pre><code>def get_marks(student_name):
    return list.get(student_name)
</code></pre>
<p>or you can just use <a href="https://docs.python.org/3/tutorial/datastructures.html" rel="nofollow">basic dictionary access (Section 5.5)</a> and handle missing keys on your own:</p>
<pre><code>def get_marks(student_name):
    if student_name in list:
        return list[student_name]
    else:
        #Key not in dict, do something
</code></pre>
</div>
<span class="comment-copy">Umm, <code>print(list[student_name])</code>?</span>
<span class="comment-copy">@Rawing It's a better pratice to use the <code>get</code> function of the dict, to prevent you from getting a key error if the element does not exist</span>
<span class="comment-copy">Possible duplicate of <a href="http://stackoverflow.com/questions/5904969/python-how-to-print-a-dictionarys-key" title="python how to print a dictionarys key">stackoverflow.com/questions/5904969/…</a></span>
<span class="comment-copy">@HolyDanna That's an odd statement. What if I want to get a KeyError?</span>
<span class="comment-copy">OT, but don't use the variable name <code>list</code> for anything, let alone for a dictionary.</span>
