<div class="post-text" itemprop="text">
<p>I am trying to get the current datetime and set the time to <code>00:00:00</code>.</p>
<p>To do this, I call:</p>
<pre><code>current_date = dt.datetime.now()
current_date.replace(hour=0, minute=0, second=0)
print(current_date)
</code></pre>
<p>The output is:</p>
<pre><code>2017-08-20 10:43:56.3243245
</code></pre>
<p>That is not what I expect. However, if I do:</p>
<pre><code>current_date = dt.datetime(dt.datetime.now().date().year,dt.datetime.now().date().month,dt.datetime.now().date().day,0,0,0)
</code></pre>
<p>Everything is as I expect and I get the result:</p>
<pre><code>2017-08-20 00:00:00
</code></pre>
<p>Why is this? What is going on?? Why does <code>replace</code> not work?</p>
</div>
<div class="post-text" itemprop="text">
<p><a href="https://docs.python.org/3/library/datetime.html#datetime.datetime.replace" rel="nofollow noreferrer"><code>replace</code></a> returns a new <code>datetime</code> instance, so you should do:</p>
<pre><code>&gt;&gt;&gt; current_date = dt.datetime.now()
&gt;&gt;&gt; current_date = current_date.replace(hour=0, minute=0, second=0, microsecond=0)
&gt;&gt;&gt; print(current_date)
2017-08-20 00:00:00
</code></pre>
<p>You should also replace <code>microsecond=0</code> in order to make it exactly <code>00:00:00</code>.</p>
</div>
<div class="post-text" itemprop="text">
<p>replace method always return a new  value after replacing  so you need to store that value.</p>
<h1>Remember it returns a new value after replacing</h1>
<p>like in your case.
It returns a new datetime instance, so you should do:</p>
<pre><code>current_date = dt.datetime.now()
current_date = current_date.replace(hour=0, minute=0, second=0)
print(current_date)
</code></pre>
</div>
<span class="comment-copy">It does work, but it <i>returns a new object</i> that you ignore; it's not a mutating operation. See the docs: <a href="https://docs.python.org/3/library/datetime.html#datetime.datetime.replace" rel="nofollow noreferrer">docs.python.org/3/library/…</a>. You could also just use <code>date.today()</code> for the date.</span>
