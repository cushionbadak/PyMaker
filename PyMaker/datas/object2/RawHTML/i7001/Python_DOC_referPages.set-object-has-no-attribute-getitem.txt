<div class="post-text" itemprop="text">
<p>I have a dictionary name typecontext and I have a list named types. I want to take  five elements of types and add it to the dict typecontext. I am getting a error that set attribute has no object getitem. </p>
<pre><code> try:
    k=0
    typecontext = dict()
    for i in range(0, len(types), 5):
        print(i)
        if i + 5 &lt; len(types):
            typecontext.update({'item_'+str(k) :types[i:i + 5]})
        else:
            typecontext.update({'item_' + str(k): types[i:len(types)]})
        k=k+1
except Exception as e:
    print(e)
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>It looks like your variable <code>types</code> is a <code>set</code> object. So when you try to slice it you get your error.</p>
</div>
<div class="post-text" itemprop="text">
<p>Take a look at the <a href="https://docs.python.org/3/library/itertools.html#itertools.islice" rel="nofollow noreferrer">islice</a> function. It works with any iterable.</p>
</div>
<span class="comment-copy">Whats the contents of types</span>
<span class="comment-copy">its a set. It contains strings eg: types = set([u'abc', u'def', u'ghi']). and its length is 22</span>
