<div class="post-text" itemprop="text">
<p>I'm trying to assign the md5 sum of a file (in ubuntu) to a variable(any) in <strong>python script</strong> as below</p>
<pre><code>aList=subprocess.check_output(["md5sum",filename])
</code></pre>
<p>i want to assign only sum to the variable for that i used below code but it's not working</p>
<pre><code>aList=subprocess.check_output(["md5sum",filename," | awk '{print $1}'"])
</code></pre>
<p>please help me to find out solution</p>
<p>thanks in advance</p>
</div>
<div class="post-text" itemprop="text">
<p>Rather than shelling out to perform the md5sum, use Python's inbuilt <a href="https://docs.python.org/3/library/hashlib.html#module-hashlib" rel="nofollow noreferrer"><code>hashlib.md5</code></a> implementation:</p>
<pre><code>import hashlib

with open(filename, 'rb') as f:
    hexdigest = hashlib.md5(f.read()).hexdigest()
    print(hexdigest)
</code></pre>
</div>
<span class="comment-copy">Doesn't <code>aList = subprocess.check_output(['md5sum', filename]).decode().partition(' ')[0]</code> work?</span>
<span class="comment-copy">Consider <a href="https://stackoverflow.com/questions/13332268/python-subprocess-command-with-pipe" title="python subprocess command with pipe">stackoverflow.com/questions/13332268/…</a>, which discusses pipes in calls to <code>subprocess</code> methods, and also consider using the Python <a href="https://docs.python.org/2/library/hashlib.html" rel="nofollow noreferrer">hashlib</a> library instead of using the <code>md5sum</code> command.</span>
<span class="comment-copy">Thanks Jon Clements, its working.</span>
<span class="comment-copy">Thanks its working</span>
