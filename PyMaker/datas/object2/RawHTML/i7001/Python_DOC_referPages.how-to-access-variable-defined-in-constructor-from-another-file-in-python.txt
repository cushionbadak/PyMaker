<div class="post-text" itemprop="text">
<p>I have a python file <code>file1</code> which has below code</p>
<pre><code>Class myClass():

   def __init__(self):
      self._variable1 = 2
</code></pre>
<p>Now how can I access variable1 from another python file</p>
<pre><code> from file1 import myClass

    class = myClass()
    class._variable1 ??
</code></pre>
<p>how can I access these variables.?</p>
</div>
<div class="post-text" itemprop="text">
<p>all done fine, except the name <code>class</code> for variable</p>
<p>try it</p>
<pre><code>from file1 import myClass

    cl = myClass()
    cl.variable1
</code></pre>
<p>newer user python keywords as variable name, list of it you may look</p>
<pre><code>import keyword
keyword.kwlist
</code></pre>
<p>more details <a href="https://docs.python.org/3/reference/lexical_analysis.html#keywords" rel="nofollow noreferrer">lexical_analysis</a></p>
</div>
<span class="comment-copy">Uh, yes, like that. Except you can't use "class" as an identifier.</span>
<span class="comment-copy">Make sure you get the indentation right. Why are the lines not aligned in file2?</span>
<span class="comment-copy">Sorry not mentioning but the code has variable like <code>self._variable1</code>. What does <code>_variable</code> means. I tried it with <code>class._variable1</code> but intellisense doesnt show it.</span>
<span class="comment-copy">That is a completely different question.</span>
