<div class="post-text" itemprop="text">
<p>I am trying to locate the 'Like' and 'Share' button on facebook pages with xpath, however after the facebook page loads, the python script execution is complete and the like button is not clicked.</p>
<p>I got the like button xpath from facebook page.</p>
<pre class="lang-py prettyprint-override"><code>from selenium import webdriver
from selenium.webdriver.chrome.options import Options
import time

def connect():
    driver.set_page_load_timeout("10")
    driver.get("https://www.facebook.com/mytheoryofevolution/")
    time.sleep(4)

chrome_options = Options()
chrome_options.add_experimental_option("debuggerAddress", "127.0.0.1:9222")

driver = webdriver.Chrome(executable_path=r"C:\Users\javed\PycharmProjects\click-website\drivers\chromedriver.exe",chrome_options=chrome_options)


connect()

like_buttons = driver.find_elements_by_xpath('//a[contains(@class,"likeButton")]')

for like in like_buttons:
    print(like)
    break
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>tYou don't need</p>
<pre class="lang-py prettyprint-override"><code>break
</code></pre>
<p>because when it will start the for loop you will get the first item and then the loop will break</p>
<pre class="lang-py prettyprint-override"><code>from selenium import webdriver
from selenium.webdriver.chrome.options import Options
import time


chrome_options = Options()
chrome_options.add_experimental_option("debuggerAddress", "127.0.0.1:9222")

driver = webdriver.Chrome(executable_path=r"C:\Users\javed\PycharmProjects\click-website\drivers\chromedriver.exe",chrome_options=chrome_options)

def connect():
    driver.set_page_load_timeout("10")
    driver.get("https://www.facebook.com/mytheoryofevolution/")
    time.sleep(4)
    like_buttons = driver.find_elements_by_xpath('//a[contains(@class,"likeButton")]')

    for like in like_buttons:
        print(like)

connect()


</code></pre>
</div>
<span class="comment-copy">@lakavos, I have tried your answer still unable to click on like button, it just opens the facebook post page and stops.</span>
<span class="comment-copy">Your terminal doesn't print anything ?</span>
<span class="comment-copy">It seems that you are targeting like buttons the wrong way</span>
<span class="comment-copy">No, terminal does not print anything</span>
<span class="comment-copy">You are selecting the like buttons with the wrong way. The only solution I found is an old issue here and the solution was to use JS</span>
