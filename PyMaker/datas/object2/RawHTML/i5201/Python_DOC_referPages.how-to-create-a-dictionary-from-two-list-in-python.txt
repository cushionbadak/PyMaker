<div class="post-text" itemprop="text">
<div class="question-status question-originals-of-duplicate">
<p>This question already has an answer here:</p>
<ul>
<li>
<a dir="ltr" href="/questions/15516413/dict-fromkeys-all-point-to-same-list">dict.fromkeys all point to same list</a>
<span class="question-originals-answer-count">
                    4 answers
                </span>
</li>
</ul>
</div>
<p>Hello guys i have two list in my code like this</p>
<pre><code>other_concords = ['a','b','c']
leamanyi_concords = ['fruit','drink','snack']

temp_dic = {
              'a':['fruit','drink','snack'],
              'b':['fruit','drink','snack'],
              'c':['fruit','drink','snack']
            }
</code></pre>
<p>Is it possible to insert items in my temp_dic using a loop and it appears like this when i output temp_dic? </p>
</div>
<div class="post-text" itemprop="text">
<pre><code>temp_dic = {v: list(leamanyi_concords) for v in other_concords}
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>Use <a href="https://docs.python.org/3/library/stdtypes.html#dict.fromkeys" rel="nofollow noreferrer"><code>dict.fromkeys</code></a> if you don't mind your dictionary pointing to the same <code>list</code>.</p>
<pre><code>temp_dic = dict.fromkeys(other_concords, leamanyi_concords)

# {'a': ['fruit', 'drink', 'snack'],
#  'b': ['fruit', 'drink', 'snack'],
#  'c': ['fruit', 'drink', 'snack']}
</code></pre>
</div>
<span class="comment-copy">Generally not a good idea to use <code>dict.fromkeys</code> with some mutable arg.  See <a href="https://stackoverflow.com/questions/15516413/dict-fromkeys-all-point-to-same-list" title="dict fromkeys all point to same list">stackoverflow.com/questions/15516413/…</a>.  It has come back to bite me before</span>
<span class="comment-copy">thank you guys it worked!</span>
<span class="comment-copy">@BradSolomon - it really depends. maybe the intention is that the lists should be linked? I did add a disclaimer.</span>
