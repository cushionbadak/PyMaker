<div class="post-text" itemprop="text">
<pre><code>from random import randint
hp=4
i = 0
random_number=randint(1,25)
while i &lt; 5:
 Num=int(input("Please Enter Number"))
if int(Num)==random_number:
  print("you win")
if random_number!=Num:
  print("Incorrect")
  hp=hp-1
  print("your hp is {}".format(hp))
if int(Num) == str:
  print("Please Enter Number")
if hp==0 :
  print("You Lose ")
  i += 1
</code></pre>
<p>I would like to know why it isn't it working. Are all of the lines correct? It is looping, any solutions?</p>
</div>
<div class="post-text" itemprop="text">
<h1>Some changes to make the game</h1>
<pre><code>from random import randint
from time import sleep
chances = 4
elenco = []

random_number = randint(1, 25)
while chances &gt; 0:
    num = input("Please Enter Number: ")
    if num == str(random_number):
        print("you win")
    else:
        elenco.append(num)
        print("\t These numbers are incorrect: " + "-".join(elenco))
        chances = chances - 1
        print("\tYou still have {} chances".format(chances) + "\n")

sleep(1)
print("\nGame over ")
print("The right answer was {}".format(random_number))
</code></pre>
<p>output</p>
<p><a href="https://i.stack.imgur.com/UVRNK.png" rel="nofollow noreferrer"><img alt="enter image description here" src="https://i.stack.imgur.com/UVRNK.png"/></a></p>
</div>
<div class="post-text" itemprop="text">
<p>I'm not sure about your indentation as it is unclear how it looks in your actual program, But this:</p>
<pre><code> Num=int(input("Please Enter Number"))
</code></pre>
<p>should be placed in a try catch to see if the input is actually convertible to an int (I.e it will only accept number inputs)</p>
<pre><code>try:
    Num = int(input("Please insert a number"))
except ValueError:
    Print("Please enter a number instead")
    i = i - 1
</code></pre>
</div>
<div class="post-text" itemprop="text">
<pre><code>from random import randint
hp=4
i = 0
random_number=randint(1,25)
while i &lt; 5:
 Num=int(input("Please Enter Number"))
if int(Num)==random_number:
  print("you win")
if random_number!=Num:
  print("Incorrect")
  hp=hp-1
  print("your hp is {}".format(hp))
if int(Num) == str:
  print("Please Enter Number")
if hp==0 :
  print("You Lose ")
  i += 1
</code></pre>
<p>I see no problem with this code. It should work fine if you use the official ide. If you do use it and it does not work, you should try reinstalling it. I have had problems with the ide before and that always fixes them.</p>
</div>
<span class="comment-copy">What do you expect this line to do:<code>if int(Num) == str:</code></span>
<span class="comment-copy">if num==str print error because num must   only number</span>
<span class="comment-copy">This will not work but <a href="https://docs.python.org/3/library/functions.html#isinstance" rel="nofollow noreferrer"><code>isinstance()</code></a> will</span>
<span class="comment-copy">It looks like your indentations are wrong</span>
<span class="comment-copy">Welcome to <a href="https://stackoverflow.com">Stack Overflow</a>! If your code is different than the code in the question, please describe the changes you made. If not, then there's no reason to re-post it.</span>
