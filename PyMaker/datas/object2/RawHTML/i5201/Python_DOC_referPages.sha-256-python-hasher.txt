<div class="post-text" itemprop="text">
<p>I wanted to create a python program thats asks for an input then hashes(sha-256) the input then prints it. Does this already exist? How would I go about doing so.</p>
</div>
<div class="post-text" itemprop="text">
<p>using <code>hashlib</code>:</p>
<p><strong>Python 3.x</strong></p>
<pre><code>import hashlib

inp = input('Enter something: ')
print(hashlib.sha256(inp.encode('utf-8')).hexdigest())
</code></pre>
<p><strong>Python 2.x</strong></p>
<pre><code>import hashlib
inp = raw_input('Enter something: ')
print hashlib.sha256(inp.encode('utf-8')).hexdigest()
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>I have programmed a package wich is able to do this.
Install the package like this:</p>
<pre><code>pip3 install py_essentials
</code></pre>
<p>Use the module to hash strings:</p>
<pre><code>from py_essentials import hasher as hs
text = "text123"
hash = hs.checksum(text, "sha256")
</code></pre>
</div>
<span class="comment-copy">Check out <a href="https://docs.python.org/3/library/hashlib.html" rel="nofollow noreferrer">hashlib</a>.</span>
<span class="comment-copy">Why install 3rd-party software when the functionality is already in the standard library?</span>
<span class="comment-copy">You don't have to. You can also copy and paste the code from the repository.</span>
