<div class="post-text" itemprop="text">
<p>I have float value as follow:</p>
<pre><code>6.07345232904e+23
</code></pre>
<p>How can I convert this to two decimal places?  </p>
<p>I have tried with <code>round()</code>.<br/>
But it is not working.</p>
</div>
<div class="post-text" itemprop="text">
<p>You can't. That value is approximately 607345232904000000000000, which already has more than two decimal places. If you want the <em>representation</em> to have two decimal places then you must specify that.</p>
<pre><code>&gt;&gt;&gt; '{:.3g}'.format(6.07345232904e+23)
'6.07e+23'
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>To answer your clarification of what you actually want to do, namely comparing two numbers with a given precision:</p>
<p>There are different ways to do this, depending on what your criteria are. The simplest solution is</p>
<pre><code>abs(a-b) &lt;= tolerance
</code></pre>
<p>If you are using Python 3.5, you can use <a href="https://docs.python.org/3/library/math.html#math.isclose" rel="nofollow">math.isclose</a>. For earlier version, this should be functionally equivalent</p>
<pre><code>def isclose(a, b, *, rel_tol=1e-09, abs_tol=0.0):
  return abs(a-b) &lt;= max(rel_tol * max(abs(a), abs(b)), abs_tol)
</code></pre>
<p>If you really need to handle all corner cases, you will have to to <a href="https://github.com/PythonCHB/close_pep/blob/master/is_close.py" rel="nofollow">some additional work</a>.</p>
</div>
<span class="comment-copy">See: <a href="http://stackoverflow.com/a/8940627/50065">stackoverflow.com/a/8940627/50065</a></span>
<span class="comment-copy">What do you want exactly? <code>607[... 20 digits here].xy</code> or <code>6.07e+23</code>?</span>
<span class="comment-copy">Possible duplicate of <a href="http://stackoverflow.com/questions/1995615/how-can-i-format-a-decimal-to-always-show-2-decimal-places">How can I format a decimal to always show 2 decimal places?</a></span>
<span class="comment-copy">why do you round the values to compare? <a href="http://stackoverflow.com/q/4548004/995714">How to correctly and standardly compare floats?</a> <a href="http://stackoverflow.com/q/17333/995714">Most effective way for float and double comparison</a> <a href="https://randomascii.wordpress.com/2012/06/26/doubles-are-not-floats-so-dont-compare-them/" rel="nofollow noreferrer">randomascii.wordpress.com/2012/06/26/…</a> <a href="http://floating-point-gui.de/errors/comparison/" rel="nofollow noreferrer">floating-point-gui.de/errors/comparison</a></span>
<span class="comment-copy">You probably have an <a href="http://meta.stackexchange.com/questions/66377/what-is-the-xy-problem">XY problem</a>. You should ask how to compare, not how to round</span>
<span class="comment-copy">@akira, Please accept sufficient answers with the checkmark button.  It's worth +2 rep to you.</span>
