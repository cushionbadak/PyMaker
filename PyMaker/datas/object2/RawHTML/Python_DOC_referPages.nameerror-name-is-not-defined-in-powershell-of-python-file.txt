<div class="post-text" itemprop="text">
<p>while I run a python file this shows name error but the environmental setting is ok and I can see the python version:</p>
<p><a href="https://i.stack.imgur.com/9d6mO.png" rel="nofollow noreferrer"><img alt="https://i.stack.imgur.com/9d6mO.png" src="https://i.stack.imgur.com/9d6mO.png"/></a></p>
</div>
<div class="post-text" itemprop="text">
<p>If you want to run a python script, you should call the python executable and pass the script path as argument. Type this in the powershell:</p>
<p>(assuming the powershell points to the same directory as <code>shakil.py</code>, otherwise you should either use <code>cd</code> command to go to the script directory, or use relative path like <code>..\some_other_dir\shakil.py</code>)</p>
<pre><code>python shakil.py
</code></pre>
</div>
<span class="comment-copy">Are you trying to run a python script? That's not how you run a script. You enter <code>python &lt;script name&gt;.py</code> into the shell prompt, not the python interactive session.</span>
<span class="comment-copy">thanks, it is working</span>
