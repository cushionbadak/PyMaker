<div class="post-text" itemprop="text">
<p>I see some tensorflow functions which do element-wise operations: <code>reduce_sum</code>, <code>add</code>, <code>negative</code>, etc.</p>
<p>e.g. this code:</p>
<pre><code>import tensorflow as tf
distance = tf.reduce_sum(tf.abs(tf.add(xtr, tf.negative(xte))), reduction_indices=1)
</code></pre>
<p>When I try using the following code instead, I found they have the same effect:</p>
<pre><code>distance = tf.reduce_sum(tf.abs(xtr-xte), reduction_indices=1)
</code></pre>
<p>So I'm wondering:</p>
<p>1, Whether <code>add</code>, <code>negative</code>, <code>multiply</code> can always being replaced by <code>+</code>, <code>-</code>, <code>*</code>? If NOT, can u give me some exceptive examples?</p>
<p>2, Also, how can I list ALL the functions that have corresponding operators?</p>
</div>
<div class="post-text" itemprop="text">
<p>Yes you can always replace them with corresponding operator.</p>
<p>Check this question for full list</p>
<p><a href="https://stackoverflow.com/questions/37900780/in-tensorflow-what-is-the-difference-between-tf-add-and-operator">In tensorflow what is the difference between tf.add and operator (+)?</a></p>
</div>
<span class="comment-copy">I've never seen someone "raise" an example. Reword a bit, please?</span>
<span class="comment-copy">@connectyourcharger thx, I've edited the question.</span>
