<div class="post-text" itemprop="text">
<p>I'm writing a for loop that checks if a variable has a least 3 characters in it, but it doesn't seem to work.</p>
<p>Any solutions / methods?</p>
<p>thanks</p>
</div>
<div class="post-text" itemprop="text">
<p>You can use either the <code>len</code> built-in method or regex.</p>
<ol>
<li><code>len</code> built-in:</li>
</ol>
<pre class="lang-py prettyprint-override"><code>s = 'a'
print(len(s) &gt;= 3)
</code></pre>
<p>Doc: <a href="https://docs.python.org/3/library/functions.html#len" rel="nofollow noreferrer"><code>len</code> built in</a></p>
<ol start="2">
<li>Regex</li>
</ol>
<pre class="lang-py prettyprint-override"><code>import re

s = 'apple'
print(re.search('^[a-zA-Z0-9]{3,}$', s))
</code></pre>
<p>Doc: <a href="https://docs.python.org/3/library/re.html#re.Pattern.search" rel="nofollow noreferrer"><code>re.search</code></a></p>
</div>
<div class="post-text" itemprop="text">
<p>You can use the len built-in method.</p>
<pre><code>myStr = 'foo'
print(len(myStr) &gt;= 3)
</code></pre>
<p>Description
The len() method returns the length of the string.</p>
<p>Syntax
Following is the syntax for len() method −</p>
<pre><code>len(str)
</code></pre>
<p>more info on python3 len() method here : <a href="https://www.tutorialspoint.com/python3/string_len.htm" rel="nofollow noreferrer">https://www.tutorialspoint.com/python3/string_len.htm</a></p>
</div>
<span class="comment-copy">Do you know how to find the length of a string?</span>
<span class="comment-copy"><code>if len(string) &gt;= 3:       # do what you want to do.</code> Why use a sword, if a needle can suffice?</span>
<span class="comment-copy">@taurus05 answered your question, but I feel you're not actually asking the correct question. Try to explain your problem, maybe showing a <a href="https://stackoverflow.com/help/mcve">Minimal, Complete, and Verifiable example</a></span>
<span class="comment-copy">It is not a good practice to use a variable name that shadows a built-in, in this case <code>str</code>. Pick another variable name.</span>
<span class="comment-copy">True! Fix, thanks for that!</span>
<span class="comment-copy">It is not a good practice to use a variable name that shadows a built-in, in this case <code>str</code>. Pick another variable name.</span>
<span class="comment-copy">thanks, rectified that!</span>
