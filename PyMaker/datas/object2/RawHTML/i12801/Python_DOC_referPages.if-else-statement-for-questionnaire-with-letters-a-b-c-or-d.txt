<div class="post-text" itemprop="text">
<p>I need help with my if/else statement its for a which marvel character questionnaire how do I add "while" to make the python coding correct. Thanks. I want to make sure they cant enter any letters besides A,B,C,OR D.</p>
<pre><code>if str(input())) not ('A','B','C','D')
else ("ask them again");
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>One simple option is check each letter:</p>
<pre><code>choice='f'
while choice!='A' and choice!='B' and choice!='C' and choice!='D':
  choice=input('Enter letter')
</code></pre>
<p>if you want to check regardless of case use <code>.upper()</code> right after the <code>input(..)</code></p>
</div>
<div class="post-text" itemprop="text">
<pre><code>answer = ""
possibleAnswers = {"A","B","C","D"}

while answer not in possibleAnswers:
    answer = input("Select an answer:")
</code></pre>
</div>
<span class="comment-copy"><a href="https://docs.python.org/3/tutorial/" rel="nofollow noreferrer"><b>The official python tutorial</b></a></span>
<span class="comment-copy">Thank you sir I appreciate the help.</span>
<span class="comment-copy">Thanks alot I appreciate the help.</span>
