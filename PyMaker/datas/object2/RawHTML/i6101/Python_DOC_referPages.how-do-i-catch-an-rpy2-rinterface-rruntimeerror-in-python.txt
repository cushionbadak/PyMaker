<div class="post-text" itemprop="text">
<p><strong>Setup:</strong></p>
<ul>
<li>Python 3.5.3 |Continuum Analytics, Inc.| (default, Mar  6 2017, 12:15:08) </li>
<li>Mac OSX 10.13.1</li>
</ul>
<p><strong>Problem:</strong> </p>
<p>I have downloaded the following R script <a href="https://github.com/daleroberts/heston/blob/master/heston.r" rel="nofollow noreferrer">https://github.com/daleroberts/heston/blob/master/heston.r</a>
and I repeatedly call one of the functions in there via Python using the package RPy2. Now, for some of my inputs that I feed into the R function, R returns the following error:</p>
<blockquote>
<p>rpy2.rinterface.RRuntimeError: Error in integrate(PIntegrand, lower =
  0, upper = Inf, lambda, vbar, eta,  :    roundoff error was detected</p>
</blockquote>
<p>How do I go about catching this RuntimeError in Python?</p>
</div>
<div class="post-text" itemprop="text">
<p>The <code>rpy2.rinterface.RRuntimeError</code> <a href="https://bitbucket.org/rpy2/rpy2/src/6c9ec88e0be3642f3e657d670d55b4f2f51731cf/rpy/rinterface_lib/embedded.py?fileviewer=file-view-default#embedded.py-57" rel="nofollow noreferrer">is derived from <code>Exception</code></a> so you should be able to catch it as you would with any other exception.</p>
<pre><code>try:
    # your code
except rpy2.rinterface.RRuntimeError:
    # handle exception
</code></pre>
<p>More on this: <a href="https://docs.python.org/3/tutorial/errors.html#handling-exceptions" rel="nofollow noreferrer">https://docs.python.org/3/tutorial/errors.html#handling-exceptions</a></p>
</div>
<div class="post-text" itemprop="text">
<p>Python makes catching exceptions relatively easy.</p>
<pre><code>try:
    # some code
except Exception, e:
    # Log the exception.
</code></pre>
</div>
<span class="comment-copy">Except it's a runtime error and not an exception?</span>
<span class="comment-copy">A RuntimeError is an exception in Python. <a href="https://docs.python.org/2/library/exceptions.html#exceptions.RuntimeError" rel="nofollow noreferrer">docs.python.org/2/library/…</a></span>
