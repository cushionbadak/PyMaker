<div class="post-text" itemprop="text">
<p>I have created a list which has the totality of all the data in the csv file.<br/>
How do I seperately call upon data in rows and columns?</p>
<p>For instance:</p>
<pre><code>   **a,  b  ,c** 
**1**  a1  b1  c1
**2**  a2  b2  c2
</code></pre>
<p>How can I identify a single cell within the list?</p>
</div>
<div class="post-text" itemprop="text">
<p>try below code:</p>
<pre><code>l = ['a', 'b', 'c','1','a1', 'b1', 'c1', '2', 'a2', 'b2','c2']
columns = 3
result = list(zip(*[iter(l[columns:])]*(columns+1)))
result2 = {i[0]:i[1:] for i in result}
item_id = '2'
result2[item_id]
</code></pre>
<p>output:</p>
<pre><code> ('a2', 'b2', 'c2')
</code></pre>
<p>or you could try below code:</p>
<pre><code>l = ['a', 'b', 'c','1','a1', 'b1', 'c1', '2', 'a2', 'b2','c2']
columns = 3
item_id = '2'
index = l.index(item_id)
l[index:index+columns]
</code></pre>
<p>output:</p>
<pre><code>['a2', 'b2', 'c2']
</code></pre>
</div>
<span class="comment-copy">Are you using Numpy / Pandas?</span>
<span class="comment-copy">No, and I cannot as well.</span>
<span class="comment-copy">We don't know how your data looks.  You say you have it in a list, is this a list for each row?  Can you post an example?</span>
<span class="comment-copy"><a href="https://docs.python.org/3/tutorial/datastructures.html#tuples-and-sequences" rel="nofollow noreferrer">docs.python.org/3/tutorial/…</a></span>
<span class="comment-copy">@YousufFarhan sounds like you want a <a href="https://docs.python.org/3/tutorial/datastructures.html#dictionaries" rel="nofollow noreferrer">dictionary</a></span>
