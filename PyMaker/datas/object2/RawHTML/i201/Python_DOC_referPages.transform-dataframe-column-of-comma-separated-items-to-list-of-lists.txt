<div class="post-text" itemprop="text">
<p>I want to transform a dataframe where in column 'predictors' I have a list of items comma separated, into a list of lists, like:</p>
<p><strong>[[Item_1,Item_2], [Item_3,Item_4,Item_5]]</strong></p>
<p>Any suggestion please?</p>
<p><a href="https://i.stack.imgur.com/KmPup.png" rel="nofollow noreferrer"><img alt="enter image description here" src="https://i.stack.imgur.com/KmPup.png"/></a></p>
</div>
<div class="post-text" itemprop="text">
<p>Don't know if you still need it, but here's a solution:</p>
<pre><code>result = [ss.split(',') for ss in df.predictors] # df is your dataframe
</code></pre>
</div>
<span class="comment-copy">What is the type of the items themselves?</span>
<span class="comment-copy">Items are strings separated by comma, ex: beer, milk, bread, etc. I need that all items in a row to be contained into a separated list and all lists into one container list</span>
<span class="comment-copy">@FlorianC: Probably you want <code>JSON</code> output? Read about <a href="https://docs.python.org/3/library/json.html#module-json" rel="nofollow noreferrer">module-json</a></span>
