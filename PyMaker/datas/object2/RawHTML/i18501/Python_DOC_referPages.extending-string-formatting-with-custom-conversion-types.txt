<div class="post-text" itemprop="text">
<p><a href="https://docs.python.org/3/library/string.html#string.Formatter" rel="nofollow">string.Formatter</a> allows the extension of new style formatting with custom conversion types.</p>
<p>Is this possible for the older <code>"%"</code>-style formatting strings too? Is there a library for this? </p>
</div>
<div class="post-text" itemprop="text">
<p>Maybe</p>
<pre><code>class CustomFormat(object):
    def __init__(self, obj):
        self.Object = obj

    def __str__(self):
        return str(self.Object).upper()  # your magic goes here...

print "abc%s" % customFormat("hello")
# abcHELLO
</code></pre>
</div>
<span class="comment-copy">if you overload the <code>%</code> op...</span>
<span class="comment-copy">but don't even try.</span>
<span class="comment-copy">Why do you want to do this?</span>
