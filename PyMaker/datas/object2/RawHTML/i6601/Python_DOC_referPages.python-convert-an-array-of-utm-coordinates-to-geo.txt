<div class="post-text" itemprop="text">
<p>I have the following method for converting coordinates:</p>
<pre><code>def UTM_to_Geo(x, y):
...
return [x,y]
</code></pre>
<p>And I have an array of x's and y's (2d array) called <code>locations</code>.
Can I invoke the method on each of the items in the array with <code>map</code>?</p>
</div>
<div class="post-text" itemprop="text">
<p>This should do the trick:</p>
<pre><code>geos=list(map(lambda l: UTM_toGeo(l[0], l[1]), locations))
</code></pre>
</div>
<span class="comment-copy">You can use <a href="https://docs.python.org/3/howto/functional.html#small-functions-and-the-lambda-expression" rel="nofollow noreferrer">lambda expression</a> to pass argument.</span>
<span class="comment-copy">The question is tagged python-2.7 where <code>map()</code> already returns a list and it doesn't make sense to copy all the elements into yet another list.</span>
