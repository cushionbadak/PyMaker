<div class="post-text" itemprop="text">
<p>I've installed python 3.4 on win7. I need faker module to be installed to run the script.</p>
<p>error:
from faker import Factory<br/>
ImportError: No module named 'faker'.</p>
<p>I've downloaded few zip's with faker, but unfortunately have no idea how to install it.
Please give simple instructions, how to make it work.(on windows)   </p>
</div>
<div class="post-text" itemprop="text">
<p>Since you have downloaded the zip source from <a href="https://github.com/joke2k/faker" rel="nofollow">https://github.com/joke2k/faker</a> according to your comments , you can also do the following after changing to the directory where you have setup.py -</p>
<pre><code>python setup.py install
</code></pre>
<p>According to the github page itself, to install using <code>pip</code> do -</p>
<pre><code>pip install fake-factory
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>Use the Python package tool <code>pip</code>.</p>
<p>Open a command prompt and do</p>
<pre><code>pip install fake-factory
</code></pre>
<p>or </p>
<pre><code>python -m pip install fake-factory
</code></pre>
<p>See <a href="https://docs.python.org/3/installing/" rel="nofollow">the documentation</a> for more information.</p>
</div>
<span class="comment-copy">Also, where did you download the zip for faker from? And what zip?</span>
<span class="comment-copy"><a href="https://github.com/joke2k/faker" rel="nofollow noreferrer">github.com/joke2k/faker</a>  for example</span>
