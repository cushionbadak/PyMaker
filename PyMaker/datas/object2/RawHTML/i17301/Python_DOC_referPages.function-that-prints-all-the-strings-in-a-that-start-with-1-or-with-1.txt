<div class="post-text" itemprop="text">
<div class="question-status question-originals-of-duplicate">
<p>This question already has an answer here:</p>
<ul>
<li>
<a dir="ltr" href="/questions/22273879/trouble-with-my-program-to-filter-a-list-of-strings">Trouble with my program to filter a list of strings</a>
<span class="question-originals-answer-count">
                    3 answers
                </span>
</li>
</ul>
</div>
<p>So I am trying to write a function that can print all the strings that start with '-1' or with '1'.</p>
<p>This is currently what I have so far: </p>
<pre><code>def Sample(A):
    for i in range(len(A)):
        if A[i] == 1 or A[i] == -1 : print (A[i])
</code></pre>
<p>I am wondering if I am doing this right. Many thanks in advance.</p>
<p>The solution provided in the previous post did not work for me. I asked the question again and the solution works for me. </p>
</div>
<div class="post-text" itemprop="text">
<p>Here is the fixed for your snippet:</p>
<pre><code>def Sample(A):
    for i in range(len(A)):
        if A[i][0] == '1' or A[i][0:2] == '-1' :
            print (A[i])

Sample(['345', '-1', '-9876', '-100', '123'])
</code></pre>
<p>Output:</p>
<pre><code>-1
-100
123
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>Assuming <code>A</code> is a list or tuple, you have to loop through its elements, then check if those element starts with <code>"1"</code> or <code>"-1"</code> using <a href="http://docs.python.org/2/library/stdtypes.html#str.startswith" rel="nofollow"><code>startswith()</code></a> function:</p>
<pre><code>for e in A: # e will be the element
    if e.startswith("1") or e.startswith("-1"):
        print e
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>Use <a href="http://docs.python.org/3/library/stdtypes.html#str.startswith" rel="nofollow"><code>str.startswith(prefix)</code></a> to check whether the string <code>str</code> starts with the <code>prefix</code>. The <code>prefix</code> can be a substring or a tuple of substrings:</p>
<pre><code>&gt;&gt;&gt; def sample(a):
...     for s in a:
...         if s.startswith(('1', '-1')):
...             print(s)
...
&gt;&gt;&gt; sample(['345', '-1', '-9876', '-100', '123'])
-1
-100
123
</code></pre>
</div>
<span class="comment-copy">You asked this question this morning what was wrong with the answers then</span>
<span class="comment-copy">This solution is very good and helped me out in learning the kinks of my program.</span>
<span class="comment-copy">The question is tagged <code>python-3.x</code>: <code>print</code> should be function form.</span>
<span class="comment-copy">e.startswith(('1','-1'))</span>
<span class="comment-copy">I think you are incorrect according to help Help on built-in function startswith:  startswith(...)     S.startswith(prefix[, start[, end]]) -&gt; bool          Return True if S starts with the specified prefix, False otherwise.     With optional start, test S beginning at that position.     With optional end, stop comparing S at that position.     prefix can also be a tuple of strings to try.</span>
<span class="comment-copy">@PyNEwbie, The argument of <code>str.startswith</code> can be a tuple of strings. Please read the documentation carefully.</span>
<span class="comment-copy">@PyNEwbie, According to <a href="http://docs.python.org/3/library/stdtypes.html#str.startswith" rel="nofollow noreferrer">the documentation</a>: <code>prefix</code> can also be a tuple of prefixes to look for.</span>
<span class="comment-copy">So what are you saying, you claim in your answer that a is a string to check for b - I don't see that in the docs str is the string to be checked from my read</span>
<span class="comment-copy">look at my comment below in your case it should be str.startswith('1','-1')</span>
