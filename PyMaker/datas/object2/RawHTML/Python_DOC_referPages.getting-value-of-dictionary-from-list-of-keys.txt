<div class="post-text" itemprop="text">
<div class="question-status question-originals-of-duplicate">
<p>This question already has an answer here:</p>
<ul>
<li>
<a dir="ltr" href="/questions/14692690/access-nested-dictionary-items-via-a-list-of-keys">Access nested dictionary items via a list of keys?</a>
<span class="question-originals-answer-count">
                    14 answers
                </span>
</li>
</ul>
</div>
<p>I need to access an element from dictionary by using list of keys.</p>
<p>Dictionary, </p>
<pre><code>groups ={
    'servers': {
        'unix_servers': {
            'server_a': '10.0.0.1',
            'server_b': '10.0.0.2',
            'server_group': {
                'server_e': '10.0.0.5',
                'server_f': '10.0.0.6'
            }
        },
        'windows_servers': {
            'server_c': '10.0.0.3',
            'server_d': '10.0.0.4'
        }
    }
}
</code></pre>
<p>Here I want to access key 'server_e' by using the list of keys,</p>
<pre><code>keys = ['servers', 'unix_servers', 'server_group', 'server_e']
</code></pre>
<p>These keys are in order but I dont know beforehand what keys are in this list.</p>
<p>So how can I access 'server_e' value i.e. '10.0.0.5' by using this list of keys ?</p>
</div>
<div class="post-text" itemprop="text">
<p>This can be done like this, replacing the dict you are querying as you move down the list of keys:</p>
<pre><code>d = groups 
for key in keys:
    d = d[key]

print(d)
</code></pre>
<p>If you want to be able to change the end value you can store a reference to the next-to-last element:</p>
<pre><code>d = groups 
p = None
for key in keys:
    p = d
    d = d[key]

p[key] = "new value here"
</code></pre>
</div>
<span class="comment-copy">You may be able to iterate over the dictionary - see <a href="https://stackoverflow.com/questions/3294889/iterating-over-dictionaries-using-for-loops" title="iterating over dictionaries using for loops">stackoverflow.com/questions/3294889/…</a></span>
<span class="comment-copy">@jessie nope, he want to iterate on the list instead.</span>
<span class="comment-copy">@brunodesthuilliers you got me</span>
<span class="comment-copy">worked !!!!! ty</span>
<span class="comment-copy">Hi Christian, how to change that value after accessing it? I forgot to add this in question.</span>
<span class="comment-copy">@ShoyebSheikh updated</span>
<span class="comment-copy">Thanks heaps !!!!</span>
