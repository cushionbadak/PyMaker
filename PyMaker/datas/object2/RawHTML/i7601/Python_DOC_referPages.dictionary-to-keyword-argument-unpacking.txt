<div class="post-text" itemprop="text">
<p>I want to pass a dictionary contains unknown numbers of key/pair to a function called <code>save</code>, and inside the function, all the key/pair from the dictionary will be unpacked into keyword arguments for a class. </p>
<p>How can I achieve something like this? </p>
<pre><code>def save(my_dict):   
   my_model = MyMode(k1=v1, k2=v2, k3=v3...)
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>my_model = MyMode(**my_dict) is the solution. </p>
</div>
<span class="comment-copy">do you mean <code>MyMode(**my_dict)</code>?</span>
<span class="comment-copy">See the <a href="https://docs.python.org/3/tutorial/controlflow.html?highlight=unpacking#keyword-arguments" rel="nofollow noreferrer">odcumentation</a> regarding <code>**</code> dictionary unpacking.</span>
