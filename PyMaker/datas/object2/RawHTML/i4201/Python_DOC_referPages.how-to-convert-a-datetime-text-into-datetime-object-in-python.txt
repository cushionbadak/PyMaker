<div class="post-text" itemprop="text">
<div class="question-status question-originals-of-duplicate">
<p>This question already has an answer here:</p>
<ul>
<li>
<a dir="ltr" href="/questions/127803/how-do-i-parse-an-iso-8601-formatted-date">How do I parse an ISO 8601-formatted date?</a>
<span class="question-originals-answer-count">
                    24 answers
                </span>
</li>
</ul>
</div>
<p>I want to convert a <code>datetime</code> string to <code>datetime</code> object which will be further processed to display in a different format.</p>
<p>The string is in the for, <code>2018-04-24T16:42:17Z</code>.</p>
<p>I have tried the following method but it gives error.</p>
<pre><code>import datetime
datetime.datetime.strptime('2018-04-24T16:42:17Z', '%b %d %Y %I:%M%p')


Traceback (most recent call last):
  File "&lt;stdin&gt;", line 1, in &lt;module&gt;
  File "D:\Python\lib\_strptime.py", line 510, in _strptime_datetime
    tt, fraction = _strptime(data_string, format)
  File "D:\Python\lib\_strptime.py", line 343, in _strptime
    (data_string, format))
ValueError: time data '2018-04-24T16:42:17Z' does not match format '%b %d %Y %I:%M%p'
</code></pre>
<p>Please help.</p>
</div>
<div class="post-text" itemprop="text">
<p>You have some mismatch in your string representation of your datetime.</p>
<p><strong>Try:</strong></p>
<pre><code>import datetime
print(datetime.datetime.strptime('2018-04-24T16:42:17Z', '%Y-%m-%dT%H:%M:%SZ'))
</code></pre>
<p><strong>Output:</strong></p>
<pre><code>2018-04-24 16:42:17
</code></pre>
</div>
<span class="comment-copy">Why is it getting downvoted ?</span>
<span class="comment-copy">The format string you're trying to use isn't even remotely close to the format you're trying to parse. Look at <a href="https://docs.python.org/3/library/datetime.html#strftime-strptime-behavior" rel="nofollow noreferrer">the docs</a>. Why did you think <code>%b</code> ("Month as locale’s abbreviated name") would match <code>2018</code>, a space would match a hyphen, etc.?</span>
<span class="comment-copy">Got ahead <a href="https://stackoverflow.com/questions/466345/converting-string-into-datetime" title="converting string into datetime">stackoverflow.com/questions/466345/…</a></span>
<span class="comment-copy">But you're supposed to give the format of the string you're trying to process, which clearly starts with a year.</span>
<span class="comment-copy">The error is pretty straightforward. The format you passed in (the second argument) doesn't work for the string you are parsing (the first argument). Why did you expect that format string to work? Please share your research. Did you read the documentation on how <code>datetime.strptime()</code> works?</span>
