<div class="post-text" itemprop="text">
<p>I wrote my program in python 3.6.2,i want install thread pakage by:</p>
<pre><code>pip install thread
</code></pre>
<p>but i get error bellow:</p>
<pre><code>Could not find a version that satisfies the requirement thread (from versions: )
No matching distribution found for thread
</code></pre>
<p>also use command bellow :</p>
<pre><code>pip3 install thread 
</code></pre>
<p>but again get error top.
how can fix it?</p>
</div>
<div class="post-text" itemprop="text">
<p>As far as <code>$pip</code> is concerned its a package manager that search <a href="https://pypi.python.org/pypi" rel="nofollow noreferrer">this index</a> for public open source packages, but there is no package name <a href="https://pypi.python.org/simple/thread/" rel="nofollow noreferrer">thread</a></p>
<p>For Python3 threading you can follow this simple <a href="https://www.tutorialspoint.com/python3/python_m%E2%80%A6" rel="nofollow noreferrer">tutorial</a></p>
</div>
<span class="comment-copy">there is no package name thread <a href="https://pypi.python.org/simple/thread/" rel="nofollow noreferrer">pypi.python.org/simple/thread</a></span>
<span class="comment-copy">Python 3 already comes with a threading module, is that what you want? <a href="https://docs.python.org/3/library/threading.html#module-threading" rel="nofollow noreferrer">docs.python.org/3/library/threading.html#module-threading</a></span>
