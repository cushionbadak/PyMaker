<div class="post-text" itemprop="text">
<div class="question-status question-originals-of-duplicate">
<p>This question already has an answer here:</p>
<ul>
<li>
<a dir="ltr" href="/questions/5658369/how-to-input-a-regex-in-string-replace">How to input a regex in string.replace?</a>
<span class="question-originals-answer-count">
                    7 answers
                </span>
</li>
</ul>
</div>
<p>I want to remove all characters except <code>a-z</code>, <code>A-Z</code>, and spaces from a title. I then want to convert all the characters to lower case.</p>
</div>
<div class="post-text" itemprop="text">
<p>Use the built-in <a href="https://docs.python.org/3/library/re.html" rel="nofollow noreferrer"><code>re</code></a> module.</p>
<pre><code>re.sub(r'[^a-zA-Z ]', '', my_string).lower()
</code></pre>
<p>Here's an example:</p>
<pre><code>&gt;&gt;&gt; re.sub(r'[^a-zA-Z ]', '', 'This is 1 test string; I like it.').lower()
'this is  test string i like it'
</code></pre>
</div>
<span class="comment-copy">Welcome to Stack Overflow! Next time, please take a look at our <a href="https://stackoverflow.com/help/how-to-ask">"How do I ask a good question?"</a> page.</span>
<span class="comment-copy">If regex is not mandotory, use the good <code>.lower</code> <a href="https://docs.python.org/3.4/library/stdtypes.html?highlight=str.lower#str.lower" rel="nofollow noreferrer">doc</a>, related So question(<a href="https://stackoverflow.com/questions/6797984/how-to-convert-string-to-lowercase-in-python" title="how to convert string to lowercase in python">stackoverflow.com/questions/6797984/…</a>)</span>
<span class="comment-copy">should be <code>r'[^a-z A-Z]'</code> yours will remove the spaces as well which the OP wants to keep</span>
<span class="comment-copy">@officialaimm: You are correct.</span>
