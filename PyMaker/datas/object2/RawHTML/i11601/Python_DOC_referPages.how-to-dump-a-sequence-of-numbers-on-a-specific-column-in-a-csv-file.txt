<div class="post-text" itemprop="text">
<p>I am trying to generate a sequence of numbers in an existing data file for a specific column. Could someone share your inputs on how to accomplish this in python. Thank you.</p>
<p><strong><code>Input :-</code></strong></p>
<pre><code>1,A,X
2,B,X
3,C,X
</code></pre>
<p><strong><code>Output :-</code></strong></p>
<pre><code>1,A,101
2,B,102
3,C,103
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>Use the <a href="https://docs.python.org/3/library/csv.html" rel="nofollow">csv</a> library:</p>
<pre><code>import csv

with open('file.csv', 'rb') as csv_file_to_read, open('file2.csv', 'wb') as csv_file_to_write:
    csv_content = csv.reader(csv_file_to_read)
    csv_writer = csv.writer(csv_file_to_write, delimiter=' ',
                        quotechar='|', quoting=csv.QUOTE_MINIMAL)
    csv_content = [_ for _ in csv_content]

    for i in range(csv_content):
        csv_content[i].insert(0, i)
        csv_content[i].append(100 + i)

    csv_writer.writerow(csv_content)
</code></pre>
</div>
<span class="comment-copy">What is your file format ? What sequence ?</span>
<span class="comment-copy">Need a sequence of integers dumped in a particular column 'x'.</span>
<span class="comment-copy">what is the file format ?</span>
<span class="comment-copy">Its a .csv file</span>
<span class="comment-copy">@SOaddict I guess you didn't earn those 3800+ rep points by reviewing bad questions :P (where's all the information??? File format? Input data? Desired output? Current code?)</span>
<span class="comment-copy">Pls find the sample input n output</span>
<span class="comment-copy">@SOaddict I edited my example.</span>
<span class="comment-copy">can u pls explain the code... I am new to python.. I would like to read the input file name and output into a different file. thanks.</span>
<span class="comment-copy">@SOaddict What part you don't understand ? I edited my example to write in a secondary file.</span>
<span class="comment-copy">Infact I am totally new to python and just started learning it...</span>
