<div class="post-text" itemprop="text">
<div class="question-status question-originals-of-duplicate">
<p>This question already has an answer here:</p>
<ul>
<li>
<a dir="ltr" href="/questions/15112125/how-to-test-multiple-variables-against-a-value">How to test multiple variables against a value?</a>
<span class="question-originals-answer-count">
                    21 answers
                </span>
</li>
</ul>
</div>
<p>How to make this code work?</p>
<p>I want to check if some substrings are in list and some are not.</p>
<pre><code>if 'A_' and 'V' and '3' and not 'ES' and not 'SF' in my_list[0:3]:
    print("It's True")
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>You could use the <a href="https://docs.python.org/3/library/functions.html#all" rel="nofollow"><code>all()</code></a> function:</p>
<pre><code>if all(x in L for x in wanted) and all(x not in L for x in not_wanted):
   # do stuff
</code></pre>
</div>
<span class="comment-copy">Can you show me how to use this function with my example ?</span>
<span class="comment-copy">@Luk: Just use <code>L = my_list[:3]; wanted = ["A_", "V", "3"]; not_wanted = ["ES", "SF"]</code></span>
