<div class="post-text" itemprop="text">
<p>I have these lines of code:</p>
<pre><code>Num1 = random.randint(1, 10)

Num2 = random.randint(1, 10)

Num3 = random.randint(1, 10)

Num4 = random.randint(1, 10)

Number = Num1 + Num2 + Num3 + Num4

print(Number)
</code></pre>
<p>I want to make it so that instead of it adding the Nums together and printing a number between 4 and 40, it prints it like this: "1234".</p>
</div>
<div class="post-text" itemprop="text">
<p>You can print the variables and define a separator character like so:</p>
<pre><code>print(Num1, Num2, Num3, Num4, sep="")
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>You need to convert each value to a string and concatenate them. One simple way is</p>
<pre><code>number = "%d%d%d%d" % (Num1, Num2, Num3, Num4)
</code></pre>
</div>
<div class="post-text" itemprop="text">
<pre><code>print ''.join([str(x) for x in [1,2,3,4]])
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>Another way is using <a href="https://docs.python.org/3/library/stdtypes.html#str.format" rel="nofollow"><code>str.format</code></a>.</p>
<pre><code>number = "{}{}{}{}".format(Num1, Num2,Num3, Num4)
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>You can encapsulate the statement with str():</p>
<pre><code>Num1 = str(random.randint(1, 10))

Num2 = str(random.randint(1, 10))

Num3 = str(random.randint(1, 10))

Num4 = str(random.randint(1, 10))

Number = Num1 + Num2 + Num3 + Num4

print(Number)
</code></pre>
</div>
<span class="comment-copy">Indeed I did, thank you.</span>
