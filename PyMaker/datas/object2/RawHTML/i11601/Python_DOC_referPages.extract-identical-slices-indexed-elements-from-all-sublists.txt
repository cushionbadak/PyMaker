<div class="post-text" itemprop="text">
<p>I'm trying to get <code>93, 96</code>, etc. I tried using slice without loop but it didn't work. I've tried it with:</p>
<pre><code>a = [[[1, 0, 1], 93], [[1, 0, 1], 96]]
print(a[:][:][1])
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>You may use comprehensions.</p>
<pre><code>a = [[[1, 0, 1], 93], [[1, 0, 1], 96]]
print([i[1] for i in a])
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>Jérôme's answer is probably more instructive and beneficial for a learner, but for conciseness' sake, this transposition should be suggested as well (using <a href="https://docs.python.org/3/library/functions.html#zip" rel="nofollow"><code>zip</code></a> and <a href="https://docs.python.org/3/tutorial/controlflow.html#unpacking-argument-lists" rel="nofollow">argument list unpacking</a>):</p>
<pre><code>print(zip(*a)[1])
</code></pre>
</div>
