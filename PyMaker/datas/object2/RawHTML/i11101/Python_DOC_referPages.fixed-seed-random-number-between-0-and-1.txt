<div class="post-text" itemprop="text">
<p>I want to generate list of fixed random numbers by giving a seed between 0 and 1 in python. For example, l = [0.1, 0.5, 0.6, 0.9, 0.75]. I can get this by using random.random(). But every time it gives a new random number list. I want to fix it by giving some some seed. </p>
<p>How can I do that. Thanks in advance!</p>
</div>
<div class="post-text" itemprop="text">
<p>you can do it like this:</p>
<pre><code>import random
l = []
n = 10      # you can change it to whatever you want, it is list length
for i in range(n):
    random.seed(i)
    l += [random.random()]

print(l)
</code></pre>
<p>it will always give same random list.</p>
</div>
<div class="post-text" itemprop="text">
<p>Check out pydocs page for <a href="https://docs.python.org/2/library/random.html#random.seed" rel="nofollow noreferrer">random.seed()</a></p>
<pre><code>[&gt;&gt;&gt; seed = 123
[&gt;&gt;&gt; random.seed(seed)
[&gt;&gt;&gt; i = [random.random() for _ in xrange(5)]
[&gt;&gt;&gt; print i
[0.052363598850944326, 0.08718667752263232, 0.4072417636703983, 0.10770023493843905, 0.9011988779516946]
</code></pre>
<p>Calling the above will return the same list each time.  In other words, the output is deterministic and predictable.</p>
<p>Note that Python 3 users have to use <code>[random.random() for _ in range(5)]</code>.</p>
</div>
<span class="comment-copy"><code>random.seed(0)</code></span>
<span class="comment-copy">See: <a href="https://docs.python.org/3/library/random.html#random.seed" rel="nofollow noreferrer"><code>random.seed()</code></a>.</span>
<span class="comment-copy">This is overly complicated for what he needs: You don't have to generate a new seed for every call to <code>random.random</code>. Calling <code>random.seed(0)</code>, or any specific number, before the loop, would work just fine. Also, <code>l.append(random.random())</code> would be the preferred way in my opinion to build that list</span>
<span class="comment-copy">Thanks @ands i wanted the list this way but @TemporalWolf was also right <code>random.seed(i)</code> should be before the loop.</span>
<span class="comment-copy">yes @TemporalWolf is right you can just put  <code>random.seed(0)</code> before loop, also than you can use just  <code>l = [random.random() for i in range(n)]</code>.</span>
<span class="comment-copy">This is the solution I would have written. set a single, specific seed. Bonus for using <code>for _ in xrange()</code>.</span>
