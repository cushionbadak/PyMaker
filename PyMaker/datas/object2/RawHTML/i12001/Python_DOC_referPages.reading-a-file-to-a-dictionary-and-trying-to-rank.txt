<div class="post-text" itemprop="text">
<p>Given a file that looks as follows:</p>
<pre><code>(u'castro', 5.4716387933635691)
(u'catcher', 5.4716387933635691)
(u'center', 4.3730265046954591)
(u'caus', 5.0661736852554045)
</code></pre>
<p>How can I read this file to a python dictionary and then sort it as per the scores?</p>
<pre><code>d={}
with open("scores.txt") as f:
    for line in f:
        key, val= line.strip().split()
        d[key]=val
</code></pre>
<p>I am trying to sort by val in this case, and then get the result in the following form: </p>
<pre><code>(u'castro', 5.4716387933635691)
(u'catcher', 5.4716387933635691)
(u'caus', 5.0661736852554045)
(u'center', 4.3730265046954591)
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>This produces a nested list:</p>
<pre><code>from operator import itemgetter

with open("scores.txt") as f:
    lst = [i.rstrip("\n")[1:-1].split(", ") for i in f.readlines()]


for i in lst:
    i[1] = float(i[1])
    i[0] = i[0][2:-1]

lst.sort(key=itemgetter(1), reverse=True)
</code></pre>
<p>Output:</p>
<pre><code>&gt;&gt;&gt; lst
[['castro', 5.471638793363569], ['catcher', 5.471638793363569], ['caus', 5.0661736852554045], ['center', 4.373026504695459]]
</code></pre>
<p>To write the names to a file:</p>
<pre><code>with open("scores2.txt", "w") as f:
    for i in lst:
        f.write("{}\n".format(i[0]))
</code></pre>
</div>
<div class="post-text" itemprop="text">
<pre><code>d = sorted(d.items(), key=lambda x: x[1], reverse=True)
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>Python dictionaries do not have order,all you can do is to create a sorted representation.</p>
<pre><code>import operator
d={}
with open("scores.txt") as f:
  for line in f:
     key, val= line.strip().split()
     d[key]=val

sorted_d = sorted(d.items(), key=operator.itemgetter(0))
print sorted_d
</code></pre>
</div>
<span class="comment-copy">Your code isn't indented correctly, please fix it. Note that a Python dictionary is an <i>unordered</i> collection, so there's not much point in trying to sort one. There <i>is</i> an <a href="https://docs.python.org/3/library/collections.html#collections.OrderedDict" rel="nofollow noreferrer"><code>OrderedDict</code></a> subclass of <code>dict</code> in the <code>collections</code> module , but I suspect that a simple <code>list</code> is all you need here.</span>
<span class="comment-copy">Is it possible to keep the output in the same format as the input? The reason being I need it in that format for further processing.</span>
<span class="comment-copy">That's probably the closest you're going to get to the input format.</span>
<span class="comment-copy">Hey, this doesn't seem to sort the list: This is the output I get: [['trial', 5.471638793363569], ['faint', 4.555348061489413], ['nevertheless', 5.471638793363569]</span>
<span class="comment-copy">I think you meant (key=itemgetter(1), reverse=True)</span>
<span class="comment-copy">Yes, edited my answer</span>
<span class="comment-copy">note that this probably wouldn't be the numerical order OP is expecting...</span>
<span class="comment-copy">@AnttiHaapala Yes,that is true should be .itemgetter(0).</span>
<span class="comment-copy">I wish to keep the results in the format shown above. I am receiving the output in this format: ("(u'castro',", '5.4716387933635691)')  How do I get rid of the extra double quotes and brackets?</span>
