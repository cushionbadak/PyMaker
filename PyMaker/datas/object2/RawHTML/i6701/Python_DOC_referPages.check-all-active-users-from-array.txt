<div class="post-text" itemprop="text">
<p>I need to choose all active users with the easiest way</p>
<pre><code>users = [{'id': 1, 'active': False}, {'id': 2, 'active': True}, {'id': 3, 'active': True}, {'id': 4, 'active': True}, {'id': 5, 'active': True}, {'id': 6, 'active': True}]
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>Using a list comprehension would work fine:</p>
<pre><code>users = [user for user in users if user['active']]
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>You could do that with list comprehension,</p>
<pre><code>active_users = [user for user in users if user['active']]
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>Use <a href="https://docs.python.org/3/library/functions.html#filter" rel="nofollow noreferrer">filter</a>. It will return a list in py2.x and a generator for py3.x</p>
<pre><code>filter(lambda x:x['active'], users)
</code></pre>
</div>
<div class="post-text" itemprop="text">
<pre><code>active_user = [user['id']  for user in users if user['active']]
</code></pre>
</div>
<span class="comment-copy">What exactly are you attempting to do? You want to return a list which only contains the dictionaries where <code>active</code> equals <code>True</code>?</span>
<span class="comment-copy">Why not use ORM and filter active users in your query? How you get the <code>users</code> list?</span>
<span class="comment-copy">simple: u = [user for user in users if user['active']]</span>
<span class="comment-copy">For anybody looking for a discussion of "should I use a list comprehension or filter": <a href="https://stackoverflow.com/questions/3013449/list-filtering-list-comprehension-vs-lambda-filter" title="list filtering list comprehension vs lambda filter">stackoverflow.com/questions/3013449/…</a></span>
<span class="comment-copy"><code>x['active']</code> not <code>x['xctive']</code></span>
