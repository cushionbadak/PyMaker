<div class="post-text" itemprop="text">
<p>I´ve got a piece of code that creates an image using matplotlib in Django and saves it to the root directory. I need to change the folder where matplotlib saves the image to /static/img folder.</p>
<pre><code>    if (len(funcion) == 2):
        plotter = plot_regions([
            [(lambda x: (matrizRestricciones[0][2]-matrizRestricciones[0]
            [0]*x)/matrizRestricciones[0][1],True),
            (lambda x: (matrizRestricciones[1][2]-matrizRestricciones[1]
            [0]*x)/matrizRestricciones[1][1],True)]], xlim=(0, 10), ylim=(0,10))
        plt.grid()
        plt.savefig("/static/img/imagen.png")
        plt.close()
</code></pre>
<p>Any idea how to save them there?</p>
<p>Thanks in advance.</p>
</div>
<div class="post-text" itemprop="text">
<p>Try this:</p>
<pre><code>import os
from django.conf import settings

# ... your code ...
# ...
 plt.savefig(os.path.join(settings.BASE_DIR, 'static/img/imagen.png'))
</code></pre>
<hr/>
<p><strong>Note:</strong> From your code, it appears that while you're saving the image you're not giving it a unique name. So, if you create a new image, it will replace the older image. If that is the desired behaviour, ignore this. If not, you can use <a href="https://docs.python.org/3/library/uuid.html#uuid.uuid4" rel="nofollow noreferrer"><code>uuid.uuid4</code></a> to generate unique names for your images. </p>
</div>
<span class="comment-copy">Alternatively, you could write <code>plt.savefig(settings.STATIC_ROOT,'imgaen.png')</code></span>
<span class="comment-copy">Yup, that does the trick. Thanks!</span>
