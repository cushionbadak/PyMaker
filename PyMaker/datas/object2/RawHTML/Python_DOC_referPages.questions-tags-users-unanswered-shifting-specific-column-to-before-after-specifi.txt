<div class="post-text" itemprop="text">
<div class="question-status question-originals-of-duplicate">
<p>This question already has an answer here:</p>
<ul>
<li>
<a dir="ltr" href="/questions/13148429/how-to-change-the-order-of-dataframe-columns">How to change the order of DataFrame columns?</a>
<span class="question-originals-answer-count">
                    25 answers
                </span>
</li>
</ul>
</div>
<p>In dataframe example :</p>
<pre><code>  medcine_preg_oth medcine_preg_oth1 medcine_preg_oth2 medcine_preg_oth3
0          Berplex           Berplex              None              None
1              NaN               NaN               NaN               NaN
2              NaN               NaN               NaN               NaN
3            obmin             obmin              None              None
4              NaN               NaN               NaN               NaN
</code></pre>
<p>'medcine_preg_oth1' 'medcine_preg_oth2' 'medcine_preg_oth3' ,these three columns are in somewhere of dataframe with other columns. . I want to shift these three : medcine_preg_oth1 medcine_preg_oth2 medcine_preg_oth3 to the place of after 'medcine_preg_oth'.</p>
<p>My idea is shifting the specific columns to place after/ before specific columns in dataframe for wider purpose . please suggest me! Thanks</p>
</div>
<div class="post-text" itemprop="text">
<p>You can re-arrange your columns like this:</p>
<pre><code>re_ordered_columns = ['medicine_pred_oth','medcine_preg_oth1','medcine_preg_oth2','medcine_preg_oth3']
df = df[re_ordered_columns+df.columns.difference(re_ordered_columns).tolist()]
</code></pre>
<p>add the remaining columns in place of <code>...</code></p>
</div>
<span class="comment-copy">Thanks for your suggestion , but there are 699 column , included of these four column . So it would take time too!</span>
<span class="comment-copy">I've edited my answer. Let me know if it works for you.</span>
<span class="comment-copy">Still erro :KeyError: "['medicine_pred_oth'] not in index"</span>
<span class="comment-copy">Don't add the list as a string. It should work then</span>
<span class="comment-copy">Hi , I got the answer ,                                     x=df.columns.get_loc('time_use_4') # getting colum index , the place after that column   var_list=new_var # this list are for column list that are needed to move  new_position = x for var in var_list:     cols = df.columns.tolist()     column_to_move = var     new_position += 1     cols.insert(new_position, cols.pop(cols.index(column_to_move)))     df = df[cols]</span>
