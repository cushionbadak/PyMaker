<div class="post-text" itemprop="text">
<p>I have a text file containing:</p>
<pre><code>ROX:KT:3:2
JAG:CJ:1:0
KDO:MST:2:1
KDO:ROX:1:3
JAG:KT:2:1
</code></pre>
<p>I want to calculate the total score for each team. Example:</p>
<pre><code>ROX:6
JAG:3
KDO:3
MST:1
KT: 3
</code></pre>
<p>Here's the code I've worked on:</p>
<pre><code>fileName = input("Enter file name:")
match = open(fileName)
table = []

for line in match:
    contents = line.strip()
    table.append(contents)

dictionary = {}
for line in table:
    teamA,teamB,scoreA,scoreB = line.split(':')
    #I'm stuck here onwards
    .
    .
</code></pre>
<p>From what i could think of, i have to code python to ensure the index of the same team corresponds to the index of the number as displayed in the other section of the text file in order to <strong>obtain the sum</strong>. Regards.</p>
</div>
<div class="post-text" itemprop="text">
<p>Try this:</p>
<pre><code>f = open('filename.txt').readlines()

f = [i.strip('\n') for i in f]


f = [i.split(':') for i in f]



dct = {}

for i in f:
   for b in range(len(i[:2])):
       if i[:2][b] not in dct.keys():
           dct[i[:2][b]] = int(i[2:][b])

        else:
           dct[i[:2][b]] += int(i[2:][b])
print dct
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>You can do so using <a href="https://docs.python.org/3/library/collections.html#collections.defaultdict" rel="nofollow noreferrer"><code>collections.defaultdict</code></a></p>
<pre><code>import collections

fileName = input("Enter file name:")
match = open(fileName)
table = []

for line in match:
    contents = line.strip()
    table.append(contents)

scores = collections.defaultdict(int)
for line in table:
    teamA,teamB,scoreA,scoreB = line.split(':')
    # even if scores does not have the team key, += will create it
    scores[teamA] += int(scoreA)
    scores[teamB] += int(scoreB)
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>Just maintain a dictionary of team wise scores.</p>
<pre><code>sum = {}

for line in table:
    teamA,teamB,scoreA,scoreB = line.split(':')
    if teamA in sum.keys():
        sum[teamA] += scoreA
    else:
        sum[teamA] = scoreA

    if teamB in sum.keys():
        sum[teamB] += scoreB
    else:
        sum[teamB] = scoreB
</code></pre>
</div>
<span class="comment-copy">Possible duplicate of <a href="http://stackoverflow.com/questions/43452802/calculating-number-of-wins-from-a-score">Calculating number of wins from a score</a></span>
<span class="comment-copy">Your question in concept is similar to your previous question(<a href="http://stackoverflow.com/questions/43452802/calculating-number-of-wins-from-a-score/43452971#43452971" title="calculating number of wins from a score">stackoverflow.com/questions/43452802/…</a>). SO is not a programming service so I think it should be closed.</span>
<span class="comment-copy">I just finished my solution with the normal inline if approach, but defaultdict is way better. Thanks for pointing out the pythonic way.</span>
