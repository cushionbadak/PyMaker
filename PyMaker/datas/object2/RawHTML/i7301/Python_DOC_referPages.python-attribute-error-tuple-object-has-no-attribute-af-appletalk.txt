<div class="post-text" itemprop="text">
<pre><code>import socket
def get_protnumber(prefix): 
    for a in dir(socket):
        if a.startswith(prefix):
            getattr((socket, a), a)


get_protnumber('AF_')
</code></pre>
<p>When i run this code it gives me this error:</p>
<pre><code>AttributeError: 'tuple' object has no attribute 'AF_APPLETALK'
</code></pre>
<p>Can someone please explain what's wrong.
How to use function <strong>getattr</strong> and what it does.</p>
</div>
<div class="post-text" itemprop="text">
<p>Docstring:
  getattr(object, name[, default]) -&gt; value</p>
<p><code>getattr((socket, a), a)</code> replace this to <code>getattr(socket, a, a)</code></p>
<pre><code>import socket
def get_protnumber(prefix): 
    for a in dir(socket):
        if a.startswith(prefix):
            getattr(socket, a, a)

get_protnumber('AF_')

5
18
8
20
3
31
7
12
19
2
10
4
23
15
26
13
16
6
17
24
11
16
14
22
30
1
0
25
9
</code></pre>
</div>
<span class="comment-copy"><a href="https://docs.python.org/3/library/functions.html#getattr" rel="nofollow noreferrer">docs.python.org/3/library/functions.html#getattr</a></span>
<span class="comment-copy"><code>getattr(object, name[, default]) -&gt; value</code> so i think <code>getattr(socket, a, a)</code> this will work</span>
<span class="comment-copy">Thanks for reply.After doing <b>getattr(socket, a, a)</b> it gives me 9.not what you have typed above.Why?</span>
<span class="comment-copy">@Taimoor if i do <code>print socket.__dict__['AF_APPLETALK']</code> it give me <code>5</code>. please check what you get.</span>
