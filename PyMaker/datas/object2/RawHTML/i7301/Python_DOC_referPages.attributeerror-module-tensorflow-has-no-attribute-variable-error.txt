<div class="post-text" itemprop="text">
<p>I have used tensor-flow for ONE day, but there come some troubles, when I import tensor-flow, there would be AttributeError: 'module' object has no attribute 'variable'</p>
<p>I use Windows10, Python 3.5.3, Anaconda-3 4.4.0</p>
<p>here is my test code:</p>
<pre><code>import tensorflow as tf
my_var = tf.Variable(tf.linspace(10.0, 13.0, 4)) 
with tf.Session() as sess:
    print (sess.run(my_var))
</code></pre>
<p>I got this error:
<img alt="Error" src="https://i.stack.imgur.com/5WGSZ.png"/></p>
</div>
<div class="post-text" itemprop="text">
<p>It looks like you have written a module, <code>random.py</code>, which shadows the standard library's <a href="https://docs.python.org/3/library/random.html" rel="nofollow noreferrer">random</a> module. Try renaming the file and check if the error goes away. You can tell it's importing your random.py at the bottom of the stacktrace you posted.</p>
</div>
