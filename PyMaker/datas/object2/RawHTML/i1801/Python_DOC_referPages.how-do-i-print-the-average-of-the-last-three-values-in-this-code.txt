<div class="post-text" itemprop="text">
<p>Here's the <strong>code</strong>: </p>
<pre><code>for i in range(0, len(my_list), 6):  
    print(my_list [i] + " " + my_list [i+1] + " - " + my_list [i+2] + " - " + my_list [i+3] + " , " + my_list [i+4] + " , " + my_list [i+5])
</code></pre>
<p>So this code takes a file, and prints 6 lines from the file into a single line, in this format:</p>
<pre><code>Marilyn Munro - 65783234 - 66, 77, 89
Johnny Lang - 83492753 - 45, 65, 45
</code></pre>
<p>I want the code to average out those last three numbers so the output while goes from this:</p>
<pre><code>James Hanniburg - 359372932 - 50, 75, 100
</code></pre>
<p>To this:</p>
<pre><code>James Hanniburg - 359372932 - 81.25 #81.25 being the average of 50, 75, and 100, assuming 100 is worth 50%, and 50 and 75 are each worth 25%.
</code></pre>
<p>EDIT: I forgot to mention, the third value is worth 50% while the other two are each worth 25%.</p>
<p>Any clues??</p>
</div>
<div class="post-text" itemprop="text">
<p>Try this:
(Value weights has been added.)</p>
<pre><code>my_list = ['James', 'Hanniburg', 359372932, 50, 75, 100] # just input

a, b, c, d, e, f = my_list
print('{0} {1} - {2} - {3}'.format(a,b,c,((d*0.25+e*0.25+f*0.5)/3)))
</code></pre>
<p><strong>Output:</strong></p>
<pre><code>C:\Users\Desktop&gt;py x.py
James Hanniburg - 359372932 - 22.916666666666668
</code></pre>
<p>You can also <strong><em>import mean from statistics</em></strong>:</p>
<pre><code>my_list = ['James', 'Hanniburg', 359372932, 50, 75, 100]
from statistics import mean 
a, b, c, *d = my_list
print('{0} {1} - {2} - {3}'.format(a,b,c,mean(d)))
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>You can e.g. modify your code to:</p>
<pre><code>for i in range(0, len(ml), 6): 
    weighted_avg = sum([int(ml[i+3]), int(ml[i+4]), 2*int(ml[i+5])]) / 4
    print(ml[i] + " " + ml[i+1] + " - " + ml[i+2] + " - " + str(weighted_avg))
</code></pre>
<p>This calculates the weighted average by <a href="https://docs.python.org/3/library/functions.html#sum" rel="nofollow noreferrer">summing</a> the <code>int</code>-converted values (multiplying the double-counting last one by <code>2</code>) and dividing the result by <code>4</code>.</p>
</div>
<div class="post-text" itemprop="text">
<pre><code>    for i in range(0, len(my_list), 6):
        print("{} {} - {} - {}".format(my_list[i], my_list[i+1], my_list[i+2],((my_list[i+3]+my_list[4])*0.25+my_list[5]*0.5)/3))
</code></pre>
</div>
<span class="comment-copy">You need to add the three numbers and divide by three. Do it on a separate line to make your code more readable (it doesn't currently fit on the screen). Oh, and Welcome to Stack Overflow. Please read about <a href="https://stackoverflow.com/questions/how-to-ask">How to Ask</a>, and what is on-topic in the <a href="https://stackoverflow.com/help/on-topic">help center</a>.</span>
<span class="comment-copy">Do not forget to accept an answer to the question and upvote it so others can find it easily and the question can be closed.</span>
<span class="comment-copy">Thanks for the help. I forgot to mention that the third value is worth 50% of the grade, while the other two are each worth 25% of the grade. Any help with this? I'm trying to figure it out myself, but something;s not working.</span>
<span class="comment-copy">@antoniomunro I amended the answer to account for the weighted average.</span>
<span class="comment-copy">which is 3rd value?</span>
<span class="comment-copy">@Rarblack That is quite obvious from the question.</span>
