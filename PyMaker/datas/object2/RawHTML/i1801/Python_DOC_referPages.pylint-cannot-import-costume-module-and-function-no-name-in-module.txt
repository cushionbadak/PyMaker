<div class="post-text" itemprop="text">
<p>I have the structure:</p>
<pre><code> |- file run_app.py

 |- folder 'tasks'

 |-- file app.py
</code></pre>
<p>There is a string in <code>run_app.py</code>:</p>
<pre><code>import tasks.app
</code></pre>
<p>And pylint alerts that</p>
<pre><code>run_app.py:8:0: E0611: No name 'app' in module 'tasks' (no-name-in-module)
</code></pre>
<p>When I rename <code>tasks</code> to <code>taskss</code>, the error disappears. Whats this? How to fix this strange behavior if I want to name the folder exactly 'tasks'?</p>
</div>
<div class="post-text" itemprop="text">
<p>Try to include an <code>__init__.py</code> file in the folder.</p>
<p><strong>Reason:</strong></p>
<p>The <code>__init__.py</code> files are required to make Python treat the directories as containing packages.</p>
<hr/>
<p>More info and examples here:
<a href="https://docs.python.org/3/tutorial/modules.html#packages" rel="nofollow noreferrer">https://docs.python.org/3/tutorial/modules.html#packages</a></p>
</div>
<span class="comment-copy">The name <code>tasks</code> is already a python module</span>
<span class="comment-copy">I am not sure:  python  Python 3.6.6 (default, Sep 12 2018, 18:26:19)   [GCC 8.0.1 20180414 (experimental) [trunk revision 259383]] on linux  Type "help", "copyright", "credits" or "license" for more information.  &gt;&gt;&gt; import tasks  Traceback (most recent call last):    File "&lt;stdin&gt;", line 1, in &lt;module&gt;  ModuleNotFoundError: No module named 'tasks'  &gt;&gt;&gt;</span>
<span class="comment-copy">can you try: <code>from tasks import app</code>? have you also included an <code>init</code> in the folder named <code>tasks</code> ?</span>
<span class="comment-copy">see alos this concerning the <code>init</code> file: <a href="https://stackoverflow.com/a/448279/5025009">stackoverflow.com/a/448279/5025009</a></span>
<span class="comment-copy"><b>init</b> helped, thanks. And the link is helpful too</span>
