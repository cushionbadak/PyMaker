<div class="post-text" itemprop="text">
<div class="question-status question-originals-of-duplicate">
<p>This question already has an answer here:</p>
<ul>
<li>
<a dir="ltr" href="/questions/466345/converting-string-into-datetime">Converting string into datetime</a>
<span class="question-originals-answer-count">
                    21 answers
                </span>
</li>
</ul>
</div>
<p>I have a string <code>"Monday, May 18, 2015"</code>. I want to convert it into datetime fromat of python keeping the format same. I mean exact replica of the string but the type changed to datetime. How can I do that?</p>
</div>
<div class="post-text" itemprop="text">
<p>To convert string to date object:</p>
<pre><code>from datetime import datetime

your_date_object = datetime.strptime("Monday, May 18, 2015", '%A, %b %d, %Y')
</code></pre>
<p>To change back date object to string:</p>
<pre><code>your_date_object.strftime('%A, %b %d, %Y')
</code></pre>
<p>For more derivative behaviour:
<a href="https://docs.python.org/2/library/datetime.html#strftime-and-strptime-behavior" rel="nofollow">https://docs.python.org/2/library/datetime.html#strftime-and-strptime-behavior</a></p>
</div>
<div class="post-text" itemprop="text">
<pre><code>from datetime import datetime

date_object = datetime.strptime('MAY 18 2015  1:33PM', '%b %d %Y %I:%M%p')
</code></pre>
<p>Link to the Python documentation for strptime :<a href="https://docs.python.org/2/library/datetime.html#strftime-and-strptime-behavior" rel="nofollow">https://docs.python.org/2/library/datetime.html#strftime-and-strptime-behavior</a></p>
<p>and a link for the strftime format mask : <a href="https://docs.python.org/2/library/datetime.html#datetime.datetime.strptime" rel="nofollow">https://docs.python.org/2/library/datetime.html#datetime.datetime.strptime</a></p>
</div>
<div class="post-text" itemprop="text">
<p>Use the classmethod of <code>datetime.strptime(date_string, format)</code>. This will return a datetime corresponding to <strong>date_string</strong>, parsed according to <strong>format</strong>.</p>
</div>
<span class="comment-copy">A <a href="https://docs.python.org/3/library/datetime.html#datetime-objects" rel="nofollow noreferrer"><code>datetime</code></a> does not <i>have</i> a format, it can be <i>formated using a format</i>. What you ask for makes no sense.</span>
<span class="comment-copy">Now we have a <code>datetime.datetime</code> instance. Fince. The second part of an answer would be to format this instance as a string.</span>
<span class="comment-copy">And it would be nice if the format applies to OP's example</span>
<span class="comment-copy">@Yalami Please don't put code into a comment, <a href="https://stackoverflow.com/posts/30322431/edit">edit</a> your answer instead.</span>
<span class="comment-copy">this is a direct copy paste of this answer <a href="http://stackoverflow.com/a/466376/2141635">stackoverflow.com/a/466376/2141635</a></span>
<span class="comment-copy">How is your answer different from the existing two answers? It is a bad copy.</span>
