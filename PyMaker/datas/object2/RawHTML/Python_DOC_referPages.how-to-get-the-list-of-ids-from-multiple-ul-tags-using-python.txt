<div class="post-text" itemprop="text">
<p>Here I want to get all the ids marked in red box in the given pic, in a list using python. This ids are random and dynamic too!</p>
<p><a href="https://i.stack.imgur.com/0wYOu.png" rel="nofollow noreferrer"><img alt="enter image description here" src="https://i.stack.imgur.com/0wYOu.png"/></a></p>
</div>
<div class="post-text" itemprop="text">
<p>You can serach the <code>ul</code> tag and loop through the search then get the attribute of the element.</p>
<pre><code>elements=driver.find_elements_by_css_selector("ul.plan-card-blue")
id_list=[]
for ele in elements:
    id_list.append(ele.get_attribute("id"))
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>extract all ul items from browser </p>
<pre><code>   links = browser.find_elements_by_tag_name("ul")
</code></pre>
<p>loop the list and get id value    </p>
<pre><code>  for element in links
         idValue = element.get_attribute("id") 
</code></pre>
<p>want to save it in an array use this</p>
<pre><code> idValue=[]
  for element in links:
         idValue.append(element.get_attribute("id")) 
</code></pre>
</div>
<span class="comment-copy">can you show what you have tried</span>
<span class="comment-copy">what do you mean with "keep changing"?</span>
<span class="comment-copy">Add a sample of that HTML or the page you are retrieving this from</span>
<span class="comment-copy">its works too, but also comes with some unnecessary items in the list. to eleminate those, the code should be:  for element in links:      #as the ids are length of five, so its only get those.      if len(element.get_attribute("id"))==5:          idValue.append(element.get_attribute("id"))</span>
