<div class="post-text" itemprop="text">
<p>I try to convert a list of tuples to a dictionary but when I do it using the dict() function, the order of the elements is changed:</p>
<pre><code>l = [('id', 2), ('osm_id', 3), ('sourceid', 4), ('notes', 5), ('ref', 6), ('rtenme', 7), ('ntlclass', 8), ('fclass', 9), ('numlanes', 10), ('srftpe', 11), ('srfcond', 12), ('isseasonal', 13), ('curntprac', 14), ('gnralspeed', 15), ('rdwidthm', 16), ('status', 17), ('bridge', 18), ('iso3', 19), ('country', 20), ('last_updat', 21)]

 dict_l = dict(l)
 print (dict_l)

{'fclass': 9, 'status': 17, 'isseasonal': 13, 'bridge': 18, 'sourceid': 4, 'country': 20, 'notes': 5, 'rtenme': 7, 'iso3': 19, 'last_updat': 21, 'gnralspeed': 15, 'osm_id': 3, 'srfcond': 12, 'numlanes': 10, 'srftpe': 11, 'rdwidthm': 16, 'ref': 6, 'id': 2, 'ntlclass': 8, 'curntprac': 14}
</code></pre>
<p>What am I missing here?</p>
<p>I also tried to loop through the list of tuples and build the dict manually as:</p>
<pre><code>c = 0
for key, value in display_order_list_sorted:
    display_order_dict_sorted[display_order_list_sorted[c][0]] = display_order_list_sorted[c][1]
    c = c + 1
</code></pre>
<p>I faced the same issue. </p>
</div>
<div class="post-text" itemprop="text">
<pre><code>from collections import OrderedDict
l = [('id', 2), ('osm_id', 3), ('sourceid', 4), ('notes', 5), ('ref', 6), ('rtenme', 7), ('ntlclass', 8), ('fclass', 9), ('numlanes', 10), ('srftpe', 11), ('srfcond', 12), ('isseasonal', 13), ('curntprac', 14), ('gnralspeed', 15), ('rdwidthm', 16), ('status', 17), ('bridge', 18), ('iso3', 19), ('country', 20), ('last_updat', 21)]

dict_l = OrderedDict(l)
print (dict_l)
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>Python dictionary is not ordered. You need to use <a href="https://docs.python.org/3/library/collections.html#collections.OrderedDict" rel="nofollow">OrderedDict</a>.</p>
</div>
<span class="comment-copy">Python dictionaries are unordered. Use an <a href="https://docs.python.org/3/library/collections.html#collections.OrderedDict" rel="nofollow noreferrer">OrderedDict</a>.</span>
<span class="comment-copy">See here for an explanation: <a href="http://stackoverflow.com/questions/15479928/why-is-the-order-in-dictionaries-and-sets-arbitrary">Why is the order in dictionaries and sets arbitrary?</a></span>
<span class="comment-copy">You may be pleased to learn that in Python 3.6 <a href="http://stackoverflow.com/questions/39980323/dictionaries-are-ordered-in-python-3-6">dictionaries <i>are</i> ordered</a>. This is currently an implementation detail &amp; shouldn't be relied on, however, it may eventually become an actual language feature.</span>
<span class="comment-copy">These are great news. But we are in 2.7 :)</span>
<span class="comment-copy">Python 2 support will cease in 2020, so you better get ready to move to Python 3. :)</span>
<span class="comment-copy">So then this answer is misleading: <a href="http://stackoverflow.com/questions/6522446/list-of-tuples-to-dictionary" title="list of tuples to dictionary">stackoverflow.com/questions/6522446/…</a></span>
<span class="comment-copy">@user1919, how so?</span>
<span class="comment-copy">Cause I do the same thing (converting an ordered by value list of tuples to a dictionary) and I don't get an odered by value dictionary as this answer implies.</span>
<span class="comment-copy">It's not misleading, it's just not clear. Dictionary COULD return an ordered dictionary  the way you like it, but it's just once in a blue moon if you have a lot of entry. It's "random" basically how it's displayed.</span>
<span class="comment-copy">If you are going to give the same answer as the comment, maybe give an example?</span>
<span class="comment-copy">Yeah, atm that I posted the answer the comment contained only a link that I didnt opened before posting. The example is in the @Ari Gold's answer.</span>
