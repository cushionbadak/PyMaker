<div class="post-text" itemprop="text">
<p>I've met words such as  top-level modules and top-level packages in Python documentation several times. <a href="https://docs.python.org/3/reference/simple_stmts.html#import" rel="nofollow">This is the documentation about such concepts.</a>
What does these words mean?</p>
</div>
<div class="post-text" itemprop="text">
<p>Modules can be nested inside each other. For example <code>os.path</code> is a <code>path</code> module nested inside <code>os</code> module. Top level module is the topmost one. I this case, it's an <code>os</code> module.</p>
</div>
<span class="comment-copy">So both modules and packages have such construct hierarchy, am i right?</span>
<span class="comment-copy">Package is module which can contain more modules inside it: <a href="https://docs.python.org/3/glossary.html#term-package" rel="nofollow noreferrer">docs.python.org/3/glossary.html#term-package</a></span>
<span class="comment-copy">Thank you so much.Now i have a clearer understanding of modules.</span>
