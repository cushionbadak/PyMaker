<div class="post-text" itemprop="text">
<div class="question-status question-originals-of-duplicate">
<p>This question already has an answer here:</p>
<ul>
<li>
<a dir="ltr" href="/questions/55196135/go-one-step-back-and-one-step-forward-in-a-loop-with-python">go one step back and one step forward in a loop with python</a>
<span class="question-originals-answer-count">
                    3 answers
                </span>
</li>
</ul>
</div>
<p>I want to concatenate a word separated by an asterisk in a list of words. The code I am trying is:</p>
<pre><code>import nltk
from nltk.tokenize import word_tokenize
import re

words = ['les','engage', '*', 'ment', 'de','la']

with open ('Fr-dictionary.txt') as fr:
    dic = word_tokenize(fr.read().lower())

l=[ ]
errors=[ ]

for n,word in enumerate (words):
    l.append(word)
    if word == "*":
        print(words[n-1], words[n+1])
        exp = words[n-1] + words[n+1]
        if exp in dic:  
            l.append(exp)
            errors.append(words[n-1])
            errors.append("*")
            errors.append(words[n+1])
        else:
            continue

print(l)
print(errors)


l=frozenset(l)
errors=frozenset(errors)

c=l.difference(errors)

print(list(c)) 
</code></pre>
<p>My output is:</p>
<pre><code>['la', 'les', 'de', 'engagement'] 
</code></pre>
<p>But my desired output has to be in the same order of the original list without:</p>
<pre><code>['les','engagement', 'de','la']
</code></pre>
<p>Is there any other way to get the desired output? </p>
</div>
<div class="post-text" itemprop="text">
<p>try this </p>
<pre><code>    while "*" in words:
        index = words.index("*")
        words.pop(index)
        words.insert(index,words.pop(index-1)+words.pop(index-1))
</code></pre>
</div>
<span class="comment-copy">Tried the <a href="https://www.geeksforgeeks.org/join-function-python/" rel="nofollow noreferrer">join()</a> function? It can take delimiters. You can do without all the loops.</span>
<span class="comment-copy">Sets are not ordered, so you can't expect any specific order by using them.</span>
<span class="comment-copy">Convert your sets to collections. That preserves order.</span>
<span class="comment-copy">@IrfanuddinShafi, After appending the concatenated words, I want to remove the first part of the word, the asterisk and the second part of the word: "engage", "*", "ment"</span>
<span class="comment-copy">@IrfanuddinShafi, how to convert sets to collections? Can you show?</span>
