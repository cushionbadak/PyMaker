<div class="post-text" itemprop="text">
<p>For example</p>
<pre><code>a = [a,b,c,d,a,b,c,d]
b = [1,2,3,4,5,6,7,8]
</code></pre>
<p>The output should be a dictionary which will look like:</p>
<pre><code>{'a':6, 'b':8, 'c':10, 'd':12}
</code></pre>
<p>I tried using zip but it just overwrites the values.</p>
</div>
<div class="post-text" itemprop="text">
<p>I believe you want this:</p>
<pre><code>from collections import defaultdict

a = list("abcdabcd")
b = [1,2,3,4,5,6,7,8]

d = defaultdict(int)
for key, val in zip(a,b):
    d[key] += val
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>It appears you want the sum of the corresponding numbers as your values.</p>
<p>I would suggest using a <a href="https://docs.python.org/3/library/collections.html?highlight=collections%20defaultdict#collections.defaultdict" rel="nofollow noreferrer"><code>collections.defaultdict</code></a> to manage your unique keys, and implement the summation yourself:</p>
<pre><code>import collections

a, b, c, d = "abcd"
a = [a, b, c, d, a, b, c, d]
b = [1, 2, 3, 4, 5, 6, 7, 8]

result = collections.defaultdict(int)

for k,v in zip(a,b):
    result[k] += v
</code></pre>
<p>A <code>defaultdict(int)</code> will create an integer with no initial value (which means '0' by default) when you lookup a key that does not already exist in the dictionary. So you will get 0 + value + value ... for each key, which appears to be what you're looking for.</p>
</div>
<div class="post-text" itemprop="text">
<p>If you want to try without default dict , Here is recipe : </p>
<pre><code>a = "abcdabcd"
b = [1,2,3,4,5,6,7,8]

no_dublicate={}
for item in zip(list(a),b):
    if item[0] not in no_dublicate:
        no_dublicate[item[0]]=[item[1]]
    else:
        for key,value in no_dublicate.items():
            if value==no_dublicate[item[0]]:
                no_dublicate[item[0]]=item[1]+int("".join(str(x) for x in value))

print(no_dublicate)
</code></pre>
<p>output:</p>
<pre><code>{'d': 12, 'c': 10, 'a': 6, 'b': 8}
</code></pre>
</div>
<span class="comment-copy">How is "merging two lists" defined?</span>
<span class="comment-copy">into a dictionary</span>
<span class="comment-copy">It looks like you want to add the integers in <code>b</code> that correspond to the same letter key in <code>a</code>. You need to say that explicitly in your question.</span>
<span class="comment-copy">It's in the OP's code. I assume he has them defined.</span>
<span class="comment-copy">Upvote: same answer, but with explanations</span>
