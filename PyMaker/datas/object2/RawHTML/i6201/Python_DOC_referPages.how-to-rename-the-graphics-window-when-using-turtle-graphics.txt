<div class="post-text" itemprop="text">
<p>I would like to provide a specific graphics window title in turtle graphics, similar to how <code>title KJR</code> works in <code>cmd.exe</code>.</p>
<p>I am currently in the process of creating a game, and would like it to display the name, <code>KJR</code>, rather than <code>turtle graphics</code>.</p>
<p>Is there a way this can be done?</p>
</div>
<div class="post-text" itemprop="text">
<p>....</p>
<p>Use the <a href="https://docs.python.org/3/library/turtle.html#turtle.title" rel="nofollow noreferrer"><code>title()</code></a> function.</p>
</div>
<span class="comment-copy">You should be able to do it with the <a href="https://docs.python.org/3/library/turtle.html#turtle.title" rel="nofollow noreferrer"><code>screen.title()</code></a> method, where <code>screen = turtle.Screen()</code>.</span>
