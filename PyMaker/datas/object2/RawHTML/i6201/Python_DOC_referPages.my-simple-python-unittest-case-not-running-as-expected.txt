<div class="post-text" itemprop="text">
<p>Created this simple unit test as shown bellow</p>
<p><strong>calculator_tester.py</strong></p>
<pre><code>import unittest
class MathsTest(unittest.TestCase):

    def test_createOperation(self):
        operation = Mathoperation()
</code></pre>
<p>This is the only test file I have created and <strong>Note</strong> <strong><em>there doesnot exist a class call Mathoperation()</em></strong> that is defined inside the test method.
n
When running this sample test code using the python test runner</p>
<pre><code>python -m unittest
</code></pre>
<p>output:</p>
<pre><code>----------------------------------------------------------------------
Ran 0 tests in 0.000s

OK
</code></pre>
<p><strong>Expectation:</strong></p>
<blockquote>
<p>I was expecting the test case to run and throw NameError: global name
  Mathoperation is not defined. But the test case didn't run.</p>
</blockquote>
<p>What is the sample test case not running. I am running it on <strong>python version 2.7.12</strong> </p>
</div>
<div class="post-text" itemprop="text">
<p><a href="https://docs.python.org/3/library/unittest.html#unittest-test-discovery" rel="nofollow noreferrer">Test discovery</a> looks for files matching the glob pattern <code>test*.py</code> by default. Your test doesn't match that pattern. Rename it, or specify the test file explicitly.</p>
</div>
