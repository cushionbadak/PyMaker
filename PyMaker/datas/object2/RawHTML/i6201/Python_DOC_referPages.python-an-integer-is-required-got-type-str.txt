<div class="post-text" itemprop="text">
<p>I just need to print a list of int to ASCII.</p>
<pre><code>a=list(str(12345))
for q in a:
    print(chr(q))
</code></pre>
<blockquote>
<p>an integer is required (got type str)</p>
</blockquote>
<p>why I'm getting that error?</p>
</div>
<div class="post-text" itemprop="text">
<p>You are passing a string value into the chr() function. This should work:</p>
<pre><code>a=list(str(12345))
for q in a:
    print(chr(int(q)))

#The above code will work but this will print out characters, as 1-5
# in the ASCII table are not visible characters.

a = [65,66,67,68,69]
for q in a:
    print(chr(q))
</code></pre>
</div>
<span class="comment-copy">because <code>chr</code> expects an integer and you're giving it a string, i think maybe you're looking for <code>ord(q)</code></span>
<span class="comment-copy"><a href="https://docs.python.org/3/library/functions.html#chr" rel="nofollow noreferrer"><code>chr</code></a> takes an integer, i guess you are looking for <a href="https://docs.python.org/3/library/functions.html#ord" rel="nofollow noreferrer"><code>ord</code></a></span>
<span class="comment-copy"><code>a</code> is not a <code>list</code> of <code>int</code> but a <code>list</code> of <code>str</code>; <code>['1', '2', '3', '4', '5']</code> specifically.</span>
<span class="comment-copy"><code>q</code> is a string because you are iterating over the list of characters <code>a</code>.</span>
<span class="comment-copy">What exactly are you trying to print?</span>
<span class="comment-copy">That's because 1,2,3,4, and 5 are not really characters. Check out this link: asciitable.com If you want to see characters view the second snippet of code above.</span>
