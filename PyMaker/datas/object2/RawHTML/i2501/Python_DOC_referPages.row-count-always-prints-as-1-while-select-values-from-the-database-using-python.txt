<div class="post-text" itemprop="text">
<p>I need to print the row count of the cursor while using select query using python.  But it always returns -1 even the select query returns some values.</p>
<p>Code I've tried:</p>
<pre><code>    cursor = conn.cursor()
    cursor.execute(query)
    print(cursor.rowcount)
</code></pre>
<p>How to print the rowcount of the cursor?</p>
</div>
<div class="post-text" itemprop="text">
<p>The below code returns the lenth of the row.</p>
<pre><code>  rows = cursor.fetchall()
  len(rows)
</code></pre>
</div>
<div class="post-text" itemprop="text">
<pre><code>len( cursor.fetchall()) 
</code></pre>
<p>this should do</p>
</div>
<span class="comment-copy">have you tried len(cursor.fetchAll())</span>
<span class="comment-copy">Are you using sqlite3, if so read this <a href="https://docs.python.org/3/library/sqlite3.html#sqlite3.Cursor.rowcount" rel="nofollow noreferrer">docs.python.org/3/library/sqlite3.html#sqlite3.Cursor.rowcount</a></span>
<span class="comment-copy">I'm using Sql server @Hari krishnan</span>
<span class="comment-copy">Yaa len is working @CSMaverick</span>
<span class="comment-copy">Cool, that's what I am about to say. Unfortunately my Cursor giving me login timeout error.! Thank you :-)</span>
<span class="comment-copy">please explain your answer!!!</span>
