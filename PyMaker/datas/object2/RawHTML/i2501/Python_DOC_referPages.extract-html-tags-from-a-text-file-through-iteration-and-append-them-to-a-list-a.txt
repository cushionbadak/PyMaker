<div class="post-text" itemprop="text">
<p>I want to be able to read a html file and extract only the tags out of it. </p>
<ol>
<li>Read one character at a time from the file, ignoring everything to get "&lt;"(ignore "&lt;" as well)</li>
<li><p>Read one character at a time, appending them to a string until "&gt;" or white space(ignore "&gt;" as well) </p>
<pre><code>  &lt;html&gt;
   &lt;body&gt;
   &lt;h1&gt;This is test&lt;/h1&gt;
   &lt;h2&gt; This is test 2&lt;h2&gt;
   &lt;/body&gt;
   &lt;html&gt;


   with open('doc.txt', 'r') as f:
            all_lines = []
            # loop through all lines using f.readlines() method
            for line in f.readlines():
                new_line = []
                # this is how you would loop through each alphabet
                for chars in line:
                    new_line.append(chars)
                all_lines.append(new_line)

            print(all_lines)
</code></pre></li>
</ol>
<p>I can iterate through the text files and can get the list as below: </p>
<p>[['&lt;', 'h', 't', 'm', 'l', '&gt;', '\n'], ['&lt;', 'b', 'o', 'd', 'y', '&gt;', '\n'], ['&lt;', '/', 'b', 'o', 'd', 'y', '&gt;', '\n'], ['&lt;', '/', 'h', 't', 'm', 'l', '&gt;']]</p>
<p>but the expected output should be : [html,body,h1,/h1,/h2,/body,/html]</p>
</div>
<div class="post-text" itemprop="text">
<pre><code>In [10]: re.findall('&lt;(.*?)&gt;', html)
Out[10]: ['html', 'body', 'h1', '/h1', 'h2', 'h2', '/body', '/html']
</code></pre>
<p>Simply use <a href="https://docs.python.org/3/howto/regex.html" rel="nofollow noreferrer">regex</a> or a HTMLParser.</p>
</div>
