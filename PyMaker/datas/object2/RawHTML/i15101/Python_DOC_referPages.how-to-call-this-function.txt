<div class="post-text" itemprop="text">
<p>How to measure the time of this code with timeit?(all functions have been defined)</p>
<pre><code>    for i in range (1,10):
        bh = BinHeap()
        facc=[random.randrange(1,101,1) for _ in range (10+i)]
        bh.buildHeap(facc)
        print(bh.heapList)
        i=i+1
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>Try the documentation on <a href="https://docs.python.org/3/library/profile.html" rel="nofollow">Profiling</a></p>
<pre><code>python -m cProfile [-o output_file] [-s sort_order] myscript.py
</code></pre>
<p>and </p>
<pre><code>cProfile.run('BinHeap()')
</code></pre>
</div>
<span class="comment-copy">possible duplicate of <a href="http://stackoverflow.com/questions/19010793/how-to-use-timeit-when-timing-a-function">How to use timeit when timing a function</a></span>
<span class="comment-copy">(you'll have to put that code in a function before)</span>
<span class="comment-copy">Preface: this is not an answer to anything ... What do you expect the <code>i=i+1</code> to do on the last line -- Because if you think it is doing <i>something</i>, it probably isn't unless there's more code to follow that you're not showing...</span>
<span class="comment-copy">i want to know how to measure with timeit</span>
