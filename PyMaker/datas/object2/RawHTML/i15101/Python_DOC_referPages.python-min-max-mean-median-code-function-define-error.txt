<div class="post-text" itemprop="text">
<p><strong>Here's my code:</strong> Any help would be appreciated. Stuck on returning an error 'int' is not iterable and not sure where to go from there. Receiving error when running first part of my function at #minimum.</p>
<pre><code> #Minimum
min_val = min(input_list)
int(min_val)
return min_val

#Maximum
max_val = max(input_list)
int(max_val)
return max_val

#Mean
total = sum(input_list)
length = len(input_list)
for nums in [input_list]:
    mean_val = total / length
    float(mean_val)
    return mean_val

#Median
sorted(input_list)
med_val = sorted(lst)
lstLen = len(lst)
index = (lstLen - 1) // 2

if (lstLen % 2):
    return med_val[index]
else:
    return (med_val[index] + med_val[index + 1])/2.0

return min_val, max_val, mean_val, med_val
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>There is no method named <code>get_stats</code>. Call <code>stats</code> instead.</p>
<pre><code>min_val, max_val, mean_val, med_val = stats(my_list)
</code></pre>
<p>By the way, your code is totally wrong in terms of both indentation and logic. I'd recommend you to use <a href="https://docs.python.org/2/library/functions.html#max" rel="nofollow"><code>max</code></a> and <a href="https://docs.python.org/2/library/functions.html#max" rel="nofollow"><code>min</code></a> functions to get the maximum and minimum values in the list.</p>
</div>
<div class="post-text" itemprop="text">
<p>Python has a lot of baked-in functions and modules for doing common operations, i.e. basic stats (max, min, mean, median) via <a href="https://docs.python.org/3/library/statistics.html" rel="nofollow">the <code>statistics</code> module</a>.</p>
<pre><code>import statistics

my_list = list(range(51))

print(min(my_list)) # 0
print(max(my_list)) # 50
print(statistics.mean(my_list)) # 25.0
print(statistics.median(my_list)) # 25
</code></pre>
</div>
<span class="comment-copy">In your function definition, the function is called <code>stats</code>. In your example where you call the function, you call a function named <code>get_stats</code>. Might that be why it isn't defined?</span>
<span class="comment-copy">Why don't your write separate functions for <code>max</code>, <code>min</code>, <code>mean</code> ,<code>median</code>, and write a wrapper function to call them all.</span>
<span class="comment-copy">Alternatively, why don't you use <code>max()</code>, and <code>min()</code> functions to get the maximum and minimum value in the list?</span>
<span class="comment-copy">If you're going to create your own functions, start with defining a class and take it from there.</span>
<span class="comment-copy">Thank you very much for the feedback. This is actually my first program and I really appreciate you taking the time to help me out.</span>
