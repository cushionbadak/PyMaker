<div class="post-text" itemprop="text">
<p>I have some code and this is an extract of the code:</p>
<pre><code>actualScoresTable = []
</code></pre>
<p>I was wondering what the <code>= []</code> bit means.</p>
</div>
<div class="post-text" itemprop="text">
<p>Assign <code>empty list</code> to variable <code>actualScoresTable</code>. </p>
<p>Th type of <code>actualScoresTable</code> is <code>list</code></p>
<p>try folowing:</p>
<pre><code>&gt;&gt;&gt; actualScoresTable = []
&gt;&gt;&gt; print actualScoresTable
[]
&gt;&gt;&gt; type(actualScoresTable)
&lt;type 'list'&gt;
&gt;&gt;&gt; 
</code></pre>
<p>We can define list type variable in following way also:</p>
<pre><code>&gt;&gt;&gt; a = list()
&gt;&gt;&gt; print a
[]
&gt;&gt;&gt; type(a)
&lt;type 'list'&gt;
&gt;&gt;&gt; 
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>It means that an empty list is assigned to the variable name <code>actualScoresTable</code>.</p>
</div>
<div class="post-text" itemprop="text">
<p>it is an empty list assigned to a variable actualScoresTable</p>
<p>if you want to add elements in the list you can use the built in function append()
like this</p>
<pre><code>  actualScoresTable.append(the_element) #the_element could be string or int
</code></pre>
</div>
<span class="comment-copy">list declaration.</span>
<span class="comment-copy">Python tutorial, <a href="https://docs.python.org/3/tutorial/introduction.html#lists" rel="nofollow noreferrer">chapter 3, section 1.3</a>. LearnPython.org, <i>Learn The Basics</i>, <a href="http://www.learnpython.org/en/Lists" rel="nofollow noreferrer">third page</a>. These are very basic syntax constructs that you can find in all Python tutorials.</span>
