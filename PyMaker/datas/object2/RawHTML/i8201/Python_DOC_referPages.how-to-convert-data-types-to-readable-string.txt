<div class="post-text" itemprop="text">
<p>I was wondering because I haven't found a working answer to convert data types to readable format.</p>
</div>
<div class="post-text" itemprop="text">
<p>You should take a look at adding <strong>str</strong> and <strong>repr</strong> methods to your data types. </p>
<p><a href="https://docs.python.org/3/reference/datamodel.html#object.__str__" rel="nofollow noreferrer"><strong>str</strong></a></p>
<blockquote>
<p>Called by the str() built-in function and by the print statement to
  compute the "informal" string representation of an object.</p>
</blockquote>
<p><a href="https://docs.python.org/3/reference/datamodel.html#object.__repr__" rel="nofollow noreferrer"><strong>repr</strong></a></p>
<blockquote>
<p>Called by the repr() built-in function and by string conversions
  (reverse quotes) to compute the "official" string representation of an
  object. If at all possible, this should look like a valid Python
  expression that could be used to recreate an object with the same
  value (given an appropriate environment).</p>
</blockquote>
<p>By example:</p>
<pre><code>class Car(object):
    def __init__(self, manufacturer, model):
        self.manufacturer = manufacturer
        self.model = model
    def __str__(self):
        return self.manufacturer+" "+self.model
    def __repr__(self):
        return '{"manufacturer": "'+self.manufacturer+'", "model": "'+self.model+'"}'

c = Car("Ford", "Focus")
print str(c)
&gt; "Ford Focus"
print repr(c)
&gt; {"manufacturer": "Ford", "model": "Focus"}
</code></pre>
</div>
<span class="comment-copy">Define "readable" or do you just want to convert any type to a string? If so then <code>str(...)</code> is probably enough.</span>
<span class="comment-copy">Please read this <a href="http://stackoverflow.com/help/how-to-ask">how-to-ask</a> to improve your question.</span>
