<div class="post-text" itemprop="text">
<p>I am working on this code to find the maximum of numbers entered by a user.  As it is, if I enter all negative numbers, the program returns <code>0</code> as the greatest.</p>
<p>What do I need to add to handle negative numbers? For instance, if a user enters <code>-3</code> and <code>-1</code>, the program should print <code>-1</code>.</p>
<pre><code>m = 0
finished = False
while not finished:
    print('Enter a number (0 to finish): ', end = '')
    s = input()
    num = int(s)
    if num != 0:
        if num &gt; m:
            m = num
    else:
        finished = True
print(str(m))
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>I see three general possibilities:</p>
<ol>
<li>Set <strong>m</strong> to a ridiculously small integer to start; I think that your current code will work from there.</li>
<li>Read the first input number <em>before</em> the loop; make that the starting value of <strong>m</strong>.</li>
<li>Don't use a value of m at all: append all of the input numbers to a list, and simply take the <strong>max</strong> of the list after the user enters <strong>0</strong> to stop.</li>
</ol>
<p>Also, <em>please</em> learn to use descriptive variable names.  <strong>finished</strong> is good, but <strong>m</strong> is semantically useless, and <strong>num</strong> is questionable (although it <em>is</em> rather non-descript).</p>
</div>
<div class="post-text" itemprop="text">
<p>Consider the following approach using built-in <a href="https://docs.python.org/3/library/functions.html#max" rel="nofollow noreferrer">max()</a> function:</p>
<pre><code>positives = []   # list for positive numbers
negatives = []   # list for negative numbers
finished = False

while not finished:
    print('Enter a number (0 to finish): ', end = '')
    num = int(input())
    if num &gt; 0:
        positives.append(num)
    elif num &lt; 0:
        negatives.append(num)
    else:
        finished = True

print('Max positive number: ', max(positives))
print('Max negative number: ', max(negatives))
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>Hey you should try to assign the maxelement initially to list[0] i.e first element of the list and then start comparing with every element of the list. This should work-</p>
<pre><code>def calculate_max(list1) :
maxelement = list1[0]
for element in list1 :
    if element&gt;maxelement :
        maxelement= element  
     return maxelement

#main
l=[-2,-5,-6]
result = calculate_max(l)
print(result)
</code></pre>
</div>
<span class="comment-copy">Well, what do you think needs to be done to make sure you're only looking at negative numbers? Show us what you've tried—we're not a code-writing service.</span>
<span class="comment-copy">Maybe start your indicator at the lowest possible value rather than zero.</span>
