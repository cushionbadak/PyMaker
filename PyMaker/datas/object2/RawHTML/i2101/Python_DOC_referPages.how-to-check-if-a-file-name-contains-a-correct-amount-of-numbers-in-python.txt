<div class="post-text" itemprop="text">
<p>I am writing a program which filters files placed in a specific folder and I need to check whether they have this structure: some_name/+++/++/++++.format, where + represents any digit.</p>
<p>Here is how my code starts:</p>
<pre><code>import glob

path = "_path_"
File_list = glob.glob(path+"/*")

for item in File_list:
  if item == path + *something*:   &lt;-------- This is my problem
    print (True)
</code></pre>
<p>I would appreciate any help. I am working with Python 3.6.</p>
</div>
<div class="post-text" itemprop="text">
<p>How about some regex to match that pattern:</p>
<pre><code>import re

pat = re.compile(".*\/\d{3}\/\d{2}\/\d{4}\.format")
if pat.match(item):
    # Your code here
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>You can use glob pattern:</p>
<pre><code>File_list = glob.glob('/'.join((path, *('[0-9]' * n for n in (3, 2, 4)), '.format')))
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>This should help-  </p>
<pre><code>import re    
f = 'fname/123/45/6789.txt'
if re.match('^\w+/\d{3}/\d{2}/\d{4}', f):
    print("Correct file name format")
</code></pre>
<p>Output:</p>
<pre><code>&gt;&gt; Correct file name format
</code></pre>
</div>
<div class="post-text" itemprop="text">
<pre><code>import re
regex = r"\w+\/\d{3}\/\d{2}\/\d{4}"
test_str = ("some_name/123/12/1234")
matches = re.search(regex, test_str)
if matches:
     print(True)
else:
    print(False)
</code></pre>
<p><em>Use Regex</em></p>
</div>
<span class="comment-copy">use regex to match the name of the file with your format <a href="https://docs.python.org/3/howto/regex.html" rel="nofollow noreferrer">docs.python.org/3/howto/regex.html</a></span>
<span class="comment-copy">You can use <code>\d{3}/\d{2}/\d{4}</code></span>
<span class="comment-copy">You could loop through the the characters in the file path string and count how many digits appear in it</span>
