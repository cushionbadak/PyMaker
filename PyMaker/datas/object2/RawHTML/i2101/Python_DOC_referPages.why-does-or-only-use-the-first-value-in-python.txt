<div class="post-text" itemprop="text">
<pre><code>b = 'a'
print(b==('b' or 'a'))
</code></pre>
<p>You will get <code>False</code> if you enter this code.
But if you change the order like this:</p>
<pre><code>b = 'a'
print(b==('a' or 'b'))
</code></pre>
<p>You get <code>True</code>.</p>
<p>So, why does <code>or</code> in this code only consider the first value of the two?
I think the upper code should also return <code>True</code>. </p>
</div>
<div class="post-text" itemprop="text">
<p>Try evaluating the following expression in the python REPL:</p>
<p><code>'a' or 'b'</code></p>
<p>This gives <code>'a'</code>, because the <code>or</code> operator short-circuits; that is, it returns the first truthy argument it finds, in this case, <code>'a'</code>. Both <code>'a'</code> and <code>'b'</code> are truthy, so you're simply getting whichever one of those you put first.</p>
</div>
<div class="post-text" itemprop="text">
<p>If I understand your code correctly, you probably wanted to write:</p>
<pre><code>b = 'a'
print(b in ('a', 'b'))
</code></pre>
<p>This checks if <code>b</code> equals one of <code>'a'</code> and <code>'b'</code>.</p>
</div>
<span class="comment-copy">Read more about the operators.</span>
<span class="comment-copy">Read the documention for <code>or</code>. You can't use <code>or</code> to check against multiple values if that's what you're trying to do.</span>
<span class="comment-copy">To be fair, "or" is a difficult thing to google about.  Here's the relevant section of the docs:  <a href="https://docs.python.org/3/library/stdtypes.html#boolean-operations-and-or-not" rel="nofollow noreferrer">docs.python.org/3/library/…</a></span>
<span class="comment-copy">In strings a non-empty string evaluates as <code>True</code> and an empty string evaluates as <code>False</code>.  <code>(X or Y or Z)</code> will return the first "Truthy" value or if none are "Truthy" it will return the last "Falsey" value.  So when you compare <code>b == ('a' or 'b')</code> it first evaluates <code>('a' or 'b')</code> to <code>'a'</code> and then compares that to <code>b</code>.  What you want is <code>b in ('a', 'b')</code>.</span>
<span class="comment-copy">Possible duplicate of <a href="https://stackoverflow.com/questions/18195322/pythons-logical-operator-and">Python's Logical Operator AND</a></span>
<span class="comment-copy">Thx for U everyone!!! I understand :)</span>
<span class="comment-copy">Thx for U everyone!!! I understand :)</span>
