<div class="post-text" itemprop="text">
<pre><code>import json
path = 'https://raw.githubusercontent.com/wesm/pydata-book/2nd-edition/datasets/bitly_usagov/example.txt'
store = [json.loads(line), for line in open(path)]
store[0]
</code></pre>
<p>The above code isn't working. I want to see the records stored in store  </p>
</div>
<div class="post-text" itemprop="text">
<p>Install and use requests libraries to pull text or whatever file from url see. Requests is one of easier to learn.</p>
<p><a href="https://www.pythonforbeginners.com/requests/using-requests-in-python" rel="nofollow noreferrer">https://www.pythonforbeginners.com/requests/using-requests-in-python</a></p>
<p>You can split retrieved text/string on lines, yet avoid any commas inside comprehensions</p>
</div>
<div class="post-text" itemprop="text">
<p>you can also try this one</p>
<pre><code>    import urllib.request
    resource = urllib.request.urlopen('https://raw.githubusercontent.com/wesm/pydata-book/2nd-edition/datasets/bitly_usagov/example.txt')
    content =  resource.read().decode(resource.headers.get_content_charset())
</code></pre>
</div>
<div class="post-text" itemprop="text">
<pre><code>import requests 
import json 
response = requests.get('https://raw.githubusercontent.com/wesm/pydata-book/2nd-edition/datasets/bitly_usagov/example.txt')
with open('pakka.txt', mode = 'wb') as file:
 file.write(response.content)

f = 'pakka.txt'
records = [json.loads(line) for line in open(f)]
records[0]
</code></pre>
<p>Hi guys - This code worked for me. Thanks for all the suggestions.</p>
</div>
<span class="comment-copy">What error you are getting while running this code...</span>
<span class="comment-copy">You need to retrieve the URL's contents.  Your code does not do that.  You want <code>urllib</code>, <code>requests</code>, or something similar to retrieve the URL contents and then parse THAT.</span>
<span class="comment-copy">remove comma from comprehension</span>
<span class="comment-copy">use requests library to pull data from url</span>
<span class="comment-copy">import requests import json  # <a href="https://docs.python.org/3/library/urllib.request.html#examples" rel="nofollow noreferrer">docs.python.org/3/library/urllib.request.html#examples</a> r = requests.get('<a href="https://raw.githubusercontent.com/wesm/pydata-book/2nd-edition/datasets/bitly_usagov/example.txt'" rel="nofollow noreferrer">raw.githubusercontent.com/wesm/pydata-book/2nd-edition/datasets/…</a>) print(r)</span>
