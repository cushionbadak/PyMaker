<div class="post-text" itemprop="text">
<p>I acquire text form excel files and database.</p>
<p>The text has in it "invisible" characters, i presume like new lines, carriage return, non breaking text, tabs etc.</p>
<p>If I print the text or check, python interprets the text, but I want to get the raw text as it is.</p>
</div>
<div class="post-text" itemprop="text">
<p>User <a href="https://docs.python.org/3/library/functions.html#repr" rel="nofollow noreferrer">repr()</a>:</p>
<pre><code>a = '\r\n'
print(a)
#
#
print(repr(a))
#'\r\n'
</code></pre>
</div>
<span class="comment-copy">What "acquires" the text? you need to make a <a href="https://stackoverflow.com/help/mcve">Minimal, Complete, and Verifiable example</a>.</span>
