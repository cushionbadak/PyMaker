<div class="post-text" itemprop="text">
<div class="question-status question-originals-of-duplicate">
<p>This question already has an answer here:</p>
<ul>
<li>
<a dir="ltr" href="/questions/12997305/empty-list-boolean-value">Empty list boolean value</a>
<span class="question-originals-answer-count">
                    3 answers
                </span>
</li>
</ul>
</div>
<pre><code>def check():
    if [1,2,3]:
        return [2,3,4]

check()
[2, 3, 4]
</code></pre>
<p>I thought <code>[1,2,3]</code> is a list but not a Boolean expression that can be True or False? But why does the function consider the list <code>([1,2,3])</code> to be True?</p>
</div>
<div class="post-text" itemprop="text">
<p>Any expression can be used as the condition in <code>if</code> or <code>while</code>. See <a href="https://docs.python.org/2.4/lib/truth.html" rel="nofollow noreferrer">https://docs.python.org/2.4/lib/truth.html</a> for how the truth value of the expression is evaluated.</p>
</div>
<div class="post-text" itemprop="text">
<p>As far as I know, any expression of any type will be considered "True" unless it's an empty list/tuple, an empty string (which could technically also be considered a list of characters) or the number 0.</p>
<p>P.S. Just as a fun fact, the number 0.000001 returns True.</p>
</div>
<div class="post-text" itemprop="text">
<p>As stated in the <a href="https://docs.python.org/library/stdtypes.html#truth-value-testing" rel="nofollow noreferrer">python documentation on "Truth Value Testing"</a>:</p>
<blockquote>
<p>The following values are considered false: </p>
<p>[...] </p>
<ul>
<li>any empty sequence, for example, <code>''</code>, <code>()</code>, <code>[]</code>. </li>
</ul>
<p>[...] </p>
<p>All other values are considered true — so objects of many types are always true.</p>
</blockquote>
<p>So any not-empty list will be evaluated as <code>True</code> and any python object can be evaluated in a boolean context (if it doesn't throw an Exception in it's <code>__bool__</code> or <code>__len__</code> method, like for example <code>numpy.ndarray</code>).</p>
</div>
<div class="post-text" itemprop="text">
<p>Maybe something like this ?</p>
<pre><code>def checkList():
  if all(x in list2 for x in list1):
     return [ 2,3,4 ]
list1= [ 1,2,3 ]
list2= [ 1,2,3 ] 
print(checkList(list1,list2))
</code></pre>
<p>You really need this?SAnyway take a look here : <a href="https://docs.python.org/3/library/stdtypes.html#truth-value-testing" rel="nofollow noreferrer">https://docs.python.org/3/library/stdtypes.html#truth-value-testing</a></p>
</div>
<span class="comment-copy">any <a href="https://docs.python.org/3/library/stdtypes.html#truth-value-testing" rel="nofollow noreferrer">not-empty list</a> is considered true.</span>
<span class="comment-copy">Any expression can be used where a boolean expression is required.</span>
<span class="comment-copy">Check <a href="https://docs.python.org/3/library/stdtypes.html#truth-value-testing" rel="nofollow noreferrer">Truth Value Testing</a> to see what is considered true and false in Python.</span>
<span class="comment-copy">wow, there is still documentation for python 2.4. :-)</span>
<span class="comment-copy">Blame google, that's the page it found.</span>
