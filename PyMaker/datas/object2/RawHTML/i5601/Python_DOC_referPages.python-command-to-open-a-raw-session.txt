<div class="post-text" itemprop="text">
<p>I have tried searching and got the following command to be run from command line:</p>
<pre><code>putty.exe -raw *ip* *port*
</code></pre>
<p>Is there any other way to open a raw session using python without having a dependency on putty, mputty or any other 3rd party application/tool.</p>
</div>
<div class="post-text" itemprop="text">
<p>Use <a href="https://docs.python.org/3/library/socket.html#example" rel="nofollow noreferrer">sockets</a>:</p>
<pre><code>s = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
s.connect((HOST, PORT))
s.sendall('Hello, world')
data = s.recv(1024)
s.close()
print 'Received', repr(data)
</code></pre>
</div>
<span class="comment-copy">Did you try <code>curl</code>? curl -K &lt;IP&gt;:&lt;PORT&gt;</span>
<span class="comment-copy">Please add proper explanation. This looks very low quality answer to me</span>
<span class="comment-copy">@Nabin I believe that the code is pretty self explanatory. And if not, there's a link to documentation.</span>
