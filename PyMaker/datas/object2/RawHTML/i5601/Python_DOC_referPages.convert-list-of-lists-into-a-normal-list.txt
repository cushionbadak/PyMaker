<div class="post-text" itemprop="text">
<p>I have a list of lists:</p>
<pre><code>a = [['0.06'], ['0.54'], ['0.61']] 
</code></pre>
<p>I want to convert this list into normal list like this:</p>
<pre><code>a = [0.06, 0.54, 0.61]
</code></pre>
<p>How can I do it it</p>
</div>
<div class="post-text" itemprop="text">
<p>Using a list comprehension:</p>
<pre><code>&gt;&gt;&gt; a = [['0.06'], ['0.54'], ['0.61']]
&gt;&gt;&gt; [float(x) for [x] in a]
[0.06, 0.54, 0.61]
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>Alternatively, using a functional approach:</p>
<pre><code>list(map(lambda x: float(x[0]), a))
# [0.06, 0.54, 0.61]
</code></pre>
<p>Using <code>itertools</code> to first merge the sublists:</p>
<pre><code>import itertools as it


list(map(float, it.chain.from_iterable(a)))
# [0.06, 0.54, 0.61]
</code></pre>
<p>A list comprehension is arguably more elegant and clever.</p>
</div>
<span class="comment-copy">Possible duplicate of <a href="https://stackoverflow.com/questions/952914/making-a-flat-list-out-of-list-of-lists-in-python">Making a flat list out of list of lists in Python</a></span>
<span class="comment-copy">Wait, this looks like a totally different question</span>
<span class="comment-copy">@pylang: I am sorry for that... The first comment looked <i>so</i> out of place, I decided to check the edits. The OP made it an entirely new question – which goes head on against SO rules.</span>
<span class="comment-copy">Which rule does this violate?</span>
<span class="comment-copy">I've rolled-back to the version that has existing answers.  OP: if you have a different question, please post a new one.</span>
<span class="comment-copy">Can you explain how <code>[x]</code> works?  I'm unfamiliar with transforming a value within a for statement.</span>
<span class="comment-copy">This is binding to a <code>target_list</code> in the grammar, and is documented <a href="https://docs.python.org/3/reference/compound_stmts.html#the-for-statement" rel="nofollow noreferrer">here</a>.</span>
<span class="comment-copy">Wow.  That is a gem.  Thank you.</span>
