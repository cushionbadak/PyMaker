<div class="post-text" itemprop="text">
<p>I am recieving below error in <code>Python 3</code>
any idea?   </p>
<pre><code>clientDtSocket.send(bytes(fileName,"UTF-8"),("localhost",8101))
TypeError: an integer is required (got type tuple)
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>Try calling <code>clientDtSocket.sendto</code> instead of <code>clientDtSocket.send</code>:</p>
<pre><code>clientDtSocket.sendto(bytes(fileName,"UTF-8"),("localhost",8101))
</code></pre>
<p>More info about these methods: <a href="https://docs.python.org/3/library/socket.html" rel="nofollow">https://docs.python.org/3/library/socket.html</a></p>
</div>
<span class="comment-copy">Please post your code; not all of us are psychic.</span>
<span class="comment-copy">Is not <code>("localhost", 8101)</code> a tuple? Is this your intended argument?</span>
<span class="comment-copy">I'd also guess it's an issue passing <code>("localhost",8101)</code> to the <code>send</code> method.</span>
<span class="comment-copy">How was <code>clientDtSocket</code> initialized?</span>
<span class="comment-copy">Please note that <code>"127.0.0.1"</code> is usually faster than <code>"localhost"</code>, because the latter does an extra DNS lookup.</span>
