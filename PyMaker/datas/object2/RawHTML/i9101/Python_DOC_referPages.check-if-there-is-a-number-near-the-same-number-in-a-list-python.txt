<div class="post-text" itemprop="text">
<p>I want to check if for example the int 2 comes after or before another 2:</p>
<pre><code>list = [2, 2, 3]
if 2 and 2 in list:
    print "True"
</code></pre>
<p>And if the list is this:</p>
<pre><code>list = [2, 3, 2]
print "False"
</code></pre>
<p>thx</p>
</div>
<div class="post-text" itemprop="text">
<p>Try this:</p>
<pre><code>def check(list_):
    last = None
        for element in list_:
            if element == last:
                return True
            else:
                last = element
    return False
</code></pre>
</div>
<div class="post-text" itemprop="text">
<pre><code>a = [0, 2, 15, 13, 13, 5, 12, 3, 1, 3, 14, 14, 17, 20, 0, 14, 2, 0,
     1, 10, 5, 13, 15, 13, 19, 11, 20, 5, 4, 17, 20, 7, 4, 20, 20,
     19, 3, 20, 20, 0, 14, 8, 18, 0, 18, 2, 5, 15, 12, 20, 5, 7, 12,
     6, 17, 18, 15, 12, 2, 16, 6, 11, 14, 20, 8, 4, 11, 10, 15, 6,
     10, 19, 1, 20, 6, 19, 2, 5, 13, 7, 5, 18, 11, 11, 3, 0, 11, 3,
     14, 18, 15, 11, 2, 9, 12, 0, 18, 1, 4, 2]
</code></pre>
<p>Run through the list once to find all the <em>pairs</em> and put them in a set</p>
<pre><code>z = {x for x, y in zip(a, a[1:]) if x == y}

&gt;&gt;&gt; z
{11, 20, 13, 14}
</code></pre>
<p>Then use it like this:</p>
<pre><code>&gt;&gt;&gt; n = 2
&gt;&gt;&gt; n in z
False
&gt;&gt;&gt; n = 13
&gt;&gt;&gt; n in z
True
&gt;&gt;&gt; 
</code></pre>
<hr/>
<pre><code>zip(a, a[1:])
</code></pre>
<p>will produce tuples of <code>(a[0], a[1]), (a[1], a[2]), (a[2], a[3]), ...</code></p>
<p>The <code>pairwise</code> <a href="https://docs.python.org/3/library/itertools.html#itertools-recipes" rel="nofollow noreferrer">Itertools Recipe</a> will do the same thing.</p>
</div>
<span class="comment-copy">What have you attempted? And what exactly is the problem -  you are checking whether there are two of some input number beside one another in a list?</span>
<span class="comment-copy">if 2 and 2 in list:       print("True")</span>
<span class="comment-copy">ps: I am new to python</span>
<span class="comment-copy">Please read my questions.</span>
<span class="comment-copy">@Telep0rter and have you tried anything yet?</span>
