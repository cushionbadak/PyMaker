<div class="post-text" itemprop="text">
<p>I need to do something which must be easy enough, but for some reason, I can't get my head around to it. </p>
<p>So, basically, I need to produce a new list that will concatenate the items from three lists with each other.</p>
<p>Please note that all the three lists are currently in a dictionary a.</p>
<p>For clarity, here's what I'm trying to do: </p>
<pre><code>def test(phrase):
    a = {}
    tmp = phrase.split(' ')
    for m in xrange(0,len(tmp)):

        a[m] = anagrammes2(tmp[m])
        # The anagram function will spit out the anagrams of the words
        # a[1] = ['mange']
        # a[2] = ['ton, 'ont']
        # a[3] = ['orange','onagre','organe','rongea']

test('Mange ton orange')

#result should be:
['mange ont onagre', 'mange ont orange', 'mange ont orangé', 'mange ont organe', 
 'mange ont rongea', 'mange ton onagre', 'mange ton orange', 'mange ton orangé', 
 'mange ton organe', 'mange ton rongea', 'mangé ont onagre', 'mangé ont orange', 
 'mangé ont orangé', 'mangé ont organe', 'mangé ont rongea', 
 'mangé ton onagre', 'mangé ton orange', 'mangé ton orangé', 
 'mangé ton organe', 'mangé ton rongea']
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>You can use <a href="https://docs.python.org/3/library/itertools.html?highlight=product#itertools.product" rel="nofollow">itertools.product()</a>:</p>
<pre><code>&gt;&gt;&gt; a = [['mange'], ['ton', 'ont'], ['orange','onagre','organe','rongea']]
&gt;&gt;&gt; from itertools import product
&gt;&gt;&gt; [' '.join(x) for x in product(*a)]
['mange ton orange',
 'mange ton onagre',
 'mange ton organe',
 'mange ton rongea',
 'mange ont orange',
 'mange ont onagre',
 'mange ont organe',
 'mange ont rongea']
</code></pre>
<p>Integrate with your code:</p>
<pre><code>def test(phrase):
    anas = [anagrammes2(word) for word in phrase.split(' ')]
    return [' '.join(x) for x in product(*anas)]

test('Mange ton orange')
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>Assuming they are strings:</p>
<pre><code>result = tuple(listA[x % len(listA)] + listB[x % len(listB)] + listC[x % len(listC)] for x in range(max(len(listA), len(listB), len(listC))))
</code></pre>
</div>
<span class="comment-copy">try zip or zip_longest........</span>
<span class="comment-copy">can you please edit with your code? for we can help?</span>
<span class="comment-copy">Please edit your question text. You are using python, so write your code correctly. If you means list: <code>list1 = ['a']</code> etc.</span>
<span class="comment-copy">What Alex said. Python lists are delimited by square brackets <code>[]</code>, parentheses <code>()</code> are used to delimit tuples.</span>
<span class="comment-copy">Also, your code is a bit confusing: it looks like your list <code>a</code> contains itself, but I suspect you don't really mean that.</span>
<span class="comment-copy">Wow, thanks. This might seem more than stupid, but how do I integrate it with my current code?</span>
<span class="comment-copy">Try the function I just added.</span>
<span class="comment-copy">Nice one, I've needed to do this kind of thing many times.  It does seem simple, but not so simple to code it.</span>
<span class="comment-copy">@MozammilKhodabacchas Does this work for you?</span>
<span class="comment-copy">It does! Sorry for the late reply and thank you so much :)</span>
