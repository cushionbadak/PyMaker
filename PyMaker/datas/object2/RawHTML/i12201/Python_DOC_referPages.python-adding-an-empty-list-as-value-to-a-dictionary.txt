<div class="post-text" itemprop="text">
<div class="question-status question-originals-of-duplicate">
<p>This question already has an answer here:</p>
<ul>
<li>
<a dir="ltr" href="/questions/2241891/how-to-initialize-a-dict-with-keys-from-a-list-and-empty-value-in-python">How to initialize a dict with keys from a list and empty value in Python?</a>
<span class="question-originals-answer-count">
                    6 answers
                </span>
</li>
</ul>
</div>
<p>I want to append an empty list as the value when the name_input is not empty and grade_input is empty to a dictionary, whose keys are the name_input. The following is the code snippet. But it doesn't seem to work. Any suggestions?</p>
<pre><code>if (name_input != '' and grade_input ==''):
        name_dic[name_input].append([])
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>The <code>.append()</code> method applies to lists, not dictionaries.</p>
<p>To add a new key/value pair to a dictionary, simply do this:</p>
<pre><code>name_dic[name_input] = []
</code></pre>
</div>
<span class="comment-copy">You don't append to a dictionary. Just do this: <code>name_dic[name_input] = []</code></span>
<span class="comment-copy">Do you have only one grade_input for each name_input? A dict of empty lists doesn't seem very useful.</span>
<span class="comment-copy">If you're trying to initialize as an empty list, you can try using a <code>defaultdict</code> (<a href="https://docs.python.org/3/library/collections.html?highlight=collections#collections.defaultdict" rel="nofollow noreferrer">docs.python.org/3/library/…</a>).</span>
<span class="comment-copy">As ldjaw commented earlier.</span>
<span class="comment-copy">You don't necessarily need square brackets on the right side of assignment when assigning values.</span>
