<div class="post-text" itemprop="text">
<p>Is there an option in Python 3 to determine whether the input matches a specified format? For example: the format is <code>d-dddd-dddd-d</code> with the <code>d</code> standing for a digit (0-9):</p>
<pre><code>&gt;&gt;&gt; ('9-9715-0210-0')
True
&gt;&gt;&gt; ('997-150-210-0')
False
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>Using a regex would work like this:</p>
<pre><code>import re

regex = r'\d-\d{4}-\d{4}-\d'

preg = re.compile(regex)

s1 = '9-9715-0210-0'
s2 = '997-150-210-0'

m1 = preg.match(s1)
m2 = preg.match(s2)

if m1:
    print('String s1 is valid')
else:
    print('String s1 is invalid')

if m2:
    print('String s2 is valid')
else:
    print('String s2 is invalid')   
</code></pre>
<p>You can try the code at <a href="http://ideone.com/7B2hj0" rel="nofollow">ideone.com</a>.</p>
<p>The regex you suggested in the comment below your question is just the long version of mine. So that should work, too.</p>
</div>
<div class="post-text" itemprop="text">
<p>Try this:</p>
<pre><code>string = "9-9715-0210-0"
split = string.split("-")
if len(split[0]) == 1 and len(split[1]) == 4 and len(split[2]) == 4 and len(split[3]) == 1:
    print(True)
else:
    print(False)
</code></pre>
<p>You could also change string to be an <code>input</code> so you can enter input or create a function from this code and run it multiple times.</p>
</div>
<span class="comment-copy">You could use a <i>"regular expression"</i>, see <a href="https://docs.python.org/3/library/re.html" rel="nofollow noreferrer"><code>re</code></a>.</span>
<span class="comment-copy">A starting point would be: <a href="https://regex101.com/r/bV8kZ4/2" rel="nofollow noreferrer">regex101.com/r/bV8kZ4/2</a></span>
<span class="comment-copy">Is something like this posible      'string = input()     pattern = \d-\d\d\d\d-\d\d\d\d-\d     result = re.match(pattern, string)'</span>
<span class="comment-copy">@Aleandro why don't you <b>try it and find out?</b></span>
<span class="comment-copy">Note that <code>{1}</code> is redundant and a regex <a href="https://docs.python.org/2/howto/regex.html#the-backslash-plague" rel="nofollow noreferrer">should be in a <code>r'raw string'</code></a>.</span>
<span class="comment-copy">That is extremely fragile; it will throw <code>IndexError</code> if there are fewer than 3 <code>-</code>s, ignores any subsequent <code>-</code>-separated groups and doesn't care whether or not the characters in each group are digits.</span>
