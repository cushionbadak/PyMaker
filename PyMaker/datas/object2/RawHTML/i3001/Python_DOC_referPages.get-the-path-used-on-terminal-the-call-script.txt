<div class="post-text" itemprop="text">
<p>How do I get the path used on terminal that called my Python script.
Example: with the terminal in $HOME, calling <code>python -m my_script</code> or even <code>my_script</code> I want to now that the script was called from <code>$HOME</code> folder.</p>
</div>
<div class="post-text" itemprop="text">
<p>You're describing the basic usage of <a href="https://docs.python.org/3/library/os.html#os.getcwd" rel="nofollow noreferrer"><code>os.getcwd()</code></a>.</p>
<blockquote>
<p><code>os.getcwd()</code>
   Return a string representing the current working directory.</p>
</blockquote>
</div>
