<div class="post-text" itemprop="text">
<p>I have a global list that I append to in a child process. Meanwhile the function that called the child process is iterating through the list. When the child process is exited, the global list is empty, even though I appended to it</p>
<pre><code>urlsToCheck = list()

def crawler():
    while limit != 0 and urlsToCheck.__len__() &gt; 0:
        curUrl = urlsToCheck.pop(0)
        processLink(curUrl)
        limit -= 1

def processLink(url):
    ...
    for i in validLinks:
        urlsToCheck.append(i)
    ...
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>Variables are not shared between processes unless if you use shared data structure explicitly.</p>
<p>See <a href="http://docs.python.org/3/library/multiprocessing.html#sharing-state-between-processes" rel="nofollow">multiprocessing documentation - Sharing state between processes</a>.</p>
</div>
