<div class="post-text" itemprop="text">
<div class="question-status question-originals-of-duplicate">
<p>This question already has an answer here:</p>
<ul>
<li>
<a dir="ltr" href="/questions/21763469/is-alpha-coding-that-works-like-isalpha-in-python">is_alpha coding that works like isalpha() in python</a>
<span class="question-originals-answer-count">
                    4 answers
                </span>
</li>
</ul>
</div>
<pre><code>ASCII_LOWERCASE='abcdefghijklmnopqrstuvwxyz'
ASCII_UPPERCASE='ABCDEFGHIJKLMNOPQRSTUVWXYZ'
ASCII_ALL=ASCII_LOWERCASE+ASCII_UPPERCASE

def is_alpha(x):     
    for ch in x:        
        if ch not in ASCII_ALL:
            return False
    return True
</code></pre>
<p>This was my original code and it is still not returning <code>False</code> in cases like <code>""</code>. When the real <code>isalpha()</code> returns <code>False</code> in the case of <code>''</code> or <code>""</code>. How to exclude all these cases?</p>
</div>
<div class="post-text" itemprop="text">
<p>Empty strings will not trigger any code in the for loop, since <code>for ch in ''</code> is essentially a no-op here (there's nothing to iterate), so your <code>is_alpha</code> returns <code>True</code> for empty strings. You should add something like</p>
<pre><code>if not x:
    return False
</code></pre>
<p>to the beginning of your function.</p>
<p>(As a side note, your <code>break</code> statement is unnecessary since <code>return False</code> will exit the function.</p>
</div>
<div class="post-text" itemprop="text">
<p>Your loop</p>
<pre><code>for ch in x: 
</code></pre>
<p>Will <em>never run</em> if <code>len(x) == 0</code>, you go straight to</p>
<pre><code>return True
</code></pre>
<p>Also, if you</p>
<pre><code>import string
</code></pre>
<p>You can use <code>string.ascii_uppercase</code> and <code>string.ascii_lowercase</code>. </p>
</div>
<span class="comment-copy">I don't understand, is the empty string your only problem?</span>
<span class="comment-copy">Have you tried <code>len(x)&lt;=0</code> or <code>x==""</code>?</span>
<span class="comment-copy">how bout <code>return x and True</code></span>
<span class="comment-copy">Btw. <code>''</code> is the <i>exact same</i> string as <code>""</code>. Also you don’t need to define <code>ASCII_LOWERCASE</code> etc., you can take them from the <a href="http://docs.python.org/3/library/string.html" rel="nofollow noreferrer"><code>string</code> module</a>.</span>
<span class="comment-copy">How's this different from your previous question <a href="http://stackoverflow.com/questions/21763469/is-alpha-coding-that-works-like-isalpha-in-python">is_alpha coding that works like isalpha() in python</a>?</span>
<span class="comment-copy">Thanks. I appreciate your input. I didn't know of any such thing as no-op. My textbook even doesn't talk about no-op. So, finally the codes are working. Thanks again.</span>
