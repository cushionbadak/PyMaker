<div class="post-text" itemprop="text">
<p>I'm trying to load an image from a folder. then blit it to 0,0
The path i'm trying to load from is :  ".\media\img\bk\spaceport.png". 
the script is in the root of the folder. 
i'm using windows if that matters</p>
<pre><code>sWIDTH = 800
sHEIGHT = 600
class IMGLOAD():
    def BAK(ImageTobeUsed):
        b1 = pygame.image.load(ImageTobeUsed)
        background = pygame.transform.scale(b1,( sWIDTH, sHEIGHT))
        window.blit(background,(0,0))

IMGLOAD.BAK(".\media\img\bk\bedroom.jpg") 
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>You should be using a <a href="https://docs.python.org/3/reference/lexical_analysis.html#string-and-bytes-literals" rel="nofollow">raw-string</a> for the file path:</p>
<pre><code>IMGLOAD.BAK(r".\media\img\bk\bedroom.jpg") 
</code></pre>
<p><code>\b</code> is a special character in Python string literals (it represents a backspace).  Below is a demonstration:</p>
<pre><code>&gt;&gt;&gt; '\b'
'\x08'
&gt;&gt;&gt; r'\b'
'\\b'
&gt;&gt;&gt; print('x\by')
y
&gt;&gt;&gt; print(r'x\by')
x\by
&gt;&gt;&gt;
</code></pre>
</div>
<span class="comment-copy">still dosnt work</span>
<span class="comment-copy">In what way does it not work?</span>
<span class="comment-copy">pygame.error: Couldn't open media\img\bd\spaceport.png &gt;&gt;&gt;</span>
<span class="comment-copy">Are you sure the path is correct?  You did <code>img\bk</code> in your question but are doing <code>img\bd</code> here.  Make sure this is correct and also try replacing <code>.</code> with the full path to the image file.</span>
<span class="comment-copy">that was just a typo when I was transfering.</span>
