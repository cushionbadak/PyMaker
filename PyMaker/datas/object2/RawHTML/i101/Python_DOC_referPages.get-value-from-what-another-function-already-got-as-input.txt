<div class="post-text" itemprop="text">
<p>How can I get a value from another function (input) in Python?</p>
<pre><code>class MyClass(object):
    def xxx(self):
        self.a = int(input("enter a value :"))
    def yyy(self):
        self.xxx()
        print (self.a)

obj = MyClass()
n = int(input("choose a number : "))

if (n == 1):
    obj.xxx()
elif (n == 2):
    obj.yyy()
else:
    print ("error")
</code></pre>
<p>The problem is that when I choose number 2, it asks me to enter my value again. But I want to get the first value from the first function.</p>
<p>So when I type 1 , the program should ask to enter a value, and then when I type 2, the program should just show my value.</p>
<p>I don't see why it keeps asking for a value. What am I doing wrong? </p>
<p>note : when i delete this line from yyy</p>
<pre><code>self.xxx()
</code></pre>
<p>i see this error</p>
<pre><code>'MyClass' object has no attribute 'a'
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>You need to instantiate the value of xxx function and you must return the value from self.a</p>
<pre><code>class MyClass(object):
def xxx(self):
    self.a = int(input("entre a value :"))
    return self.a
def yyy(self):
    xxx_func = self.xxx()
    print (xxx_func)

obj = MyClass()
n = int(input("choose a number : "))
if (n == 1) :
    obj.xxx()
elif (n == 2):
    print(n)
else:
    print ("error")
</code></pre>
<p>Take a look at this link from the official python documentation if you have doubts or any troubles while using the<code>class</code> statement.</p>
<p><a href="https://docs.python.org/3/tutorial/classes.html" rel="nofollow noreferrer">9. Classes — Python 3.7.2 documentation</a> <em>Python 3</em></p>
<p><a href="https://docs.python.org/2/tutorial/classes.html" rel="nofollow noreferrer">9. Classes — Python 2.7.16 documentation</a> <em>Python 2</em></p>
</div>
<span class="comment-copy">Just don't call <code>self.xxx()</code> in <code>yyy</code>.</span>
<span class="comment-copy">@ThierryLathuille he show this : 'MyClass' object has no attribute 'a'</span>
<span class="comment-copy">You're supposed to have entered the value before by typing <code>1</code>, which would call <code>obj.xxx</code>, which would set this attribute.</span>
<span class="comment-copy">@ThierryLathuille Yes but why i see the error idk i have see all the tuto and i can't find any solution</span>
<span class="comment-copy">You'll only see that error if you haven't called <code>xxx</code> before you call <code>yyy</code>.</span>
<span class="comment-copy">when i click 2 , he still ask my to entre a value . i want when i click 2 i get just the value of my variable .</span>
<span class="comment-copy">I just edited, and put the whole code, try again ♥</span>
<span class="comment-copy">i don't want so show n variable . i want to show to value entered in the first fonction .</span>
<span class="comment-copy">What you are asking is not possible, because self.a is defined by user,  if you enter the number 2 on the n input, you can only get <code>n</code> value, but if you want to get <code>self.a</code> value from <code>yyy</code> function, you will get that error, because <code>self.a</code> is not defined yet, <code>self.a</code> will be defined once you call xxx function, and the user input get stored on <code>self.a</code>, <code>self.a</code> is a local variable, if you want to get the value of <code>self.a</code> outside the function, you will get an error, check <a href="https://www.programiz.com/python-programming/global-local-nonlocal-variables" rel="nofollow noreferrer">this</a></span>
<span class="comment-copy">in c language this thing is possible , but in pythone no , thanks now i understand</span>
