<div class="post-text" itemprop="text">
<pre><code>n = int(input()) #fixed length
l = input().split()
</code></pre>
<p>the list that I want should be given as one line input so I dont know how range will help??</p>
</div>
<div class="post-text" itemprop="text">
<p>Check the length using len() and raise the error.</p>
<pre><code>if len(l)&gt;n:
    raise Exception('Please give only {} inputs'.format(n))
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>You can use <a href="https://docs.python.org/3/library/functions.html" rel="nofollow noreferrer">len</a> to determine the size of a list or tuple.</p>
<pre class="lang-py prettyprint-override"><code>if len(l) &gt; n: # assuming n is the max size 
    print("List too large")
</code></pre>
</div>
<span class="comment-copy">Are you editing the data at all? If not just use a tuple because it is immutable!</span>
<span class="comment-copy">Are you by chance looking for the <code>maxsplit</code> argument to <code>split()</code>?</span>
<span class="comment-copy">Sounds like an XY problem. What are you trying to solve with this?</span>
