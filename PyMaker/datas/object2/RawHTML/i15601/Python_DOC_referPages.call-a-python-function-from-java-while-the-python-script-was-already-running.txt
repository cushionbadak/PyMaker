<div class="post-text" itemprop="text">
<p>I need to call a python function from java, pass it an array, and get an array back. I realise I can do this with Jython. </p>
<p>The problem is, my python script needs to load a very heavy (and slow to load) model in order to run the function. So I was wondering if I could leave the python script on somehow, and let it listen to the next function call. Any ideas on this? I could have the java i/o files and communicate like this, but this seems like a (smaller though) bottleneck as well. </p>
<p>I read something about multiprocessing in python, but I am not sure this is the way to go <a href="https://docs.python.org/3/library/multiprocessing.html#multiprocessing-managers" rel="nofollow">https://docs.python.org/3/library/multiprocessing.html#multiprocessing-managers</a></p>
</div>
<div class="post-text" itemprop="text">
<p>I was able to keep the python script running by setting up a python socks server and a java client to communicate. </p>
</div>
<span class="comment-copy">Why not communicate through a socket? Have python listen to a socket, serialize your data (I recommend JSON) and pass it using the socket from Java to python.</span>
<span class="comment-copy">That would be an option. I would have to setup a server from python I guess like here <a href="http://stackoverflow.com/questions/20913440/connecting-python-socket-and-java-socket" title="connecting python socket and java socket">stackoverflow.com/questions/20913440/…</a></span>
<span class="comment-copy">There are plenty of examples all over the internet. You can utilize existing protocols to pass more complex messages. But if you plan on keeping it simple making your own implementation is great.</span>
