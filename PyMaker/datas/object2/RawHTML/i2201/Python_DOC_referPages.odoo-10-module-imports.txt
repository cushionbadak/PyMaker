<div class="post-text" itemprop="text">
<p>I created a new module called <strong>logs</strong> which structure is :</p>
<pre><code>logs
  |_models
        |_models.py
</code></pre>
<p>In this models.py I have a class :</p>
<pre><code>import logging

class Log():

  def __init__(self):
    self.x=5
    .
    .
    .




  def warning(self,msg):
    print(msg)
</code></pre>
<p>Now I want to call this <strong>warning</strong> function from other module called <strong>contacts</strong> which structure is also :</p>
<pre><code>contacts
  |_models
        |_models.py
</code></pre>
<p>in this models.py I import my module <strong>logs</strong> like this :</p>
<pre><code>from ... import logs
</code></pre>
<p>and i call <strong>warning</strong> function like this :</p>
<pre><code>log = logs.models.models.Log()
        log.warning("YAYYYYY")
</code></pre>
<p>This works fine. But I would like to have a smaller line instead of  <strong>logs.models.models.Log()</strong> .Something like <strong>log=Log()</strong> . What change should i do in the code? </p>
<p><strong>Info</strong> : Both modules are in addons folder.</p>
</div>
<div class="post-text" itemprop="text">
<p>Start with</p>
<pre><code>from ...logs.models.models import Log
</code></pre>
<p>The official docs for this are at <a href="https://docs.python.org/3/reference/simple_stmts.html#grammar-token-import_stmt" rel="nofollow noreferrer">https://docs.python.org/3/reference/simple_stmts.html#grammar-token-import_stmt</a> .</p>
</div>
