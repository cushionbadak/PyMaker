<div class="post-text" itemprop="text">
<p>As working on a program using Dict in Python, I am getting a format other than required: the code is as follows:</p>
<pre><code>a = {1:2,2:3,3:4,4:8}
print(a.values())
</code></pre>
<p>getting an output:</p>
<pre><code>dict_values([2, 3, 4, 8])
</code></pre>
<p>here in the output, there is no requirement of "dict_values" and "{ [ ] }", How to remove these wanted things? Actually, I have tried striping but I am thinking of some professional functions to use.</p>
</div>
<div class="post-text" itemprop="text">
<p>You may use the * operator to unpack the sequence and print the values.</p>
<pre><code>a = {1:2,2:3,3:4,4:8}
print(*a.values())
#2 3 4 8
</code></pre>
<p>You can also customize the separator and the end using the keyword arguments <code>sep</code> and <code>end</code> respectively in the call of the <a href="https://docs.python.org/3/library/functions.html#print" rel="nofollow noreferrer">print function</a>.<br/>
For example:</p>
<pre><code>print(*a.values(), sep=',')
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>you could <a href="https://docs.python.org/3/library/stdtypes.html?highlight=str%20join#str.join" rel="nofollow noreferrer"><code>join</code></a> the <code>dict_values</code> (which you need to cast to <code>str</code>) to a string:</p>
<pre><code>', '.join(str(x) for x in a.values())
</code></pre>
</div>
<span class="comment-copy">I just want an output in the form of 2,3,4,8 simple in a iline</span>
<span class="comment-copy">@zwer you need to cast the values to a <code>str</code> for <code>join</code> to work...</span>
<span class="comment-copy">yes, that works, can you please explain the use of * asterisk here?</span>
<span class="comment-copy">You can find additional details in many questions like this: <a href="https://stackoverflow.com/questions/2921847/what-does-the-star-operator-mean" title="what does the star operator mean">stackoverflow.com/questions/2921847/…</a></span>
<span class="comment-copy">Add the <code>sep=','</code>to the <code>print</code></span>
