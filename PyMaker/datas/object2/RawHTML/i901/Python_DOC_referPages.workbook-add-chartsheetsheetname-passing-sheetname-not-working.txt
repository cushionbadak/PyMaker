<div class="post-text" itemprop="text">
<p>workbook.add_chartsheet([sheetname]) passing sheetname not working</p>
<p>Looking at the reference website: <a href="https://xlsxwriter.readthedocs.io/workbook.html?highlight=add_chartsheet" rel="nofollow noreferrer">https://xlsxwriter.readthedocs.io/workbook.html?highlight=add_chartsheet</a></p>
<pre><code>"workbook.add_chartsheet()

add_chartsheet([sheetname])

Add a new add_chartsheet to a workbook.

Parameters: sheetname (string) – Optional chartsheet name, defaults to Chart1, etc.

Return type:    A chartsheet object.

The add_chartsheet() method adds a new chartsheet to a workbook."
</code></pre>
<p>Passing any string variable or string as add.chartsheet(['Data1Chart']) returns with KeyError 'Data1Chart'.</p>
<p>Output should have a chartsheet with custom name instead of 'chart' which is default.</p>
</div>
<div class="post-text" itemprop="text">
<blockquote>
<p>Passing any string variable or string does not work.</p>
</blockquote>
<p>It should work as <a href="https://xlsxwriter.readthedocs.io/workbook.html#workbook-add-chartsheet" rel="nofollow noreferrer">documented</a>. Here is an example that changes the chartsheet name:</p>
<pre><code>import xlsxwriter

workbook = xlsxwriter.Workbook('chartsheet.xlsx')

# Add a worksheet to hold the data.
worksheet = workbook.add_worksheet()

# Add a chartsheet. A worksheet that only holds a chart.
chartsheet = workbook.add_chartsheet('My Chart')

# Add the worksheet data that the charts will refer to.
worksheet.write_column('A1', [2, 3, 4, 5, 6, 7])
worksheet.write_column('B1', [2, 3, 4, 5, 6, 7])

# Create a new bar chart.
chart1 = workbook.add_chart({'type': 'bar'})

# Configure the first series.
chart1.add_series({
    'categories': '=Sheet1!$A$2:$A$7',
    'values':     '=Sheet1!$B$2:$B$7',
})

# Add the chart to the chartsheet.
chartsheet.set_chart(chart1)

# Display the chartsheet as the active sheet when the workbook is opened.
chartsheet.activate();

workbook.close()
</code></pre>
<p>Output:</p>
<p><a href="https://i.stack.imgur.com/jpXph.png" rel="nofollow noreferrer"><img alt="enter image description here" src="https://i.stack.imgur.com/jpXph.png"/></a></p>
</div>
<span class="comment-copy">"not working" is not  useful description of whatever your exact problem is...  What actually happens when you try to use this?</span>
<span class="comment-copy">I think I fixed the wording, also someone posted a very good solution below by jmcnamara.</span>
<span class="comment-copy">The reference was a little confusing with the square brackets. Thanks for clearing that up.</span>
<span class="comment-copy">That is a little confusing since it looks like a Python list. However, that seems to be the standard way of documenting an optional parameter in Python RST docs. See for example the stand library: <a href="https://docs.python.org/3/library/functions.html#func-set" rel="nofollow noreferrer">docs.python.org/3/library/functions.html#func-set</a></span>
