<div class="post-text" itemprop="text">
<div class="question-status question-originals-of-duplicate">
<p>This question already has an answer here:</p>
<ul>
<li>
<a dir="ltr" href="/questions/11063458/python-script-to-copy-text-to-clipboard">Python script to copy text to clipboard [duplicate]</a>
<span class="question-originals-answer-count">
                    11 answers
                </span>
</li>
</ul>
</div>
<p>I want to write simple lambda or function that takes a string, changes to lowercase except for the first case and copies the result to clipboard in Linux. </p>
<p>I have lambda:</p>
<pre><code>to_lower = lambda s : s[0].upper() + s[1:].lower()
</code></pre>
<p>This is ok: </p>
<pre><code>to_lower("some Sentence with Incorrect Size of characterS")
Some sentence with incorrect size of characters
</code></pre>
<p>But how I copy the result of this lambda to clipboard automatically, so that I can paste it into a text editor. At the moment, I have to manually select text in python command line to copy to clipboard. </p>
</div>
<div class="post-text" itemprop="text">
<p>Check out pyperclip:
<a href="http://coffeeghost.net/2010/10/09/pyperclip-a-cross-platform-clipboard-module-for-python/" rel="nofollow">http://coffeeghost.net/2010/10/09/pyperclip-a-cross-platform-clipboard-module-for-python/</a></p>
<pre><code>import pyperclip
pyperclip.copy('The text to be copied to the clipboard.')
</code></pre>
</div>
<span class="comment-copy">there is <a href="https://docs.python.org/3/library/stdtypes.html#str.title" rel="nofollow noreferrer"><code>word.title()</code> method</a></span>
