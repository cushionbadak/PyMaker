<div class="post-text" itemprop="text">
<p>Obviously, in conditional statements you need to make sure a true or false value is returned to either execute or skip the block of code associated with the if statement. How does a single value serve as a true Boolean in python? I'm not sure if this is a universal for all languages but I've discovered it in python 3.x.</p>
<p>Example:</p>
<pre><code>value = 1
if value:
   print("value == True") # prints every time
</code></pre>
<p>I'd expect the compiler to complain or just return false. Why does the compiler perceive <code>if value</code> as true?</p>
</div>
<div class="post-text" itemprop="text">
<p>Integer or floats values other than 0 are treated as True:</p>
<pre><code>In [8]: bool(int(1))
Out[8]: True

In [9]: bool(int(0))
Out[9]: False

In [10]: bool(int(-1))
Out[10]: True


In [16]: bool(float(1.2e4))
Out[16]: True

In [17]: bool(float(-1.4))
Out[17]: True

In [20]: bool(0.0)
Out[20]: False

In [21]: bool(0.000001)
Out[21]: True
</code></pre>
<p>Similarly, empty lists, sets, dicts, empty string, None, etc are treated as false:</p>
<pre><code>In [11]: bool([])
Out[11]: False

In [12]: bool({})
Out[12]: False

In [13]: bool(set())
Out[13]: False

In [14]: bool("")
Out[14]: False

In [19]: bool(None)
Out[19]: False
</code></pre>
</div>
<span class="comment-copy">also <code>None</code> and of course <code>False</code>. Might also provide a link to the relevant <a href="https://docs.python.org/3/library/stdtypes.html#truth-value-testing" rel="nofollow noreferrer">docs</a></span>
<span class="comment-copy">Is there a reason just 0 returns false and why give non Boolean values Boolean values?</span>
<span class="comment-copy">@AdamSmith Just added None and floats values as well.</span>
<span class="comment-copy">Also what is the benefits over using a let's say "real" Boolean value over a "fake" Boolean value if they are both capable of the same things?</span>
<span class="comment-copy">@JGerulskis What do you mean? Build in types can be used as booleans. For example to add to list if is empty or not: <code>if a_list: print("List not empty")</code></span>
