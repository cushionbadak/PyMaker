<div class="post-text" itemprop="text">
<pre><code>spam = ['cat','dog','bat']
spam.insert(0,'poo')
spam
</code></pre>
<p>this is my code, why is there nothing showing after I run this program?  </p>
</div>
<div class="post-text" itemprop="text">
<p>Use </p>
<pre><code>print(spam)
</code></pre>
<p>to show the content of "spam". Writing "spam" won't change anything.</p>
</div>
<span class="comment-copy">If you search on the phrase "Python output", you’ll find resources that can explain it much better than we can in an answer here.</span>
<span class="comment-copy">I've seen an instance where someone did a very similar quote and put spam which had the same output</span>
<span class="comment-copy">if you run python3 on the terminal it will probably show the content of the variable by typing "spam" only, but when you run as a program this is the way.</span>
