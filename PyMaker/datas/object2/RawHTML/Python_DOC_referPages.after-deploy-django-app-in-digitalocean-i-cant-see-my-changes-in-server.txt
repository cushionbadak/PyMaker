<div class="post-text" itemprop="text">
<p>I was deploy my webapp in digitalocean which is written by django. Now i change and add some file in it. I change the template folder and also add some photos in static folder. After change i pull the changes from github in my server app directory. But i can't see the changes. I stop the nginx server. I use this command </p>
<pre><code>sudo systemctl start nginx 
sudo systemctl stop nginx 
sudo systemctl restart nginx
</code></pre>
<p>then again i start but no changes. what should i do?</p>
</div>
<div class="post-text" itemprop="text">
<p>In order to reload Django app restart your <code>gunicorn</code> service.</p>
<pre><code>sudo systemctl restart gunicorn
</code></pre>
</div>
<span class="comment-copy">Try to restart uwsgi service.</span>
<span class="comment-copy">I don't use uwsgi service. I use gunicorn and nginx</span>
<span class="comment-copy">If you use gunicorn, so restart it, because nginx doesn't reload Django app.</span>
<span class="comment-copy">did i need to first stop gunicorn then restart ?</span>
<span class="comment-copy">@SergeyPugach thanks bro. But one my page show me Server Error (500). can you please tell me why?. I mean some of pages work and some of pages show me this error?</span>
<span class="comment-copy">Thanks bro. Then i get a service error 500. I solved this problem. I just set debug=true and found my error. And did my migrations. Thank you .</span>
<span class="comment-copy">If it solved your problem please upvote my answer and mark it as solution according to StackOverflow rules. Good luck!</span>
<span class="comment-copy">I marked it but i can't upvote because of reputation. sorry bro!.</span>
