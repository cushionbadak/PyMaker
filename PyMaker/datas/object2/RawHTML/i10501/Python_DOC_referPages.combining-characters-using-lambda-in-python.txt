<div class="post-text" itemprop="text">
<p>I want to result using 'str += ' style in the lambda function </p>
<p>example (with error) : </p>
<pre><code>t=lambda text text : [c for c in text str += c.upper()]
t(['h','e','l','l','o'])
</code></pre>
<p>I expect to result as :</p>
<pre><code>HELLO
</code></pre>
<p>How can i fix above lambda function with state variable like 'str +=' style</p>
<p>if 'str +=' style not possible, please explain in detail why the impossible. Do not just Short-Answer that wrong. </p>
</div>
<div class="post-text" itemprop="text">
<p>I think you mean this:</p>
<pre><code>&gt;&gt;&gt; t = lambda text: ''.join(text).upper()
&gt;&gt;&gt; t(['h','e','l','l','o'])
'HELLO'
</code></pre>
<p>Documentation: <a href="https://docs.python.org/3/tutorial/controlflow.html#lambda-expressions" rel="nofollow">Lambda Expressions</a></p>
</div>
<span class="comment-copy">There are at least two syntax errors in the above code. Please post valid Python.</span>
<span class="comment-copy">BTW, although you <i>can</i> concatenate strings using <code>+=</code> it is better to use <code>.join</code>, please see <a href="http://stackoverflow.com/questions/39312099/why-is-join-faster-than-in-python">Why is ''.join() faster than += in python?</a> and the linked pages for details.</span>
<span class="comment-copy">Note that assigning a <code>lambda</code> expression to a name defeats the entire purpose of <code>lambda</code> expressions and is expressly recommended against in PEP-8.</span>
<span class="comment-copy">i will confirm to lambda function which possible save state like 'str +=' style</span>
<span class="comment-copy">Also, don't use <code>str</code> as a variable name (even in example code) as that shadows the built-in <code>str</code> type.</span>
<span class="comment-copy">Is it more efficient to do the <code>.upper()</code> after the <code>join</code>?</span>
<span class="comment-copy">@Chris_Rands Probably. I was just mimicking OP's style a little</span>
<span class="comment-copy">'I want to use the <code>str += </code> style in the lambda function.'</span>
<span class="comment-copy">@DanielLee That's not really an (efficient) thing</span>
<span class="comment-copy">@TerryA, His question mentions it twice. I agree your way is better but at least explain that to him in your answer.</span>
