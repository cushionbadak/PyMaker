<div class="post-text" itemprop="text">
<pre><code>import random
import urllib.request

def down_load_imag(url):
    name = random.randrange(1, 1000)
    full_name = str(name) + ".jpg"
    urllib.request.urlretrieve(url, full_name)

down_load_imag ("http://3.bp.blogspot.com/-WjQkpjkw9uQ/Vij8lG0pCdI/AAAAAAAAAJ4/-CifLZ5KG-Y/s1600/fedora_infinity_140x140.png") 
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>in python3 you must use <code>input()</code> function:</p>
<pre><code>import random
import urllib.request

def down_load_imag(url):
    name = random.randrange(1, 1000)
    full_name = str(name) + ".jpg"
    urllib.request.urlretrieve(url, full_name)
url = input('Enter url: ')
# here you can evaluate url with regex and if passed:
down_load_imag (url) 
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>Python comes with the <a href="https://docs.python.org/3/library/argparse.html" rel="nofollow" title="argparse">argparse</a> module. Suppose your code lives in <code>downloader.py</code>, then add this:</p>
<pre><code>import argparse
import sys

parser = argparse.ArgumentParser(description="Download image to random filename")
parser.add_argument("--url", help="image url to download")
args = parser.parse_args() # parses sys.argv

url = args.url

if url is None:
    print("Give me an URL please!", file=sys.stderr)
    sys.exit(-1) 

down_load_imag(url)
</code></pre>
<p>Then use like this:</p>
<pre><code>python3 downloader.py --url http://what.ever.com/image.jpg
</code></pre>
<p>You can refine this to take the url using <code>input()</code> if <code>url</code> is None.</p>
</div>
<span class="comment-copy">Please format your code and explain exactly what you need.</span>
<span class="comment-copy">that is nice code, I am able run without error so I have no idea what you want help with, some explanation is definitely required.</span>
<span class="comment-copy">Just do <code>down_load_imag ( input() ) </code> instead of the last line.</span>
<span class="comment-copy">i do and that what i get                                                                                            Traceback (most recent call last):   File "/usr/lib64/python3.5/code.py", line 91, in runcode     exec(code, self.locals)   File "&lt;input&gt;", line 1, in &lt;module&gt; NameError: name 'down_load_imag' is not defined                            @AhsanulHaque</span>
<span class="comment-copy">Remove the space <code>down_load_imag</code> and <code>(</code></span>
<span class="comment-copy">thank's guys it's work</span>
