<div class="post-text" itemprop="text">
<p>I am trying to create epoch values for a date field using Python. It works great for all the dates after '1970-01-01'. Any date before 1970 doesn't work.</p>
<p>Here is the code I am using:</p>
<pre><code>int(time.mktime(time.strptime('1969-01-01', '%Y-%m-%d'))) - time.timezone
</code></pre>
<p>Error:</p>
<pre><code>OverflowError: mktime argument out of range
</code></pre>
<p>Is there any alternative way to create negative epoch value?</p>
</div>
<div class="post-text" itemprop="text">
<p>From the documentation of the <a href="https://docs.python.org/3/library/time.html" rel="nofollow noreferrer"><code>time</code></a> module:</p>
<blockquote>
<p>The functions in this module may not handle dates and times before the epoch or far in the future.</p>
</blockquote>
<p>So that's out.  I would suggest using the <a href="https://docs.python.org/3/library/datetime.html#date-objects" rel="nofollow noreferrer"><code>datetime</code></a> module instead, which can handle dates in  years between 1 and 9999. I'm afraid without knowing your use case we can't be of more help.</p>
</div>
<span class="comment-copy">Thanks Patrick. Dates that i am trying to deal with ranges from 1900-currentdate.</span>
