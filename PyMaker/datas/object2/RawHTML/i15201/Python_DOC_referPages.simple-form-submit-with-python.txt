<div class="post-text" itemprop="text">
<p>I am totall newbie to python so if the question will be totally newbie please excuse me.
I have simple landing page <code>index.html</code> and have a form there:</p>
<pre><code>&lt;form action="getPhone.py" method = "POST"&gt;
First name:
&lt;input type="text" name="firstname" &gt;

Phone:
&lt;input type="text" name="lastname" &gt;

&lt;input type="submit" value="Submit"&gt;
&lt;/form&gt;
</code></pre>
<p>So can you provide a simple basic example of <code>getPhone.py</code> for me How to get data from this form ( I want to send this data via email, but as first step i just have to get it) ?
<code>UPD</code>: I am developing on windows.I don't know about such things as will my server be able to execute cgi scripts or not.</p>
</div>
<div class="post-text" itemprop="text">
<p>If your web-server doesn't support Python CGI scripts, you could use some micro-framework.<br/>
For example, using <a href="http://flask.pocoo.org/" rel="nofollow">Flask</a>:</p>
<pre><code>from flask import Flask, request
app = Flask(__name__)

@app.route('/get_phone/', methods=['POST', 'GET'])
def get_phone():
    if request.method == 'POST':
        print 'First name:', request.form['firstname']
        print 'Phone:', request.form['lastname']

    return 'Take a look at your terminal!'

if __name__ == '__main__':
    app.run()
</code></pre>
<p>(Please note that you use a textbox named <code>"lastname"</code>, but you call it <code>"Phone"</code>. I did the same.)</p>
<p>Then run it and use <code>http://127.0.0.1:5000/get_phone/</code> instead of <code>getPhone.py</code>:</p>
<pre><code>&lt;form action="http://127.0.0.1:5000/get_phone/" method="POST"&gt;
</code></pre>
<p>It's a more popular way, especially if you consider growing your site. If you won't need anything else, you could go with CGI and python-supported web-server.</p>
<p>Actually, it would make sense if you provide some more information on what you're trying to do. Do you want to create your own website and have no idea on how it works? I guess it'd be easier for you to jump right into the web-frameworks like <a href="http://flask.pocoo.org/" rel="nofollow">Flask</a> or <a href="http://djangoproject.com/" rel="nofollow">Django</a>.</p>
<p>If you aren't sure you'd like to progress in web-development and just want to learn Python and its builtin libs, you can set up some webserver or take a look at <a href="https://docs.python.org/2/howto/webservers.html" rel="nofollow">Python's webservers manual</a>.</p>
</div>
<span class="comment-copy">Can your webserver execute <a href="https://docs.python.org/3/library/cgi.html" rel="nofollow noreferrer">Python CGI scripts</a>?</span>
<span class="comment-copy">i don't really know=) I am totall newbie as I stated before.If it is necessary I can make my webserver execute cgi scripts.</span>
<span class="comment-copy">I will try this now=)</span>
<span class="comment-copy">if you do go down the road of using flask - this is a fantastic tutorial - <a href="http://blog.miguelgrinberg.com/post/the-flask-mega-tutorial-part-i-hello-world" rel="nofollow noreferrer">blog.miguelgrinberg.com/post/…</a></span>
<span class="comment-copy">The answer to your question about progressing in web development with python - right now I just have to build simple landing page which is needed only for collecting names and phone number.So that's why I don't want to use something like django. I just need things to work with as less actions from me as available.If I like the result later I will consider to use python  for bigger projects of course=)</span>
<span class="comment-copy">i just heard that python web development is easy and interesting so now trying it=)</span>
<span class="comment-copy">I did as stated  in your answer but when I  submit my form I get the following : <code>page: http://127.0.0.1:5000/get_phone/</code> is not available</span>
