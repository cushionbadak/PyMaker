<div class="post-text" itemprop="text">
<div class="question-status question-originals-of-duplicate">
<p>This question already has an answer here:</p>
<ul>
<li>
<a dir="ltr" href="/questions/1894269/convert-string-representation-of-list-to-list">Convert string representation of list to list</a>
<span class="question-originals-answer-count">
                    14 answers
                </span>
</li>
</ul>
</div>
<p>I have a text file that looks like this</p>
<pre><code>['M', '&gt;', 'F', 'P', ' ', 'N', 'I', '$', 'F', '\x19', 'J', 'B', 'P', 'T', '%', '&lt;', 'M', 'Q', '&gt;', '\x00', 'I', 'F', 'N', 'J', '\x16', 'X', '\\', 'H']
</code></pre>
<p>Its already in a list form. 
How do i load it into python as a list without it think that all the separators between the sections are actual characters?</p>
</div>
<div class="post-text" itemprop="text">
<p>Read the contents as a string, and use <a href="https://docs.python.org/3/library/ast.html#ast.literal_eval" rel="nofollow"><code>ast.literal_eval</code></a> to convert it into a Python data structure.</p>
<p>That is:</p>
<pre><code>import ast
with open('datafile.txt') as f:
    data = ast.literal_eval(f.read())
</code></pre>
</div>
<span class="comment-copy">Usually, the <i>right</i> answer to this question is "don't generate files like that, then you won't need to parse them".</span>
<span class="comment-copy">or: <a href="http://stackoverflow.com/questions/1894269/convert-string-representation-of-list-to-list-in-python" title="convert string representation of list to list in python">stackoverflow.com/questions/1894269/…</a></span>
<span class="comment-copy">I still get ['M', '&gt;', 'F', 'P', ' ', 'N', 'I', '$', 'F', '\x19', 'J', 'B', 'P', 'T', '%', '&lt;', 'M', 'Q', '&gt;', '\x00', 'I', 'F', 'N', 'J', '\x16', 'X', '\\', 'H'] as the output.     It was supposed to be "M&gt;FP NI$F..." etc.</span>
<span class="comment-copy">it is a <code>list</code>. That is the output you get if you print a list. Please read the <a href="https://docs.python.org/3/tutorial/introduction.html#lists" rel="nofollow noreferrer">Python tutorial 3.1.3 <i>Lists</i></a></span>
<span class="comment-copy">To convert it to a string, use <a href="https://docs.python.org/3/library/stdtypes.html?highlight=join#str.join" rel="nofollow noreferrer"><code>join</code></a>.</span>
