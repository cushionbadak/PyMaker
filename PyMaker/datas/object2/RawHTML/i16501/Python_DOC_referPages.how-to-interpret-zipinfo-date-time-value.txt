<div class="post-text" itemprop="text">
<p>The documentation on <a href="https://docs.python.org/3/library/zipfile.html#zipinfo-objects" rel="nofollow">ZipInfo objects</a> wasn't really helpful regarding this. What is this <code>date_time</code> value? Is it UTC or local machine time?</p>
</div>
<div class="post-text" itemprop="text">
<p>Ok, I guess I just found my answer <a href="http://mindprod.com/jgloss/zip.html" rel="nofollow">here</a>:</p>
<blockquote>
<p>Inside zip files, dates and times are stored in local time in 16 bits, not UTC (Coordinated Universal Time/Temps Universel Coordonné) as is conventional, using an ancient MS DOS format. Bit 0 is the least signifiant bit. The format is little-endian. There was not room in 16 bit to accurately represent time even to the second, so the seconds field contains the seconds divided by two, giving accuracy only to the even second.</p>
</blockquote>
</div>
