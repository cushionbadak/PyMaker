<div class="post-text" itemprop="text">
<p>I want to calculate number of number of IP addresses from 2 given IP addresses.</p>
<p>Example: 127.0.1.10 and 127.0.0.200 is 67 IP addresses..</p>
<p>What's easy way of doing this?</p>
<p>I've seen other examples, but I'm looking for a Python example. Thanks.</p>
</div>
<div class="post-text" itemprop="text">
<pre><code>&gt;&gt;&gt; import socket
&gt;&gt;&gt; def iptoint(ip):
...     return int(socket.inet_aton(ip).encode('hex'), 16)
... 
&gt;&gt;&gt; iptoint('127.0.0.200')
2130706632
&gt;&gt;&gt; iptoint('127.0.1.10') - iptoint('127.0.0.200')
66
&gt;&gt;&gt; def ipdistance(ip1, ip2):
...     return abs(iptoint(ip1) - iptoint(ip2)) + 1
... 
&gt;&gt;&gt; ipdistance('127.0.1.10', '127.0.0.200')
67
</code></pre>
</div>
<span class="comment-copy">So you're essentially asking how to do base-256 subtraction?  What have you tried so far?</span>
<span class="comment-copy">Convert the IPs to numbers, then subtract.</span>
<span class="comment-copy">for more general IP calculation, like listing out the IPs in a <code>/24</code> network, see <a href="https://docs.python.org/3/howto/ipaddress.html" rel="nofollow noreferrer">docs.python.org/3/howto/ipaddress.html</a></span>
<span class="comment-copy">Add 1 to make it include both endpoints.</span>
