<div class="post-text" itemprop="text">
<p>I have create dict dynamiquly from a list with :</p>
<pre><code>exec("mydict_%=%" % (key, value))
</code></pre>
<p>i try to get a objet from a string like:</p>
<pre><code>def getdict(String):
   code ...
   return dict


mydict = []
mydict = getdict("mydict_sample")
</code></pre>
<p>have you the solution ?</p>
</div>
<div class="post-text" itemprop="text">
<p><code>ast.literal_eval</code> can evaluate strings that represent python dicts. See <a href="https://docs.python.org/3/library/ast.html#ast.literal_eval" rel="nofollow">the documentation</a>.</p>
</div>
<span class="comment-copy">You should use a list instead of defining multiple variables.</span>
<span class="comment-copy">thanks a lot : Safely evaluate an expression node or a string</span>
