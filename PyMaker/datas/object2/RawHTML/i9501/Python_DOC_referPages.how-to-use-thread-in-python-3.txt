<div class="post-text" itemprop="text">
<p>When I run Python script I get error:</p>
<pre><code>NameError: name 'Thread' is not defined
</code></pre>
<p>And error:</p>
<pre><code>AttributeError: 'Queue' object has no attribute 'join'
</code></pre>
<p>This is my code:</p>
<pre><code> q = Queue()

    for i in range(num_worker_threads):
        t = Thread(target=worker)
        t.setDaemon(True)
        t.start()

    for item in urls:
        q.put(item)

    q.join()
</code></pre>
<p>How to use Thread in Python 3?</p>
</div>
<div class="post-text" itemprop="text">
<p>Import the corresponding library:</p>
<pre><code>from threading import Thread
</code></pre>
</div>
<span class="comment-copy">I can assume that should be: <code>t =  threading.Thread(target=worker)</code></span>
<span class="comment-copy">This is not a <a href="https://stackoverflow.com/help/mcve">Minimal, Complete, and Verifiable example</a>.</span>
<span class="comment-copy"><a href="https://docs.python.org/3/library/threading.html" rel="nofollow noreferrer">docs.python.org/3/library/threading.html</a></span>
<span class="comment-copy"><code>t = threading.Thread(target=worker)</code> or <code>t = Thread(target=worker)</code> can both be right, depends on how you import. If you use the long option you only need <code>import threading</code> but if you use the short answer you need <code>from threading import Thread</code>, this is, importing the whole module threading for the first case and just the <code>Thread</code> class for the second.</span>
<span class="comment-copy">But what is reason this: <code>AttributeError: 'Queue' object has no attribute 'join'</code></span>
<span class="comment-copy">Which <code>Queue</code> object are you using? Write the import statement</span>
