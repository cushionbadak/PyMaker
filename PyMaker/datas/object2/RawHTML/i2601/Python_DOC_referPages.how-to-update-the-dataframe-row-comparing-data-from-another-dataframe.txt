<div class="post-text" itemprop="text">
<pre><code>df1
    antecedent  consequent  p
0   [5] [1] 1.0
1   [3] [1, 5, 4,]  1.0

    nodealarm   count1  nodealarmrank1
0   test1   110 1
1   test2   201 2
2   test3   300 3
3   test4   600 4
4   test100 60  5
</code></pre>
<p>output </p>
<pre><code>df1
    antecedent  consequent  p
0   [test100]   [test1]  1.0
1   [test3]   [test1,test100,test4] 1.0
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>Creating dictionary <a href="https://docs.python.org/3/library/stdtypes.html#mapping-types-dict" rel="nofollow noreferrer"><code>d</code></a> and <a href="https://pandas.pydata.org/pandas-docs/stable/generated/pandas.DataFrame.apply.html" rel="nofollow noreferrer"><code>apply</code></a> on columns:</p>
<pre><code>d =dict(df2[['nodealarmrank1','nodealarm']].values)

df1['antecedent'] = df1['antecedent'].apply(lambda x: list(map(d.get,x)))
df1['consequent'] = df1['consequent'].apply(lambda x: list(map(d.get,x)))

df1
    antecedent  consequent              p
0   [test100]   [test1]                 1
1   [test3]     [test1, test100, test4] 1
</code></pre>
<p>Or using for loop and <a href="https://pandas.pydata.org/pandas-docs/stable/generated/pandas.DataFrame.update.html" rel="nofollow noreferrer"><code>update</code></a>:</p>
<pre><code>for column in ['antecedent','consequent']:
    df1.update(df1[column].apply(lambda x: list(map(d.get,x))))

for column in ['antecedent','consequent']:
    df1[column] = df1[column].apply(lambda x: list(map(d.get,x)))
</code></pre>
<p>Setup<br/>
dataframe 1:</p>
<pre><code>df1 = pd.DataFrame({'antecedent':[[5],[3]],'consequent':[[1],[1, 5, 4,]],'p':[1,1]})
df1

    antecedent  consequent  p
0   [5]         [1]         1
1   [3]         [1, 5, 4]   1
</code></pre>
<p>dataframe 2:</p>
<pre><code>df2 = pd.DataFrame({'nodealarm':['test1','test2','test3','test4','test100'],'count1':[110,201,300,600,60],'nodealarmrank1':[1,2,3,4,5]})
df2

    nodealarm   count1  nodealarmrank1
0   test1       110     1
1   test2       201     2
2   test3       300     3
3   test4       600     4
4   test100     60      5
</code></pre>
</div>
<span class="comment-copy">Welcome to SO. Please ensure that you format your question correctly. In this case it looks like there's insufficient information to answer your question even with correct formatting. Please see how to create an <a href="https://stackoverflow.com/help/mcve">MCVE</a></span>
<span class="comment-copy">Please <a href="https://stackoverflow.com/help/someone-answers"><code>upvote and accept</code></a> if the solution provided your required result.</span>
