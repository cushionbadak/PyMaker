<div class="post-text" itemprop="text">
<p>I have a list of file like the following</p>
<pre><code>'TRIAL_20134_75690_TOTAL_2018-08-12-17-18.csv'
</code></pre>
<p>I want to rename them the parte after the last underscore, such as the file will be renamed like:</p>
<pre><code>'TRIAL_20134_75690_TOTAL.csv'
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>Use <code>os.rename</code> from the <a href="https://docs.python.org/3/library/os.html#os.rename" rel="nofollow noreferrer"><code>os</code></a> library to do the renaming.</p>
<p>To get the sting up to the last index of an underscore (<code>_</code>), use the <code>rindex</code> method of <code>string</code>.</p>
<p>You also need to concatenate the extension back on.</p>
<p>I.e.</p>
<pre><code>import os
f = 'TRIAL_20134_75690_TOTAL_2018-08-12-17-18.csv'
os.rename(f, f[:f.rindex('_'] + '.csv')
</code></pre>
</div>
<span class="comment-copy">Check this out: <a href="https://stackoverflow.com/questions/225735/batch-renaming-of-files-in-a-directory" title="batch renaming of files in a directory">stackoverflow.com/questions/225735/…</a></span>
<span class="comment-copy">The <a href="https://docs.python.org/3/library/os.html" rel="nofollow noreferrer">os</a> and <a href="https://docs.python.org/3/library/shutil.html" rel="nofollow noreferrer">shutil</a> modules in the standard library have functions that do this sort of thing.  It's worth checking out if there's something helpful <a href="https://docs.python.org/3/library/index.html" rel="nofollow noreferrer">in the standard library</a> <i>before</i> asking SO.</span>
<span class="comment-copy">@DavidMaze I not everyone has the same philosophy as me, but in the time it took you to write the comment, I just got the guy on his way. :p</span>
