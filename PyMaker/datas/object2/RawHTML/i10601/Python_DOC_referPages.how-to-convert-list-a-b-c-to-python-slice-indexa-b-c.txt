<div class="post-text" itemprop="text">
<p>For example, I'm having a group of multi-dimension arrays. I want to write a method to specify the size of the slice for this array such as:</p>
<pre><code>slice = data[:a, :b, :c]
</code></pre>
<p>Because I could only get a list of [a, b, c]. I want to know how can I convert this list to slice index. Or is there a way to connect the list with slice index so as to operate this array as:</p>
<pre><code>list = [a, b, c]
slice = data[list]
</code></pre>
<p>Any reply would be appreciated.</p>
</div>
<div class="post-text" itemprop="text">
<p>Use the <a href="https://docs.python.org/3/library/functions.html#slice" rel="noreferrer">slice()</a> function.</p>
<pre><code>my_list = [a, b, c]
my_slices = tuple(slice(x) for x in my_list)
my_slice = data[my_slices]
</code></pre>
<p>(I updated the variable names to avoid shadowing the builtins by mistake.)</p>
<p><code>slice(x)</code> is equivalent to the slice <code>:x</code>, and <code>slice(x, y, z)</code> is <code>x:y:z</code></p>
</div>
<span class="comment-copy">Or you using numpy or native python lists?</span>
<span class="comment-copy">I am using numpy list</span>
<span class="comment-copy">Thanks for your quick reply! I tried your method. But it returned a "TypeError: 'tuple' object is not callable" error.</span>
<span class="comment-copy">Try it with a clean run. I think you overwrote <code>slice</code> in your session. Never use names of built-in functions as own variable or function names.</span>
<span class="comment-copy">Did you use 'tuple' or 'slice' as a variable name? If so, you should rename your variables to avoid clashing with builtin names.</span>
