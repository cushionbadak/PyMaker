<div class="post-text" itemprop="text">
<p>I spent my time almost 2 hrs to use the pre-trained model (tensorflow) from  <a href="https://github.com/AnjieZheng/Tensorflow-Number-Plate-Recognition" rel="nofollow noreferrer">weights.npz</a> for detecting license plate but I can't fix it. I got this error message, I've never seen before. so, how to fix it?</p>
<blockquote>
<p>Traceback (most recent call last):   File "./detect.py", line 189, in
  
      pt1 = tuple(reversed(map(int, pt1))) TypeError: 'map' object is not reversible</p>
</blockquote>
</div>
<div class="post-text" itemprop="text">
<p>In python3, <a href="https://docs.python.org/3/library/functions.html#map" rel="nofollow noreferrer"><code>map</code></a> returns an iterator, not list. You need to wrap the call to <code>map()</code> with the <code>list</code> constructor:</p>
<pre><code>pt1 = tuple(reversed(list(map(int, pt1))))
</code></pre>
<p>See more: <a href="https://stackoverflow.com/questions/1303347/getting-a-map-to-return-a-list-in-python-3-x">Getting a map() to return a list in Python 3.x</a></p>
</div>
<span class="comment-copy">This is clearly a python 2 module, the code for python 3 should be <code>pt1 = tuple(reversed(list(map(int, pt1))))</code>; <code>list</code>s  after-all are definitely reverseible</span>
