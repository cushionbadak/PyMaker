<div class="post-text" itemprop="text">
<p>Hello i want to retrieve data from mysql database and i want to add that in my pyqt5 tablewidget but it is giving me error that int object is not iterable </p>
<p>this is my code </p>
<pre><code>def LoadData(self):
    conn = mdb.connect("localhost", "root", "", "pyqt5")

    with conn:
        query = "SELECT * FROM data"
        cur = conn.cursor()
        result = cur.execute(query)
    self.tableWidget.setRowCount(0)
    for row_number, row_data in enumerate():
        self.tableWidget.insertRow(row_number)
        for col_number, data in enumerate(row_data):
            self.tableWidget.setItem(row_number, col_number, QtWidgets.QTableWidgetItem(str(data)))
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>Try this. you are missing "enumerate(result)" into first for loop.</p>
<pre><code> def LoadData(self):
    conn = mdb.connect("localhost", "root", "", "pyqt5")

    with conn:
        query = "SELECT * FROM data"
        cur = conn.cursor()
        result = cur.execute(query)
    self.tableWidget.setRowCount(0)
    for row_number, row_data in enumerate(result):
        self.tableWidget.insertRow(row_number)
        for col_number, data in enumerate(row_data):
            self.tableWidget.setItem(row_number, col_number, QtWidgets.QTableWidgetItem(str(data)))
</code></pre>
</div>
<span class="comment-copy">Please provide error message in details. Are you facing as issue with this line? for row_number, row_data in enumerate():</span>
<span class="comment-copy">the error is for row_number, row_data in enumerate(result): TypeError: int object is not iterable</span>
<span class="comment-copy">@Parwiz. Do you ever bother reading documentation or tutorials? If you did, you'd see that the <a href="https://docs.python.org/3/library/sqlite3.html#cursor-objects" rel="nofollow noreferrer">Cursor.execute</a> method clearly does not return a result-set. Please do some basic research before posting questions on SO.</span>
<span class="comment-copy">yea i have give the result in the loop but the same problem     it is telling for row_number, row_data in enumerate(result): TypeError: int object is not iterable</span>
