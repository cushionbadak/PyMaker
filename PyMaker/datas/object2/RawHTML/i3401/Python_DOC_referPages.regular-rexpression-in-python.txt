<div class="post-text" itemprop="text">
<p>Please help
This is an error I am getting when I am run the code.</p>
<pre><code>import re

pat = ['ashu', 'gupta']

text = "my name is ashutosh gupta"

for pattern in pat:
    print('i am seraching for:  '+pattern)

if re.search(pat, text):
    print("Match")
else:
    print("NOT mactch")
</code></pre>
<p>error is below .blease help                                                                :</p>
<pre><code>i am seraching for:  ashu
i am seraching for:  gupta
Traceback (most recent call last):
  File "Regular_expression.py", line 11, in &lt;module&gt;
    if re.search(pat,text):
  File "/usr/lib/python2.7/re.py", line 146, in search
    return _compile(pattern, flags).search(string)
  File "/usr/lib/python2.7/re.py", line 237, in _compile
    p, loc = _cache[cachekey]
TypeError: unhashable type: 'list'
</code></pre>
</div>
<div class="post-text" itemprop="text">
<pre><code>import re

pat = ['ashu', 'gupta']

text = "my name is ashutosh gupta"

for pattern in pat:
    print('i am seraching for:  '+pattern)

    if re.search(pattern, text):
        print("Match")
    else:
        print("NOT mactch")
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>Well the question is already answered by @jishad av above.
But i dont think that you would require using re in this case.</p>
<pre><code>pat = ['ashu', 'gupta']
text = "my name is ashutosh gupta"
for pattern in pat:
    print('i am seraching for:  '+pattern)
    if pattern in text:
        print("Match)
    else:
        print("Not Match")
</code></pre>
</div>
<span class="comment-copy">you are passing <code>pat</code> (which happens to be a list) in <code>re.search</code> take a look at [<a href="https://docs.python.org/3/library/re.html#re.search]" rel="nofollow noreferrer">docs.python.org/3/library/re.html#re.search]</a>, it expects regex string.</span>
