<div class="post-text" itemprop="text">
<p>My input should be a integer. Based upon the integer value, I have to give strings in the list..and my code is:</p>
<pre><code>words = []
i = int(raw_input())
for i in range(0,i):
   j= str(raw_input())
   words.append(j)

unique_word_count = len(set(words))

print unique_word_count
for j in words:
    print words.count(j),
</code></pre>
<p>and my <strong>input is</strong></p>
<pre><code>4
bcdef
abcdefg
bcde
bcdef
</code></pre>
<p>and the output is</p>
<pre><code>3
2 1 1 2
</code></pre>
<p>but I need output like this:</p>
<pre><code>3
2 1 1
</code></pre>
<p>What should I do?</p>
</div>
<div class="post-text" itemprop="text">
<p>Use <a href="https://docs.python.org/3/library/collections.html#collections.Counter" rel="nofollow noreferrer"><code>collections.Counter</code></a>:</p>
<pre><code>import collections
print collections.Counter(words)
</code></pre>
<p>For your input you'd get:</p>
<pre><code>Counter({'bcdef': 2, 'bcde': 1, 'abcdefg': 1})
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>You're sooooooo close!</p>
<pre><code>for j in set(words):
    print words.count(j),
</code></pre>
<p>Take note that, since you're reporting each word's count only once, you've lost a strong association between each count and the word it represents.  Both <code>set</code> and <code>Counter</code> will lose that ordering.</p>
</div>
<div class="post-text" itemprop="text">
<p>You forgot to convert the list to a set in your second loop:</p>
<pre><code>for j in set(words):
    print words.count(j)
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>you should use <code>from collections import Counter</code>
and </p>
<pre><code>words = []
i = int(input())
for index in range(0,i):
   j= input()
   words.append(j)

unique_word_count = Counter(words)
print(unique_word_count)
</code></pre>
<p>its output the amount of each word</p>
</div>
<span class="comment-copy">Use a <a href="https://docs.python.org/3/library/collections.html#collections.Counter" rel="nofollow noreferrer"><code>Counter</code></a>.</span>
<span class="comment-copy">Do you need to retain an association between a word and its count?  That's not in your output.</span>
<span class="comment-copy">i dont want any association between them</span>
<span class="comment-copy">i need output only like this 3 in first line and 2 1 1 in second line</span>
<span class="comment-copy">@Bhavani Note that <code>len(Counter(words))</code> is <code>3</code>.</span>
<span class="comment-copy">@Bhavani is the 2nd line ordered by when the element is first seen in <code>words</code> or based on frequency? If it's frequency then you can call <code>.most_common()</code> otherwise you could create a custom OrderedCounter</span>
<span class="comment-copy">@Bhavani Put another way would the 2nd line output for input <code>['a', 'b', 'b']</code> be <code>1 2</code> or <code>2 1</code>?</span>
<span class="comment-copy">that was repeated element so it gives 2 but i dont need it's duplicate</span>
