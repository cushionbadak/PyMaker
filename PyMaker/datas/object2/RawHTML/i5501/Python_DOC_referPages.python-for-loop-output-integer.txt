<div class="post-text" itemprop="text">
<p>This for loop should take the entered input and convert the character into an integer.</p>
<pre><code>input = a
output = []
for char in input:
     num = ord(char) - 96
     output.append(num)
</code></pre>
<p>The problem is that at the end the output is equal to "[1]".</p>
<pre><code>&gt;&gt;&gt;print(output)
&gt;&gt;&gt;[1]
</code></pre>
<p>I need it to be an int to multiply it.</p>
</div>
<div class="post-text" itemprop="text">
<p>The <strong>ord()</strong> method returns an integer representing Unicode code point for the given Unicode character.</p>
<p>In your case, you have your input as 'a', which is represented by the number: 97</p>
<pre><code>&gt;&gt;&gt; print ord('a')
97
</code></pre>
<p>Hence, when you subtract 96 from 97, you get 1.</p>
<p>It already is an int ....</p>
<pre><code>&gt;&gt;&gt; if type(ord('a')) is int:
...     print 'is int'
... else:
...     pass
...
is int
</code></pre>
<p>Lastly, if you want to multiply it, change your operator from ' <strong>-</strong> ' to ' * ' ...</p>
<pre><code>&gt;&gt;&gt; input = 'B'
&gt;&gt;&gt; print ord(input) 
66

&gt;&gt;&gt; output = []
&gt;&gt;&gt; for char in input:
...     num = ord(char) * 2
...     print num
132
</code></pre>
</div>
<div class="post-text" itemprop="text">
<pre><code>input = a
output = []
total = 0;
for char in input:
    num = int(char)
    total = (total*10)+num
</code></pre>
<p>My answer assumes that the input is a number but in string format and that you want to iterate over it. Because in case it is a small number you can do:</p>
<pre><code>int(input)
</code></pre>
<p>I would need more explanation to clarify my answer. Hope this helps.</p>
</div>
<span class="comment-copy">Then why append it to a list?</span>
<span class="comment-copy">Why is <code>output</code> a list to begin with?</span>
<span class="comment-copy">Is that first line supposed to be <code>input = "a"</code>? BTW, you should not use <code>input</code> as a variable name because that shadows the built-in <code>input</code> function.</span>
<span class="comment-copy">To <a href="https://docs.python.org/3/tutorial/introduction.html#lists" rel="nofollow noreferrer">retrieve an item from a list</a>, use its index <code>n = output[0]</code>.</span>
<span class="comment-copy">is your input a string containing just digits?</span>
<span class="comment-copy">I <i>think</i> the OP wants to do arithmetic on the result of <code>ord(char) - 96</code>.</span>
