<div class="post-text" itemprop="text">
<p>In Python3 i'm Facing the issue,(in if day=="monday" line) please help me to resolve it,the question is divided into two parts</p>
<pre><code>print("Enter the Day")
    day=input()
    print("Enter the age")
    age=input()
    if day=="Monday":
        if age&gt;10:
            print("Wow")
        else:
            print("Bad")
    elif day=="Tuesday":
        print("Average")
    elif day=="Wednesday":
        print=("Good")
    elif day=="Thursday":
        print("better")
    elif day=="Friday":
        print("Well")
    elif day=="Sunday":
        print("So Far So Good")
    else:
        print("Bad Luck")
</code></pre>
<p>And When I'm Replacing (&gt;) mark with (==),Then getting output "Bad"</p>
<pre><code>print("Enter the Day")
day=input()
print("Enter the age")
age=input()
if day=="Monday":
    if age==10:
        print("Wow")
    else:
        print("Bad")
elif day=="Tuesday":
    print("Average")
elif day=="Wednesday":
    print=("Good")
elif day=="Thursday":
    print("better")
elif day=="Friday":
    print("Well")
elif day=="Sunday":
    print("So Far So Good")
else:
    print("Bad Luck")
</code></pre>
<p>Please Help</p>
</div>
<div class="post-text" itemprop="text">
<p><code>input()</code> returns a string type. You will need to use the following instead.</p>
<pre><code>age = int(input())
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>You are using </p>
<pre><code>age=input()
</code></pre>
<p>so age is a string. You can't compare string to int as says the error message, you've got to convert it to an int:</p>
<pre><code>raw_age = input()
age = int(raw_age)
</code></pre>
</div>
<span class="comment-copy">I recommend you take some time to <a href="https://docs.python.org/" rel="nofollow noreferrer">read the excellent Python documentation</a>. For example <a href="https://docs.python.org/3/library/functions.html#input" rel="nofollow noreferrer">this reference about the <code>input</code> function</a>.</span>
<span class="comment-copy">Also please <a href="http://stackoverflow.com/tour">take the tour</a> and <a href="http://stackoverflow.com/help/how-to-ask">read about how to ask good questions</a>. Lastly please learn how to create a <a href="http://stackoverflow.com/help/mcve"><b>Minimal</b>, Complete, and Verifiable Example</a>.</span>
<span class="comment-copy">age = int(input) or age = int(input())?</span>
<span class="comment-copy">int(raw_age) actually, but thanks for noticing there was a mistake</span>
