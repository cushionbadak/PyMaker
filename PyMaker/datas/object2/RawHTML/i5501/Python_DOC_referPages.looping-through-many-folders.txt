<div class="post-text" itemprop="text">
<p>I have to loop through multiple folders and finally check if there are three folders in the final folders.
If the three folders are present in the folder then do nothing,else print the folderpath or name.Im finding it difficult to make it work,
Any suggestions are welcome,Thanks in advance </p>
<p>This is what i have at the moment,</p>
<pre><code>check_empty =['One','Two','Three']
path = '/mnt/sdc1/Username/'
folders = next(os.walk('/mnt/sdc1/Mahajan/'))[1]
for folder in folders:
    files_in_folders = os.listdir(path +folder)
    for files_in in files_in_folders:
        for files in os.listdir(path+folder+'/'+files_in):
            for items in check_empty:
               if files in check_empty:
                    print(folder +'Good')
               else:
                    print(folder+'NotGood')
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p><a href="https://docs.python.org/3/library/os.html#os.walk" rel="nofollow noreferrer">os.walk</a> already provide you ability to get all files in folder:</p>
<pre><code>check_empty = set(check_empty)  # set is faster for simple check in unique items
for root, dirs, files in os.walk('/mnt/sdc1/Mahajan/'):
    for file in files:
        filename = os.path.basename(file)
        if filename in check_empty:
            print(root + 'Good')
        else:
            print(root + 'NotGood')
</code></pre>
</div>
<span class="comment-copy">I may be wrong for this specific use-case, but I learned through experience and lectures that, usually, when you have more than 2 nested loops in python, you are doing something wrong.</span>
<span class="comment-copy">So how do i approach this if i were to start with a blank slate?</span>
<span class="comment-copy">You should consider using either <a href="https://docs.python.org/3/library/glob.html?highlight=glob#glob.glob" rel="nofollow noreferrer"><code>glob.glob</code></a> or <a href="https://docs.python.org/3/library/os.html#os.walk" rel="nofollow noreferrer"><code>os.walk</code></a>.</span>
<span class="comment-copy">Alright Thanks .</span>
<span class="comment-copy"><a href="https://stackoverflow.com/questions/973473/getting-a-list-of-all-subdirectories-in-the-current-directory" title="getting a list of all subdirectories in the current directory">stackoverflow.com/questions/973473/…</a></span>
<span class="comment-copy">let me try this</span>
<span class="comment-copy">Im getting module has no attribute basename!,any tips</span>
<span class="comment-copy">sorry, try to use <code>os.path.basename</code> instead</span>
