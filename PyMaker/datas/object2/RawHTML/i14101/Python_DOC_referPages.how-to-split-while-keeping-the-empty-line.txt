<div class="post-text" itemprop="text">
<p>For example</p>
<pre><code>'a b\n\n\nc'.split()
</code></pre>
<p>gives</p>
<pre><code>['a', 'b', 'c']
</code></pre>
<p>while I want</p>
<pre><code>['a','b','','','c']
</code></pre>
<p>Any ideas?</p>
</div>
<div class="post-text" itemprop="text">
<p>Use <code>re.split</code></p>
<pre><code>re.split(r'[ \t]', s)
</code></pre>
<p>This would do splitting on white spaces or tabs.</p>
</div>
<div class="post-text" itemprop="text">
<p>Use <a href="https://docs.python.org/3/library/stdtypes.html#str.splitlines" rel="nofollow"><code>str.splitlines</code></a>. Check <a href="https://docs.python.org/3/library/stdtypes.html#str.splitlines" rel="nofollow">the docs</a> and you'll see that it splits on other newline-like characters too. If you want it to <strong>only</strong> split on <code>\n</code> then use <a href="https://docs.python.org/3/library/stdtypes.html#str.split" rel="nofollow"><code>str.split("\n")</code></a></p>
<pre><code>&gt;&gt;&gt; "a\n\n\nb".splitlines()
['a', '', '', 'b']
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>Use - <code>.split('\n')</code></p>
<p>Example - </p>
<pre><code>&gt;&gt;&gt; 'a\n\n\nb'.split('\n')
['a', '', '', 'b']
</code></pre>
<p><code>split()</code> splits by all whitespaces, hence you do not get the lines inbwetween, instead you can send in <code>\n</code> as the parameter to split to tell it to split at each <code>\n</code> .</p>
</div>
<div class="post-text" itemprop="text">
<p>Use str.splitlines():</p>
<pre><code>&gt;&gt;&gt; 'a\n\n\nb'.splitlines()
['a', '', '', 'b']
</code></pre>
</div>
<span class="comment-copy">your question and expected output is totally different.</span>
<span class="comment-copy">@AvinashRaj Would it be better if they pluralized "line" to "lines" in the title?  Otherwise, the question and expected output are pretty much exactly matching.</span>
<span class="comment-copy">I think this is the solution to my question. Thank you. Though I think the '\t' should be '\n'.</span>
<span class="comment-copy">no, <code>\t</code> matches tab chars only.</span>
<span class="comment-copy">Thank you very much. I think I did not put it quite clear. I need to split on whitespace while keeping the empty lines encountered somehow.</span>
<span class="comment-copy">Thanks. Yeah, this works. Only that I still want to split by space while keep the empty lines.</span>
