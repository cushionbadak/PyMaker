<div class="post-text" itemprop="text">
<p>I want to make a program something like password program.
When words entered by the user are the same words that I give to the program, it should print "Done", otherwise "Wrong".</p>
<pre><code>x = raw_input("Enter Your name:")
y = raw_input("Enter Your L name:")
if x=Sajad and y=Ha:
    print "Done"
else:
    print "Wrong"
</code></pre>
<p>Python tells me "Invalid Syntax".
I gave a value for Sajad and Ha,
but still it does not work.
(I don't want to use any lists.)</p>
</div>
<div class="post-text" itemprop="text">
<p>Man , it's not a good question .</p>
<p>Go through the <a href="https://docs.python.org/2/tutorial/" rel="nofollow">Python 2.7 Tutorial</a> Or <a href="https://docs.python.org/3/tutorial/" rel="nofollow">Python 3.x Tutorial</a> seriously and write your program again .</p>
<p>If you still don't know how to do that .
Answer is <a href="https://gist.github.com/KIDJourney/fe425c0fd35386227bda" rel="nofollow">here</a></p>
</div>
<div class="post-text" itemprop="text">
<pre><code>x = raw_input("Enter Your name:")
y = raw_input("Enter Your L name:")
if x=='Sajad' and y=='Ha':
    print("Done")
else:
    print("Wrong")
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>All errors in line <code>if x=Sajad and y=Ha:</code></p>
<p>1) x=1 is assignment. x==1 is comparsion. Use <code>==</code> here. (read about difference between <code>is</code> and <code>==</code>)</p>
<p>2) <code>Sajad</code> is string, thats why you should use quotes</p>
<p>Final code is:</p>
<pre><code>x = raw_input("Enter Your name:")
y = raw_input("Enter Your L name:")
if x=="Sajad" and y=="Ha":
    print "Done"
else:
    print "Wrong"
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>You have to use Python comparison operators in an 'IF' statement. 
Change your code to this to get the output you desire.</p>
<pre><code>x = raw_input("Enter Your name:")
y = raw_input("Enter Your L name:")
if x==Sajad and y==Ha:
    print "Done"
else:
    print "Wrong"
</code></pre>
</div>
<span class="comment-copy">Use <b>==</b> for comparison</span>
<span class="comment-copy">And if you want to compare x/y with strings don't forget the enclosing <code>"</code> (<code>x=='Sajad' and y=='Ha'</code>), unless <code>Sajad</code> and <code>Ha</code> are intended to be variables.</span>
