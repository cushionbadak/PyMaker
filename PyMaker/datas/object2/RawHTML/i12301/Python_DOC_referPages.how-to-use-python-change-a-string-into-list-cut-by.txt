<div class="post-text" itemprop="text">
<div class="question-status question-originals-of-duplicate">
<p>This question already has an answer here:</p>
<ul>
<li>
<a dir="ltr" href="/questions/7844118/how-to-convert-comma-delimited-string-to-list-in-python">How to convert comma-delimited string to list in Python?</a>
<span class="question-originals-answer-count">
                    4 answers
                </span>
</li>
</ul>
</div>
<p>I have a string like this </p>
<pre><code>string_ = 'hello world, world, hello, stackoverflow, me, not, me'
</code></pre>
<p>How to change it into a list like this?</p>
<pre><code>['hello world', 'world', 'hello', 'stackoverflow', 'me', 'not', 'me']
</code></pre>
<p>I have try </p>
<pre><code>alist = list(string_)
</code></pre>
<p>but when I format <code>alist</code>
It shows </p>
<pre><code>['h', 'e', 'l', ... ]
</code></pre>
</div>
<div class="post-text" itemprop="text">
<p>This should do the trick:</p>
<pre><code>alist = string_.split(', ')
</code></pre>
</div>
<span class="comment-copy"><a href="https://docs.python.org/3/library/stdtypes.html#str.split" rel="nofollow noreferrer"><code>string_.split(', ')</code></a></span>
