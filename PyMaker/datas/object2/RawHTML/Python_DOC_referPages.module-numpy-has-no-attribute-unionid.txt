<div class="post-text" itemprop="text">
<p>upgraded/reinstalled numpy, still getting the same error as,</p>
<p>module 'numpy' has no attribute 'intersectId'
module 'numpy' has no attribute 'unionId'
module 'numpy' has no attribute 'setdiffId'</p>
</div>
<div class="post-text" itemprop="text">
<p>You are trying to use the NumPy intersect1d module,  not intersectId. Given that you correctly installed Numpy, you can import/use it like this: </p>
<p><code>import numpy as np
print(np.intersect1d([1, 3, 4, 3], [3, 1, 2, 1]))</code></p>
<p>Which will give you:</p>
<pre><code>array([1, 3])
</code></pre>
</div>
<span class="comment-copy">That's supposed to be a <code>1</code>. The digit before <code>2</code>. Not the capital version of <code>i</code>.</span>
<span class="comment-copy">@Separius: Don't edit the bug out of the error messages.</span>
<span class="comment-copy">user2357112 has already answered this, in future, it might be helpful to use the <code>dir()</code> function to debug these kinds of errors, like <code>dir(numpy)</code> shows <code>intersect1d</code> as a member of the class.</span>
