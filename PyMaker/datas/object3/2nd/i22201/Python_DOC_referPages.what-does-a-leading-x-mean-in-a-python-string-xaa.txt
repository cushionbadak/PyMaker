Natural Text
What is difference between  and ? What does the  part mean? And which chapter of the Python documentation covers this topic?
The leading  escape sequence means the next two characters are interpreted as hex digits for the character code, so  equals , i.e.,  -- a small raised lowercase  character.Escape sequences are documented in a short table here in the Python docs.
That's unicode character escaping. See "Unicode Constructors" on PEP 100


Answer URL
https://docs.python.org/3/reference/lexical_analysis.html#string-and-bytes-literals
