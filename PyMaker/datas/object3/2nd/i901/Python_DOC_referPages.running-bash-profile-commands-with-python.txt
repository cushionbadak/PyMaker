Natural Text
In my bash terminal, I can run the following command:and this works correctly.I am then trying to run the same command in a python script; where I have tried the following approach:which throws the error:I am confused what the issue could be, as I can run basic bash commands fine. Thank you for any help.
Environment variable expansion is not done by default using subprocess unless you pass the  argument to the  call. This will pass your arguments to a shell (e.g. ) which will handle the desired expansion and any other shell-like functionality.Here's the code change necessary:Note that it is worth reading through the security considerations for using this given in the python documentation.


Answer URL
https://docs.python.org/3/library/subprocess.html#security-considerations
