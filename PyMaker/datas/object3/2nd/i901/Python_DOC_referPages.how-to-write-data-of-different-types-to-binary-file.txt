Natural Text
From what I understand,  is an example of a format character which represents an unsigned integer and  is used to represent a floatBut when I try to write  to a binary file as bytes:OutputTypeError: an integer is requiredSo is it not possible to store floats and integers as bytes in the same list?
Don't pass in a . Pass in your values directly, as arguments:It is the  call that throws the exception you see, and you don't even need this type here: takes integers (and strings) and produces bytes as output:


Answer URL
https://docs.python.org/3/tutorial/inputoutput.html#methods-of-file-objects
