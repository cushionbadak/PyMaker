Natural Text
I find it really useful to have Python types such as , which declare the types they contain and allow them to be type checked too. However I can't find how I can create such a type. If, for example, I writehow can I express it as , , , etc?
Use  to declare type variable, and use  as base class.You can try:Then you will see:


Answer URL
https://docs.python.org/3/library/typing.html#user-defined-generic-types
