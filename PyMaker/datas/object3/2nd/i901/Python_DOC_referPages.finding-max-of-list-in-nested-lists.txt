Natural Text
i want to put all the max elements of each list in b to c.but i keep getting the max element of the whole list, while i want max of each list in nested lists which is [45,56]
You have a 2D list and are trying to return a list of the maxes for each element in that 2D list. Iterate over the 2D list and take the max for each element:Additionally you can also use :
You can use a list comprehension that takes the max for each sub-list :OutputThe above list comprehension is equivalent to the following for loop:
You can also convert your nested list to  and use  function. You won't have to worry about loops then.


Answer URL
https://docs.python.org/3/tutorial/datastructures.html#list-comprehensions
