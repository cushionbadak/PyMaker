Natural Text
I am on windows system.I created two utf-8 file  for python and  for perl respectively, the two files contains same line as belowand perl has  delimiter.My CMD is in  by default, and I run I gotHowever, when I run for the first time, it givesrunning it for the second time, I gotwhy??I continue testing, I run  to change cmd encoding to utf-8, and this time, both python and perl gives correct .Now I am completely confused, it seems that print in python and perl are quite different. It seems that perl alway print out utf8 bytes? and python print can detect CMD code page to print correct byte? Can somebody explain my test result?
perl is printing the literal bytes you have in your source file. It sees the string as "\xe4\xb8\xad" unless you explicitly declare that your source file is utf8 with .Once you do that, you would (if you enabled warnings as you should) get a  warning; perl requires you to specify the encoding to be used when outputing non-ASCII characters.  You can do that with  or with  or (for some filehandle you are opening) with the 3rd argument to .


Answer URL
https://docs.python.org/3/howto/unicode.html#the-string-type
