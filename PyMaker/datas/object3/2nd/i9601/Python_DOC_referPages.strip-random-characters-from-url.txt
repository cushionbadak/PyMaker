Natural Text
I have a list of urls as follows:Where Term might be whatever term we want: , , etc..My part of code (among the whole code) is the following:which results in:How can I strip the Term in the list of urls to result as:
You could use  to parse the URL and then the query part. Benefit of this approach is that it will work the same if order of query parts are changed or new parts are added:Output:In above  returns  object that contains URL components:Then  will return  of query parameters where values are lists:
If you just want the last part after the last , it's easy:
Continue with what you were doing and use Example:If the terms are random, then use  like this:Or this if the term isn't known before the program is run:Example:


Answer URL
https://docs.python.org/3/library/urllib.parse.html#urllib.parse.urlparse
https://docs.python.org/3/library/urllib.parse.html#urllib.parse.urlparse
https://docs.python.org/3/library/urllib.parse.html#urllib.parse.parse_qs
