Natural Text
I'm a noob and I need to use the sessionid to post other commands like search.do, Im using Python 3.5 but Im not sure the best way to get and post it.here is how I posted the request. printing gets this result.I need to use the sessionid in subsequent request.  Whats the best way to go about this. 
In fact the response in composed of lines (in bytes) one of which contains the session id. You could simply read and parse what you get:
One idea is to split by  and extract the last item:Another, is to use a regular expression:


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.split
