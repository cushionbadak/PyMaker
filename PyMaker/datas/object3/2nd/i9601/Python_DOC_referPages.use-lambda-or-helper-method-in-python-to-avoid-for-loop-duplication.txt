Natural Text
I have the following simple class and am wondering if there is a simple way to use a , , or , etc... to avoid the duplicated for loop that appears in each method in  and ?Duplicated for loop is this section of each method:
You could use  within helper method to generate the names:Then you could change your class to utilize it:
You could perhaps use something like:
ohhh shiny :-}


Answer URL
https://docs.python.org/3/library/itertools.html#itertools.product
