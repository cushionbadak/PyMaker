Natural Text
This question already has an answer here:Why does (1 in [1,0] == True) evaluate to False? [duplicate]                    1 answer                Why does the expression 0 < 0 == 0 return False in Python?                    9 answers                Why does the statement  return  in Python?Is the operator priority in Python ambiguous?
Because, per the documentation on operator precedence:Note that comparisons, membership tests, and identity tests, all have  the same precedence and have a left-to-right chaining feature as  described in the Comparisons section.The Comparisons section shows an example of the chaining:Comparisons can be chained arbitrarily, e.g.,  is equivalent  to So:is interpreted as:If you override this chaining by adding parentheses, you get the expected behaviour:Note that, rather than comparing truthiness by equality to  or , you should just use e.g.  and .


Answer URL
https://docs.python.org/3/reference/expressions.html#operator-precedence
https://docs.python.org/3/reference/expressions.html#comparisons
