Natural Text
I can use  in Python to iterate through a Comma Sep text file but I wish to add another level of complexity.I need to return the value at  of a row where a string is found.For example, I have a text file containing:now I use:and output would be:now though I wish to get the  value that relates to these entries, i.e.MaxTemp needs to find that the entry 19.92 in in the row starting with  of 01:00 & be displayed as such, using variable  as  value:Thanks for lookingUPDATEthanks goes to Henry Heath for the help & pointers.Needed 2 time variables as the  &  were returning the exact same time using  here is the correct working code:
This might be easier if you use the csv library
Following up on @henry-heath ’s suggestion:Iterative version:


Answer URL
https://docs.python.org/3/library/csv.html
