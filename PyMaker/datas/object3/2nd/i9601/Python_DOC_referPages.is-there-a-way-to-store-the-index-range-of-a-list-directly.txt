Natural Text
If we do thenI wonder if there a way can do something like so that prints 
You can use :This is equivalent to .You can also pass a step parameter to slice, for instance:Which is the same as .


Answer URL
https://docs.python.org/3/library/functions.html#slice
