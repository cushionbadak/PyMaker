Natural Text
I'm working on a little pet project in python, and I want to be able to write external modules and dynamically import them. So far, I've got something like this:I'd use , but that just gives a Syntax Error.
You can use  like this:Example:Let's say we have a module  that contains the following function:The following is the result:
You should use the  function. Something like this will help,A still better choice would be  which is a wrapper around . It's got a similar syntax to . Of course, you need to .


Answer URL
https://docs.python.org/3/library/importlib.html#importlib.import_module
