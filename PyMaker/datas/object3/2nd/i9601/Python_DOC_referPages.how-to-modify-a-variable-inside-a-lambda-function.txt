Natural Text
I am trying to do something like this:and expecting the output to be . Instead I get the following error: 
Only expressions are allowed in the body of a  function;  is an augmented assignment statement, when compiled, this will lead to a  as the grammar doesn't allow it. You can either change it to simply return the addition:and then proceed to set the result of calling it to  appropriately:You could of course resort to using the function that is used for that operation. Though you could directly do , this is clunky and using dunders like this isn't the best practice. Instead, you should use the appropriate operation from the  module. The  function from  allows you to bypass this "restriction" if you truly need to. Function calls are expressions


Answer URL
https://docs.python.org/3/reference/expressions.html
https://docs.python.org/3/reference/simple_stmts.html#augmented-assignment-statements
https://docs.python.org/3/library/operator.html#operator.iadd
