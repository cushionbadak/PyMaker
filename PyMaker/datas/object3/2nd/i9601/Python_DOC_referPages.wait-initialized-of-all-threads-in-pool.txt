Natural Text
I have main thread where I created ThreadPool. Before call function, makes initialization each thread in pool. How wait when all threads will be initialized?Example:It is necessary not to cause map_async if initialization of a thread exception occurred
I would implement a barrier in your -function and skip the . But that works only beginning with Python 3.2.Barriers have a predefined number of parties that have to call the -function of the barrier. When the barrier registers the correct number of calls it "drops" and the calling parties return to there their work simultaneously.Something like that:


Answer URL
https://docs.python.org/3/library/threading.html#barrier-objects
