Natural Text
I have this:and I want to split it to create a dictionary like this:How can I do it with little code?
By splitting twice, once on  and then on  for every element resulting from the first split:Now,  looks like:
In your case, the more convenient way would be using of  module:The output:Note, that  function would be more useful if there multiple keys with same value in a query string. Here is an example:The output:https://docs.python.org/3/library/urllib.parse.html#urllib.parse.parse_qs
This is what I came up with: returns what you want. This code works by splitting the query up into the different parts, and then for each of the new parts, it splits it by the . It then updates the  with each new value. Hope this helps!


Answer URL
https://docs.python.org/3/library/urllib.parse.html#urllib.parse.parse_qs
