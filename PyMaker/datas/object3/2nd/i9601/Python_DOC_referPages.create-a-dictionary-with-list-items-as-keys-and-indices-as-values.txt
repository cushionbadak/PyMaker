Natural Text
The output I got is But I want
You can use a dictionary:
It seems like  is a perfect fit for this case (see also the answer of @mhawke) :Then convert this to a plain dictionary again:I recently created a package containing a function that could be used as alternative :
Use a  of lists:
This can also be done using a fairly simple dictionary comprehension:This works by making keys using each unique element in the list (which  does), and getting the index of each of the unique elements(which the list comprehension does).


Answer URL
https://docs.python.org/3/library/collections.html?highlight=defaultdict#collections.defaultdict
