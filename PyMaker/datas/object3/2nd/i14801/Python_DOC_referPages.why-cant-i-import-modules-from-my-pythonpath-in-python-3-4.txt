Natural Text
I have a package installed in . It looks like:The source for  is simply:And for :First, I make sure I'm in my home directory, and set my PYTHONPATH:Then I run :Okay, that's weird. But if I change  to be blank:What on earth is going on?
In Python 3 all imports are absolute.  You can't do  unless  itself is a top-level module/package available on .  If you want to import  from inside , use an explicit relative import: 


Answer URL
https://docs.python.org/3/reference/simple_stmts.html#import
https://docs.python.org/3/reference/import.html#importsystem
