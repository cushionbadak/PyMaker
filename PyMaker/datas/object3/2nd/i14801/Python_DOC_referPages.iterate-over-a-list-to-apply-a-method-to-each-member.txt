Natural Text
I have a list of strings that I want to apply a method (). I know this can be done by a  loop but knowing the mentality of python I assume there is a better way, like the  functionBelow is the thing I want written using  loop
You can use a simple list comprehension, like thisIf you want to use , you can pass  function to it. But, Python 3.x's  returns an iterable map object.So, you need to explicitly convert that to a list, with the  function, like this
As an alternative to anwser by @thefourtheye you can use :
Create an empty listIterate over each item, split, add to new list.


Answer URL
https://docs.python.org/3/tutorial/datastructures.html#list-comprehensions
https://docs.python.org/3/library/functions.html#map
https://docs.python.org/3/library/stdtypes.html#str.split
https://docs.python.org/3/library/functions.html#func-list
