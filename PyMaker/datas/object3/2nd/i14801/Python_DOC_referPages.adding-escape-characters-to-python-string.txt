Natural Text
I have a file name that I want to pass to a program or a bash script. For example if it's , I have to change it to   to pass it to os.system like . Is there a function to do it the backslashes automatically?
You should use the  module to call shell scripts from Python. Then you don't have to worry about escaping things yourself. will escape everything correctly for you. If you need to read the output of the shell script, use  instead.
You can simply pass as is and use subprocess,  is depreciated.To call a script use , if you want to pipe you can use , there are lots of example in the docs linked above including replacing-os-system.
I can offer several incomplete suggestions that could be helpful to you.Use  -- double quotes escape single onesUsing spaces in a UNIX file system generates only headaches.  You could pass the filename inside of double-quotes.  If you are using filenames with backslashes in a Windows system, use raw string


Answer URL
https://docs.python.org/3/library/shlex.html#shlex.quote
