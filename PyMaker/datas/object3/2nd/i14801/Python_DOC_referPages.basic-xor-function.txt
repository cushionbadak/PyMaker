Natural Text
I am in need of some guidance as to how to program an XOR Python function XOR(x,y) that returns the XOR(x,y).
XOR is defined as a bitwise operation and is a standard operator...That being said there is nothing stopping you from defining it.


Answer URL
https://docs.python.org/3/reference/compound_stmts.html#function-definitions
