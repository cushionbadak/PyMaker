Natural Text
I've been having trouble getting functions to work that rely on the output of another function. When I run the following code (this is a very simplified version)......I get I need to keep these in separate functions because in my actual code they are very complicated. How can I get random() to recognise the random number generated by event()?
A simple way is to use  and 
While returning a variable is a good way to achieve your goal, sometimes you do need to have global variables. In that case, put  in the definition of  before you assign to it. e.g.:


Answer URL
https://docs.python.org/3/reference/simple_stmts.html#global
