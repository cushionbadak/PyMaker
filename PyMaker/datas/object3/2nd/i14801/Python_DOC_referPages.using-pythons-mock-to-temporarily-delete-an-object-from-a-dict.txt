Natural Text
I am writing a test for some code that checks for a value in  (I know this isn't optimal, but I have to go with it).  I would like to remove an entry from os.environ for the duration of the test.  I am not sure if mock supports this.  I know  can be used to modify an item, but I want the key/value pair removed.  I would like something along these lines:Is there a way to perform such a feat?
 doesn't quite work like your sample desired code.  is a function which requires an argument. You probably want to use it like this:
For deleting the item, you can simply use:And then restore it with:


Answer URL
https://docs.python.org/3/library/unittest.mock.html#patch-dict
