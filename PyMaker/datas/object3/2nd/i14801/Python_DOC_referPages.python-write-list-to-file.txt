Natural Text
from this post, I've been using the following code to write a list/array to a file:where  is: In the file, I get the output:when I need to to say What would be the best way to do this?
Use:This way each part of item will be printed with an ", " as seperator.But if you want to make the code even shorter, you can also join each item with a newline:

I would use the csv module simply calling writerows on your list of lists:Output:
From what I can see you have a list with lists as elements. This is why you get the result that you get. Try the following code (see the minor change on the third line) and you will get the wanted result.
You are currently printing string representations of the lists to the file.  Since you are only interested in the lists' items, you can use  and argument unpacking to extract them:


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.format
