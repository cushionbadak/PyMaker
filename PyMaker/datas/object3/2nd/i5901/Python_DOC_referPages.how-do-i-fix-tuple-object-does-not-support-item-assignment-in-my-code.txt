Natural Text

The problem is that  returns a  which is immutable and you're trying to modify it here:It can be fixed by creating a  instead:Warning: eval is evil!
The core problem is that the expression you enter into eval, such as "1, 2, 3" are tuples, which are immutable, i.e., unable to be changed.Additionally, using eval like this is dangerous, and can lead to some confusing errors. (What happens if you put in an empty list?) Python has plenty of nice string manipulation functions, so it is also completely unnecessary. Try,


Answer URL
https://docs.python.org/3/tutorial/datastructures.html#tuples-and-sequences
