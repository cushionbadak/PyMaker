Natural Text
My code is:I want to replace  with , but I don't know how to extract the  returned:
It's not clear which HTTP library you're using in , but for a generic one (where  returns a string), you can parse the JSON response manually with :If you're using , you can read the JSON response directly with  method on the response:


Answer URL
https://docs.python.org/3/library/json.html#json.loads
