Natural Text
I cannot install a filter on a logging handler using  syntax.  is simply ignored, nothing happens.I want to filter out error messages so that they do not appear twice in the log output. So I wrote  class and overrode .My configuration:What am I doing wrong here? Why is my filter ignored?
Actually, 's answer is not correct in general. The following script:When run, produces the following output:which shows that you can configure filters using .
You can specify a class name, but it is done with the strangely named  key, and it has to include the module name. E.g.:See 16.7.2.4. User-defined objects in the documentation.


Answer URL
https://docs.python.org/3/library/logging.config.html#user-defined-objects
https://docs.python.org/3/library/logging.config.html#user-defined-objects
