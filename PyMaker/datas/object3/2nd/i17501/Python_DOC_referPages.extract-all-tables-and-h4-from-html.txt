Natural Text
I have a html file from which I want to extract all tables and h4 elements. That is I want to take only tables and h4 from the file and use it somewhere else. I am using Notepad++ and looking for some pythonscript to do so.Thanks
I suggest using the module BeautifulSoup.You could accomplish what you want by doing:
Since BeautifulSoup already has been mentioned i just want to hint at the tools of the standard library. You may use the builtin html parser or  an regular expression (see tutorial).Sometimes these tools are sufficient. It depends on the task.BTW: Notepad++ supports regular expressions. or  allows you to select those blocks.
The established go-to library for parsing and editing HTML with Python is called .


Answer URL
