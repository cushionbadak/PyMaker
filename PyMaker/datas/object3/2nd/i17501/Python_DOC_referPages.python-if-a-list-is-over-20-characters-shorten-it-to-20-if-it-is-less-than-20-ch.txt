Natural Text
in a python program, I haveplease help with having the stuff commented do what it says
The simplest way is to use slicing and  function, like thisWhen  is , the output isNote: If you really meant, appending zeros, you can use  function, like thisThe advantage of using  and  is that, we can use arbitrary fill character.
Use :or :About format specification used:
One simple can be (read comments): output: 
If you want to work with it as a list, as stated, then list comprehension will get you there:output:This also covers the >= 20 characters cases as well.


Answer URL
