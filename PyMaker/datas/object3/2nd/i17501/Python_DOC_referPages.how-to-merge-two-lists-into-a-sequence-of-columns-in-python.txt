Natural Text
This question already has an answer here:How to merge lists into a list of tuples?                    8 answers                Suppose I have two lists:How can I merge it using python so that output list will be:  The program that I have tried is:Output is:I don't want repeated entries.
In Python 2.x, you can just use :However, in Python 3.x,  returns a zip object (which is an iterator) instead of a list.  This means that you will have to explicitly convert the results into a list by putting them in :
Use zip:If you do not want repeated items, and you do not care about order, use a set:If you want to uniquify in order:


Answer URL
