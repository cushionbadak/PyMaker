Natural Text
Python 2.7 documentation (and Python 3 documentation as well) contain the following line about the  function:Do not use os.linesep as a line terminator when writing files opened  in text mode (the default);Why is that? And how is it different from using it on binary mode?
When you open a file in text mode any  that you write to the file is converted to the appropriate line ending for the platform you are using.So, for example if you were on Windows where  is , when you write that to a file the  will get automatically converted to  and you will end up with  written to your file.For example:


Answer URL
