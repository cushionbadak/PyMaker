Natural Text
I am new to python and am trying to create a copy of a list without a certain element. This is how I am doing it at the moment:I was wondering if there is a better more eloquent way to do this, instead of copying the list and then deleting the element in two lines?

Using list comprehension:
Try this with 'slicing':
Consider using the built-in function 's arguments are .  It applies the function you give it to every element of the iterable and returns a list of the elements that the function returns True on.


Answer URL
