Natural Text
Ive to run awk from the python. When I run the script from the terminal, gives the desired output but showing error when executing from inside the python.gives the error:awk: line 1: runaway string constant " ...when I surfed from the web, I found that awk can not be used with os module and there are not much contents. I am confused how to proceed ahead.
The  in your  string is being interpreted by Python as a literal newline character, rather than being passed through to  as the two characters  and . If you use a raw string instead, by preceding the opening triple-quotes with an :... then Python won't treat  as meaning "newline", and  will see the string you intended.


Answer URL
https://docs.python.org/3/reference/lexical_analysis.html#index-16
