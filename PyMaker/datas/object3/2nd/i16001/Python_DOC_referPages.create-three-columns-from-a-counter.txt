Natural Text
Right now, my Counter looks like this:I am trying to pass these values into a Pandas DF with Three columns, Bigrams, Score, and FrequencySo for example, using the first value above, my expected DF looks likeI am trying to use this bit of code to do so, but it keeps giving me an errorHowever, this keeps saying: Value Error: need more than 2 values to unpack.  What am I doing wrong?
You are using both keys and values.  Iterating a dictionary (mapping) yields keys only. Use  to make it yields both.And, use tuple unpacking correctly. It should match items yielded.


Answer URL
https://docs.python.org/3/library/stdtypes.html#dict.items
