Natural Text
This question already has an answer here:match trailing slash with Python regex                    1 answer                I can't match the question mark character although I escaped it.I tried escaping with multiple backslashes and also using . What am I missing?Code:Output:
 only matches a pattern at the begining of string; as in the docs:If zero or more characters at the beginning of string match the regular expression pattern, return a corresponding match object.so, either:or 


Answer URL
https://docs.python.org/3/library/re.html#re.match
https://docs.python.org/3/library/re.html#re.search
