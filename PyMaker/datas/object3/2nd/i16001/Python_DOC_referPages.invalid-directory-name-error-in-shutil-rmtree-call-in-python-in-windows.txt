Natural Text
My objective: In the current working directory, if a folder called  exists then delete it and create a new one, else simply create the folder .Then copy a user entered  from the current working directory to the newly created  folder.Problem: I'm getting  stating 
You can avoid a lot of potential issues by using  to create your paths.  What the function does is automatically insert the OS's path delimiter between the args.  Since the delimeter is  on Windows, you can make your life much easier by using it instead of manual string concatenation.  
Problem with backslash escaping. Try doing and the same for temp_xml_path before passing it to copyfile.
temp_path is with only one \, but it is better to use os.path.join


Answer URL
https://docs.python.org/3/library/os.path.html#os.path.join
