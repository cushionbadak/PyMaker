Natural Text
This should be easy, but as ever Python's wildly overcomplicated datetime mess is making simple things complicated...So I've got a time string in HH:MM format (eg. '09:30'), which I'd like to turn into a datetime with today's date.  Unfortunately the default date is Jan 1 1900:datetime.combine looks like it's meant exactly for this, but I'll be darned if I can figure out how to get the time parsed so it'll accept it:This monstrosity works......but there must be a better way to do that...!?
The function signature says:so pass a  object as the first argument, and a  object as the second argument:
https://dateutil.readthedocs.io/en/stable/parser.html


Answer URL
https://docs.python.org/3/library/datetime.html#datetime.datetime.combine
