Natural Text
If I have two dictionaries (in my program there are more than two):and want to get the common keys between them:what should I do?I understand  but do not know how to turn the dictionary keys into a set.
Use dictionary views with  and set operations:If you are using Python 3 instead, the  method returns a dictionary view already:Either operation returns a  object.Demo:


Answer URL
https://docs.python.org/3/library/stdtypes.html#dict.keys
