Natural Text
This question already has an answer here:How do I get the number of elements in a list in Python?                    6 answers                I am trying to find a simple way of getting a count of the number of elements in a list:I want to know there are 3 elements in this list.

just do This also works for , ,  objects. 
 should do it. works with all the collections, and strings too!
it will count the element in the list, tuple and string and dictionary,eg.To learn Python you can use byte of python , it is best ebook for python beginners.
To find count of unique elements of list use the combination of  and .
you can get size of a list by following two ways.
Len won't yield the total number of objects in a nested list (including multidimensional lists). If you have , use .  Otherwise use list comprehensions within recursion.


Answer URL
https://docs.python.org/3/library/stdtypes.html#common-sequence-operations
