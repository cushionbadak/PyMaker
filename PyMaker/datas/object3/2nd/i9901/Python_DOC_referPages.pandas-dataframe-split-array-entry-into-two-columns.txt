Natural Text
I have a data frame that looks like this:I'd like to split the 'helpful' column into two different columns with names 'helpful_numerator' and 'helpful denominator and I can't figure it out.Any help would be much appreciated!
You can use  to unzip  into separate columns:EditAs mentioned by @MaxU in the comments, if you want to drop the  column from your DataFrame, use  when selecting the column in :TimingsUsing the following setup to create a larger sample DataFrame and functions to time against:I get the following timings:
If  is a column of lists, you can use  to access the element in the list:
yet another solution:
Assuming that the column contains a list, you can use .apply


Answer URL
https://docs.python.org/3/library/functions.html#zip
