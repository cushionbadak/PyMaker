Natural Text
This question already has an answer here:Can I store slicers in a variable? (Pandas/Python) [duplicate]                    1 answer                I want to write code like this:but this does not work.Is there any way I can store all parts of the  syntax in a variable?
You want a  object: models the start, stop and stride values you can specify in the   subscription syntax, as an object.From the documentation:Return a slice object representing the set of indices specified by . The start and step arguments default to . Slice objects have read-only data attributes ,  and  which merely return the argument values (or their default).Under the covers, Python actually translates subscriptions to a  object when calling custom  methods:A viable alternative would be to store start and stop as separate values:
You can instead create a  object:Be careful not to use  as name to avoid shadowing the builtin  function.From the docs:Return a slice object representing the set of indices specified by  


Answer URL
https://docs.python.org/3/library/functions.html#slice
https://docs.python.org/3/glossary.html#term-slice
