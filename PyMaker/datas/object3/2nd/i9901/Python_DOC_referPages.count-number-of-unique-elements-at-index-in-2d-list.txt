Natural Text
Given list:I want to produce a 2D list of unique elements at index [1] and their count. Executing on list a would produce:I've gotten as far as writing this code:...however, it produces the list:I'd love to solve it using a one liner if possible :)
You could get a set from your code which would provide just unique entries
The problem with your code is that you are looping over all sub-lists and preserve the count of the second item within the whole list which certainly would produce the duplicate result for same items. Instead, you can use  function to get the second column and then use  to count the number of items within second column:In python 3.X:
Using  combining with generator expression, you can get counts of items:To get result as a list of sequences:


Answer URL
https://docs.python.org/3/library/collections.html#collections.Counter
https://docs.python.org/3/tutorial/classes.html#generator-expressions
