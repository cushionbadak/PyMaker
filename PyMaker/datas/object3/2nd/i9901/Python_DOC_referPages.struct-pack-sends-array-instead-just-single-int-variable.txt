Natural Text
I haveAnd when I receive and unpack is set for example to  when sending so it prints thisInstead of It creates a string I guess, but why? I need that  as 4 byte integer number...
 always returns a tuple. From the documentation:The result is a tuple even if it contains exactly one item.You appear to try to use assignment to extract the one value, but you only used parentheses: is just a parenthesised name, not a tuple, so no extraction is taking place;  is bound to the whole tuple that  returns. You need a comma to get that effect:You can omit the parentheses altogether:or just use indexing to extract the one result:


Answer URL
https://docs.python.org/3/library/struct.html#struct.unpack
