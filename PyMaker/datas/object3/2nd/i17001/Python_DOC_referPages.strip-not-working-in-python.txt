Natural Text
I don't really understand the .strip function.Say I have a stringand I want to strip the comma.Why doesn'twork?
 removes characters from the start and end of a string only. From the  documentation:Return a copy of the string with the leading and trailing characters removed. Emphasis mine.Use  to remove text from everywhere in the string:For a set of characters use a regular expression:Demo:
 removes characters from the beginning or end of the string, not in the middle.If you want remove character from everywhere, you should use  instead:
You can also use the translate method of the string class.  If you pass in None for the table parameter, then only the character deletion step is executed.


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.strip
https://docs.python.org/3/library/stdtypes.html#str.replace
