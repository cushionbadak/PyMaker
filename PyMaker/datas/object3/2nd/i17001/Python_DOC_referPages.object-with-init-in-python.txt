Natural Text
I have written some python code:When I create an instance of  with , I got a TypeError. I don't know why I am getting this error. Is it because of ?If yes, please help me in explaining why we use  and what the purpose of that is and help me solve this code.
Your code contains several errors:output:
Some pointers:Actually, you can can name the  instance parameter whatever you like in Python (even ), but it makes the code harder to read for other people, so just use .You don't have to use  here. If you remove that line and implement the changes above, your code will work just fine.
When you do  you are actually passing three arguments: the instance, the  and the . However your initialiser is defined to take exactly two arguments so you get a . is the argument implicitly passed when referring to an instance of an object. For your  function, I would just do:So we can assign the arguments passed as attributes to the current instance, conventionally called .It makes no sense here to call  at all, just remove that line.Apart from that, everything works fine (except use  instead of ).Testing:


Answer URL
https://docs.python.org/3/reference/datamodel.html#object.__init__
