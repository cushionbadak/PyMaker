Natural Text
I'm finding max value and min value of a list by using  and  in Python. However, I wonder how to manage empty lists. For example if the list is an empty list , the program raises  but I would like to know how to make the program just print  instead of just crashing. How to manage those errors?
Catch and handle the exception. is raised with an empty sequence.  is raised when the sequence contains unorderable types.
In Python 3.4, a  keyword argument has been added to the  and  functions. This allows a value of your choosing to be returned if the functions are used on an empty list (or another iterable object). For example: If the  keyword is not given, a  is raised instead.
Specifying a default in earlier versions of Python:


Answer URL
https://docs.python.org/3/glossary.html#term-eafp
