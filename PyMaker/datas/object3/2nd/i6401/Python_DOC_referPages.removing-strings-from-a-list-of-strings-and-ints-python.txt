Natural Text
I have a list such as I want a function so that I can remove the strings from my list and make it only have integers? There are always alternating strings and integers in the list if that helps.
You can simply loop through your list and only keep the elements with type integer:Which outputsIf you want to wrap it to a function you can use this:new_list = remove_all_but_int(original_list)Which gives you the same result. 
Output:
1.2.
Because you simply want to remove every second item you should do this:The modulo  gets the remainder after dividing.
Just one more option - use of filter function:
One line solution :if you are having trouble with understanding above syntax here is detailed solution :output:


Answer URL
https://docs.python.org/3/library/functions.html#filter
