Natural Text
I would like to select data between two dates. The date's format is in the code I copied. I have seen a lot of codes for this, but couldn't find anything with this format. Thanks
Assuming you are using pandas, and that  is a DataFrame, the problem here is that pandas does not automatically recognized strings like  as date-strings:But it can if you give it a little help:printsAbove, I'm also assuming that  contains values of dtype  (parsed dates, as opposed to date strings). If  contains date-strings, then you'll need to parse them too:Pandas uses  to parse date-strings. You can test if a date-string is automatically parsable by pandas by passing it to :whereas, for example,  is automatically parsable:


Answer URL
https://docs.python.org/3/library/datetime.html#strftime-and-strptime-behavior
