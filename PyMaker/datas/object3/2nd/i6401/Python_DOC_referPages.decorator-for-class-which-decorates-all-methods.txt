Natural Text
I want to write class decorator which for all non-magic methods, to decorate these methods. The idea is that to all methods of class print its name after call; I do not want decor all methods but only class. Decorator  works. I have problem with  decorator. There are no errors and no output.
You should rebind the method to the class object using  passing the method name ; assigning to the local name  like you've done, does nothing. More so, you're currently passing , a string, to . Instead, you should pass the function object itself after retrieving via :PS:  is never used for decorating, so it's not a decorator.


Answer URL
https://docs.python.org/3/library/functions.html
