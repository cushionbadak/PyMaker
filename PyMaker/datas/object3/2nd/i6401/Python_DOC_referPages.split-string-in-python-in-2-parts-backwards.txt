Natural Text
I am trying to split my string   in two parts. The output should beThere is no info about the length of the words given as input.One way do this is to split it in three and add the first two. Is there any other way?  
You can use , which splits from the right, with a  of :As an aside, don't use the name . It masks the built-in class  and will lead to errors down the line.
You can use :Output:


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.rsplit
