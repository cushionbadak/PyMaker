Natural Text
I am struggling to successfully import my project to the test-suite in my project, as well as being able to run the program from the command-line. I've been able to run my test-suite for some time, under the impression that if the tests work, so does the command-line stuff--evidently this isn't the case. I do not yet intend on using my program as a library. The  acts is the entry-point for the program.I have a project with the following structure (the same directory hierarchy as requests):In the file  I have a path modification adding  to the , so I can successfully run the tests on my code. Here is the contents of that fileI've tried imaginable import combination I can think of. Far too many to list! I have also perused the Python reference import system page, and this tutorial.How should I import my dependencies?
Turns out this was the correct layout, I mistook the error for something else. Although for future reference, relative imports in Python 3 must be explicit: when in the  package directory you can't say , you must instead import it as . This was defined in PEP 328 also see this SO post on the topic. Run your package with  not  as the latter won't give the interpreter context of the current path.


Answer URL
https://docs.python.org/3/reference/import.html
