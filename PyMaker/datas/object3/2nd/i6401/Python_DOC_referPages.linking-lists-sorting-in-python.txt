Natural Text
Here's an example of what I want (Python 3.6):Linked with  in the same orderExpected output when  is changed in order of  or in order smallest to largest.  would change too.Desired output:
You're looking for the argsort functionality. In python, one way to achieve this would be using  + :With , you'd be able to do this with:
Here is another approach of doing it in pure Python:You zip the two lists, and then sort by first(default) element.If you wanted to sort by 2nd element you would do:


Answer URL
https://docs.python.org/3/library/functions.html
