Natural Text
Although pylint raises a warning on , I see from the enum doc that one could programmatically create an Enum like the followingBut I'd also like to add methods:I think this should involve overloading metaclass  to augment bases with the names dict, but apparently bases is not how the Enum attributes are created. Anyone have any tips?
It is quite simple using the 1:The  tells  what to, um, ignore and, in fact, anything in  is removed from the final  class.Due to a bug in Python's Enum this doesn't yet work unless you use .1 Disclosure:  I am the author of the Python stdlib , the  backport, and the Advanced Enumeration ()  library.
What if you create a new subclass of  instead?Or, depending on the use case, you could create another class that wraps the enum.Either should work as follows:Hope this helps!
You can also use  to avoid needing the  package:


Answer URL
https://docs.python.org/3/library/enum.html
https://docs.python.org/3/library/enum.html#restricted-subclassing-of-enumerations
