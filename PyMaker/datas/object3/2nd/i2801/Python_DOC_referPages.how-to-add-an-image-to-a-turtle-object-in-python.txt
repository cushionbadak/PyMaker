Natural Text
then i get this error: 'Turtle' object has no attribute 'register_shape'
 is not a method on  objects, it's a global function.So, instead of this:… do this:Also, notice that you don't have any use for that  turtle. Your app only wants to display a single turtle, , so don't create any others.Finally, even after you fix this:At least according to the docs, only GIF images are supported, but you're trying to use a PNG image. The docs may be wrong about that, but there's a good chance they're right, and this is going to fail.If so, the only way to fix it is to use some other program (it could be one you write yourself in 4 lines of Pillow code, or it could be something like MSPaint or Preview, or a command-line tool like ImageMagick convert) to make a GIF image out of your PNG image.


Answer URL
https://docs.python.org/3/library/turtle.html#turtle.register_shape
