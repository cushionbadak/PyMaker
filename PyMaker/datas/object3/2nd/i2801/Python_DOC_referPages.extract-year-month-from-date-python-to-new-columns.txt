Natural Text
I have a column with dates in object typeThats the resultI would like to create new columns for year, month, day, hour.So I get something like that:How can I manage it?
You can try using  and then to split on  as given inside  function. The code: If you want all the columns in a single dataframe use  along .
Maybe:
One flexible approach is to use  with . Such an approach enables you to specify an arbitrary list of properties, which are then extracted via the  accessor.Setup
 will be helpful to get required resultofficial Document: https://pandas.pydata.org/pandas-docs/stable/generated/pandas.DatetimeIndex.htmloutput:


Answer URL
https://docs.python.org/3/library/operator.html#operator.attrgetter
