Natural Text
We can use  package to get the data from mTurk account (or do anything else in AWS). For example:Is there a way to use boto3 command as a string? Something like: is totally just for illustrative purposes here of course.
You could use the built-in :
Use the  built-in function:


Answer URL
https://docs.python.org/3/library/functions.html#getattr
