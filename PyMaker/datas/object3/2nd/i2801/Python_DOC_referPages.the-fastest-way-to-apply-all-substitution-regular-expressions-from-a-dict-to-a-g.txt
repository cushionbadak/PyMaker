Natural Text
I have a dict in the form (regexp, substitution_string) and I want to apply all regular expressions from this dict to the text (the order of operations doesn't matter). Now I have something along these lines:Is there any faster/shorter way to do the same thing? I also know aboutbut that doesn't appear to be any faster.
You can merge all your regexps in one and do all the replacements in one call to  (with a function as replacement) instead of several times in a loop:


Answer URL
https://docs.python.org/3/library/re.html#re.sub
