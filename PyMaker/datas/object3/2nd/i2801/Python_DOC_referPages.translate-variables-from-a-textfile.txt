Natural Text
Let's say I have a textfile which is built like this:Now I'm trying to translate this file with Python and I have figured out how to translate strings with googletrans and how to read a file but I can't figure out how to actually read what's within the quotation marks only.This is how far I have gotten
You can get just the values inside the quotes by using the very helpful  module, which provides utilities for parsing shell-like syntaxes:Note that in addition to stripping the quotation marks, this supports spaces, and escaped quotes, etc:You can hook this up to your file with something like:


Answer URL
https://docs.python.org/3/library/shlex.html
