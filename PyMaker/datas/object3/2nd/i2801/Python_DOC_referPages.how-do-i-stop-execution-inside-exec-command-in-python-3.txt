Natural Text
I have a following code:If I run it I get:How to force  stop without errors? Probably I should replace  with something? Also I want the interpreter work after  call.
Here, just do something like this:If your worry is readability, functions are your first line of defense.
This will work, return only works from within a defined function:but if you want to use return, you must do something like:
There is no built-in mechanism that allows you to abort execution of an  call. The closest thing we have is , but that exits the whole program, not just the . Fortunately, this can be worked around with a minor amount of exception handling boilerplate:
Just for fun, here's another way:


Answer URL
https://docs.python.org/3/library/sys.html#sys.exit
