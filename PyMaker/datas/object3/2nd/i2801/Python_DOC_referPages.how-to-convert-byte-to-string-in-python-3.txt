Natural Text
This question already has an answer here:What's the correct way to convert bytes to a hex string in Python 3?                    8 answers                I have a variable  whose value is . How can I convert it to ?, and  are converted to  and  respectively because their ascii code are  and . gives me this errorUnicodeDecodeError: 'utf-8' codec can't decode byte 0xac in position  0: invalid start byte
Simply use -methodto get the wanted result, because it's not a problem with decoding or encoding. Your bytestring looks ok to produce a proper string object with hexadecimal numbers.


Answer URL
https://docs.python.org/3/library/stdtypes.html#bytes.hex
