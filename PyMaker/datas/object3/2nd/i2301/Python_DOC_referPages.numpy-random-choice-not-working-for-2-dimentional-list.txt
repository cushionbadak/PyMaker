Natural Text
I ran the following python code:expecting a result like this:but what I got instead was the error message below:How do you randomly choose from a 2-dimensional list?
Or can do :Demo:Output:
Numpy doesn't know if you want to extract a random row or a random cell from the matrix. That's why it only works with 1-D data.You could use  instead:With Python 3.6 or newer, you can use  directly:If you really want to use a numpy array, you'll have to convert your list of lists to a 1-D array of objects.
You will need to use the indices:Or, even shorter (based on hpaulj's comment):


Answer URL
https://docs.python.org/3/library/random.html#random.choice
https://docs.python.org/3/library/random.html#random.choices
