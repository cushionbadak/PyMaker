Natural Text
This question already has an answer here:How to extract all UPPER from a string? Python                    6 answers                Upper case first letter of each word in a phrase                    10 answers                This sounds simple but how can i display the capital letters of a string like:string = "Hey There"Then displays'HT'
Try this being the string you want to find the capital letters, you split the string into words and choose those which have a upper case first letter.In case you want all the capital letters off the stringIn this we check if every character  is upper case and join all such characters.


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.isupper
