Natural Text
I'm pretty new to SQL and the Sqlite3 module and I want to edit the timestamps of all the records in my DB randomly.When run, I get a  .To add the  value originally I set  to a tuple and the current UNIX timestamp as the first value of a it e.g . Is this error displaying because I've used two  unlike the single one I used in the statement to add the timestamps to begin with?
You're using  instead of .  takes an iterator of tuples and executes the query for each tuple.You want to use  instead, it executes the query once using your .


Answer URL
https://docs.python.org/3/library/sqlite3.html#sqlite3.Cursor.executemany
