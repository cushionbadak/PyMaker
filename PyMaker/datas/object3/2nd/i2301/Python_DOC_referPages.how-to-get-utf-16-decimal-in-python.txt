Natural Text
I have Unicode Code Point of an emoticon represented as U+1F498:I would like to get utf-16 decimal groups of this character, which according to this website are  and .I tried to do  but did not help me at all because it gives some other characters.Also, trying to decode from UTF-8 before encode it to UTF-16 as follow  does not help either.How do I correctly get the utf-16 decimal groups of a unicode character?
You can  the character with the  encoding, and then convert every 2 bytes of the encoded data to integers with  (or  in python 2).Python 3Python 2 + Python 3Result:
In a Python 2 "narrow" build, it is as simple as:This works in Python 2 (narrow and wide builds) and Python 3:Output:This is a more general solution that prints the UTF-16 code points for any length of string:Output:


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.encode
https://docs.python.org/3/library/stdtypes.html#int.from_bytes
https://docs.python.org/3/library/struct.html#struct.unpack
