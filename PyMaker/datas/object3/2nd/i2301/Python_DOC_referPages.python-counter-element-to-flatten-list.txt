Natural Text
Now to flatten Counter element i'm using the codeSo i'm wonder, if it could be done more directly. 
This is Note that (per the docs)Elements are returned in arbitrary orderSo you may want to sort the result to get a stable order.
You can use a nested list comprehension:Output:
With just for loop:output:


Answer URL
https://docs.python.org/3/library/collections.html?highlight=counter#collections.Counter.elements
