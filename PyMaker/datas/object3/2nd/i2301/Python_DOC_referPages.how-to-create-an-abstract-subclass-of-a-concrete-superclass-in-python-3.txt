Natural Text
Say I haveThen, I want to create an abstract subclass  of , which itself is concrete. Should I use multi-inheritance for this purpose? If so, should I import  first, as in, or should I import it last, as in
Yes, multiple inheritance is one way to do this. The order of the parent classes doesn't matter, since  contains no methods or attributes. The sole "feature" of the  class is that its metaclass is , so  and  are equivalent.Another option would be to directly set 's metaclass to  like so:


Answer URL
https://docs.python.org/3/library/abc.html#abc.ABC
https://docs.python.org/3/library/numbers.html#numbers.ABCMeta
