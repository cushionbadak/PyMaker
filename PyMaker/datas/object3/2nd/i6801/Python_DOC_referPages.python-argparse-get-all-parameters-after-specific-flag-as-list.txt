Natural Text
I'm trying to make argparser like in gdbThe problem is I need treat all arguments after  just as values, and get them as a list inside my program. How can I do that?ThanksP.S. Use all unrecognized arguments it's bad solution because python and binary args may have same signature.
Have you tried using  instead of ? You can do something like that then:This basically constructs an argument dictionary with flags as keys and list of arguments to flags as values. You might want to check what  is. If you invoke your python script with might be  but I am leaving that to you to find out. Then instead of  you want to do  or something you want.So if we callthe  will look like this:Now if you want to use what is the value for , you can do something likeWith  you can do something like


Answer URL
https://docs.python.org/3/library/argparse.html#arguments-containing
