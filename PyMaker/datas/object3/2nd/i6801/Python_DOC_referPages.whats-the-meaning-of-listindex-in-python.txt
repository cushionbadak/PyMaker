Natural Text
What's the difference between list[index, :] and list[index:]??At the beginning, returnMat is a zero matrix.
 is an example of numpy's array slicing syntax. It would retrieve rows (different rows correspond to the first index) of the matrix ( means "get all of this index"). Example:To go a bit further, it looks like you're constructing a matrix row-by-row from a file in your code snippet, which means you probably need to be doing something with  as well. I would suggest looking into .



Answer URL
https://docs.python.org/3/library/functions.html#enumerate
