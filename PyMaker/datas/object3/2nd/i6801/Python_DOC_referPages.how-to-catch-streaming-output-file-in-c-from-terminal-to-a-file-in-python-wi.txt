Natural Text
This is the python code which is to be edited .The Output is coming from Command==2 Which needs to be saved in files(Dynamic names) each containing 200 lines.This means that i want to make batches of outputs coming from and save them in files with dynamic names.i have tried this code but it doesn't work
If you want to extract a part of the output from the stream and then save it, consider using  as follow:If you don't care about extracting, consider using the shell redirection as suggested by @Someprogrammerdude with the  redirection in the shell. More details here : http://www.tldp.org/LDP/abs/html/io-redirection.html


Answer URL
https://docs.python.org/3/library/os.html#os.system
