Natural Text
It seems to me  and  do the same thing: running a script.What is the purpose of using ? Thanks.
In some cases, especially for very small projects,  and  will be pretty much the same.The biggest difference is when your module lives in a package and has relative imports. If you have a script that import something like , you will most likely get a  when you run it with . On the other hand,  will produce whatever output you expected.
You can load modules and invoke them as script. The exact file name or path is not needed. Example:This will print a formatted version of , and it loads the module  for this. Python searches for this module automatically. You don't need to know the exact path.


Answer URL
https://docs.python.org/3/using/cmdline.html#cmdoption-m
