Natural Text
I am attempting to call a handful of local bash scripts that I have written for health checking applications on remote servers. The above runs perfectly fine from the command line. However, when I wrap the call in python, I keep getting errors stating: As for my python, I am using the subprocess module.Python:Any help would be appreciated!
Redirection in your first example is done by shell. Standard input for  read from the file , which  passes to the process on remote machine.You can not use shell redirections, because you are not running a command from shell. Instead you can use  and  arguments for  to set the standard input and output for your process. You need to open the file and then pass the handle to . This is explained in: Using files as stdin and stdout for subprocess.In python example, you pass  as 1st argument to , which excepts a list of arguments, or a single string: the path to the executable. You get  error because your string argument is not a path to an executable. Correct format following the standard convention would be .All this put together, your example should look something like:This all is explained in Python documentation for subprocess module.


Answer URL
https://docs.python.org/3/library/subprocess.html#popen-constructor
