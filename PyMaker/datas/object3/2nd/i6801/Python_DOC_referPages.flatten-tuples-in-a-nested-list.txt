Natural Text
is there a compact oneliner or python idiom to handle the following task?I want to transform a list of list of tuples like this:to this:Using map and flattening the list only gave me the follwingMany Thanks in Advance!
I'd do:The  part, as you already know, transposes the outer list of lists. Then I grab each pair of tuples and concatenate them, then turn the combined tuple into a list. If you don't care if you have a list of lists or a list of tuples in the end, you could get rid of the  call.
You should be able to generalise Blckknght's answer to any number of tuples inside a list, using .
You can use list comprehension with  like below:Output:
Here's one way.gives me


Answer URL
https://docs.python.org/3/tutorial/datastructures.html#list-comprehensions
https://docs.python.org/3/library/functions.html#zip
