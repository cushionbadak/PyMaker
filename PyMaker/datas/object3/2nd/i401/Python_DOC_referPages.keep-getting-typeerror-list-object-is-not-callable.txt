Natural Text
I'm not sure where my error is but here's the code where i got the error from :
As said by @Robin Zigmond in a comment, you are trying to call a list, as you would call a function, using the  syntax. The following:Is different from:The latter doesn't work because  is not callable. The Python documentation enumerates callable types:User-defined functionsInstance methodsGenerator functionsCoroutine functionsAsynchronous generator functionsBuilt-in functionsBuilt-in methodsClassesClass Instances: Instances of arbitrary classes can be made callable by defining a  method in their class.A list (ie a  instance) is none of them, since the  class does not have a :In your example, the first lines states that  is an iterable. The error shows it is a list. And you try to call it with . My guess is that you want to get the index of  in . To achieve this in Python, you can write:Hope it helps!


Answer URL
https://docs.python.org/3/reference/datamodel.html#the-standard-type-hierarchy
