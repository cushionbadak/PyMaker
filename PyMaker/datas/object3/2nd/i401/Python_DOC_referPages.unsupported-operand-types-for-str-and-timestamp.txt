Natural Text
Statement :Error : unsupported operand type(s) for &: 'str' and 'Timestamp'My pandas dataframe is df. It consists of a trading log.When I filter the df on Symbol and(&) timestamp I get the above error What did I do incorrectly ? I don't want to change my timestamp to a str.dfExpected results : 63Actual results : unsupported operand type(s) for &: 'str' and 'Timestamp'
You can try like below to find the result   here the '&' operator takes precendee over the '=='operator.
Add parenthesis, because operator precedence in Python. The bitwise and (&) has precedence over equal (==) so in this case the parenthesis are required. Also compare column  with :


Answer URL
https://docs.python.org/3/reference/expressions.html#operator-precedence
