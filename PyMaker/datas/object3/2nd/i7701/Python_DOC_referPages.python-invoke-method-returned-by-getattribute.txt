Natural Text
If this question has a duplicate, sorry, I didn't find it, I will erese the question if someone does.I have this simple python class:And trying to get the method with different ways I did:The question is, if I do:$hi!Is return, but when I do:TypeError: 'classmethod' object is not callableHow can I execute the  method?
You are bypassing the descriptor protocol, and you have an unbound class method.The solution is to invoke the protocol, if there is a  method present:Now the classmethod is bound to the class and it works again:Alternatively, use the correct , one that actually knows how to apply the descriptor protocol to class attributes; classes do not use , but :You'd actually want to access  to allow metaclasses to override the implementation of  here.


Answer URL
https://docs.python.org/3/howto/descriptor.html
https://docs.python.org/3/reference/datamodel.html#object.__get__
https://docs.python.org/3/reference/datamodel.html#special-lookup
