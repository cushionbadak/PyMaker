Natural Text
I have the following use case:Call a function which returns iterator in pythonCheck if iterator is not emptyIf not empty, then do some operationHowever, the process of checking if the iterator is empty, seems to empty it. Is there a better way to do this?
To get a copy of an iterator so that you can operate on it independently of the original, you can use itertools.tee.  You can test if an iterator is empty by seeing if it throws StopIteration. So you could do something like:


Answer URL
https://docs.python.org/3/library/itertools.html#itertools.tee
