Natural Text
This question already has an answer here:Add new keys to a dictionary?                    15 answers                I've two json objects, I 'm figuring out a way on how to merge them in pythonI need the final result in the following mannerI am trying to append it, but its showing some error. Help me with the code in python.
your input seem to be strings and not dictionaries. you need to convert them to dicts using :if you need the output as string again, use :if you insist on having the ouput sorted:
You can simply do 
I assume that they are strings and not python dicts. So here is what you can doThe code above gives you thisIf you want to have the structure in the json string preserved you can look into this link. That will make the decoding a little bit more complex.


Answer URL
https://docs.python.org/3/library/json.html#json.loads
https://docs.python.org/3/library/json.html#json.dumps
