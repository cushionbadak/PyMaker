Natural Text
I've been using  to create some templates but when using it, it changes the casing of my boolean valuesHere's how to replicate:What's the reason for this?
 converts a Python dictionary to a JSON object formatted as a string. According to the JSON specification, the two boolean literals in JSON are  and . So,  isn't changing the casing, it's only converting Python's boolean literals,  and , to JSON's boolean literals,  and , respectively.If you do wish to get a string representation of the Python dictionary without converting to JSON, you could cast the dictionary to string using 
JSON stands for  and  is boolean representation in JS. Compare with these docs.


Answer URL
https://docs.python.org/3/library/json.html#encoders-and-decoders
