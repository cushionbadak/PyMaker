Natural Text
I want to concatenate user input with a  colon in-between.My script is taking options through user input and I want to store it like:I have created a loop to read user input, but not sure how to store it.
One simple approach to keep as close to your code as possible is to start off with an empty list called colors before entering your loop, and appending to that all the valid colors as you take inputs. Then, when you are done, you simply use the join method to take that list and make a string with the ':' separators.Demo:
You can concatenate a ':' after every input color
you're looking for , to be used like so: . Read more at https://docs.python.org/2/library/stdtypes.html#str.join
Using a list is neat:


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.join
