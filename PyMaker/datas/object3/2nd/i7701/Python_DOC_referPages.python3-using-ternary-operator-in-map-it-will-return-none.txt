Natural Text
For python 2.7.13For python 3.6.1Two Questions:I want to know why the python2 return the correct result, but python3 return NoneHow should I modify the code with python3 to return the correct result
This is a change to the functionality of the  function (together with the change to becoming an iterator). Because the inputs are now iterators,  has been updated to follow the same behaviour as , and not pad shorter inputs with  values.Compare the documentation for  in Python 2:If one iterable is shorter than another it is assumed to be extended with  items.with the Python 3 version:With multiple iterables, the iterator stops when the shortest iterable is exhausted.You can use  together with  to get the Python 2 behaviour again: has the added advantage that you can now specify what value to use as a filler; as an example, you can set it to :Demo:


Answer URL
https://docs.python.org/3/library/functions.html#map
https://docs.python.org/3/library/itertools.html#itertools.zip_longest
https://docs.python.org/3/library/itertools.html#itertools.zip_longest
