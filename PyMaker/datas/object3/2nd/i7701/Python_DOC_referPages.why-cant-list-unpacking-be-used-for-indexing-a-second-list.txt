Natural Text
… e.g.  where  and  are both lists and Using the simple example below:Why does running the above raise an exception?
Because that syntax is invalid.Iterable unpacking is allowed in various circumstances – for example:Unpacking Argument ListsAssignmentComprehensions… but not universally, and definitely not for indexing. In fact, it's unclear what your proposed syntax even means. Consider: what would  return if  were ?


Answer URL
https://docs.python.org/3/tutorial/controlflow.html#unpacking-argument-lists
