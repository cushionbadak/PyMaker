Natural Text
I am new to Python,I am writing a simple program Inputexpected output is as belowBelow is the code I have used ...Actual output is:Missing one more value. 
your join did not join the new itemreturnsyou could also use returns
You could use a , that could make the program much easier:I haven't debugged your algorithm but there are several things that stand out:Python doesn't need  at the end of lines. is actually called as . You used the seperator as input for the function.
 doesn't do what you think. What you want is:


Answer URL
https://docs.python.org/3/library/collections.html#collections.defaultdict
