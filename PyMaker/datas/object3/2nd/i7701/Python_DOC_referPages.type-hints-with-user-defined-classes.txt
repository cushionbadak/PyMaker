Natural Text
Couldn't seem to find a definitive answer. I want to do a type hint for a function and the type being some custom class that I have defined, called it .And then let's say in some function, call it , I have one argument named . Would the correct way to type hint  be:Or would it be:?
The former is correct, if  accepts an instance of :In case you want the class  itself (or a subtype), then you should write:Like it is written in the documentation about Typing:A variable annotated with  may accept a value of type . In  contrast, a variable annotated with  may accept values that  are classes themselves - specifically, it will accept the class  object of .The documentation includes an example with the  class:


Answer URL
https://docs.python.org/3/library/typing.html#typing.Type
