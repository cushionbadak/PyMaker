Natural Text
Additional explanation:What I want to achieve is what's the pythonic way to achieve this
You can find out how many positional arguments a function accepts by accessing the  attribute:However, that doesn't respect varargs:So the more robust solution is to use :Demo:
One way would be to use  (Check the number of parameters passed in Python function; https://docs.python.org/3/library/functions.html#locals), and do some math in the body of each function to figure out how many args are left over (unused). You can then return a result which includes a tuple of the unused arguments.


Answer URL
https://docs.python.org/3/library/inspect.html#inspect.signature
https://docs.python.org/3/library/functions.html#locals
