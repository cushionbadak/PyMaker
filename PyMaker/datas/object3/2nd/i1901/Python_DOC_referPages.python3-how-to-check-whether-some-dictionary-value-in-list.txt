Natural Text
I want to know value  is in list's key .Example:How can I do this?
Just use , Python's built-in function to check if a condition holds for, well,  out of many.  breaks early and avoids spurious iterations once the first truthy element is encountered:Using  instead of  is more robust as it won't raise an error if  is not in one of the dicts. Note, that if by any chance, the value you are looking for were , you would have to provide a different default value to , e.g. .



Answer URL
https://docs.python.org/3/library/functions.html#any
