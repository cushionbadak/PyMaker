Natural Text
I want to check if ANY of the items in the array exist in the given object.Here is my current code that works:but if I do this....It doesn't work.How can I achieve this? Any help will be appreciated
Use set intersection, as in the following toy example:OutputIn your example it will be something like this:Or if you prefer:
Do:What is it doing?, first of all we have a "role names list" made with the expression . Let's call it .It reduces now to:Then it comes a little of theory about iterables. These are my favourite posts about it: Introduction to Python Generators and Iterables vs. Iterators vs. Generators. expects an iterable. A generator is "[..] a special kind of iterator" is an iterable.The generator is made by the expression in parenthesis. Just try:And it prints:Any() will then:Return True if any element of the iterable is true. If the iterable is  empty, return FalseWhich means that for every  listed in  (in your example ) and return  if any of them is belongs to .
You can also use  instead,


Answer URL
https://docs.python.org/3/library/functions.html#any
https://docs.python.org/3/library/functions.html#any
