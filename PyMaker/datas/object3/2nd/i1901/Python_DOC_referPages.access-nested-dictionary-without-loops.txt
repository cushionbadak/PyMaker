Natural Text
This question already has an answer here:Access nested dictionary items via a list of keys?                    14 answers                If I have a nested dictionaryand a list of indexes  leading to an inner value, is there a pythonic / one-liner way to get that value, i.e. without resorting to a verbose loop like:
You can use . is a function that takes two arguments, the  and a key (and another optional argument not relevant here).  is used as the initial value.In case you ever need the intermediary results, use .Unfortunately, the function does not take an optional  argument, so we are prepending  to .


Answer URL
https://docs.python.org/3/library/functools.html#functools.reduce
https://docs.python.org/3/library/itertools.html#itertools.accumulate
