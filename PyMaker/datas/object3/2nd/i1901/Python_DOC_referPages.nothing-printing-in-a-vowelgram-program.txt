Natural Text
I was creating a vowelgram program that checks for a string to contain all the vowels. The vowels should also occur only once.The program I created is as followsUnfortunately the program is printing nothing. I cannot seem to find the error in the code. Help is appreciated. Thank you
You could use  to check if all of vowels are in the string and are exactly once:
Use (Counter)[https://docs.python.org/3.7/library/collections.html#collections.Counter] class to count letters. Then check if every vowel count  is lower or equal to zero.
If it is not printing anything, for some input, you must be ending up in the missing  branch of the inner :You can remedy that, e.g. by using the following construct covering all (other) cases:Generally however, your nested loops are not optimal performance-wise. You could use a  to obtain a result in linear time:


Answer URL
https://docs.python.org/3/library/collections.html#collections.Counter
