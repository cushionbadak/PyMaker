Natural Text
This question already has an answer here:Is there a portable way to get the current username in Python?                    12 answers                I have a code that makes a folder:I want this to be able to run on any computer, so how can I get the default user without asking and doing something like:EDIT: By default user, I mean the one currently running the program.
You can try the following to first get the username using  and then use it in the path using a placeholder . I tried it on my Mac and it returned the correct username.
Since you're already using , make use of the  method in :
Since the answer is about the process ownerUntested, but should work on Windows and Linux


Answer URL
https://docs.python.org/3/library/pathlib.html#pathlib.Path.home
