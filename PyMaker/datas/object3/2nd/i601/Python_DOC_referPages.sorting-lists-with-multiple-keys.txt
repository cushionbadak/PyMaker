Natural Text
I have a list like the following:How could I sort this list in the most readable way, if the main key would be the first element and the secondary key is the second element, in case there are multiple of the first element? 
You can use  with  parameter: part basically asks Python to sort by first element and then by second element in case first element is duplicate.
The  built-in allows you to specify custom keys. There is also a useful how-to on multi-key sorts. Adapting your example to theirs
The sort() method can be used. The basic syntax is: Here, I have created a function and passed that function as the key in sort() method which returns 1st and 2nd element.


Answer URL
https://docs.python.org/3/library/functions.html#sorted
https://docs.python.org/3/howto/sorting.html#sort-stability-and-complex-sorts
