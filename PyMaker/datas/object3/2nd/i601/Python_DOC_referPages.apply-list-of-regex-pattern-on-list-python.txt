Natural Text
I have data frame in which  column contains a list. I want to clean the  column using function clean_text().My Solution:But I am getting below error:Error
 is not a valid regular expression, since the repeater  must be preceded by a character or a pattern that can be repeated. If you mean to make it validate a string that is at least 53 characters long you can use the following pattern instead:
The culprit is the first pattern from the list - . It reads:  - match the beginning of the string and then  repeat the previous character or group 53 times. Wait... but there is no other character than  which cannot be repeated! Indeed. Add a char that you want to match 53 repetitions of. Or, escape the sequence  if you want to match it verbatim, e.g. using .


Answer URL
https://docs.python.org/3/library/re.html#re.escape
