Natural Text
If I have a dictionary with lists in it like this one:How can I make a code that first checks if any of the dictionary keys exist as strings inside the value lists. If they do compare the values of these two keys and count the number of similarities.For example, the final result in this dictionary will be:

The following code reports, for each  in , how many times any associated item occurs among the top-level dictionary keys:
It is not so clear, but i can answer.So to get each keys' values to see if any of them are keys, do:To get counts of all values:To get most common:Final answer:Use the below:
You can try:You will get the output : 


Answer URL
