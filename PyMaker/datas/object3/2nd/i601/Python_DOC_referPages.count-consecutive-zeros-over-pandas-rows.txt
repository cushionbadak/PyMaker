Natural Text
Having the following I would like to count the consecutive zeros over rowsLooking for different efficient ways
For efficiency, I would suggest going pure NumPy way -Sample run -Timings on larger array -
Using  with a list comprehension:
If you're interested in a pure pandas/numpy solution... you can do this with  and :Or, avoiding the ,
You could use itertools.groupby:Output
One method is to transform the values to boolean values, and splitting the string by  values


Answer URL
https://docs.python.org/3/library/itertools.html#itertools.groupby
https://docs.python.org/3/library/itertools.html#itertools.groupby
