Natural Text
I am learning about Regex and am stuck with this code:When I try with  instead of , it works. But it can't find any match with .What am I doing wrong? 
 returns a simple list of strings matching the pattern.It has no  method, just omit that:
It seems that you're trying to match phone numbers in , for that you can use:Output:Python DemoRegex Demo & Explanation
Since (it looks like) you're just out for the numbers, you could do something likeThat might save you some trouble from internationally formed numbers (such as  and the like).
 module is used when you want to iterate over the lines by line, it will return a list of all the matches not group.So in your case it returns as list


Answer URL
https://docs.python.org/3/library/re.html#re.findall
