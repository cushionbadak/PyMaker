Natural Text
How can I pretty-print a (possibly nested) Python list with indices, similar to the Perl's  module, e.g. for  the output should be something like this:
you can use enumerate to get the indices, and use pprint to pretty-print a python list.For example:and the output is:


Answer URL
https://docs.python.org/3/library/pprint.html?highlight=pprint#module-pprint
https://docs.python.org/3/library/pprint.html?highlight=pprint#module-pprint
