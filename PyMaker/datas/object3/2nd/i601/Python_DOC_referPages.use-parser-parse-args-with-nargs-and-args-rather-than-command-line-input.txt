Natural Text
I have an  argument  that accepts a variable number of strings:This works fine when using the CLI as usual with command-line input: .I also want this to work with arguments that are passed to  through the  argument from within the program code.But when I try to pass , it's interpreted as single string  and not as a list of  and .Similarly, passing  is somehow interpreted as single argument, which is a list. So, when accessing  I get the list and  gets me an error.
As your link shows, you can give  a list of strings.To test parsing without any commandline arguments, by giving it an empty list:With a list of strings:or split a string:Answers to  questions often use one of these forms to illustrate their actions.  The  is convenient.It is also possible to create an  Namespace directly:I don't follow your interpretation of the result forThat should produce a  that is .This isn't a good test case:to produce the same thing from the commandline you'd have to useThe quotes override the normal split on white space.  You have to use  to emulate that behavior.
It is tricky for argparse to handle lists directly use a csv list instead:


Answer URL
https://docs.python.org/3/library/argparse.html#args
