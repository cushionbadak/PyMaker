Natural Text
I have a list in python that looks something like this:I would like to calculate the math problem in the list so:So the variable "anwser" is -27.
This will work: But be careful using !
This is what you call a Infix notation (https://en.wikipedia.org/wiki/Infix_notation)You can use a stack to evaluate it. I found a gist here which might help (https://gist.github.com/nava45/6333409#file-infix-expression-evaluation)
Evaluating an equation with priority of operations requires a parser.Fortunately, writing a parser for basic arithmetic, that is addition and multiplication, can be achieved fairly simply without the use of parsing tools.CodeExampleOutputIf you ever need to parse equations with more than two levels of priority, than an external parsing tool may become necessary.


Answer URL
https://docs.python.org/3/library/functions.html#eval
