Natural Text
Ruby on Rails JSON is able to parse out curl calls in the following format:. It will interpret this as .Python's  seems to take this as a JSON error (which it obviously is). Is there a way however, to take parse payload in the format of ? I'm using Python's  with Flask here.
If you are using flask, why not use jsonifyThis will send a JSON response like this to the browser:
Try


Answer URL
