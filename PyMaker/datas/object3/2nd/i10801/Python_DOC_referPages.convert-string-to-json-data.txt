Natural Text
I am converting string to json format as belowAnd json data is I can use regex and search pattern  and add to dict.But is there any better way or in build functions I can use for more generic solution?
Not sure about all the variations of the input string and what is the scope of characters ,  and  can have, but here is what you can start with:where  are named capturing groups,  would match a single digit,  would match one or more consecutive alphanumeric (and underscore) characters.Demo:


Answer URL
https://docs.python.org/3/howto/regex.html#grouping
