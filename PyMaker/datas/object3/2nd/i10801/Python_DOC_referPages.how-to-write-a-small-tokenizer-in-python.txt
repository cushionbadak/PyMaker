Natural Text
This question already has an answer here:Can you add new statements to Python's syntax?                    13 answers                Mini-languages in Python                    6 answers                Normally, Python calls functions byBut I would like to change toFor example,If I input , then I expect Python to execute .Args will contain string, so that we can't simply split words.Actually, I would like to write some scripts to use on my mobile. So it would be a little annoying to type parentheses.
You can do this : If I put in input callFunction hello world it works :)
if none of the args contains whitespace you could doEdit:If it is not that simple you should have a look at https://docs.python.org/3/library/cmd.html and https://docs.python.org/3/library/argparse.html but these require some preparation before you can execute arbitrary codeEdit2:If you do not need your args to be exact python, you could parse them as json with the standard libraryyou could do it like
The builtin  module is probably what you want:If you can trust the input, then actually calling this will look like:
All I want is a simple tokenizer. And I would like to run functions by calling . So that's what I did for my project.Here's the result:Attentsion: This may not suitable for everyone, this's not a complete parser or tokenizer.Code:


Answer URL
https://docs.python.org/3/library/cmd.html
https://docs.python.org/3/library/argparse.html
https://docs.python.org/3/library/json.html#json.JSONDecoder.raw_decode
