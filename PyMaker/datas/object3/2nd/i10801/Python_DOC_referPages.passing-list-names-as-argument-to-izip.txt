Natural Text
I have number of lists that is user defined I'm performing some operations on these lists and I want to pass them at the end to  in the following format, for example if the user entered numLists = 2 I want the line to be Usually if I already have some predefined lists The line will be Is there a way to pass the list names into ?NOTE:I do not want to do this     for j in range(numLists):        for val in itertools.izip(d[j]):          writer.writerow(val)because it does not give the needed output.
Unpack the list of lists into positional arguments of :


Answer URL
https://docs.python.org/3/tutorial/controlflow.html#unpacking-argument-lists
