Natural Text
If you scroll down a bit you can see this code from g.d.d.c return SQL table as JSON in python:I just want to keep the order of my columns.For example when I  I get this:But the columns are saved in a wrong order:The more columns I add, the more random it gets.
Python  don't save the order of keys, use OrderedDict instead.
If I understand You want dictionary to have ordered key. It's not possible, because dictionaries are not keeping keys in some order, because keys are used only to access elements. You can always print columns of data using raw column information:


Answer URL
https://docs.python.org/3/library/collections.html#collections.OrderedDict
