Natural Text
Does this:or this:...make a copy of iterated sublist?
Assuming that  is a regular Python list, and not a Numpy array, Pandas dataframe, or some custom class supporting slice indexing, then the slice  will create a new list, whereas  does not.As suggested in comments, you can see this for yourself by creating both  objects and modifying the original list before consuming them:Also note that this does in fact have nothing to do with , which will create a generator in both cases (on top of whatever iterable was created before by the slice).You could also just create the two variants of slices and check their types:


Answer URL
https://docs.python.org/3/library/itertools.html#itertools.islice
https://docs.python.org/3/library/functions.html#enumerate
