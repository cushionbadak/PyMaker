Natural Text
I have a class similar to this:now when I run in the console:  being  an instance, I get the following:(property object at 0x0000000004FDCBD8,)What am I doing wrong here?I tried using the  above the method but it didn't work.I'd like to get "Humpty Dumpty" when I access . So the method would assign it's value to the attribute.
If you just want to get the function called on attribute access, define name as property and provide your function  to it:This will set the  inside the property to your function . Take note, there's a pretty good tutorial on descriptors in the Python Official docs.
That's invalid syntax. You need to do something like this:


Answer URL
https://docs.python.org/3/howto/descriptor.html
