Natural Text
I have a list  and I want to use my dict  to get the result ? What is the most pythonic way to achieve this?
Most Pythonic is probably a list comprehension:For larger inputs, the following might be faster, but less Pythonic, as it pays a slightly higher setup cost but (on CPython reference interpreter) pushes the per-element work to the C layer, bypassing the (relatively slow) byte code interpreter:
The following list comprehension will do:


Answer URL
https://docs.python.org/3/tutorial/datastructures.html#list-comprehensions
