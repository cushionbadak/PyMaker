Natural Text
When i need to count elements of different type , i find myself writing something like:Sometimes i do the same thing with a new empty list that will grow over time.The above code works fine, but it feels like there is a better way of writing it.Is there?
In addition to defaultdict/Counter mentioned in comments, you can also have a default value returned from a failed . This allows you to set the initial count to 0 if the key lookup fails and immediately increment by 1, or increment by 1 each time the key is found as you loop through.
One way to do it:
This is exactly what the  is for!https://docs.python.org/2/library/collections.html#collections.Counter


Answer URL
https://docs.python.org/3/library/collections.html#collections.Counter
https://docs.python.org/3/library/collections.html#collections.defaultdict
https://docs.python.org/3/library/collections.html#collections.Counter
