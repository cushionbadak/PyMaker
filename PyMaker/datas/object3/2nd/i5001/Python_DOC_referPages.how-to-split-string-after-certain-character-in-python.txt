Natural Text
If I have a string, lets just say, , how could I split the string by all s AFTER the first occurrence of the letter ?  As in, it would return .
This is documented here: str.rsplit()
If you note the location of the gate (first 'a') then you can split the string after that point like:Code:Result:


Try this:-
Use the following code
In your example result you split the string by ' b ' so I'm going to use that.If you want to split by 'b' then replace them.


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.rsplit
