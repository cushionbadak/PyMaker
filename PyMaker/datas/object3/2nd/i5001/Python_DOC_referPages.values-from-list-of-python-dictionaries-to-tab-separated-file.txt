Natural Text
I have a dictionary of various ID's I need to write to a file with the values under each key as their own row, tab separated.In the end I want the file to look like this:I cannot use pandas since this library is too large for the application we are using. Currently I have my results in a list of dictionaries with each element as a user and their various ID's as key value pairs within each elementIs there a way to extract the values within the dictionaries within the list and then write those to a file with the id's contained in each element as their own row, tab separated?
Here is a simple example of using Reading it back in as dictionaries is easy as well using 


Answer URL
https://docs.python.org/3/library/csv.html#csv.DictWriter
https://docs.python.org/3/library/csv.html#csv.Dialect.delimiter
