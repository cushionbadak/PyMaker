Natural Text
I am having nested list of strings as:I am trying to join the strings in sublist to get a single list of strings as:OR,I tried using  and  but they didn't work.This is the error I got:TypeError: sequence item 0: expected str instance, list found
You can use  with  here to achieve this as:OR you may use it with list comprehension to get the desired result as:For getting the results as , you need to wrap the resultant string within another list as:Issue with your code: In your case  didn't worked because  is a nested list, but  tried to join together all the elements present in  treating them as string. And that's the cause for your error "expected  instance,  found". Instead, you need to pass each sublist to your  call. This can be achieved with  and list comprehension as illustrated above.
Simple
You can try this:Output:


Answer URL
https://docs.python.org/3/library/string.html#string.join
https://docs.python.org/3/library/string.html#string.join
