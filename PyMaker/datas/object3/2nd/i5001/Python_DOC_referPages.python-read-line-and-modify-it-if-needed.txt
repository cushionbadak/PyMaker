Natural Text
let's say I have a file  like this:Now, I'd like to have a python script which performs these tasks:If the line containing  parameter is not commented ( symbol), to rewrite the line adding , like it is with To perform the task in 1. independently of the line orderTo leave untouched the rest of the file The file I wrote is:Up to now the output is the same  with a further line  down the original line .Thanks everybody
I found the solution after a while. Everything can be easily done writing everything on another file and substituting it at the end.


Answer URL
https://docs.python.org/3/library/fileinput.html
https://docs.python.org/3/library/fileinput.html#fileinput.input
