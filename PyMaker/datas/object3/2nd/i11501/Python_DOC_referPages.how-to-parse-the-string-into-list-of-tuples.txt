Natural Text
I have a string of the format -What is the best way in which I will get this string into meaningful integer tuples?Lets say  is a function which takes such   as parameter and returns  of  Ex.
You can use Python's built-in eval() function.In this case, you can write your code like this:
For this kind of thing,  is preferred to just the built-in , especially if the string is something that may come from an unknown source. The call interface is the same as :However,  will guard against code insertions that may call system services (such as those that might delete files, etc.), and only accept literal values.


Answer URL
https://docs.python.org/3/library/functions.html#eval
