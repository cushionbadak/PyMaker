Natural Text
The following seems to work either way. What is the advantage (other than the nice ) of using ? Or is it the same thing?
This is explained pretty well in the types module description. It shows you that  is roughly equivalent to this:This provides the following advantages over an empty class:It allows you to initialize attributes while constructing the object: It provides a readable : It overrides the default comparison. Instead of comparing by , it compares attribute values instead.


Answer URL
https://docs.python.org/3/library/types.html?highlight=types#module-types
