Natural Text
I have the following lists inside a listThis list updates daily. Sometimes there is a list where the 1st element has two periods (.)  like the following bbd.pr.c When this happens I want the entire list deleted. Not to sure to how to go about doing this. Using the count function.Any help? 
For python 2, use filter:For python 3:
I've never thought  is particularly readable in comparison to list comprehensions, especially if you have to use a lambda. Here's the equivalent of Smac89's answer, for both Python versions:( corresponds to  but  is not generally a good name because , , and  all look too similar)


Answer URL
https://docs.python.org/3/library/stdtypes.html#common-sequence-operations
