Natural Text
Take  i.e: , , , each of which contains the string  or .If all the values in every  are , I want to display an error message e.g:Is there a Pythonic way of checking if all the values in  are the same, ? While the above example works, it gets very messy when I have 10 .
Use 
Interestingly using all() seems about 10x slower. 
You could [also] use the built-in  function which might be a little faster than  because it will stop checking as soon as a failure is detected. 


Answer URL
https://docs.python.org/3/library/functions.html#any
