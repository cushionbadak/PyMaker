Natural Text
Trying to add an insert(self, key) method that inserts the key into the appropriate position while linear probing for resolving collisions.My current code, I'm just not 100% sure how to inset the str into the hash table. Testing code: result:Im currently getting a Type errorI just need some advice on how to add the string into the hash table. 
 () is a string object. You need to convert it to integer to do modulo. Otherwise it will perform -style string formatting; causes error because there's no  in the string:Call  to get hash value:UPDATE  body can be merged into :


Answer URL
https://docs.python.org/3/library/stdtypes.html#old-string-formatting
