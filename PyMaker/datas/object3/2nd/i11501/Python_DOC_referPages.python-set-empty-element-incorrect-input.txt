Natural Text
I can't understand how it works.I have input of :And simple code Returns:Why I'm getting this  el. and how to get 10 instead of  and  ?
 takes an iterable (such as list, tuple, dict, etc.) and makes a set of its items.  For example:A string is iterable, too:Therefore, a set can convert a string:If you want each word, use :
Use  with a  function to remove the space elements:


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.split
