Natural Text
I am looking to sort a list of dictionary based on a key which is in the format \d+:\d+The list of dictionary should have the expected key(age)So when I pass the whole(lst) to compare(function?!) then it should give me a list of dict ordered in desc order based on the age.How should we do this ? Thanks for your time. 
You can use sorted().Output
Using the  and  argument of python built-in function :This prints:
Using NumPy you can try this:Output


Answer URL
https://docs.python.org/3/library/functions.html#sorted
