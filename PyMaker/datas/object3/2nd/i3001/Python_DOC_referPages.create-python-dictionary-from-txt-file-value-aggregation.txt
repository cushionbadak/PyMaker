Natural Text
I have a TXT file in the formwhere first column is a font size ("px") and second column is a character count. Is there any way to aggregate (sum) the character count (values) over all font sizes (keys) and divide by total character count by using a python dictionary? The supposed solution should be in the form:
No need to bother with pandas. Text files are iterable. Just open it, operate on the line (string) and fill a dictionnary.Output:Then it's simple math:Output:


Answer URL
https://docs.python.org/3/library/collections.html#collections.OrderedDict
