Natural Text
I am trying to build a list from lines from two different files.File1.txtFile2.txtdesirable outputI tried to simple add linesoutputI also wrote a function which tries to merge both in a third file file3.txtBut it still writes in 2 different Listscat file3.txtThe best would be a function which return such a list.
Even though the lines in the files resemble lists, you're actually treating them as strings.  You can use  to evaluate the strings into lists, combine them, and then write them to the other file
Using Then just


Answer URL
https://docs.python.org/3/library/ast.html#ast.literal_eval
