Natural Text
I have a project laid out like this:In , I want to create a file, , and put it in I was wondering if I could use the relative package structure. I was thinking of putting something like this in :Is there some way to do something like this?
Modules loaded from a file will generally have a  attribute. You can use this as a starting point for getting around your package. You could use the utilties in  or  to navigate around the path. Either of the following should work:ORIn both cases, use a context manager to make sure your file gets closed no matter what:Neither of these solutions is super elegant because they involve walking up the directory tree. Instead, you could start with the project root, and walk down:ORIn both cases,  should point to the root level .


Answer URL
https://docs.python.org/3/reference/import.html#__file__
https://docs.python.org/3/library/os.path.html
https://docs.python.org/3/library/pathlib.html
