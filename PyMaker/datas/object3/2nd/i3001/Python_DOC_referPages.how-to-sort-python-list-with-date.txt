Natural Text
I have a list in Python like thisi want to sort this list by the dates 
Here's a method that should work in a more general case:This way you won't be forced to have dates at the end of the list strings, output:
Using  with  in Ex:Output:
You can split each string, take the last part, and sort by this part:Output:
You can also sort the list by applying  to :Note: This might be over complicating it slightly, since ISO formats dates, and sorts string dates perfectly fine, as shown in the other answers. Using  is to just ensure that the dates are sorted by correct date format. 


Answer URL
https://docs.python.org/3/library/datetime.html#datetime.datetime.strptime
