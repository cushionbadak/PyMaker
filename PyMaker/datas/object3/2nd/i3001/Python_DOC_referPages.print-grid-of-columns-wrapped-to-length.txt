Natural Text
I am trying to print a 2-dimensional list in a format that wraps the length of each column to the length of the longest word in that column (plus 2 spaces padding). Example of what I'm trying to achieve below:Currently, my code almost achieves this, but it keeps clipping off the last 'n' number of columns if there is a row with less than the max number of items in it. Example below:This is the part of my code so far:What do I need to add/change to stop it from removing incomplete columns?
The problem seems to be with this line:From the docs on zip: https://docs.python.org/3/library/functions.html#zipThe iterator stops when the shortest input iterable is exhausted. So each list in  would have to be lengthened to the length of the longest list in  for this script to work. You could achieve that with this:You could then replace  with 


Answer URL
https://docs.python.org/3/library/functions.html#zip
