Natural Text
Person is a new class that has two properties:  and . I want import another  that has the same class and some objects of this class. For example:But I don`t know how each variable called and how much variables exist in a given file. How can i get all objects of imported file? The goal is to create list() of objects from several files.
A good way to do this would be with  by utilizing its  parameter.Firstly, you could get all user defined classes in the module with  and then, create a new predicate to capture any instances that are of the class(es) you got before.Initially, grabbing the classes is easy with  as defined in :Now,  is a tuple of classes:If classes are already known beforehand, the previous step is of course, not needed; just create a tuple of the classes you need to find and continue with the next step.You can then create a new predicate (a simple  would suffice) that grabs objects that are instances of any of the classes in :The result now being that  is populated with any name in the module  that is  for the the  check in :


Answer URL
https://docs.python.org/3/library/inspect.html#inspect.getmembers
https://docs.python.org/3/library/inspect.html#inspect.isclass
