Natural Text
I created simple python script to do infinite loop to test my android app, however it only runs through succesfully once and stops.
You should have each of the commands within a  (https://docs.python.org/3/tutorial/errors.html). It's not preferable to have a bare except (i.e. a catch-all except), but you can try it for debugging. 
You can use AndroidViewClient/culebra which is almost a drop in replacement for  but it is pure python.I made the changes to your script to show thatAlso, you can take advantage of Culebra GUI and automatically generate this kind of scripts (or even unittests) just by point and clicking on the UI.


Answer URL
https://docs.python.org/3/tutorial/errors.html
