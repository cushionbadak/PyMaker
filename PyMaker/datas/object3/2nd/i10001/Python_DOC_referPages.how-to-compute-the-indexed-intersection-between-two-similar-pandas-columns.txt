Natural Text
I have the following list:And the following pandas dataframe:How can I get into a new column the elements of  which appear in   (*)?:From the docs, I noticed that this can be done with contains(), so I tried the following:However, I am not sure if this is possible. For instance, is it possible to match  with , since  is similar to ?. For that constraint, I tried to use , but I am not getting the expected outcome (*). Any idea of how to retrive such strings in a new dataframe?.
You can first join values by  (regex or is ) and convert all values to lower-case by  - output is in . Then lowercase all values in column by  and call  with  for checking if  or  or  ... is in column:


Answer URL
https://docs.python.org/3/library/re.html
