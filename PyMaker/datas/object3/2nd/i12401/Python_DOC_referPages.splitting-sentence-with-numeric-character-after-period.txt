Natural Text
I am using python3, trying to split a text with note numbers after the period:this is the closest sentence splitting regex I've gotten to that still works: I'm basically lost w/r/t catching the numeric instance immediately after a period via regex. Any help in properly incorporating [0-9] into the expression? Thanks.edit this is how it ideally is split:
Using :If you are okay using third party module, you can use , which allow non-fixed-width look-around assertion, split on empty string:


Answer URL
https://docs.python.org/3/library/re.html#re.findall
