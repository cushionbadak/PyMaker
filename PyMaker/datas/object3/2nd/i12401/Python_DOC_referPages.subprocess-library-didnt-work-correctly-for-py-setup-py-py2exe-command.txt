Natural Text
I tried to write a code that can execute python codes easily.but when I used  library such:I saw just this resultplease help me please
Most likely the commands you are trying to run are producing a , which your code does not display. It is possible to send the  messages to  if you don't want to handle it separately.I'll use a different command in the subprocess that is relatively safe. And I will break it up a little instead of having one long line.See that I added the parameter , this sends all the error messages to . The  tries to run  and since  is a file that doesn't exists, it will  this message:But you might just want to get the  instead of , and you can add the parameter  like this:Now it prints just the  like this:For additional information, visit the python documentationEditThe documentation recommends using , which can be done like this (updated after comments from J.F. Sebastian) :If you need to handle  in some way, add parameters described earlier in the  examples.


Answer URL
https://docs.python.org/3/library/subprocess.html#module-subprocess
https://docs.python.org/3/library/subprocess.html
