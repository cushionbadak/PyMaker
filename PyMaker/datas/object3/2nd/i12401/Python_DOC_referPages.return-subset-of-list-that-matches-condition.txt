Natural Text
Let's say I have a list of s:And I want to return a list of the elements that meet a certain condition, say:What's a Pythonic way to return a sub- of element in a  for which   is ?A naive approach:Is there a simple way to do this, perhaps with with a list comprehension or  functions, and without the temporary outputList?
Use list comprehension,or you can use the  also,you can actually write the same condition with Python's truthy semantics, like thisalternatively, you can use  function with your , like this


Answer URL
https://docs.python.org/3/library/stdtypes.html?highlight=truth#truth-value-testing
