Natural Text
I´m trying to use argparse of Python but I cannot get a command line parameter.Here my code:But then when I run my python script like:Instead of getting this path it is getting the default value defined ().What I´m doing wrong here?
The standard documentation doesn't mention . Instead there's a class FileType intended for  argument, not for .So I would write something like this:This gives me the following:You may subclass  to something like  which would check if the supplied file is actually a JSON of expected format etc, but this seems to be out of the scope of the question.


Answer URL
https://docs.python.org/3/library/argparse.html
https://docs.python.org/3/library/argparse.html#filetype-objects
