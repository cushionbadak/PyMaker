Natural Text
I am translating the following Kaggle code into Python3.4:In the final lines when outputting a CSV file, there's a Type Errorwhich occurs at the line .I believe this is because Opening a file in binary mode to write csv data to doesn't work in Python 3. However, adding  in the  line doesn't work either. What is the standard way to do this in Python3.4? 
Creating a CSV file is different between Python 2 and Python 3 (as a look into the docs for the  module would have shown):Instead of you need to use(And you should use a context manager to handle the closing of the file for you, in case an error does occur):


Answer URL
