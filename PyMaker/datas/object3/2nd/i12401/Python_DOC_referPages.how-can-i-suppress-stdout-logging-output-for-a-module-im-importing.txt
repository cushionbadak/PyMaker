Natural Text
I'm importing a module  which uses Python's  module. However,  produces a huge amount of logging output, and I need to use  to communicate important information to the user, which is largely being drowned out by the ridiculous output of the module I'm importing.How can I disable the module's ability to log to  without modifying 's code? I still want it to log to the files it logs to, but I don't want it logging to .I have tried the following:and
I'm referencing this article.In general, if you want to capture anything written to  you can use the  in Python 3:On Python 3.4 and older,  can be implemented like this:If the library has any C bindings that print using  then it gets more complicated. See the article.The easiest case is when you're running another program using ,then all  output can be easily captured.
Try the following:


Answer URL
https://docs.python.org/3/library/logging.config.html#module-logging.config
