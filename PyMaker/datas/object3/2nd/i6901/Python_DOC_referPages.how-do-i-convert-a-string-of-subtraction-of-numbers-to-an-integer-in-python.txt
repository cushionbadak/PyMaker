Natural Text
This question already has an answer here:Evaluating a mathematical expression in a string                    14 answers                So, I know how to convert basic strings into integers, but how does one convert a string of subtraction (or any other mathematical function)? This doesn't work:So how can I make it to work? c must be equal to 8.
You can use  to evaluate  as the following:Output:
You can use regex:Output:
Use the builtin .Or use the  module:
Get them with a simple regular expression and then sum them up:Note that there are no security measurements yet (i.e. a float number won't work) - you'll need to precise your requirements here.


Answer URL
https://docs.python.org/3/library/ast.html#ast.literal_eval
https://docs.python.org/3/library/stdtypes.html#str.split
https://docs.python.org/3/library/ast.html#ast.literal_eval
