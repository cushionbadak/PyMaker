Natural Text
I have a file with following format:So let's say I am only interested in device with  and I want to extract its tag number(this is configurable and will be changed from some other code). So I need to extract tag number of the device id 2. How can I do this in python. I have done following:Thanks
You can read the next line using  try to use this code:
With re.search function:The output: - reads the file contents (as text) - constructing regex pattern, so it would become  where  is one or many whitespace characters(including newlines) and  is the 1st captured group containing tag number - extracting the value of the 1st captured/parenthesized group  from the match object 



Answer URL
https://docs.python.org/3/library/re.html#re.search
https://docs.python.org/3/library/re.html#match-objects
