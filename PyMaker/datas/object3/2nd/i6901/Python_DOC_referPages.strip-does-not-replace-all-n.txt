Natural Text
I'm trying to remove all  in this string. However the  method does not entirely clean the textThe result is How to remove them all ?
Use  to replace '\n' for '' (empty string):
You have '\n' and '\t' to be replaced by '' and ' ' respectively. So you can use 
If you want to remove only the duplicate line feeds, you can use a regex through :Or to remove them all:
Use  not :This method will replace the old  with a new . In your case, you want to 'replace' the   with nothing . As seen belowthis will return a new  which you can re-assign to body:now  is:so if you finally want to remove the   you can just do a further  on them as you said above:


Answer URL
https://docs.python.org/3/library/re.html#re.sub
