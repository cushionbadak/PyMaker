Natural Text
Here is a list I do this in python:output:I want to know what's wrong with my code and find an more efficient way to achieve this.
You can avoid keep re-indexing and building a set first - simply build a  iterating over  backwards as the dictionary will only keep the last value for a key (in this case - the earliest appearing index), eg:This way you're only scanning the sequence once.
This code would work as you expected in Python 2. In Python 3,  returns an iterator. You could, e.g., convert it to a list:
Try this:or append this:


Answer URL
https://docs.python.org/3/library/functions.html#map
