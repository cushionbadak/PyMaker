Natural Text
I am having trouble deriving the overall sum of string values.While I am able to generate the following if there are in integers form:If I used , I will be able to get output such as However, instead of having integers in the values of the list, I got string as follows:And if I used the same command to generate the sum of values, while there are no errors, the sums are clearly wrong in which the output is 
You can use  to map the values to s:Note: if you have values that aren't integers this will raise a  exception.Don't really understand why you had the  in your base case. 
Simply use Also, keep in mind using  is typically considered an antipattern. If you want type checking check out Python 3.6 type annotations or use a different language.


Answer URL
https://docs.python.org/3/library/typing.html
