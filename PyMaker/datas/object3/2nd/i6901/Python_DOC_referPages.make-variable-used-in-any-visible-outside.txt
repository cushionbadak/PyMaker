Natural Text
How to print the match string outside of the if statement?says:
You can use a generator, and call for the :Here we use  in the  as a fallback value if no such  can be found that matches and check for .Note that this will match only the first of the matches. If there are more, these will not be emited. You can however alter the code by using a  loop:
As the  document says:Return  if any element of the iterable is true. If the iterable is  empty, return .And you can not access the intermediate variable used within  (scope of generator expression) outside the scope of it. In order to achieve this, you may instead do:
You just can't, the variable is declared only inside the generator.You'll have to add another for loop for that:But you can avoid nesting for loops using 
I agree with @Willem Van Onsem's comments.


Answer URL
https://docs.python.org/3/library/functions.html#any
