Natural Text
How to index a list using a variable?Using the below example, how can  and  be used to return the equivalent of ?
 is a list, not an integer. If  was set to  you can just use that to index:If you were looking to store the operation as a variable (not just the index, but that you are indexing in the first place), use functions or helper objects from the  module.For example, you can store a  object:


Answer URL
https://docs.python.org/3/library/operator.html
https://docs.python.org/3/library/operator.html#operator.itemgetter
