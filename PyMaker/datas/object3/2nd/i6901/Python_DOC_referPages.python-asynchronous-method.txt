Natural Text
I'm having trouble with async methods. I've already tried many examples from stack overflow, such as:The problem with those approaches is that they need to specify the number/methods before executing. What I need is a way to call a specific method many times from the terminal, but it can't be stuck on the last call.
You can use the  module:When executed, you can type "foo" and  will run in the background. You can, of course, change  to whatever you like (like ), and with a little work, you could even get some arguments working.


Answer URL
https://docs.python.org/3/library/multiprocessing.html
