Natural Text
To filter list of strings by another list of strings in Python we can use the following code:But how can we filter list of substrings by another list of strings? For example:Result should be:
You can use something like that:
Elegant way to achieve this is via using  with a list comprehension as:Here  will return  if any of string in  is present as substring in . As soon as it finds the match, it will short-circuit the iteration (without checking for other matches) and will return the result as . Due to the short-circuiting property of , it won't make the unnecessary iteration across the entire list, resulting in better performance.


Answer URL
https://docs.python.org/3/library/functions.html#any
