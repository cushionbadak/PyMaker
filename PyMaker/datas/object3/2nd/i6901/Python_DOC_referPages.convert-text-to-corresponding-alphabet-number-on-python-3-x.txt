Natural Text
I want to be able to have an input where someone can type a sentence which will convert the letters to its corresponding number i.e. a-1 and z-26
If  is a variable, you can do something like this: could only take the values 'a', 'b', ..., 'z'.
Call  will give you You can tweak the output format a little bit, not sure if this is what you want?


Answer URL
https://docs.python.org/3/library/functions.html#ord
