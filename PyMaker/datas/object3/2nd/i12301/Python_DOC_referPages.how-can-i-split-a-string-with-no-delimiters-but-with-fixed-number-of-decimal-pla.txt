Natural Text
What is the best way to split the following string into 6 float values. The number of decimal points will always be six.The output should read:
Assuming that you want to get  instead of  since that would be consistent I suggest using a regex in combination with the  function as follows:Giving:Update due to comment:You could replace  with  which is equivalent since  matches a digit (number) as shown here.
This should do the trick.


Answer URL
https://docs.python.org/3/library/re.html#module-re
https://docs.python.org/3/library/re.html#re.findall
