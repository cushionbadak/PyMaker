Natural Text
I have tried to create a word counter in python, but my code fails to run.Could you please help me fix the problem?The current result is 
You could try the below code:Here, I'm using  instead of just . Because  returns a number and it's an  object. And you're doing , and Python doesn't understand what do you really want to do. Let's see:So you have to convert the  (which returned by ) to a  object use  function.For example:You can also use  instead of two . Which can auto convert all objects to , also it's more readable than your code.So just use:
You have an error:Or:
 takes a string input. If you wish to print, you must use .  returns an integer value.  converts it to string. 


Answer URL
https://docs.python.org/3/library/stdtypes.html#str
https://docs.python.org/3/library/stdtypes.html#str.format
