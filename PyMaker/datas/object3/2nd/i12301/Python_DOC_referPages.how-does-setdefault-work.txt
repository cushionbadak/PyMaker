Natural Text
I am using 'setdefault()' in my program but I'm not sure what it actually does...so I'm asking on here if anyone one knows?thanks
The docs are here.  is something like the "lvalue" analog of , which is typically an "rvalue" (occurs on the righthand side of assignments — it retrieves a value, as opposed to mutating the state of something).Informally,  means "Give me . What,  isn't in ? OK, then assign , just in time; now give me ."Suppose . ThenHere,  is in , so  leaves  unchanged and returns . However,Because  is not in ,  returns 100 and sets .  now has two items, and both  and  are keys:


Answer URL
https://docs.python.org/3/library/stdtypes.html?highlight=setdefault#dict.setdefault
