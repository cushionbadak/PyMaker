Natural Text
I want to write a program that asks a user how many numbers they wish to process, then ask for that many numbers. Then it will find the total and the maximum of the numbers processed. Example output:One thing I am lost on is that if if I have the code and  gets assigned multiple values, how do I extract both those values?
Set , then do a  loop where each time you ask for input and add the value to the list via .Extract the values back out using the same  loop to cycle through the list.For what you want, you could also just use the built-in functions max and sum:



Answer URL
https://docs.python.org/3/tutorial/datastructures.html?#list-comprehensions
https://docs.python.org/3/tutorial/datastructures.html?#more-on-lists
