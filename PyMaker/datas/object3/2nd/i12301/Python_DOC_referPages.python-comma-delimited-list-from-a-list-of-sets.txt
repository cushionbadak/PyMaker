Natural Text
I have a list that looks like this:Is there a way to come up with a comma-delimited "values" as: I think I can do something like:but was wondering if I can do it in less code.
You can save a few lines thanks to comprehension:Demo:

This comprehension does it easily:
is one way to create a comma-delimited sequence based on an input list/tuple of strings. In this case the result is In your case, your command might look like this (assuming you want to join 'values' of each dictionary element:


Answer URL
https://docs.python.org/3/tutorial/datastructures.html#list-comprehensions
