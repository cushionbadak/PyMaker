Natural Text
In Learning Python 3rd, I saw this codeI want to know exactly what happens after calling this  method, which only returns the method by .Why is the result of the last line ?There is no code that execute the returned function with the original argument.
The  class does not have a  attribute, so Python falls back to the  method. From the  special method documentation:Called when an attribute lookup has not found the attribute in the usual places (i.e. it is not an instance attribute nor is it found in the class tree for ).The wrapped object (the list object with ) does have a  attribute (a method), so  returns it.The returned method is called, passing in , appending that to the  list object.You can easily reproduce the steps yourself:


Answer URL
https://docs.python.org/3/reference/datamodel.html#object.__getattr__
