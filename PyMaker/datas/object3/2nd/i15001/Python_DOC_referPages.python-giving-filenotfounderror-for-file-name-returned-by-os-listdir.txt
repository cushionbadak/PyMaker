Natural Text
I was trying to iterate over the files in a directory like this:But Python was throwing  even though the file exists:So what is wrong here?
It is because  does not return the full path to the file, only the filename part; that is , when open would want  because the file does not exist in the current directory.Use  to prepend the directory to your filename:(Also, you are not closing the file; the  block will take care of it automatically).
 returns a list of file names in . So unless  is your current working directory, you need to join those file names with the actual directory to get a proper absolute path:


Answer URL
https://docs.python.org/3/library/os.html#os.listdir
https://docs.python.org/3/library/os.path.html#os.path.join
https://docs.python.org/3/library/pathlib.html#pathlib.Path.iterdir
