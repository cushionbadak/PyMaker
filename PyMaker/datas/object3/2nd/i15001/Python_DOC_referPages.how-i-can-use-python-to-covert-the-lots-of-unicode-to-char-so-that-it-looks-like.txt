Natural Text
I got it in javascript:I want to transfer this unicode into string so that I can know what code it runs.Please help me convert this to python 
In Python 2, use the  function to convert an integer to a Unicode codepoint; In Python 3, replace  with .Demo:Note that I had to replace the  integer literals with ; leading zeros in Python 3 are a syntax error, in Python 2 they are interpreted as octal literals (and  is not a valid octal number). I used the  function to convert from a string representation to a list of integers.
If that is a list of values obtained from (or equivalent) you can use  to invert the process.In formatted code, this string would be


Answer URL
https://docs.python.org/3/library/functions.html#chr
