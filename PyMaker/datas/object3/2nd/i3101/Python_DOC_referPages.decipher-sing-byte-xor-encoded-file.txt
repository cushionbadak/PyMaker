Natural Text
I have a binary file single-byte xor encoded with a single byte, I tried to brute force files and compare the hash of the generated file with original and couldn't find it, I confirmed the original hash with the author.the code I wroteit will generate 255 .bin file then I compare the hash of the files and didn't find it.
By using the stop value of  in your , you are actually missing  itself.Excerpt from 's documentation:For a positive step, the contents of a range r are determined by the  formula r[i] = start + step*i where i >= 0 and r[i] < stop.You should instead use  as the stop value.


Answer URL
https://docs.python.org/3/library/stdtypes.html#ranges
