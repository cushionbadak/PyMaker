Natural Text
This question already has an answer here:How can I convert a string with dot and comma into a float in Python                    9 answers                my question is simple.I got my string :I want to convert it to a number but when i compile i got the following message error :ValueError: could not convert string to float: '0,0127'Is there another way to convert it to a number ?
Using Ex:Output:
The reason this isn't working is because the decimal type only recognizes periods () for the decimal delimiter as this is what is common in, e.g., english. You could manually change the string or doWhich should work.


Answer URL
https://docs.python.org/3/library/functions.html#float
