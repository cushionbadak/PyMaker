Natural Text
This question already has an answer here:What is the purpose of the -m switch?                    2 answers                What does the  mean while creating a virtual environment?For example, .What does the  mean? What's the difference of  and ?Thanks.
What does the -m mean?From python manual page:-m module-name  Searches sys.path for the named module and runs the corresponding .py file as a script.  will tell Python (the Windows version uses ) to use execute the  module and create a virtual environment that will be stored in the  directory. From virtualenv documentation:Virtualenv has one basic command:Where  is a directory to place the new virtual environment.What's the difference of venv and virtualenv?venv and virtualenv are just shorter versions of virtual environment, meaning that venv is the same as virtualenv -- there is no difference.


Answer URL
https://docs.python.org/3/library/venv.html
