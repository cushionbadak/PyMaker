Natural Text
I have this list. I want to make a  loop that will use in a function combinations of these files in the list. I am not sure how to make these combinations that for each 'check' it will take the correct combination.The function if it wasn't for the loop it would look like this:What I've tried:Here's the list.start with this:where returns:Finally:gives:Other method:gives:I have tried sorting the list but it just ruins it in a different way when put through the processes mentioned above.The ideal result is this when trying this:to give this:where will be put in like this:
zip the list into chunks of check3, check2… Then you can do your for loop.( would output 


Answer URL
https://docs.python.org/3/library/functions.html#zip
