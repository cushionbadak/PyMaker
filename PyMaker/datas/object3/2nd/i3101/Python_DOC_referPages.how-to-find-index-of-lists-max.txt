Natural Text
There is a list:I tried to find index by typing something like this ,but it didn't work. Instead of printing '3' it printed '0'.Why did it do that, and how do I fix it?
Your example works fine:If you want to avoid going through your list multiple times you may want to use something like:It also works if those integers are actually strings:Also, as pointed out by RoadRunner in the comments, you can also use :


Answer URL
https://docs.python.org/3/library/operator.html#operator.itemgetter
