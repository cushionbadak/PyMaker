Natural Text
According to the documentation  inherits :  ->  -> . Consequently, it is supposed to implement  and , but it doesn't seem to be the case, because I get an . A minimal example:Data sample:Test code:Exception: Am I missing something? UpdateThe funny thing is that you can manually get an  out of  wrapping a byte stream and the  method will work. Seems like  instances returned by  are not the same as  instances, though the latter is directly imported from .
Yes, you are indeed missing a critical detail:  wraps around an existing  object:class io.BufferedReader(raw, buffer_size=DEFAULT_BUFFER_SIZE)A buffer providing higher-level access to a readable, sequential  object.And because you've opened your gzip file in text mode, it is not a  instance:In other words, this is a user error.If you wrap a  around a binary file you'll see that the  method works just fine:


Answer URL
https://docs.python.org/3/library/io.html#io.BufferedReader
https://docs.python.org/3/library/io.html#io.RawIOBase
