Natural Text
Given an array  using Python 3, how can I add all the elements in the array?The above code did not work. Also in the case, if you're given an array with an unknown amount of arrays inside arrays, How can those numbers be calculated?

You need a flatten function. Here is one:which you can then use in , for example:
You can use  to sum this nested listIf the list can be more than one level nested, you have to use a recursive approach
You can use the closure property for finding sum of infinite nested list.like


Answer URL
https://docs.python.org/3/library/array.html
