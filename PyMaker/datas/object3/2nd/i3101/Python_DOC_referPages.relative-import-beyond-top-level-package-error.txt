Natural Text
I have a project named  which has two apps  and ,i have  file in both of the apps to serialize data. So when i try to relative import the serializer class from  to another app , the relative import shows error.1- 1st i tried full path , it gave error in console that 2- Then i tried ,its showing error .So what am i doing wrong? How to do relative import properly?@editadding init.py in srcStill same error persisting...[![User Serializer class in  app][4]][4]
Seems you are missing  in  folder.From the python docsThe  files are required to make Python treat the directories as containing packages; this is done to prevent directories with a common name, such as , from unintentionally hiding valid modules that occur later (deeper) on the module search path. In the simplest case,  can just be an empty file, but it can also execute initialization code for the package or set the  variable, described later.Remove all relative imports used from your project/app and use absolute import, becauseAbsolute imports are recommended, as they are usually more readable and tend to be better behaved (or at least give better error messages) if the import system is incorrectly configured Update - 11. Change in Commented out two lines, because of their  doesn't have any 2. Change in Change  to 


Answer URL
https://docs.python.org/3/tutorial/modules.html#packages
