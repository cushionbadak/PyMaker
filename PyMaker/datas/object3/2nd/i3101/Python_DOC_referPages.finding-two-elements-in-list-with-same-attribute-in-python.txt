Natural Text
I want to find any pair of elements in a list that have the same attribute. For example,So if comparing on , I'd be looking for  or  or  or . However, there's no guarantee that the list would necessary have any elements with the same property, e.g.might also be a valid parameter to this function.The obvious way to do this seems to be:But it seems a bit inelegant and I'm wondering if there's a nicer way to do this.
 offers an O(n) solution to group objects by attribute:The result indicates one object with , two objects with  and three objects with :To extract pairs of objects with the same attribute, you need only to filter items in the dictionary with a value with length greater than 1. Then use  to extract all combinations for these keys.


Answer URL
https://docs.python.org/3/library/collections.html#collections.defaultdict
