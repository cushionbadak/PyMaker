Natural Text
I know how to use property setters to perform actions every time an attribute of a class is modified to avoid having to code in every action every time the variable is changed.I wanted to know if it was possible to do the same for mutables, like lists and dictionaries ?What I want to achieve is the following,I have a dictionary with  an instance of a class which has an attribute called .when I add a new  pair to my dictionary, and that the attribute  of the object is , then I also add the  to a list called .
You'd have to use a custom class; you could subclass  or  , and override the appropriate container special methods to detect changes.For example,  is translated to , letting you inspect  and act on that:Do look over the other methods that  objects implement; you may want to override  too for example.


Answer URL
https://docs.python.org/3/library/collections.html#userdict-objects
https://docs.python.org/3/reference/datamodel.html#emulating-container-types
https://docs.python.org/3/library/stdtypes.html#dict
