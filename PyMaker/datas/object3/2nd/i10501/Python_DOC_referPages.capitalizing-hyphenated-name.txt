Natural Text
I have a script looking like this:It will work nicely with simple names like jack black or morgan meeman but when I input hyphenated name like  then I'd expect  but I receive .How can I get python to capitalize the character right after hyphen?
You can use title():Example from the console:
Use insteadSplit the argument into words using str.split(), capitalize each word  using str.capitalize(), and join the capitalized words using  str.join()
I'd suggest just using str.title, here's a working example comparing your version and the one using str.title method:Resulting into this:


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.title
