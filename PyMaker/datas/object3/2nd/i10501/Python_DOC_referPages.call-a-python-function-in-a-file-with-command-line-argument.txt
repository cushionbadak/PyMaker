Natural Text
I'm trying to pass arguments from Node.js to Python with  spawn. I also want to invoke the specific Python function with one of the argument that I specified in the Node.js array.The second argument here is , which should call the  Python function.:If I try to run this Python file without any Node.js from the command line, the execution looks like this:Where , , and  are just command-line arguments, but  should call the  function, which will use the ,  arguments for .
I'd recommend using argparse to parse the command line arguments. Then you can use eval to get the actual function from the input.


Answer URL
https://docs.python.org/3/library/argparse.html
