Natural Text
This question already has an answer here:Removing from a list while iterating over it                    4 answers                How to remove items from a list while iterating?                    23 answers                I am trying to clear a python list by removing every element in loop by codeI thought that after this code x must be [], but instead only every second element of list is removed. len(x)=5000 instead of 0. Why is it so? What am I doing wrong.Thanks
If you want to clear a python list like you're doing, the right way is just using , docs about that method here, now, if you want to remove elements using some fancy conditions, just use filter, example clearing the whole  list:
The a.remove(i) messed up the indexing is my guess.instead useIts a good way to clear a list.
If you want to implement a list object that erases itself while iterating over it that would be fairly easy:



Answer URL
https://docs.python.org/3/tutorial/datastructures.html#more-on-lists
