Natural Text
Here is a code i would like to, insert the value of "K" and "v" in cmd  after robocopy command  , so that with in the for loop it will  perform robocopy for all the  source and destination mentioned in dictionary  D = {} i would also like that the script checks for failures in the robocopy  output logs in error.log file  if two files have failed then the script should send a mail to some email address. 
The way in Python to do it would be:or, in Python 3.6:However, this is wrong! It will fail if  and  have spaces, and it can be a security hazard (imagine if ). The right way to spawn subprocesses is with this:


Answer URL
https://docs.python.org/3/library/subprocess.html#security-considerations
