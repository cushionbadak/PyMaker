Natural Text
This function already does what I want. But is there a way I could simplify the nested ifs here?This is the sample input-output:
You can combine the s like thisYou can even shorten it with  function, like thisYou can even simplify it further like thisSince Boolean values are integers in Python, if  evaluates to be Truthy, the value will be taken as , otherwise . So, we pick the corresponding operation from the tuple. Then we check if any of the items in the list match that condition. This can also be written asNote: Never name your lists as , as that shadows the builtin  function.


Answer URL
https://docs.python.org/3/library/functions.html#any
https://docs.python.org/3/library/functions.html#func-list
