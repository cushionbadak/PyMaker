Natural Text
I am trying find an efficient way of creating a list of dates only including the first day of the month for a given period. Something like this but better:Alternatively, some direction on what to Google for this. Cheers!
For Python 3, you'll need to use  instead of , and  (floor division) instead of  (which does float division in Python 3):
If you're only creating the list for a few years then efficiency should not be a concern. Clarity of code is the most important aspect.
There is no reason to bruteforce this:Output:
With pandas :result : 
You can use relativedelta from dateutil, and then create a function to use any date range:


Answer URL
https://docs.python.org/3/reference/expressions.html#binary-arithmetic-operations
