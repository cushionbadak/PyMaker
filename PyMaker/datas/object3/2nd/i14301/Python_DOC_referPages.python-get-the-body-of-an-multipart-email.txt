Natural Text
i get an email with the following code:FYI: This is always a part of the examplecode. But body is now a biiig list of objects. If the Content_Type would be Plaintext, it would be much easier. How can i get access to the body of that mail now? 
Short answerYou have a multiparted email. That's why you're getting a list instead of a string:  returns a list of  if it's a multipart message, and  if it's not.ExplanationFrom the docs:Return the current payload, which will be a list of  objects when  is , or a string when  is .Hence  returning a list.Your code for getting the body would be something like:Again, from the docs:Note that  returning  does not necessarily mean that "msg.get_content_maintype() == 'multipart'" will return the . For example,  will return  when the  is of type message/rfc822.


Answer URL
https://docs.python.org/3/library/email.message.html#email.message.Message.get_payload
https://docs.python.org/3/library/email.message.html#email.message.Message.is_multipart
