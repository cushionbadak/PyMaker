Natural Text
I want to create a list of possible combinations of a list of lists (example will explain better)The result should beThe length of the list can vary, and the length of the lists within the variable list can also vary. How would I make this/these loop(s) programmatically? (preferably explained in Python or pseudo-language)
That's what  is for:

You can do it recursively:



Answer URL
https://docs.python.org/3/library/itertools.html#itertools.product
