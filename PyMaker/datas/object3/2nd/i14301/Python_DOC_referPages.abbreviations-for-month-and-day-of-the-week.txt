Natural Text
I am trying to write regex to capture the output of I was wondering what are the possible outputs for the abbreviations of day of the week and month?
Here's some information about the formats of the string  produces. Basically it's always a fixed length string with that basic format.
Python  class does not invoke C  function i.e., it works the same even on platforms that do not provide C  (the equivalent of ).CPython source code says that the format is:You could parse it using  in C locale.The above is (almost) the same as the corresponding  definition:


Answer URL
https://docs.python.org/3/library/datetime.html#datetime.datetime.ctime
