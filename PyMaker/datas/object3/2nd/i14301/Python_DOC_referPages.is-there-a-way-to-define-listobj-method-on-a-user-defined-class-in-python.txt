Natural Text
One can can implement the method  to return a string. Is there an  counterpart? ( doesn't work!)Is .toList() the way to go?
There is no direct counterpart, but  is implemented by iterating over the argument. So implement either the iterator protocol () or bound indexing ( and ).


Answer URL
https://docs.python.org/3/library/stdtypes.html#iterator-types
