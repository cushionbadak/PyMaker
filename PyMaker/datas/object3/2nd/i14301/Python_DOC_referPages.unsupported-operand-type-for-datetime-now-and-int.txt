Natural Text
I want to compute elapsed time from  to . The way I store datetime in  is:The way I want to compute elapsed time is:But as a result I get this error message . I guess the solution is to somehow convert values stored in session, but I do not know how.
It appears that your session stores datetime objects as seconds since the UNIX epoch; probably because  objects themselves are not serialisable.Convert back to a  object first:You may want to look into Session serialisation in the Django documentation if you want to handle  objects transparently.


Answer URL
https://docs.python.org/3/library/datetime.html#datetime.datetime.timestamp
https://docs.python.org/3/library/datetime.html#datetime.datetime.fromtimestamp
