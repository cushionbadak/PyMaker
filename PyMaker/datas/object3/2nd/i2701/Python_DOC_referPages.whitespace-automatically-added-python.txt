Natural Text
I'm running this code to print a triangle pattern but I can't seem to understand why two extra whitespaces are added in between.the output of program is the extra white spaces are denoted by letter 'd'.
 automatically adds spaces between its parameters:You can concatenate the strings manually to solve this problem:or use :From the documentation:Print objects to the text stream file, separated by  and followed by .Note that argument  defaults to a single space.
 outputs each argument with a space as the default separator. You should use the  parameter if you want no space in between the output of the arguments:
It's due to the default separator between items.You can customize it with the  keyword argument:


Answer URL
https://docs.python.org/3/library/functions.html#print
