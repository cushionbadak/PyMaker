Natural Text
I have had it up to here with Python's import system... thought I'd finally got something reliable and then the inexplicable happens!This is the directory structure of my application:I run all my tests through , which looks like the following (in an attempt to resolve all the import problems):This worked like an absolute charm, until I made one modification to the file  - I added a local function call. So e.g. my_module.py:UPDATE: THIS works fine:As soon as I added that local function call, the tests stop working, with the error .The  looks like this (basically):Note that in  I am using  as the first part of my import statement, because I am using the  file that sets  as a system path. If I change the import to start with  I get the same error referring to .I am still learning python, so suggest any change to my application structure you like. I can't believe the hassle of this import system - I do feel like I'm missing something basic here...Thank you in advance!
Managing import paths manually is difficult.A  is the best way to manage python packages.  by convention package names should use  not .Create a  with this content next to I recommend an application structure like the following:Install package locallyThis will symlink (magically) install your package into the python package pathNow in any scripts you can use paths as you'd expect e.g.  also recommend you use a virtualenvMore on  here


Answer URL
https://docs.python.org/3/library/venv.html
