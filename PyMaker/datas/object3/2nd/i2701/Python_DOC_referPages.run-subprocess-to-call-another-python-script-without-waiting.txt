Natural Text
I have read way to many threads now and really lost.Just trying to do something basic before I make it complicated.so i have a script test.pyI want to call the script from within runme.py but without waiting so it will process the other chunk of code, but then when it gets to the end wait for test.py code to finish before continuing on. I cant seem to figure out the correct syntax for the p = subprocess.Popen (I have tried so many)and do I need the that to the test.py if its in the same directory? here is what i have but cant get to work.
I cant seem to figure out the correct syntax for the p = subprocess.Popen (I have tried so many)You want to pass it a list of arguments. The first argument is the program to run,  (although actually, you probably want  here); the second is the script that you want  to run. So:and do I need the that to the test.py if its in the same directory?This will work the same way as if you ran  at the shell: it will just pass  as-is to , and  will treat that as a path relative to the current working directory (CWD).So, if  is in the CWD, this will just work.If  is somewhere else, then you need to provide either an absolute path, or one relative to the CWD.One common thing you want is that  is in not necessarily in the CWD, but instead it's in the same directory as the script/module that wants to launch it:… or in the same directory as the main script used to start your program:1Either way, you just pass that as the argument:1. On some platforms, this may actually be a relative path. If you might have done an  since startup, it will now be wrong. If you need to handle that, you want to stash  in the main script at startup, then pass it down to other functions for them to use instead of calling  themselves.


Answer URL
https://docs.python.org/3/library/sys.html#sys.executable
https://docs.python.org/3/library/asyncio-subprocess.html
https://docs.python.org/3/library/threading.html
