Natural Text
I want to create a dict from a list of strings:However, I get I can't understand why, as  returns 2 values, as can be seen from my previous checks ( returns 2)My desired output:
It returns two values but the loop expects a list of two values.You have this: But the code expects this: You could do:Note that you'll get all the data and not just one item.You may want to parse the list using  because currently its a string:


Answer URL
https://docs.python.org/3/library/ast.html#ast.literal_eval
