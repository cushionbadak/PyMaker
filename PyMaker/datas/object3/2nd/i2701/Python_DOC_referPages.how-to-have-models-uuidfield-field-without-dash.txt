Natural Text
I tried to have  without dash, by using  (Instead of )However, the resultant result still come with dash.May I know how to have models.UUIDField field without dash?. I would like to store in database without dashes, and use it without dashes.
Are you using PostgreSQL by any chance? The  may be using the native uuid type for the column. It stores it efficiently using only 16 bytes (without dashes). If that is the case, it is not storing the dashes, only showing them when you . The good news, is that in python code, you are getting a UUID object, so you can do  to get a string without dashes.
Use  field instead of ,


Answer URL
https://docs.python.org/3/library/uuid.html#uuid.UUID
