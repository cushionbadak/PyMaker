Natural Text
This question already has an answer here:How to create a list of empty lists                    4 answers                I need to create empty arrays inside an array in order to fill each one of them differently in a loop later on but I couldn't figure out the correct syntax. I tried something like this:Bu it gives a syntax error. I really appreciate some help.
Everything is an object in Python. And Lists can accept any type of data. So List can accept Lists too. Using this you can just place lists in a list. This will create the following
here's my examplealso, check this out for referencing:Access item in a list of lists
i think this will help you (if i understand your question correctly):


Answer URL
https://docs.python.org/3/tutorial/datastructures.html#dictionaries
