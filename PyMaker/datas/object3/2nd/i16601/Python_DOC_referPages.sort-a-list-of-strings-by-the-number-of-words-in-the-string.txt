Natural Text
I have a list of strings as such:  I want to sort this in such a way that the string with the largest number of words is first, ie,  Its a bit tricky, since if I sort in place by length  I'm back where I started.  Has anyone come across a graceful solution to this?  Thanks.
Assuming that words are separated by whitespace, calling  with no arguments returns a list of the words that a string contains:Therefore, you can get the output you want by sorting  based off of the length of these lists:You will also need to set the  parameter of  to , as shown above.


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.split
