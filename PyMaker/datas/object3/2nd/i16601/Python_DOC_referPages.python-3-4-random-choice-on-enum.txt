Natural Text
I would like to use random.choice on an Enum.I tried :But this code is not working, how can I do that ?
An  is not a sequence, so you cannot pass it to , which tries to pick an index between 0 and . Like a dictionary, index access to an  instead expects enumeration names to be passed in, so  fails here with a .You can cast it to a list first:This works because  does support iteration.Demo:


Answer URL
https://docs.python.org/3/library/enum.html#iteration
