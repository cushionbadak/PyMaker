Natural Text
I tried searching the site for similar problems, but none seemed to have my exact dilemma.For a tutorial book on Python, I wrote a program that converts Celsius temperatures to Fahrenheit. When I run it in the IDLE shell, the program works, but returns an error. Here is the code for the program itself:And here's the error.I am using Python 3.4.1.
You do not add the file extension when importing a Python module.  You should do:This code:is telling Python to import the nonexistent  module that is located in the  package.Here is a reference on importing modules and packages in Python.
You need to drop the  extension; Python imports use just the base name:What happens is that Python imported , then looks for a nested module named . That part fails, because  is not a package and cannot contain another module.


Answer URL
https://docs.python.org/3/reference/simple_stmts.html#the-import-statement
