Natural Text
I was wondering how can I know if when a user inputs a value, that value already exists in a list.For example;user inputs:Now my question is how can I tell the user this value already exists inside the list.
Use  operator:You can also use  which will return the index of the element.If the element is not found, it will raise :UPDATEAs Nick T commented, if you don't care about order of items, you can use :
One more way you can do is use collections :-But the above one is not efficient. So for make it efficient use set as falsetru indicates in the answer:-


Answer URL
https://docs.python.org/3/reference/expressions.html#in
https://docs.python.org/3/library/stdtypes.html#types-set
