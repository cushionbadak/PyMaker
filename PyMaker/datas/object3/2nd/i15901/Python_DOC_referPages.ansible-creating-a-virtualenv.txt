Natural Text
How do you create a virtualenv for a specific python version using ansible. Is there a command in the ansible standard library?I would like something like:
I have at times experienced some erratic behaviour with specifying  (e.g.: getting a python 2.7 executable in my virtualenv even though I specified to run the command with . If you experience that problem, you can create the virtualenv manually with the  module:(note: by specifying  this command will only run in the case that a virtualenv does not exist at ).Then you can install your requirements as normal with the  command: UpdateI've found that specifying the  (available in Ansible 2.0+) also seems to work  in the case stated above. For example:(with this you shouldn't need to manually create the virtualenv first)
You can do it with the  module and a specific  binary:
With ansible 2.0 you can specify a python version for your virtualenv with For example:


Answer URL
https://docs.python.org/3/library/venv.html
