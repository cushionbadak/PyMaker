Natural Text
The snippet above will block itself at . But if changing it tothe content of  will be printed immediately. So why is the  in the first snippet never ready? (Python version: , under Linux)
The problem is that you're waiting outside of the  block. As soon as you exit the  block, the Pool is terminated, which prevents any of your tasks from completing. From the javadocs:Pool objects now support the context management protocol – see Context Manager Types.  returns the pool object, and  calls .... Stops the worker processes immediately without completing  outstanding work. When the pool object is garbage collected   will be called immediately.Moving the call to  inside the  block fixes the issue.


Answer URL
https://docs.python.org/3/library/multiprocessing.html#multiprocessing.pool.Pool.terminate
