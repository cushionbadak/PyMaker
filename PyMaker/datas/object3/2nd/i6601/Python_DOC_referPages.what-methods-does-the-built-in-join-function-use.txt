Natural Text
I'm trying to usebut I getThe python docs says an iterable is accepted as the parameter for  but that seems to contradict what the exception is telling me.I tried defining some of the base class methods in  but it didn't help.str.join(iterable) docs
The doc also says:A TypeError will be raised if there are any non-string values in  iterable...And after all,  is not a String.
Ok, this should do it, not . The joys of a dynamic language, glance at parameter name, but not realize that only certain value types are accepted.


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.join
