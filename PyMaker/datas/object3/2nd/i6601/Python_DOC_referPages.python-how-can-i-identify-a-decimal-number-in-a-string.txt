Natural Text
This question already has an answer here:Checking if a string can be converted to float in Python                    13 answers                How can I identify decimal numbers in a list of strings, in order to remove them? Ideally in a single operation, something like (Sadly, isdecimal() and isnumeric() don't work here)For instance, if  I would like the output to be 
You should use a regex to test if a string is a decimal:
Just adding to Mr Geek answer, You should also check out the python's docs on Regex.


Answer URL
https://docs.python.org/3/howto/regex.html
