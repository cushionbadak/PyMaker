Natural Text
I have the following code:With typical input, the list  looks like this:What is the issue with ? Apparently I am not evaluating it correctly. The concept seems fine, but it seems like set  is not able to take an attribute which is actually an element of a list.what I want to evaluate is:Here's the error I get:
You can't do that kind of indirect attribute access using the dot operator in Python. Use  instead:
I think you want to use , to get an attribute who's name stored as a string in another variable:Your current code is looking for an attribute named  on the  set.


Answer URL
https://docs.python.org/3/library/functions.html#getattr
