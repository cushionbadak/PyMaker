Natural Text
i have attached numpy-array to nodes in a networkx graph. How to store the graph in gexf-format on disk? (without the numpy vector, as its just something intermediate...)However, this breaks. I'm new to python, but to me it seems like the ndarray is not serializable?! So, how to tell networkx to ignore that node attribute?
Use a library like the native , or  for HDF5, to serialize your graph object. For example you can do:The pickled graph can be loaded back into Python by:
I solved this by removing the property "vector" from the data items:full MWE:


Answer URL
