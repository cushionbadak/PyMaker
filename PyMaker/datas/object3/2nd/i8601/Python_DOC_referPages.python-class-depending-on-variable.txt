Natural Text
This question already has an answer here:Changing the class type of a class after inserted data                    3 answers                Is there a way to change objects class dynamically depending on passed variable? For example:
This can be done in a  method for the Parent class.  It gets to make the decisions about what object is really created:The output proves that it works:
Define a conditional check if, else  in the  function of class parent prior to the .


Answer URL
https://docs.python.org/3/reference/datamodel.html?highlight=__new__#object.__new__
