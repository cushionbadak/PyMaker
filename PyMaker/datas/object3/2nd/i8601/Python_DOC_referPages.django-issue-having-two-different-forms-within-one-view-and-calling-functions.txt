Natural Text
I have two different forms which I want to display in one view. When I have something like the following then the form will go through and the information will successfully be updated...However when I change the if statement to include the second form:then this doesn't update any of the information for the models.Is it possible to call is_valid() / save() with two forms at the same time? Thanks
 returns nothing; . The None is falsy value when used as a predicate.So,  is not called because of short-circuit. See ,  operator documentation.You need to replace the following line:with:


Answer URL
https://docs.python.org/3/library/stdtypes.html#boolean-operations-and-or-not
