Natural Text
In Python 3.5 I am trying to get the values from a dictionary like so:Now if I try to convert this into a listI get an error:These expressions are being executed inside :
You're using the  command  from what I can understand. If  follows the interface defined by  this doesn't invoke the  function as you'd expect.Exit the  debugger to get this to work correctly or, again if  uses the same commands as , use  in order to get an expression evaluated inside the debugger.
you can try this:
Keep Coding Keep learning


Answer URL
https://docs.python.org/3/library/pdb.html#pdbcommand-list
