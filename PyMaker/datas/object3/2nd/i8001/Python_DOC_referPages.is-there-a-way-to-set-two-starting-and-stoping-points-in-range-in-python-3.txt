Natural Text
This question already has an answer here:How can I combine range() functions                    3 answers                I want i to range from 65 to 91 and from 97 to 123 (ASCII for upper and lower case letters) omitting 92 to 96, but I can only input one start and one stop value. Is there a way to omit those or set diferent start and stop values?
You can chain the two range objects using :
You can use :
will output:
First, you should not use 'magic numbers' in your code. If 65 is the ASCII code for 'A', say so: . Second, you can combine two lists into one list and iterate over it:


Answer URL
https://docs.python.org/3/library/itertools.html#itertools.chain
