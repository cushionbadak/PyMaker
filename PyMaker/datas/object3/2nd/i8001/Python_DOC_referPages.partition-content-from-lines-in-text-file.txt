Natural Text
I have some data in a text file, which is mostly paths of foldersI want to select the string between "Desktop/" and "/2017", I tried using position but definitely won't work since the strings are different in lengthCurrently I'm usingThe output is up toAny suggestion?
Use split()
You can either use a regular expression:Or direct search:
Use  module
you could use the  module: returns a tuple of the directories (and the file) that make up the path.the module provides more path-related methods in case you need them.


Answer URL
https://docs.python.org/3/library/pathlib.html
https://docs.python.org/3/library/pathlib.html#pathlib.PurePath.parts
