Natural Text
I have a list of UTC time stamps stored as string format as follows:Is there a function in python to convert these strings to unix time? I have tried:but these have not been working for me as these functions are not meant to take arguments. 
You need to convert the string '20170124T1815' to a datetime instance:Then use  method to convert to UNIX time:The documentation:datetime.timestamp()Return POSIX timestamp corresponding to the datetime instance. The return value is a float similar to that returned by .EDITFor Python version < 3.3, you can use:Or, you can use:Thanks to Peter DeGlopper


Answer URL
https://docs.python.org/3/library/datetime.html#datetime.datetime.timestamp
