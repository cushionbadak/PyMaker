Natural Text
I have a class that maps a database objectWith such design, an object is created each time, and we check if it is present in database with .exists attribute.  It works.  But I suspect there is a cleaner way to init, and we would just have to check like that:
You can't do this in , because that method is run after the new instance is created.You can do it with  however, this is run to create the instance in the first place. Because it is normally supposed to return that new instance, you could also choose to return something else (like ).You could use it like this:


Answer URL
https://docs.python.org/3/reference/datamodel.html#object.__new__
