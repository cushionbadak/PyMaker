Natural Text
How can I escape any of the special shell characters in a Python string?The following characters need to be escaped:For example say I have this string:TIA
In Python3, the required batteries are included as .Return a shell-escaped version of the string . The returned value is a string that can safely be used as one token in a shell command line […].In your example:Output:
Not sure why you want to escape everything rather than quote as much as feasible, but, this should do it (replace  with another character not present in your string, if needed):It may be doable with a tad less escape-trickery, but the unfortunate fact that strings, , and the shell, all use the  (backslash) for escaping and other special purposes, does complicate things a bit:-).
 will do the job:Output


Answer URL
https://docs.python.org/3/library/shlex.html#shlex.quote
