Natural Text
How to solve this on Python3, using what lib, and using what sample code?I have html file, at position Line:Col I have middle of html tag ; how to get position of  tag edges (brackets ) and position of its  tag edges?(note: several table tags may be one inside another).
Like said in this SO answer, you should not use regex to parse an HTML file as the standard is highly irregular. You should instead use an HTML parsing library like  : This library offers you  which returns you the line number and offset of the tag.
This gets you the coordinates of each tag with html.parser, where I monkeypatch the goahead function with a simple modification, calling the custom method get_endpos:


Answer URL
https://docs.python.org/3/library/html.parser.html
