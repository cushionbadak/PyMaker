Natural Text
I have some code which gets a list from a text file and puts it into a dictionary. This is the code:Then, using this, I find the top marks and the top average score from the results which are in this list. My current code is:However, it says:Can anyone fix this?
Data read from a file is returned as strings, but  by default expects numeric values. You'l need to convert your data to numbers first.You can use  or  to convert numeric values:You appear to be reading CSV data; consider using the  module to parse the file instead:If your first column is quoted, you can even have the  do the string-to-float conversion for you; using  when creating the  object will result in anything that doesn't use  around the column value to be passed to your Python code as a .


Answer URL
https://docs.python.org/3/library/csv.html
https://docs.python.org/3/library/csv.html#csv.QUOTE_NONNUMERIC
