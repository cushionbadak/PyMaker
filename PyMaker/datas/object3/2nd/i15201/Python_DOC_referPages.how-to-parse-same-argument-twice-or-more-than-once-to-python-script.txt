Natural Text
I have written a python script which uses argeparse module for handling arguments. e.g. Now, I want to give it two options for same argument, likeHow can I do that?
You can do as follows:Prints: 
You want the  action to .  This will accumulate values into a list -- once for each time the command line argument is present.  e.g.


Answer URL
https://docs.python.org/3/library/argparse.html#action
