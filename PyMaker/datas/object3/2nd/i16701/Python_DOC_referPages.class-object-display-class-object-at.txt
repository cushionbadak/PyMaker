Natural Text
I'm wondering that when creating a class in python, for example, a class Car,with the attributes being the make and year of the car, can return the name and the make,without using anything except forinstead of returning is there anyway to just return a tuple of without implementing a  method in the car class?
You'd implement a  method; it is that method that is used when representing an object:This produces a representation string that looks just like the original class invocation:
Use  instead of . represents the objuect, while  is only called with a .To do that, use the following:This will run as:


Answer URL
https://docs.python.org/3/reference/datamodel.html#object.__repr__
