Natural Text
I want to check if any string elements in a list  contains certain keywords in a set . I want to use  but it doesn't work.How should I fix this?
That may happen if you use IPython's  magic:Here's why: and  get shadowed with  functions, and those behave differently than the builtins. This is the reason I stopped using  and started using  so that it doesn't clobber the namespace like that.To reach the builtin function when it is already shadowed, you can try . The name  seems to be available in IPython on both Python 2 and Python 3, which is probably on itself enabled by IPython. In a script, you would first have to  on Python 2 and  on Python 3.


Answer URL
https://docs.python.org/3/library/builtins.html
