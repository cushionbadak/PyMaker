Natural Text
How does one mock the behavior of a function?For example, if you had the following App Engine code that issued a HTTP request, how would you mock the function to make it return a non 200 response?Here is the mock I've written, but I don't know how to mock a non-200 response.
You test really isn't doing much: its simply testing if the function was called with the parameters you passed it.If you wanted  to return a certain value use :So, a quick example of testing your  function when  returns a 500-error:


Answer URL
https://docs.python.org/3/library/unittest.mock.html
