Natural Text
I'm tring to declare type of following function parameter with  module: parameter of  must be  with childclass of  as key and  as value. Actual notation fail with  check:example.py:17: error: List item 0 has incompatible type "Tuple[C, str]"How to declare type of  with typing ?
The code is using the class  itself as a key, not an instance of the .Passing the instance of C class should be fine.If you really need to pass the class itself (I doubt it), you need to use :


Answer URL
https://docs.python.org/3/library/typing.html#typing.Type
