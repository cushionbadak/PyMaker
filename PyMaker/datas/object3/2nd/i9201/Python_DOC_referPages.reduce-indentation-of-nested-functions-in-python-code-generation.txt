Natural Text
My goal is to generate functions dynamically and then save them in a file. For e.g, in my current attempt, On calling The output I want is('/tmp/code.py'):The output I get is('/tmp/code.py'): is indented. How can I reduce the indentation? I can do  etc. but I wonder if there is a better way.
Use the  function to remove the common leading whitespace:Note that there is no need for a  dance here.
There's a function in built-in module, .


Answer URL
https://docs.python.org/3/library/textwrap.html#textwrap.dedent
