Natural Text
I have a .py file containing some functions. SO it looks  like:-Now I have another file which takes input from user and try to call that specific function from first python file.It looks like:-but it is throwing a error :-ImportError: cannot import name 'Test_Suites'  Please give some insight of how to solve this problem.thanks
Like I said in my comment, use  to access a function by its name.
In your code  is a string with the name of the function you want to call.  looks for a function namend string on the object . To get a an attribute with name  from an object  use , so in your case:Also as Tagc points out in the comments  is a bad idea in the way you use it. Instead use  which parses the given string to an integer.If you ever need something more flexible you can use  from the ast module which can also parse lists, dicts, ... (https://docs.python.org/3/library/ast.html#ast.literal_eval)


Answer URL
https://docs.python.org/3/library/ast.html#ast.literal_eval
https://docs.python.org/3/library/ast.html#ast.literal_eval
