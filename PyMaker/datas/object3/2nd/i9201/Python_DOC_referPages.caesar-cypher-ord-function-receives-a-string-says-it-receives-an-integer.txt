Natural Text
I'm supposed to be creating a Caesar-cypher function. It was recommended I use the built-in ord() and chr() functions to help me do this(from the textbook my course is using). That may or may not be the best way(definitely not from what I've looked up), but it's what they want you to do.My problem is that in a for loop when I send the placeholder variable into the ord() function I get an error that it expected a string of length 1, but receives an integer instead. I put a print function just before it to confirm that the variable, c in this case, has the value of 'i' but it seems to fail regardless.Here is the function I created:And this is the error I receive:This error occurs for integers other than -1 as well. To be fair, I'm not totally certain if the code itself does what I want(I've been stuck trying to figure this part out and I don't see the point in ensuring the rest works if this part does not).
 takes  as a parameter and returns :Given a string representing one Unicode character, return an integer representing the Unicode code point of that character. For example, ord('a') returns the integer 97 and ord('€') (Euro sign) returns 8364. This is the inverse of chr().In your code you're passing  instead which gives you the error you see on command line. You don't need to convert  to anything. Just convert the character to number, add the rotation amount and convert the result back to character again with :Note that above doesn't handle the overflow/underflow cases where  is rotated left or  is rotated right.


Answer URL
https://docs.python.org/3/library/functions.html#ord
