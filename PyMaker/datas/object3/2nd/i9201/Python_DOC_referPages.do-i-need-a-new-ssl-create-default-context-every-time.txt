Natural Text
This is my code using a SSL context:Is the default context object a constant that can be shared and reused in a multi-threaded program? I mean creating it just once:and then:for every message sent.
Each connection should have its own context. You can see in the Python source code for http.client, that HTTPSConnection creates the new context for every connection.https://github.com/python/cpython/blob/master/Lib/http/client.py
Indeed  can be used again (as it's purpose) after initializing it:(220, b'2.0.0 Ready to start TLS')↳ https://docs.python.org/3/library/ssl.html#best-defaults


Answer URL
https://docs.python.org/3/library/ssl.html#best-defaults
