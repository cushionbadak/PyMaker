Natural Text
So I have a jq command that converts my json array into a json object. The command works normally in the windows terminal, but when I try to implement it into my python program i get this: jq: error: Here is my jq command which works and puts the transformed json into newfile.json:Here is my subprocess call in my python program:
Open the output file yourself, and you won't need to rely on shell behavior (which is, obviously, not working in the present case) to do it for you.


Answer URL
https://docs.python.org/3/library/json.html
