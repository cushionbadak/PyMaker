Natural Text
I have the following This is part of a multi level application. In level one, the command I care about is of the format  (for example ). So I'd except to get  and .So far so good. However, if I include a flag, all hell breaks loose (). This will return a . But I want to get the same as before:  and .Why is this failing?
I think you can use . Please see https://docs.python.org/3/library/argparse.html#nargs.


Answer URL
https://docs.python.org/3/library/argparse.html#nargs
