Natural Text
I want to filter strings in a list based on a regular expression.Is there something better than  ?
You can create an iterator in Python 3.x or a list in Python 2.x by using:To convert the Python 3.x iterator to a list, simply cast it; .
Full Example (Python 3):For Python 2.x look into Note belowPrints:Note:For Python 2.x users,  returns a list already. In Python 3.x  was changed to return an iterator so it has to be converted to  (in order to see it printed out nicely).Python 3 code example Python 2.x code example
Just in case someone comes here in future, there is another pythonic way to do it. First you need to create the  and then then results:


Answer URL
https://docs.python.org/3/library/functions.html#filter
