Natural Text
I'm using Python's . When I implement the do_GET method I find myself parsing by hand  looks something like:How should I parse it in order to get a dict like Thanks,
Use  from the  module, but make sure you remove the "/?":Note that each parameter can have multiple values, so the returned dict maps each parameter name to a list of values.
Considering self.path could potentially be hierarchical, you should probably do something like the following :
In case somebody needs it for Python3: was renamed to  in Python3.
The cgi and urlparse modules have that: https://docs.python.org/2/library/urlparse.html#urlparse.parse_qs


Answer URL
https://docs.python.org/3/library/urllib.parse.html
