Natural Text
At shell env, the fist time and the third time, "test.zip" md5 values are the same,the second time and the forth time, "test.zip" md5 values are the same, but when i usepython zipfile, the result is not like that.How can I do this with the Python  module?
The  class has a  attribute you can set.Open the file in append mode, alter the comment, and it'll be written out when closed:When you use a  as a context manager, like a regular file it'll automatically be closed when the  block is exited.


Answer URL
https://docs.python.org/3/library/zipfile.html#zipfile.ZipFile
https://docs.python.org/3/library/zipfile.html#zipfile.ZipFile.comment
