Natural Text
I would like to implement the concatenation of paths to files using "\" as an operator, redefining the corresponding function (if any).For example:Thus, adding str "\" between path1 and path2 in the redefition of the method, I should assert 
The  module supports  for concatenating path objects.As an example of how to use , here's a simple class that extends . It should work as long as at least one argument is an instance of  (i.e., it won't work with two plain strings).


Answer URL
https://docs.python.org/3/library/pathlib.html#module-pathlib
