Natural Text
This should be a simple thing to do, but I just can't find the answer to it. How can I get this:...to write:I can remove the brackets OR get the posts on separate lines, but not both. Thanks in advance!
 can handle this by itself if you use argument unpacking (splatting) and set the function's  parameter to a newline:To explain further about argument unpacking, writing this:is equivalent to writing this:


Answer URL
https://docs.python.org/3/library/functions.html#print
