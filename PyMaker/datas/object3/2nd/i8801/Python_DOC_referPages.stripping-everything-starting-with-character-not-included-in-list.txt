Natural Text
With code:I'm getting list of strings like . I would like to remove  part from every element of list. I can use  but I rather would like to remove everything starting from first character (from left) that is not in list because string to remove may differ in some cases. How to do this?
Using  with generator expression to filter only digits and dots, then  to join filtered characters back to a string:
You could use regex for that, something like:Thus you need to change this part:Into:


Answer URL
https://docs.python.org/3/library/stdtypes#str.isdigit
https://docs.python.org/3/tutorial/classes.html#generator-expressions
https://docs.python.org/3/library/stdtypes.html#str.join
