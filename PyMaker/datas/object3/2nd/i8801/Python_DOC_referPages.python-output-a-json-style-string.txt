Natural Text
here defining a variable:and it print out ok like this : , but i want to pass a dynamic value to its value, so its format should like this: , but it output a , I have no idea of this Error, and change all kinds of string format such as triple quotation and change  with , but all seems useless. how can i solve it.
My suggestion is using What you can do is using  convert the json string to dictionary and then change the value, finally convert it back to string


Answer URL
https://docs.python.org/3/library/json.html
