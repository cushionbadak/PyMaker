Natural Text
I would like to intersect two nested lists:So I can return the lists in  where intersections were found:How can I do this using ?
You can use set to check if there is any intersection between elements:This could be slightly more efficient if convert the target to a list of sets before hand:When saying , maybe you need a  function:Or in python 3:It seems like your logic is equivalent to the following as @JohnColeman commented:
You can simply use  in the  combined with a filter:This will keep any list that contains at least one item that is in any of the -lists.Or if you don't like s you can do the checks explicitly:


Answer URL
https://docs.python.org/3/library/stdtypes.html#set.difference
