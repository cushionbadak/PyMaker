Natural Text
I need to convert a json-string to python object. By object I mean "new" python3 object like:I found several help for example on jsonpickle documentation. But all I found are tutorials which convert object to json first and after this convert backwards.I want to convert a json-string from a Rest-API.Here is what I have done so far:This results in:It's quite clear to me that jsonpickle can't convert this to my classes (Goal, Match), because I don't tell jsonpickle in which class the output should be converted. The problem is I don't know how to tell jsonpickle to convert the JSON in object from type Match? And how can I tell that the list of goals should be of type ?
The following lines will give you a dictionary:But none of above will give you what you want (python object (not dicitonary)). because the json from the url is not encoded with  - whcih add additional information to a generated json (something like )If you want an actual Python object which is not a dictionary, i.e. you prefer  to , use  with object_hook:
Do you mean something like this?which prints:
A clean approach on recent python versions might be to use marshmallow-dataclass :


Answer URL
https://docs.python.org/3/library/json.html#json.loads
https://docs.python.org/3/library/json.html#json.JSONDecoder
https://docs.python.org/3/library/types.html#types.SimpleNamespace
