Natural Text
I have code in multi threads to create folder if not existsI got error like thisI am not sure what can I do for this error, do you have any idea?
Read the docs. If you don't care whether the directory already existed, just that it does when you're done, just call:Don't even check for the existence of the directory with  (subject to race conditions), just call  with  and it will create it if it doesn't exist and do nothing if it already exists.This requires Python 3.2 or higher, but if you're on an earlier Python, you can achieve the same silent ignore with exception handling:


Answer URL
https://docs.python.org/3/library/os.html#os.makedirs
