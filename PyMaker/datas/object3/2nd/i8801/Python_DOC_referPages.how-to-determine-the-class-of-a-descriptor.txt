Natural Text
In this example code, I would like to determine if  is an instance of :However, I get the following exception:Is there anyway to tell if an attribute is an instance of a class when it is a descriptor?
With the current ,  causes the  because when the code accessing the descriptor using class,  is passed ; (=>  => )You should modify  to handle such case:BTW, as you may know, with , accessing  attribute through an instance will cause another exception, because it will call the  (-> getattr(..) -> __get__ -> getattr(..) -> ...) recursively until stack overflow.
The best way to implement a property is with the  decorator:It returns  when I run it.See the documentation for  at https://docs.python.org/3/library/functions.html#property.


Answer URL
https://docs.python.org/3/library/functions.html#property
