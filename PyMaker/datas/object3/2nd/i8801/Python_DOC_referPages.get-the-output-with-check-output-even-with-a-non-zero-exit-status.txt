Natural Text
I use  a number of times in a script of mine, but I just ran into an issue with an external application.  in this case.When openvpn is called with the  parameter, which I want to parse in my script, it returns  as its exit code.  chokes on the non-zero exit code and fails with the following message:Q: I don't understand why openvpn does this, but how can I have  give me the output, even with a non-zero return code?edit: I used the exact same code with  or other parameters and all seemed to work just fine.
According to the docs the output is available in the  attribute of the  exception.So something like this should work:


Answer URL
https://docs.python.org/3/library/subprocess.html#subprocess.check_output
