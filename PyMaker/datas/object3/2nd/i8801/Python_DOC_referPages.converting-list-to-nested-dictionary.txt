Natural Text
How can I convert a  into nested `dictionary'?For example:I'd like to convert it to a dictionary that looks like this:
For that reverse the list, then start creating the empty dictionary element.
You could also use  for this. DemoThe flow of construction looks something like as  traverses the reverse iterator making a new single-element dict. 
You can do something like this:{1: {2: {3: {4: {}}}}} [Finished in 0.4s]
Here is an abstraction.  Uses for  are typically overshadowed by , but here is an interesting application if you have one or more lists (iterables):Nested BranchesUnlike , this technique accepts duplicate keys by appending to existing "branches".  For example, we will append a new  branch at the third level of the first (A) branch:Visually:


Answer URL
https://docs.python.org/3/library/functools.html#functools.reduce
