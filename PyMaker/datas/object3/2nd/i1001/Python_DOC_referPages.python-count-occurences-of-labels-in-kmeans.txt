Natural Text
Im trying to compare a list of labels from Sklears Kmeans, with the predicted labels for another dataset. But the label list are different sizes, so i want the occurrence of each label.So I have already tried using Counter, but im not getting exactly what i want. At the moment im using np.unique and there are still some problems.As an Example:Then here is where i have the problem.I would like the output of my count of the labels to also say 0 if the label of a cluster is not there. So I would like the count of my predicted labels to beCould anyone help with this?
You can use the following list comprehension, which goes through all possible cluster assignments and  the occurrences of each element:


Answer URL
https://docs.python.org/3/tutorial/datastructures.html
