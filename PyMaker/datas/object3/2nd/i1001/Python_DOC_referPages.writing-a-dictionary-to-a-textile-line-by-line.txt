Natural Text
This question already has an answer here:How to prettyprint a JSON file?                    7 answers                I have some code that creates a dictionary and pastes it into a text file. But it pastes the dictionary as one line. Below I have the code and the textfile it creates. Is there any way to make the textfile give each key-value pair it's own line like this?
Using formatting string and assuming that  is your dictionary:EDITAs pointed by @wwii, the code above can be also written as:And the string can be formatted using formatted string literals, available since python 3.6, hence  instead of .
You can use the  module -- it also works for all other data structures.To force every entry on a new line, set the  argument to something low. The  argument lets you directly write to the file.will produce:
You can use json.dumps() to do this with the indent parameter. For example:


Answer URL
https://docs.python.org/3/library/pprint.html
https://docs.python.org/3/library/json.html
