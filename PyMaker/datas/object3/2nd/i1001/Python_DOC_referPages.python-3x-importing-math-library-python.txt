Natural Text
I have the following code which i want to execute :But it throws an exception :What am i missing ? 
 was introduced in Python 3.3. You are probably using an earlier version.In those earlier versions, you can useinstead.
You have a file called  in the same directory as the code you're trying to run. Delete or rename it.When you , Python walks through the directories in  and imports the first file called  (or a directory called  with an  file inside) that it sees. The first entry in  is the current directory, so it sees your  first.See the documentation for modules or for the  statement. 
As already suggested either use Python3.3 or aboveoruse Another slight modification is required.Please change to Else this error occurs.


Answer URL
https://docs.python.org/3/library/sys.html#sys.path
https://docs.python.org/3/tutorial/modules.html
https://docs.python.org/3/reference/import.html
