Natural Text
I have a function who reads  of file. It is reading most of the files but for one file it is raising .here is my code File which raise no issue.File which is rasing issue.Complete error is as follows : In other question where user have asked questions of similar errors. they are using  to streamline the file but I already have streamline MemoryObject so can't use  
Your  is already opened in binary mode:  is a method of , not .For your problem, the  and  parameter of  works the same as for . You can apply the appropriate encoding, or ignore errors:Note that you either must know the encoding, or suppress errors by ignoring them. You can try different encodings to find one that works, but in the end you must know what your data means.


Answer URL
https://docs.python.org/3/library/stdtypes.html#bytes.decode
