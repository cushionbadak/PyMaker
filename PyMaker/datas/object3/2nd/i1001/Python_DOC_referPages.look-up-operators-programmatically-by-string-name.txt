Natural Text
Suppose I have the following code:How can I distill this down more programmatically? I'm thinking of creating a lookup like this:Then doing something like this:Which obviously isn't valid Python...Something like  might work:Which produces this, in an example:Is there a better (safer?) way of accomplishing this?
Operators as callables are hidden in the  module. You can dynamically access a specific operator by its name with the  builtin.Demo:We can use this to rewrite your function as follows.(Add sanity checks as required.)
The  module is what you want:


Answer URL
https://docs.python.org/3/library/operator.html
https://docs.python.org/3/library/functions.html#getattr
https://docs.python.org/3/library/operator.html
