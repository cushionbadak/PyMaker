Natural Text
I want to call a function sending several arguments inside a single variable. In other words, I wanted to do  with the same result as  in the example.
You can use the  operator to unpack a tuple or list:If you have keyword arguments and a , you can also unpack the dict with two s:See here.The unpacking operator is very versatile and is not just for function arguments. For example:
You have to unpack the argument using operator :By the way, operator  is used when you want to assign arguments by position. If you want to assign arguments by name you can use operator  in with a dictionary:


Answer URL
https://docs.python.org/3/tutorial/controlflow.html#unpacking-argument-lists
