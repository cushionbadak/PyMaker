Natural Text
This question already has an answer here:Use `__dict__` or `vars()`?                    3 answers                I find the documentation on the vars() function to be kind of cryptic: https://docs.python.org/3/library/functions.html#varsWhat is the point of having this function? Why would you not just call  rather than  or  directly?Would someone give a use case of the function?
The point of having the  function is:you do not need to touch the classes  dunder function directly from the outsideIt is the same with most :See also: Should I ever directly call object.__str__()?
The python builtin  function returns the  attribute of the object in the parentheses; if not applicable, it gives a  exception. It should be used to view dictionaries. When you run:The output is: 


Answer URL
https://docs.python.org/3/library/functions.html#vars
