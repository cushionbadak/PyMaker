Natural Text
I need to find all names of files that contain the string "TeNam":But, there's an error:Could you help me with code?
find_value returns a set ([Python 3]: Sets).res is a dictionary (not the empty set - as I think you intended). Its update method ([Python 3]: update([other])) doesn't work with the provided set (well, unless its string element has a length of 2).Example:As seen from above, the fix would be to properly initialize res (as a set):
Split to 3 parts:Check each fileCheck the entire dirCall (2) and get results.Something like:


Answer URL
https://docs.python.org/3/tutorial/datastructures.html#sets
https://docs.python.org/3/library/stdtypes.html#dict.update
