Natural Text
Trying to loop through titles of movies in a text file to call API and store the response to a text file. file contains one title on every line ex. the api i am trying to use is from www.odmpapi.comThis is what i have to correctly join and create the weblinkThis writes to responses.txt only the last title in the list. 
Try the following kind of approach:This writes the URL and the result into the output file. Python's  command can be used to put your search time into the  without having to split it up and use string concatenation. It works by replacing each  with a passed argument.


Answer URL
https://docs.python.org/3/library/urllib.request.html
