Natural Text
How do I encode multiple jpeg files in a folder as base 64. Currently my code can encode one file. However I would like this to encode all the jpeg files in a folder path. extract_ocr_words(encoded_string)In the above path there'll be multiple jpeg files such as the final output should combine all the images in the folder and extract text and show as list in the final command 
You have to iterate over the files inside the folder. You can use  library (link):Also you can use  (link to function) function which concatenate two parts of path. It will works on Windows and Unix system. It is better than escaping of . Then it is up to you what do you want to do with the encoded images.You can still shorten your code by using  package.The functionality can be achieved as follows:


Answer URL
https://docs.python.org/3/library/base64.html
https://docs.python.org/3/library/os.path.html#os.path.join
