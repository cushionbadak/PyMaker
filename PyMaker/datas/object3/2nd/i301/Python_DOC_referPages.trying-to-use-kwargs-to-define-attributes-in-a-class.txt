Natural Text
So I have a class that defines characters and its attributes and it goes like this:and when I create an instance as this:it works perfectly. But what I´m trying to create is a way of creating it with key values, like this:So i tried to write the  like this, using :It says:What am I doing wrong? I´m really new to programming so I can´t figure it out.
 is just a mapping; it doesn't magically create local variables for your function. You need to index the python dictionary with the desired key.A dataclass simplifies this:This generates your original  automatically.If you need to do additional work in  after adding the dataclass decorator you can define  which a dataclass will call after .
You do not need to define your method with  to support passing arguments by keyword. Your original version of  already supports the  syntax you want to use. Your original version is better than using , because it ensures that exactly the right parameters were passed, rejecting things like  typos.
You should use , for example:


Answer URL
https://docs.python.org/3/library/dataclasses.html
