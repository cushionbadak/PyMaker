Natural Text
You can generate triangle numbers like thisbut is there a better way to do this in a one liner using a more functional approach?
Yes, use the built-in :Note,  can take any binary operation, but it defaults to addition, 


Answer URL
https://docs.python.org/3/library/operator.html#operator.mul
