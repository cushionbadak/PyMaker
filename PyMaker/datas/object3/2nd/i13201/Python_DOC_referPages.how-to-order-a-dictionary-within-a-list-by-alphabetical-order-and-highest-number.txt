Natural Text
I have a basic piece of coding which follows:I have sorted out it so it will print out people's names in alphabetical order, however, I now need the code working so it would print out the names in alphabetical order, but also so it prints out the highest score only.
Your scores are stored in three separate keys; use the  function to pick the highest one:You could make your life easier by storing all the scores in a list, rather than three separate keys:You can then still address individual scores with  (where  is an integer, pick from 0, 1 or 2), but the highest score is then as simple as .


Answer URL
https://docs.python.org/3/library/functions.html#max
