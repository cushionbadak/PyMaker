Natural Text
I'm trying to cut a string into pieces in Python with the following code:I expect some result like this:however I get:
Since you have no capturing groups in your re, the things you split on are not part of the result. What you're getting is the empty string before the 'C', the empty string between 'C' and 'O2' and the empty string after 'O2'.If you useyou'll getOne dirty approach would be to useto remove elements that are not truthy (in this case, empty strings).
If you want to splitUse look arounds like this:Regex live here.ElseTo simply match (without split):Regex live here.Hope it helps.
You can also use this one:


Answer URL
https://docs.python.org/3/library/re.html#re.match
