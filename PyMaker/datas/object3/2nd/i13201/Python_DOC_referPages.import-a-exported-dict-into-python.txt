Natural Text
I'm writing something like a database, but rather simple and small. So I used a dict, which is then saved in a file. The code is roughly:I want it to be imported the next time I run it, i.e. import it as a dictionary from file, anyway to do it?
If you'd like save some data such as ,  or , etc. to a file. And you want to edit them or you just want them be readable . Use  module like this:Now you could save these data to a file. If you want to load them, use  like this:
Use :


Answer URL
https://docs.python.org/3/library/json.html
