Natural Text
I have a list of item as follows:I want to print a list item as follows:I have tried as follow:However it is not working as per my requirement.
When you print -  - Python would not automatically substitute the  inside the string, you would need to format the string correctly so that  is printed there.You can use  for that. Example -If you want the elements in the output within quotes, you can use the following  function call -If you want the first letter capitalized, then -
The string in  is being treated as a literal string, that would work in PHP but not python. Use string concatenation like this:



Answer URL
https://docs.python.org/3/library/stdtypes.html#str.format
https://docs.python.org/3/tutorial/inputoutput.html
