Natural Text
When outputting a list of objects how can you return an int type representation for the objects?I've tried this:Where list_of_objects is a list of  instances. But this gives an error .I could solve this problem using a string for both, but I'm wondering if type int is possible?
 is required to return a string representation of the object. Returning a different type is not a valid implementation of .If you want a way to return some number, then add a custom method that does that.Btw., note that implementing  is not a way to make  work. For that to work, you should implement  and . But at that point, you should really think about if you want  to be true; probably not. So you should change the way your check works by explicitely looking at the  property instead.Instead of , you could do the following:
You could check a parallel list of the object ids instead.
 must return a  as you have seen.The proper way to do what you are asking is:



Answer URL
https://docs.python.org/3/reference/datamodel.html#object.__repr__
