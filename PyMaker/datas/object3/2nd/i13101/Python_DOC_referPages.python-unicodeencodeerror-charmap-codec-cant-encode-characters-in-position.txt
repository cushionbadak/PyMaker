Natural Text
I want to write the HTML of a website to the file I created, tough I decode to utf-8 but still it puts up a error like this, I use  and the html is printed properlyand I am using python 3.5.0
You've opened a file with the default system encoding:You need to specify your encoding explicitly:See the  function documentation:In text mode, if encoding is not specified the encoding used is platform dependent:  is called to get the current locale encoding.On your system, the default is a codec that cannot handle your data.
This should work, it did for me


Answer URL
https://docs.python.org/3/library/functions.html#open
