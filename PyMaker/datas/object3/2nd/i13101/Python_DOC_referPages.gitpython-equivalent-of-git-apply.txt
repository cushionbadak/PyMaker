Natural Text
Is there a way to apply a patch (in the form of a diff file) to a repo using only the gitpython library?In other words, is there a gitpython equivalent of the git-apply command?
Solution is to doI had tried this before but I had omitted the 'git' at the beginning of the argument list, which gave an error about the command not existing.
You can use git directly, like this:this will perform the git command: 


Answer URL
https://docs.python.org/3/library/tempfile.html
