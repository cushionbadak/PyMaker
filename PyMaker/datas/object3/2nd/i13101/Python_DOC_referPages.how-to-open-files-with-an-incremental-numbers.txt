Natural Text
Right now I'm doing this:But I have many files written as chxx.md (until ). Is there any what to modify the script using ranges? So I don't have to type in all the files names?
You can probably use . It's an easy way to collect files with the use of wildcard characters.
You can also use  syntax, as follows:
Just do :
python 2 string formatingpython 3 string formating
My attempt with  and  pattern is customizable.Better is you have nested directory, in this case just get the path of mother folder as .In this approach you have more control over the file name then any other methods.Prints-


Answer URL
https://docs.python.org/3/library/string.html#format-string-syntax
