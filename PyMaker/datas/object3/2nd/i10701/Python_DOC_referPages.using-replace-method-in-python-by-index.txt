Natural Text
This question already has an answer here:Python: String replace index                    5 answers                I want to do something like this.expecting the outputhowever because the letter "t" is being passed into the method the t at the end also gets replaced with a "b". Resulting in the outputHow do I do it in a way that would result in the output "best" instead of "besb"?
The problem is that  returns a copy of the string in which the occurrences of  have been replaced with .Instead, you can swap the character at index  using:
Check the documentation.You can usethough it would be much more natural to use a slice to replace just the first character, as @nbryans indicated in a comment.



Answer URL
https://docs.python.org/3/library/stdtypes.html#str.replace
