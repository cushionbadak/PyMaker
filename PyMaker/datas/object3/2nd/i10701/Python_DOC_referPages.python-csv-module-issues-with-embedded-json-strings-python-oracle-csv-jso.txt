Natural Text
I am importing a small amount of baseline data into tables immediately after creating them.  Only one table is giving me trouble, and this is because one of the fields is JSON.I have not found a syntax engine capable of properly interpreting escapped quotes and commas within the JSON.  I have not tried them all and am, of course, open to suggestions based on any experience with a similar problem.I don't know if it matters, but I'm using Toad for Oracle to export the CSV files as a baseline for development rebuilds of data.  Toad has no option to replace the delimiters in the CSV and while it wouldn't be hard for me to manually change a single CSV file doing so as a maintenance task would be a PITA.Here is a sample of the CSV data causing the problem:And here is what the python csv module returns as a row when it attempts to parse a row:Finally, here is the csv reader code:
Line 69-70 (from https://docs.python.org/3.5/library/csv.html#dialects-and-formatting-parameters ) Dialects.  Escapechar is set to None by default.Modified to backslash


Answer URL
https://docs.python.org/3/library/json.html#json.JSONDecoder.raw_decode
