Natural Text
I know functions can be passed around as arguments in Python, but I don't get why I can't do this: when this is perfectly valid:
 is a statement. If you want an anonymous function then use a  expression.
You can't use  for that. For the object representation of a function, you can use a .Or you can use the name of a function as your second example shows.


Answer URL
https://docs.python.org/3/reference/compound_stmts.html#function-definitions
