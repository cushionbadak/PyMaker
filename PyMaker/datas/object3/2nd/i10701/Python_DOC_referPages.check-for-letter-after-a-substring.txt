Natural Text
I'm trying to find a way to check whether or not an alpha-numeric character follows a portion of a string. For example, if the sub-string is: , and the text is: , it should return  and if the text is:  it should return .
If you're looking for any valid letter after a given sub-string, you can first find the position of that sub-string inside the string with , then check if the index after the sub-string matches  from :Now, your check can look like this, index the string after at the position  i.e the position after the sub-string:This prints  if  is followed by a letter, if not:it prints .
You could use a regular expression with the substring as a lookbehind, matching a character:


Answer URL
https://docs.python.org/3/library/re.html
