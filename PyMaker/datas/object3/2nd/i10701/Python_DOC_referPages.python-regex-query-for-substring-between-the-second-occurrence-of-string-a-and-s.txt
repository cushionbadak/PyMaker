Natural Text
I'm trying to parse some values from a raw output and having difficulties with the regex expression. The raw string contains two or more values with the same 'start' keyword. This is what I have so far, which gives me the first instance:How do I get the second instance between the same 'start' and 'end'? Thanks in advance!Example:Desired output:I can get server1, but not 2.
The output is:and the function can be like this:for the same   :
This works on the given example.
You can also make use of  which will create an iterator that will be exhausted until the end of your string:


Answer URL
https://docs.python.org/3/library/re.html#re.finditer
