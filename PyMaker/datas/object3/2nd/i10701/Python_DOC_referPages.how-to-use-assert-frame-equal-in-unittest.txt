Natural Text
New to unittest package. I'm trying to verify the DataFrame returned by a function through the following code. Even though I hardcoded the inputs of  to be equal (), the unittest still fails. Anyone would like to explain why it happens?
 is coming from the  package, not from the  class. Replace:with:When you had , it tried to find  attribute on the  instance, and, since there is not  attribute or method exposed on an  class, it raised an .
alecxe answer is incomplete, you can indeed use pandas'  with , using 


Answer URL
https://docs.python.org/3/library/unittest.html#unittest.TestCase.addTypeEqualityFunc
