Natural Text
I have a list which look like this:I want to get the dict angle 25, how can I do it?UPDATE: After playing a while with Pandas, I find this might be possible Returns But this is a bit hack.
Assuming each angle in your dictionary is unique and each dictionary contains the key 'angle':
The built-in  function will do what you want... you just need to feed it a function that determines which objects to keep.  That function below is .I wrapped  in , so I could use  to avoid hard-coding the angle sought.   is a generator function, so it can  any number of matching shapes.This prints:Note that the  syntax requires Python 3.3 or greater.


Answer URL
https://docs.python.org/3/library/functions.html#filter
https://docs.python.org/3/library/functools.html#functools.partial
