Natural Text
I want to find all elements containing a string using Beautiful Soup in Python.It works when I use non-Persian characters, but not when I use Persian characters.For the code above, the output is , but it works if I use ASCII in the query.Is there any UTF-8 conversion or something to solve it :) ?
It works!


Answer URL
https://docs.python.org/3/library/unicodedata.html#unicodedata.normalize
