Natural Text
I have a text file (.txt) with different lines of wordsFor example: How can I use the  command to just get the 2nd column...so 2 and 12 for example of the text?
Assuming no spaces around :If there could be whitespaces, use :
First you have to split the file into lines (easily done simply by iterating over it), than you have to extract the 2nd column from each line.Simple example:To accumulate a list of the values:


Answer URL
https://docs.python.org/3/library/csv.html#csv.reader
