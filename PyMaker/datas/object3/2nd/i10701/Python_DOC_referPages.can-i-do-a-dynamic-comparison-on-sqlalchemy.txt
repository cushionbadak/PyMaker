Natural Text
I have this model:And I need to do a query dynamically (This way I tried):The value of  come from the Frontend.An example of  is: , , What I really need is a way to do. or  dynamically, like the first way I tried.The result using () is There is a way I have used before, but isn't pretty way to do that, but isn't pretty way to do that:
Here's an approach that uses  and keyword argument unpacking:


Answer URL
https://docs.python.org/3/tutorial/controlflow.html#unpacking-argument-lists
