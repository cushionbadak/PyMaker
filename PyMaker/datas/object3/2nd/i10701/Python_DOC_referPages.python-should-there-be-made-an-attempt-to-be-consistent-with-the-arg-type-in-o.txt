Natural Text
My doubt is simple. Should I do an effort to be consistent with argument types when using optional arguments?An example:My  is allways a ,except when it is not passed in the method call, so I'm not sure if is a good practice to use in this case.I came to this doubt because I don't know how to write my docstrings, is the consider  even if sometimes is ?
Names don't have types; only the values they refer to have types. In this case, it is perfectly normal to document that  will refer to a  object or , with an explanation of what each means. As far as a docstring is concerned, it's safe to say everyone will understand what you mean if you state that an argument that should be a  may also take .In PEP-484, which deals with providing statically checkable type usage, this notion of using  is not just acceptable, but catered to.If  should always be a string, you would hint it asIf it is allowed be  as well, you would indicate that with a union typeAs this is a very common case, there is a shortcut for indicating this:In fact, if the default value is , the static checker that uses these type annotations assumes the use of , so the following are equivalent:


Answer URL
https://docs.python.org/3/library/typing.html
