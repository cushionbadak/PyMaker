Natural Text
I'm trying to get a timestamp from an email like this:First of all, I parse the timestamp with:Here comes the problem. I try to convert the result to a datetime, but in vain. What's the problem?
 returns a 9 tuple similar to the structure  but with the index 6,7 and 8 unusable:And  objects require different values for its constructorYou could directly create a  using the useful parts of your tuple asEdit: Careful with this, because this will create the object in local time, disregarding the time zone information.Edit 2: You can solve this by using , you just need to cut the  from the end of your string, since PDT is not a valid name for , but  is enough
Check out  or  for converting a struct_time tuple to a float. You can then use  with that float to create a DateTime object.
Last two items of the tuple are strange, they don't look like timezone data. But if you don't need timezone aware  object, you can do something like this 


Answer URL
https://docs.python.org/3/library/time.html#time.mktime
https://docs.python.org/3/library/datetime.html#datetime.datetime.fromtimestamp
