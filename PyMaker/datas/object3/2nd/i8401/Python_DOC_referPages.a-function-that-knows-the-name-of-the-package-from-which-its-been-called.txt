Natural Text
I have two packages: , a series tools including function  which has  as a dependency and which is using its  functionMy problem is that when I call  from  package's code, I need  to be able to find it's been called from the  package (and for instance return the package's name).  For example: I've been playing around with  and  but couldn't find any solution so far.
Use  to get frame information, then check  attribute of the module:


Answer URL
https://docs.python.org/3/reference/import.html#__package__
