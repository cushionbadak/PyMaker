Natural Text
I have a huge corpus of text (line by line) and I want to remove special characters but sustain the space and structure of the string. should be
You can use this pattern, too, with :Output:Edit:Otherwise, you can store the final lines into a :Output:
I think nfn neil answer is great...but i would just add a simple regex to remove all no words character,however it will consider underscore as part of the word
Create a dictionary mapping special characters to NoneMake a translation table using the dictionary.  Read the entire text into a variable and use str.translate on the entire text.


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.maketrans
https://docs.python.org/3/library/stdtypes.html#str.translate
