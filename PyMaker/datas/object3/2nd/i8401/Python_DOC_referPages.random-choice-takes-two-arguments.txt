Natural Text
I made a simple mistake in the following die roll function:I do know that I need to pass the sequence as a list or tuple, but I was more curious about the following error message.The message says "choice() takes 2 positional arguments but 7 were given".  But the documentation indicates only one argument (sequence).  https://docs.python.org/3/library/random.html What is the second argument (or seventh in my case)?  Is this the seed (which I have not specified so is being initialised by the clock)?
 is a method on the hidden  instance the  module maintains. Because it is a method, it has 2 arguments:  and the iterable from which to make a choice.From the module documentation:The functions supplied by this module are actually bound methods of a hidden instance of the  class. and the  module source code:


Answer URL
https://docs.python.org/3/library/random.html
https://docs.python.org/3/library/random.html
https://docs.python.org/3/howto/descriptor.html
