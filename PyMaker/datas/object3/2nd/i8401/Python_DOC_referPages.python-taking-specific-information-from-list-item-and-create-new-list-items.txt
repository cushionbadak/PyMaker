Natural Text
I am using list in my first python project.I want to use another list name "new_data_items" and display only TRUE conditions like below example:How to write for loop for this
You could do something like the following:But list comprehensions would be quicker and less verbose:
You can use filter to do that:Note that filter returns an iterator so you may also want to convert that to a list:If the 'Writable' field may not be present for some records replace indexing with :
use python list compressionChk this link : https://repl.it/HEyl/0



Answer URL
https://docs.python.org/3/library/functions.html#filter
