Natural Text
I have a list of lists of lists with strings, something like this (representing chapters, paragraphs and sentences of a text)):I know how to flatten this list completly (for example by ), but what I would like to do is to flatten it partially, to finally look like this:I managed to solve this by some for loops:But I wonder if there is a better, shorter solution? (I don't think,  is a way to go, because my lists are different in size.)
The easiest change I can see is using the  method:So, what does  mean:


Answer URL
https://docs.python.org/3/tutorial/controlflow.html#unpacking-argument-lists
