Natural Text
I write a linked list，when I use multiple assignment ,the result is strange。What's the different between python and javascript?The sample python codeThe same logic JavaScript code 
In Python, evaluation is in reverse order. Therefore this:is the same as this:So, you have to reverse order of elements before last assignment:
Actually you're reassigning  to be equal to , so  will always be  because those two variables are pointing at the same object.
I'm not sure what you are trying to accomplish with the line:If you just do:You will get the result you want
The assignment operation is evaluated from right to left:


Answer URL
https://docs.python.org/3/reference/simple_stmts.html#assignment-statements
