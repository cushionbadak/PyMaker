Natural Text
I have joined two datasets in Spark (pySpark)and the output looks likes thisI would like to do the following: Define a function that extracts and returns only ABC, 500. I wrote a function like thisBut this function results in an error "tuple object has no attribute split()"Is it possible to extract the variables without saving the results as text files and then processing them?
Tuples are immutable.  is for  types.This will return the b and c separately:
Your value is a tuple with two elements, whereby the second element is a tuple by itself


Answer URL
https://docs.python.org/3/tutorial/datastructures.html#tuples-and-sequences
