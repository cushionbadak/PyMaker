Natural Text
I want to round the float numbers in a list to floor in python, I tried , the error is:  and I also tried , I received this error: .I couldb't find any proper code to solve this problem. Any help would be appreciated!Here is the code that I have so far:
 only accepts a single float value argument (or an object with a  method). To apply it (or another callable taking a single argument) to a whole list you can use list comprehensions as shown below:If you don't need the  list for anything else, you could even combine the two list comprehensions into one and do things like this (although it's less readable):
 requires a float value. But you seem to be passing a list of list instead. You can use list comprehension: That should get a list of rounded values from . You can also work the  function:


Answer URL
https://docs.python.org/3/library/math.html#math.floor
https://docs.python.org/3/tutorial/datastructures.html#list-comprehensions
https://docs.python.org/3/library/math.html#math.floor
