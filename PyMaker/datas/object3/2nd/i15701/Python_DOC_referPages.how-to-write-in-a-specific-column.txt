Natural Text
So this is pretty simple I think but I can't seem to get it working.The objective is to simply define a function that will print a string, and the last letter of that string must be in the column .I did this.In the attempt to create  spaces and subtract them to the string's length.I get this error though:I know what it's telling me but can't turn it around.
(if , you will have a problem)
you can use : here is demo
Use the format function:
You should be doing:or, with a little spacing:This will create a string of spaces whose length is equal to  minus the length of the string .  So, if  is  characters long, the code will make a string of  or  spaces.That said, a better solution would be to use :Below is a demonstration:The , , and  methods (all explained in the link above) were designed specifically for tasks like this.
You could doIt will be more clear.
Something like this perhaps:


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.rjust
