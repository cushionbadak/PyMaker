Natural Text
Code so far:  This outputs:I want it to output:Any and all suggestions welcomed :)
The  "magic comma" always inserts spaces, so you can't do things this way.You have three choices:Join the words up into a string first, then print that string: .Write directly to  instead of using : Use Python 3-style , which can do things this way. First,  at the top of your code. Then, .
The comma creates a space.  Try creating a list comprehension and joining it with empty string:To see how it's working in smaller steps:
[:2] selects 1st two letter .  converts them to lowercase.using  you can concatenate string.python 2.x:
The print function from Python 3 has a parameter  (for "separator") which can be set to a null string. Using that and using the  operator to specify passing in a variable number of arguments:


Answer URL
https://docs.python.org/3/library/functions.html#print
https://docs.python.org/3/library/functions.html#print
