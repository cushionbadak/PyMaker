Natural Text
This question already has an answer here:Enumerate or list all variables in a program of [your favorite language here] [closed]                    16 answers                The question is as it is in the 'Title', but for the sake of completeness -Suppose :Are three variables. Then I do a whole bunch of other stuff. And I want to see what variables I have initialised so far, is there a way to do it? Something that will return [a,h,k].
If you only want user defined variables, you could use  and strip out the builtins:Demo:
yes, using the  function:dir([object])  Without arguments, return the list of names in the current local scope. With an argument, attempt to return a list of valid attributes for that object.example:


Answer URL
https://docs.python.org/3/library/functions.html#dir
https://docs.python.org/3/library/functions.html#dir
https://docs.python.org/3/library/functions.html#dir
