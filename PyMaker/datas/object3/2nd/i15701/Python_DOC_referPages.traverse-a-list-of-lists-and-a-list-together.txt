Natural Text
I have a list of lists, well a list whose elements can have an attribute which is a listand a listboth lists contain 10 elements,ie to say they will always contain the same number of elements.What I want to do is   if  for all i.If  has a list assign each element of  the corresponding I have solved it like this(quite C'ish),and was wondering if there is a simpler way.
Since both lists have the same length when  is flattened, you can flatten , then you can use :


Answer URL
https://docs.python.org/3/library/functions.html#zip
