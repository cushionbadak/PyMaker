Natural Text
When working with multiple files in Python code can get ugly when using the recommended style:That's three indentation levels just for working with files! The alternative would be thisI have a feeling that either of these examples could be refactored to something more elegant. Any examples?
Python 2.7 and up let you specify multiple context managers in one  statement:The line does get long, and you cannot use parentheses to keep that below 80 characters. You can use  backslash continuations however:Another option would be to use  context manager (only in Python 3.3 and up):


Answer URL
https://docs.python.org/3/library/contextlib.html#contextlib.ExitStack
