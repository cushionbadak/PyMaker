Natural Text
I want to be able to call  and get something like  in a variable.  I can instead use  and do the splitting manually, but I'd rather not do this, because I'm building a wrapper around another program and would like to mimic it's command line option input format.I have tried the following:but then I get  when I call it as .
You can use a custom  class.https://docs.python.org/3/library/argparse.html#argparse.Actionoutput:


Answer URL
https://docs.python.org/3/library/argparse.html#argparse.Action
