Natural Text
:in python3 repl:How to fix test_globals function to actually modify ?
The problem with what you did is that  didn't import  because it didn't exist. If you repeat the  line, it will work:Another thing that would work:However, if you're only after reloading a module (as you wrote in the comments), consider using .


Answer URL
https://docs.python.org/3/library/importlib.html#importlib.reload
