Natural Text
This question already has an answer here:Split a string with unknown number of spaces as separator in Python                    3 answers                I'm looking for the Python equivalent of 
The  method without an argument splits on whitespace:

Another method through  module. It does the reverse operation of matching all the words instead of spitting the whole sentence by space.Above regex would match one or more non-space characters.
Using  will be the most Pythonic way of splitting on a string.It's also useful to remember that if you use  on a string that does not have a whitespace then that string will be returned to you in a list.Example:


Answer URL
https://docs.python.org/3/library/shlex.html#shlex.split
