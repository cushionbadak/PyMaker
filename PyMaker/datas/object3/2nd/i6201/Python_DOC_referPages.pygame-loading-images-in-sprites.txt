Natural Text
How do I load an image into a sprite instead of drawing a shape for the sprite?Ex: I load a 50x50 image into a sprite instead of drawing a 50x50 rectHere is my sprite code so far:
First load the image in the global scope or in a separate module and import it. Don't load it in the  method, otherwise it has to be read from the hard disk every time you create an instance and that's slow. Now you can assign the global  in the class () and all instances will reference this image. If you want to use different images for the same class, you can pass them during the instantiation: Use the  or  (for images with transparency) methods to improve the blit performance.If the image is in a subdirectory (for example "images"), construct the path with :


Answer URL
https://docs.python.org/3/library/os.path.html#os.path.join
