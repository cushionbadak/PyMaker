Natural Text
I am using the logging module in the following way:and for writing. The output is:QUESTION: instead of the current timestamp, I would like to have a past (or future, whatever) timestamp. So whenever I want to write something in the log, I can pass the date as argument with something like:in order to haveAny help?
You can do that simply by:and logging with:Do note that since you hardcoded the  argument in the format, a log message without one will fail. If you want to make it optional, you'll need to use something like  to modify your log message on the fly.


Answer URL
https://docs.python.org/3/library/time.html#time.strftime
