Natural Text
For the os library what is the difference betweenos.listdir('.') vs os.listdir()They both seem to produce the same results (a list of everything in the active directory) however:https://www.tutorialspoint.com/python/os_listdir.htmsays that os.listdir specifically excludes '.' and '..' even if they are present in the directory.  What does that mean?
There is no functional difference, see the docs.  The definition of  looks like thisSo the default value for path when you call  is 
From :That is,  is the same as .[...] says that  specifically excludes '.' and '..' even if they are present in the directory. What does that mean?That concerns the returned values.In UNIX filesystems, every directory has  and  entries,where  refers to the current directory,and  to the parent directory.The documentation says that these entries will not be included in the list returned by .
.[dot] in listdir() refers to current directory & when we do not provide any input to listdir() then by default it lists current directory that's the reason it shows same result.    enter code here


Answer URL
https://docs.python.org/3/library/os.html
