Natural Text
So here's what I'm trying to do:The inputs will be in the correct syntax for Python. For example for this part:what the input will be something like "and 5 % 2 == 0" So basically if conditionOne contains "and 5 % 2 == 0" and I have a statement which reads:I need the program to get the string in the variable and put it into the if statement so it becomesIs there any way to do this?
If I understand you correctly you can do something like this:But I wouldn't recommend you to code this way because you allow a user to enter any Python commands through the standard input that is completely unsafe because the user may execute an arbitrary Python program though yours.


Answer URL
https://docs.python.org/3/library/functions.html#eval
https://docs.python.org/3/library/functions.html#eval
https://docs.python.org/3/library/functions.html#exec
