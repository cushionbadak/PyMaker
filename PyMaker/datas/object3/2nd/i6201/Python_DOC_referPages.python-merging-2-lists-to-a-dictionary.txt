Natural Text
For exampleThe output should be a dictionary which will look like:I tried using zip but it just overwrites the values.
I believe you want this:
It appears you want the sum of the corresponding numbers as your values.I would suggest using a  to manage your unique keys, and implement the summation yourself:A  will create an integer with no initial value (which means '0' by default) when you lookup a key that does not already exist in the dictionary. So you will get 0 + value + value ... for each key, which appears to be what you're looking for.
If you want to try without default dict , Here is recipe : output:


Answer URL
https://docs.python.org/3/library/collections.html?highlight=collections%20defaultdict#collections.defaultdict
