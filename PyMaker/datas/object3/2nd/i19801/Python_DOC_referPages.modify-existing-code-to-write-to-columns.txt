Natural Text
I am looking to change the below code so that instead of writing row by row with the contents of  the code will only write on one row and will separate all contents of  with a comma, thus making all of the values be in columns in the .csv instead of rows (as it is right now).What would be the easiest way to do this? I would essentially want the following:Turned into: (I would already have the .csv headings created and would just want the  section appended.The  data would then just be looped and appended to the existing .csv file.Is this possible, and if so, easy to do?



Answer URL
