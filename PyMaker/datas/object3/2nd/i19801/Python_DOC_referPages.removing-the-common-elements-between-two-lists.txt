Natural Text
Possible Duplicate:Python list subtraction operation I want to remove the common elements between two lists. I mean something like thisIs there any simple pythonic way to do this ?
use sets :
Set have a methods:http://docs.python.org/3/library/stdtypes.html#setprint(set(a).difference(b))


Answer URL
