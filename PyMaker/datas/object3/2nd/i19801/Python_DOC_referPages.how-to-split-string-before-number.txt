Natural Text
I have a string I would like to outputI have tried to add  before every number by using  but it didn't work
The following works just fine:Demo:The expression  matches 1 or more digits, and I replace that with a newline followed by the matched digits (via the capturing group).
Use a group:To remove the first newline, you can add a negative lookbehind:
Here is another way to do the substitution without inserting a newline before the first number:
Without using :Output:


Answer URL
