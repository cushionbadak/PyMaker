Natural Text
I have a folder structure like thisIn my test_app.py, I have a line to open the sample.csv file. When I run py.test command on the root  level. It throws errors saying that sample.csv does not exist. How should I configure py.test so that it can find my sample.csv file?Edit:Nothing fancy about the test. I am just trying out how to configure paths.
If the csv file is always in the same directory as the test file you should use , something like:See the python datamodel docs for some scant notes on .


Answer URL
