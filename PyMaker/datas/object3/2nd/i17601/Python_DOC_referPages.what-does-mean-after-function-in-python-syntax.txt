Natural Text
This question already has an answer here:Understanding slice notation                    31 answers                What mean in python when i'm after function set  or  for example? Here is code example
 returns a string. The brackets are applied to that object, not the function. It is just a short version of


Answer URL
