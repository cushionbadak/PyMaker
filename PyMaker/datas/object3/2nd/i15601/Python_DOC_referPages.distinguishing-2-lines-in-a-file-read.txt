Natural Text
I am doing this in order to read a fileThe file's contents that is being read from above looks like this:How should i go about it. When there was only 1 "val1" and 1 "val2", i did a simple search with regex and saved the values. Now that there are two, I need to be careful to know which one I am dealing with.. is there an easy way out..?
Use the  module to parse JSON.Python 3 code:Output:Edit:For Python 2, use this instead.


Answer URL
https://docs.python.org/3/library/json.html
