Natural Text
Assuming I have a list of lists:I want to convert it into a dictionary:I currently have following code that does this:Is there a simpler/elegant way to do this?I am using Python 3, if that matters.
Use a dict-comprehension with :Here  with splat operator () transposes the list items:Now we can loop over this iterator return by  and using the extended iterable packing introduced in Python 3 we can get the key and values very cleanly from each tuple.


Answer URL
https://docs.python.org/3/library/functions.html#zip
