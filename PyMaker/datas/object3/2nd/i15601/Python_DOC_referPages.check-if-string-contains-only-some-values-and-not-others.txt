Natural Text
This is where i want to ask if password contains only numbers, but i don't know how to do that, i already tried using ands and ors, but failed miserably.
Use 
You can do:This asks if the set of the characters in password is a subset of the set of all number characters.A set is an unordered collection of values that can't have duplicates. Some examples:Sets have several useful functions, for example checking for a subset:This can be easily extended to test something else, for example if the password contain only punctuation:Or only alphanumeric characters:
Well, string has only numbers only when it can be cast to int, so...


Answer URL
https://docs.python.org/3/library/stdtypes.html#set-types-set-frozenset
