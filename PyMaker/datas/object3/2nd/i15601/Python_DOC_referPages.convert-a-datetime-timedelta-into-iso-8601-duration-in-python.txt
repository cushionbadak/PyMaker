Natural Text
Given a datetime.timedelta, how to convert it into a string of ISO 8601 duration format?Ex,
This is a function from Tin Can Python project (Apache License 2.0) that can do the conversion:E.g.
Although the  module contains an implementation for a ISO 8601 notation for  or  objects, it does not currently (Python 3.7) support the same for  objects.  However, the  module (pypi link) has functionality to generate a duration string in ISO 8601 notation:which means 12148 days, 4 hours, 20 minutes, 39.47017 seconds.


Answer URL
https://docs.python.org/3/library/datetime.html
