Natural Text
This question already has an answer here:Accessing a value in a tuple that is in a list                    5 answers                I have a list of lists which look like this:What would be the best way to extract all elements occurring at index position 1. I know I can use a for loop like;But is there  a more "pythonic" (short /easy /less code/ efficient) way to do this?
You can use a list comprehension:The above is equivalent to:except that it is a lot more concise and also avoids all those calls to  (which means that it is more efficient).
use list comprehension:this is more general if nested list is moree...


Answer URL
https://docs.python.org/3/tutorial/datastructures.html#list-comprehensions
