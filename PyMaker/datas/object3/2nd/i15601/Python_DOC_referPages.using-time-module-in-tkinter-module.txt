Natural Text
Instead of displaying the first image, waiting a second, and displaying the second image over the first one, it waits a second and then the box pops up and displays both at the same time.  
 cannot be called in the same thread as the Tkinter event loop is operating in.  It will block Tkinter's loop and thereby cause the program to freeze.You should be using the  method to schedule the operation to run in the background after 1000 milliseconds (or one second):Also, I used a lambda expression for the sake of brevity.  However,  accepts normal function objects as well:


Answer URL
https://docs.python.org/3/reference/expressions.html#grammar-token-lambda_expr
