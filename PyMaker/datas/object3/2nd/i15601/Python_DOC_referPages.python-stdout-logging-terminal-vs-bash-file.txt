Natural Text
I am not expert in Bash and Python, so this question might appear silly.I have a Python script called  and I noticed two different behaviours of the standard output, when redirected to a log file.If I call this from terminal, I can see the log file size growing while the script is running.However, if I create a bash file for the same purpose:the script starts (I checked with ) but it does not seem to run, as the log file stays empty. Am I missing something?
I solved using the Logging facility for Python, by insertingand replacing every occurrence of  withThe final Bash script:


Answer URL
https://docs.python.org/3/library/syslog.html
