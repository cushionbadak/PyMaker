Natural Text
I have a text file of the looks like this:I'd like to make it a dictionary looking like thisthe file text list is a list of edges for a graph. I need to make a graph out of it without using any package. My final aim is to calculate the diameter and clustering coefficient. However, before starting I need to create the graph.My attempt so far was:Outcome:Thanks
As one other possible option, you can also use  here:This saves you from having to check whether a key is already in  or not, and it also saves you a couple of lines.
Explanation:Just make the values of  lists, and append to the lists.
try this:if key is found in dictionary it will append the value else create a new pair



Answer URL
https://docs.python.org/3/library/collections.html#collections.defaultdict
