Natural Text
Why does this code print ?
Because  returns ?So, the print is done () and then the list comprehension is returned ()What you want to do is use join:or use the  argument of print (I don't have python3 right now but something like this:)
print is a function but it return  that's why you are getting noneDo thisso when you do this  prints  1, 2, 3, 4, and then each time  return the output which is  hence None, None, None, None


Answer URL
https://docs.python.org/3/library/functions.html#print
