Natural Text
I have this:the above code return a dictionary like this:I want to loop through that dictionary and get just the age:I tried:No data returned and I got this error:
 doesn't return anything, you need to configure a file-like object to capture the value. A  object would do, you can then call  on that after the call completes:If you are not married to , you might find  to be a lot easier:Even the standard library  module will be easier than using :


Answer URL
