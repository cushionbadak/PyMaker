Natural Text
E.g., I have:In the python interpreter,each time I run  it takes a lot of time to re-read the database, which is unnecessary.How can I store the result from  to avoid this reducdant process?Edit:I found a similar question here but I don't quite know the answerSave functions for re-using without re-execution
This will only hit the database once.Basically, you want to save the results of readDb() to a seperate variable which you can then pass to calculation().
Write a simple decorator:For more general solutions, look here: http://code.activestate.com/recipes/498245-lru-and-lfu-cache-decorators/.
Updated answer for modern PythonFor anyone still searching for how to do this, the standard library  includes a decorator function  to do this. For example (from the docs): This would store the last  calls to  and when it is called with the same argument, the cached value will be returned. 


Answer URL
https://docs.python.org/3/library/functools.html#functools.lru_cache
