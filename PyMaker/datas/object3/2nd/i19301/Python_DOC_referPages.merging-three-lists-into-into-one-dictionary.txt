Natural Text
I need to merge three lists into one dictionary. These lists are from reading a txt file I formatted, and here is a snippet from that file:Into the following:Here is what I did:When I enter this I get:Not all the data from list is shown and I don't want the  shown.When I tried the following:Only part of the data shows up:What should I do?
You were on the right track with , but beware that:The returned list is truncated in length to the length of the shortest argument sequence.If you're fine with that, you can zip your data into a list of tuples, zip the keys, and hand everything off to .If you'd like to handle missing values, checkout   (Python 2) or  (Python 3) whereIf the iterables are of uneven length, missing values are filled-in with fillvalue.Output:
How's this:
It sounds like what you want is:which will give you:
Is this the kind of thing you are looking for?


Answer URL
