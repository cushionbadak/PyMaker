Natural Text
In python I get "n" number of json objects on request to an api. I need to convert this to a list and display it in django. What is the easiest way to do it.I did use json.dumps(data) it just dumps the entire data.
You can use  and then iterate on the result
If you want data out of a .json file you can load it from a string using. You can load from a file directly via . Now its just a matter of displaying the data to the user.


Answer URL
https://docs.python.org/3/library/json.html#json.loads
