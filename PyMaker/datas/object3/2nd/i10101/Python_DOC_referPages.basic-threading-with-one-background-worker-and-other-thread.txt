Natural Text
I'm very much a beginner in both python and programming. Trying to get multithreading work but haven't managed so far. Grateful for any help or tips.
It has been a while since i have done thread programming in python but I remembered that you will have to call  on each thread or else the main thread will exit before your spawn thread get a chance to execute.should do the trick I just looked into python  library the issue is that  is expecting a callable object. when you call  the method was being evaluated rather than passing the method. import timeimport requestsclass crawler:ref: https://docs.python.org/3/library/threading.html#threading.Thread


Answer URL
https://docs.python.org/3/library/threading.html#threading.Thread
