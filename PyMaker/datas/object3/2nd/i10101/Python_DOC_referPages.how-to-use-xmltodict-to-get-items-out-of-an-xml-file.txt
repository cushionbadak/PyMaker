Natural Text
I am trying to easily access values from an xml file.If i want to acces the value ABC123, how do I get it?
Using your example:If you examine , you'll see it's an , ordered by tag:The root node is called , and there a subnode  which is a list of  objects, so if you want the  for every article, you would do:If you specifically want the  only when  is , you could do this:That said, if you're parsing XML documents and want to search for a specific value like that, I would consider using XPath expressions, which are supported by .
This is using xml.etreeYou can try this:if you want to extract a specific code based on the attribute 'nummer' of artikel, then you can try this:this will print only the code you want.
To read .xml files :
You can use lxml package using XPath Expression. The thing to notice here is the expression.  is the root element.  chooses the first  element under (Notice first element is not at index 0).  is the child element under . You can read more about at lxml and xpath syntax.


Answer URL
https://docs.python.org/3/library/xml.etree.elementtree.html#elementtree-xpath
