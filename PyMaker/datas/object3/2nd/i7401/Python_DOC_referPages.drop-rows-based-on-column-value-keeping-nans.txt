Natural Text
I have a pandas df (5568, 108) where the column of interest is df.Age, which has some NaNs (303). I want to keep the NaNs but drop some of the outliers.df.drop(df[df.Age<18]) and df.drop(df[df.Age>90]).I tried But this results in TypeError: unsupported operand type(s) for &: 'float' and 'numpy.float64'Any thoughts on how I can achieve this?
There is an operator precedence issue. Wrap parentheses. , etc. The  is evaluated before the  otherwise, leading to the expression .
I think you need  with  and  for filtering, what is most common as  by conditions:


Answer URL
https://docs.python.org/3/reference/expressions.html#operator-precedence
