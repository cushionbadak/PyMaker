Natural Text
I have a string  as belowThis string should be Unicode string, but cannot be displayed (print) correctly.And the string  is Unicode string/ bytes started with , And  can be displayed correctly by My question is how to convert x to y ?
Assuming we're talking about Python3, the Unicode string  is 6 code points long. It happens to be that each of those code points is in range  to  (ASCII subset). We can get the exact byte string with the  codec, like this:Note that this will only work if the string  contains only ASCII subrange of Unicode; otherwise you'll just get escaped Unicode code points (as the codec's name suggests):


Answer URL
https://docs.python.org/3/library/codecs.html#text-encodings
