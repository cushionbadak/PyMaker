Natural Text
im trying to plot a data .... they contain commas and after converting them to floats i get error. data sample '   0,8750044   'this is part of the scripti get following error;i printed all 3 rows ...results down hereif the rows are fine... then there is something wrong plt.errorbar() can someone plz explain what went wrong and how can i fix it?
It appears that you are using an older version of matplotlib that does not have the 'xkcd' colors defined.  Using  works in matplotlib 2.0.2. Either update matplotlib to a newer version, or use a different color (e.g.  should work).


Answer URL
https://docs.python.org/3/tutorial/errors.html#handling-exceptions
