Natural Text
This question already has an answer here:Multiple repeated `in` keyword [duplicate]                    3 answers                Why does the second  statement output ?
 and  are comparison operators in Python, the same in that respect as, say,  and .  In general,is treated asexcept that  is evaluated only once.  That's why, e.g.,works as expected.  However, it applies to any chained comparison operators.  In your example,is treated asThe  part is False, so the whole expression is False.  To get what you probably intended, use parentheses to change the grouping:Click here for the docs.
Your statement  is internally evaluated as  as shown below(seq is True) is False and therefore your statement outputs False.
The answer below is not correct. The comment explains it an i verified: TypeError: argument of type 'bool' is not iterablePrevious answerI believe it is evaluating  (which evaluates to the bool ), then evaluating  (which evaluates to ). Presumably you mean  (which evaluates to )? 


Answer URL
https://docs.python.org/3/reference/expressions.html#comparisons
