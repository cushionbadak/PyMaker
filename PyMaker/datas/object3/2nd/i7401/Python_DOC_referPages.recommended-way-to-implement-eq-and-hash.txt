Natural Text
The python documentation mentions that if you override  and the object is immutable, you should also override  in order for the class to be properly hashable.In practice, when I do this I often end up with code likeThis is somewhat repetitive, and there is a clear risk of forgetting to update one when the other is updated.Is there a recommended way of implementing these methods together?
Answering my own question. It seems one way of performing this is to define an auxillary  function and to use that in defining  and . This way, there is no duplication:


Answer URL
https://docs.python.org/3/reference/datamodel.html#object.__hash__
