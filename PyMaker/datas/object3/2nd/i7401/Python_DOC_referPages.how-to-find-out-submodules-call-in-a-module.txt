Natural Text
I want to list out all the function call including submodule calls in a list. In my case it is os_list for os module. I want to store calls of os.path module calls along with this.For identification of function call I am using "__call", What is used for identifying the module.
You can check an object type using [Python]: isinstance(object, classinfo).For modules, the  argument should be [Python]: types.ModuleType:While we are on the subject, you could do the same for functions. So, your code would look like:@EDIT0: Included builtin functions as well.


Answer URL
https://docs.python.org/3/library/functions.html#isinstance
https://docs.python.org/3/library/types.html#types.ModuleType
