Natural Text
If one want to get the (absolute) file paths (file listings) of all files with names of certain pattern how can this be done in Python (v 3.5 on unix). Something similar to the bash command . I have been looking as the os, glob, os.path  and this SO but cannot get it together.Say you want absolute paths to the files that  matches /.*[pat.txt]$/ and you have the below diretories:Then you would get want the three indicated paths:One try was:
You can use  and  operator
Here is one using regexes, but for the simple case I would go with akash`s answer using  operator


Answer URL
https://docs.python.org/3/library/os.html#file-descriptor-operations
https://docs.python.org/3/library/os.path.html#module-os.path
