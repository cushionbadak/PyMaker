Natural Text
I have a functions like this Now I am defining linux commands(options) for this functions using argparse.I tried with this codeBut I am getting error while passing command as 
 is the form of an ,   is the correct form for a subparser nameproducingI added  to the  command, and  to give more information.  Note that the subparser name is now available as .  So you don't need the added .  However the argparse docs do suggest an alternative use of https://docs.python.org/3/library/argparse.html#sub-commandsWith this  is actually a function object, not just a string name.  So it can be used asNote that I had to change how the functions handle their parameters:producing


Answer URL
https://docs.python.org/3/library/argparse.html#sub-commands
