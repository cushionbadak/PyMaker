Natural Text
Currently I have database filled with 1000s of rows.I want to SELECT the first 100 rows, and then select the next 100, then the next 100 and so on...So far I have:This allows me to get the first 100 rows, however, I can't find the syntax for selecting the next 100 rows after that, using another  statement.I've seen it is possible with other coding languages, but haven't found a solution with Python's SQLite3.
When you are using  you don't have to issue another  statement. The cursor is keeping track of where you are in the series of results, and all you need to do is call  again until that produces an empty result:or using the  function (which can be used to repeatedly call a function until a sentinel result is reached):If you can't keep hold of the cursor (say, because you are serving web requests), then using  is the wrong interface. You'll instead have to tell the  statement to return only a selected window of rows, using the  syntax.  has an optional  keyword, together these two keywords specify at what row to start and how many rows to return.Note that you want to make sure that your  statement is ordered so you get a stable result set you can then slice into batches.Pass the  value to a next call to your code if you need to send these batches elsewhere and then later on resume.
sqlite3 is nothing to do with Python. It is a standalone database; Python just supplies an interface to it.As a normal database, sqlite supports standard SQL. In SQL, you can use LIMIT and OFFSET to determine the start and end for your query. Note that if you do this, you should really use an explicit ORDER BY clause, to ensure that your results are consistently ordered between queries.
You can crate iterator and call it multiple times:


Answer URL
https://docs.python.org/3/library/functions.html#iter
