Natural Text
How do you load a compressed grammar file into NLTK? I can load an uncompressed grammar file just fine, with:but since my grammar file is nearly 100MB, I want to load the much smaller compressed version, which is only 1MB, but this fails with:Unfortunately, looking at  doesn't list any compressed formats.
As you can see by examining the source of , it relies on  to open  URLs.That function autodetects GZipped data by checking if the path ends with  and reads it with .But, for this to work, the data should be packed with  directly, not through  or anything.


Answer URL
https://docs.python.org/3/library/tarfile.html
