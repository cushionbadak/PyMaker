Natural Text
I want to write a decorator in : if a called function contains 's, the decorator prints her name before this function being called. I'm familiar with decorators syntax, but I have a problem with checking if a function has  within itself.It is not necessary to check if the 's from function will actually be called.
This can be done by holding the buffer of 'print' from flushing and checking it to see if a print has been done.Now to check
I this case you can redefine printEDIT:i test and this workEDIT2:


Answer URL
https://docs.python.org/3/library/logging.html
