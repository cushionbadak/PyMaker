Natural Text
This question already has an answer here:Converting integer to string in Python?                    9 answers                My code:Result:The function is supposed to return the sum of each digit of the argument "n".Why do I get  in the commented line? The variable  is of type ing so it shouldn´t be an issue to apply a type cast to .
In Python you do not cast that way. You use:Technically speaking this is not casting: you call the  builtin function which takes as input the string and produces its equivalent as . You do not cast in Python since it is a dynamically typed language.Note that it is of course possible that  contains text that is not an integer. Like . Of course  cannot make sense out of that. In that case it will raise a . You can use - to handle these:
In Python, you cast a string to an integer by using a function .
Basically, you need to cast string to integer using 


Answer URL
https://docs.python.org/3/library/functions.html?highlight=int#int
