Natural Text
Output:Why here is the output ?
You're misusing . You're supposed to name your own class, not a parent. And given this is Python 3, you don't even need to do that, a simple:would work (as long as the first parameter to the function is a single argument, regardless of name; there are exceptions for when you're accepting  via , but that's rare, and only for complicated cases involving simulating a ).The reason it misbehaves as written is that you've told it explicitly you're doing  based in , not , so it scans the MRO (method resolution order) to find the next class after , which happens to be .


Answer URL
https://docs.python.org/3/library/functions.html#super
https://docs.python.org/3/library/stdtypes.html#class.__mro__
