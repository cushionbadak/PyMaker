Natural Text
I am using a package that has operations inside the class (? not sure what either is really), and normally the data is called this way . Since I have to do multiple operations thought of shortening it and do the followingbut since it's considering  as a string it doesnt do the operation, and if I take away the string then it is an undefined variable. Is there a way to go around this? Or will I just have to write it the long way?. In particular I am using , its package  and with the  I want to call specific suborbitals. A real example of how it  would be used is , the first [0] denotes the element in question for which to get the orbitals  (that's why I omitted it in the code above).
You can use  in order to dynamically select attributes from objects (the  package in your case; for example ).So your loop would be rewritten to:


Answer URL
https://docs.python.org/3/library/functions.html#getattr
