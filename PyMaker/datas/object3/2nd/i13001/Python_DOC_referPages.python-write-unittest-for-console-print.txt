Natural Text
Function  prints to console. I want to test the console print. How can I achieve this in python?Need to test this function, has NO return statement : My test : 
You can easily capture standard output by just temporarily redirecting  to a  object, as follows:The output of this program is:showing that the redirection successfully captured the output and that you were able to restore the output stream to what it was before you began the capture.Note that the code above in for Python 2.7, as the question indicates. Python 3 is slightly different:
This Python 3 answer uses . It also uses a reusable helper method , although this helper is specific to the function being tested.Note that the  arg is passed automatically by the  decorator to the  method.A general-purpose  class, possibly a mixin, can in principle be derived from the above.For those using Python ≥3.4,  also exists, but it seems to serve no benefit over .


Answer URL
https://docs.python.org/3/library/unittest.mock.html
https://docs.python.org/3/library/contextlib.html#module-contextlib
https://docs.python.org/3/library/unittest.mock.html#unittest.mock.patch
