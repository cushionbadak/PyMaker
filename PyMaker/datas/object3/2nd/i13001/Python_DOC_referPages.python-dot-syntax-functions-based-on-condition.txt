Natural Text
Is it possible in python to call dot-syntax-function based on condition. Simple example to turn:into:
No, it is not possible.The syntax for attribute reference is Quoting the documentation,An attribute reference is a primary followed by a period and a namename must be a regular Python identifier and identifiers can't contain special characters like .However, you can use a simple conditional expression to select primary:It's equivalent to Note that in this case we have to use parentheses, because attribute reference has higher precedence than conditional expression.
Since  is called in either case, start by doing so unconditionally. Save that object to , with the intention of calling . Before that, though, check your condition to see if  should really be the result of .


Answer URL
https://docs.python.org/3/reference/expressions.html#attribute-references
https://docs.python.org/3/reference/lexical_analysis.html#identifiers
https://docs.python.org/3/reference/expressions.html#operator-precedence
