Natural Text
In the following code, I am trying to parse the arguments of the command line:When I run the code, I got:The command line is:Why does the opts have null values?
Testing this with only the actual arguments getting passed to , I get the expected behaviour:Per the documentation:args is the argument list to be parsed, without the leading reference to the running program.If you also have  and your script name in , you need to slice more from the start of it. You could also consider switching to , as suggested in the  docs.
Since I use Spyder, the problem was with the command arguments. The command must be:without In the regular command line, it works fine with 
I'd suggest you use ; it's far easier once you understand.


Answer URL
https://docs.python.org/3/library/argparse.html
