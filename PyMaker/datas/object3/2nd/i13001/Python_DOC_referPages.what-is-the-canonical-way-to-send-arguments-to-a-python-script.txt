Natural Text
The following code in file  makes it possible (after ) to either  or to .Is this the most elegant way to call one of two functions from the command line?

There are various options. The argparse module in the standard library is one. There are also some wrappers that make it easy/easier to use, for example argh. This lets you write:
I recommend argparse: https://docs.python.org/2/howto/argparse.htmlIt supports mutual exclusion of arguments among other features.


Answer URL
https://docs.python.org/3/library/argparse.html
