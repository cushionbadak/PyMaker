Natural Text
I am learning python and have a very simple queryBasically  what exactly does this code below mean in englishdoes it mean compare all values in values to all values in othervalues ?
As @khelwood said, it simply iterates on two loops.It iterates through the values in , assigning each value to the variable Inside such loop it does the same thing, iterating on the values of  and assigning each value to the variable You can verify it simply adding a print statement inside the loop, that shows the values of  and e.g. with an input ofit producesPlease make sure you understand how iteration works in python, reading the official docsand this SO Q&Aand more tutorials you can find on the internet.


Answer URL
https://docs.python.org/3/library/stdtypes.html#iterator-types
