Natural Text
I must find a way to get the  position when iterating over a list using a  structureFor each  inside my , i got a  that have a correspondent  in the .the dict's first key, "19", corresponds to the "10" inside the other list..So i have to get the item position while using a for structure, so then i can access the correspondent key in the dict, and use the dict's value in a 
There are three issues here, not one. In Python (and most other languages), dictionaries aren't sorted, and  returns a new string. They have no order. In order to get around this, you can use an  and do something like:Look into  and .Also notice that I renamed  to  and  to , as  and  builtin functions. In addition, there may be a bug in your code, as you are never actually using ; I'm not sure what it's purpose in your code was.
This should be helpfull:


Answer URL
https://docs.python.org/3/library/collections.html#collections.OrderedDict
https://docs.python.org/3/library/functions.html#enumerate
https://docs.python.org/3/library/functions.html#zip
https://docs.python.org/3/library/functions.html#func-dict
