Natural Text
Edit:random.random() returns values from 0.0 to 1.0How does seeding with numbers outside of these bounds (0.0-1.0) work? The wiki page says seeds are used to "initialize" the generator. What does the generator do with the seed to keep it within these bounds?It isn't clear to me how you could use a large # such as 10000 to initialize a function that will return values from 0.0 to 1.0.
 returns values from  to , but it's based on the Mersenne Twister algorithm, a pseudorandom number generator that works entirely with integers. When  is called, a float value in that range is generated from the integer data.According to the docs for , when the seed is an , it's used directly as the seed for the Mersenne Twister. Integers work as seeds because the underlying algorithm works with integers.You may have been confused by the statement thatAlmost all module functions depend on the basic function , …This seems to be referring to two things:There are a lot of functions in  that return floats. These, logically, are based on .Older versions of Python used  to implement  and . A  subclass that doesn't provide , like the default implementation does, will do the same.


Answer URL
https://docs.python.org/3/library/random.html#random.seed
https://docs.python.org/3/library/random.html#random.getrandbits
