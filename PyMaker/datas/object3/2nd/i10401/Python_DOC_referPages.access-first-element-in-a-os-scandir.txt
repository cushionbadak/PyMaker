Natural Text
This question already has an answer here:there's no next() function in a yield generator in python 3                    2 answers                I am using  for getting the list of files in a folder:I want to get the first element.I am trying So I tried:So just like in list we can get the first element value, can we get it from ?
 returns an iterator containing the directory entries for a given path, see :To get a value from it call the built-in  on it:This will return values from the iterator until it is exhausted (and  is raised).You can also wrap it in a  call which create a  out of all entries and make it indexable but if you only require the first element that is overkill.  was removed as of  with ; a dunder  method replaced it along with the built-in  that calls it.
 would give you the next item from the  iterator:


Answer URL
https://docs.python.org/3/library/functions.html#next
https://docs.python.org/3/library/stdtypes.html#iterator-types
