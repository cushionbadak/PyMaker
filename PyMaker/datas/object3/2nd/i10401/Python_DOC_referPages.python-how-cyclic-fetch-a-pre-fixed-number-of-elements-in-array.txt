Natural Text
I'm trying to make a function that will always return me a pre-fixed number of elements from an array which will be larger than the pre-fixed number:where  stands for index of array to fetch and  represent the array of all elements:Example:where  and the array return the  to complete 10 elements with  don't need to cyclic the array just get thanks for the helpInitial code (not working):EDIT:I can't make any   for this script
You could return For example
The is a fantastic library with lots of cool things. For this case we can use  and .Works just as you wanted.
Is that what you want? Updated to work with lower numbers.



Answer URL
https://docs.python.org/3/library/itertools.html#itertools.cycle
