Natural Text
i'm trying to initialize the config filebut it keeps throwing this error I searched everywhere but didn't find anything
The answer by teivaz is correct, but maybe incomplete. The way you are using the ConfigParser object is almost correct in Python 3 (docs), but not Python 2 (docs).Here is Python 2:And Python 3:Note: Python 2's  won't complain if you give it a non-string value (e.g. ), but it will throw a  when you try to retrieve it. Python 3 will throw a  when you use the  method with a non-string value, but it will quietly convert values to strings with the  access. E.g.:
You are simply not using it right. Here you can find examples.About the error you are getting you can read here:  Called to implement assignment to .



Answer URL
https://docs.python.org/3/library/configparser.html
https://docs.python.org/3/reference/datamodel.html#object.%5F%5Fsetitem%5F%5F
