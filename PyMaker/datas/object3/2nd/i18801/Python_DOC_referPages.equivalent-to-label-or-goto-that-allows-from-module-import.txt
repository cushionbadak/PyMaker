Natural Text
I've developed a simple multilingual console-based set of calculators, my problem is, in the first execution when the user chooses his language he'll get two times the software's header.The launcher's code is:I've googled and people suggest to use functions like def lang() but I can't do that because:
The best solution is not to use  at all. Instead, leave the module namespace in-tact, and just use  and  where you need things from it.Arguably, even better would be to use another format for your configuration files like JSON. This will make working with config files easier, and avoid the possibility of malicious config files.


Answer URL
