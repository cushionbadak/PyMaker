Natural Text
In PHP the accepted way to secure database login credentials is to store them outside the web root, and to  the files with the passwords. How are MySQL database login credentials safely stored in Python applications?
Well, one way of doing this is putting the passwords in a separate config/ini file that is not deployed with the project. And then, pass the path of this file to the main entry of the application, e.g.: Note that you'll need to parse this  argument (see argparse) and then read and parse the  file. Update:Since you mentioned web applications, there is also another way of passing configuration information - through the . For example, if you use , you can putt this in the wsgi directives:And then, this value will be accessible in the application with through :


Answer URL
https://docs.python.org/3/library/configparser.html
