Natural Text
Borrowing the implementation of an instance method from another class is straightforward, but how do I do it with a class method?I also tried changing line X into , which caused 
When you access , you invoke the  object as a descriptor, which means it is already bound to the  class. Unwrap it again:The  attribute gives you access to the original function, so you can rewrap it with a new  call.Alternatively, use the  mapping to get the actual classmethod itself, bypassing the descriptor calls:
You need:When you access , the resulting classmethod already "knows" that it belongs to A.  If you want to rewrap it for a different class, you need to extract the underlying function with  and then rewrap it with .Also, in your example, the line  does nothing, because you don't assign the result to anything.


Answer URL
