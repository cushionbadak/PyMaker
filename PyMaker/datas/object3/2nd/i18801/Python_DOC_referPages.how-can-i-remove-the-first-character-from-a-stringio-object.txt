Natural Text
I couldn't figure it out with the Python documentation, so maybe i can ask it here:Is there any method i can use, that will do what  would do on a string,so  will return ?Thanks.
I can't figure out how to do it with , but you can use StringIO objects like normal file objects.  Just seek to byte 1 and read as you normally would.
The StringIO getvalue() function return the content as string, so this could be work:


Answer URL
https://docs.python.org/3/library/io.html#io.IOBase.seek
