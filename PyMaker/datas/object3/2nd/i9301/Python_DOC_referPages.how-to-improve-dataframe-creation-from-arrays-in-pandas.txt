Natural Text
I have two arrays  and  which contain a series of numbers.My goal is to create a dataframe having the following structure:for each element of B I want to correspond all the values of A. For example:if  and  I want to obtain the following pairs: ,  and .I was able to achieve my goal in the following way:Column  but be calculated as shown above.The code I wrote works fine but it is pretty slow when I increase the values of .Is there a more elegant way to achieve my goal? I would like to avoid nested for loops and it should be a more efficient way than mine obviously.
You can create all combination by itertools.product.For column  convert  to  and then to  for remove decimal, fom column  get absolute value and cast to :


Answer URL
https://docs.python.org/3/library/functions.html#zip
