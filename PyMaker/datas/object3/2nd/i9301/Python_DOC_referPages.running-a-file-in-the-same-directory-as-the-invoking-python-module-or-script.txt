Natural Text
How can I run a batch file with Python (in the same directory as the Python scripts)?Note that the directory cannot be constant as it can be changed from one user to another.
You need to find out where your script is and assemble an absolute path:In StepsYou can find out the name of script with:Now make it an absolute path:and get the directory it is in:Finally, join this path with your batch file name:before you feed it to:
you can achieve it use subprocess module


Answer URL
https://docs.python.org/3/library/subprocess.html
