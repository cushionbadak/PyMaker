Natural Text
this is a dumb question, I know.I am making a request, the request returns something like thisNow if I do  (with  being the variable the above string is stored in), it prints it out as it is represented above.However if I do It prints out ".So I am wondering, how do I print like the second version.This might not be clear so to sum it up:When I print the raw string with  etc in it, it prints with  replaced with its unicode character. But if I print it is the response from a request, it prints out without replacing the character.
It seems like r.text is being stored as a raw string. i.e.In order to convert it to unicode, you need the codecs module:


Answer URL
https://docs.python.org/3/library/codecs.html
