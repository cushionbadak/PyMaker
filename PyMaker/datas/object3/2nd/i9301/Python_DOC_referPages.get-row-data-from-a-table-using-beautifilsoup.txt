Natural Text
I'm Using Beautifulsoup to turn the data in this table to a json. However, how do I get the data between the tags? Unfortunately, this is not working: 
Here is an example. See this question on how to decode the escaped HTML strings according to your version of python.
Taking into account your actual problem statement of converting an HTML to JSON, you can use  to read HTML into a  and then convert it to a dictionary:Prints:You can then use  to further dump the dictionary into a JSON string. There is also  method that can directly dump a  to , but I have not figured out how to use it in this particular case.


Answer URL
https://docs.python.org/3/library/json.html#json.dumps
