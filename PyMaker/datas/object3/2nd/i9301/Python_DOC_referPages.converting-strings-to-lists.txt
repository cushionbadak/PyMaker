Natural Text
I'm reading a lot of strings from a file in this format:  that I need to convert to lists. I tried using , but it doesn't seem to be working as intended. I need to use the string as a regular list, for example when I have  it will point to .
One way would be to use :
If say this is your given string:Then do this:and  should give you 


Answer URL
https://docs.python.org/3/library/ast.html#ast.literal_eval
