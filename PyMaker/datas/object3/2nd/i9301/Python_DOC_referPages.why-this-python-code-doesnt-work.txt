Natural Text
This question already has an answer here:How to clone or copy a list?                    20 answers                Can you help me with this code? It returns: Why? My desired output is: 
This works: Replaced  by Output:What  does is that it deletes all entries of the list. The key to understand here is that you operate by reference and you need to point  to a new list so you don't operate on the old list you just put into Doing  leaves the old list (which you just inserted into ) and assigns the variable (you can think of a pointer) to a new empty list which is not associated with the just inserted list.As jonrsharpe correctly notes above a better solution would be :Output:


Answer URL
https://docs.python.org/3/library/itertools.html#itertools.groupby
