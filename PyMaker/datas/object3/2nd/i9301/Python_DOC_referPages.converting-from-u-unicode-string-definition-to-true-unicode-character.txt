Natural Text
I have a long list of unicode definitions and description mappings that use the  coding convention.In python (3), how can I read these in as true unicode characters? (i.e.  or '💚'I've tried array slicing and composition eg  but end up with  as the initial string instantiation craps out on a partial unicode declaration.
You can also use  to parse the number, and  to convert the number to a single-character string. For example:If you want to convert all of the  instances in a larger string, you can use the same / pattern in the 2nd arg of :


Answer URL
https://docs.python.org/3/library/functions.html#int
https://docs.python.org/3/library/functions.html#chr
