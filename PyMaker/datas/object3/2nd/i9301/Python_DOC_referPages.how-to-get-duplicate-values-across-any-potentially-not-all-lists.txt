Natural Text
I have a dataset along the lines of:I've searched SO and found ways to return duplicates across all lists using  (so, in this example, ), but I actually want to return duplicates from any lists, i.e.,:Since  and  are duplicated at least once among all lists. Is there a built-in for Python 2.7 that can do this?
Use a  to determine the number of each item and  to pass the items from the sublists to the .


Answer URL
https://docs.python.org/3/library/collections.html#collections.Counter
https://docs.python.org/3/library/itertools.html#itertools.chain.from_iterable
https://docs.python.org/3/library/collections.html#collections.Counter
