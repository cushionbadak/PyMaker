Natural Text
I receive a 'HTTP Error 500: Internal Server Error' response, but I still want to read the data inside the error HTML.With Python 2.6, I normally fetch a page using:When attempting to use this on the failing URL, I get the exception :How can I fetch such error pages (with or without ), all while they are returning Internal Server Errors?Note that with Python 3, the corresponding exception is .
The  is a file-like object.  You can catch it and then  its contents.
If you mean you want to read the body of the 500:In your case, you don't need to build up the request.  Just doso, you don't override urllib2.HTTPError, you just handle the exception.



Answer URL
https://docs.python.org/3/library/urllib.error.html#urllib.error.HTTPError
