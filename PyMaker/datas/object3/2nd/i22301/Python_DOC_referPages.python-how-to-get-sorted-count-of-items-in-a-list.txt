Natural Text
In Python, I've got a list of items like:And I'd like to output something like:How can I output a count and leaderboard of items in a list? I'm not too bothered about efficiency, just any way that works :)Thanks!
So this function uses a  to count the number of each entry in our list.  We then take each pair of the entry and its count and sort it in descending order according to the count.  We then take the  number of entries and return that.So now we can say
A two-liner:
I'm surprised that no one has mentioned . Assumingit's just a one liner:which will print:Note that  is a subclass of  with some useful methods for counting objects. Refer to the documentation for more info.


Answer URL
https://docs.python.org/3/library/collections.html#collections.Counter
