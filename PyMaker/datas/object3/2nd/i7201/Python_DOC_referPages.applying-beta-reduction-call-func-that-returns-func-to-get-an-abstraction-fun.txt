Natural Text
I wrote a simple example of what im trying to do :TypeError: 'staticmethod' object is not callableI was expecting second line to print 6 (as 3*2), how to do that right?
well that was easier then i thought :this works
you are evaluating the ; instead you should  it:which gives:a different way to go is to use :


Answer URL
https://docs.python.org/3/library/functools.html?highlight=functools
