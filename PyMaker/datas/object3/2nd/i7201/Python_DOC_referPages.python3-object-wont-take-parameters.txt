Natural Text
I am working through reviewing a video on Lynda and sometimes instead of just running the provided code I typed it myself. The instructor's code works, but mine doesn't. It returns . Mine is on top, his is on the bottom. Can anyone explain?
The reason is the  function in the class. The way this method works as seen here to let an object be assigned parameters when initialized. I am sure you understand this, but the simple error you made was that it requires  underscores on either side to take effect. Like this: . That is the difference between your two codes.


Answer URL
https://docs.python.org/3/reference/datamodel.html#object.__init__
https://docs.python.org/3/reference/datamodel.html#object.__init__
