Natural Text
I am making in a python script which replaces and add a command at the end of a file. I am using:Is it possible to make this function replace  or  file, or more generally, to replace any extention of the infile? I need the script to replace the name of any infile as it does for .
I recommend using pathlib:


Answer URL
https://docs.python.org/3/library/pathlib.html
