Natural Text
Given an array of separators:and a string where some columns were left blank (and there is random white space):How can I get this:I've tried simply removing the substrings to see where I can go from there but I'm still stuck
use the list to generate a regular expression by inserting  in between, then use  to remove spaces:yields:the  part can be handled by the regular expression at the expense of a more complex regex, but simpler overall expression:more complex: if field data contains regex special chars, we have to escape them (not the case here):
How about using ?To get rid of the whitespace, split on whitespace too:


Answer URL
https://docs.python.org/3/library/re.html#re.split
