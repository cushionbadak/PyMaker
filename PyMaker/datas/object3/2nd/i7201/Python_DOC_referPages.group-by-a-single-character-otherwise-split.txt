Natural Text
I have the following string:I would like to be able to group by the T's into a list and then count the number of T's to the first H.i.e. so likeWhats the most efficient way to do this in python ?
 is your friend
You can do this in one line with a list comprehension:Output of :
You can achieve this with itertools:gives:


Answer URL
https://docs.python.org/3/library/itertools.html#itertools.groupby
