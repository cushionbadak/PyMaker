Natural Text
Why can't I append to the  returned by  inline as I can do with the dict sal ?Doesn't  returns just a  and that should be same as any other dict ?
The assignment statement  implies that both  and  will take on the value of . As an example, look at the byte code for the assignment :With , a is assigned first to 2, followed by  where  is then assigned to the same value, 2.So, with receives the value . Also, a temporary variable is created when you call  and that is modified, following which the reference is lost.In order to get this to work, you'll need to break this up into 2 parts, as you have already done.
Why can't I append to the dict returned by json.loads inline as I can do with the dict sal ? You can, and you do, but then you just discard that dict. It doesn't have any effect on the  variable, and you didn't assign the dict to . You assigned 0 to .When you assign , that makes a new dict, unrelated to the first dict, and then you actually assign the new dict to . Modifications to this new dict are still visible when you view the dict through .


Answer URL
https://docs.python.org/3/reference/simple_stmts.html#assignment-statements
