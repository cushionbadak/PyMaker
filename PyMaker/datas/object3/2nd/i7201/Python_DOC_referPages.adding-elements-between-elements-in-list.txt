Natural Text
If I have a list eg.and a function f(), how do I insert such that the new list is like:
You can use:with  the initial list, and  of course the function you want to apply.For example if  is the  function (converts a number to its textual equivalent), it will generate:So here we obtain  where  is the initial , and  is .Benchmarks: I altered the following test setup to:This means we can test the different answers for different file sizes using pandas. For every configuration, we perform 10 tests with timeit.This generates the following timings (in seconds):Or in relative terms (best is ):

A slightly different approach from Willem's answer, that avoids two loops: creates a list of two elements with the current element and the function applied to it. flattens the list.

You can use  and :Output:


Answer URL
https://docs.python.org/3/library/functions.html#sum
