Natural Text
I started using python Cmd module, and I would like it to print a greet message and/or a short command summary before beginning the input loop (so the user won't be totally lost).Can it be done?
From Cmd documentation for :The optional argument is a banner or intro string to be issued before the first prompt (this overrides the intro class attribute).
You could use Cmd.preloop() and just print whatever you like with 


Answer URL
https://docs.python.org/3/library/cmd.html#cmd.Cmd.cmdloop
