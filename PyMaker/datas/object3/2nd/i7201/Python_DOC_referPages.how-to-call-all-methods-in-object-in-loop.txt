Natural Text
I have an object that has different methods like , ,  and I can call it like this:How can I call it all in the cycle. I have the following algo:How can I call  with all 's algorithms that I mention(, , )?
You can use   to get the  attribute of each optimizer, then call it:There's also  which does exactly the same thing with a slight change of syntax:
Directly enumerate the optimize function


Answer URL
https://docs.python.org/3/library/functions.html#getattr
https://docs.python.org/3/library/operator.html
