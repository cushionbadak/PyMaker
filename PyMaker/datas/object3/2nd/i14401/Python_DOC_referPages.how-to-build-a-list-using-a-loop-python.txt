Natural Text
How do I get items in a python loop to build a list. In php I would use something like this:which produces stores the item in the foreach in an array.In Python i tried:which producesBut it doesn't return  as a list and  doesn't work. how do I get the loop to feed into the list?
This should get you started: list1 now is If you want a third list, simply define it and append to it instead. 
So a couple of things are wrong with your code.First: should be Second:should be


Answer URL
https://docs.python.org/3/tutorial/index.html
https://docs.python.org/3/library/stdtypes.html#sequence-types-list-tuple-range
https://docs.python.org/3/library/stdtypes.html#mutable-sequence-types
