Natural Text
If word is , the output will be:I want the output to be:
The Good way:The best way is to use The Bad way:Using C-like statement, note that this is de-emphasised, (but not officially deprecated yet)The Ugly way:One way is to have your string add with a  and using  attribute
You can use  of a word to include the Python quotation marks (as they appear in e.g. the interactive interpreter); this has the added benefit of working reasonably when quotes are already in the string:


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.format
