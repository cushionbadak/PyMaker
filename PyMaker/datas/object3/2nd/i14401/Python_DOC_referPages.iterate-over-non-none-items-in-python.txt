Natural Text
I have a list of strings, some which happen to be None:Now I would like to iterate over this list, but skip the None entries.  For instance, something like this would be nice:But this doesn't work.  There are two ways I could get this to work, but I don't like either method:andI just was curious if there was a better way.  I feel like ignoring None's should be quite fundamental.

You can use :
You can filter out the  with a simple list comprehension:Then call your code:
You can get rid of the  items with a list comprehension:


Answer URL
https://docs.python.org/3/library/functions.html#filter
