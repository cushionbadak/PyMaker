Natural Text
I use a memoization decorator:And then I use @memoize on the functions I want to apply it to.However I try, after I am done, to save the cache to memory.Not working. How do I fix? "AttributeError: class memoize has no attribute 'memo'"
You would need to create an instance, it is an instance attribute not a class attribute:If you want to access memo from the func that the decorator was used on:To make it clear, each time you decorate a function a new memo dict is created so you cannot access a global version of memo because there is None. If you want to access it from the class and share it among all functions you would make it a class attribute:


Answer URL
https://docs.python.org/3/library/shelve.html
