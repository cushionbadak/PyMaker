Natural Text
When dealing with open files, Python has the  syntax that makes sure the file closes when leaving the block - regardless of exceptions etc.Since processes are resources too, I was wondering: is something similar possible or recommended when using ? For example, should  be run in a  clause - assuming I don't mind blocking until the process finishes?
Starting from Python 3.2  is a context manager.from the docs:Popen objects are supported as context managers via the with statement: on exit, standard file descriptors are closed, and the process is waited for.This should do pretty much what you want.This is the relevant part from  from the standard libin Python 3.4:Now you can do in Python 2.7
For 2.7 you could also use the :e.g:
You can add just two custom methods to any class to implement comptability with  statement.


Answer URL
https://docs.python.org/3/library/subprocess.html
