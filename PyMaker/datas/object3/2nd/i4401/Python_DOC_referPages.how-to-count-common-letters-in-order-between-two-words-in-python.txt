Natural Text
I have a string  and when comparing it to  - it is not the same. How can you make a program that counts common letters (in order) between two words, and if it's a 60% match then a variable  is ?For e.g.  and  have 4 out of 6 letters in common, which is a 66% match, which means  must be , but  and do not have any letters in order in common, thus  is 
You can write a function to implement this logic. is used to loop through the 2 strings simultaneously.
Optimised algorithm using :This will directly return the length of LCS.


Answer URL
https://docs.python.org/3/library/difflib.html#difflib.get_close_matches
