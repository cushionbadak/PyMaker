Natural Text
Python doesn't provide  on generators. What is most efficient way to check if generator contains at least N elements.Some test cases:My naive implementation:Is there any more pythonic way to do this check?
You can  the iterator with  and then iterate and count the elements:Keep in mind that this will consume the next  elements of the iterator:There's no way to avoid this, but we can split the iterator into two with  and return one of the new iterators along with the result:


Answer URL
https://docs.python.org/3/library/functions.html#zip
https://docs.python.org/3/library/itertools.html#itertools.tee
