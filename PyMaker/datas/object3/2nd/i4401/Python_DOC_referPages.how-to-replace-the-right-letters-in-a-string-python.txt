Natural Text
The assignment is:Your task is correcting the errors in the digitized text. You only have to handle the following mistakes:S is misinterpreted as 5O is misinterpreted as 0I is misinterpreted as 1My code: I know this solution will not work for a word like: Does anyone have tips on how to make this a more general function that will work for every word?
Don't use , since that only does a test if the previous one failed. Change them all to ordinary  and the code will work correctly.But as mentioned in the comments, there's no need for any of the tests. If the letter isn't in the string,  will just return the original string, so there's no harm.
You can use  directly.
Why don't you make use of  and :Wrapped in a function:
Here's another method using list comprehension:


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.maketrans
https://docs.python.org/3/library/stdtypes.html#str.translate
