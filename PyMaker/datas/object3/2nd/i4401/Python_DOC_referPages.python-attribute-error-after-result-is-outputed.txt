Natural Text
My program reads a file line-by-line and uses reg-ex at every line.The program executes well for all lines, but throws the following at the end:My code is the following:
According to document,   returns None if no position in the string matches the pattern.So, may be you need to check if pattern search returned something before calling . May be you can try your  as following:May be something like below for above code:


Answer URL
https://docs.python.org/3/library/re.html#re.search
