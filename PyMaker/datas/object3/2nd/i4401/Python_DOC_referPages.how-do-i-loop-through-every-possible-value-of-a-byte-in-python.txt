Natural Text
I am conducting a Padding Oracle Attack for my Information Security course. Without getting into the details of the attack, I need to have a for loop that loops through all possible 1 byte hex values.Pseudo-code of what I need to do:I cannot figure out how to accomplish this. Any ideas?
Bytes are really just integers in the range 0-255 (inclusive), or in hex, 0 through to FF. Generate the integer, then create the byte value from that. The  type takes a list of integers, so create a list of length 1:If you store the ciphertext in a  object, you could even trivially replace that last byte by using the integer directly:


Answer URL
https://docs.python.org/3/library/stdtypes.html#bytearray-objects
