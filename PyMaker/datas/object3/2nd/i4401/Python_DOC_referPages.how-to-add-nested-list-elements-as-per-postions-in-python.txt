Natural Text
Input will be :Result would be :The goal is given  to return a list where the values are 
You can group your data by element type:Output:
Per user comment:So the inline comments can be helpful, but a quick breakdown and links may do you some good:isinstance checks if the element in  is a  object or not (since you have lists within lists)map does what its name implies, it "maps" a function () to each list element within list is used to convert the result from  to a  object that shows with the zip simultaenously iterates over multiple iterables so we can call  on the iteratorlist comprehension is a fast way to make a ; this is the  syntax


Answer URL
https://docs.python.org/3/library/functions.html#isinstance
https://docs.python.org/3/library/functions.html#map
https://docs.python.org/3/library/functions.html#func-list
https://docs.python.org/3/library/functions.html#zip
https://docs.python.org/3/tutorial/datastructures.html#list-comprehensions
