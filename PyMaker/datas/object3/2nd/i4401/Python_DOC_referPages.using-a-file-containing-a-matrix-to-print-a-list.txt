Natural Text
I have a score_matrix.txt file which contains:I'm trying to code a function "read_scores" that consumes a file name (string) as its parameter and returns a list of list of ints (4x4 matrix).This is what I have:
You can use  with :Output:
You can use  and read it into a :And if you prefer it as a list of lists, you can call :
You can also use  to read and process csv files:Output:You can also turn the resulting  to a matrix using:Output:


Answer URL
https://docs.python.org/3/library/functions.html#map
https://docs.python.org/3/library/stdtypes.html#str.split
https://docs.python.org/3/library/csv.html
