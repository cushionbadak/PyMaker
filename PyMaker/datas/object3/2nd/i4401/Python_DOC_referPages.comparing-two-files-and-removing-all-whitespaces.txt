Natural Text
Is there a more elegant way of comparing these two files?Right now I am getting the following error message: ... .Python seems to read  as a literal character.
The constructs you are using are interpreted by  and do not form a standalone statement that you can pass to  or .As such, you will need to wire-up the redirection and pipelines yourself, or call on  to interpret the line for you (as @wizzwizz4 suggests).A better solution would be to use something like  that will perform this internally to your process rather than calling on  /  / .Using  will give you a similar result:
This will fail when you fix the syntax error because you are attempting to use  syntax in what is implemented as a C  call.If you wish to do this in this way, either write a shell script or use the following:This is not an elegant solution, however, since it is relying on the GNU coreutils and . A more elegant solution would be pure Python. You could do this with the  module and the  module.


Answer URL
https://docs.python.org/3/library/difflib.html
https://docs.python.org/3/library/difflib.html#difflib.unified_diff
https://docs.python.org/3/library/difflib.html
https://docs.python.org/3/library/re.html
