Natural Text
This question already has an answer here:In Python, how do I determine if an object is iterable?                    20 answers                In this question there are numerous ways to test for an iterable.  Two of the solutions are:They both seem to do the same thing and I can't find any doc that differentiates them.  What is the difference and why would I choose one over the other?
The pythonic way isassume it is iterablecatch the exception if it is notHowever, use it with common sense. That's, do it only if you have reason to believe that your object is iterable in the majority of cases.Also, take care of strings, which are treated as iterables, but in most practical use cases, they should not be. In that case, the practice is to explicitly check 


Answer URL
https://docs.python.org/3/glossary.html#term-eafp
