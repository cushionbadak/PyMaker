Natural Text
Making the replace case insensitive does not seem to have an effect in the following example (I want to replace jr. or Jr. with jr):Why? What am I misunderstanding?
The  argument is actually a convenience as an alternative to specifying . It has no bearing on replacement if the replacement is not regex-based. So, when , these are your possible choices:Or,You can also get cheeky and bypass both keyword arguments by incorporating the case insensitivity flag as part of the pattern as . SeeNote  You will need to escape the period  in regex mode, because  the unescaped dot is a meta-character with a different meaning (match  any character). If you want to dynamically escape meta-chars in patterns, you can use .For more information on flags and anchors, see this section of the docs and  HOWTO.From the source code, it is clear that the "case" argument is ignored if . SeeYou can see the  argument is only checked inside the  statement.IOW, the only way is to ensure  so that replacement is regex-based. 


Answer URL
https://docs.python.org/3/library/re.html#re.escape
https://docs.python.org/3/library/re.html#re.IGNORECASE
https://docs.python.org/3/howto/regex.html#regex-howto
