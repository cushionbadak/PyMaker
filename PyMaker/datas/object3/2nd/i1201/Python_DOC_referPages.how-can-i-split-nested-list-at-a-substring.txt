Natural Text
Where  can be as large as needed and  can be as large as needed..how can I get out  ?I have tried somethings along the line ofand just general list comprehension with .join, but have had no luck.
should give you what you want. Works like this: makes list from every stringyou then add those using sum:  is basically  uses list comprehension to make a split list from all pieces of small list (), which is the fed to and it's all wrapped in another comprehension that runs it for every part of your big list 
You could use a nested list comprehension:Output
You can use  to flatten the results of splitting the strings in a list into a single list
Here's a one-liner using list comprehension.
Output:



Answer URL
https://docs.python.org/3/tutorial/datastructures.html#list-comprehensions
https://docs.python.org/3/tutorial/datastructures.html#nested-list-comprehensions
https://docs.python.org/3/library/itertools.html#itertools.chain.from_iterable
