Natural Text
This is what it outputsI have managed to get to that stage but i'm still having issues as for some reason it is separating the values in the array.
Here you change  applying  to it. For example, To fix this issue, use : For single input,
You are using  function, which expects an iterable or more than two arguments to select the higher value as its definition says: Return the largest item in an iterable or the largest of two or more arguments", But you are passing an integer
Python is telling you the problem, total_price is not iterable because it's an int, it's like asking what max(7) is. You probably want to do something like store all 5 inputs in some array and then call sum, max, min on that array.Example --
If you store the prices in a list, which is iterable, you can then find the price of the most expensive sweet. Such as: See https://docs.python.org/3/library/functions.html#maxFor finding max in tuple, such as the title suggests, try 


Answer URL
https://docs.python.org/3/library/functions.html#max
