Natural Text
I have list of lists:  and I need to convert it to dict like: . Also it possible to have list of strings as value with  some names of errors.I saw variants with dict(), but it doesn't work for me because empty values is possible.I tried to use , but I have error: "dictionary update sequence element #0 has length 1; 2 is required".
You could add  to the end of each sublist, then take the first two items.e.g. using  to print nicelyedit: To perhaps be clearer we could also use  to give a name to what we're returning:Then the above solution becomes:
You can do this:This works by making the dictionary  and assigning it's keys to be the values of  and their values to either  or the second item in the corresponding list.
Just a small check and a generator
You can use a generator with basic conditional. 
There are probably easier ways, but you can use:Output:


Answer URL
https://docs.python.org/3/library/pprint.html
https://docs.python.org/3/library/operator.html#operator.itemgetter
