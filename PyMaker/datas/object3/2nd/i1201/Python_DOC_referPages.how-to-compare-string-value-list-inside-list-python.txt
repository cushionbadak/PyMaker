Natural Text
How to compare string in listI want to compare every value between two lists.For one, I want result to be (values in b but not in a): Other one to be (values in a but not in b): 
This is a job for setsConvert your lists of lists to sets of tuples (you can't have sets of lists, as sets can only contain hashable objects, which lists, as all built-in mutable objects, are not)or create them directly as sets of tuples:You can then easily and efficiently get your differences:     or even the intersectionor the union:
If you want to have a quick solution without much concerns about performance you can use 


Answer URL
https://docs.python.org/3/library/stdtypes.html#set
https://docs.python.org/3/glossary.html#term-hashable
