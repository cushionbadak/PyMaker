Natural Text
I need to find a string in a file and reformat it.String to find format:example:I would like to change that to HTML link:example:What is the best way to do it?I am thinking regular expressions but I have no clue to how achieve that. Is there a simple way?
You  could take advantage of the fact that the sub function receives another function as a parameter for replacement:OutputThe pattern  captures everything between brackets followed by something link like (starts with http), notice that you must escape brackets and parenthesis because they have a special meaning inside a regex.Or as suggested by @JonClements, you could use:instead of the  function.
You can reach your desired result using  as Daniel said, But if you dont want to use regex, you can do this with :if you are using Python 3.6 or higher:


Answer URL
https://docs.python.org/3/library/re.html#re.sub
