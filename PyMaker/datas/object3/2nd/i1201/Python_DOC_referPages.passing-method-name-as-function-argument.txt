Natural Text
I'm probably missing something obvious here, what I'm trying to do is extract part of this Django view into a function since I'm using same code multiple times:I would like to pass model_name as a method name (in bold). What is the correct way to do this?My solution would be calling 
You can use  to access attributes by name: implicitly goes through the various attribute lookup options:, , Usually, you do not call magic methods directly. They often are hooks for built-in methods or operators, e.g.  calls ,  calls . 


Answer URL
https://docs.python.org/3/library/functions.html#getattr
