Natural Text
I have a python list  containing instances of the class :Now I want to sum all  members of the classes  if their  is equal to obtain this list:What is the most pythonic way to do this?
There is (almost?) nothing that  cannot do. Take a look at :which results in:
One way would be to create a  that maps ids to sums of values.  Then we can take those results and use them to build a new list of .  One way to do that is to use  to map the items of that dictionary to the arguments to 
You could also get the desired result using  to get just the unique ids and  to total the values. For example:


Answer URL
https://docs.python.org/3/library/itertools.html#itertools.groupby
https://docs.python.org/3/library/collections.html#collections.defaultdict
https://docs.python.org/3/library/itertools.html#itertools.starmap
