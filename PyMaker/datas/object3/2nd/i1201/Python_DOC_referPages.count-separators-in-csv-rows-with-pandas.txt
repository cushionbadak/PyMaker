Natural Text
I have a csv file as follows:And when I put it into a dataframe it looks like:How would I get the count of a comma in the raw row data. For example, the answer should look like:
Very simply, read your data as a single column series, then split on comma and concatenate with separator count.Another solution for concatenation is to  on the index (this is a neat one liner):
Doing this Data 
You can use the  module for the counting delimiters. This is a two-pass solution, but not necessarily inefficient versus alternative one-pass solutions.
One line of code: 


Answer URL
https://docs.python.org/3/library/csv.html
