Natural Text
I made this simple Python  Maths Quiz Program for some reason sometimes when it is supposed to ask a question it doesn't display the question or allow any input and just says it is incorrect. The problem doesn't happen every time I run the program just sometimes.
Your code's problem is that you do not always call the input:if  no input is done and the one from before is used (again).You can solve it by changing one of them to  or use an additional .You can shorten/improve your code a lot if you do not  multiple timesuse a dictionary to decide which function to call (reduces use input validation to avoid crashing on bad inputuse  or even better f-strings instead of python 2.7 % formattinguse   for file operationsstructure the code with some more functionsUsage:Output:
Your  line only runs in two situations:andWhat happens when  and  are the same? You won't enter the  block, because  is ; And you won't enter the  block, because  is also .That means the  won't run at all;


Answer URL
https://docs.python.org/3/library/string.html#custom-string-formatting
https://docs.python.org/3/whatsnew/3.6.html#whatsnew36-pep498
https://docs.python.org/3/tutorial/inputoutput.html#reading-and-writing-files
