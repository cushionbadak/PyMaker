Natural Text
I was wondering if this same code could be made with a  instead of a 
As GLHF reminded me, the  should be an int in order for the code below to work.I see no reason for a variable , representing a number of bounces to be a float. Moreover, in your original code you have a comparison between  (an int) and  (float from user input).In a case, where b is not a float with a  for the decimal part, the check would fail, so you might want to change the line to range()
Since you don't change your variables from  type to  you can't do it, because you need  function and  only accepts integer type.If you set your variables to an integer instead of float, you can use it in a for loop like;


Answer URL
https://docs.python.org/3/library/functions.html#func-range
