Natural Text
This question already has an answer here:How to convert a string to a number if it has commas in it as thousands separators?                    8 answers                I am using beautifulsoup4 to extract prices tag from a website. The code i m using is thisThe output i m getting is a string with a format with commas in it. e.g.What i want:Change the format from string price to integer price without commas in it so that i can use them as values intead of strings in excel
You can do this :
Checkout https://docs.python.org/2/library/string.html or https://docs.python.org/3/library/string.html depending on the Python version you are using and use the "replace()" function:This replaces all commas within the string and then casts it to an INT:
If the last part is a fractional part, you could do something like this:Output:
or another method will be,


Answer URL
https://docs.python.org/3/library/string.html
