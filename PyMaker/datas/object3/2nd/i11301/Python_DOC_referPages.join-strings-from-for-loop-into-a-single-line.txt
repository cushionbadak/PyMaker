Natural Text
This question already has an answer here:Python: for loop - print on the same line [duplicate]                    3 answers                I have a grammar tag producing nltk block which is,This gives me the following output,However, I want the output to single lined like How do I do this?  
If you want not just print, but store the result in a string, you can use  and a single list comprehension:Note that the  is a common variable name for throwaway variables.
I dont think ultimately you want to print the whole string and keep using  so assign it to new variable is explained below. Initialize a variable tag_str on top and use it after print statement like this.
If you aren't using Python 3, you could write directly to , which would enable you to skip the newline character.
Do thisThat should leave a space and not go to next line.
If you are using python 2.xuse , the  puts output in the same line. so you can use If you are using python 3use  or  depending on whether you want whitespace or not.


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.join
https://docs.python.org/3/tutorial/datastructures.html#list-comprehensions
