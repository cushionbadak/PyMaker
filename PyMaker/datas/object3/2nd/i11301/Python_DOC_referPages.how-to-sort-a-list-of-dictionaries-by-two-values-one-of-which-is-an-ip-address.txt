Natural Text
This question already has an answer here:How to sort IP addresses stored in dictionary in Python?                    6 answers                I have a list in Python that shows the network location of some machines:And I would like this sorted by role first and then numerically by address, so the output should look like:So far I have:But this doesn't sort the IPs numerically, it reckons  is less than  for example.
To numerically sort the IPs you need to split the address but the general approach of @sparkandshine still works, with just a slight update to the key:
To sort by IP addresses like you want, this could work :Input Output 
Convert ip address as a list of s using , i.e., Or use the Python module ,


Answer URL
https://docs.python.org/3/library/ipaddress.html#module-ipaddress
