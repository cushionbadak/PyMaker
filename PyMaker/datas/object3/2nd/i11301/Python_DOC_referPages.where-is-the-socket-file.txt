Natural Text
I'm looking through python built in library modules, and for example in  I see the line:I understand that the socket module acts as a wrapper for .  I want to read through some of the source code files within  to see how certain tasks are accomplished. Where can I find  or any of these other shared files on a Linux box?
 is a C extension. The  module wraps this with some additional information that doesn't need the speed boost or access to OS-level C APIs.If you are versed in C, you can read the  source code. There is no one-on-one mapping between the final  or  file and the original source file however. You can grep the  file for the names instead:Take into account however that some modules are built-in, compiled as part of the  binary; these are all listed in the  tuple.
You can use the  attribute:In python packages you can also use the  attribute (for the directory):


Answer URL
https://docs.python.org/3/library/sys.html#sys.builtin_module_names
