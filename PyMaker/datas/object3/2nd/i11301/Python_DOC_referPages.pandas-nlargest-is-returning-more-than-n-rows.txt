Natural Text
I have a  that looks like this:I want to get the top 3 rows (according to value) preferably per date. I'm expecting something like this:but I'm ok also with this:I tried  but I get this weird result:I tried running it day by day:but I got the same problem (each name is duplicated 3 times) 
To start, this will get the job done:
Use  slice of  resultUse  in descending mode and take the first  results in a slice, then use  to keep the days monotonically increasing. 


Answer URL
https://docs.python.org/3/tutorial/introduction.html#lists
https://docs.python.org/3/tutorial/introduction.html#lists
