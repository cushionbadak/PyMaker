Natural Text
I have two arrays (A & B).I would like to calculate the % of users of array A , who are included in array B.I have tried but I can´t find the solution.
If  is large, use a set for efficiency:
The most pyhtonic way is like Rogalski above commented. Python in my opinion is very strong at sets:  https://docs.python.org/2/library/sets.htmlyou can make an intersection in two ways set(A) & set(B) or set(A).intersection(set(B))And the formula is like mentioned above (just corrected)
You only need to create one set and sum the times an element in a is in the set of the members in b:If you actually have numpy arrays:


Answer URL
https://docs.python.org/3/library/array.html
