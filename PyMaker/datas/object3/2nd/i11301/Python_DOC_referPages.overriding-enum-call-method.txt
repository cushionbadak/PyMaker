Natural Text
I have an  like so:I would like to override the  method so that a call like           returns  or  instead of raising . I would rather avoid a  statement everytime I call the . What would be a pure Python equivalent of  ?
Update 2017-03-30With Python 3.6 (and 1) you can specify a  method that will be called to give your class one last chance before raising .  So now you can do:Original AnswerTo be clear: you want the  that is associated with  which is actually on the metaclass ( in ).This is a bag of worms you don't want to get in to, as it is very easy to break things.See this answer for more details, but the simple solution is to create a  method for your  enum:and then  will return .1 Disclosure:  I am the author of the Python stdlib , the  backport, and the Advanced Enumeration ()  library.


Answer URL
https://docs.python.org/3/library/enum.html
