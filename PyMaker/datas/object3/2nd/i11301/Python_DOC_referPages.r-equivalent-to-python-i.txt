Natural Text
Typing  at the command line runs  and then drops into the python terminal preserving the run environment.https://docs.python.org/3/using/cmdline.htmlIs there an equivalent in R?
I may be misinterpreting what  does, but try:From inside R, at the terminal, you can do:and it will run , with the global environment reflecting what was done in If you're trying to run from the command line, review this post


Answer URL
https://docs.python.org/3/using/cmdline.html
