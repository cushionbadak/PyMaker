Natural Text
Let's say I have two lists generated by another function:I want to convert them to associative arrays for quick lookup like so:I can do it like this:But, that isn't very Pythonic.  Is there a more Pythonic way to accomplish this?
Use :
You could also do this by dictionary grouping, something like this:Or you could use setdefault(), if the key is not available:These can easily be manipulated to use defaultdict to if you want to.
This seems like a perfect use case for :Note that  works on iterables, and that it returns a  object by default. Results are:


Answer URL
https://docs.python.org/3/library/collections.html#collections.defaultdict
