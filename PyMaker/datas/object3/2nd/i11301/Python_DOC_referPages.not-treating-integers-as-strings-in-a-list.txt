Natural Text
This question already has an answer here:How to convert strings numbers to integers in a list?                    4 answers                Given this list:How can I produce a list where integers are not treated as strings? Like this:
You can use list comprehension and  to convert each character that is a digit:
You can convert all strings in a list containing only digits this way, using  and :For example:@niemmi's solution using list comprehensions is probably a better approach given that we start with a string, not a list.


Answer URL
https://docs.python.org/3/tutorial/datastructures.html#list-comprehensions
https://docs.python.org/3/library/stdtypes.html#str.isdigit
