Natural Text
is there a way to use assertTrue() or assertFalse() like a function in pytest for python unittests.I have a function which if returns with list of elements, the test needs to get failed through assertion.is there anything like below:
Why not test for the length of the list:
You can  to confirm list is not empty, or  to confirm list is empty:So in your case, you can just write down:You can read more about Truth Value Testing on python.org.


Answer URL
https://docs.python.org/3/library/stdtypes.html#truth-value-testing
