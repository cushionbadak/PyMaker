Natural Text
I would like to learn how to call a function asynchronously in Python3. I think Tornado can do this. Currently, my code is returning nothing on the command line:Thanks
To call an async function, you need to provide an event loop to handle it. If you have a Tornado app, it provides such a loop, which allows you to make your handlers asynchronous:Outside of a Tornado app you can get the event loop from any number of providers, including the built-in asyncio library:


Answer URL
https://docs.python.org/3/library/asyncio.html
