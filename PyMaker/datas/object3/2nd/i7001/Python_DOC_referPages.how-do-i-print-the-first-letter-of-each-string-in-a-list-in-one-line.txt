Natural Text
For one of my tasks, the user inputs multiple strings until they enter a blank line and then it prints it out in one line, this is how I did it. However, there's another part where it takes the first letter of each string in the list and prints it out in capital letters in one line. I cannot figure out how to print this on one line. This is my code:e.g. If I entered : it would print outACEinstead of ACECan someone assist me thank you (:
Lots of possibilities, but this is how I would do it:
Updated my answer since @Smarx gave an identical one.Considering:This is how I'd put it:    Prints
your problem is that the print function going one line down after each print, to solve it you can use the end parameter:will print:abinstead of:ab
Create an empty string outside of the for loop and add each letter to it inside the loop. Once the loop ends, print the string.An alternate method is to change the terminating character of the print to nothing:


Answer URL
https://docs.python.org/3/library/functions.html#print
