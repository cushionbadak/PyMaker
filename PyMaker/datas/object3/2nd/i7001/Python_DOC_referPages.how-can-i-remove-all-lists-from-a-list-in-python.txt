Natural Text
What is the most pythonic way to remove all lists in a list?For example, if there is a list like this , how can I make it like this Here is what I've tried:
You can use a list comprehension to easily filter all non-list elements from your list:Note I used  rather than . This is for two reasons. The former function is preferred because it takes parent classes into account. And because  allows you to easily extend the list comprehension to filter out other types such as s or s:
I would use list comprehension:


Answer URL
https://docs.python.org/3/library/functions.html#type
