Natural Text
I wonder if I can return value in list comprehension likeOf course it doesn't work. At the moment I had to use classic for loop
Use the  function with a generator expression:This will return the first matching  value, or  if there is no such value.You can't use a list comprehension, no, because you are not producing a list.
No. The purpose of a list comprehension is to build a list, not to save lines of code. Since you do not want a list as the result, a list comprehension is the wrong tool for the job. A loop is perfectly fine.
The closest thing would be a calling  with a generator expression:


Answer URL
https://docs.python.org/3/library/functions.html#next
