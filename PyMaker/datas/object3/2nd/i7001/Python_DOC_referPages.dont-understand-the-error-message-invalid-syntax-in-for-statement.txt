Natural Text
I am writing a very simple program to output the 0-10 in numbers using a for loop. However it comes up with a syntax error when I click run, highlighting in red the "=" in the 8th line. I don't understand why it is wrong? I am using python 3.5.2 in idle mode.
Try it like this:
This is wrong syntax for . As per docs:The for statement is used to iterate over the elements of a sequence (such as a string, tuple or list) or other iterable object.which is not your case, or you can use  to create the sequence:yet it's artificial workaround and while it will work, is not what you should be doing really.You should use  instead. Docs say:The while statement is used for repeated execution as long as an expression is true
A couple of points. The excerpt:has multiple errors. is not valid python syntax. is not a valid  statement.The lines  and  are lacking proper indentation.Replace those four lines with


Answer URL
https://docs.python.org/3/reference/compound_stmts.html#for
