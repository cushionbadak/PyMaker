Natural Text
This question already has an answer here:Transpose nested list in python                    4 answers                Transpose list of lists                    10 answers                It should work likeso far i have done this:It's not working every time..For example:it's working forbut not for
You would usually do that in Python using the  function:The additional  call is to convert the elements returned by , that are s, into s, but if you are okay with tuples you can just do:Also, the outer  is only necessary in Python 3, where  returns a generator, but not in Python 2.


Answer URL
https://docs.python.org/3/library/functions.html#zip
https://docs.python.org/3/library/functions.html#map
