Natural Text
I'm trying to configure my Python logging module using logging.config.dictConfig()However, my JSON file appears to be corrupt even though it works as a Python dict with  changed to , so I don't think it's a syntax error.My code says:My  file is:When I run that simple program, I get an error: What could be causing the JSON decoder to fail?Thanks in advance for any help.
You have multiple extraneous commas which will break the JSON deserialization.Here's your original JSON with every extraneous comma pointed out:Removing those, this is now a valid JSON document:
This happens because your JSON is invalid. this one JSON is valid.I removed extra commas from your JSON.You can use any JSON validator to fix it.
As others already answered, there are some excess commas in your JSON.You could feed your JSON to https://jsonlint.com/ ; it tends to give more useful help messages.


Answer URL
https://docs.python.org/3/library/logging.config.html#logging.config.dictConfig
