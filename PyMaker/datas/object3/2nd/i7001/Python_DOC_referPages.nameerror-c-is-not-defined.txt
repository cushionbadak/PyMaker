Natural Text
This question already has an answer here:Behavior of exec function in Python 2 and Python 3                    4 answers                How does exec work with locals?                    3 answers                I write below code in python(version 3.6.2):when run it, I get below error:but when  run below code :it print 1.How do I fix this?
The exec() function can only be followed by a string or an object. What you asked it to do is not one of those. You should try just defining c in another way or stop using exec() 


Answer URL
https://docs.python.org/3/library/functions.html#exec
