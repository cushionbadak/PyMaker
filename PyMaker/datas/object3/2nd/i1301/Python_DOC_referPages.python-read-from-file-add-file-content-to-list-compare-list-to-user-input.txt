Natural Text
Ok so as the title states am retrieving a word from a txt file then adding it to a list then am trying to compare the content of the list to user input. I’ve compared types and they match, have tried python 2&3 and nothing. I’ve also tried with a preset array and it works fine. I’ve tried to separate halves into functions and still no. Repr gives:List = ‘a\n’Input = ‘a’
The string you are reading from a file has a new line at the end.You can easily remove it using  (remove surrounding whitespace) or  (remove trailing whitespace).For example:
Does this help?


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.strip
https://docs.python.org/3/library/stdtypes.html#str.rstrip
https://docs.python.org/3/library/functions.html#repr
