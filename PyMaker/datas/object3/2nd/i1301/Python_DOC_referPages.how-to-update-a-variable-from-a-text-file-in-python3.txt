Natural Text
So, let's say that I got a shell script that generates a v.py on the output that contains  And I'm accessing it in the main.py script via But later the main Script toggles generation process again and the variable in v.py gets updated, but the variable  is not updating for the main script.To make main script work I need to update the variable from v.py while script is runningI've tried  - didn't workI'm still new to python and don't understand it completely
Presuming you are using python 3.*, v needs to be a module, so the way you import it changes to example below.Also note the document of importlib "When a module is reloaded, its dictionary (containing the module’s global variables) is retained. Redefinitions of names will override the old definitions, so this is generally not a problem."Though our minimal example works, this is something to bear in mind for more complicated cases.


Answer URL
https://docs.python.org/3/library/importlib.html
