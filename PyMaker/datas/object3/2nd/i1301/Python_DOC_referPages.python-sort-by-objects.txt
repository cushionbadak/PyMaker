Natural Text
How can I sort by name and age in PYTHON?I have the following list in .txt file:I want to sort this by name and by age. I wrote this code but it doesn't work:Thank you very much guys :)
Here's one approach:Using:a context manager to handle automatic file closure (this is the )the  iterator to loop over the file's lines one at a timea list comprehension to split the lines into lists as needed instead of changing all  references to  -- you could make the lines proper classes (or s if you wanted the  access.Though, in general, solutions for parsing csv files exist (e.g.  -- there were a few more issues in your code than just that.


Answer URL
https://docs.python.org/3/library/csv.html
