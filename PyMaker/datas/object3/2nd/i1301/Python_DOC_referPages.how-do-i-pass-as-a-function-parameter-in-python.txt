Natural Text
I am working with python3 and pandas, and I would like to pass  as a function parameter to state all rows in a slice passed to .For example, say I have a function that fills na values like so:Sometimes I may not want to apply it to some rows but to apply it to all rows, in pandas this is accessed with If I am calling this from a function it would like But the above will give me a syntax error because of the ; how can I pass this as a function parameter?
Use  to represent . Note you can use  to pass your dataframe through a function, and  accepts a list for row and index filtering:Here's an example using a  instead of a  object:


Answer URL
https://docs.python.org/3/library/functions.html#slice
