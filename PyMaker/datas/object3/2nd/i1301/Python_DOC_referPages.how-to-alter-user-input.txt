Natural Text
So atm I'm making a table in python, and for it, I need the user to supply a name of a person for the table (e.g. David Beckham). However when the user has entered this and the table appears, the name needs to look like this: . How would I go about doing this?
Just store the input in a variable:
With Python 3.6+ you can use formatted string literals (PEP 498). You can use  with  to account for middle names:


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.rsplit
