Natural Text
Hello I have a python string in such a wayI want to count the number of trailing new line characters in the string in this case it is 2.If I use  it just removes new lines and returns the string but not how many new line characters it has removed in the process.How to get the count as well as remove the trailing new line characters.Thanks.
Use :Then just check the difference:
Returns 2Then 
You can count the number of newline characters before removing them. which should return the number of new lines.
Quick, but perhaps "dirty", is to do it in two steps:Count: 2Followed by


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.rstrip
