Natural Text
I have 2 date columns (begin and end) in a data frame where the dates are in the following string format '%Y-%m-%d %H:%M:%S.%f'. How can I change these into date format in python? I also want to create a new column that shows the difference in days between the end and begin dates.Thanks in advance!
If you're using a recent version of pandas you can pass a format argument to :Note: it isn't necessary in this case to pass format but it may be faster/tighter:
The  module has everything you need to play around with dates. Note that in the format you describe  the  does not appear in the known directives and is not included in my answer


Answer URL
https://docs.python.org/3/library/time.html#time.strftime
https://docs.python.org/3/library/time.html#time.strptime
https://docs.python.org/3/library/datetime.html#strftime-and-strptime-behavior
