Natural Text
I have one weird problem. I have following code:And output is something like this:So how can I get  instead of second ?
The magic  closure is set for the method context and only really meant for use by .For methods you'd want to use  instead:or better still, use :If you want to be able to call the method on a class, then use the  decorator to be handed a reference to the class object, rather than remain unbound:s are always bound to the class they are called on, so for  that'd be , for  you get handed in .


Answer URL
https://docs.python.org/3/library/functions.html#classmethod
