Natural Text
I'm having some trouble importing own packages in my programs, and so I made a test folder to try and understand what I'm doing wrong.It's the simplest of things, But I still can't get it to work.This is my folder structure:Both -files are empty. looks like this:and  looks like thisWhen running the code in PyCharm, everything works fine! But when trying to execute from cmd I get Is  my only option, or is there another reason why cmd will not cooperate?
Regardless of version, python has to know where to look for packages. Manipulating  is a quick and dirty option, which will break sometimes in the future, if your code grows more complex. Try making a package and install it via  or (Look for this at the nice distutils introduction)
In regular Python, there are only certain folders that are checked for importing packages and the  folder you have doesn't seem to be one of those files. To change this, edit  in  and then import .Also, instead of editing , you could add the  folder into the  folder within your Python directory. This will work because this is, by default, one of the folders in .
You say you execute it via: Problem is that  doesn't know where  is.Execute it as module:
If you do not want to modify scripts or directory layout you can use  environmental variable.ExampleMore about searching modules - https://docs.python.org/2/tutorial/modules.html#the-module-search-path


Answer URL
https://docs.python.org/3/distutils/introduction.html
