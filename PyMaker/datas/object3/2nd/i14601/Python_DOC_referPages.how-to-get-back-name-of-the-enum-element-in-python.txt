Natural Text
I have an enum defined like this:I have a function that returns status as  enum. How can I get the name of the enum value, and not just value?I would like to get , instead of 
You'd have to loop through the class attributes to find the matching name:The generator expression loops over the attributes and their values (taken from the dictionary produced by the  function) then returns the first one that matches the value .Enumerations are better modelled by the  library, available in Python 3.4 or as a backport for earlier versions:giving you access to the name and value:


Answer URL
https://docs.python.org/3/library/functions.html#vars
https://docs.python.org/3/library/enum.html
