Natural Text
I want to insert some variables  in a "TarFile Object". What is the correct way? Thanks.
This line:… makes a list with one element, , then loops over that one element.If you want the actual files in the directory on your filesystem whose name is , you have to ask the computer for those filenames, usually either using  (if you just want direct children) or  (if you want all descendents, recursively).In this case, because  knows how to recurse into directories, you probably just want the direct children. For example:


Answer URL
https://docs.python.org/3/library/os.html#os.listdir
https://docs.python.org/3/library/os.html#os.walk
https://docs.python.org/3/library/tarfile.html#tarfile.TarFile.add
