Natural Text
I want to filter a string in Python, to get only commas  and numbers .The result is:instead of:
With your current code, you simply match . Simply add a comma  as a valid character, and use a backslash to escape to the literal ():Outputs:The docs have further information regarding other special characters that must be escaped with a backslash to acquire the literal, such as  and .


Answer URL
https://docs.python.org/3/library/re.html#regular-expression-syntax
