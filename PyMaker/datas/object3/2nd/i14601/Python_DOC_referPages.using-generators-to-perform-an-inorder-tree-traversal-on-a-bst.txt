Natural Text
So given the following: only contains the root node, why?Here's the full code; note that the BST implementation is correct, it's the  implementation with generators that is somehow wrong.
 only creates the generator object, it doesn't actually run it. You need to iterate and yield the values produced by each of the subgenerators:This becomes a lot cleaner with the introduction of new syntax in Python 3.3+:


Answer URL
https://docs.python.org/3/whatsnew/3.3.html#pep-380
