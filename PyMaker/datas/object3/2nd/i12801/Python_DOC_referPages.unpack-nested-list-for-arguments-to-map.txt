Natural Text
I'm sure there's a way of doing this, but I haven't been able to find it. Say I have:Then how can I use  such that it passes ,  for the first iteration, i.e., it does , then  for the second, etc.?Trying  obviously does  for the first iterationTrying  does  for the first iterationI want something like  to do  on the first iteration.Expected output: 
It sounds like you need :This unpacks each argument  from the list  for you, passing them to the function . As with all the tools in the  module, it returns an iterator which you can consume with the  built-in function.From the documents:Used instead of  when argument parameters are already grouped in tuples from a single iterable (the data has been “pre-zipped”). The difference between  and  parallels the distinction between  and .
try this:
There was another answer with a perfectly valid method (even if not as readable as ajcr's answer), but for some reason it was deleted. I'm going to reproduce it, as it may be useful for certain situations


Answer URL
https://docs.python.org/3/library/itertools.html#itertools.starmap
