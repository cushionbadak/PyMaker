Natural Text
I have a function in a loop that takes 5 parameters, saves them and uses their new values over and over again. But it looks messy and I wonder if I can make a class for these variables, and just use an object as parameter in my function.Is it possible? If so, how do I do it? Thank you!
If I understood what you're trying to do, you might try using a dictionary.You can then pass that single object to a function, and the receiving function could look something like:


Answer URL
https://docs.python.org/3/library/stdtypes.html#dict
https://docs.python.org/3/tutorial/classes.html
