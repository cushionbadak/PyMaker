Natural Text
I have the dictionary I need to sort this dictionary first numerically, then within that, alphabetically. If 2 items have the same number key, they need to be sorted alphabetically. The output of this should be I tried using lambda, list comprehension, etc but I can't seem to get them to sort correctly. 
Using  with key function (order by value () first, then key ):
Sort on the dictionary's items (which are tuples) using . You can specify the sort key which will be by the dictionary's values, and then its keys:


Answer URL
https://docs.python.org/3/library/functions.html#sorted
