Natural Text
I have a program which prints 3 bits of information, forename, surname and date of birth. These are all stored in separate text files. Each line of one text file goes with the same line number of the other text files.I want to print the information like this:My code does this but is very long, is there a way to shorten my code and still print the information the way i want. The code below prints the information of 10 people.
File objects are iterators, so you could use  to aggregate the corresponding lines from all the files, e.g.
You can try this:


Answer URL
https://docs.python.org/3/library/functions.html#zip
