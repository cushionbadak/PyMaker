Natural Text
I have a model with a JSON field:The data contains some JSON such as:I want to be able to get a sum of the cost across all rows in the table with a filter. I tried the following but that didn't seem to work.
You're using the wrong aggregate.  counts the number of rows for which the expression is not null. If you want a sum, use :Note that monetary values and binary floating point math is a bad mixture due to binary floats not being able to represent all decimal values. Instead use a proper monetary type, or  in which case SQLAlchemy uses  to represent the results in Python.
You need to use  instead of  as argument of 


Answer URL
https://docs.python.org/3/library/decimal.html
