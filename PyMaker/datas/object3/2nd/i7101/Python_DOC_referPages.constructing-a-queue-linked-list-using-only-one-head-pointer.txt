Natural Text
I'm trying to construct a Queue linked list using only a head pointer (no tail).but i cant seem to enqueue at the end of the list. example: at the moment the code will: , however i would like reverse it .
This should do it:Besides some logic fixes (we need to create a new node and store it in ,  is just a variable pointing to a node), note we use  operator for testing for  and  constructor to set data (so we can create and assign new nodes without  var).For example:Outputs:Btw, note that such structure requires  insertion time and  deletion (pop) time. Double-ended queue (like the standard ) will do both insertion and deletion in constant time.


Answer URL
https://docs.python.org/3/library/collections.html#collections.deque
