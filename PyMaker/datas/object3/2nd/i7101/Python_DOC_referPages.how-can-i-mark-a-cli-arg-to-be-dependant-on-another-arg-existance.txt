Natural Text
I am using python3.4The question refers to the https://docs.python.org/3/library/argparse.html package  If I want an arg  to always acompany  or  and give an error message if one of those two is missing?  Examples:  That should work  That should work That should fail with informative error.  I am doing it right now in the code itself. No issue there, but is there an intrinsic way for the  lib to do this?  
You can do it using add_mutually_exclusive_group. Here example(test.py):Now let's check our script:Let's try with  and :Without any errors:Hope this helps.


Answer URL
https://docs.python.org/3/library/argparse.html
https://docs.python.org/3/library/argparse.html#argparse.ArgumentParser.add_mutually_exclusive_group
