Natural Text
I set the  to  and then run , but then I receive the error  . There's no error on PyCharm whatsoever, which confirms that syntactically this is correct. api.piThe directory structureAs you can see above, both  and  are in the same directory, and using the  line should import the script's code into  . For reference, this flask server was made using , following the official Flask docs, hence there is a  directory in the  directory. Also,  further imports stuff from the , so that's why I included it above for completeness. Does this have something to do with Flask/VirtualEnv/Directory structure? 
When module is used as main its name is  . And so relative imports do not work. You should use absolute imports.i.eHere is link for more info DocumentationNote that relative imports are based on the name of the current module. Since the name of the main module is always "main", modules intended for use as the main module of a Python application must always use absolute imports.Note: According to PEP8 use absolute imports rather than relative ones.


Answer URL
https://docs.python.org/3/tutorial/modules.html#intra-package-references
