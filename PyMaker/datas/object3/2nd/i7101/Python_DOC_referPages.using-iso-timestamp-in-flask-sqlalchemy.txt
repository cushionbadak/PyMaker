Natural Text
I have a date which I obtain from an API. It is in the form of .How can I accept this date into a model using Flask-SQLAlchemy?So far I have tried, and which gives me the errorAlso, when I retrieve it using a , I need it to be in the exact same format.The  python module parses it well but when I retrieve it from the table, I need to get .
You need to convert the string into a Python datetime object, which you can do using :Then you can safely set it on your instance and commit it.To get it back in the same format, use  on the other side:And of course remember to watch out that the order of the arguments is different between  and  :)If you need to change/update the format, see  here : https://docs.python.org/2/library/time.html


Answer URL
https://docs.python.org/3/library/datetime.html#datetime.datetime.isoformat
