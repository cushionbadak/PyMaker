Natural Text
I'm stumped. I'm coding Python 3.6.2, using PyCharm as my IDE. The following script fragment illustrates my problem:You can see that  finds three matches. However,  only calls the  function twice. If I change  to  then  still calls  twice, but picks up  and . Baffled. I thought it might be greedy vs. posessive, etc. but - changing  to  and the resulting behavior negates that. Can anyone explain? I'm trying to do some basic text parsing of SQL to refactor message formatting for a large number of files. I use regexr.com to prototype most of the regex stuff I do and it also finds three occurrences of the pattern in the line. Thanks.
See the documentation.  The fourth argument to  is , not .  Since  happens to be 2, you are telling it to only do two substitutions.  Instead, pass  by keyword:
By giving the fourth argument . If you put the other positive numbers instead of  than it will replace the string exactly the same number of time. output:


Answer URL
https://docs.python.org/3/library/re.html#re.sub
