Natural Text
I am trying to robocopy media from one windows machine to another on a network using robocopy and python 3.I have tried just about every combination I can of  and  but I am still getting errors.Here is my most recent try:I ma not sure if it is the backslashes, or what, but I always get the following response:Does anyone have any suggestions?
You either don't split the arguments (and use ) or split them completely (and don't use ), you can't split only the command from the arguments. Also, for Windows paths, you want to use raw strings to avoid problems with ASCII escapes being processed in paths by accident (prefixed with an , e.g. , and don't include the trailing slash or things get weird) so you might do:


Answer URL
https://docs.python.org/3/library/subprocess.html#frequently-used-arguments
