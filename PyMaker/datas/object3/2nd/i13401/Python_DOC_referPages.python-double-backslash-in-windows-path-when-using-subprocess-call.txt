Natural Text
I can't get this code to work.wherewhich, given my (probably wrong) use of , , and , expand to the following paths:I suspect that passing those double backslashes to Windows is a problem. It certainly chokes on Note that appending  did not help.
Appending  to string paths that contain delimited backslashes should do the trick.For example:Intuitively, that method gives you what you see when you call  on something, and is the answer to the occasional desire on Windows where you may wish you could store the result of printing a string object.  There are good posts on the difference between  and  for more context.


Answer URL
https://docs.python.org/3/library/pathlib.html
