Natural Text
For example, if ,  ,, how to compute ?Is there a simple way?
Yes: use modular exponentiation. Python's built in  function allows you to do this with its optional third argument:

I shall blithely assume that you are using  as the exponentiation operator here (rather than as XOR). You can either use the built-in  or you can code your own:


Answer URL
https://docs.python.org/3/library/functions.html#pow
