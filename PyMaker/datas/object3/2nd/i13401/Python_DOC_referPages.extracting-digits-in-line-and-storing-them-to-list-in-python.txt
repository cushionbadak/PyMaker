Natural Text
I have a text file which produces the string " Created Form XX," where XX is an integer. I would like to extract only the integer and save that to a list to be used later in my python program.Below is code for what I have been able to do so far but am not able to display the digitsHow do I print the output of the last line of code and assign to a list?
 returns a list of matched values (in your case list of numbers that got matched). You should assign the list back to a variable (so that you can use it later on) - If you want to print this list, you can simply do -For if you want to print each matched element in a separate line , you can use a  loop or  -You can keep a list at the top, that stores all the numbers you found the file, and extend that list with the  list. Example -


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.join
