Natural Text
I would like to simulate my problem in python, I'm new to python programming. Just wanted to check whether if there is a way to avoid calling same function(return same value every time) multiple times.Eg: Is there is a way to call fun() at one place and
Assignment can bind the same object to multiple names.Note that all names point to the same object, so if the object is mutable then it will appear to change for each of the names.
A very simple method to manually cache return values is this:Generally, what you are looking for is memoization. There are different approaches to do this, depending on the complexity of your queries; python 3 has the quite useful lru_cache.


Answer URL
https://docs.python.org/3/library/functools.html#functools.lru_cache
