Natural Text
hello  i have made a python script that tells me my usage of my disk on my nas over ssh but everytime i run the program i get this.i want to get rid of the b and the quotes around the 422G and the others.here is my code
That is because you retrieve a binary string, since a program can send all types of bytes through a channel, we can  it to a string with :Note that we here thus use the ASCII "codec", since that seems the one used, but there are of course several ways to encode a string in raw bytes.


Answer URL
https://docs.python.org/3/library/stdtypes.html#bytes
