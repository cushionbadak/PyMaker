Natural Text
The docs cover 3 usages of the slice() object: =  =   =  But how to do this when I don't know the length of the object in advance? Setting  to a very high value, like ? =  ? =  ?
You can set the  argument to  to continue indefinitely.
You may use Python's  function with all the capabilities provided by the slice notation. Internally, both  function and the slice notation uses the  object.In your slice notation , where you pass empty value, you may get the equivalent  function  via passing that value as . For example:usage of only  with empty usage of only  with empty both  and  as empty (copy of list)Few more example:


Answer URL
https://docs.python.org/3/library/functions.html?highlight=slice#slice
https://docs.python.org/3/library/functions.html#slice
https://docs.python.org/3/glossary.html#term-slice
