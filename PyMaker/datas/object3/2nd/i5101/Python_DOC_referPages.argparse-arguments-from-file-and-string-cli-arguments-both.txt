Natural Text
I want to launch my script like this:where  is a file and ,  are string arguments. I know how to parse file alone:But how to parse following arguments?
An argument prefixed with  is treated as if its contents were in the command line directly, one argument per line. So if the contents of isAnd you define a parser likeand you call your script asthen your arguments  through  will be set to 1 through 4, respectively.
You just add more  calls.Like this:


Answer URL
https://docs.python.org/3/library/argparse.html
