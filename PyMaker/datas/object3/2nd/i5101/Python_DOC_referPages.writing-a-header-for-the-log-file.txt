Natural Text
My logger looks like this:The output looks like:I would like to write the logger so that I get such an output format:So instead of logging key-values I would like to print the keys (column names) as a header.Is it possible do to so? 
I agree with Norrius that the logging might not be the right tool, but if you insist for whatever reason, you can log the header first, and then log only the values:Although this would log the timestamp at the header as well, so you need to play with the format between lines.


Answer URL
https://docs.python.org/3/library/csv.html
