Natural Text
What is the best way to generate empty, named lists? Do they need to be created manually? I had hoped the following would work:Actual result:Desired result:
Use a dictionary. There is rarely, if ever, a need to dynamically name variables from strings.
Using globals. Also you could try locals(), vars(), then check this URL to understand the differences.PS: Thanks the corrections from @ShadowRanger.As API defined:globals()Return a dictionary representing the current global symbol table. This is always the dictionary of the current module (inside a function or method, this is the module where it is defined, not the module from which it is called).The codes will be like below if using globals():Output:


Answer URL
https://docs.python.org/3/library/functions.html#locals
