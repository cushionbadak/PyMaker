Natural Text
BackgroundI want to create dictionary that each word has an unique id for word embedding purpose. The dataset looks like:The following function is building a dictionaryIt works and output QuestionWhen I use it for a large dataset (around 50w strings), it will cost around 30s (ENV mbpr15-i7). It is too slow and I want to search a solution to improve the performance but i have no idea at the moment.
Try the following code with . In my test case it works about x4 faster:
You can use  and  from itertools
Try something like this,Making a list of lists to flatter this the best option in case of the speed of execution.Result:    


Answer URL
https://docs.python.org/3/library/itertools.html#itertools.chain
