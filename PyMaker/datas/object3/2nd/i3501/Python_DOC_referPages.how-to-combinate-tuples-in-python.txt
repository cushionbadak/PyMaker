Natural Text
How to combinate following tuples? The solution should be:At the starting point I got  tuples. The number of tuples increases with every iteration and after every iteration a new tuple for recombining will be added. So after the first iteration I get:Then combinate those 4 tuples etc. Is there a possibility to do this dynamically until a stopping condition hits?
yes, its possible
Try with 
You can use recursion, thus only needing several  loops for any input dimension:Output:


Answer URL
https://docs.python.org/3/library/itertools.html
