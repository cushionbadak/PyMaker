Natural Text
Folks,I have a problem during including  to  namely: uses Robot library :And when I try to include  in my I receive:How can I mock this? This is posible at all?
Sure, the issue is just that you can't mock the  class where it is used (in ). You have to mock the class where it is declared (in ).Using mocks:Using monkeypatch fixture:Also note that the mocking is done for the scope of a single test only, so you can't  on top of the test module as the  will be unpatched there, raising the context error.


Answer URL
https://docs.python.org/3/library/unittest.mock.html
