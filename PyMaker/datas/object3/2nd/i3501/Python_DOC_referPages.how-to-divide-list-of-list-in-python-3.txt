Natural Text
Can we divide each element from list1 and list2? Output should again be in lists of list.
This is commonly known as . Python doesn't have a builtin function to do this, but it's easy to build yourself with a list comprehension.This is actually a  of s, though, so let's nest:EDIT: as Aran-Fey points out,  does exist as  in Python, which makes this:which is, admittedly, uglier than sin. But it makes my little functional heart go a-pitter patter.
IIUC
You can try the following using the built-in function :this will produce:


Answer URL
https://docs.python.org/3/library/functions.html#zip
https://docs.python.org/3/library/functions.html#map
