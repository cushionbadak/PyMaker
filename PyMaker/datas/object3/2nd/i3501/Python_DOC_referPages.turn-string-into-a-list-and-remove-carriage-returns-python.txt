Natural Text
I have a string like this:How do I remove all of the "\r\n", and then turn the string into a list like so:
 removes all whitespace; this includes  and :As described in the str.split docs:If sep is not specified or is , a different splitting  algorithm is applied: runs of consecutive whitespace are regarded as a  single separator, and the result will contain no empty strings at the  start or end if the string has leading or trailing whitespace.
To limit the split to  you can use :
Try this:Output:This first  replaces the  and the second one replaces the single quote from the string as you expected as the output.


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.split
