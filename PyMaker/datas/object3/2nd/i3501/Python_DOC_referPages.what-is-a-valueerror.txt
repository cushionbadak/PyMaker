Natural Text
I've found the following definition in the docs but it's still not clear to me:Raised when a built-in operation or function receives an argument that has the right type but an inappropriate valueDo you have any examples?
Take a look at  - it takes a number, but this number must be non-negative. If you try calling it with a negative number (which can't be done in real numbers' math), you'll get a :
An example that gives the specific  is this:which gives back:Many other math function will produce the same when given bad inputs (,  and so on...)


Answer URL
https://docs.python.org/3/library/exceptions.html#ValueError
https://docs.python.org/3/library/math.html#math.sqrt
