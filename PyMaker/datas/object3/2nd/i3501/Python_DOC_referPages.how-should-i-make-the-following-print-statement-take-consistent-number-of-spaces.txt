Natural Text
How should I make the following print statement take consistent number of space for each portion (epoch, time, loss, etc.). Currently, they are not consistent because sometimes some of the float values may take 5 spaces or 6 spaces (e.g 10.345, 2.123). Example:As you can see, the spacing in each section is not consistent. Pre-adding spaces does not work because the number of spaces to add have to be different depending on the length of the values (90/1770 vs 100/1770).I understand that I could do  in print format but the problem with my print statement is that some are fixed words like "Epoch", while some are variables, the values. How should I deal with case like this? I need to make the whole chunk consistent not just the values.
You already know how to add a width to floats:I understand that I could do {:5.3f} in print format You can do the exact same with integers, or any other type.That  means that  will show up with a space before it, like this:So it'll line up with the next line:If you haven't read the documentation for the Format Specification Mini-Language, and are just guessing at how you can change samples you found in random online code in hopes they'll do what you want, read the docs. It's all pretty simple.You also say:the problem with my print statement is that some are fixed words like "Epoch"The fixed strings don't seem to be a problem in the first place. If they are, you can make them all the same width by just manually adding spaces.If you really want to, you can store those fixed strings in variables and add format fields for them instead, so you have somewhere to add a width field:… but that seems kind of silly.
You can play with 'center','rjust' and 'ljust', too:


Answer URL
https://docs.python.org/3/library/string.html#format-specification-mini-language
