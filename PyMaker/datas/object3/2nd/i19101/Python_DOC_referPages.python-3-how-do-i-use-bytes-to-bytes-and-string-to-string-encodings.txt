Natural Text
Check out the bytes-to-bytes and string-to-string encodings in Python 3:http://docs.python.org/3/library/codecs.html#standard-encodings (Search for )How do I use these? I've tried using them in  and  but it didn't work.
Accessing them through the  module by a non-alias name seems to work, in Python 3.2 and above:Using an alias name (), Python < 3.2, or  all seem to fail.


Answer URL
