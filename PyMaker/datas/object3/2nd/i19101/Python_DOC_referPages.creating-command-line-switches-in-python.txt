Natural Text
For example, sqlmap uses .This command above lists all available switches in sqlmap, and  is a switch itself.When you are creating a python tool for use in terminal, what is the basic method to create a switch?A hello world example would be most appreciative!
These are command line options. You can use the stdlib  module for that.
The command line arguments a program was started with are exposed as a list of strings in , with  being the program name.Other, older standard-library modules to help deal with them include  and . You should indeed be using , though.


Answer URL
