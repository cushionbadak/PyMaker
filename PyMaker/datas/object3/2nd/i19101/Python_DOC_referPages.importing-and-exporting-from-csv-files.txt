Natural Text
So I am trying to create a code which can search the data file:By surname to retrieve and display the details for a contactBy date of birth to retrieve and display all contacts with a birthday in a particular month.Here is the code I have created:I keep getting this result when I try the code:I want to know why? someone please help?
You test for the surname on every row, then print  for every row that doesn't match.Break out of the loop when you find the name, and use the  suite for the  loop instead: on a  loop only is executed if you exhausted the iterable, so when you did not break out of the loop early.Your surnames are the first value on the line, so you would be better off testing if the line starts with the surname:Pro tip: when reading CSV files, use the  module:


Answer URL
