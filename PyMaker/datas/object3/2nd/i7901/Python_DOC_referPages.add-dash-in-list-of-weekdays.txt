Natural Text
I have a sequence:There are inputs like:And I want output like:I am very confused how to do it.
Here's one way to do it with . The items from the input list are clustered based on their distribution in the  by grouping those with an index difference of 1:You can make list  a dictionary if you need to avoid multiple  calls as in the current approach.


Answer URL
https://docs.python.org/3/library/itertools.html#itertools.groupby
