Natural Text
My code:What is supposed to happen:What happens:Where it probably goes wrong: The above would have worked (tried and tested) for a single list, however, it doesn't seem to work for nested lists.
By saying  you are indeed making a copy of . However, this is a shallow copy, and the elements within  will still be the same objects as the elements within .What you need to do instead is make  a deep copy of  in your . E.g.
You should use It will recursively copy your object.


Answer URL
https://docs.python.org/3/library/copy.html#copy.deepcopy
