Natural Text
I want to create a YAML file from scratch inside a python program (with PyYAML), to send it inside a HTTP request body. The closest thing I've found in the PyYAML docs is this:However, I find it ugly and not very maintainable, so I would like to be able to do something like this:So I can modify its structure in a programmatic way, with variables and so.How can I create a YAML document from a python object structure?
I think what you want is .For simple Python objects you can even serialize  directly:


Answer URL
https://docs.python.org/3/library/stdtypes.html#object.__dict__
