Natural Text
I currently finalising a Scrapy project however I have quite a lengthy  file.I noticed that in my  the pipe lines are show as follows (trimmed down):I have tried the following ways to rectify this.1.) I created a new file/folder and tried referencing it from pipeline in the same manner.Folder was  with a class name  then referenced in pipeline settings as .This threw me errors.I then thought I could export the module and import into my current  and it would take the reference from that. I added an empty  in my  directory and then added  but scrapy still throws an error of...Many thanks in advance!
When you start a scrapy project, you get a directory tree like this:And the generated  looks like this:But your scrapy project can reference any Python class as item pipelines. One option is to convert the generated one-file  module to a package within its own directory, with submodules.Notice the  file inside the  dir:The individual modules inside the  dir could look like this:You can read more about packages here.The  files are required to make Python treat the  directories as containing packages; this is done to prevent  directories with a common name, such as string, from unintentionally  hiding valid modules that occur later on the module search path. In  the simplest case,  can just be an empty file, but it can  also execute initialization code for the package or set the   variable, described later.And your  would contain something like this:


Answer URL
https://docs.python.org/3/tutorial/modules.html#packages
