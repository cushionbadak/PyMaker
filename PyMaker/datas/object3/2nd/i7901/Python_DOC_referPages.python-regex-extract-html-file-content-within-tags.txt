Natural Text
I have lots of html format files in a folder. I need to check whether each of them contains this tag: and need to extract only "QQ" and its content.I first read one of the files to test, but it seems my regex does not match.If I replaced fo_read as the tag it will match though.

You can try with BeautifulSoup:Output:


Answer URL
https://docs.python.org/3/library/html.parser.html
