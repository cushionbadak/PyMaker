Natural Text
Write a function, top_three, that takes a list as its argument, and returns a list of the three largest elements. For example, . My attempt:
Don't wrap your input in another layer of .should be:or it's just sorting a one-item  (containing the whole of the  provided).Side-note: For real world code, just use , which scales to large inputs and small numbers of items to select better than a global sort and slice:


Answer URL
https://docs.python.org/3/library/heapq.html#heapq.nlargest
