Natural Text
So for a point class I'm writing, I need to write a method that rounds and converts to an int in string form. This is what I did:So it rounds and converts to an int but it isn't in string form. I've tried using str but that doesn't work at all. So how can I get that to string form? Here's my entire point class:Now I also have a line class that I haven't finished writing yet so if this class looks fine then the error must be in my line class. 
This does print 
Just cast the value you return into string. In the Python data model, Called by str(object) and the built-in functions format() and print() to compute the “informal” or nicely printable string representation of an object. The return value must be a string object.From Python Data ModelThe  function is a built in function that must return a informal string representation for the object. You are returning an Integer representation.So just change your  to 


Answer URL
https://docs.python.org/3/reference/datamodel.html
