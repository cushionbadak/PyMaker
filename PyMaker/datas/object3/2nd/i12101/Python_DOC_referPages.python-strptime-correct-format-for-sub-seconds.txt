Natural Text
This question already has an answer here:How can I parse a time string containing milliseconds in it with python?                    7 answers                My datetime data is like this: I am trying to use But I get this error: How can I fix the format "" to match the datetime format that I have? 
This is the correct format:Breakdown:: Hour (24-hour clock) as a zero-padded decimal number.: Minute as a zero-padded decimal number.: Second as a zero-padded decimal number.: Microsecond as a decimal number, zero-padded on the left.
You don't need to include two characters for hours, seconds and minutes in your format string;  is "Second as a zero-padded decimal number",  is "  Hour (24-hour clock) as a zero-padded decimal number.", etc. It's also worth noting that you're completely missing the symbol for microseconds (). You should add that if you want your format_string to work. Here is a link to the  and  behavior documentation
Per the documentation (I recommend bookmarking that link if you're planning to do any significant work with python dates and times), the correct directives for zero-padded hours, minutes, and seconds, are , , and , respectively (no doubled characters)Further, your input strings include microseconds, so you'll need to either remove those from the inputs or include the  directive as well.Thus: 


Answer URL
https://docs.python.org/3/library/datetime.html#strftime-and-strptime-behavior
