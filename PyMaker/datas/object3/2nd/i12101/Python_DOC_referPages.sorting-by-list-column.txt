Natural Text
This question already has an answer here:Python list sort in descending order                    5 answers                I have this list like this:And I'm trying to sort it by the second column. I would expect list1 to then be Because 9 is greater than 5 right? I'm usinghowever it doesn't sort it; it just remains as it was before. Why isn't this working?
:Return a new sorted list from the items in iterableIt does not sort in place.
You just need to reverse it:9>5, so it comes afterwards, unless you reverse.
using operator.itemgetter() function:Output:


Answer URL
https://docs.python.org/3/library/functions.html#sorted
