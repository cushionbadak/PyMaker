Natural Text
I'm trying to apply regex to gather data from a file, and I only get empty results. For example, this little test ()Results with empty tuple (). Clearly, I'm doing something wrong here, but what? Comment:What I'm actually trying to do is to interpret expressions such as , by finding the value inside the parenthesis. 
It happens because there are no groups in your regex. If you replace it with:you'll get the groups:Using  should work with the first option:
 returns an empty tuple, because your regular expression did not contain any group.Have a look at the  module documentation:(...)      Matches whatever regular expression is inside the parentheses, and indicates the start and end of a group;Edit: If you want to catch the bit between the brackets in the expression , you could use the following pattern:


Answer URL
https://docs.python.org/3/library/re.html
