Natural Text
For example, if I wanted to avoid generation of  files when using the usual  command I would write:But what if I want to use  instead? All I know is that doesn't work
Most of the command line options for the Python interpreter have equivalent environment variables. These will work when starting IPython.For instance, instead of the  flag, to avoid writing bytecode, you can use:


Answer URL
https://docs.python.org/3/using/cmdline.html#environment-variables
https://docs.python.org/3/using/cmdline.html#envvar-PYTHONDONTWRITEBYTECODE
