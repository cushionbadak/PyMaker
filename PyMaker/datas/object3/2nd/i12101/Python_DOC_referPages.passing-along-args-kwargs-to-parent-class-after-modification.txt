Natural Text
Based on a previous post, I implemented the following modified  which produces key-value pairs.I want to allow  to be dynamically set, as well as provide the option to use a  for cases where keys may appear more than once.Here is the implementation I have:However, using this leads to . However, if I delete  and  from args/kwargs, it still does not work:Leads to .How can I go about having this class take arguments, and still properly have the parent initialized?
You're expecting  to be positional, but it's an optional argument to ; per the docs:The Action class must accept the two positional arguments plus any keyword arguments passed to  except for the  itself.You might try:


Answer URL
https://docs.python.org/3/library/argparse.html#argparse.Action
