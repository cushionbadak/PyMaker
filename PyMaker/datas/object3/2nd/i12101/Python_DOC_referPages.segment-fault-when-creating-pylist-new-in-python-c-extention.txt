Natural Text
parser.cpp:compiles by:test.py:Executing python3 test.py is terminated with segmentation error and the last line I've got by debug output isBesides, it's works properly if I don't import datetime in test.py or decrease LIM in parser.cpp.I can't figure out why that happends.Can someone help me?
The issue is that ctypes releases the global interpreter lock and so calls to Python api functions are undefinedFrom the documentation:The Python global interpreter lock is released before calling any function exported by these libraries, and reacquired afterwards.You need to re-acquire it at the start of your function, and re-release it at the end:


Answer URL
https://docs.python.org/3/library/ctypes.html
