Natural Text
rookie question here but I am making a very basic program with a GUI. It takes French words and translates them to English (sort of)Problem is, if I type in multiple words, it opens upI have triedBut that gives me errors, as does
You need to change  and use 's  method:If you want the words to be separated by something other than spaces, then you need to pass the delimiter to . For example, if the words are separated by a comma followed by a space (as in ), then the  call should be .As for the errors you get,  makes  a list, which doesn't have a  method. So you get an  when you call .  does not make an  instance but is trying to index into  (the class) which isn't the kind of thing you can index into. So this should immediately raise a .


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.split
