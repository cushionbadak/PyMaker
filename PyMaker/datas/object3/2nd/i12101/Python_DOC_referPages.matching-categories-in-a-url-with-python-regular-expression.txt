Natural Text
I would like to match the categories in the url below:  and Note that there may be 1 or more categories. A category can be identified by having a  on with side of text or numbers with a  in between.What I tried:response is What I would like (Expected results):
Rather than a regex, I'd use  which is made to parse urls among other things
According to your definition, something like this:
Others have shown useful answers, however, if you really really need to use regexp:


Answer URL
https://docs.python.org/3/library/urllib.parse.html#module-urllib.parse
