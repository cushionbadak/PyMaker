Natural Text
Python subprocess call is supposed to run as command as is, but it is complaining if there is a pipe in it. Here is my code:The Error I get is : If I change the command to just I am able to run the script successfully. It is just the pipe and the arguments following it which is a problem. If I run  directly on command line it works fine.How can I get this to work? Note that I run this python script using jython2.7. I wasn't successful in using win32serviceutil because it couldn't find the module win32serviceutil.
As already stated,  can't handle single  inputs and shell metacharacters like  unless . But in this case, you really don't need a pipe anyway. You can have Python do the filtering and avoid the pipe to  completely:
I'm not sure about  specifically, but the  documentation suggests that your command needs to be a list, not a string, unless you set the  variable to . Your code should work if you change your call to be , but please be sure to read the warnings in the documentation about the security risks inherent in setting  to .If you don't want to set , you won't be able to use the pipe directly in your command, but there is a section in the documentation, replacing shell pipeline on how to mimic the functionality of the pipe using .


Answer URL
https://docs.python.org/3/library/subprocess.html#frequently-used-arguments
