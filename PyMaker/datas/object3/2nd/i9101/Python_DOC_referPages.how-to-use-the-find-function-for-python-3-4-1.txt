Natural Text
Here is my code:This works in python 2.7.3 but I need it to work in 3.4. When I run the code in 3.4 it gives .Is there a way of altering the code slightly so that it will work or will a whole new method need to be used instead?Also the program can't be case sensitive.Thanks.  
You need to use input() instead of  and use print() function to print anything in .The following code works for . Sample I/O:
Change this:The difference is that  does not exist in Python 3.x, while  does. Actually, the old  has been renamed to Also,  become  as well.


Answer URL
https://docs.python.org/3/library/functions.html#input
