Natural Text
I have the following string  which I want to convert to a datetime tz-aware object.According this SO question, from python 3.2 it can be done using only  module. In addition, from the documentation, I seeSo I try the followingI do not get any error, but then the object I get is not tz-awareOn the other hand, if I convert my string to  and then convert it to an object with I do get a tz-aware datetime:Any ideas of why it works with  but not with ? What am I doing wrong?
Judging from the information given by tzinfo, the  does not work the way that you feed in a string like  and it will return a datetime instance with the correct timezone. pytz provides a possible solution to your problem. However, the documentation says, that there is no guarentee that your timezone will be recognized by datetime and recommends working with UTC as long as possible. Solution using :Out:


Answer URL
https://docs.python.org/3/library/datetime.html
