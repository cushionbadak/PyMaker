Natural Text
I have XML starts with following:And I am using  parser. But this parser deletes the second line starting with I am using the following arguments in the  method:but it only keeps Is there some way how to not delete  line? Or will I have to use some other XML parser?
It seems  has very poor support for doctype declarations.You can create them for new XML with  objects, but not read them from  or  objects created from existing XML. As answered here, you'd have to manually copy paste the doctype declaration...pretty fugly IMHO. So, in all, it would seem best to switch to .


Answer URL
https://docs.python.org/3/library/xml.etree.elementtree.html
