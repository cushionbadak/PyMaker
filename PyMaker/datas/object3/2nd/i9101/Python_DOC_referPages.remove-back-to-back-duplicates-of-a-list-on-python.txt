Natural Text
I want to delete the back to back duplicates of a list.I want to get this list:I tried converting it into a set then back to a list but I keep deleting all duplicates and not just Thanks in advance for the help!
Use :
There is probably a better way, but a linear scan and keeping track of the previous element can work. My simple tests say this also handles more than two back-to-back elements
Because you can, here is a function which uses a generator and a list comprehension:Used as:
A simple (if inelegant) solution would be to append valid elements to a new list:


Answer URL
https://docs.python.org/3/library/itertools.html#itertools.groupby
