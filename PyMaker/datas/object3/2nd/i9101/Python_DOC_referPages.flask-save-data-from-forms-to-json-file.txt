Natural Text
I can't figure out how to save data server side. Here is my route:There I convert the request form from a dictionary to a string. Here is my HTML that makes up the form:I made a result page to see if I could convert the form to JSON.But now I'm stuck, I don't know how to save results to a file. Every time I post the form, I get back something that looks like this:
If you read the documentation on the JSON module you'll see that there's a  method that works like . You just have to feed it a file object as well:That will write out your form to . You probably want to set something up in your flask config to store the full path to the file that you plan to save.


Answer URL
https://docs.python.org/3/library/json.html#json.dump
