Natural Text
I'm trying to write a small program that allows the user to click a button to select files, type in a stock number, and then click a button to rename those files.This is what I've come up with so far:I can select the files, and type in the stock number, but when I click the rename button, it gives me this error in the console:
list.append() requires a string - you're giving an integer.ChangetoThe error was thrown within the Python code ( module, see the docs over here (Python 2)) when it tried to get the , which is impossible (since  doesn't have the method )
In your above code,  is an integer and os.path.basename(path) expects a pathname like string. When iterating over a list, you can direcly iterate through items. or if you want to use indexes, you can do 
Try this:Instead of 


Answer URL
https://docs.python.org/3/library/os.path.html#os.path.basename
