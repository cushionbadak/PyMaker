Natural Text
I have the following repetitive simple code repeated several times that I would like to make a function for:I want to be able to call this function: Where it preforms the code above and returns  with  to  decimal points.  For example: would return  for  in .Any ideas how to accomplish this kind of functionality?
where  is the number of decimal places.BTW, in the recent Python where  has superseded , you could usefor the same task.Or, with field names for clarity:If you are using Python 3.6+, you could simply use f-strings:
I know this an old thread, but there is a much simpler way to do this:Try this:
This should work toowhere  is the precision required.


Answer URL
https://docs.python.org/3/whatsnew/3.6.html#pep-498-formatted-string-literals
