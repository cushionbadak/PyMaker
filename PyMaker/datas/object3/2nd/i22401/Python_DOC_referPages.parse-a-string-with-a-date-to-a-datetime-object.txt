Natural Text
This question already has an answer here:Converting string into datetime                    21 answers                How can I parse a string like "01-Jan-1995" to a Python  object?
On the whole you'd parse date and time strings with the  functions in  or  modules. Your example could be parsed with:Note that parsing month names is locale-dependent. This table shows the directives for parsing various formats of dates and times.
dateutil can parse this sort of format without you even having to define custom date formats. Just install it with:Then use it:
If you need to parse natural language date and time strings, consider parsedatetime (and this answer).


Answer URL
https://docs.python.org/3/library/datetime.html#strftime-and-strptime-behavior
