Natural Text
In python is it possible to strip off all special characters and spaces and replace with hyphen but retain dot extensions (i.e. file extension such as .mp4 or .mov or .txt or .jpg or .png or .pdf)For example:should result inI am using the following method but don't think its a right way
If it's really file names you're dealing with, consider using  to temporarily detach the extension:Now you can freely operate on , keeping  intact:And then you just recombine:If it bothers you that this isn't a one-liner, make it into a function. I would also recommend pre-compiling the regex.


Answer URL
https://docs.python.org/3/library/os.path.html#os.path.splitext
