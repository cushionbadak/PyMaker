Natural Text
If I have , I'd like to change them in to .I am using if statement for this.. but it looks too inefficient.Maybe Regex would be helpful, but regex for searching only the consecutives is possible?if I have  then I'd like to change them  list this.. which means the algorithm only search the "consecutives and count them" change into integer.Any hint to proceed would be appreciated.
:Make an iterator that returns consecutive keys and groups from the iterable to finish up:If needed,  to remove : instead of 
Maybe itertools groupby?


Answer URL
https://docs.python.org/3/library/itertools.html#itertools.groupby
