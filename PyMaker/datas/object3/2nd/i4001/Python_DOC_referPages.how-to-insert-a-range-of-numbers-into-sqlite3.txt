Natural Text
This is my code:As you can see I am trying to insert a range of numbers from (0,10) to Table_1, but I continuously run into this error:How can i get around this error and insert the range of numbers?
the second argument of  needs to be iterable (i.e. it will do the looping for you):should do. note that each entry in the second argument (when iterating over) needs to be a . therefore the 1-tuple  in the  generator.
The second argument of  is an iterable (per row to execute) of iterables (per parameters). So the correct syntax is:


Answer URL
https://docs.python.org/3/library/sqlite3.html?highlight=sqlite#sqlite3.Cursor.executemany
