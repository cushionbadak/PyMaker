Natural Text
i have a mysql sample data resulting like below tableI want to print top 2  for every organization. For every top 2 , I want to print top 2  for every organization. So every organization should have 4 or less records with top 2  and top 2  for each Please help me.
For grouping python provides , which groups sorted input by a given key function.In this case the input needs to be sorted first by , then by  then by  in descending order, e.g. if your data as a list looks like this:then you'd sort it like this:or by altering your sql statement to include , then you'll aready get it in the right order from the database.Now you can use  to group, and  to limit the number of results per grouped category:Output:
You can use pandas to process your query into dataframe with read_sql:Variable  is your connection to db and  is your  string.


Answer URL
https://docs.python.org/3/library/itertools.html#itertools.groupby
