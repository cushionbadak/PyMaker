Natural Text
I'm trying to do an interactive console using the curses lib.similar to this :But it return this :(after the  line I pressed the  key)The  function only wait for input half the time. The other half  without any input. This is why you see the double  sign or the  sign at the end of each output.The expecting result should be : Any recommendation ?
I was using the pyCharm terminal (with the  >  >  checked) but apparently curses lib and pyCharm are not compatible.When running the script in the normal terminal, it worked perfectly.Hope this will help someone!


Answer URL
https://docs.python.org/3/library/curses.html#module-curses
