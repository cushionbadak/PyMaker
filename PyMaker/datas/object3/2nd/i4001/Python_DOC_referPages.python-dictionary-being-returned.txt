Natural Text
I'm trying to create dictionary with the count of each letter appearing in a list of words. The method , print the letters but does not return the dictionary. Why? And how do I fix this? Thank you.
It does return the dictionary. That's what  does. Do you want to print out the dictionary? Then change  to For the record, there's a  object (a subclass of  so can do all the same things) in the standard library that will do this all for you.Output:
As said, you code works but you didn't do anytinhg with the return value of the function.That said, I can think of some improvements though. First, the  method of  will make the case of a new letter cleaner, setting the default value to :Otherwise, you can use a  object from collections:If you have a very long list of words, you shouldn't use  since it builds new string. The  method of  module will chain the letters for free:


Answer URL
https://docs.python.org/3/library/stdtypes.html
