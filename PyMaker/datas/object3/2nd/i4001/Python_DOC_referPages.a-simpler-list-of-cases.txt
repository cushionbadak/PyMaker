Natural Text
I have to test many cases, but this solution is not very elegant:Is there a way to do this sequence of cases with a list?
Use  to take the first value from a generator.The first argument of  is the generator to consume, and the second is the default value if the generator is entirely consumed without producing a value.
Yes, it is called looping:


Answer URL
https://docs.python.org/3/library/functions.html#next
