Natural Text
Given this list:I'd like  to match  and  using globbing. and  seem to expect filename paths to do this work.  Is there anything that implements globbing for arbitrary string searches?
The  function works with any string, not just filenames:The first parameter is named filename because the glob pattern style matching is traditionally used to filter filenames. And as the documentation states, the filename separator ( on a POSIX system) is not special to  and won't alter how a string is matched.If you want to test if there are any patterns that match a given string, use the  function:


Answer URL
https://docs.python.org/3/library/fnmatch.html
https://docs.python.org/3/library/functions.html#any
