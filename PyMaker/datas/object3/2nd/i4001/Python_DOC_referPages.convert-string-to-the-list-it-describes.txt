Natural Text
This question already has an answer here:Convert string representation of list to list                    14 answers                Say someone gives me a string like:and I want to extract the list it describes:How do I do this?P.S.A, B, C, D are just placeholders, they could be numeric values or strings.
To prevent an  from being raised when passing the input directly to , s need to be added around the characters in the string:Output:
Based on , , , and  being numbers (, or ) or strings (), you can simply use:For example:


Answer URL
https://docs.python.org/3/library/ast.html
