Natural Text
i have my here my explain return string eg. PT. Counter Avenger Manequin = CAMPT. International Job = PIJI want my code take first char of letter. but don't have idea how to right now. find the solution but its only for show on so  will like:its mix string & int. also last  har if return . i need make it thank you!
You can split the  with  and then use only the first letter of each word with . Example:This will output:Edit: Same for the year, to get  from  use:This will output:
Use  instead. To pick the first letter of a string just index it with . You can pass  to zero pad with three zeroes, like so


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.format
