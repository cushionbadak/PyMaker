Natural Text
I want to extract all docstrings from my python file using grep or awk. I tried But I see no output.Say the the test  looks like this. Then the output should be all that is enclosed in triple quotes. 
The proper way to extract docstrings from Python code is via actual Python parser (the  module):Run on your sample will output:Just for fun, we can do also do it with GNU :
With P(perl) grep you can do the following:Output:


Answer URL
https://docs.python.org/3/library/ast.html
