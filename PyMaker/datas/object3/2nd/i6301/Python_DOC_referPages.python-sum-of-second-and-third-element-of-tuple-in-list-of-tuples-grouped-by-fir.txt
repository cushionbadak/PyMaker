Natural Text
How do you get the sum of second and third values in a list of tuples grouped by the first value?I.e:I found several great answers on StackOverflow doing this found tuples with two values, but couldn't figure out how to adjust them for summing both second and third values.One of the good answers for just the second value was this:
You could also do it like this:
Use a  as a grouper:Now, you can always get a list of tuples back like this:
You can use  to group based on the first item, and then take the cumulative sums of all the last two items in each group:


Answer URL
https://docs.python.org/3/library/itertools.html#itertools.groupby
