Natural Text
I have the following code however the output does not appear correctly.Any idea as to why it is causing this.  I do not have this issue in LinuxI have tried to add: encoding="utf-8"However this did not resolve the issue.
You're telling the  module to use  as a delimiter whereas your actual file uses the (standard) comma to delimit records.Also, depending on your Python version, you should use the newline parameter or rb mode when opening the file. Read the docs for the  module (Python 2) or the  module (Python 3)
Your delimiter is  not :


Answer URL
