Natural Text
How do i sort the DataFrame by weekday names? I cannot use pd.to_datetime() method because my dates aren't numbers.
You can convert values of column to ordered categoricals, so possible use :Or create index by column  by , then  and last :Notice:Solution working only if unique values of column
Use  with categorical data:If in your culture Monday is not considered the first day of the week, you can rotate your days of the week by n days. For example:Then feed  as an argument to .


Answer URL
https://docs.python.org/3/library/calendar.html#calendar.day_name
