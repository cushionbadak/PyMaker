Natural Text
Assuming I have a string . What I would like to do is to get all the words that are next to (from the right side) the word , so in this case .How could I do that in  ? I found this but it only gives the first word, so in this case, 
You can write a regular expression to find the words after the target word:
Simple generator method:Usage:
One way is to use a regular expression with a look behind assertion:
You can  the string and get the next index of the word "i" as you iterate with :Note that as @PatrickHaugh pointed out, we want to be careful if "i" is the last word so we can exclude iterating over the last word completely.



Answer URL
https://docs.python.org/3/library/re.html
