Natural Text
This question already has an answer here:Splitting integer in Python?                    9 answers                I'm learning python and I wanted to know how I would convert an input  to a list This is the code I have and I'm getting errors:My error:
You can do as this:
You could do something like this:OutputFirst convert it to a string, using str then iterate over the characters of the string (the digits) and transform each back to an integer. As an alternative you could also use map, for example:Or as pointed by @Ev.Kounis you could simply do:
In python split is a method of strings, not integers.Convert the integer to string first and then transform it to list of characters:You can then map each element to int again:


Answer URL
https://docs.python.org/3/library/functions.html#func-str
https://docs.python.org/3/library/functions.html#map
