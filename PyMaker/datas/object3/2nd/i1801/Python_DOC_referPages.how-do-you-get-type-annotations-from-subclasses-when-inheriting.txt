Natural Text
I am trying to build a typed dictionary class, similar to  but with the ability to subclass said tuples. Unfortunately, I find that in the base constructor where I want enforce types, I do not have access to the type annotations of subclasses.Here's a minimal example:And on the console:How do I get the constructor in  to also see the annotations of ?
You're looking for , not .  will merge the annotations of all classes in the MRO, as well as resolving string annotations.
Ah, I have a workaround solution using the class .On the console:


Answer URL
https://docs.python.org/3/library/typing.html#typing.get_type_hints
