Natural Text
Trying to tokenize sentences in python using nltk except I want to tokenize the \n and \t characters as well.Example:In: "This is a\n test"Out: ['This', 'is', 'a', '\n', 'test']Is there a way that's directly supported to do this?
You could use a regex:OutputThe idea is to first split on a single whitespace then, apply findall for each element in the list resulting from the split. The pattern  matches everything that is not a tab or a newline and multiple times and also everything that is a new line or tab multiple times. If you want to consider each tab and newline as a single token change the pattern to:Output


Answer URL
https://docs.python.org/3/library/re.html
