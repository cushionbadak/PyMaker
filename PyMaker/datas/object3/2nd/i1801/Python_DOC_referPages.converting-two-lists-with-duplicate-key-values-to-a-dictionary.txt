Natural Text
I want my output to look like this:
You could use setdefault:OutputIf you want the list of a single item, to be just the single item do this:Output
One approach is to use  as follows:
It's not exactly a list-only implementation but should do the work.
You could use the python built-in .Result:


Answer URL
https://docs.python.org/3/library/stdtypes.html#dict.setdefault
