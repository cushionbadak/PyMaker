Natural Text
Say I have a pandas.DataFrame ,  was fed to function  and returned , a DataFrame with some columns from  removed. The function is a blackbox and the column number is large. How could I find the columns in 'x' that are removed?Or,  is a subset of , how could I find the columns in  that are not in ?Example:And I'd like to have column  returned.My solution now:Is there a way to get the result without extracting column names?I thought this was some sort of basic operations but I couldn't find an easy answer online.Thanks.
Use : will return elements that only exist in  but not in 
Use  or pandas  as:Or:This will give the column values which are in  dataframe but not in  dataframe.or:
You can use  for this:ORBut, it's always good to use native methods:


Answer URL
https://docs.python.org/3/library/stdtypes.html#set
