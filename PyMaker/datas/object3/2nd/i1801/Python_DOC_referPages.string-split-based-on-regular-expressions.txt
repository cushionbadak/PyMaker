Natural Text
I have the mathematical expression in string format. Which contains only '+' or '-' operators. I have to split the string on the basis of operators.I have tried this but it gives original string as it is.
Use  to split on multiple delimiters:
Use group parenthesis if you want to preserve the operators:
Your title suggest using regex, your own solution uses string.split() which is also the reason you get the same string back:Fixed (but not what you want):Output:Here is an alternate solution w/o using regex:Iterate over all characters in the string, if its a number (no space and no +-) add it to a temporary list. If it is a + or - join all numbers from the temporary list and add it to the result list:Output:


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.split
https://docs.python.org/3/library/stdtypes.html#str.split
