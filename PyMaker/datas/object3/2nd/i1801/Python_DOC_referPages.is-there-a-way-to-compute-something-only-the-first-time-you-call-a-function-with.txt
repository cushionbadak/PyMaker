Natural Text
Currently I use the following technique:Is there a more elegant way? I don't like creating globals
The technique is called memoization. The functools module has an  function that does what you want. The python docs have more information like what maxsize is and how lru_cache works so that you can implement it suitably.
You can use a generator:Here you have a live exampleExample code:
You can create a custom callable that will maintain it's own state:
If you don't pass any parameters to a function, use this decorator (I had it lying around):If you have one or more arguments that change (including ) use 
Here's a cool way to do it using closures.


Answer URL
https://docs.python.org/3/library/functools.html#functools.lru_cache
https://docs.python.org/3/library/functools.html#functools.lru_cache
