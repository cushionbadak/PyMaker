Natural Text
Can someone tell me why this doesn't throw an error?  It prints True when the user types in http:// and false when they type in https://.  I can't understand why it would work at all.
 is a boolean expression which evaluates to , because that's what an  statement is (because  is the first True-ish value encountered in the  statement), you need to do this instead:Also, as @ShadowRanger suggested, you could make this shorter and faster by passing a tuple of accepted starting strings to the  method, it will then return  if any of the strings in the tuple matched with the start of the string:


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.startswith
