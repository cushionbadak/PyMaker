Natural Text
I have a script saved as workspace.pyWhich I call from terminal with:Resulting in the errorworkspace.py: error: the following arguments are required: titleI have no idea why this is happening because I supply "myTitle" in the terminal. If I specify a  for the title argument it works perfectly with that value. The part that is throwing me is it doesn't even get to the  so I cannot see what the program thinks is what, but instead fails at I tried to even redo the exact example at: https://docs.python.org/2/howto/argparse.html#introducing-positional-arguments (copied below)RunningResults in (after adding parenthesis to the print for 3.X)workspace.py: error: the following arguments are required: echoIs there something I'm missing? Why does it not just print "hello"? Is there some Python 3 specific syntax I'm missing or something?
I've gotten it to work if I run  instead of . I do not understand why this version works, since command prompt obviously recognizes it as Python and runs it correctly until . There were no errors like  There was no problem importing modules either. Consider the below command prompt session if you are running into a similar error. Maybe you will simply have to include  in your commands like I have to...


Answer URL
https://docs.python.org/3/using/windows.html#arguments-in-shebang-lines
