Natural Text
I encoded a comma delimited list (ex. ) of ids to base64 then the returned data from the form looks like  below.I tried decoding and encoding and all sorts of things but nothing seems to return a the original string.What am I missing?
If you have  in your data, that's the esentation of a bytestring.If you can't get your data source to fix their content (it should just be : what they're giving you isn't valid base64 encoding!), you can use  to read it into a bytestring:


Answer URL
https://docs.python.org/3/library/stdtypes.html#bytes
