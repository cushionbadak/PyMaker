Natural Text
I have the below list sorted in descending order.List is:And I have to take some percentage of values from this list say 80% and that must be in order .eg. Take 7 values from the List in sorted order that is considered I am not getting how to write the code for it. 
You need to use list slicing:
Try this :
First, calculate the number of elements you need -Next, splice the old list -Since your own list is sorter already, we don't need to sort again. In case you do need to sort the list, see sortedAlso, see how slicing works.


Answer URL
https://docs.python.org/3/library/functions.html#sorted
