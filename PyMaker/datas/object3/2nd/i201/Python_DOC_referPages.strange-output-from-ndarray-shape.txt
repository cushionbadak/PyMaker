Natural Text
I am trying to convert the value of a dictionary to a 1d array using:, but when I tried to print the shape of the output array, I have problem.My array looks like this:but the output of array.shape is:by which I was expecting  or after I changed the code to ,the output of array.shape became I have read the document of , but can't get a hint why the outputs are like these. Can someone explain it to me? Thx
This must be python 3.From docsThe objects returned by dict.keys(), dict.values() and dict.items()  are view objects. They provide a dynamic view on the dictionary’s  entries, which means that when the dictionary changes, the view  reflects these changes.The issue is that  is only returning a dynamic view of the data in dictionary's values, Leading to the behaviour you see.Notice that the numpy array isn't resolving the view object into the actual integers, but rather just coercing the view into an array with To avoid this issue, consume the view to get a list, as follows:


Answer URL
https://docs.python.org/3/library/stdtypes.html#dictionary-view-objects
