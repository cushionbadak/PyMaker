Natural Text
That above code runs perfectly. But when I use a variable it gives an error. AttributeError: module 'PyQt5' has no attribute 'qw'.Is there anyway I can do that? The reason I use variable is because I want a user input to choose what modules to print(dir()). 
The  built-in function will let you look up an attribute by name:


Answer URL
https://docs.python.org/3/library/functions.html#getattr
