Natural Text
Currently, i have a list containing:and I'm trying to print out in the format below where:I tried:but I'm getting the output ofWould appreciate some help on this
a simple variant using this lets the  do the work of counting and just displays the items in your desired format.the  objects work like dictionaries an look like this (last item in your list):as with a  you can iterate over the  pairs using . the format string  then produces strings like  which then are ed using the separator .
You could use itertools.groupby only thing issue is the trailing  on the print     


Answer URL
https://docs.python.org/3/library/collections.html?highlight=counter#collections.Counter
https://docs.python.org/3/library/stdtypes.html?highlight=str%20join#str.join
