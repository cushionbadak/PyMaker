Natural Text
Here is my code:I wanna the result is that after  , the  becomes something except , like  and then again when I give  instead of  to the value of , it would go to  state. How can I do this?Here is the target:
You can compare against the current value of  instead of hardcoded  - and move your nested -calls up to the beginning of your while loop. Also provide a way to leave the loop (beside Ctrl-C) :You can use a string-format param to adjust your message so it matches whatever curently sits in .See str.format(..)Output:
Here is one solution:


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.format
