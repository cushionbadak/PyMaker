Natural Text
You will get  if you enter this code.But if you change the order like this:You get .So, why does  in this code only consider the first value of the two?I think the upper code should also return . 
Try evaluating the following expression in the python REPL:This gives , because the  operator short-circuits; that is, it returns the first truthy argument it finds, in this case, . Both  and  are truthy, so you're simply getting whichever one of those you put first.
If I understand your code correctly, you probably wanted to write:This checks if  equals one of  and .


Answer URL
https://docs.python.org/3/library/stdtypes.html#boolean-operations-and-or-not
