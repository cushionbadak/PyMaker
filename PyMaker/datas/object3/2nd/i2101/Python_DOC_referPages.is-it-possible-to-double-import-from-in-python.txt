Natural Text
Let's say i have a  folder with  file, that has class  in it. Can i directly import that class from  file that is in same folder as ?Normally, to use that class i would doIs it possible to directly import the  class?
Yes, as long as  is a Python file () or Python module (directory containing an  file), you can do:See the docs on importing submodules here: https://docs.python.org/3/tutorial/modules.html#packages
The previous is an extremely common import. I'd guess your answer would be:It works for something like:Hope it helps!


Answer URL
https://docs.python.org/3/tutorial/modules.html#packages
