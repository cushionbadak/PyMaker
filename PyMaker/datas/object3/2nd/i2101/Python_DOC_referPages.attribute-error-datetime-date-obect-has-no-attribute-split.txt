Natural Text
What can be done to avoid the error that comes when I try to split ? I'm trying to split the  into year, month and date components.The error I get is 
There are at least three problems with what you are trying to achieve:Don't use  objects within a Pandas series. Use Pandas-friendly  to create  series via . This allows you to take advantage of NumPy arrays and efficient vectorised operations.Date objects, whether  or , are stored internally via integers, not strings. So don't try and "split" them via . returns a list. This is anti-Pandas, since it means you are storing two layers of pointers, a series of lists which themselves hold pointers. Instead, construct  columns to represent components of your dates.This is one approach:To make your code work with  objects is possible, but requires string conversion first and leaves you with a series of lists. Neither method or result are recommended:


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.split
