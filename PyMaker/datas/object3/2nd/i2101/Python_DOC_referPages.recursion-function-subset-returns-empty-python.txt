Natural Text
I wrote a recursion function to get all subsets out of a list of integers. For example, given a list , the return will be .Here is my code:I print out the  in each recursion and they are right. However, the final return  is always empty. Anyone knows why? Appreciate it! 
Pretty close!  The issue is that  is being appended to , but later modified.  What you want is a "frozen" version of what  is then, not a "reference" to what it will eventually be.So instead of , consider appending a copy of the list, for example with:And you'll get your expected result:


Answer URL
https://docs.python.org/3/library/itertools.html#itertools.combinations
