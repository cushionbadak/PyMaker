Natural Text
I have a directory structure: imports . This works when I run  from . However, in  when I try to import  and then run It complains about  not being found. I can add  to the pythonpath so that when I run , it can find  and also . But, I am concerned if there will be another A in a different directory () I use which might conflict with the pythonpath that includes .
I am guessing the basic issue you are getting is because of the change to  between Python 2.x and Python 3.x. From documentation -6.4.2. Intra-package ReferencesWhen packages are structured into subpackages (as with the sound package in the example), you can use absolute imports to refer to submodules of siblings packages. For example, if the module  needs to use the echo module in the  package, it can use from  .Basically, if you are importing  and  imports a sibling, in Python 3.x you would need to use absolute path to import  in  . From what you said , that you are importing  as - Then in  you would need to import  as -Or you can also try -


Answer URL
https://docs.python.org/3/tutorial/modules.html
