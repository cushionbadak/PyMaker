Natural Text
I have a script in which I just need to retrieve the date in the format  of the last git commit. but using  in terminal if I get  then if I am trying to pass this as string to  like but this throws me error  which I guess python things we are passing a char to %c while that was for getting date using git command.I need this date to be concatenated to a string a my python script.
The error message does not correspond to your code: the code in question would produce , not . You don't need . To get git's output, you could use  function:
The code is missing , and there is an extra :After getting the output of the command, you can use  to convert the string to  object, and convert it to the  format you like using :


Answer URL
https://docs.python.org/3/library/subprocess.html#subprocess.check_output
