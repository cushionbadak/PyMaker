Natural Text
I am trying to install EasyGUI. There is no  so I can't install the module like you can with a proper module that has a  fileAny help would be appreciated. Thanks
You haven't install them, you just have to put it a directory, where Python find them, e.g. in a folder is already in your .Perhaps the best if you put  directory.
Don't put manually python file into  directory, use  instead e.g.:The advantage is that you can upgrade it later:Or uninstall:pip is the preferred installer program.


Answer URL
https://docs.python.org/3/installing/
