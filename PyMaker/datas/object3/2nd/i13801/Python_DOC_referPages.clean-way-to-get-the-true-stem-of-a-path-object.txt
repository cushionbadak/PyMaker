Natural Text
Expected inputs and outputs:Here's my implementation that feels dirty to me:Because it breaks on s without suffixes:So I need to check if the  has a suffix first:And this is my current use case:Thanks.
You could just  it: works just as well:It passes all of the tests:
How about a while loop method, where you keep taking  until the path has no suffixes remaining , Example -Please note, the while loop exits the loop when  returns an empty list (As empty list are False like in boolean context) .Example/Demo -For your second input -  -


Answer URL
https://docs.python.org/3/library/pathlib.html#pathlib.Path
