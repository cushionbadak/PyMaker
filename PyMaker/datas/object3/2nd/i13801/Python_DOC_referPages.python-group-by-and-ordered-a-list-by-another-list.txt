Natural Text
I wonder if there is more Pythonic way to do group by and ordered a list by the order of another list.The  has couple pairs in random order. I want the output to be ordered as order in . The result should have all pairs containing 's then follow by all 's and 's.The  would only have either format in  or . input:output:updateThe  is not actual data. it just make logic easy to understand. the actual data are more complex string pairs
Using  with customer  function:


Answer URL
https://docs.python.org/3/library/functions.html#sorted
