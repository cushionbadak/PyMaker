Natural Text
This question already has an answer here:Access a particular field in arbitrarily nested JSON data [duplicate]                    3 answers                I'm working on doing a live currency converter in Python. I've successfully fetched all the data needed from the URL into Python. However I'm now trying to call a specific string in the url. Here's my current code:As you can see I've printed all the data it's fetched, but it's now printing specific strings from it.My question is, how do i parse specific strings from the url ? I've heard of json.load ,is that something i should use ?
You'll need to load the data as JSON; the  module can do this for you, but you need to decode the data to text first.From there on out  is a Python object.Judging by the documenation you should be able to access rates as:for example.


Answer URL
https://docs.python.org/3/library/json.html
https://docs.python.org/3/library/json.html
