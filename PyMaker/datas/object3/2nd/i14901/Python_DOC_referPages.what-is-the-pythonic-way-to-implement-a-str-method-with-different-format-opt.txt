Natural Text
I'd like to create a  method that creates the string in various formats according to user choice.The best I have come up with is to make a  method, and this seems to work ok, but it isn't compatible with  or . In other words I have to use  rather than .
Implement the  method instead, and a user can then specify the formatting required with the  function and  method:orYou probably want to delegate most of the handling of the format on to :That way you can still support all the field width and padding alignment options that the default  method supports.Demo:


Answer URL
https://docs.python.org/3/reference/datamodel.html#object.__format__
https://docs.python.org/3/library/functions.html#format
https://docs.python.org/3/library/stdtypes.html#str.format
