Natural Text
I am trying to indent the output of pprint so that I will get an 8 space indentation with pprint. The code I used is:OutputDesired outputIn short, I need a space indentation when written to file or printed with . I tried but it does not work
You cannot get  to add additional leading indentation, no.Your best option is to use the  function instead, then add indentation manually:This uses the  method to split the  output in to separate lines for ease of re-joining.In Python 3, indenting can be done with :
Minor update to Martijn's answer.  No need to use .format() where simple str concat will do.


Answer URL
https://docs.python.org/3/library/textwrap.html#textwrap.indent
