Natural Text
There is a list of tuple like I can use to get the result But if given a variable list such as ,  how to get the similar result?
Use , like thisIf you have a list of indices, then you can unpack them to the , like this
You could use a generator expression and  to pull out specific indices:or you can use a  object to create a callable that does the same:where  is your list of indexes. This works because  just happens to return a  object when retrieving multiple indexes.Demo:


Answer URL
https://docs.python.org/3/library/operator.html#operator.itemgetter
https://docs.python.org/3/tutorial/controlflow.html#tut-unpacking-arguments
