Natural Text
I have two problems:a) Python csv module could not handle particular csv file properlyI have the following data in Take note that there are whitespaces between the double quote and comma.I used the following script to read the file, the first column will be decoded as , instead of The output isTo remove the whitespaces, I do thatThe output is nowb) Using namedtuple to read in the csv data into memoryWith the same csv file , I use another script:The error is
Instead ofYou simply wantRight now you're looping over every element in  and calling  on it.  That's why you're getting "Expected 6 arguments, got 8" -- you're trying to make a  instance out of .After changing this, the output isPS: for Python 3, you should open files for use with  using , see here.


Answer URL
https://docs.python.org/3/library/csv.html#csv.reader
