Natural Text
I want to add fruits to a list depending on the value of the variable . I think I am on the right path, but in trying to append each  to the list I am getting an error. :(
Assignment won't do an append operation on a list. You need to call  function.
You cannot use the same name for two different jobs; either  is the list, or  is the value the user entered.Use a different name for the  return value, so you don't end up trying to append  to :Rather than use a  loop, you could also use a  loop looping over the  object; it'll iterate exactly  times:


Answer URL
https://docs.python.org/3/tutorial/introduction.html#lists
