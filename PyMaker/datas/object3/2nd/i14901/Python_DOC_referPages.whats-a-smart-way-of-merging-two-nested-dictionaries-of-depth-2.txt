Natural Text
What is the "best" way to merge two nested dictionaries of depth 2?For example, I'd like to merge the following two dictionaries:In order to get:Note that the subvalues in dicA and dicB will always be disjoint.Currently, I'm using:Is there an alternative to update or a one-liner that can merge nested dictionaries? 
You can use  (bottom of section):This creates a list of all keys to use in a dict comprehension, and uses the more robust  to prevent errors if key is only in the one of the s.For Python 3  objects are returned from  and , so you can use  (or wrap in  each  object in ):Note: Using  would make the whole thing nicer.
I don't think there's too much wrong with the way you suggest, though I would use a  clause:The result is then, for :


Answer URL
https://docs.python.org/3/library/stdtypes.html#dict-views
https://docs.python.org/3/library/itertools.html#itertools.chain
