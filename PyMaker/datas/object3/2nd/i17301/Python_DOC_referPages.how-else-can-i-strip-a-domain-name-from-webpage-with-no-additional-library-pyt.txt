Natural Text
The post Get domain name from URLsuggested multiple libraries to get the top level domain. but how else can I strip a domain name from webpage with no additional library?I had tried it with regex it seems to work but I am sure there are better ways of doing it and lots of urls that will break the regex:I've also tried urlparse but it ends up with :
How about make a function that wraps  ?
Use urllib.parse standard library.


Answer URL
