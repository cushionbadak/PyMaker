Natural Text
I have four functions named , , , , and I need to find out the execution time of these functions.When I execute this, I am getting error as global name  is not defined.
Tests with  are executed in their own separated scope. This is primarily done so the timing is not influenced by some side effects in the module that is executing the test. As such, neither , nor your functions  are defined during the test.You can import them by setting them up though, for example:But this won’t fix the issue for you because  is still not defined, and even if it was, it would be a string, and not a function you can execute. Your intention was probably this:
The  functions run in their own function space. It doesn't have access to your scope, which is why it provides for a "setup" parameter that it executes to set up the timing environment. You need to define any functions and variables that you need in a string, which you then pass to  as the  argument.You seem to be misunderstanding how a couple of other things work, too. When you put  in a string, it's just the letter . It doesn't magically take on the value of the variable . For that matter, neither does . You need to create a string that holds some actual values, not variable names.It looks to me like you want to do something like this:I highly recommend that you read the documentation, since  isn't a legal use of the function in the first place.


Answer URL
