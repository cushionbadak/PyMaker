Natural Text
Why does this code snippet result in: ? is a hexadecimal string. For example, it may be .
You're doing this:If you intended to remove the first two character, use following:If the orignal string is hexadecimal representation, you should pass optional  argument 16If  is used to remove  generated by  (not to remove leading characters from the original string), then following is what you want.BTW, you can use  or  instead of  + :UPDATEIf you specify 0 as a base,  will automatically recognize the number's base using the prefix.
Parentheses in the wrong place. Assuming you have a string like "0x0101" where you want to end up with a 16-digit binary string:The  call interprets that as a hex number (and can accept a number of the form ). Then  turns that into a string of the form .The  then gets rid of the  at the front and  fills that out to 16 "bits".


Answer URL
