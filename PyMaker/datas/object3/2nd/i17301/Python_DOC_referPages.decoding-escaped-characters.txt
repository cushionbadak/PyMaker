Natural Text
I'm trying to convert string1 so it would give the output as what's on string2 I'm using plone with limited options, lets assume I cannot import any external libraries. When I tried to use .decode( 'unicode-escape') it returns following stringNote that it takes Ä by converting first part of the unicode ( \xc4 )Thanks
Use  encoding instead of :or use decode it using , then encode it using  encoding:NOTE  is only available in Python 2.x.


Answer URL
