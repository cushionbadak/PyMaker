Natural Text
I to convert each element of list to tuple like following :convert to tuple list:Actually I have dict with keys like this so for searching purpose I need to have these.
You can use a list comprehension:Or, if you are on Python 2.x, you could just use :However, the previous solution will not work in Python 3.x because  now returns a zip object.  Instead, you would need to explicitly make the results a list by placing them in :I personally prefer the list comprehension over this last solution though.
Just do this:


Answer URL
