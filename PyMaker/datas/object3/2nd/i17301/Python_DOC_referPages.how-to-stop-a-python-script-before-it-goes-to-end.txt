Natural Text
In the main script, I want to stop it under some condition. I tried  but it is wrong since it is outside the function. I tried , but it does not stop. See the following: what should I do ? the version I used is IDLE 2.7.5+
use 
If you're not in a function, use Note that this will exit python entirely, even if called from a module that's inside a larger program.It will usually be better organization to  from a function, and keep as much code inside functions as possible.
Another way to exit a Python script is to simply raise the  exception with :This solution does exactly what  does, except that you do not need to import  first.Also, your specific problem was caused by the fact that you were not actually calling the  function:However, you should not use this solution because it is considered a bad practice.  Instead, you should use  or  as shown above.


Answer URL
