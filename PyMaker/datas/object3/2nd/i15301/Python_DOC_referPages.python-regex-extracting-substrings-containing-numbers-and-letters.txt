Natural Text
I am attempting to extract a substring that contains numbers and letters:I only want 11m56.95s and 13m31.14sI have tried doing this:that doesn't give me what I want, I also tried this:that did not work either, any other suggestions?
Your Regex pattern is not formed correctly.  It is currently matching:Instead, you should use:Below is an explanation of what the new pattern matches:
Try this:Output:
Your current regular expression does not match what you expect it to. You could use the following regular expression to extract those substrings.Live DemoExample:

If your lines are all like your example split will work:


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.join
