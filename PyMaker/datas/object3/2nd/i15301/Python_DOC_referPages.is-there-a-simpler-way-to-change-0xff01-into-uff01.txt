Natural Text
I want to print all the full-width characters in the Python console.I want to make it simpler; the important key is that to change  into , here is my way to do the transformation.Is there a simpler way to do that?
Your code produces single Unicode characters from integers, in a very roundabout way. Use the  function instead:Note that the  call is very much redundant and wasteful here too; I removed it.From the documentation:Return the string representing a character whose Unicode code point is the integer i. For example,  returns the string .Demo:If you really wanted to produce escaped  sequences (without interpreting them to their actual codepoints), use  to format the integers as 4-digit hex values:The leading  makes the format zero-padded,  sets the field width to 4 characters, and  produces hexadecimal characters:


Answer URL
https://docs.python.org/3/library/functions.html#chr
https://docs.python.org/3/library/stdtypes.html#str.format
