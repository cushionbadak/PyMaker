Natural Text
This script:produces:where  and  are not delimited by "=" but with " = " (equal sign surrounded by spaces).How to instruct Python, to use "=" as delimiter with ConfigParser?
You could extend the  class and override the  method such that it behaves the way you would like.This produces the following output file:
You can just use configparser (very recomended) but in simpler way, just add: As described in: https://docs.python.org/3/library/configparser.html#configparser.ConfigParser.writeWrite a representation of the configuration to the specified file object, which must be opened in text mode (accepting strings). This representation can be parsed by a future read() call. If space_around_delimiters is true, delimiters between keys and values are surrounded by spaces.


Answer URL
https://docs.python.org/3/library/configparser.html#configparser.ConfigParser.write
