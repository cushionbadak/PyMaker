Natural Text
This question already has an answer here:Why does “return list.sort()” return None, not the list?                    6 answers                Why does it print ? Can you elaborate on all functions?
 and  are different: sorts the list in-place and returns . creates a new list and returns it.See here for further details.


Answer URL
https://docs.python.org/3/howto/sorting.html
