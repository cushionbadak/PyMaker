Natural Text
In python 2.7:In python 3.4:how to change  into ?    how to change  into ?
In Python 3.x, use  (str -> bytes) and  (bytes -> str) with latin1 encoding (or iso-8859-1):
Both  and  are bytestrings in Python 2. If you compare Unicode and bytes on Python 2; you also get  in this case: is a Unicode string in Python 3. You could add  to make the code work the same on both Python 2 and 3:Don't mix bytestrings and Unicode strings.To convert Unicode string to bytes:To convert bytes to Unicode string:


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.encode
https://docs.python.org/3/library/stdtypes.html#bytes.decode
