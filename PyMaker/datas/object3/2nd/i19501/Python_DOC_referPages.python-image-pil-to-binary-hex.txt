Natural Text
Questionconverting hex to image and draw a text on the image, after that convert image to binary hex,but having the problem at here , how to convert img to hex?
The  object needs to be saved again; write it to another  object:then get the written data with the  method:The PIL-for-python-3 landscape is rather muddled at the moment. The Pillow fork looks to be the best, maintained version out there at the moment. It includes fixes that make saving to a  object work. If you run into a  exception using the above code, you have a version that was not yet fixed, in which case you'll have to resort to using a temporary file instead.


Answer URL
