Natural Text
I have written following piece of code with an intention to copy contents of abc.txt into another file xyz.txtbut the statement  doesn't seem to work as intended. If I replace a_file.read() with some string, it (the string) gets printed.How do I go about this?
You're looking for .
You're calling  twice. The first time it reads the whole file, but that is lost with the attempt to do it again after opening  -- so nothing gets written to that file. Try this to avoid the problem:
To copy the contents of abc.txt to xyz.txt, you could use :


Answer URL
