Natural Text
How can one instrospect to receive the current thread object?Consider this somewhat artificial code snippet. The use case is different, but for the sake of simplicity, I've boiled it down the the essential bit
You can use  function. Compare  with  as follow:In Python 3, use .You can also use your own identifier instead of :
To make minimal changes to your code, here is probably what you are after:But I do not understand what do you mean by introspection.  is shared by all threads, all threads have by their own is some local data, accessible via .


Answer URL
