Natural Text
This question already has an answer here:Adding all the numbers from a list together(not summing). Python 3.3.2 [duplicate]                    4 answers                So say I have a list named  and it looks something like this :How would you print it to the screen so it prints :(yes, no space inbetween)If I use It prints the following:Help would be much appreciated.
With Python 3, you can pass a separator to .  in front of  causes  to be unpacked into items:
Use :Example:This joins every item listed separated by the string given.


Answer URL
