Natural Text
I have some code like this :How can I read file name in directory, and treat file like variable, then call it in external script to execute?In variable like:Can you give me some solution, or advice for this issue?
You can use  to insert the contents of a variable into a string.Here, the  in the string will be replaced by the contents of .Result:You could also just compose the string manually, but this isn't as nice looking.
The above answer using .format is cleaner, but you can also use the scanf syntax


Answer URL
