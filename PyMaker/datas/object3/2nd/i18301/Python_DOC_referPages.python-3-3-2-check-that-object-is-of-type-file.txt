Natural Text
I'm porting from Python 2.7 to Python 3.3.2. In Python 2.7, I used to be able to do something like , but it seems that in Python 3.3.2 this is wrong. How do I do a similar thing in Python 3.3.2?
Python 3 file objects are part of the  module, test against ABC classes in that module:Don't use  in Python 2; you'd use  instead. Even then, you would want to test for the capabilities; something the  ABCs let you do; the  function will return  for any object that implements all the methods the Abstract Base Class defines.Demo:


Answer URL
