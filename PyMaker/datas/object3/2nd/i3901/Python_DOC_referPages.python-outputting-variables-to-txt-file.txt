Natural Text
Working on outputting text and variables to a  file through python. And it doesn't work. This is the error that is given when i run it.Any help is greatly appreciated
Try using .Ex:Note:  takes only a single argument, You are trying to pass 3
You can try like this:Explanation:If you use  there is no need to explicitly specify the . And also in  using string concatenation  you can get the required one.
You can simply use  functionThe str() function is meant to return representations of values which  are fairly human-readable.And your valid code will be like-your code giving Error, because- method takes only 1 argument, but you are providing 3 argument 1) "Number Plate:" , 2) np and 3) "\n"


Answer URL
https://docs.python.org/3/tutorial/inputoutput.html
