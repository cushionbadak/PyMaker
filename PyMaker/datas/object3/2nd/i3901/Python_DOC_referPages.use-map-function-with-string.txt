Natural Text
Is there a way to use  function with a string instead of a list? Or the  function is meant only to work with lists?For instance, ignoring the content of the lambda function, this code returns a map object and not a string:
Using list/generator comprehensions is preferable (more Pythonic) to :
python3  returns a generator, so you have to consume it, using  is the easiest way:It is similar to this code:


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.join
