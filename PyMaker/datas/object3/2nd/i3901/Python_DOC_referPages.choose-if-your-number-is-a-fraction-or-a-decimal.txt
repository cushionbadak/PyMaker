Natural Text
I'm trying to make it so if the denominator of the fraction is over 2, it prints the decimal. Else, it prints the fraction 
You are making it way harder than it needs to be. Use the  attribute of the  instance:You may want to use the  method to account for the imprecise nature of floats: finds the closest fraction to the original where the denominator is limited be below 1 million:Another alternative is to pass the user input straight to ; the constructor accepts both valid values for  and strings in the form of numerator / denominator (with optional sign). This would let your users input exact fractions such as '2 / 3':


Answer URL
https://docs.python.org/3/library/fractions.html#fractions.Fraction.denominator
https://docs.python.org/3/library/fractions.html#fractions.Fraction.limit_denominator
