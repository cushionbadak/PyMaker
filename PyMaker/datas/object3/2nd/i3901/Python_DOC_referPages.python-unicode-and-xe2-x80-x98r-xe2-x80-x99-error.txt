Natural Text
Python version 3.5.2So I have file output from  called , and I want to read it using:and returningbut if I change command to , like:and it works fine!. returning:So what's the solution?
The data is returned in bytes, as indicated by the  in fron of the output.You have to decode it to a string.
I am pretty sure your command isn't being piped as you might expected when calling  - the arguments  after  is somehow being passed to , thus running this:Produce that error message you are seeing.Now that you tell us what  actually is, looking at the documentation for the function we can see that it takes a  argument which is passed to , which is the argument to enable the processing of shell commands.  To resolve this issue, you may try the following instead:


Answer URL
https://docs.python.org/3/library/subprocess.html#subprocess.Popen
