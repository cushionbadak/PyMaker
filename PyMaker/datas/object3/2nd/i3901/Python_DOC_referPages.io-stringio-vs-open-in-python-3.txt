Natural Text
All I could find is this statement:The easiest way to create a text stream is with open(), optionally  specifying an encoding:In-memory text streams are also available as StringIO objects:But this gives no insight at all on when I should use  over  and vice-versa. I know that they do not work exactly the same behind the scene. But why would someone go for  in Python 3 ?
The difference is:  takes a file name (and some other arguments like mode or encoding),  takes a plain string and both return file-like objects.Hence:Use  to read files ;Use  when you need a file-like object and you want to pass the content of a string.An example with :It's very useful because you can use a string where a file was expected. In the usual case, with a csv file on your disk, you would write something like:


Answer URL
https://docs.python.org/3/library/io.html#text-i-o
