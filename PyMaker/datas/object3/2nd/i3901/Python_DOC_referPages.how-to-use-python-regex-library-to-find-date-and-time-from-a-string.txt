Natural Text
Given a text : . How do i extract  as  in a variable and  as  in another variable using python? 
if your date in the string follows YYYY-MM-DD or YYYY-MM-DD pattern, code to extract date fieldthen you can manipulate string to get desired output, for your case with a small change in regex pattern you can timeBrief Explanation: looks for digits matches 4 digits looks for a group having (4 digits)-(2 digits)-(2 digits)to know more about regex you can follow the official link
Quick and dirty way,


Answer URL
https://docs.python.org/3/library/re.html
