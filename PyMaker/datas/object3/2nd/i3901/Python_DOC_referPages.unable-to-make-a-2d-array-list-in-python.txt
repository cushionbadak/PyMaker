Natural Text
this is my code in python I don't understand how to combine given output in a one single array (2d array)code is:-and the output is :-
A 2D array in python is just a list of lists. List can be defined with some pretty simple syntax --  defines  as the list . Your check function currently just prints the lists you make. If you want to keep the lists you make to use later, you need to  them, so that  will assign the value of  to the variable .If your check function returns the lists, you can create a list of your results with .


Answer URL
https://docs.python.org/3/tutorial/datastructures.html
