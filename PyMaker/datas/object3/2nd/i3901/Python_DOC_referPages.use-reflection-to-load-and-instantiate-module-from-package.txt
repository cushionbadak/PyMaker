Natural Text
I have a package (a folder) named  containing, , .my  contains:In my  I doWhich throws the error Basically what I want to do is elegantly load the model which is set in the config. Accessing the config like this works fine.
You're trying to do programmatic attribute access, so you need to use the  function:Subscripting (with square brackets, like ) is not the same as attribute access (with a dot, like ), in exactly the same way that  means something different to .
i don't know is it write but you can try this,


Answer URL
https://docs.python.org/3/library/functions.html#getattr
