Natural Text
When I use  in both Python2 and Python3, the string  is written to the file, but what I expect is three separate lines of .Here is the document of :Please note that it accepts any iterable and  is iterable, so why instead of writing a line for each item in the string,  simply writes a single string? Should I need to use  to get the desired result?I wonder whether this is a deliberately consideration or simply a novel bug.
from documentationWrite a list of lines to the stream. Line separators are not added, so  it is usual for each of the lines provided to have a line separator at  the end.Example:Output:Please note that the line separator  is part of each string.


Answer URL
https://docs.python.org/3/library/io.html
