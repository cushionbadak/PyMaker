Natural Text
I finally figured how to convert my dictionary with data into a string which gives me the desired output. I was wondering how to make this code more dense. Any suggestions? 
By preprocessing your  input, you could avoid the need for :So you could limit your code to:But this feels cumbersome to modify the user input, so using  might be beneficial. By the look of things,  and  could be of varying length so you might benefit from the  action with a specific default case handling:Usage being something likewhich would query the following URL:


Answer URL
https://docs.python.org/3/library/argparse.html
https://docs.python.org/3/library/argparse.html#action
