Natural Text
I'd like to keep the columns in the order they were defined with . In the example below,  shows that GroupId is the first column and  also prints GroupId.I'm using Python version 3.6.3
One way is to use , as below. Note that the OrderedDict object takes a list of tuples as an input.
Unless you're using python-3.6+ where dictionaries are ordered, this just isn't possible with a (standard) dictionary. You will need to  your items together and pass a list of tuples:Or,


Answer URL
https://docs.python.org/3/library/collections.html#collections.OrderedDict
