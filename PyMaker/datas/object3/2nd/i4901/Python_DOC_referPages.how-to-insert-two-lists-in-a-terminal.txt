Natural Text
I want to do this exercise which I have done well with the following code but I want to do it with the second code but I don't know how to introduce two lists in a Terminal:Exercise: Create a function that receives two input parameters of type list and that returns a list where the two lists are combined, alternately.This is the way I have to do:
Another way is to use argparse: Usage:UPDATEI've noticed your comment for the previous answer. If you want to distinguish between strings and numbers you can do as following:So now your input should be of form:and you will get:Note that now your input lists can be of mixed types.
You can do it like thatIn your current code you are probably passing each list item as a serperate argument. To pass two lists you need to quote each list like .Outputs: .


Answer URL
https://docs.python.org/3/library/argparse.html
