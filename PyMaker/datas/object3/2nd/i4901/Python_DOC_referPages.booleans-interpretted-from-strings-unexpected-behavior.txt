Natural Text
Can anyone explain this behaviour in Python (2.7 and 3)I would expect that because I am using logic operators  and , the statements would be evaluated as .So what is happening here?
As explained here using  on strings will yield the following result: returns a if a is True, else returns b. returns b if a is True, else returns a.This behavior is called Short-circuit_evaluation and it applies for both  as can be seen here. This explains why  in the 1st case and  in the 2nd.As for checking , , using logical operators on strings yields a specific result (as explained in above), and it is not the same as .


Answer URL
https://docs.python.org/3/library/stdtypes.html#boolean-operations-and-or-not
