Natural Text
I made a small discord bot in python. On windows it works perfectly fine, but when I try to run it on raspbain, it says invalid syntax (with the command "python3 Bot.py")Here's the code:The syntax is at  with the "
The code uses formatted string literals (the ), which were only introduced in Python 3.6, so you need to use at least that version of Python.


Answer URL
https://docs.python.org/3/reference/lexical_analysis.html#f-strings
https://docs.python.org/3/reference/lexical_analysis.html#f-strings
