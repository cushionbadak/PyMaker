Natural Text
I got something like this:I would like to sum values 'failed', passed and total into one dictionaryso the output should be like this:What is the easiest solution to do such thing?I can use basic libraries, except collections.The solution must be generic, for example if some other dictionaries would appear in the future
This will give you the output you are looking for, with no libraries.Result:
input:Output:
Using  and  that is fairly straight forward like:Code:Test Code:Results:
If you cannot use , another solution is to use Using this in your code would look like this.Although, this assumes all your  are correctly formated.
You can use  along with :Output:


Answer URL
https://docs.python.org/3/library/collections.html#collections.Counter
https://docs.python.org/3/library/collections.html#collections.defaultdict
