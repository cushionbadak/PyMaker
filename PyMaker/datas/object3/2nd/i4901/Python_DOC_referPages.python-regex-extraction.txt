Natural Text
I have a string called "strtosearch2" like this:I want to extract the dates which are having the lines "ERROR" only. I wrote my regex as follows:output as follows:My target output:How can I fix this ?. Thank you
You may anchor the pattern at the start of the line/string and remove the  modifier:See the regex demoWith , the  matched any char including line break chars.With ,  matches the start of each line, not only the start of the whole string.Also,  can match line break chars, so you might want to use  instead of it to only match horizontal whitespace.
Try this:


Answer URL
https://docs.python.org/3/library/re.html
