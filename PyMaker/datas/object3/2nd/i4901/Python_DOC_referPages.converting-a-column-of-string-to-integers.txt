Natural Text
The above code is the most basic machine learning snippet to convert a column of string to integers(or one hot encoding).However I am having difficulty understanding the code esp.  what does these two lines do that makes it to do one hot encoding?

So  is what is called a list comprehension. It is shorthand way to create a list in python. This statement is the equivalent of saying:These are extremely common in Python, so definitely read about them and practice with them. They make your code cleaner and save time.Set can be ready about here: SetsIn short, they are a grouping that is unique. So if you make a set of a list you get an object with all the unique values from the list


Answer URL
https://docs.python.org/3/tutorial/datastructures.html#sets
