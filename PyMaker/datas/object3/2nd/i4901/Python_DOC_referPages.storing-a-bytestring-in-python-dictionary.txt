Natural Text
I am trying to use a dictionary store byte strings.Calling 'Serial':I know this is the ASCII representation, however I later want to use this to calculate checksums and the conversion adds a layer of complexity. Is there a way to store the data without it being changed, or a way to call the data differently?
Is there a way to store the data without it being changedThere is no "conversion", only representation.  Your data is not being changed.  So, simply, don't worry about it.It is quite common for objects to have literal syntaxes which are non-unique, yet these are parsed and represented into a unique form.  Here's another example:


Answer URL
https://docs.python.org/3/library/functions.html#repr
