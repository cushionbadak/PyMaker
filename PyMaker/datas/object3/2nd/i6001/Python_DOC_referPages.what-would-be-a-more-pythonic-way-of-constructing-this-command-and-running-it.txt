Natural Text
This is part of the pip console app that comes with qpython for android
A better way might be to use  and :The interface is much cleaner and more robust. It also offers much more control over how the process is called.Notice that you can use a list for the arguments instead of artificially concatenating them. This makes life much easier if the path contains a space.
I think this is more pythonic but still needs something.


Answer URL
https://docs.python.org/3/library/subprocess.html#subprocess.run
https://docs.python.org/3/library/os.path.html#os.path.join
