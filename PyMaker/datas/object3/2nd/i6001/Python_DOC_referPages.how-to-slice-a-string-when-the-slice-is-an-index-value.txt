Natural Text
What is the most pythonic way to do:where if myIndices were  it would just return me "el". I'm happy to use numpy or other packages. I can do something similar in R but can't seem to get the same to work in python. Thank you!
Slice objects can be assigned a name using the builtin : Return a slice object representing the set of indices specified by .Literal slices are actually translated to slice objects behind the scenes. And these are different from  objects in that they are not iterable. They only encapsulate the logic with which a sequence should be sliced:


Answer URL
https://docs.python.org/3/library/functions.html#slice
