Natural Text
Does anyone know a simple way to convert the given list into the desired format?Given:Desired Format:Thanks in advance
Try this:
I think json.loads is quite suitable for this particular one, ie:
It looks like you have a list with a single element, which is a string, and you want to evaluate the string and return it, discarding the outer list.  If so, then you can use  from  to safely evaluate the expression:This evaluates to:
This should do it easily:


Answer URL
https://docs.python.org/3/library/json.html#json.loads
