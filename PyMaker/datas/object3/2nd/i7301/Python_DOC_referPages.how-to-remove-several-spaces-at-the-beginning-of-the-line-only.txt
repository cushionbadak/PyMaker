Natural Text
I've got a file that look like this:And I'd like to be able to substitute only the spaces starting a line with something else.eg:It looks simple, but it does not work for me.this is what I tried wrongly:
You can use  with a callback:As a complete example, consider:When a match is encountered, the match object is passed to the callback, which returns another string as the replacement.
For a non-regex solution, this is what I could come up with.DisclaimerIt works fine for the sample input but you'd have to run more tests on it to clear out bugs; if any.


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.replace
