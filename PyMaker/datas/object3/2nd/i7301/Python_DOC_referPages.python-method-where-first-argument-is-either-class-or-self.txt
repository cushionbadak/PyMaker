Natural Text
I want to write some Python code that when called from the context of a type behaves like an , however when called on an instance of an object the first argument is behaves as normal, with  referencing the current instance. I.e. I want to write:
I once wrote a code snippet in daniweb about this
After some research I managed to do this. The first thing I found is that you can actually implement  in pure Python, as referenced in the documentation.With that knowledge it's fairly simple to adapt the code to test if  exists: All we have to do over and above the example in the documentation is check if  is  and favour  over  if it is not.


Answer URL
https://docs.python.org/3/howto/descriptor.html#static-methods-and-class-methods
