Natural Text
Currently i'm doing it like:So wanted to know if there is a more optimized/effective way of doing this?
Do it like that instead:this is a dictionary-comprehension that loops through the - pairs of  and for every one of those s fetches the corresponding  from  too. It converts both to egers and derives their average.Note the  method used instead of the  that makes sure no error is raised if a  is missing from  but present in .


Answer URL
https://docs.python.org/3/library/statistics.html
