Natural Text
Python 3.6Consider this code:outputs:Can I filter out the lists with  inside the list comprehension?  Update:Due to the two excellent answers given. I felt it might be worth a bench markResults:Looks like the list comprehension has an edge. 
YesA list comprehension consists of brackets containing an expression followed by a for clause, then zero or more for or if clauses. The result will be a new list resulting from evaluating the expression in the context of the for and if clauses which follow it. For example, this listcomp combines the elements of two lists if they are not equal:and it’s equivalent to:Note how the order of the for and if statements is the same in both these snippets.Since calling  twice doesn't work in your particular example (as it consumes the generator yielded by ), you can introduce a temporary variable in your list comprehension by using a generator expression:Alternately, using , , and :


Answer URL
https://docs.python.org/3/tutorial/datastructures.html#list-comprehensions
