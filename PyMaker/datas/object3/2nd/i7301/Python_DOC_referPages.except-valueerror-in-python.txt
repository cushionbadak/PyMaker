Natural Text
Is there better way of writing this code: 
You can have it all inside a  block (calculation and ):Also note, the default return value from function is , so the second  is not really necessary (you could have  instead), but it makes code more readable.
You can also use contextlib.suppress, if you find it more readable.See the documentation here.


Answer URL
https://docs.python.org/3/library/contextlib.html#contextlib.suppress
