Natural Text
I'm programming a loop in which in each iteration the variable is either a series or an integer. I need to do different things in each case. How do I check the datatype and use it in a condition?I've tried doingif(type(i)==)But it does not work
I think you need compare with :
You can use the  built-in function from Python defined here.Return true if the object argument is an instance of the classinfo argumentSo you can use it like this:and


Answer URL
https://docs.python.org/3/library/functions.html#isinstance
