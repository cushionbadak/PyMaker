Natural Text
I cant't for the life of me figure out how to make this into a decorator.Any help or an example would be awesome.Here is the codeHere is an example use
A decorator is simply a function that takes a function and returns a function.Usage:Check out  to further improve the decorator.
Define an inner function:
A common way to make decorators is to use two nested functions:The only major between this code and your original (other than stripping out the comments for space) is that the  is supplied by a call to the outer function while the other arguments are supplied to the wrapper function. We also need to save the  time on the  function instead of the original function (since the original will not be accessible any more when you a decorator).


Answer URL
https://docs.python.org/3/library/functools.html#functools.wraps
