Natural Text
When iterating the attributes of a class, I can see the @classmethod and @staticmethod attributes, but I'm not sure how I can generically identify them based on their typeand the attributes look like:So instances methods have a And classmethod have And staticmethod have Is this a good way of identifying the attributes?
No, you should not rely on the string representations of these attributes. Instead, note that  and  are types, i.e., they are class objects. For those who want to know, they are implemented as descriptors. Just iterate over the attributes of a class and use :


Answer URL
https://docs.python.org/3/howto/descriptor.html
