Natural Text
I understand the basics of  (Pandas) broadcasting but got stuck on this simple example: My understanding of the z shape is that you had a  array multiplied with a  array, the trailing dimension for 5 is equal so you end up with a  array. Okay that sounds good. My problem is why is ? Isn't it one-dimensional so it's really ? 
 1D array like  gives you shape such as  without reshaping. If you want to treat it as 1 column matrix of shape  then do 
The broadcasting rules are:With your  and :If  was (5,2), it would raise an error, because  cannot be paired with .  But  is ok, because .


Answer URL
https://docs.python.org/3/tutorial/datastructures.html#tuples-and-sequences
