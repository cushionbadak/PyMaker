Natural Text
I managed to reproduce this on both Python 3.4 and 3.7.Consider:Clearly one would expect  to be defined here, since it is defined under  which  is a subclass of.Lo and behold, it is defined, but evaluates to . What gives?The same behavior is reproduced if implementing  as  in  and .
Found it in the docs:A class that overrides  and does not define  will have its  implicitly set to None. andIf a class that overrides  needs to retain the implementation of  from a parent class, the interpreter must be told this explicitly by setting From ticket 1549:This was done intentionally -- if you define a comparison without  defining a hash, the default hash will not match your comparison, and  your objects will misbehave when used as dictionary keys.(Guido van Rossum)


Answer URL
https://docs.python.org/3/reference/datamodel.html#object.__hash__
