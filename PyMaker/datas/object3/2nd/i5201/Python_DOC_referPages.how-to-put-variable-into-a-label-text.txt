Natural Text
I wrote a tkinter code, with a label. There was a variable in the label text.When I ran the code, it gave an error message here: , x,It said: positional argument follows keyword argument
You must put all label pieces together before passing them to the :or:
Using commas to stitch strings together only works in the  function. Everywhere else you need to do the string formatting yourself: 
Your problemCommas are used to separate 's arguments, so  and  are interpreted as  arguments. This is not what you want.SolutionYou need to concatenate your strings.You can use the  operator to do so:If  is not a string, you can convert it with , like this:


Answer URL
https://docs.python.org/3/library/functions.html#func-str
