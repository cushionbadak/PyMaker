Natural Text
I'm looking for a way to parse arguments that look something like  or . I've done some research and found out about argparse, but as far as I know, this module only accepts command line arguments. I, however, want it to parse the arguments in the same way, but instead they are stored inside a string. Does anybody of you know of a way to do this?
You can use the  module and explicitly pass arguments to . You will first split your string into individual arguments with .
You just need to take a look at the   module, which do what you are looking for.If you are using python < 3.2, then the module is , which is then  deprecated from newwer versions  


Answer URL
https://docs.python.org/3/library/argparse.html
https://docs.python.org/3/library/argparse.html#the-parse-args-method
https://docs.python.org/3/library/shlex.html?highlight=shlex#module-shlex
