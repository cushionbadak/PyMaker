Natural Text
I need to create a multi tiered dict in Python that ends up similar to thisIts a dict that shows work hours logged by each employee per week by Location. A typical record follows this format [LOCATION,EMPLOYEE,HOURS_LOGGED,DATE], For each record I am deriving the week number from the date.My current algorithm which takes approx 10 secs to make this using 10,000 records is as followsAnyway I can imrpove on this
 offers one way: is a subclass of . We have defined a nested structure of . Since the defaultvalue for  is 0, we can easily increment values by  given arbitrary keys , , .


Answer URL
https://docs.python.org/3/library/collections.html#collections.defaultdict
