Natural Text
I have to find if word is present in text.I have to do  present in . In this it should detect PS though  is not present in . I have tried Thank you.
This looks for the substring  in full (so  is  but  is ), but I think you want to find if any of  or  or  are in the string. In which case you should use:

If you want to find all that are contained in your text:Output:Thiscreates all combinations from  of length . For  this iswhich are joined back to a string with ''.join(..) and each checked against your big string.Doku:itertools.chain.from_iterableitertools.combinationsThe "mechanism" is documented in power-recipies on the itertools-page as :which I modified to not contain the empty tuple itself is an iterables so you can "use" it only once, persist it into a list if you need it more then once:
You can try this approach if you want:output:


Answer URL
https://docs.python.org/3/library/itertools.html#itertools.chain.from_iterable
https://docs.python.org/3/library/itertools.html#itertools.combinations
https://docs.python.org/3/library/itertools.html#itertools-recipes
