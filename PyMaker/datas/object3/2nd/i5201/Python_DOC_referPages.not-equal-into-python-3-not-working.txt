Natural Text
This question already has an answer here:What's the difference between raw_input() and input() in python3.x?                    7 answers                I'm trying to switch a code from Python 2.7.10 to Python 3, and some things aren't working. I just recently was introduced to Python.How do I change the "!=" into something Python 3 will understand? When I type in 1 or 0, it gives me my "hello, no" invalid print.
 in Python 3 is equivalent to Python 2's ; it reads a string, it doesn't try to  it. This is because  of user input is intrinsically unsafe.If you want to do this safely in Python 3, you can wrap the call to  in  to convert to , or in  to interpret arbitrary Python literals without opening yourself up to arbitrary code execution.
The  function returns user input as a string. You can convert it to a number using the  and  methods:


Answer URL
https://docs.python.org/3/howto/pyporting.html
