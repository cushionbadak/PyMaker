Natural Text
I have a function from the  package,In order to update an object in the salesforce database, you call sf by:For example,  is the native customer account object in salesforce, so you would feed  for updating accounts like this:I was wondering if there is a way in python to set that specific piece of the chain as an argument.what I would like to do:That way I could call:The only other way I can think of doing this is to create a dictionary with the dozens of values for objects in the instanceIs there a way to do this?
You can use builtin function  to fetch your desired entity:To also able to dynamically select operation(insert, update, delete) you can chain  


Answer URL
https://docs.python.org/3/library/functions.html#getattr
