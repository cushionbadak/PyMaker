Natural Text
I have a list another list and an empty list . list has the index of columns of  list that has to be copied to .So  should be .How can i do this??I want  as a dataframe.

With numpy you can use a list as list index:
You can use  along with simple list comprehension to fetch your desired elementsTo Convert it to  you can do
Just to put it out there, the enumerate solution:This would work faster if  was a set:


Answer URL
https://docs.python.org/3/library/operator.html#operator.itemgetter
