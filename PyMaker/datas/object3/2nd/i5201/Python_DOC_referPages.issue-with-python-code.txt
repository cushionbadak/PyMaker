Natural Text
For some reason my code is only converting AUD to USD no matter which option I Choose. I was wondering where I went wrong and what I missed. Thanks in AdvanceCurrency converter
The  function returns a value, but you haven't used it. Your statementis always true (because  is not zero). Here is where you would use the return value returned by  from the previous statement. Use something like:
The right side of an  statement is evaluated. So when you write The computer converts this to a boolean value (either True or alse). For integers, all numbers but 0 are converted to True, so this code always runs.Use a variable to store the result of , and compare that to 2:


Answer URL
https://docs.python.org/3/tutorial/introduction.html#numbers
