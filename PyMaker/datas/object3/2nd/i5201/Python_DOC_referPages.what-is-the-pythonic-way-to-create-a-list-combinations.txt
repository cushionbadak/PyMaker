Natural Text
This question already has an answer here:Combining lists in python                    2 answers                Let's assume I have two lists:How can I get a list containing pairs of words from both these arrays? To visualize:I know I can do it using two  loops but is there a 'pythonic' way?
You can simply use  from .I do not think it gets any more pythonic than this.Under the hood,  generates tuples of the form  which we simply  to produce the desired result.


Answer URL
https://docs.python.org/3/library/itertools.html#itertools.product
