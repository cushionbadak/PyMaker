Natural Text
Sorry noob here.I would like to have a function which, for a given natural number, will printout all possible pairs of 2 natural numbers whose product makes the input.I think it could be like this, but still can't figured out how to write it properly: (the last output line is missing in my code output)And output should be like this:Thanks for the help and possible explanation
The  function does not include the end (stop) value so  is never actually . This is easy to correct with a . You should also use integer division () as it is neater than passing a  into .Making your function:and it now works:We can see from the documentation that  does not include the  parameter:For a positive step, the contents of a range  are determined by the formula  where  and .The proper terminology is to say that  does not attain the upper bound ().
Just use  in the range function:


Answer URL
https://docs.python.org/3/library/stdtypes.html#range
https://docs.python.org/3/library/stdtypes.html#typesseq-range
