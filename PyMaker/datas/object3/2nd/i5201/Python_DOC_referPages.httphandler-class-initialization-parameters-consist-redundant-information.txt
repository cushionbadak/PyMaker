Natural Text
From the official docs on the HTTPHandler class:Returns a new instance of the HTTPHandler class. The host can be of the form , should you need to use a specific port number.There is no documentation of the  parameter, and I'm not quite sure what to put there.For example, if I have a server listening on , then I think I should set:But wouldn't that always be redundant information?Or else, what should I set the  parameter to?Thank you.
You only need to add the path, like:


Answer URL
https://docs.python.org/3/library/logging.handlers.html#httphandler
