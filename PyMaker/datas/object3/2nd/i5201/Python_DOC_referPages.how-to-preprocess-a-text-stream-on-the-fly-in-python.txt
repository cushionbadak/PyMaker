Natural Text
What I need is a Python 3 function (or whatever) that would take a text stream (like  or like that returned by ) and return a text stream to be consumed by some other function but remove all the spaces, replace all tabs with commas and convert all the letters to lowercase on the fly (the "lazy" way) as the data is read by the consumer code.I assume there is a reasonably easy way to do this in Python 3 like something similar to list comprehensions but don't know what exactly might it be so far.
I am not sure this is what you mean,  but the easiest way i can think of is to inherit from file (the type returned from open) and override the read method to do all the things you want after reading the data. A simple implementation would be:
I believe what you are looking for is the  module, more specifically a .You can then use the  method to get the initial data and modify, then pass it around:


Answer URL
https://docs.python.org/3/library/io.html#overview
https://docs.python.org/3/library/io.html#text-i-o
