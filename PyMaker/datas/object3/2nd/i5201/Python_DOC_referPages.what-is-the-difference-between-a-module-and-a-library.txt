Natural Text
Is the  python module considered as an external module for python?I am asking because I need to write a python script (without the use of external Python libraries) that parses Portable Executable (PE) header files (e.g. extracting the section names of a PE file).
What is the difference between a module and a library?A library is a collection of code intended to be used by other libraries and/or applications.  A library can be made up of many modules.A module is an object that serves as an organizational unit of Python code. Modules have a namespace containing arbitrary Python objects.  Typically, a module is written in one .py file, but there are other ways to create modules.  A module might be a script, an app, or a library.is "pefile" python module considered as an external module for python?The  module is a 3rd-party module.  It is not included in Python standard libraries.  You can get the module by installing an external distribution, which you usually download from PyPI (the Python package index) using .


Answer URL
https://docs.python.org/3/library/index.html
