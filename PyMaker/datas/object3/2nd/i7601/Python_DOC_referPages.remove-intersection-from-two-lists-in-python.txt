Natural Text
Given two lists, what is the best way to remove the intersection of the two?  For example, given:I want to return:
Let's assume you wish to handle the general case (same elements appear more than once in each list), the so called multiset.You can use :For your values of , , you'll get:Note that for a special case of each element appearing exactly once, you can use the standard , as the other answers are suggesting.
You can loop through the two lists and remove elements as you find an intersect point as the following:output:
Output:


Answer URL
https://docs.python.org/3/library/collections.html#collections.Counter
