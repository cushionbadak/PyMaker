Natural Text
“each_ID”and “each_Title” are from website unicode parameters, but why it cause a “float”error,  %s is not a string? 
You have loads of  formatters in your string.  formats a float object. You have several of those in your string, including at the start:You'd need to double up every single  used in a URL character escape:That'd be a lot of work, you'd be better off using a different string formatting style. Use :I broke the string up into multiple chunks to make it easier to read; the  brackets delineate the placeholders.
Try using the  method on string. The existing  chars conflicting with your  placeholders :


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.format
