Natural Text
My input:This is just an example, but the real data follows the same structure.Why is my output always , and never ?
 will return the first match, so for   is always going to be .Use the  function to provide a running index instead:Take into account that  can be outside of the range of valid indices.


Answer URL
https://docs.python.org/3/library/functions.html#enumerate
