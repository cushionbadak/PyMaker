Natural Text
I'm trying to make a desktop notifier, and for that I'm scraping news from a site. When I run the program, I get the following error.How do I resolve it? I'm completely new to this. I tried searching for solutions, but none of them worked for me.Here is my code:
You're trying to convert a  to , and then store those bytes in a dictionary.The problem is that the object you're doing this to is an,not a .You probably meant to get the text from within or around that element, and then  that.The docssuggests using themethod:This will evaluate to a , which you can then .Note that the and  attributesmight not contain text(emphasis added):Their values are usually strings but may be any application-specific object.If you want to use those attributes, you'll have to handle  or non-string values:Even this is not really enough.If  or  contain  objects of some unexpected(or plain wrong)encoding, this will raise a .Strings versus BytesI suggest leaving the text as a , and not encoding it at all.Encoding text to a  object is intended as the last step before writing it to a binary file, a network socket, or some other hardware.For more on the difference between bytes and characters, see Ned Batchelder's"Pragmatic Unicode, or, How Do I Stop the Pain?"(36 minute video from PyCon US 2012).He covers both Python 2 and 3.Example OutputUsing the  method, and not encoding the strings, I got a reasonable-looking list-of-dictionaries from :


Answer URL
https://docs.python.org/3/library/xml.etree.elementtree.html#xml.etree.ElementTree.Element
https://docs.python.org/3/library/xml.etree.elementtree.html#xml.etree.ElementTree.Element.itertext
https://docs.python.org/3/library/xml.etree.elementtree.html#xml.etree.ElementTree.Element.text
https://docs.python.org/3/library/xml.etree.elementtree.html#xml.etree.ElementTree.Element
