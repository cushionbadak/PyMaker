Natural Text
So how would I convert from test2 to test (i.e. so that unicode characters are printed)?  doesn't do it.  
You can use  encoding to decode  to .Note: But even with that,  cannot be decoded to match exactly  because there's a  in  just before the closing quote ().


Answer URL
https://docs.python.org/3/library/codecs.html#python-specific-encodings
