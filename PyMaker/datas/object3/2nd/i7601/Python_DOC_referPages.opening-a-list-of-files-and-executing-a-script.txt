Natural Text
I wanted to list all the .pdb files and .ent files and execute the script and prints the sequence but it seems the program is not running properly and it does not show me any result.The code is
os.listdir return a list of strings [1] so you can't do "for lines in file" on them (doing that, you just iterates over file name string, not over file contents). You can use os.walk() [2] for this, e.g.:Also remember not to use reserved words like "list" or "id" - check those here docs.python.org/3.3/reference/lexical_analysis.html#keywords[1] https://docs.python.org/3/library/os.html#os.listdir[2] https://docs.python.org/3/library/os.html#os.walk
Working on windows, you can try specifying your path with an escaped backslash. Also, the s have been collapsed but they do the same thing. Overly deep indentations can hinder readability. 


Answer URL
https://docs.python.org/3/library/os.html#os.listdir
https://docs.python.org/3/library/os.html#os.walk
