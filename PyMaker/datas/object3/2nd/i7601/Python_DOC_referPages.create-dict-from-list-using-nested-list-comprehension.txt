Natural Text
I have a list like:from which i have to create dict.this way it works but when i tried to put it in a single line, error comes, too many value to unpack.Is it because we cannot add operational statement in right side (like split). Is there any other optimized way for the task?
You can't do  because that would only work for an iterable returning pairs of elements (such as , for example), and  returns a flat list. You need to use a list comprehension or a generator expression for the  part:Or just use the  constructor:


Answer URL
https://docs.python.org/3/library/stdtypes.html#dict.items
