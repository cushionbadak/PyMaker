Natural Text
I have a simple script that create async tasks for loading different pages. The first request fails with TimeoutError, and it causes next queries to fail too. But the second one has much longer timeout and should pass.Is it possible let other queries don't fail?Log:
Yes, It's possible. I rewrite your code and use some of this concept to fill your's requests:I used  in order to pass multiple arguments and create a list of each arguments which passed to the  function.I used  in order to wrap the tasks together, and used changed the return_exceptions flag to true to ensure that the exceptions won't raised.Changed the  before the . It returns the gathered tasks right now.I closed the loop in the end.The code:For more reading about: asyncio.gather.For more reading about: itertools.starmap.Enjoy!


Answer URL
https://docs.python.org/3/library/asyncio-task.html#asyncio.gather
https://docs.python.org/3/library/itertools.html#itertools.starmap
