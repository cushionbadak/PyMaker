Natural Text
I've got stringText like thatI'd like receive: cooking food blog 5 yearsI tried many different regexlike:orand using (.+) according regex101.com my regex should work correctly, but it doesn't. I have no idea why
Try use findall():Output:
Seems like it's because you're using  instead of .  Searches from the start of the string while  searches anywhere. This works:By the way, you do not need to escape the quotation marks () if you're using  meaning this is enough:
 returns a  object, from which you need to retrieve the desired group.Note that it's also possible to access the group using the index (0 in this case) but I prefer to specify a keyword.


Answer URL
https://docs.python.org/3/library/re.html#re.match
https://docs.python.org/3/library/re.html#re.search
