Natural Text
how would I go about extracting data such as that below. eg extract the body style. I will be doing this for a number of entries in the api and not just one entry so I guess i will need to use some sort of loop. Thanks alot :DThe output above is just a small extract (too long otherwise)
Reading the documentation, surprisingly the first example from the first google match for  on the first screen tells how to get the json body:Then you could for example print all top-level objects like thisOr :
Not very well structured question but you could use the json_normalize from pandas  to extract data from json. This is not from requests though. 


Answer URL
https://docs.python.org/3/library/json.html
