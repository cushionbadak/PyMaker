Natural Text
Let's say I have bunch of strings which start with 'name' and are followed by a number of any length.How can I parse out the number in the string?  Is  the only way or is there a built-in module that can accomplish this task?
In Python 3, you could do the following:Giving you: gives you a string containing all upper and lowercase letters. Python's  function takes a string specifying the set of characters to be removed, which is this case is all alpha characters, thus leaving just the numbers behind.Note: This would not be suitable for a string such as .
If you know that the prefix is , then you can either remove just that string, or you can skip the first four letters, like so:


Answer URL
https://docs.python.org/3/library/stdtypes.html?highlight=strip#str.strip
