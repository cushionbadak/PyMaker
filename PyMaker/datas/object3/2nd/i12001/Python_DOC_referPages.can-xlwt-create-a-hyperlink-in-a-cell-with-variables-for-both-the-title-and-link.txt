Natural Text
For instance, How would the following line be changed such that "test" is variable T and "http://google.com" is variable L?
Try this:This uses  to insert the values of variables  and  in to the string . Following the above assignments  will contain:It's also possible to do it without using the temporary variable:UpdateThe code above answers the question as it was stated, however, the OP actually requires that "test" be displayed as the link and the URL be the target, so:


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.format
