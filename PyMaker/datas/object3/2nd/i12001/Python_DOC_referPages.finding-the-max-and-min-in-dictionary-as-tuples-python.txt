Natural Text
so given this dictionary i'm trying to find the max value and min value the output should be in tuples for examplekey: (min,max)
This will return 

Just an example:will print:I'll leave you the task of finding out how the  is going to be inserted.The Python documentation on built-in functions is very helpful on how ,  and  can be used.An alternative would be to simply create a  function:Or if you want it faster I recently created a package containing such a function () that requires only one-pass (and thus less comparisons):


Answer URL
https://docs.python.org/3/library/functions.html
