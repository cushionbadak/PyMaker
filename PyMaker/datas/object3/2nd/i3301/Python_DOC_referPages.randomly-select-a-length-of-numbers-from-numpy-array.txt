Natural Text
I have a number of data files, each containing a large amount of data points. After loading the file with numpy, I get a numpy array:How do I randomly select a length of , but the order of numbers should not be changed?For example:if I wanted to select a random length of 3 data points, the output should be:, or, or, etc.
Pure logic will get you there.For a list  and a max length , the valid starting points of your random slices are limited to :So all valid starting point can be selected with  (where the  is because  works like ).Store the random starting point into a variable  and slice your array with , or be creative and use another slice after the first:


Answer URL
https://docs.python.org/3/library/random.html#functions-for-integers
