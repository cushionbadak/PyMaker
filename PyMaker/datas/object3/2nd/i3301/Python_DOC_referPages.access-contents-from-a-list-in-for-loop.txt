Natural Text
I can't think of a solution for this:i have this list: i want to make a process for every shapefile in the list but the problem is that it is a list of lists:How can I make it access each one at a time from the list?To get a an idea of how is the list, check these tests.
You have a list of lists, so flatten it first then use your loop
Seems you have a list of lists, here. Each element of  is itself a list of two filenames. So, I'd recommend just nesting two loops:


Answer URL
https://docs.python.org/3/library/itertools.html#itertools.chain.from_iterable
