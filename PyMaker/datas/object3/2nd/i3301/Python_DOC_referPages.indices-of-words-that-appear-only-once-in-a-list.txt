Natural Text
I have the following list of words:I need to remove the words that appear only once, and return there indices, or positions.Any simple function to do this?
Alternative one-liner with  module and its  function:To remove all non-duplicates:
You can use  class for exactly this purpose:Output is:
You can use  and isolate items appearing only once. then use list comprehensions for your desired results. The solution is O(n) overall, although it involves 3 passes.
Could you create a new list, and work with that?
You can also use list comprehensionoutput['Gaga', 'Gaga', 'Lam', 'Gaga', 'Lam']


Answer URL
https://docs.python.org/3/library/collections.html#collections.Counter
