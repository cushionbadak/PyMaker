Natural Text
According to Python's docs,  uses  and  if  is not implemented.I've encountered a weird behavior and failed to explain it:Although calling  on mapping types makes no sense, how does it know it's a mapping? Does it internally check ? Does it check for any general mapping like ? Is there any way to override this behavior without implementing ?I thought it might be due to  implementing a  that throws a , or one that equals  much like how you disable , but  turned out empty with  thrown.
Yes, there's a check for  type in  used by .


Answer URL
https://docs.python.org/3/library/functions.html#reversed
https://docs.python.org/3/c-api/sequence.html#sequence-protocol
