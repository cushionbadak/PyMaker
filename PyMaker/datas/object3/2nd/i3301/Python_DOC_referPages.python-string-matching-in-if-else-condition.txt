Natural Text
I am currently trying to find line based on the same pattern. If the line match the pattern, i want to print the line in output file.Here is one the example of the line in "in.txt":The script that i currently have with the help of @gilch:But then, is it possible to make the output like below:e.gOutput in "out.txt":
You'll need to import the  module to use regex.Also, indentation is part of Python's syntax. The colon isn't enough.This also assumes that  is already some iterable containing your lines. (The above code never assigns to it.)
Try this:


Answer URL
https://docs.python.org/3/library/re.html?highlight=repetitions#regular-expression-syntax
