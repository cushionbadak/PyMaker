Natural Text
Right now I have a script that is meant to loop forever. However, I would like to do something different on the first 'lap' along the lines of the following:Note that this code does not work because math.inf is a float, NOT an integer. And this post here says that in Python there is no way to represent infinity as an integer.Using  could make sense in this situation, but is there any way to get the function to print something different for first (or the xth) repetition for that matter?
If you wish to use an infinite  loop while keeping a counter, you can use :
Put it before the loop.
The obvious way to do that is to put first iteration before the loop:But, this example does not specify rest of the problem. 
Just count the laps and compare the current lap with the desired lap. For the xth repetition:


Answer URL
https://docs.python.org/3/library/itertools.html#itertools.count
https://docs.python.org/3/library/itertools.html#itertools.count
