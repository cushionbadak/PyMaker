Natural Text
I am trying to decode data from an API, I just cannot think of a clean way to extract the value and time values. I been trying to do string manipulations, but ends up very complex. 
This is in JSON format. Use the python json encoder/decoder to load this data. It will turn it into a dictionary, and something likewill return you that list.
You can use Python's  module for this, or you could just munge the data yourself and turn it into a dict since it is practically one already. Then all you would need to do is loop over it like so:


Answer URL
https://docs.python.org/3/library/json.html
