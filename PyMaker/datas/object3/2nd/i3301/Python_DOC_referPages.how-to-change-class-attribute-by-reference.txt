Natural Text
I'd like to be able to update attributes, for example  and have the data be recalculated automatically.I tried doing something likebut it threw an error 
That  can only be done if you've already declared  as a .You're also going to need a getter—which you usually just declare with the initial .Finally, you probably want to go behind the  property and store stuff directly in the raw  attribute in your , because otherwise you're going to end up setting  twice.So:You might also want to make  a read-only  while you're at it, to make sure nobody accidentally breaks it, but that may not be worth it ("consenting adults" and all that).


Answer URL
https://docs.python.org/3/library/functions.html#property
