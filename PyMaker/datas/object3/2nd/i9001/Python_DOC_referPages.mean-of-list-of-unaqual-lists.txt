Natural Text
I try to get a list that include the mean of all of (i)th element in all lists of a list.To explain more, if i have the next list:I want to have  with:Any help please!
use  to zip values together. The extra difficulty is that the length of the data varies.  inserts  as a fill value. We have to create a filtered list, and count only those items:result:EDIT: one-liner (maybe a bit more complex, using  to compute mean):result: (Python 2 users have to replace  by )
You can try with  like so if you are on Python2:Output:
Use  to generate the sublists, and  to calculate the average for each group, disregarding the s:With your example:


Answer URL
https://docs.python.org/3/library/itertools.html#itertools.zip_longest
https://docs.python.org/3/library/statistics.html#statistics.mean
