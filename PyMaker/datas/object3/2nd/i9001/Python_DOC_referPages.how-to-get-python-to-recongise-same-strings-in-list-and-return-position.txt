Natural Text
I have a sentence words and When a word in that sentence is inputted as a sort of "find this word" feature, the program identifies all of the positions where the word occurs in the sentence and return the resultif the sentence was:And I wanted to find the word upon, the outcome would bethe sentence isn't case sensitive so Upon and UpoN are treated the same. Also the sentence would be inputted by the user.Any ideas on how to code this?
One way is to  the list. Check value against keyword then keep index.Output:


Answer URL
https://docs.python.org/3/library/stdtypes.html#text-sequence-type-str
