Natural Text
I have been coding for about 1 and half years now on and off, but I still don't get how grids work in Python.So my problem is that I have to make a grid to display a population model. So I need to populate my grid with different variables that change for every row you have in the grid:So how would I got about this?
You really don't need a concept of a grid to accomplish what you want.  You simply need a  to propagate your population from one generation to the next.  You can then use Python's Format Specification Mini-Language to print the values in a table that matches your example output.You can use the above functions like so:


Answer URL
https://docs.python.org/3/library/string.html#formatspec
