Natural Text
In the following code, I want to count the occurrence of every word in  in , the code below can do this job but it may not be efficient, is there any better way to do it?
Use  to count all the words in  in one go, then just get that count from the  for each word in .Or using a dictionary comprehention:Creating the counter should be O(n), and each lookup O(1), giving you O(n+m) for n words in  and m words in .
You can do it in linear time using a dictionary.
You can combine  and  :Which gives:or if you rather want it as a :
You could try to use dictionnaries :But you would end with a different structure.If you do not want that, you could try this instead


Answer URL
https://docs.python.org/3/library/collections.html#collections.Counter
https://docs.python.org/3/library/collections.html
https://docs.python.org/3/library/operator.html#operator.itemgetter
