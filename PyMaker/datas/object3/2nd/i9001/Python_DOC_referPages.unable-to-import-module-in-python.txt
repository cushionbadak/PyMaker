Natural Text
Why am I having an 'undefined bdir module' error, here is my directory,a.pybdir->bdir>module.pyin a.py 
Put an  file (even empty) in your bdir folder.
You must create a  file, that's how Python knows which folders are packages that can be imported using the . Here's the documentation:The  files are required to make Python treat the  directories as containing packages; this is done to prevent  directories with a common name, such as string, from unintentionally  hiding valid modules that occur later (deeper) on the module search  path. In the simplest case,  can just be an empty file, but  it can also execute initialization code for the package or set the   variable.
Any folder without a  file inside of the folder is not considered a module. Furthermore, if you want to  from a module, make sure that actually import the things you require into , or declare a  list.Also, if you want to make a relative import, meaning that you want to import a file from the package that a module is currently in, then you do a relative import. So, for example, if you have:In order to import anything from the , you have to import it like so if you are in :If outside the  module then:


Answer URL
https://docs.python.org/3/tutorial/modules.html#packages
