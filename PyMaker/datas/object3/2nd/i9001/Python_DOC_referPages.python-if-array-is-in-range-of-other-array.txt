Natural Text
Meaning "if each item is within range of other item with the same index".This works:I figure there's some way to just reference the whole array though. Like so: 
You could use:This will create tuples of  and then for each of these tuples we check that  is in . Nevertheless, you can optimize this further to:Note that this makes the assumption that s are all integers. If you however use  it will fail if  is not an integer. So  would fail whereas our second approach will succeed (but it depends of course on what you want).
Assuming that both prices and budgets must be non-negative, using  seems to be over-complicating things. Instead, you could just use the  operator.Regardless of whether you use  or , it seems like the easiest approach would be to  both lists and apply the condition on the pairs:


Answer URL
https://docs.python.org/3/library/functions.html#all
