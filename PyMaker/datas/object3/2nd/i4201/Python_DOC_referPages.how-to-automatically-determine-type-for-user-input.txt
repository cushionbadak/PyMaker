Natural Text
I want to make a simple math function that takes user input, yet allows the user to not input an integer/float. I quickly learned Python does not identify type by default. Quick Google search shows using , but it returns with  if a string is the input. This is what I have so far:
Like you mentioned you will get malformed string error () if you get input like .  You will also get  if you do something like .  You will want get the input data and then pass it to .  You can then catch both of these exceptions, and then return your .
Just answer your query why  occurred if you read the literal_eval doc :Safely evaluate an expression node or a string containing a Python  expression.  The string or node provided may only consist of the  following Python literal structures: strings, numbers, tuples, lists,  dicts, booleans, and None.so string should be inclosed by "" as used to write in editor like raw_input takes the input and convert to string data type so i have tried this and able to convert using 


Answer URL
https://docs.python.org/3/glossary.html#term-eafp
