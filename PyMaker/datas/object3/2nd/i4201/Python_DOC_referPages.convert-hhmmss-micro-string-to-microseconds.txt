Natural Text
I'm sure there must be a better way to convert a string like  to microseconds like this:but yet I didn't find it.
This is one way using only :
If you're not opposed to third party libraries,  might be useful for you:This returns: 
The basic idea is to use  to parse your string to get a  object.  Since  objects don't have a nice method for total seconds, but  does, we subtract another , which results in the  we're looking for.  Now we can take .  Multiply by 1000 and you have your answer (as a floating point number).


Answer URL
https://docs.python.org/3/library/datetime.html#strftime-and-strptime-behavior
