Natural Text
I have a nested list(the sublists will only have 2 elements), like this:I want to subtract the elements inside each nested list, like this should be the output:To sum the numerals in the nested list, I only had to use:but in subtracting since I couldn't any function like 'sum' for subtracting on the internet, I tried create one like this:but it returned:How can I get over this error and also make my code work?
You can use Extended Iterable Unpacking operator.Another way is to use .
You can achieve this by using the list comprehension
The way I would go about it is:
You can use a combination of  and :Demo:


Answer URL
https://docs.python.org/3/library/functions.html#map
https://docs.python.org/3/reference/expressions.html#lambda
