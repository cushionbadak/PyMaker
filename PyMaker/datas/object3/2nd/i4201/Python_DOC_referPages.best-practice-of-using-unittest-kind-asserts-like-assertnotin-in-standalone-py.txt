Natural Text
In standalone  test cases I would like to use assertions like  (and s other asserts) without any dependency on the  module. Is best practice to use libraries like python-assert?
The best practice is using the language  statement, which yields the 's  methods unnecessary. ComparewithThe latter is much more pythonic. It is also one of the key features that  names in the docs:Detailed info on failing assert statements (no need to remember  names)Should you be missing any  methods,  aims to provide replacements for them: becomes ,becomes etc.


Answer URL
https://docs.python.org/3/library/unittest.html#assert-methods
