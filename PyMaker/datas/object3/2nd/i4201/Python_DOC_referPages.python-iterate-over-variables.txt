Natural Text
Is there a python built-in for iterating over some variables?Something like:It would be useful for instance in the following case:Instead of allocating a list, which looks wasteful (is it?):Haven't been able to find anything among python built-ins.
Any such built-in would be much more expensive than creating a tuple, and would in fact involve creating a tuple for . Just make a tuple.In the specific case where , , and  are initialized like your example:you can just iterate over whatever you unpacked instead of unpacking it.


Answer URL
https://docs.python.org/3/library/functions.html
