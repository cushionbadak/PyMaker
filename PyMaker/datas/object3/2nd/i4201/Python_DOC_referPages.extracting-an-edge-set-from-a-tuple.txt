Natural Text
I have created the following program. It takes an input G, which consists of the vertices of a graph, along with the edges and the corresponding edge weights. The aim of  the program is to extract just the edgesOn the following inputThe following output is produced:The output I am trying to get is:Can anyone explain why the tuples I have extracted are reordered?
A  is an unordered collection. What you are requesting is not possible.The best you can do is use an ordered collection, such as  or . Below is an example.This is also possible functionally, but messy since Python has no native function composition. For composition, you can use 3rd party library .


Answer URL
https://docs.python.org/3/tutorial/datastructures.html#sets
