Natural Text
I have a path:I would like to remove the end path so that the string only contains:So far I am using split, but it just gives me a list, and im stuck at this point.
You should not manipulate paths directly, there is os.path module for that.Like this.
You can also use , like thisIt splits the path into two parts and returns them in a tuple. You can assign the values in variables and then use them, like this


Answer URL
https://docs.python.org/3/library/os.path.html#os.path.split
