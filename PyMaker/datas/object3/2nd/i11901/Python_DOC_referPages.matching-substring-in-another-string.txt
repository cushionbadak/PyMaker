Natural Text
I have two lists that contain strings. The first list contains a list of files and directories:The second list contains dirs that I want to check against  for existence.  The only results I want is , but when I use  it is returning any string that contains  or  or  regardless of where it occurs in the string.So instead of just getting:I get everything in My code is like so:
All non-zero numbers are Truthy.  When your string is not found,  returns  which is still .  You need to make sure the result is not :As @PadraicCunningham mentioned in a comment, that isn't the easiest way.  Just use the  operator:
I think you what you need is 


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.startswith
