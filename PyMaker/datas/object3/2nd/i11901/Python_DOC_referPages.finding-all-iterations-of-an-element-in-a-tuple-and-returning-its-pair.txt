Natural Text
So I have a list of tuples that looks something like:I've managed to sum all of the first entries which have a second entry 'United States' as followsIs there a way to automate this task so that I don't have to write a whole new line for every single country to sum all of its paired integers? Possibly a function that will return a list with each unique country and its total visits from each occurrence in the original list (visits_country)?Thanks!
Easy:
Yup. The most obvious approaches are to use either  or , the latter requiring a presort. For example:or:


Answer URL
https://docs.python.org/3/library/collections.html#collections.Counter
https://docs.python.org/3/library/itertools.html#itertools.groupby
