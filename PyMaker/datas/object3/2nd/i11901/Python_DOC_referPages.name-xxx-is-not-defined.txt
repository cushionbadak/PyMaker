Natural Text
Why these two function want not work ?I have that error:What I'm doing wrong ?How this should look like ?
In python, it is necessary to use  to access other methods and attributes of the same object. When you simply call , python looks for the function definition but will not look at the methods of . By explicitly prefixing  with , python then knows that you want the method  and it should work as you expect.So for your specific code, this would require you to update your  method to be the following:I'm assuming that there is additional code outside of the class which defines  and  as global variables which  can then access.


Answer URL
https://docs.python.org/3/tutorial/classes.html
