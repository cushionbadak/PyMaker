Natural Text
Why does this:delete all entries in the list ?As far as I understand,  returns a copy of . So shouldn't  delete the copy of ?
 is a special statement that will check the original value and delete it using the given slice.It calls  on the object and the object itself handles the deletion.If you're curious regarding the operation happening under the hood, you're welcome to implement the following class and use the  statement with different slices or key references:The test in the interpreter:


Answer URL
https://docs.python.org/3/reference/datamodel.html#object.__delitem__
