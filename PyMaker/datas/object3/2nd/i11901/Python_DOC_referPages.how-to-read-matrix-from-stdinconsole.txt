Natural Text
I'm trying to read from stdin and the first line specify the dimension of matrix always square matrixbut my code reads only the first line Example Input My code
You're only asking the number of rows but not the content of rows themselves. Following code shows how to generate the full matrix:It will first query the user the number of rows and pass that number to  which will ask the user to input every line.
According to the official documentation,The function then reads a line from input,  converts it to a string (stripping a trailing newline)Please, note reads a line. But here you are trying to read 4 lines with one call to . You might want to run a loop to take this input.


Answer URL
https://docs.python.org/3/library/functions.html#input
