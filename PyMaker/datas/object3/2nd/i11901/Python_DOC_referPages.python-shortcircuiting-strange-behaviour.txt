Natural Text
In the following code fragment function  gets executed as expected:But in the following similar code fragment  doesn't increment:But if we shortcircuit with  True instead of False  gets incremented:How does shortcircuit work for this to run this way?
That's because  is an expression (technically a single-expression statement) whereas  is an assignment statement. It actually resolves to , and since  equals  and  is actually the integer 0, what happens is , a no-op., however, evaluates to  (which is the integer 1), so  means .(also note that Python's  and  always return the first of their operands that can unambiguously determine the result of the operatio)
I believe that is equivalent toandis equivalent toFor example:


Answer URL
https://docs.python.org/3/reference/expressions.html#operator-precedence
