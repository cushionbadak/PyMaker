Natural Text
I have a list with strings called , I need to search each element in the  list with each element from the  list. Found several guides that can loop through for a individual string but not for a list of strings Thank you in advance!Desired output:
Use the any() function with a generator expression: is a short-circuiting function, so the first time it comes across a pattern that matches, it returns True.  Since I am using a generator expression instead of a list comprehension, no patterns after the first pattern that matches are even checked.  That means that this is just about the fastest possible way of doing it.
You can do something like this:The code is self explanatory, just read it out loud; we're creating a list of all names that have one of the patterns in them.Using two loops:


Answer URL
https://docs.python.org/3/library/functions.html#any
https://docs.python.org/3/reference/expressions.html#grammar-token-generator_expression
