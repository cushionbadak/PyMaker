Natural Text
I'm exploring python. If have now lerned from the answers, that when I use ,  is called to compute  and  to compute  (and this is more expensive, since the integer is tried to be called first :))So, in general:How can I see which function is called for some shortcut operator like , , or , ...
Python language reference 3.3.7: Emulating numeric types says this of the  magic methods:These methods are called to implement the binary arithmetic operations (, , , , , , , , , , , , , ) with reflected
 is called when the object itself is on the left, and  is for when it's on the right. For these particular objects,  has the same result as  because this kind of multiplication is commutative, so it comes out to the same thing, so both docstrings are right.


Answer URL
https://docs.python.org/3/reference/datamodel.html#emulating-numeric-types
