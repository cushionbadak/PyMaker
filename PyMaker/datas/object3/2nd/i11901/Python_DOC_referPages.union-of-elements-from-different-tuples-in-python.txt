Natural Text
I have 3 tuples: I need to join the first element of each tuple, the second element of each tuple, the third one... In order to print something like this:
You can use  to do this combination for you. It accepts multiple iterables and will return an iterable where each result contains an element from each of the inputs just as you have described.Here is a complete example using your input data:Then you can print each of these using  and expanding the tuple:If you want, you can also do this in one line if you don't care about the intermediate values and just want to print the combinations.
Just use a loop and index into each (Makes for easily readable code)
Solution is:


Answer URL
https://docs.python.org/3/library/functions.html#zip
