Natural Text
Whenever I run this piece of code it gives me this huge error.Is there anyway to fix the code to give the output, which is meant to be the index of all the , of something like .
 uses a regular expression to find matches. That regular expression is  in your case. The  has a special meaning in regular expressions, so if you just mean to search for a star character, you need to escape it:
You can enumerate the indices:
You could write a function that finds the index of all characters like this:Or take a look at poke's answer if you really want to use re.


Answer URL
https://docs.python.org/3/library/re.html#re.finditer
