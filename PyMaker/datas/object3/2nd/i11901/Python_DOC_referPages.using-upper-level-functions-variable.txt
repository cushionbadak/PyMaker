Natural Text
This question already has an answer here:Read/Write Python Closures                    7 answers                I want to use the  variable that I have defined in the  function. When I use the variable in  it should search it in the scope of its  function.However I get an Error
Changing:into:Should help. You can access  from the outer function but you cannot assign to it with .I do not recommend it for this case, but in Python 3 you can use  to actual do what you intend:It works for the first call of . But the next time you call  the value of  will be .


Answer URL
https://docs.python.org/3/reference/simple_stmts.html?highlight=nonlocal#nonlocal
