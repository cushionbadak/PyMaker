Natural Text
Im making a quick snap game for fun, just learning to code, and I can’t seem to get around this problem.I want them to input 2 numbers for  and , then store what the corresponding values from  and  are in  and . But I keep getting this error message:Using python 3 on pycharm community edition. Thanks!
 returns the index of the element value you pass it to. So for a list ,  would return .If you want to go the other direction, getting a value for an index, then you should use the item access syntax:So in your case, you want to use  in order to get the value that the user chose from .
 actually returns the index of the value that you pass to the method. You're getting an error because the value  isn't in the list that you're calling the  method on. If you're already turning the prompt into an  after getting it from the user, then you can just specify that you'd want the element at that index from the list like this:Be careful, though. The  that you get could be out of range of your lists. So, I would would put your inputs into a while loop to make sure that you get a valid input:
comparing the two pieces code:


Answer URL
https://docs.python.org/3/library/stdtypes.html#common-sequence-operations
