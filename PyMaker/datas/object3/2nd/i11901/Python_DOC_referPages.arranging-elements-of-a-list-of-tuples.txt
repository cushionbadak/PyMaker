Natural Text
I have a list 'l' of tuples.I want to arrange the first elements of tuples according to the values in ascending order. The expected result is:I tried as:I suppose that there is more better way of doing it. Any ideas please.
Here is my solution:And here is the result:
You could use a defaultdict to collect those values, and then get the items from the dictionary to get the desired result:In order to sort those sublists then, you could use a list comprehension:And if you want to sort that also by the “key”, just sort that resulting list using .
Here's a one liner for you:This sorts both the first element (, , ...), and each second element (  ).Note that @poke's solution does not sort it.


Answer URL
https://docs.python.org/3/library/collections.html#collections.defaultdict
