Natural Text
I need to do something like this:I found here simpler version:Is there version for ? Something like:How should I write this (if this exists)? The code above doesn't look right.
No, no s. Just chain the s:Which is equivalent to (as precedence is left to right):Obviously this could get complicated (and over the PEP8 80 char limit), E.G.:In which case the longer form is better:


Answer URL
https://docs.python.org/3/reference/expressions.html#operator-precedence
