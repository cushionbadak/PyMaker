Natural Text
I have a string like this:I want this text:I've tried to use this code:But as long as the  is in the string ,  always returns .  Note, I've tried using different combinations of  instead of  without success.
The character ranges  and  only capture ASCII characters.  You can use  to capture Unicode characters:Note too that I simplified your pattern a bit.  Here is what it does:Also, you do not need Regex to remove whitespace from the ends of a string.  Just use :


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.strip
