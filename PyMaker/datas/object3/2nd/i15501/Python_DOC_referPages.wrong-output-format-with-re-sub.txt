Natural Text
I expect this code to print  instead it printed . Please can someone point me in the right direction.
You need to use a raw-string for :Otherwise, it will be interpreted as an escape sequence:Note too that  is a special character in Regex patterns.  It tells Python to match any character (except a newline).  I think you meant to escape it before :Now Python will match a literal period.


Answer URL
https://docs.python.org/3/reference/lexical_analysis.html#string-and-bytes-literals
