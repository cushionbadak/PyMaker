Natural Text
The typical structure for exception handling is below:May I know what does or mean?Typically I will use  to print the error message but I am wondering what the programme has done to generate the e.If I were to construct it in another way (below), how would it be like?What should the method be to replace the ?When the body of code under  gives no exception, the progeamme will execute the code under . But, what does  do here?Thanks! :)
, or  (Python 2.x only) means that it catches exceptions of type , and in the  block, the exception that was raised (the actual object, not the exception class) is bound to the variable .As for , it's a block that always gets executed, regardless of what happens, after the  block (if an exception is raised) but always before anything else that would jump out of the scope is triggered (e.g. ,  or ).


Answer URL
https://docs.python.org/3/reference/compound_stmts.html#the-try-statement
