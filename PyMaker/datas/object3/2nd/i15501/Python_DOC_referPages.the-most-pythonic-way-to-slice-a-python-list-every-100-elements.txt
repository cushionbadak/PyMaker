Natural Text
This question already has an answer here:Slice a binary number into groups of five digits                    7 answers                I have a list contains many elements. I want to slice it every 100 elements to a list of several lists.For example: should then be .What's the most pythonic and elegent way to do that?
This should do the trick: takes an optional third  argument, so  will be .


Answer URL
https://docs.python.org/3/library/functions.html#func-range
