Natural Text
I'm stuck on dictionaries and sorting in python, I have the following values stored in a dictionary:I want to: 1) Sort these so that they are ordered by highest score first, to lowest score 2) Remove the scores from the dictionary, leaving only the namesE.g in the example above the function should return:
You can do this easily with :Note that the output is a list of names though.  You cannot sort a dictionary or a set in Python because they are both naturally unordered.
You can do as follows:Which results in:
It is not possible to sort a dictionary , because dictionary is orderless, but other types, such as lists and tuples, are not.But you can have a sorted representation.For instance,or you can also do
An option to keep the dic and sort it:Detail: sorted has the reverse(bool) arg, ex:The usage is normal:font: https://docs.python.org/2/library/collections.html#ordereddict-examples-and-recipes


Answer URL
https://docs.python.org/3/library/functions.html#sorted
