Natural Text
I would like to show a space after each . My code is:The output I get is:But I want:Also, I want to print from right side likeIs there any method to print from the last position?
You can use  to more easily join a sequence of letters with a given delimiter, in this case a space This gives
When you multiply a string, it doesn't have to be a single character, you can use Note that this will also show trailing space after each line; if this is problematic for you, you can use:You could also import the print function that use the separator argument :


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.join
