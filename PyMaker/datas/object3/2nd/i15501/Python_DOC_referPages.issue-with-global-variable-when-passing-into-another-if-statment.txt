Natural Text
I am trying to get Ardinput to pass into the  but it seems like Ardinput loses it's contents here is my output for the codeCode:
You removed just the  newline but left the  carriage return in place.Use  instead to remove all whitespace from start and end:and remove the space from :For future reference, it is far easier to spot these differences if you use the  or even  functions to produce a string literal version of the value:Note how the latter shows that there is still a  carriage return character in the value. and  are considered whitespace, as are spaces and tabs and the non-break space:Note that the space in the middle was not removed;  works on the lead and trail ends of a string only.
 represents a carriage return with a new line (hence:and not ). You will have to strip it for instance with   to remove the newline, or remove the last 2 characters with ).


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.strip
https://docs.python.org/3/library/functions.html#repr
https://docs.python.org/3/library/functions.html#ascii
