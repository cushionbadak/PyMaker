Natural Text
How can I get the exist status (if command succeed or not) of any line in Python?For example in bash,  will tell me the last exist status of any command.I need it to know if my connection to FTP server was successful or not.
If it is a function you call, it should have a retrun code you can collect likeThen you can check what happened.
Have you tried using a ? If there was an error while executing the command, an exception will be raised. You can retrieve it with the sys module.Example code:


Answer URL
https://docs.python.org/3/library/ftplib.html
