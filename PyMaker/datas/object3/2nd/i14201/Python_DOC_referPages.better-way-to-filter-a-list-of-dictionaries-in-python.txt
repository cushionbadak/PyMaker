Natural Text
I have a list of dictionaries that has the structure similar to this one:and is sorted by timestamp value. I would like to delete sequential identical actions done by the same user leaving just the first one, e.g. if I have the following list:I would like to obtain this list as a result:Currently I am doing it like this:Is there a better way to do this?
If you use  and group by the  and , you can grab the first element out of each of the groups.
Use  to group sequential actions by the same user and then take the first element of each group:
If you were to use a loop you just need to simply keep track of the last key you saw:If the actions are not always grouped check both keys:
Here's a verbose attempt at using :You will get the following results:


Answer URL
https://docs.python.org/3/library/itertools.html#itertools.groupby
