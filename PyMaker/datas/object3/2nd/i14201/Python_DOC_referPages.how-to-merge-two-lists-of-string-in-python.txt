Natural Text
I have two lists of string:I should result:What is the most pythonic way to do this ?
Probably with :You can also put multiple sublists into one large iterable and use the  operator to unpack it, passing each sublist as a separate argument to :You can even use the  operator with  to go in the other direction:
You can use 
How about:
You can use zip:You can also use enumerate with list comprehension:
You can use  together with :Or add  to manage more than 2 lists:For python < 3 I would prefer the former option or replace  for , for python 3 the change is not necessary ( already return an iterator).


Answer URL
https://docs.python.org/3/library/functions.html#zip
https://docs.python.org/3/library/functions.html#zip
https://docs.python.org/3/library/functions.html#zip
