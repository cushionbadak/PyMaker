Natural Text
This is my code for finding the speed of cars, when it runs it leaves a line between the printed  and the speedI have looked at how to remove the  from the end but all of them that I've seen and tried have made it print out the word  and not the number in the variable:
Lines read from a file include the line separators. You can use  to remove it:This will remove all whitespace (spaces, tabs, line separators, etc.) from the end of the line before splitting the line by comma.Your file format is perhaps more easily read with the  module:


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.rstrip
https://docs.python.org/3/library/csv.html
