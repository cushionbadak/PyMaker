Natural Text
I have defined some constant in my python code. Those constants are the following :I want to put those constants in text file and read them from my code. How is it possible to do so? How can I read in exact the same way those constants?EDIT: I tried to read the variables using the following code:However it does not produce the desired output.
Creates the output in list format Input file is created and saved as follows
Writing them is easyInterpreting them is similarly easy. First read the fileThis will be a string. Then turn it into a tuple using the following as a source of informationUsing python's eval() vs. ast.literal_eval()?


Answer URL
https://docs.python.org/3/library/csv.html
https://docs.python.org/3/library/csv.html#csv.QUOTE_NONNUMERIC
