Natural Text
I have a datetime list and would like to convert it into a date using the following code:The datetime list looks like the following:[datetime.datetime(2016, 11, 21, 0, 0), datetime.datetime(2016, 11,  22, 0, 0), datetime.datetime(2016, 11, 23, 0, 0)]I receive the following error message:TypeError: strptime() argument 1 must be str, not datetime.datetime
It appears the values in your  sequence are not strings; they are already  objects. You don't need to parse these any further. It is also possible you have a mix of strings and  objects; the exception you see is thrown because at least one of the values in  is a  instance already.Just call the  method on all objects in  to format them:Demo:In the event that you have a mix of strings and  instances in , you'll have to do some extra processing:


Answer URL
https://docs.python.org/3/library/datetime.html#datetime.datetime.strftime
