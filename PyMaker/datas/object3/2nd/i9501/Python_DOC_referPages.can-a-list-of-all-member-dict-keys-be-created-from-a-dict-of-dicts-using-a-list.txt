Natural Text
Say I have a dict of dicts:To get a single list of all keys in the member-dicts, I have a function as follows:The generated list looks like: .This is simple enough, and it works, but I wonder: is there a way to accomplish this with a one-liner list comprehension? The keys of the member dicts will always be unique.
Here's one way to do it:
An approach would be using :If what you want is just a one line of code, and not necessarily a list comprehension, you can also try (mentioned by @Duncan):Output:


Answer URL
https://docs.python.org/3/library/itertools.html#itertools.chain
