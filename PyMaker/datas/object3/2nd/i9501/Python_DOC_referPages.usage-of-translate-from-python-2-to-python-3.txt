Natural Text
I have a legacy code from  with in it:this lambda function is used to make a string like this:to look like this:In  this is no longer working, is there another simple way to do this?Because I've tried on  and I get the following error:if I remove the encoding I get this:Many thanks.
this is one way to get  to work in python3: you create a table using  first:
That lambda seems too obscure to salvage. More simply you could just use:


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.translate
https://docs.python.org/3/library/stdtypes.html#str.maketrans
