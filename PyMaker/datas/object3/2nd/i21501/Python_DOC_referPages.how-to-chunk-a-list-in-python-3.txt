Natural Text
I found the following code that is compatible with python2However, this isn't working with Python 3. I get the following errorCan someone help?I'd like to convert my list of  to EditNow Python3 compatibleCode below is adapted from the selected answer. Simply change name from  to .
In Python 3's  there is a function called . It should do the same as  from Python 2.Why the change in name? You might also notice that  is now gone in Python 3 - that's because in Python 3, the  built-in function now returns an iterator, whereas in Python 2 it returns a list. Since there's no need for the  function, it also makes sense to rename the  variant for consistency.
According to the doc:
After all that discussion above, here's a python3 solution that I believe gives safer, more predicable results.The results are:


Answer URL
https://docs.python.org/3/library/functions.html#zip
