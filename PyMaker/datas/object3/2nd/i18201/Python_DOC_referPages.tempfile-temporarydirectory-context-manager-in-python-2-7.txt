Natural Text
Is there a way to create a temporary directory in a context manager with Python 2.7?
Another option is the "backports.tempfile" package on pypi: https://pypi.python.org/pypi/backports.tempfileQuoting the project's description: "This package provides backports of new features in Python’s tempfile module under the backports namespace."Install with:Then use it in your script:
 was added to the tempfile standard library in Python 3.2It's a simple wrapper for . It is coded in pure Python and can be easily ported to Python 2.7. For example:


Answer URL
https://docs.python.org/3/library/tempfile.html#tempfile.TemporaryDirectory
