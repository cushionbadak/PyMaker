Natural Text
I am trying to match a set of complex numbers with re.match(). I am pretty sure the regex should work but still Python doesn't return anything.The output should be something like:But instead it is:
Try this:


Answer URL
