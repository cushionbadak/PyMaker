Natural Text
I tried: But it doesn't seem to work. I want the program to await user input, but if there's no user input after 10 minutes, continue with the program anyway. This is with python 3. 
Why does the code not work? returns nothing; the value of the  become ;  loop body is not executed.How to solve itIf you're on Unix, you can use .Otherwise, you should use thread.Windows specific solution that use :
Here is a simple way using an alarm. When the time expires, the lambda function is called, which raises a 
Another way to do it is this:Not the greatest of answers and it definitely feels like an abuse of exceptions, but it works.


Answer URL
