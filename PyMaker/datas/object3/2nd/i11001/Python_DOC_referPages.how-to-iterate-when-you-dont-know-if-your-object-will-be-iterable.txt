Natural Text
Sometimes I have multiple axes in , and sometimes just one. So to iterate over it/them, I use the following:What is the idiomatic way to do this?
In general, the best way is to just iterate. Don't ask for permission (with  checks, rather wrap it in a  block for handling the "forgiveness" cases.As others have commented though, you shouldn't create different cases if  is a list of size , that still is iterable. If you use  it will stop at the shortest iterable: If you need to work with all values of the longer iterable and provide defaults for the missing values of the shorted one, use  from itertools with an appropriate :


Answer URL
https://docs.python.org/3/glossary.html#term-eafp
https://docs.python.org/3/glossary.html#term-eafp
