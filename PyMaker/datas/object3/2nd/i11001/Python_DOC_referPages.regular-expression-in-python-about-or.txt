Natural Text
I know  can work, but I want to rewrite  using variables, something likebut this doesn't work. How can I rewrite this so that variables are used (and it works)?
You are not doing the string concatenation correctly. What you are doing is applying the "bitwise or" (the pipe) operator to strings, which, of course, fails:Instead, you can use :Demo:And, make sure you trust the source of  and  (beware of Regex injection attacks), or/and properly escape them.


Answer URL
https://docs.python.org/3/library/re.html#re.escape
https://docs.python.org/3/library/re.html#re.escape
