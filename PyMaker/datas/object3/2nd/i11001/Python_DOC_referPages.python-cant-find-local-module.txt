Natural Text
I have a folder structure like this:If I try to import  in  and run it, I get an  saying that the  module can't be found. However, I can import  in  without an issue. My IDE doesn't freak out, so why is this error occurring? 
When you  your package, Python searches the directories on  until it finds one of these: a file called "core.py", or a directory called "core" containing a file called . Python then imports your package.You are able to successfully  from  because the path to the  directory is found in .  You can see this yourself by running this snippet from your file:If you want to import  from a different file in your folder structure, you can append the path to the directory where  is found to  in your file:


Answer URL
https://docs.python.org/3/reference/import.html
https://docs.python.org/3/glossary.html#term-package
