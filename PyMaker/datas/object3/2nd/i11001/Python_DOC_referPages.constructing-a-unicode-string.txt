Natural Text
I'm trying to construct and print a Unicode string with Python 3.x. So, for example, the following works fine:But if I try to construct this by appending two strings, I have several problems:What am I missing here?
 is an escape sequence, a notation used in string literals. You can't construct that notation from parts, at least not directly like that.Generally, you'd use the  function to produce individual characters from an integer instead:for example, where I first interpreted the hex string  as an integer in base 16.If you must generate the Python string literal escape notation, use  with the  codec:


Answer URL
https://docs.python.org/3/library/functions.html#chr
