Natural Text
How can I open and FTP url and download it into a file. What I'm trying looks something like this:However, this fails with the following error:Is there any simple way to make this work? Especially if I want to extend it, so that the file is extracted while it is downloaded?
The  requires object with buffer interface in particular , but the  is actually  (or  if http). To get bytes you need to call 


Answer URL
https://docs.python.org/3/library/io.html#io.BufferedReader.read
