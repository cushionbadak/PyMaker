Natural Text
I have string:And knowing list of strings like I want to get  selection like:I joined list and tried: idea from hereGotChanging places in regex with  also didn't work out. How to do it?I could use something like  but I need good performance as this will be used with million rows of pandas dataframe with str.replace
The  character has special meaning in regular expressions. You can use  to make a string "safe" for use in a regular expression.Pandas has its own  which should work like 
Maybe split stringusing your list and remove from oryginal string what left after spliting.


Answer URL
https://docs.python.org/3/library/re.html#re.escape
