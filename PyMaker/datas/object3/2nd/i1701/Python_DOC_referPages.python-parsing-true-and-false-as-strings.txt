Natural Text
This question already has an answer here:Converting from a string to boolean in Python?                    27 answers                Is there a way in python to parse the string  as  (boolean) and  as  (boolean)?I know I could do  or  but each would be 
Use :If you do , you see :You could also write your own function that returns  as boolean ,  as boolean  and if you supply any other input, it returns the same back:Now, you call as:
In this case I would not recommend  or . The best thing to do is probably this: will return  and  will return .


Answer URL
https://docs.python.org/3/library/ast.html#ast.literal_eval
