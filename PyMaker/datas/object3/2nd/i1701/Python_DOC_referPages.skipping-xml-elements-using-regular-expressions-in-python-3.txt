Natural Text
I have an XML document where I wish to extract certain text contained in specific tags such as-In this toy example, if I want to extract all the text contained in  tags by using the following Regular Expression code in Python 3-It gives me the text within the XML tags ALONG with the tags. An example of a single output would be-My question is, how should I frame the pattern within the re.findall() or re.search() methods so that  and  tags are skipped and all I get is the text between them.Thanks for your help!
Just use a capture group in your regex ( takes care of the rest in this case). For example:


Answer URL
https://docs.python.org/3/library/xml.etree.elementtree.html
