Natural Text
Say I have the following string:I would like to convert it into a list that would keep words as strings in separate elements, and numbers in another (as integers):My current code looks is:and the result prints out as:How would I be able to write a code to keep words in separate entries, and turn numbers inside the string into integers in the list?  I am a beginner to programming who is currently learning Python in parallel with C.
Look into the  function.It takes the form ofso you want your code to look like this to break it apart.To convert the numbers to integers, just check for if you're not familiar with list comprehensions, this is analogous to
You really should google it....but you can use 


Answer URL
https://docs.python.org/3/library/stdtypes.html
https://docs.python.org/3/tutorial/datastructures.html
