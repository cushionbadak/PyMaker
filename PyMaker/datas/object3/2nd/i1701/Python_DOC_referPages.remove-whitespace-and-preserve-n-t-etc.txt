Natural Text
How do I preserve line breaks, tab, ...etc from a text? Currently, I can take out extra white spaces in the text document, along with that it also removes \n, \t, unicodes, ...etc. I tried this but doesn't help. Current Output: Needed Output:
You told the regular expression to match all whitespace, not just spaces. Don't use  if you only want to match spaces, use an actual space:Demo:From the Regular Expression Syntax section of the  module documenation, on the  sequence meaning: Matches Unicode whitespace characters (which includes , and also many other characters, for example the non-breaking spaces mandated by typography rules in many languages). If the ASCII flag is used, only  is matched.
You can use  and :The key element is the regex  that splits on runs of actual  space characters longer than 2 spaces long. You can use the same regex with :


Answer URL
https://docs.python.org/3/library/re.html#regular-expression-syntax
