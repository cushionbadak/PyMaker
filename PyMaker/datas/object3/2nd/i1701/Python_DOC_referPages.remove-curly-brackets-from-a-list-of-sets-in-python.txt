Natural Text
my code looks something like this:when i want to get all values from the finals list i use: This returns however the items as a set item within a list. like this:How can i make it that the curly brackets will be omitted and that my result will look like this: ???
You can change to which will add each element of that intersection to the list, rather than the intersection itself.
You can flatten your list of sets using :
Fixing the code upstream by using  is the way to go here.If you end up with a listwhere you can't modify how it's created, you can use iterable unpacking like this:
You can use a list comprehension:


Answer URL
https://docs.python.org/3/tutorial/datastructures.html#list-comprehensions
