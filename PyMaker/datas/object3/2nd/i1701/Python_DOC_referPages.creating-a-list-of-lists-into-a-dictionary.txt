Natural Text
This question already has an answer here:Creating a dictionary with list of lists in Python                    3 answers                I'm trying to find a code that transforms a list of lists into a dictionary. Lets say I have a list that is The dictionary should have keys to number each list in dictionaries in this formatCan such a conversion be done in list comprehension or through a for loop? 
a simple dict-comprehension using enumerate:results in
In your use case, the keys are the first list in your list, and the objects are the rest. You can zip them together and create that dictionary like so: will take the element-by-element pairs and create tuples like  and  will make  from that tuple. The docs can be found here
If you are okay with third party library pandas
The following should do the trick:


Answer URL
https://docs.python.org/3/library/functions.html#zip
