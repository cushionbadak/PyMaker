Natural Text
I'm experimenting with type annotations in Python. Most cases are pretty clear, except for those functions that take another function as parameter.Consider the following example:The output simply prints .How should I annotate the  parameter in ? ANSWERThank you @Alex for providing the answer. The  module offers the  annotation (see: python docs). For my example, that gives:As you can see, the  annotation gets also annotated itself, following this scheme:
You can use the  module for  annotations.The  annotation is supplied a list of argument types and a return type:
Shouldn't it just be ?


Answer URL
https://docs.python.org/3/library/typing.html#callable
https://docs.python.org/3/library/typing.html#callable
