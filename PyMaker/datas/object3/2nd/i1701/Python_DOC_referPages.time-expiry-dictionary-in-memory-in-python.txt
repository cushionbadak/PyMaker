Natural Text
I was just wondering how one might efficiently implement a time expiry dictionary in memory in Python such that key-value pairs expire after a specified time interval.
The design pattern to typically do this is not via a dictionary, but via a function or method decorator. The dictionary is managed behind the scenes by the cache.This answer uses the  decorator in  with Python 3.7. It works a lot like , but with a time to live. As for its implementation logic, consider its source code.If however you insist on using a dictionary,  also has .


Answer URL
https://docs.python.org/3/library/functools.html#functools.lru_cache
