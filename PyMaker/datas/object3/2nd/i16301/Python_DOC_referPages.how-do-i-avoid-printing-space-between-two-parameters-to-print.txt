Natural Text
I haveHow do I get the output (notice the lack of space between  & )Without manipulating the strings prior to the 
There are two choices.First, you can use the  keyword argument to the  function:Or, better, you can use string formatting instead of a multi-argument  call:The second one is a lot more flexible, and more readable for any but the simplest cases. But either one works.

or
Another way you could do it is:When you do:You are making it so it does not skip to the next like after finish that print statement.


Answer URL
https://docs.python.org/3/library/functions.html#print
https://docs.python.org/3/library/stdtypes.html#str.format
https://docs.python.org/3/library/functions.html#print
