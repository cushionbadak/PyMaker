Natural Text
Let's say I haveAs a result I wantGive an id to the first item of every sub-list (i.e ) and then continue with the second item of every sub-list (i.e )I've tried:result:Which is "close" but I want the id to start from  and go on.I know what  returns and I don't need that.
Use  and ; transpose the sublists with the  function first, transpose back again after assigning numbers:The  stores a new  value each time a key has not yet been seen, producing a unique counter per string.Demo:


Answer URL
https://docs.python.org/3/library/collections.html#collections.defaultdict
https://docs.python.org/3/library/itertools.html#itertools.count
https://docs.python.org/3/library/functions.html#zip
