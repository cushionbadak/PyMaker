Natural Text
I am plotting in Python using Geany, but when I try to label my axis with say  i.e. the greek letter xi, I get theinvalid \x escapeerror. This does not happen for any other greek letter. Any idea how to resolve this?
In Python strings,  indicates a hexadecimally escaped character, such as , which is the same as , because the ASCII code of  is  in hexadecimal. You have to escape the backslash into  (your resulting string would look like ) or use raw strings () where the escapes are (mostly) ignored.The official docs provide a list of escape sequences.
You could use a raw string:  which will not treat the  as an escape for a  unicode char.Or, you could escape the slash: It doesn't happen with other greek letters you've tried because they don't happen to start with a character used as a control code. But if you tried "\rho" I imagine you might be surprised, as "\r" is a carriage return code.There are various such codes, including 


Answer URL
https://docs.python.org/3/reference/lexical_analysis.html#string-and-bytes-literals
