Natural Text
From the numpy documentation on logarithms, I have found functions to take the logarithm with base e, 2, and 10:However, how do I take the logarithm with base n (e.g. 42) in numpy?
To get the logarithm with a custom base using :To get the logarithm with a custom base using :As you would expect, note that the default case of .As a reminder, the logarithm base change rule is:


Answer URL
https://docs.python.org/3/library/math.html#math.log
