Natural Text
I'm using Python 3 to get the response from Instagram API using the code belowthe  method gets the response from the Instagram API URL, when I print the object it looks like a legit JSON object but when I do the  method on it I get the following error The same code works on Python 2 , I tried to search for it Python docs for Python3 but no luck. Can anyone help me out with this?Thanks a lot !
If the  is a  then you should use  instead of . If  is a string then it would have raised the same exception in Python 2 so there may be more going on.Python JSON documentationIf it is a response generated by  then the top answer to this question may be relevant.


Answer URL
https://docs.python.org/3/library/json.html#json.loads
