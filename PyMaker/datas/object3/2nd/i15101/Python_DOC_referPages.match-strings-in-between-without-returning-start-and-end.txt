Natural Text
I have following style of string: I want to match elements that are in between  and but after match I don't want  and  to be in the result: above expression will return: but how do I get regex to return I was thinking strip the results after matching but is there a better way? 
You must be doing , which returns the entire match.  You should be accessing capture group  instead.  This will return only the text matched by :Below is an excerpt from the documentation:Returns one or more subgroups of the match...If a   argument is zero, the corresponding return value is the entire  matching string; if it is in the inclusive range , it is the string matching the corresponding parenthesized group.
In addition to iCodez's answer, you could also use lookarounds to handle the boundaries.
This regex:DEMOwill match between the anchors without including them.Python demo:You can also use named capture groups to make your intent more clear:Or,


Answer URL
https://docs.python.org/3/library/re.html#re.match.group
