Natural Text
Consider the following two modules:andIs the output of the following program well-defined and the same across all Python 3 implementations?
According to the documentation for the builtin function  (which is called behind the scenes for each  statement):... the statement  results inHere, the  module is returned from . From this object, the names to import are retrieved and assigned to their respective names.While this explanation refers to importing specific names, the mechanism is clear: first, the module is imported, and then, the relevant attributes of that module are assigned to names in the local namespace.So, yes, the behaviour is well-defined: any names assigned as a result of  which conflict with names assigned as a result of a subsequent  are overwritten, just as they would be in the case of ordinary reassignment along the lines ofNote that this remains true even if you mess about with the import machinery by replacing  with a custom-defined import function:


Answer URL
https://docs.python.org/3/library/functions.html#__import__
