Natural Text
So I'm trying to read a list of IP addresses from a .txt into a subprocess (Nmap) in Python.  It's also worth asking if the problem could be the use of quotes or not.  Here's the code:Trusty error message:
 returns a list; you should catenate this list with 1 element list containing  when building command line arguments:The error occurs because instead of you are executing something likeand  expects to be given a list of strings, not nested lists.


Answer URL
https://docs.python.org/3/library/shlex.html#shlex.split
