Natural Text
I have a class which is essentially used to define common constants for other classes.  It looks something like the following:And I want to get all of the constant values "pythonically".  If I used  I get those values (, etc.) but I get other things such as:Which I don't want.I want to be able to grab all the values because this code will be changed later and I want other places to know about those changes.
You'll have to filter those out explicitly by filtering on names: This produces a list of values for any name not starting with an underscore:For enumerations like these, you'd be better of using the  backport of the new  library added to Python 3.4:


Answer URL
https://docs.python.org/3/library/enum.html
