Natural Text
It might be my mistake, but  will always get a newline between the two args... like thiswhat should i do to put them in one line ( need to be passed to system shell)?
 contains . Use strip()


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.strip
