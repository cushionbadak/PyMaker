Natural Text
I tried using:all help would be great thanks :)
The simplest way would be with  and . You could also use the  statement with an empty  in it if don't plan on using the file and automatically want it closed:This is equivalent to:On consecutive calls, this generates filenames such as:

 takes two arguments: a lower and upper (inclusive) bound for the generated number. Your code will generate a number between 1 and 1000 (inclusive), which could be anywhere from 1 to 4 digits.This will generate a number between 1 and 9999999999:You will then want to pad it with zero's and make it a string, in case it is less than 10 digits:You can then open this and write to it:


Answer URL
https://docs.python.org/3/library/string.html#string.digits
https://docs.python.org/3/library/random.html#random.sample
https://docs.python.org/3/reference/compound_stmts.html#with
https://docs.python.org/3/reference/simple_stmts.html#grammar-token-pass_stmt
