Natural Text
I'm looking to unpack from a buffer a string and its length.For example to obtain  from this buffer :Does someone know how to do ?
The length data looks like a big-endian unsigned 16 bit integer, and the string data looks like it's using the Latin1 encoding. If that's correct, you can extract it like this:outputAnother possibility for the encoding is the old Windows code page 1252, which can be decoded using .The above code works in both Python 2 & Python 3. But in Python 3 there's an easier way: we don't need , we can use the  method.


Answer URL
https://docs.python.org/3/library/stdtypes.html#int.from_bytes
