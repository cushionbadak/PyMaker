Natural Text
I have this in built filter function for my homework.We are supposed to convert a give function to one that has only one return line using the designed filter function (which overwrites the more powerful python version). The code that we are supposed to convert is this: my question is: how am i supposed to convert the predicate for my filter function by adapting from the given lookup_bus_Stop_by_road function? But I have always got the TypeError: bool object not callable. This is the return line i have put in: what is wrong here?
is a boolean value, not a function.  What you want to do is create a function that calls  and checks if the result is equal to For more reading on this topic see here: https://docs.python.org/3/tutorial/controlflow.html#lambda-expressions


Answer URL
https://docs.python.org/3/tutorial/controlflow.html#lambda-expressions
