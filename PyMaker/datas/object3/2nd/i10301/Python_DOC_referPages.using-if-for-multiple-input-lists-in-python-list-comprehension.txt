Natural Text
How do you use an if statement in a list comprehension when there are multiple input lists.  Here is the code that I'm using and the error that I'm getting: (I get that it's not able to apply modulus to a list, but not sure how to specifically reference the x in each of the lists as it iterates through them)
This isn't a cause with the  statement, the issue here is with . When that executes,  takes on a  value (first , then ) and then Python will try try to execute your  condition on it, an expression of the form:is done, which obviously isn't allowed.Instead, use  from  to chain both lists together and make  take values from them:If you're using Python  you could also unpack in the list literal :
As Jim said, you are  a list to a .You can also use , e.g., .


Answer URL
https://docs.python.org/3/library/itertools.html#itertools.chain
https://docs.python.org/3/library/itertools.html
