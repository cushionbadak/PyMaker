Natural Text
Write a class and implement a list using embedded python list.Input like : 4 9 3 5Output should be like: 3 4 5 9I use this code for taking the input values and split it to the listHow can i build up a class for this listPQ that takes the lists values and put, get and check if the list is empty?To try if your queue works:
You could use the heapq module from the python standard library. Then it is even possible without a class.Without class:the output would be (space instead of newline):If you need a class you can do it as follows:
Now you can go ahead and use  and this will print out the list as it's written in the  method.Hope this helps now!


Answer URL
