Natural Text
I'm taking this online Python course and trying to solve the following problem called Coding Exercise: It's Natural:Write a function naturalNumbers which takes a positive integer n as input, and returns a list [1, 2, ...] consisting of the first n natural numbers.Do I even need a for loop to create a list? Here's my code (which doesn't work obviously). Keep in mind, they have not taught list comprehension. I found this concept on stackoverflow.Should I take another approach where I create multiple lists of 1,2,3...n and concatenate them all together like [1] + [2] + [3]....
Do I even need a for loop to create a list?No, you can (and in general circumstances should) use the built-in function :i.e.Python 3's  is slightly different in that it returns a  object and not a list, so if you're using 3.x wrap it all in : .
Here are a few ways to create a list with N of continuous natural numbers starting from 1. 1 range:2 List Comprehensions:You may want to look into the method xrange and the concepts of generators, those are fun in python. Good luck with your Learning!
There are two problems with your attempt.First, you've used  instead of , so you're going to return something like  instead of .Second, you can't -loop over a number like , you need to loop over some kind of sequence, like . So:But if you already have the  function, you don't need this at all; you can just , as arshaji showed.So, how would you build this yourself? You don't have a sequence to loop over, so instead of , you have to build it yourself with :Of course in real-life code, you should always use  with a , instead of doing things manually. In fact, even for this exercise, it might be better to write your own  function first, just to use it for . (It's already pretty close.)There is one more option, if you want to get clever.If you have a list, you can slice it. For example, the first 5 elements of  are . So, if you had an infinitely-long list starting with , that would be easy. Unfortunately, you can't have an infinitely-long list… but you can have an iterator that simulates one very easily, either by using  or by writing your own 2-liner equivalent. And, while you can't slice an iterator, you can do the equivalent with . So:


Answer URL
