Natural Text
I want to transform my list  into a nested list .The following works:but this does not work with Python 3 (it does work with Python 2.7!):Why is that?Is there a more concise way then  to achieve what I want?
Python 3's  returns a  object, which you need to convert to list explicitly:Though in most cases you should prefer list comprehensions to  calls:


Answer URL
https://docs.python.org/3/library/functions.html#map
