Natural Text
I have this simple py script that makes an xml file and saves, it and was wondering if there was a simple way to indent it?I looked at some other SO Q&A's Pretty printing XML in Python but these seem to mostly require other external libs? and was wondering if there is a way to not use those?Thanks for the help.
You could use the standard library's minidom module's  method:yieldsOr, if you prefer the  methods for creating XML and don't mindbeing a bit inefficient, you could use  to write the unformatted XMLto a StringIO (for Python2) or ByteIO (for Python3), parse that into a minidomDocument, and then write it back out again using :


Answer URL
https://docs.python.org/3/library/io.html#io.BytesIO
