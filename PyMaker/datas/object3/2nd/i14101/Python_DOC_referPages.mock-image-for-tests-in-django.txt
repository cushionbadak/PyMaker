Natural Text
I want to use mock-image for creating an object in Django.tests.py:But I have an error:I use spec-argument, how it described in documentation.Why it does not work and how to fix that?Thanks!UPDATEIf I use this in setUp():And then run test, nothing happens:So, no errors and no running tests. UPDATE 1:In setUP():And it generates new error:Traceback:UPDATE 2:models.py:
Have you tried using ? I doubt it will help, though. The problem is, that  requires your mock object returns useful values when it invokes methods like  on your  instance. However, mocks only return mocks in the first place and you'd have to configure the mock's return values and attributes so that  can work with it.Depending on what you do with the image file in the database, I would assume a simple  instance with arbitrary data might be good enough for providing a stub for an image file.UpdateYour  requires a  instance. So you need to pass the  buffer to  and you probably have to save the image into the buffer, I'm not sure if the  would work:


Answer URL
https://docs.python.org/3/library/unittest.mock.html#the-mock-class
