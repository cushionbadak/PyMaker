Natural Text
Suppose we have a sentence like: , and a list of marks like: . What I intends to do is split the source string into segments by using the list of marks: if a character has index  in the source string, and by using this index we could get  in the list of marks, then this character is the end of a word.So the source string could be split into How to achieve this in Python? I mean, not the simple way like using a temporary buffer.
A simple approach would be:
 is probably the fastest way to find the s. Also allows you to slice the source string directly
We can use simple  loop to acheive this -
A simple answer:
Another variation on a theme.


Answer URL
https://docs.python.org/3/library/itertools.html#itertools.groupby
