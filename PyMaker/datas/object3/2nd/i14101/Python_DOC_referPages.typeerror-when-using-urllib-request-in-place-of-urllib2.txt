Natural Text
I am trying to convert code that was previously written for  to code that will work in . The code is below and I had to change  to . However, this change resulted in the error  in the line . 
You should decode the bytes before passing it to the write functionAlso see the docs.  may be omitted because it's the default, but explicit is better than implicit ;-)
Append a call to  to decode the  into a . 


Answer URL
https://docs.python.org/3/library/stdtypes.html#bytes.decode
