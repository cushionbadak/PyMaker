Natural Text
Am trying to a run this piece of code, and it keeps giving an error saying "String argument without an encoding"Any help?
You are passing in a string object to a :You'll need to supply an encoding argument (second argument) so that it can be encoded to bytes.For example, you could encode it to UTF-8:From the  documentation:The optional source parameter can be used to initialize the array in a few different ways:If it is a string, you must also give the encoding (and optionally, errors) parameters;  then converts the string to bytes using .


Answer URL
https://docs.python.org/3/library/functions.html#bytearray
