Natural Text
I have a dataframe  which contains the information Swiss municipalities. I want to replace the letter with accents with normal letter.This is what I am doing:but I get the following error:data looks like:I found the solution
This is one way. You can convert to byte literal first before decoding to utf-8. uses  module. As per the docs:The normal form KD (NFKD) will apply the compatibility decomposition,  i.e. replace all compatibility characters with their equivalents.
Try  unidecode module.Ex: Or:Note: The function is from this linkUpdate as per comment


Answer URL
https://docs.python.org/3/library/unicodedata.html
https://docs.python.org/3/library/unicodedata.html#unicodedata.normalize
