Natural Text
My text file named test.txt is like below.From another file code.py, How can I read the each line as a list with only values?mean I want to get the values in a mylist likeI tried doing below but It is not working.It gives output likeBut, I want output like this.I want each line of input to be in a sub-list of its own.
You can use a nested list comprehension to retain the structure of the input:OutputHowever, instead of a list comprehension, you can create a temporary list before your inner  loop to store the result of grabbing the first character:Edit: to remove the brackets, you can use additional unpacking:Output:


Answer URL
https://docs.python.org/3/library/json.html
