Natural Text
I'm suppose remove all the vowels from the any string that would be entered. I'm trying to make the code as simple as possible.Thank you for the help.
Two problems: is the wrong method; it only removes from the beginning and end of a string. Use .Strings are immutable; a method cannot modify a string.  returns the modified string.
So you call strip on each character.... and then what? You don't update the string, because strings are immutable and  is not an inplace operation. A naive improvement over your solution would filtering characters out inside a list comprehension and then doing a  on the result:A small note: if your string contains mixed case, you may want to eitherLowercase , orAugment  to contain uppercase vowels: , orUse  (as per @Adam Smith's comment)—augmenting  is no longer needed in that case: You can get even better with  (this works on python-3.x):
This code worked too! The dude who wrote the anwser deleted their post. Shame it worked really well. 
 is not an in-place operation and takes a string as an argument. Therefore, your code will not work as desired.You can use  to efficiently remove certain characters from your string. You may see an order of magnitude better performance for larger strings.Character removal can be performed with  directly, without the need to use .


Answer URL
https://docs.python.org/3/library/stdtypes.html#bytearray
