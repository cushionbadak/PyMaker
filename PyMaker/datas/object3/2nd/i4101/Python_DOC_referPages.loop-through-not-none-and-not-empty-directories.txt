Natural Text
Given two directory e.g.,now I want to loop through the two directories if they are not None and not empty stringwhat I am doing now is:Any more elegant syntax for this purpose?
There is not much better way to do this but you can use something like this:or:
You can use  method:
One approach could imply using list comprehensions:But if you can modify the called functions, the better strategy will be to return a list from that functions.Example from interpreter:


Answer URL
https://docs.python.org/3/tutorial/datastructures.html#list-comprehensions
