Natural Text
I have a list of tupples like so:The ints are ages of the individuals and the strings are their names. The tupples are ordered by the individials age on purpose.I understand list indexes work here. So if I want to access the first tupple then:will access How do I access the values within the tupples?I would like to loop through the tupple and print:    "They are" AGE "years old, and their name is" NAME. So for tupples[0] it would look like:
I guesswould work just fine.Edit:If you want both age and name as variables, do something like this:
You can use sequence unpacking like:Or you can access the elements in the tuple as  and  like:Or you can use argument unpacking like:


Answer URL
https://docs.python.org/3/tutorial/datastructures.html#tuples-and-sequences
https://docs.python.org/3/tutorial/controlflow.html#unpacking-argument-lists
