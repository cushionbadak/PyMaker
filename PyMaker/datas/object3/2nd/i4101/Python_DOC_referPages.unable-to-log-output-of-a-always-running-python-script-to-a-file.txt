Natural Text
I have this script that checks and waits for some LAN connections to be established :I want to be able to see its output in the specified log file but it stays empty.
Flush the output stream after each write. You seem to be using Python 3, so you can set the  parameter for the  function:If you are using Python 2 then you can use  and write a logging function:Perhaps a better way is to use the  module. Here is a very simple example:This will append to the file. If you want to overwrite the file pass  to .Also, if you don't like the  prefix you can use  to log the message only:
I believe you have to close it after everytime you write.You can implement a print wrapper like this:Your code with wrapper should look like this:Edit: Be careful, opening file with  will delete all contents of the file and create content from scratch. If you want to append, you can use .


Answer URL
https://docs.python.org/3/library/logging.html
