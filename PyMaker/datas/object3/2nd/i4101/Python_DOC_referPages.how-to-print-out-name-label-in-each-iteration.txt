Natural Text
This question already has an answer here:How to iterate through two lists in parallel?                    8 answers                I have a function which prints out the tally of pos/neg/neu scores of text. So my output looks like:But instead of doing this for every text, I put the text into a list:For my function to print out 'a', 'b', 'c' before giving the tally, I assumed I should put the labels into a list.So, I tried something along the lines of:This just prints out the entire label after each pos/neg/neu count.I want my output to look like:What can I do to make this work?Thanks.
You should simply be able to use  to iterate through your labels and topics side by side, here is an example (just using a random  function):Output:


Answer URL
https://docs.python.org/3/library/functions.html#zip
