Natural Text
I'm new to Python and I'm trying to create sublists for list elements sharing the same base:I've tried to first isolate the base from each list element using: which gets me: but from then I don't know how to proceed to get to the desired ouput.Can someone explain to me how to do it?
Typical usecase for :The  function helps in extracting the sorting/grouping key:  == .
Another way to do this would be to split each element and create a dictionary and then construct your desired output from that dictionary, e.g.:
This is a typical usecase for itertools. But you could also consider storing the values in a dictionary:You can then access the values to  with a simple command as:


Answer URL
https://docs.python.org/3/library/itertools.html#itertools.groupby
