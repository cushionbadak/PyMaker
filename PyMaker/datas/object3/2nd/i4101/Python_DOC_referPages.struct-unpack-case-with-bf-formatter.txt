Natural Text
Can someone explain this to me? Why is unpack behaving like this (adding 4 times 0x00 instead of once in first case)?I can use  formatters but:So I do not get the rule
By default, C types are represented in the machine’s native format and byte order, and properly aligned by skipping pad bytes if necessary (according to the rules used by the C compiler).source


Answer URL
https://docs.python.org/3/library/struct.html#byte-order-size-and-alignment
