Natural Text
I'm trying to do a linear regression using . However when I run my script, i get the error message I'm using the Anaconda python 2.7 distribution which in its documentation says to have the module installed. Anaconda documentation In the python interactive interpreter, I can import the scipy module, but can't find stats. When I look up its  it says , which should have the stats module..I really can't guess why the stats is unavailable..
This error:Means what it says. There is no attribute named  in the  module.Not because no such thing exists on disk, but because no such thing has been imported—because you never even tried to import it. is a package. As the Python tutorial explains, importing a package doesn't import all of its submodules.Some packages have a  that automatically imports some or all packages.* But that would be a bad idea for , because there are a ton of them, so it would take some time to import all of them, and usually you only need one or two in a given project.So, you just need to do this:* There are also some cases like  which fake being packages but aren't, so you can use  without importing it, or cases like  that create special placeholder objects for their modules that automatically import the real modules when first accessed.
I get the same error when I import  instead of . Have you tried


Answer URL
https://docs.python.org/3/tutorial/modules.html#packages
