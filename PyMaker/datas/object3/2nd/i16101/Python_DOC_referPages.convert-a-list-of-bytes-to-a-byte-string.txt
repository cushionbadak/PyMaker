Natural Text
I have the following list of bytes:Is there a simple way for me to convert that into one long ? I guess techincally called a  (though I am not sure about that).Note, I looked here but it does not work for me. Also, is the correct term for one long  object (such as ) a  or something else?
Use :
 and  objects have the  method:


Answer URL
https://docs.python.org/3/library/stdtypes.html#bytes.join
https://docs.python.org/3/library/stdtypes.html#bytes.join
