Natural Text
I'm using the library turtle.It has the onkey command, as specified:However, I need to pass an argument. Was there any way to do this?My code:menuInitial.pymenu.pyAs can be seen, the "MenuInitial" class is heir to the "Menu". I'm learning object orientation.
It looks like you should just do this:Because you're passing  as a bound instance method () to  instead of as an unbound method (),  will automatically be passed as the first argument. You can do this because  is a child of , so they share the same internal state.If for some reason you did need to pass another argument to , you could use :Then you could have this:


Answer URL
https://docs.python.org/3/library/turtle.html?highlight=logo#turtle.onkey
