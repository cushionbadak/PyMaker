Natural Text
This question already has an answer here:Re.match() returns always none                    1 answer                In the following example shows weird problem: Python pattern does not work.It would print out:What did I miss here?
You need to use  instead, and  has no special meaning so you do not need to escape it.
 tries to match the pattern at the beginning of the string. Use  instead. Also,  doesn't have any special meaning in Python's regex, so you don't need to escape it.Working code:


Answer URL
https://docs.python.org/3/library/re.html#re.match
https://docs.python.org/3/library/re.html#re.search
