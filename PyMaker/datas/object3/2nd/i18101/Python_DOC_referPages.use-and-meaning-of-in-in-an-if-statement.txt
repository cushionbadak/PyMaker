Natural Text
In an example from Zed Shaw's Learn Python the Hard Way, one of the exercises displays the following code:I'm having a hard time understanding the meaning of  in this statement. I'm used to using  statements, such as in javascript, where the syntax is something like:Is this  /  statement (in python) the same as  in javascript?Finding an answer to this has been tricky because the  is such a short string to narrow down an answer via search engine, and I don't know the proper name for its operation.
It depends on what  is.If it's a string (as in your example), then  checks for substrings.If it's a different kind of iterable (list, tuple, set, dictionary...), then  checks for membership.In a dictionary, membership is seen as "being one of the keys":
Using  is simply translates to , which should return if b includes a or not.But, your example looks a little weird, it takes an input from user and assigns its integer value to  variable if the input contains  or .
Since you claim to be used to JavaScript:The Python  operator is similar to the JavaScript  operator.Here's some JavaScript:And the equivalent Python:With objects/dicts, they're nearly identical, both checking whether  is a key of the object/dict. The big difference, of course, is that JavaScript is sloppily-typed, so  would be just as true.With arrays/lists, they're very different. A JS array is an object, and its indexes are the keys, so  will be . A Python list is completely different from a dict, and its  operator checks the values, not the indexes, which tends to be more useful. And Python extends this behavior to all iterables.With strings, they're even more different. A JS string isn't an object, so you will get a . But a Python  or  will check whether the other operand is a substring. (This means  is illegal, because  can't be a substring of anything, but  is true.)With objects as objects, in JS there is of course no distinction, but in Python, objects are instances of classes, not dicts. So, in JS,  will be true for an object if it has a member or method named , but in Python, it's up to your class what it means—Python will call , then, if that fails, it tries to use your object as an utterable (by calling its , and, if that fails, by trying to index it with integers starting from ).Also, note that JS's , because it's actually checking for object membership, does the usual JS method-resolution-order search, while Python's , because it's checking for keys of a dict, members of a sequence, etc., does no such thing. So, technically, it's probably a bit closer to the  method than the  operator.
Maybe these examples will help illustrate what  does. It basically translate to 
You are used to using the javascript , and I assume you know how it works. is a Pythonic way of implementing iteration. It's supposed to be easier for non-programmatic thinkers to adopt, but that can sometimes make it harder for programmatic thinkers, ironically.When you say , you are literally saying:"if  is in ", which assumes that  has an index. In that  statement then, each object at each index in  is checked against the condition.Similarly,iterates through 's in , where  is that indexable set of items.Think of the  situation this way (pseudo-code):It takes care of the iteration over  for you.Happy coding!
Here  is , so if you wanted to check,  then you should convert next to double, ie  and do as you would do :, but in most cases
the reserved word "in" is used to look inside an object that can be iterated over.Output:However will throwIn your case, raw_input("> ") returns iterable object or it will throw TypeError
This may be a very late answer. in operator checks for memberships. That is, it checks if its left operand is a member of its right operand.  In this case, raw_input() returns an str object of what is supplied by the user at the standard input.  So, the if condition checks whether the input contains substrings "0" or "1". Considering the typecasting (int()) in the following line, the if condition essentially checks if the input contains digits 0 or 1.


Answer URL
