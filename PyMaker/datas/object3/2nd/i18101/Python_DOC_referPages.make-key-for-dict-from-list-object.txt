Natural Text
I have list:I know for certain that  is a dict such that  is valid.How can I transform list  as a key for dict  so as to return ? 
You can try using  with :In Python 3.x, you should use . simply takes a 2-argument function and applies it to the elements of a list consecutively, accumulating the result. There is also an optional initializer argument, which we have used here. As the docs state,  is roughly equivalent to:In our case, we are passing an  so it will not be . Hence what we have is:Our  in this case is  (see link above) which simply returns the . Moreover, our  is  and our  is . So the  call above is equivalent to:
After this code finishes temp_d will contain your value
This is one of the few times when  might be a good idea - what it does is successively apply the same operation on a value.This is equivalent to:


Answer URL
