Natural Text
I'm on Mac OS X using Python 2.7; using  with  fails yet running the same command at the shell succeeds. Here's a copy of my terminal:I've also tried with full paths and had the same result.
Because running a command in the shell is not the same thing as running it with ; the shell expanded the  wildcard.Either expand the list of files with  or the  module yourself, or run the command through the shell from Python; with the  argument to  (but make the first argument a whitespace-separated string).Using  is probably the best option here:
Martijn's advice to use  is good for general shell wildcards, but in this case it looks as if you want to add all files in a directory to the ZIP archive. If that's right, you might be able to use the  option to :
Try shell=True. subprocess.call('zip example.zip example/*', shell=True) would work. 


Answer URL
