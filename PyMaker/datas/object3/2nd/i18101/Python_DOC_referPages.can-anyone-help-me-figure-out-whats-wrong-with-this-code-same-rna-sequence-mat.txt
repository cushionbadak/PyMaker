Natural Text
Here is what I have so far: There seems to be an error in prompting the user for two sequences.
It seems to me that instead of  you should have something like this:In this case, you don't need the function to have parameters, you can just do .If you want to use parameters, remove the line with input, keep your original  and instead of just doing this:do this:or this:(See this question for what  is doing here. Also, here are the docs for .)
You could try something like:Where a pair of sequences is formatted:


Answer URL
