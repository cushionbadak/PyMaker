Natural Text
I know you can create a simple HTTP web server in Python 3 usingHowever is there a simple way to secure the connection to the WebServer, do i need to generate certificates? How would I do this?
First, you will need a certificate - assume we have it in a file  which contains both the private and public keys, then:Make sure you specify the right parameters for !


Answer URL
https://docs.python.org/3/library/ssl.html#combined-key-and-certificate
