Natural Text
Simply return some (markdown) string in flask:If you call pandoc directly () or with , you get no problem:But if you call pandoc within flask method:Then when you access to  you get Responsetimeout error from pandoc:References: url_for in flask API
As I recall the Flask http server is single threaded so it can't handle the '/raw' request while it's still in the process of handling the '/get' request.Answer to another SO question suggests using  that may be enough for personal use. For production use you should consider a real web server like nginx or apache.Even then, assuming pandoc supports it (I've no idea) you may want to consider sending the markdown input to pandoc stdin and avoid the extra HTTP request altogether, something like (untested)


Answer URL
https://docs.python.org/3/library/io.html#io.StringIO
