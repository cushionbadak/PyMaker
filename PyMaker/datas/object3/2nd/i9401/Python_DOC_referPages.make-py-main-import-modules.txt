Natural Text
I have a C++ Program in which I call  to get into Python. Now I would like to execute some commands inside of  but cannot seem to figure out how to pass that.I saw that  calls  so I wonder whether it is possible at all.EDIT:I currently try to do:And in  have  not available. I can do  then fine.
If you need to execute simple commands from C/C++ in Python you should be using the  function which takes a  containing the command to execute. You shouldn't need to dabble with , just set the program name and initialize Python with  before you call . If you need to import a module and use it you can use .


Answer URL
https://docs.python.org/3/c-api/veryhigh.html#c.PyRun_SimpleString
https://docs.python.org/3/c-api/import.html#c.PyImport_Import
