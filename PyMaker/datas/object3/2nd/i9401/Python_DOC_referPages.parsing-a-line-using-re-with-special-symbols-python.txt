Natural Text
I am trying to parse a file like this:while this is not Python syntax, i am using python for the parsing.My code is:my result is:which is cool since I only used re with a [\s] regex, but how would I get a result like this:Where I get all symbols (lets assume I have a string that contains them one after the other, for example symbols = '(){}:,=+-') ?
You can use re.split with a group to get the split text and the split characters.For instance, a symbol can be matched with the  RegEx.Here is an example:You'll get:With a filtering, you can drop empty string…Or, if you need to match one-character symbols, you cantry:You get:
Try this:


Answer URL
https://docs.python.org/3/library/re.html#re.split
