Natural Text
In Forsyth-Edwards notation, I have a string like this:where  represents an empty space.It needs to be notated like this:where the '8' represents the number of empty spaces.For example, given a string like this:The result should be:Can anyone help me in the right direction?
The first thing that comes to mind is regular expressions. A regex that matches a sequence of spaces is just , and you can use  or the  method of a compiled pattern to replace matches (i.e. sequences of spaces) with whatever you like. You can give  a function as the replacement value, and that function will be called on each match object to compute the actual replacement.
You can use  and substitute into the string the length of the matches:
Without regex, you may achieve the same result using  with list comprehension expression as:Cleaner version of this could be written with explicit  loop as:Final value hold by  will be:


Answer URL
https://docs.python.org/3/library/re.html
https://docs.python.org/3/library/re.html#re.sub
https://docs.python.org/3/library/re.html#re.regex.sub
https://docs.python.org/3/library/re.html#match-objects
