Natural Text
Can someone explain to me why this doesn't work?I am trying to create a change machine using recursion. The first parameter is the amount of change that we need to give back, and the second parameter is an array of the number of bills with the first element representing $25, the second representing $50 and the last representing $100.When I call  it now does not return "T" or "F"instead it just prints out Here is the code:
The ampersand  in Python is the  operator.  It's not the same as the boolean  operator. When you have a statement likeThat's actually being read as , because  binds more tightly than .  Here's a useful chart on operator precedence
I assume the condition is wrong  should be .Outcome:


Answer URL
https://docs.python.org/3/reference/expressions.html#operator-precedence
