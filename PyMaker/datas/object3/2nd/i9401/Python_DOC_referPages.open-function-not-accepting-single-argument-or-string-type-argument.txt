Natural Text
I'm learning Python and I'm trying to just simply read file into my program but the  function is not accepting  or any sting as argument, I don't know how it is possible.
 isn't the function you should be using. Just call  directly which uses the built-in. From the docs on :Note: This function is intended for low-level I/O. For normal usage, use the built-in function , which returns a file object with  and  methods (and many more). To wrap a file descriptor in a file object, use .
just like Jim said : try below if you want to read the file line by line then use a loop :


Answer URL
https://docs.python.org/3/library/os.html#os.open
