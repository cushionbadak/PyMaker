Natural Text
I was writing a wrapper class for sockets so I could use it as a file-like object for piping into the  and  of a process created with .I didn't use  as it gives a  error, but with my present code I'm getting the following error on Windows (works fine on Linux):
According to the Python documentation about , it is stated that this won't work in Windows. Quoting from Python Documentation:Return the socket’s file descriptor (a small integer). This is useful with select.select().Under Windows the small integer returned by this method cannot be used where a file descriptor can be used (such as ). Unix  does not have this limitation.Note:The above code will work in Linux and other *nix systems.


Answer URL
https://docs.python.org/3/library/socket.html?highlight=socket#module-socket
