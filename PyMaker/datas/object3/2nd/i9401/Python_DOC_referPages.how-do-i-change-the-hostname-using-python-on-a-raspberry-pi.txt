Natural Text
I tried using (going from memory, this may not be 100% accurate):I got a permissions error. How would I approach this entirely from within the Python program?
I wanted to change the hostname permanently, which required making changes in a few places, so I made a shell script:In Python, I ran the script with subprocess.run:This was happening from a webserver which was running as , so I allowed it to  this specific script without a password.  You can skip this step and run the script without  if you're running as  or similar:
If you only need to do change the hostname until the next reboot, many linux system can change it with:or with less typing but some potential pitfalls:


Answer URL
https://docs.python.org/3/library/subprocess.html#subprocess.run
