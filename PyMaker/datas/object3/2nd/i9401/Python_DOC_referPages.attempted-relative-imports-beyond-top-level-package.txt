Natural Text
I have a directory structure like the following:and my  reads , but every time I run it it says .How can I fix this?
Do you need a relative import? Can you use ?Though, since you haven't shown the exact stacktrace, the error could be coming from . An attempted "import beyond top-level package" is self-explanatory, though. PEP 328 could be worth a read
I'm guessing that you are trying the following, or something similar: But this is treating render.py as a script and not a component of a package.  If you write an external script, where you, and then test  functions, the imports should work fine.You may want to have a look at Hitchhiker's Guide to Python


Answer URL
https://docs.python.org/3/tutorial/
