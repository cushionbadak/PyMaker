Natural Text
I have a string like this:I tried doing like this:This process works fine, but i want to do only in one operation, rather than doing in two steps(splitting and matching)So i will create a list which will contain dates:
With , you can achieve that like below:If you want only the second date in each line, try:Output:
Try to split by \n first, then you can iterate by line and with a reverse substring get the date and with .append() function get the desired list


Answer URL
https://docs.python.org/3/library/re.html#re.findall
