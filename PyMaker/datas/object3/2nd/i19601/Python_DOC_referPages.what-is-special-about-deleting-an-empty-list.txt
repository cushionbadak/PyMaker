Natural Text
Consider the following...What is special about ? I would expect this to raise a  too. Why doesn't it? I've observed this behavior in Python2 and Python3.
The  statement syntax allows for a , and that includes a list or tuple of variable names.It is intended for deleting several names at once:which is the equivalent of:orBut python does not enforce the list to actually have any elements.The expressionon the other hand is a syntax error;  is seen as a literal empty tuple in that case.


Answer URL
