Natural Text
I notice the following at the top of Twisted's  script:What does  do? I can't seem to find any references to it on the google-mage. 
 is a module like any other. In twisted's case, this is simply the module that sets up  so that you can run twisted in development setups.


Answer URL
