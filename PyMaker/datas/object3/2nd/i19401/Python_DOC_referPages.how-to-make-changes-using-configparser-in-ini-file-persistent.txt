Natural Text
How to modify the .ini file?  My ini file looks like this. And i want the format section ini to be changed like this[Space to be replaced with a tab followed by $]Format="[%TimeStamp%]   $(%ThreadID%)   $<%Tag%>    $%_%"This is what i wroteAnd the Output is the exact what i wanted.But when i open the .ini file, i see no changes here?Could be when i reading again the section , its loading from memory? How did i make the changes permanent 
Try using the  method.When you read in a config with  you have "saved" the file exactly as it is.  Now what you want to do is modify the contents.


Answer URL
