Natural Text
This question already has an answer here:Python ( or general programming ). Why use <> instead of != and are there risks?                    3 answers                I was reading the Python docs earlier, and I discovered . How exactly does this differ from ? As far as I can tell, the only difference is that  won't work with strings. Does  do something special that  doesn't, or is there a reason I hadn't heard about it for over two years?
 is a remnant of the past. Quoting the docsAlso, worth noting that Python 3 doesn't support .
 exactly the same thing as , for strings or any other type:However,  is deprecated. It has been removed from Python 3. Always use  instead.
Operator  is simply the deprecated verision of .See from the doc.


Answer URL
