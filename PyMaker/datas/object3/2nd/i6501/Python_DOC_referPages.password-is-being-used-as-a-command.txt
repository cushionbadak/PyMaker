Natural Text
I'm trying to run a command from a python file: But the  and even  keep getting hanged up on my password stringThe password is similar to this structure:the command line error:
As already mentioned in the comments, don't use . See https://docs.python.org/3/library/subprocess.html#security-considerations.Pass the list of arguments directly to the  constructor, instead of letting the shell do the splitting.The issue with  is better explained in the older version of the documentation: https://docs.python.org/2/library/subprocess.html#frequently-used-arguments
You need to quote the password to protect shell metacharacters (such as ) from being treated specially by the shell, e.g.:However, this won't work if the password itself can contain quotes. A better alternative would be to pass the list of arguments to  and do the redirection yourself:


Answer URL
https://docs.python.org/3/library/subprocess.html#security-considerations
