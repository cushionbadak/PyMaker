Natural Text
I have a Pandas Dataframe as such,I use a dictionary to map values into a new column called continent using the following code I found in an similar answer,being ,All the new items in column  are NaNs, can anyone help me here?
You wantWhat you tried:generates  because you're constructing a  from the numpy values so the index returned is the auto-generated  so when it tries to align against your df, it fails so all rows become 
For completeness:  definitely has a  method.  However, as of pandas 0.20.3, it only takes a callable.  That means it does not work with a dictionary.  We can change that by passing a callable to it.  In fact, a dictionary already has the appropriate method for us.  Namely, 


Answer URL
https://docs.python.org/3/library/stdtypes.html#dict.get
