Natural Text
Why can the attribute '13579' of map be a string, shouldn't it be a list?
 takes a function and a sequence. Strings are sequences as well. You can also iterate it:map(...):   map(function, sequence[, sequence, ...]) -> list
In Python a string is a list of chars. Thats why you can loop (or use map) over a string and then work on every char separately inside the loop.You can find more infos here http://www.openbookproject.net/books/bpp4awd/ch03.html
From the :The documentation describes the   as taking a  which is applied to each  in an  :map(function, iterable, …)Apply function to every item of iterable and return a list of the results.And since   are , just like , you can pass them into  as you can . We can show that they are  by simply  over them in a :which will  out:Hopefully this helps you to see why  can be passed into .


Answer URL
https://docs.python.org/3/library/functions.html#map
