Natural Text
I'm trying to implement IPC using named pipes in Python, but there is a problem.  blocks the processSame with What am I doing wrong?Python 3.6.1
Figured this out.  blocks until the pipe is open on the other side
According to os.mkfifo, you need to use 
the function  create the path is needed (if all the folder does not exists it build all the path).so, you need to add the file name like this: Generally, FIFOs are used as rendezvous between “client” and “server” type processes: the server opens the FIFO for reading, and the client opens it for writing. Note that mkfifo() doesn’t open the FIFO — it just creates the rendezvous point


Answer URL
https://docs.python.org/3/library/os.html#os.mkfifo
