Natural Text
I tried to convert the two lists to a dictionary. But the types of  and  were still s.
The problem is the line sorted is a function that takes an iterable object (such as the keys() object that you give it) and returns a list. You are overwriting your dictDccp with this list.If you were trying to iterate through the keys of the dictionaries in sorted order you could do


Answer URL
https://docs.python.org/3/library/functions.html#sorted
