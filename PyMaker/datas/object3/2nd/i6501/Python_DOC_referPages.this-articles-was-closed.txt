Natural Text
I am using the following code:Which shows the following result:Can anyone explain what  function actually does?
The iterweekdays() method returns an iterator for the weekday numbers that will be used for one week. The first number from the iterator will be the same as the number returned by firstweekday().You have set 3 as start of the week.Now iterweekdays(), print you all days in a week starting with day 3 so you get 
As the docs stated:iterweekdays()  Return an iterator for the week day numbers that will  be used for one week. The first value from the iterator will be the  same as the value of the firstweekday property.Notice how the documentation says that the first value will be the same as defined in . In your case:  and that why it goes from 3 to 6 and then 0, 1 and 2. In computer science it is usual to begin counting from 0.It is hard to understand from your question, but in case you wish to display the names of the days you can use:And you'll get in return:


Answer URL
https://docs.python.org/3/library/calendar.html#calendar.Calendar.iterweekdays
