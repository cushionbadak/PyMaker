Natural Text
This question already has an answer here:Python: how to build a dict from plain list of keys and values                    5 answers                I have a list where the content of list are key:value pairs something like shown below I want to convert the above list into dictionary key:value pairs.Then I want to convert it into json format.
You can try this:Output:
Just use , splitting on the colons. Or the equivalent functional approach,From there  to get a JSON formatted string. 

Iterate over your list and split the pair into two to form a dictionary  gives you the list of lists:  You can put key as string and value as int, as it seems to be count. 


Answer URL
https://docs.python.org/3/library/functions.html#func-dict
https://docs.python.org/3/library/json.html#json.dumps
