Natural Text
I have the following method that generates data for me:How to correctly await  in ?
You may use a  wrapper around the list of tasks:Or, if you can't mark  method as , wait it synchronously:Also you can try to implement an  iterator and use it with  syntax, something like this:
For me it worked something like this:Enjoy!
If you don't use a loop already, try:Or in a coroutine:


Answer URL
https://docs.python.org/3/library/asyncio-task.html#asyncio.wait
