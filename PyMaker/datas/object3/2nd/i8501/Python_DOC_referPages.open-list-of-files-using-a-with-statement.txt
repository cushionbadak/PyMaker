Natural Text
I'm trying to open several files from  inputs using a  statement.I know I can do it by manually typing out each one:But is there a way to do this without doing so, like the following pseudocode:
You can do this in Python 3.3+ with :Amusingly, the example in the documentation is exactly what you want.This correctly closes any open files upon exiting the  statement - even if something went wrong before they were all openFor earlier version of python, there is a backport in the  package, which you can get through 


Answer URL
https://docs.python.org/3/library/contextlib.html#contextlib.ExitStack
