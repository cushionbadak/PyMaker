Natural Text
How to perform element-wise addition of different length lists?Assuming "0" for missing elements.Note: len(a) will always be less than or equal to len(b)example:Desired result:I can almost, but not quite do this with:Is it possible to automatically append '0' elements to (list a) so it is the same length as (list b), prior to doing the zip? If that is a good pythonic solution, how would one do that?
There's an alternative  that does not stop at the shortest: . You can specify a fill value for the shorter lists:
You can use :Then you can do:
you can pad a with zeroes like this and use sumresults in


Answer URL
https://docs.python.org/3/library/itertools.html#itertools.zip_longest
