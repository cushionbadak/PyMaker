Natural Text
I have a to generate all possible combinations of a tuple of tuplesThe  and the .Requirementsno repeatsthe bases can be the same but the positions must bedifferentthe order must be kept.For example: and should be discarded.Sample output:Should have a length of 448Example:For a string length 2:would be all the combinations... I thinkI have this so far
It sounds like you want the Cartesian product of (every possible 2-base word)  X (every 2-combination drawn from range(8)).You can get this in general bywhich gives us


Answer URL
https://docs.python.org/3/library/itertools.html
