Natural Text
I have a directory with a number of files which all have the same basic filename format (below is a sample)I am trying to select a file based on using:where roll is an int that I convert to a string and ID is a string.  I'm using the wildcard * for the _oe_date element as i don't care about this part. I however get the following error:How can I construct a filename using  wildcard in the middle?
You have to embed the wild card as a character, and make sure that you're file search interface accepts wild cards.However, note that this does not identify a specific file name: to do that, you need a system command (there are several to search and fetch file names).  The Python open function does not accept wild cards. 
I think you want the  module, and the  method:
You could use :


Answer URL
https://docs.python.org/3/library/glob.html?highlight=glob#module-glob
https://docs.python.org/3/library/string.html#formatstrings
