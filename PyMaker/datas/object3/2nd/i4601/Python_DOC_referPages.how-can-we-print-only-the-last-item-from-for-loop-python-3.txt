Natural Text
I want to print only the last date in the form 2075-01-30 and Not as 2075-01-30 00:00:00
Unindent the print statement in the last line 
To not print the date every time, you just need to move the  outside the loop by unindenting it, as Stephen Rauch explained.If you want print just the date instead of the date and time, you probably actually want to use a  object instead of a  object:If there's some reason you actually want to use a  for your intermediate calculations (which may have implications for weird cases like daylight saving transitions, which maybe you've thought through and want?), you can instead convert to  only at the end, or print any format you want with :But if not, it's simpler to just use .
That can be done simply as:Result:


Answer URL
https://docs.python.org/3/library/datetime.html#date-objects
https://docs.python.org/3/library/datetime.html#strftime-strptime-behavior
