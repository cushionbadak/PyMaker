Natural Text
Perhaps this has been answered, but I sincerely could not find it.I wish to have a specific output in the form of:I tried:And this returns:
Your code makes no sense as you overwrite  each iteration, which in result kills what your code produced in previous one. You want rather to use simple list comprehension instead:which would then produce list with elements like ,  ...
Try a list comprehension to make that list of strings like:
Here is the functional approach. This will work with a list of any type, so if you decided to switch from ints at the end to strings/characters, all that would need to change would be the contents of .


Answer URL
https://docs.python.org/3/tutorial/datastructures.html#list-comprehensions
