Natural Text
Facing issues while converting date to string,My from_date value is in an ini file, My TraceBack log is,I've used type(from_date) which returns from_date as a string.Also tried,still the same error persists.Changed from_date as suggested in the answers below,The error persisits,
Replace to in ini file Tested with code :and Test.ini is output is :
As per docs, The  method accepts a  as the first argument. So, I tried this:If I do , I get an  error for the  of :This means that  must be a string. You could use  and that would work. If I check the type of  now, I will get a type of class : But when I look at the first Traceback log, it raises a . This means that the value stored in  is of type . Consider the following: 


Answer URL
https://docs.python.org/3/library/configparser.html
