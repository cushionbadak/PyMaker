Natural Text
Can we use del function in print statement like this:
 is a Python keyword. It can't be used in .Even if it would be a function and could be used, it would not return anything. So usage in  would not make much sense.
From the documentationdel is a statement, it does not return anything.
you can use del keyword to delete the slice [2:4] like this:but it is not the best way to delete a slice; And you can't use del to deletea slice in one call. 


Answer URL
https://docs.python.org/3/reference/lexical_analysis.html#keywords
https://docs.python.org/3/reference/simple_stmts.html#the-del-statement
