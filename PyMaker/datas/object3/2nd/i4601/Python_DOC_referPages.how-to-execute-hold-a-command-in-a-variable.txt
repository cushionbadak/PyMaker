Natural Text
I'm trying to see if it's possible to execute a command from a variableExample of what I'm going for:And then be able to print that by writing the variable name after this.Example:but when I try the output is:I know you could do this by just writing the thing you want to print in the variable, but I would like to use this for other commands.
For this you define functions or in this short case you can wrap it into a lambda-function:You call it with the . The  would be that what you called . With a function you can execute more commands:
You can use functools.partial to call any already defined function with some fixed arguments.


Answer URL
https://docs.python.org/3/library/functools.html#functools.partial
