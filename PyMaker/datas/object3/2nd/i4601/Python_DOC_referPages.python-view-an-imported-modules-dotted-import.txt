Natural Text
I want to get all the modules imported from an arbitrary module .When  imports a module in the following way:or:(where  and  are packages, and  is a module)The result of  would contain  (among other things).However, when I use:The result of  would contain , and not .I also tried iterating over  after importing , but got the same result.How can I, having the object of  that imports  that way, know that it actually imported ?
You can use the  module to parse the module's source code and extract imports:Keep in mind that this only works if the module's source is available as plain python code.As an example, consider this module:The output for this module would beNotes:This code also catches imports in functions, not just imports in the global scope.If the code performs an import like ,  will appear in the output. However, if the code performs an import like , only  will appear in the output.


Answer URL
https://docs.python.org/3/library/ast.html
