Natural Text
I believe that re.sub() is replacing the Full Match, but in this case I only want to replace the matching groups and ignore the non-capturing groups. How can I go about this?output is :Expected output is:
The general solution for such problems is using a lambda in the replacement:However, as bro-grammer has commented, you can use backreferences in this case:


Answer URL
https://docs.python.org/3/library/re.html#re.sub
