Natural Text
I have a string and I want to match something at the start and end with a single search pattern. How can this be done? Let's say we have a string like:I want to do something like this:Obviously, it's incorrect, but I hope it gets my point across. Is this possible?
 will match the string at the beginning, in contrast to :Two things to note here: is used for the string literal to make it trivial to have backslashes inside the regex is a standard module, so I chose  as a variableIf you use a regex more than once, you can use  to built the state machine once and then use  afterwards to match the stringsIf you want, you can also use the  module to parse the URL for you (though you still need to extract the extension):
How about not using a regular expression at all?Don't you think this reads nicer?You can also support multiple options for start and end:
Don't be greedy, use 
Tryif you want a regular expression search. Note that you have to escape the period because it has a special meaning in regular expressions.

I want extract all numeric, include int and float.and it works for me.refs: https://www.tutorialspoint.com/How-to-extract-numbers-from-a-string-in-Python


Answer URL
https://docs.python.org/3/howto/regex.html#greedy-versus-non-greedy
