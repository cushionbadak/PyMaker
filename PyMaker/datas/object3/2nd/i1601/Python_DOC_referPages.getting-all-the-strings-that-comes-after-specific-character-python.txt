Natural Text
Getting strings that comes after a character which is '$' and store them as lowercase strings in a list.For example:I triedEDITIt also should end before a number or special character likestring = "apple $is a $tasty fruit and $CHEAP#chs"['is', 'tasty', 'cheap']where it shouldn't be cheap#chsCan someone help?
Use a regex:Yields:
This one gives op as . Regex is simple though.
 returns a new lowercase string and doesn't do anything to the original. One possible way to do this is to split your string by spaces, and check if a word  "$":
You can use The count() method, as follows:and the output would be:


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.lower
https://docs.python.org/3/library/stdtypes.html#str.startswith
