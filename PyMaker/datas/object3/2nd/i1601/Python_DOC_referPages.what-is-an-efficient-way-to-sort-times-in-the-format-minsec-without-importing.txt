Natural Text
I have a table of strings where the final column is the 'time' is written as below:I would like to present the largest and smallest from this list, with the restriction being I cannot import any modules (it's not my choice to). While I thought of comparing the first character of each element as an int, then if there is a tie, looking at the 2nd number to compare, it seems like there could be a better way?
You can use the  method with your own key function:which modifies  to:
The traditional method is to use an imported module, e.g.  from . However, given your restriction you can calculate minutes in each "time delta" string and then sort:


Answer URL
https://docs.python.org/3/tutorial/datastructures.html#more-on-lists
https://docs.python.org/3/library/datetime.html#timedelta-objects
