Natural Text
This program is to count the number of sub-strings in a string.Test case:    string     pattern The answer should be  but I am getting .
At the end of the  loop  can be at most  (if you don't break early). So  is always . You want  or simply  (meaning you've reached the end of the  loop) instead.Also  should be initialized at the start, not in the first  loop:
Strings are sequences of 1-character substrings in Python, so you can use the built-in  method — in other words, you don't have to re-invent the wheel and write your own function to do it.


Answer URL
https://docs.python.org/3/library/stdtypes.html#sequence-types-list-tuple-range
