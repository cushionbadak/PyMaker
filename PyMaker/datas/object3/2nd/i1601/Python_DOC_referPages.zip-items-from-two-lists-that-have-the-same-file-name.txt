Natural Text
I have two list: this:list1(has way more items)and this:list2(has way more items)How to ensure that the pairs will match with the corresponding same name on the other list after the zip?Maybe we match with their previous folder? Like:wanted final output :
I would just group the files with a , then output the pairs of length 2 in a separate list.Demo:Output:Note: Using  to extract the filename from Windows paths will only work on Windows. It will simply do nothing on Unix enviorments.  


Answer URL
https://docs.python.org/3/library/collections.html#collections.defaultdict
https://docs.python.org/3/library/os.path.html#os.path.basename
