Natural Text
Hey I am using easygui and appending the user input to a excel (csv file). However the userinput will continuously append to the same line, and not the next line. Here is my code :
You could use the csv library, defining a  object to write to your file. So you would need to replace the else statement with something like that:The  method will automatically put your new data into a new row. Also you don't need to explicitly convert your data to string.


Answer URL
https://docs.python.org/3/library/csv.html#writer-objects
