Natural Text
I'm having a problem sorting list in Python here's my code:Error is:
The  method sorts the list in-place; it always returns .What you can do:Or, as @Delgan pointed out, you can use the  function, which returns the sorted list:
 doesn't return anything.  If you want to modify the list to be sorted you can door if you want to keep the list in the original order you can use  which returns a new list of the sorted elements:
 only change the list but return None, you need: Or simply use : return a new list but sorted, note that it doesn't change the current list.


Answer URL
https://docs.python.org/3/library/stdtypes.html#list.sort
https://docs.python.org/3/library/functions.html#sorted
