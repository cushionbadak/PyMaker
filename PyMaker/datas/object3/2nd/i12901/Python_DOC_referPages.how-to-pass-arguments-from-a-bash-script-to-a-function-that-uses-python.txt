Natural Text
In bash I want to pass func2 an argument. In func2 I want to print the argument using python. Desired output:I have chickensI have 100 chickens
Regular bash string variable substitution works:Output:Because of this, you have to be very careful if you wish to continue your (rather unusual) approach. A much more common way would be to have the Bash and Python programs in separate files and run the Python script from the Bash script with some command line arguments. You can then access them from Python as , or, if you want to do anything more sophisticated, argparse.


Answer URL
https://docs.python.org/3/library/argparse.html
