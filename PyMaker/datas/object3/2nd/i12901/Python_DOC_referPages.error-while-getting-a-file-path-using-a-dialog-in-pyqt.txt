Natural Text
I am using a dialog to get a filename path. I have managed to get it running and saving it to a local variable but Qt does not return the file path written in the pythonic way.My cood is simple as that:but it returns me something like this:insteadI have tried to replace / for \ with no success. What should I do?
Qtreturns all paths using / as the separator. You can use the  method:Or the toNativeSeparators method provided by Qt's QDir:


Answer URL
