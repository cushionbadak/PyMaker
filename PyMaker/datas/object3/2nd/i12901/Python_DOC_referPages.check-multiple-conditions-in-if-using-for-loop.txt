Natural Text
I have to check for multiple 'and' conditions in ifEg:can I do above using for loop in if, say something like Above suggested won't work as it sets the condition as true even if one of them is true.
Simply use  here:From the document:Return  if all elements of the iterable are true (or if the iterable is empty).And  return a generator(it's an iterable), then  return  if all elements in that generator is , else . Like your code does.
You can do Usage of :if all the elements in iterable  are  returns  if one lement is  it return eg:  --> --> 


Answer URL
https://docs.python.org/3/library/functions.html#all
