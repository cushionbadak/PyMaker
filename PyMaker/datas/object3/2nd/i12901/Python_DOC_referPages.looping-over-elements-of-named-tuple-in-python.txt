Natural Text
I have a named tuple which I assign values to like this:Is there a way to loop over elements of named tuple? I tried doing this, but does not work:
namedtuple is a tuple so you can iterate as over normal tuple:but tuples are immutable so you cannot change the valueif you need the name of the field you can use:


Answer URL
https://docs.python.org/3/library/collections.html#collections.somenamedtuple._asdict
