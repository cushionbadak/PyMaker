Natural Text
I am new to the  python module. I try to do a project. I learned something new about the menus and I'm trying to make a little UI project that allows the user to write something in a scrolled text widget and then save it (using the  module).I've already tried some things that worked at buttons. For example  but it didn't work. I also tried the  method.The problem is, as I already said, I don't know how to get the text out of a  widget.
So, out of curiosity I tried what described here (same link in my comment to the OP question). It works also for the .I've added a save button at the bottom of the  widget. The widget content is saved inside the  area.
 indicates it's a subclass of  widget, which apparently means that the way to get the text from it is the same—using the  method and indices (here's some documentation about  widget indices).Below is an example the gets all of the text in the widget (I added a Save text  to test the  method):


Answer URL
https://docs.python.org/3/library/tkinter.scrolledtext.html]
https://docs.python.org/3/library/tkinter.scrolledtext.html
