Natural Text
input is like:In output i want these 2 lines only, want to remove table tag with data in python:Hi Team from the following Server :Please archive the following Project Areas :
Use  to parse HTMLEx:Output:
You can use  as demonstrated below:Output:
If you do not want to use external library, you can use  module to remove tables: printing output give:(and 2 empty lines which are not visible there).Regarding pattern notice that  is followed by  meaning use non-greedy matching - otherwise it would purge anything between begin of first table and end of last table.  is required, because our substrings contain newlines ()


Answer URL
https://docs.python.org/3/library/re.html
