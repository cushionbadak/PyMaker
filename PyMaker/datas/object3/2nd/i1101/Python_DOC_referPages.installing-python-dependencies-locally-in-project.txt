Natural Text
I am coming from NodeJS and learning Python and was wondering how to properly install the packages in  file locally in the project.For node, this is done by managing and installing the packages in  via . However, the convention for Python project seems to be to add packages to a directory called . When I do  I think this does a global install on my computer, similar to nodes  global install. How can I install the dependencies of my requirements.txt file in a folder called lib?
use this command
The recommended way to do this is by using a virtual environment. You can install virtualenv via pip with Then create a virtual environment in your project directory:Which will create a directory called  (you can call it anything you like though) which will mirror your global python installation. Inside  there will be a directory called  which will contain Python and will store your dependencies.Then activate the environment with:Then install your dependencies with pip and they will be installed in the virtual environment :Then any time you return to the project, run  again so that the dependencies can be found.When you deploy your program, if the deployed environment is a physical server, or a virtual machine, you can follow the same process on the production machine. If the deployment environment is one of a few serverless environments (e.g. GCP App Engine), supplying a  file will be sufficient. For some other serverless environments (e.g. AWS Lambda) the dependencies will need to be included in the root directory of the project. In that case, you should use .
I would suggest getting the Anaconda navigator. You can download it here: https://www.anaconda.comAnaconda allows you to create virtual environments through a graphical interface. You can download any pip package that is available through Anaconda. Then all you have to do after you have created and added onto your environment is to got to your designated python editor (I mainly use Pycharm) and setting the path to the virtual environment’s interpreter when you select or change the interpreter for your project. Hope this helps.


Answer URL
https://docs.python.org/3/tutorial/venv.html
