Natural Text
What is the best way to change ONLY the header separator to ":" while doing "to_csv" on a panda data frame?For Example:When I convert this to CSV, i getOutputInstead i want the output as:
It doesn't seem to support it, because the CSV writer underneath the hood treats the header like any other line.The following snippet works but loses the atomicity of .
Different delimiters for headers versus data isn't supported by Pandas . You can use the  module from the standard library instead:Result:


Answer URL
https://docs.python.org/3/library/csv.html
