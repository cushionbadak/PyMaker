Natural Text
For the last part #print(new_path), the output is "None". Can one explain me in detail the reason for the occurrence of this error. Thank you in Advance.
 operates by side-effect and always returns . If you want the current working dir after a  you must ask for it.
That is because  does not return any value. Below is the fix.
 returns  in all cases.  If you want to know which directory your program is currently in, you must call . Additionally, if you want to make sure your program switches back to the original working directory safely, you could use a contextlib.contextmanager:Which will show the following behaviour:Explanation of context manager:Which switches to the directory in the  block, does something with the changed directory(in this case just print it), then switch back to the current working directory in the  block when the context manager exits. The FileNotFoundError exception is also raised in the  block if the directory doesn't exist, and yields  from the generator. 


Answer URL
https://docs.python.org/3/library/os.html#os.chdir
https://docs.python.org/3/library/os.html#os.getcwd
https://docs.python.org/3/library/contextlib.html#contextlib.contextmanager
https://docs.python.org/3/library/exceptions.html#FileNotFoundError
