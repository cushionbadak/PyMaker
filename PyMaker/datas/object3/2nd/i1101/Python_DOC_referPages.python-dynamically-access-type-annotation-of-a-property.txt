Natural Text
Given the following class:How can I dynamically access the type annotation of  either from the class or an instance?The following does not work:
You need to get the  object itself, without doing the dynamic lookup.  The  module has a function,  for that. Then we can use  to get the signature of the  attribute of that object. That's where  objects store the function they use to do the dynamic lookup.  The other two functions of a property are stored in the attributes  and 


Answer URL
https://docs.python.org/3/library/inspect.html#inspect.getattr_static
