Natural Text
This question already has an answer here:Format numbers to strings in Python                    7 answers                I want to make a simple addition program but get stuck at an error stating the following:TypeError: can only concatenate str (not 'int') to str.I have no idea what to do, I am pretty new to Python coding.I expect the code to return the value of the sum of the two entered values.
The  operator can work in multiple contexts. In this case, the relevant use cases are: When concatenating stuff (, for example);When you want to add numbers (,  and so on). So when you use the  in a concept that uses both strings and int variables (,  and ), Python won't handle your intention correctly. In your case, in which you want to concatenate numbers in the sentence as if they were words, you'd have to convert your numbers from  format to  format. Here, see below:
The problem is when you print the output (), you are adding strings to integers (, , and ).Try replace it with


Answer URL
https://docs.python.org/3/tutorial/
