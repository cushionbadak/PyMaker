Natural Text
In C++, I often use the following pattern:But in python I have to repeat call twice:How to avoid duplication of ?
Either with the classic / pattern:or by using the two-argument form of  to build an iterator out of your function:where  is whatever value you're expecting to terminate the loop. Probably something like . The iterator will call  to get elements and stop when  returns the sentinel value.
Currently, the least repetitive version of this pattern (and often seen) is the following:Looking ahead, however, Python 3.8 will introduce assignment expressions via pep-572 which will allow:


Answer URL
https://docs.python.org/3/library/functions.html#iter
