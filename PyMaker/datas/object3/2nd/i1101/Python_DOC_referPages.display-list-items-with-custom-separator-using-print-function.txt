Natural Text
Can someone explain to me why when I'm trying to pass unpacked data into print function using asterisk, the optional argument "end" is applied only for the last list's element, and for the rest is default (space)I expected  instead of 
According to the  docs, Print objects to the text stream file, separated by  and followed  by . All non-keyword arguments are converted to strings like  does and  written to the stream, separated by  and followed by .The  argument is used to control the separator between arguments to .  just controls the line terminator.
In addition to previous answer, you may find the following useful as well:


Answer URL
https://docs.python.org/3/library/functions.html#print
