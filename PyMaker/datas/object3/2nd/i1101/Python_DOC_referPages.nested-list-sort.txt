Natural Text
How do I append two values to a key in dictionaryIt is throwing error str object has no attribute append
You need to add a  to your dict - not the string directly. Three ways I know of, from worst to best. You can either use a normal dict and change your code to or use :or use a .The last method is most performant -  creates lots of empty default-lists that are not used if the key already exists.If you need a top n result, you can create it directly from the dictionaries item()s:
If I understood correctly, you need to change the line:to:In your current version you are assigning the string  to , so you need to change to a list hence the change.


Answer URL
https://docs.python.org/3/library/stdtypes.html#dict.setdefault
https://docs.python.org/3/library/collections.html#collections.defaultdict
https://docs.python.org/3/library/stdtypes.html#dict.items
