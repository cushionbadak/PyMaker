Natural Text
I would like to map a function that splits names with the help of the  package in python. The function I use is the following:And the dataframe would be like the following for example with the  column that contains the names that need to be split with the  package.And I loop the function and try to map it, but it only returns the last value. I guess it makes sense. But  did not work, or I don't know how to apply it properly.And it should return a dataframe:Whereby  represent the First name that is extracted by nameparser HumanName(),  the Middle Names and  the Last name.
Assuming your dataframe has a  column, for example:You could use zip to unpack the values into multiple columns:


Answer URL
https://docs.python.org/3/library/functions.html#zip
