Natural Text
I need to know if there is a command that allows you to execute specific lines of a larger script in python. Do insert an if at the beginning of the script like this (pseudocode, values is an input parameter). I do not want to insert the whole script inside the conditions because the code would be too long and because there are other conditions.
I would definitely try creating a function for each group of lines. This can be done like so:You can then call the functions later in your program like so:


Answer URL
https://docs.python.org/3/tutorial/controlflow.html#defining-functions
