Natural Text
I tried to send a HTTP request to my jupyter notebook with the help of the module  like this:Output(in the terminal where jupyter notebook was running):And the data received is And it says that it uses this delimiter  
If you dont want to supply any headers in your  request, just terminate it with  sequence: uses  sequence as a line-delimiter (like in Windows) and double sequence () to mark the request headers end. So normally the request would look like


Answer URL
https://docs.python.org/3/library/socket.html
