Natural Text
Say you have a  which inside has so  is a dictionary.in one script  you have:and in another script  you haveHow would you keep the printed value in  up to date with the new value after running  at any time without the obvious reading the file again seeing as the while loop in  needs to be as quick as possible!I currently run these on seperate bash threads i.e:But I could run bar in a thread if that is possible?
I don't know how fast you need this to be. But it would certainly be possible to just reload the  module with . So  and  stay the same and your  changes to:UpdateThe example above works for Python >= 3.4, use  for earlier versions of Python 3 or  for Python 2.


Answer URL
https://docs.python.org/3/library/importlib.html#importlib.reload
