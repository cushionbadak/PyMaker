Natural Text
This question already has an answer here:Find full path of the Python interpreter?                    4 answers                I have several software packages that install various installs of Python.  For example:Using  does not work for my case since I need to know where the actual install is located, not the version.How can I determine which install my Python interpreter is using?
What you want is , which will give the path to actual interpreter executable.


Answer URL
https://docs.python.org/3/library/sys.html#sys.executable
