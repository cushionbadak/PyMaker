Natural Text
This question already has an answer here:Do Python regular expressions from the re module support word boundaries (\b)?                    4 answers                Why isn't the word boundary working? reading this site, I know a word boundary works like this: There are three different positions that qualify as word boundaries:Before the first character in the string, if the first character is a word character.After the last character in the string, if the last character is a word character.Between two characters in the string, where one is a word character and the other is not a word character.The  string below appears to fit at least one of the positions listed above. output. There is no change in the 'The'.Why isn't the word boundary working? When I put spaces before and after ' The ' pattern, the regex appears to work.output:
You need to use a raw-string for your Regex pattern (which does not process escape sequences):Otherwise,  will be interpreted as a backspace character:
Try this oneoutput:Here is DEMO


Answer URL
https://docs.python.org/3/reference/lexical_analysis.html#string-and-bytes-literals
