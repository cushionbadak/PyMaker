Natural Text
I found this code written in Python 2.7 to bypass a deadlink while reading a list of urls and retrieving their content:It worked fine in Python2 but I can't find the equivalent in Python3 because of the urllib2 merging.
You can do the exact same thing with  here. Don't catch every conceivable exception, only catch what is reasonably going to be thrown:This code does the same, but more efficiently.


Answer URL
https://docs.python.org/3/library/urllib.request.html
