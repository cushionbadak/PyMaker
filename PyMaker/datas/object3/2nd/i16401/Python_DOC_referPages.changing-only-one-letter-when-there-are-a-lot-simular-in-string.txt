Natural Text
Suppose I have the following string:I need to make a loop that changes the last '.' to '_' so it looks this wayThey are all simular style(3 words, 3 dots).How to do that in a loop?

 lets you specify the number of replacements. Unfortunately there is no , so you'd need to reverse the string before and after :) eg.Alternatively you could use one of , , 
This doesn't even need to run in a loop:


Answer URL
https://docs.python.org/3/library/stdtypes.html#str
