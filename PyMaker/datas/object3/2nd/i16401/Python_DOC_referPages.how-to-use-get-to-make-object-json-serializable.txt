Natural Text
I have a decorator that returns a string on . How can I make it compatible with ?This minimal example raises an exception in  stating that  is not serializable. Since it is not a real string but just provide a string like interface that's not surprising. How can I make it JSON serializable using the value returned by ?
You need to extend  class to be able to handle  objects; Example almost copy pasted from documents:


Answer URL
https://docs.python.org/3/library/json.html
