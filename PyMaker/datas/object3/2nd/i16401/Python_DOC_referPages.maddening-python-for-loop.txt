Natural Text
I am writing a simple checkers program. I am trying to get a nice ASCII output of the game state. Here is the function I am trying to use:(outside the class definition):I cannot see what is wrong with it, yet here is what is happening.The desired output isEDIT: For those who get stuck in a similar place, here is the working solution:
A common mistake.  does a shallow copy, e.g. in  the  is the same as . Now if  is a list, then you edit the references in multiple places.See How do I create a multidimensional list? in the official Python FAQ for more details. But the short version is: instead of , write , and you'll get four separate copies of  instead of four references to the same .


Answer URL
https://docs.python.org/3/faq/programming.html#how-do-i-create-a-multidimensional-list
