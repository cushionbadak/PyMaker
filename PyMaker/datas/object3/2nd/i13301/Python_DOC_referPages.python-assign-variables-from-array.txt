Natural Text
I have some code in Python:Is there a one-liner way to assign these variables?Also, what would I do if I wanted to skip a value?
Yes you can separate each variable with a  to perform unpackingIf there is a particular value that you are not concerned about, the convention is to assign it to an underscore variable, e.g.
but it won't be readable
If you want to skip a value in sequence unpacking, you can do:


Answer URL
https://docs.python.org/3/tutorial/controlflow.html#unpacking-argument-lists
