Natural Text
How can I add "http://test.url/" to the result  below, but only if it doesn't contain "http"

Use :In Python 3.x, use :Complete example
I would use In your example you only need to do this when you find a relative url.
In the spirit of BeautifulSoup, this works well with your original code.If what you want is to preface the non- sites with a  then you need to do this:You're set either way.



Answer URL
