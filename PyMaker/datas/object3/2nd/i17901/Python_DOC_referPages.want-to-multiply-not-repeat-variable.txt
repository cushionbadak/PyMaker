Natural Text
I want to input a command-line variable and then multiply it, but when I print the variable it repeats by the number I want to multiply it by.eg:When I run the script I get the following:    python3 test.py 2
 is string. Multiplying string by number casue that string repeated.To get multiplied number, first convert  to numeric object using  or , ....
You are multiplying a string with an integer, and that always means repetition. Python won't ever auto-coerce a string to an integer, and  is always a list of strings.If you wanted integer arithmetic, convert the  string to an integer first:


Answer URL
