Natural Text
I am looking for something like  in python, but  doesn't accomplish this. Here's an example:How would I accomplish the above -- to trim all whitespace characters at the edges of text -- where I could get  (other than just doing a hackish ?
Since you are using Python 2.7, first convert your string to unicode and then strip:yields:This will cause Python to recognize the  as a Unicode non-breaking space character, and properly trim it.  Without that, Python assumes it's an ASCII string and in that character set  and  aren't whitespace.
I would suggest you use the  function. You can do this:You could encapsulate this logic if you have a large number of possible characters you want to trim off:You could also implement this using :


Answer URL
https://docs.python.org/3/library/string.html#string.whitespace
