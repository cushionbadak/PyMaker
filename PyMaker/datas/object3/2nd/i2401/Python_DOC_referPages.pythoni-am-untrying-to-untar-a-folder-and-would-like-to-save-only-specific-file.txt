Natural Text
Below is the folder structure: I have tried the below code to just save report into output directory and also I would like to rename it as same as  filename that is .below is the code:How can I just save report into output directory and rename the file.
You should escape a backslash literal with a backslash:or you can use a raw string:But then since you have this condition:you should use a forward slash instead for extraction:Also,  does not return the directory name it made, so your  variable would be . You should assign the path name to  directly:and then use it for  and for extraction (note the lack of quotes around ):EDIT: Now that you mentioned that you want to extract  without the  directory that is part of its path in the tar file, you would have to use the  method instead and write the content to the desired location yourself:If you want to rename  to the name of the tar file you can simply open the file different name:and if you want that name to be the same as the tar file, use  twice to obtain it (since there are two extension names,  and , for your tar file), so that in the beginning you should do this:and then when you open the json file for writing:


Answer URL
https://docs.python.org/3/library/tarfile.html#tarfile.TarFile.extractfile
