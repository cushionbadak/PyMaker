Natural Text
Say I want to download files from websites and files are listed by this styleAnd I want to make some code that'll download these using a loop. This would not work when i is bigger than 9 because then the concatenation would look like instead of I could code the case for each hundreth's value but I have a feeling there is a more elegant way to code this. 
Using the a format string, you could do:Output:So, for your application:
You can use , which is a string function that pads a string with leading zeroes. In your case:
You can do . The  is a placeholder and the  is a numeric format-specifier. The  tells Python to zero-pad up to three characters.
Use:instead.


Answer URL
https://docs.python.org/3/library/string.html#format-specification-mini-language
