Natural Text
if  is added, the result is , while if  is deleted, the result is , why?
As mentioned in the comments,  return an iterator, and when you apply  on , you iterate over the iterator and receive to end, then get nothing for the next statement (), when you call the iterator again ().Hence, it does not happen when you just print the  instead of .


Answer URL
https://docs.python.org/3/library/itertools.html#itertools.takewhile
