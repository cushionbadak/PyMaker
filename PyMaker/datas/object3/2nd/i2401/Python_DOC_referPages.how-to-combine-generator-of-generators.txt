Natural Text
I want to combine a generator of generators so that when I call list() on said generator I get a list of the elements of the generators all toghether, not a list of generators. For example, I am currently doing:which is returning:But i want it to return something like:
You can do like this, is not won't give your desired output. You have to use You could be done like this without using the iterator.


Answer URL
https://docs.python.org/3/library/itertools.html#itertools.chain.from_iterable
