Natural Text
I have a set consisting of sets of 2 elements, the first element is still the word and the second one is the file from where the word comes from and now I need to append the name of the file to the word if the word is the sameE.G. should output Can you give me some tips on how to this?
Also, you can do as below if you wish not to use defaultdict:
You could use a set and the defaultdict:OutputAs @ShmulikA mentioned set does not preserve ordering, if you need to preserve ordering you can do it like this:Output
keeping insertion order using set of seen items:outputs:without keeping the order using defaultdict & set:outputs:
It is possible to use an OrderedDict to solve this. It is a dictionary that allows iteration in the order by which keys were added.Output:


Answer URL
https://docs.python.org/3/tutorial/datastructures.html#sets
https://docs.python.org/3/library/collections.html#collections.defaultdict
https://docs.python.org/3/library/collections.html#collections.OrderedDict
