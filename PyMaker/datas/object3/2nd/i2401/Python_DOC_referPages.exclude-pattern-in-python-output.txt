Natural Text
I'm scraping a .txt file in python, and I'm using regex to capture ticket numbers and names:Based on my .txt file, this is capturing the correct fields as well as my blank lines (not yet filled out):If I append a  to my regex it'll print my blank lines, but is there a way to keep my "filled out" lines and also exclude the blanks with their line break?
the following code will skip empty lines by adding \s+ which Matches Unicode whitespace characters (which includes ) one or more time then adding \w+ which Matches Unicode word characters one or more; this includes most characters that can be part of a word in any language, as well as numbers and the underscore. If the ASCII flag is used, only  is matched output:
Perhaps you could use rstrip and specify a newline Demo


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.rstrip
