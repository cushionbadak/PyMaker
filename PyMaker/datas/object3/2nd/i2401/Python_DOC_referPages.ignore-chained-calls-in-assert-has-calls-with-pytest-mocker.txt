Natural Text
When using  from , how can I use  to check the calls to a specific method without checking all the calls made to the return value of that method?For example:Fails with:Why are calls to the return value also included? I just want to check that I had a call to  followed by a call to . I don't care about calls made to anything it returns, but I do care about the order of calls to  itself.
Add  to your  call, like this:The reason is, it looks like  uses the  attribute on a mock which based on the documentation (https://docs.python.org/3/library/unittest.mock.html#unittest.mock.Mock.mock_calls):records all calls to the mock object, its methods, magic methods and return value mocks.With  though, it's not an exact matching problem it was just the ordering that was failing.EDIT: Since order matters, use . Like this:
Use :


Answer URL
https://docs.python.org/3/library/unittest.mock.html#unittest.mock.Mock.mock_calls
https://docs.python.org/3/library/unittest.mock.html#unittest.mock.Mock.assert_any_call
