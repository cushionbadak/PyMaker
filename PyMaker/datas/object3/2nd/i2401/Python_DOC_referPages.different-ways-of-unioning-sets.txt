Natural Text
Python documentation suggests two ways to union sets:I've recently come across yet another one:How does it work?
In your example,  actually calls the  method with  for the  argument. Therefore, it is equivalent to .As an aside, Python allows union-ing of arbitrary numbers of sets according to this. Actually you can see this in the Python documentation, here. Because the  method accepts  as an argument, you can pass in any number of sets and it will perform the union of all of them. 


Answer URL
https://docs.python.org/3/library/stdtypes.html#set
https://docs.python.org/3/howto/descriptor.html#invoking-descriptors
