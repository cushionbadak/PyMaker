Natural Text
I've a list like below:I'm trying create:1) List is grouped by the very first element2) Quantity is sum(third element)3) New column is added forth element is newts date    and the fifthis the oldest one.Do You have any suggestion how I can get table like list2 using Python functions ? 
You can use :Output:   
You could use Pandas to do thisWhich will give you:(note you shouldn't name your variable 'list', as this overrides the built in type)
You can use :Btw, it's not good to use  as a name of a variable. 
You can also use Also, don't call your lists, 


Answer URL
https://docs.python.org/3/library/itertools.html#itertools.groupby
