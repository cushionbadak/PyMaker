Natural Text
Can somebody suggest a better way to execute multiple commands within JSON using Python.For example how to improve the following:JSON:$ python -m json.tool test2.jsonPython:Output:]$ python parse.py test2.jsonstart{u'command1': u'mkdir -p /home/ec2-user/data4/opt/ODS', u'command2': u'mkdir -p /home/ec2-user/data5/opt/ODS', u'command': u'mkdir -p /home/ec2-user/data2/opt/ODS'}endTIA
Here's the structure and code you are looking for:Python:Output:
In your example it would be better, to use os.makedirs.But if you REALLY need  module - i suggest you to exec command with single subprocess instance. For example:
Similar to @Mark's answer, but keeping your original structure:


Answer URL
https://docs.python.org/3/library/os.html#os.makedirs
