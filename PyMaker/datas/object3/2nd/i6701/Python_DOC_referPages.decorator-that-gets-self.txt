Natural Text
I'm trying to write a decorator that can be added to instance methods and non-instance methods alike. I have reduced my code to a minimum example that demonstrates my pointThis gives the beautiful errorIs it possible to do something like this without resorting toOr is that my only option?
You have to implement your decorator as a class and implement the descriptor protocol. Basically, the descriptor  function is what's responsible for creating bound methods. By overriding this function, you get access to  and can create a bound copy of the  function.The following implementation does exactly that. The  instance is saved in the  attribute. The decorator has a  method which calls the decorated function, and a  method which does the same thing.Test:


Answer URL
https://docs.python.org/3/howto/descriptor.html
