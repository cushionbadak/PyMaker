Natural Text
This question already has an answer here:is_alpha coding that works like isalpha() in python                    4 answers                This was my original code and it is still not returning  in cases like . When the real  returns  in the case of  or . How to exclude all these cases?
Empty strings will not trigger any code in the for loop, since  is essentially a no-op here (there's nothing to iterate), so your  returns  for empty strings. You should add something liketo the beginning of your function.(As a side note, your  statement is unnecessary since  will exit the function.
Your loopWill never run if , you go straight toAlso, if youYou can use  and . 


Answer URL
