Natural Text
How can I sort a list of tuples according to the int in a certain position? (without a for loop)eg. Sorting  according to the third element in each tuple?
You can use , its key function, and :You could also use a lambda function instead of :However, the latter is generally faster.
You can specify the key by which to sort a list using the  builtin:


Answer URL
