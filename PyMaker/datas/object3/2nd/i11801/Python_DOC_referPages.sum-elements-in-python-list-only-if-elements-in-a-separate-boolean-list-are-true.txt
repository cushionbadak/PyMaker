Natural Text
I have two python lists,lists A and B are the same length.I want to sum only the elements in A that correspond to True elements in B.I know I can do that with something like:but I was wondering if there was a more elegant solution that didn't involve looping over elements in each list?
Using :The implementation of  is described on the linked page. It's short and simple, so you don't have to import  1:1 OTOH,  is implemented in C, and therefore should be faster
Generator expression + :
CodeOutHow it works:If we expand the list comprehension a little more we can see what is happening a little more clearly.


Answer URL
https://docs.python.org/3/library/itertools.html#itertools.compress
