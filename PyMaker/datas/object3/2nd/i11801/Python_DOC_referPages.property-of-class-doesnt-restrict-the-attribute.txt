Natural Text
So i made a class and want, that attribute hp stays always between 0 and maxhpIn theory making hp a property should give me the wished result: Somehow it doesnt work though.Is there way to link attributes for and back? So i have stored the position of the unit class object. At 2 places, once the attribute position which contains a [x,y] array and the other time its stored in 2 attributes x and y and each contains a int. Changing self.x or self.y should changes self.position and the other way around too.
In your , the line  should be . That way it's only set/get in the  methods.You would handle ,  and  the same way as you've done for . Use ,  and  internally to correspond to the values in the getters and setters; and set all of the  values in the setters of each. Using  as a an example:Similarly for  and , though I think you should only do it via :Btw, the  setter can be re-written as follows:
Another problem is in . The second  statement should be replaced with  because when  is less than 0,  is set to 0 in first  and then, without , it is set to a negative value in :


Answer URL
https://docs.python.org/3/library/string.html#format-string-syntax
