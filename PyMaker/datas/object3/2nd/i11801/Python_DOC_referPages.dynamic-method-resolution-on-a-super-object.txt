Natural Text
I need to find a method on a  object by name. This works when using the "dot operator" notation: . However, I need to do that dynamically, something like , which does not work.I tried to use , but super object does not have that method. And  returns  instead . How to do that correctly and elegantly?For instance:This code outputs:
What you're looking for is . This works:By using  directly, you lose the wrapping  does to the object. You should always use  for getting methods or other attributes. Generally, going directly to  methods is discouraged if there are other options.


Answer URL
https://docs.python.org/3/library/functions.html#getattr
