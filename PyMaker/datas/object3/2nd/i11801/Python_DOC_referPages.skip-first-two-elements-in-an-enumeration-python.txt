Natural Text
This is not what I want from enumerate:This IS the functionality I want from enumerate:See the difference?The first one just says, "fine I'll call your 0th element 2 if you really want me to"The second one says, "I understand that you just want to begin the loop at the second index, much like range(2, len(seasons)) would do"Is there no way to do this simply with enumerate?
Why not just slice the first two elements? Output:To understand what is going on with enumerate and the start. Try iterating over it to see what it outputs: As you can see your , which simply increments along iterating through your  list is what starts at 2. But each value in the list is actually from the start of your list. This just further proves that  does not have anything to do with the actual starting point of your list iteration.
You could use  in order to take a slice of an iterable:


Answer URL
https://docs.python.org/3/library/itertools.html#itertools.islice
