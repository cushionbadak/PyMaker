Natural Text
Currently I am doing as follow, to getting response and save the file locally in my PC-But what I want, to save the response In my PC when  with error handling, instead of doing  again. Any help would be appreciated.
Something like this? is the defacto serializer in python, but, does have issues 
you can pickle the response and save it.  is used for object serialization before storing it into disk. opens the file in append mode so that all the responses are appended to . Otherwise, if opened in  mode will overwrite the file each time you open itLater to see the output you can do:The reason to use the above code for unpickling is this 


Answer URL
https://docs.python.org/3/library/pickle.html
