Natural Text
I want to display the title of the label in my messagebox which will display like "you have selected project". How can I do it with all of them like with save and exit also only using one function?
You can do it with lambda functions (for the submenu commands) and string formatting:The lambda functions are used to pass the label argument corresponding to the submenu item to .For the messagebox, the syntax is , so you need to insert the label argument inside your message with string formatting: , th  is replaced by the content of the  variable.


Answer URL
https://docs.python.org/3/library/string.html
