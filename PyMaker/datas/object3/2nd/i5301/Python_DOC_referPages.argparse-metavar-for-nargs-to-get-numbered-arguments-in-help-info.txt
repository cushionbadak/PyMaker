Natural Text
In the argparse document, there is an example using :The corresponding help information is Is it possible to set the  such that the help info looks like this?
 can be tuple, as inBut that only works for optionals (flagged arguments), not positionals. I'm not exactly sure why, though I may have dug into this issue before.https://docs.python.org/3/library/argparse.html#metavarWith the positional, the error stack includes:A  doesn't have  (or its empty), so it's using the  unpacking idiom to ask for just one string.  Hence the error: .So expecting only one  variable is quite intentional, but I'm not sure about the rational.  Maybe it's because it would be harder to tell where one positional ends and the next starts in the :I looked at this a couple of years ago, https://bugs.python.org/issue14074.  Looks like there can be problems with the , help-lines and error message.Actually the problem isn't in the , it's in formatting the help lines:The normal help line for a positional just shows one symbol, not the  of the usage:
I would make a custom  and use that.  Below is a quick hack.


Answer URL
https://docs.python.org/3/library/argparse.html
https://docs.python.org/3/library/argparse.html#metavar
