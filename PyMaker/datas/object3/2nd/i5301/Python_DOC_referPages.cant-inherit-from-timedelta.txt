Natural Text
i was annoyed by the fact that  has no  method. so i wanted to inherit from  (monkey-patching is no option for )so i tired inheritance ( is just here for reference; this one works!).the method resolution order of both looks similar; but it seems that  in the  case is skipping  and jumping directoy to .am i doing something very wrong? how can i inherit from ? what goes wrong here?
According to How to initialize an inherited object with either the 'usual' (keyword) arguments or an instance of the parent, you have to override  method. Something like:


Answer URL
https://docs.python.org/3/library/datetime.html?highlight=timedelta#datetime.timedelta
