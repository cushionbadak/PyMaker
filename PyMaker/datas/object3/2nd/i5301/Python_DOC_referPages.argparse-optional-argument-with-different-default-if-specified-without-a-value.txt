Natural Text
Consider three different runs of a program:Is it possible to use  such that, for example, in the first case, , in the second case, , and in the third, ?
 with a  parameter handles this 3-way input nicely..I could have also given it a  parameter.how to add multiple argument options in python using argparse?
If you are happy to do a little post-processing, you can get it:However, it is bending the tool in a bit of a strange way.  You may want to consider instead the count action, which is commonly used to specify verbosity levels, for example  (-v, -vv, -vvv).  


Answer URL
https://docs.python.org/3/library/argparse.html#action
