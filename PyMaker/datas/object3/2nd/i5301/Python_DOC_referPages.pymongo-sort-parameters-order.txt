Natural Text
I have a sort like this:It has 2 sort fields, mongo will sort the  field first, then .But in python  is disordered, it sometimes sorts  first, sometimes  first, so the question is how can I make sure it always sorts the  first?
You can simply go by a sort instead of This is the pattern:You can chain the functions so that you can limit the documents to sort by a filter first:As a side note: the fields in .find() and .sort() do not need to be different.


Answer URL
https://docs.python.org/3/library/collections.html#collections.OrderedDict
