Natural Text
Looking into the documentation of the  statement in python:The context manager’s  method is invoked. I was wondering what exactly happens if I doJudging from the documentation and  evaluating to True suggests, that the  seems to be somewhat equivalent to a . But I was wondering if there is any more detail to be found about the actual behaviour of the  function. Any hints are appreciated.
As a more specific answer,  returns a subclass of  (depending on options). The  method is implemented in C in  with the codewhich you can see, just calls the objects  "method".
Anything can be used as a context manager - it just has to provide it's own  and  methods. See PEP343 for all the details on how  and context managers work.In the case of  this is actually a call to  class under the hood. This is implemented mostly in C, but there's a fallback python implementation () which contains the following code, which confirms that it just calls 
This depends on the context manager itself; the point is that a context manager can determine what to do when it exits.In the case of , yes what it does is close the file; but other context managers are free to do whatever they like. A database connection manager might commit or rollback the cursor before closing the connection, for example; or a temporary file manager might delete the tempfiles.


Answer URL
https://docs.python.org/3/reference/compound_stmts.html#with
https://docs.python.org/3/library/io.html#io.IOBase
