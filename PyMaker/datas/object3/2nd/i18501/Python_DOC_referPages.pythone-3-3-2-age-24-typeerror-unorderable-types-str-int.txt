Natural Text
This is the error I am getting:
On Python 3,  always returns a string value. Use the  type to convert it:A string value and an int are not orderable:Note that  can throw a  exception, if the input cannot be converted:
 is a string, not an int. To make it an int, use the  function, so:Note the line:


Answer URL
