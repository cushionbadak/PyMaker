Natural Text
I have a list of stocks and positions as tuples. Positive for buy, negative for sell. Example:How can I sum the positions of stocks, to get the current holdings?
How about this? You can read about .
Here is a solution that doesn't involve importing:Note this is for Python 2.x.  In 3.x, you'll need to do: .
I would do this using :Then you can call the  method of  objects to get a list of tuples sorted by the values in descending order.In case you need to sort tuples by their first elements, use :
without any explicit loops:


Answer URL
https://docs.python.org/3/library/collections.html#collections.Counter
