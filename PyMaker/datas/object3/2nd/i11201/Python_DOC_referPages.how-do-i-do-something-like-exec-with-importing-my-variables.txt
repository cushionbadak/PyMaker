Natural Text
I'm trying to do something like this for a project:Now I get an error, because exec() just starts a new session and ignores my functions and variables. How can I change that?
The  builtin takes two additional arguments that can be used to pass in the local and global scope:Update: Given your example, I think a "better" solution (or at least something more realistic) is to not use exec. To call a function given by the user, try something like:


Answer URL
https://docs.python.org/3/library/functions.html#exec
