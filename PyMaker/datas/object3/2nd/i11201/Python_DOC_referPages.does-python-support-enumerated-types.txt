Natural Text
This question already has an answer here:How can I represent an 'Enum' in Python?                    43 answers                Is there a way to have C++ like enumerated types like in Python? For example, in C++ I can do:and use these as global constants. Is there a similar thing in Python? 
From the Docs:Enumerations are created using the class syntax, which makes them easy to read and write. An alternative creation method is described in Functional API. To define an enumeration, subclass Enum as follows:
Python3.4+ supports various types of enumerations in the  module.  Obviously it's not quite the same as an enumerated type in  or , but it serves the same purpose.  If you're stuck in an older version of python, there's a 3rd party backport available on pypi.Basic usage would look like:


Answer URL
https://docs.python.org/3/library/enum.html
https://docs.python.org/3/library/enum.html
