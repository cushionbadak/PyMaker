Natural Text
I want to work with the error message from an exception but can't seem to convert it to a string.  I've read the os library man page but something is not clicking for me.Printing the error works:But this does not:
From the docs for All non-keyword arguments are converted to strings like str() does and written to the streamSo in the first case, your error is converted to a string by the  built-in, whereas no such conversion takes place when you just try and concatenate your error to a string. So, to replicate the behavior of passing the message and the error as separate arguments, you must convert your error to a string with .
In my experience what you want is , which will return both the exception type and the message. only gives the message.


Answer URL
https://docs.python.org/3/library/functions.html#print
https://docs.python.org/3/library/stdtypes.html#str
