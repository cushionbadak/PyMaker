Natural Text
I have the following code to compute all the possible combinations of odds for a number of games. I would like to do the followingRemove all the ugly nested loops.Allow dynamic games, i.e I will not have to stay creating a match list adding and add an entry to the nested loop. (ex I want to add match k)Code is as follows;
You should have a look at . Also, your  list seems to be kind of pointless (note that it will contain many refrences to the same list over and over again) and  is just the  of the resulting list.You could even make this a list comprehension:


Answer URL
https://docs.python.org/3/library/itertools.html#itertools.product
