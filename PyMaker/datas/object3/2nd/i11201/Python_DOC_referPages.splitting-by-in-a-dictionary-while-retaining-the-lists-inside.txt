Natural Text
I've been looking for a way to split by ',' in a dictionary while retaining the present lists but have not been succesful. I want to split this dictionary:To appear like this:Help is much appreciated!
You can use , eg:If you want to get the result as a string to then use elsewhere (maybe write to a file), then use  as  writes directly to stdout and returns .


Answer URL
https://docs.python.org/3/library/pprint.html#pprint.pprint
https://docs.python.org/3/library/pprint.html#pprint.pformat
