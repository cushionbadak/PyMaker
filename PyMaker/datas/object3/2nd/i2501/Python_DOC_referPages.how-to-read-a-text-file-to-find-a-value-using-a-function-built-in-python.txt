Natural Text
I have a function that reads in a list. it will iterate through the list and use that as the function input. It reads a text file that only contains 2  lines. LC1  LC2When it inputs LC2 the function break right away and I'm not sure why.The end goal is to have a dictionary that looks like thisd={LC1:None,LC2:None}
Simple way to do it:But still the easiest way is:Both reproduce:


Answer URL
https://docs.python.org/3/library/stdtypes.html#dict
