Natural Text
I'm new to Python, and I was wondering why I can't change the attribute for a class instance using a function, if I pass the instance as a function parameterFor example, if I had:Why can't fun1 change the object attribute? Is there a way to change it, or is the "val" attribute immutable?
Python class instances and  are completely different beasts; you're likely coming from a JavaScript background where  is equivalent to , but that's not how Python works.If you want to set an attribute via a  name, use :


Answer URL
https://docs.python.org/3/library/functions.html#setattr
