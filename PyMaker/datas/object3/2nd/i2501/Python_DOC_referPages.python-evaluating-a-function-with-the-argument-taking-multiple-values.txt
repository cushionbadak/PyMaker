Natural Text
Asssume I have a function in python which returns the power of 2 of its inputAssume also I have a vector of integers 1,2,3,4,5Now I want to evaluate the function  with all inputs from  and the results should be in a vector of dimensions 5 (A 1x5 array in Python). My desired result is then: .Can I get this result WITHOUT the use of a  or any other loop?I have used  package 
directly from the pythontips website...use the  function!if you want to use your function inside the -function, just do .
There are generally two classical ways to apply a function to all elements of an iterable and store the results in a list. The more flexible one is a list comprehension:The other option is  which allows a more concise notation, in particular if you have a named function available and do not need to use a lambda or the like:


Answer URL
https://docs.python.org/3/tutorial/datastructures.html#list-comprehensions
https://docs.python.org/3/library/functions.html#map
