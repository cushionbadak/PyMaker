Natural Text
I would like to iterate through a list of strings and replace each instance of a character ('1', for example) with a word. I am confused why this would not work.Side note, the strings within the lists are multiple characters long. ('1 of Spades)
You can use a list comprehension:This is natural in Python. There is no significant benefit in changing your original list directly; both methods will have O(n) time complexity.The reason your code does not work is  does not work in place. It returns a copy, as mentioned in the docs. You can iterate over a  object to modify your list:Or use :


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.replace
https://docs.python.org/3/library/functions.html#enumerate
