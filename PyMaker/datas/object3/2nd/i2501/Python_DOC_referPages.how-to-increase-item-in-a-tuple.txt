Natural Text
6 and im trying to create sort of a loop to increase the item in a tuple example;(so the output shows this)basically separate the integers and use them individually in a loopthis is what i got so far but i got this as a resultand im trying to get this as a result  
Use :This is a list comprehension.It's expanded for would be 
You should avoid creating a variable called  as that is a native Python keyword already!You can also consider using  in a loop if you want to operate both lists at the same time like this:Output:


Answer URL
https://docs.python.org/3/tutorial/datastructures.html#list-comprehensions
