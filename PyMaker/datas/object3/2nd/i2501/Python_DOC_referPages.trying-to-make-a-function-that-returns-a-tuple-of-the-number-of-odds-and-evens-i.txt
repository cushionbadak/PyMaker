Natural Text
I tried to do What am I doing wrong?I expect  to give the answer 
Solution without for-loops:If you really needed to use a for loop, here's a way to do it:
SolutionBefore you jump into the comprehension, its easier to construct the code more in the more expanded versionOutput
Use a list comprehension and exploit the neat function . No need for for-loops:
Only the even (or odd) numbers need to be counted.


Answer URL
https://docs.python.org/3/reference/expressions.html#conditional-expressions
