Natural Text
to create a fullscreen window in Python through Tkinter i do this:And it works, of course.But I'd like to know why it is like this.Why am I typing "%dx%d+0+0"? What does Python read in that command?
, and  are now holding the value of the screen height and width, say , and Then it is a straightforward python string formatting (the old fashion way):which is a string that tkinter can parse. (welcome to the 1990'es)


Answer URL
https://docs.python.org/3/library/string.html
