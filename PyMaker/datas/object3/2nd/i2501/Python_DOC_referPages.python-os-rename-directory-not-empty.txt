Natural Text
I'm trying to do  but in python. I have written the following code but throws 
You're telling the OS to move , not its contents. It would normally replace the target ( in this case) but that target obviously isn't empty, so the implicit  fails. Even if it weren't empty, it's likely impossible to remove or replace the  name. The shell  is a glob, which would expand to each thing within , which you could move individually; however, you'd want to transfer their name to the target directory, i.e.  to .  can help you extract that portion. 
You may want to use shutil.move() to iteratively move the files from a directory to another.For example,


Answer URL
https://docs.python.org/3/library/glob.html?highlight=glob#module-glob
https://docs.python.org/3/library/os.path.html#os.path.basename
https://docs.python.org/3/library/shutil.html
