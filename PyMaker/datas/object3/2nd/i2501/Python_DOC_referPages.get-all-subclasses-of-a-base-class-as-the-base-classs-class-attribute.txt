Natural Text
Given a class inheriting from an abstract class, I'd like to be able to get all of its subclasses, but from within the class itself.I've managed to create a  doing just that: but is there a way I could do that using  or through class attribute? (It seems that properties are only for intances of a class, not the class itself.)Here is the one with  (using taxonomy):
You can use a descriptor class:so that  would return:


Answer URL
https://docs.python.org/3/howto/descriptor.html
