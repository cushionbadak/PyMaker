Natural Text
I'm create new projectI create two function. First func normally run, but second function not workingFirst func:Second func:How to fix second func(facebook)I get the error for facebook func:twitter func out:How can I solve this problem?Thanks
The Facebook response doesn't include a  parameter indicating the encoding used:Note the  part in the Twitter response.The JSON standard states that the default characterset is UTF-8, so pass that to the  method:Now, when no content  parameter is set, the default  is returned instead.Note that Facebook's JSON response contains a list of matches; because you are passing in just one URL, you could take just the first result:


Answer URL
https://docs.python.org/3/library/email.message.html#email.message.Message.get_content_charset
