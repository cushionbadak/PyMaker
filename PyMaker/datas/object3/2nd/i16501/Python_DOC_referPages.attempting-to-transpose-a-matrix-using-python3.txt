Natural Text
I know I am missing some pieces to produce this matrix but I cannot seem to find my mistakes from the start.I have three tasks.1) Show what the empty matrix will look like. 2) Show the original matrix(4x3).  3) Then transpose the matrix to a 3x4.I keep getting this error, not sure how to fix it.
If you are managing a matrix as a list of lists, transposing is quite simplying using your list as an argument into the  function, like so:We  the  function across the output of  as zip returns list of tuples, not a list of lists.The above prints:edit: That error suggests that the code you've added doesn't match what you are running. Check your file is saved, and verify there are brackets  after each call to .


Answer URL
https://docs.python.org/3/library/functions.html#zip
https://docs.python.org/3/library/functions.html#map
https://docs.python.org/3/library/functions.html#func-list
