Natural Text
I was looking at a repo and came across a somewhat weird lineWhat does it mean to import like this? I have not seen it before and unfortunately it's rather hard to google.
That line tells Python to import  and  from the package , but to import  under the name of .From the docs:If the module name is followed by , then the name following  is  bound directly to the imported module.Below is a demonstration with the  and  functions from the  module:


Answer URL
https://docs.python.org/3/reference/simple_stmts.html#grammar-token-import_stmt
https://docs.python.org/3/library/re.html
