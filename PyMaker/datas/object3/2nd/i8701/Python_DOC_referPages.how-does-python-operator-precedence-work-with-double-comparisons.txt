Natural Text
This question already has an answer here:Python shorthand for multiple conditionals in an if statement [duplicate]                    1 answer                 returns .However I would expect it interpreted as How is that possible ?
This is a chained comparison. Instead of being left-associative like  or right-associative like , it's actually treated asexcept that  is evaluated at most once.
From the docs:Unlike C, expressions like  have the interpretation that is conventional in mathematicsComparisons can be chained arbitrarily, e.g.,  is equivalent  to , except that  is evaluated only once (but in both  cases  is not evaluated at all when  is found to be ).Therefore is equivalent to 
It's stated in the documentation that it is part of the language


Answer URL
https://docs.python.org/3/reference/expressions.html#comparisons
