Natural Text
We know about the  function in python.What I want to do is I want the user to input any number.If the input is 2, the output must be the string .If input is 4, the output must be .Note that I am not trying to compute the value of the  function, but rather to just assemble the strings.
The code can go like this:It prints  as required.Note that the actual min function in Python, unlike some other languages, can take more than two arguments:  is a valid call in Python.


Answer URL
https://docs.python.org/3/library/functions.html#min
