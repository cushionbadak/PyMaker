Natural Text
I am creating a number of slices  of a list, where  ranges between  and :When , this returns a slice of length 0:I don't want to have to do this to solve it:… because if I have many lists to append to , it gets messy.Is there a clean way to do this?
One workaround for this quirk in Python slicing is to take advantage of these facts: always evaluates to  is the only integer that is false-ish in a boolean context is equivalent to With those in mind, you can write your slice as:… and the slice will be interpreted as  (which is the same as ) only when  (and therefore ) is .


Answer URL
https://docs.python.org/3/library/stdtypes.html#truth-value-testing
