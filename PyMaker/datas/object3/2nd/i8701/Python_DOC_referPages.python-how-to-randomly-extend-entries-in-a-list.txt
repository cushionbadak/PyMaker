Natural Text
Suppose we have a list . I want to extend it with the same values in a random way, so 
Consider something like this:
You could use functions from the  module to generate extra elements:Then use  to add the elements to the list.
If we want to insert random number at random index, we can use insert function of list.


Answer URL
https://docs.python.org/3/library/random.html#random.choices
