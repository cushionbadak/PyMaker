Natural Text
Using Python regular expressions how can you get a / returned? All Python returns is:
 objects are always true, and  is returned if there is no match. Just test for trueness.
If you really need  or , just use As other answers have pointed out, if you are just using it as a condition for an  or , you can use it directly without wrapping in 
Ignacio Vazquez-Abrams is correct. But to elaborate,  will return either , which evaluates to , or a match object, which will always be  as he said. Only if you want information about the part(s) that matched your regular expression do you need to check out the contents of the match object.
One way to do this is just to test against the return value. Because you're getting  it means that this will evaluate to true. When the regular expression isn't matched you'll the return value None, which evaluates to false.Produces  as output.
Here is my method:


Answer URL
https://docs.python.org/3/library/re.html#match-objects
