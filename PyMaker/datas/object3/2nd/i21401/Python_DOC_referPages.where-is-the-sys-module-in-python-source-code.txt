Natural Text
I have found most python modules in python source directory, under Python/Lib or Python/Modules ,but where is the sys (import sys) module ? I didn't find it .
The AnswerI find it here: If you're on Linux or Mac OS X, and in doubt, just try  in the Python directory.Other StuffThe way I found it was by searching for the string  throughout the Python directory (using TextMate), as I've used e.g.  before from the  module... something similar can be done with  and .Another suggestion could be : This will loop through all  files present in anywhere up the file tree you're currently located at, and search the file for . The  flag will ensure we get a filename path so we can trace the matches back to the files they are found in.
It's in .
Then you will see something like the following:In my working environment, sys is built into python itself.


Answer URL
https://docs.python.org/3/library/sys.html
