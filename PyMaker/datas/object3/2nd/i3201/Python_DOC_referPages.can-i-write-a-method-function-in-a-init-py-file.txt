Natural Text
Can I write a method/function in a  file? as demonstrated belowExample:In  file
sure, they are normal files. Only difference is that they define that the folder is a package.From python official basic tutorial:The  files are required to make Python treat the directories as containing packages; this is done to prevent directories with a common name, such as string, from unintentionally hiding valid modules that occur later on the module search path. In the simplest case,  can just be an empty file, but it can also execute initialization code for the package or set the  variable, described later.Note that you usually don't put your code in  but instead define it in the packages' modules and import only the names you want to expose in the package namespace.Here's an example on how to access then, suppose you have this directory structure:The  folder is considered a package because of  so in  you can do:


Answer URL
https://docs.python.org/3/tutorial/modules.html#packages
