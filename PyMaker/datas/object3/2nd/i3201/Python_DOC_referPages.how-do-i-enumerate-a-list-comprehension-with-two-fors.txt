Natural Text
I am trying to dobut also pass in  into myfunc, which is an index starting at 0 and incrementing for each new element.For example:should result inI know that I can use  for just the normal case, ie.But I can't figure out how to do it cleanly when there are two s. I couldn't find this question posted previously either. 
You are creating a Cartesian product over two lists, so use  instead of a double  loop. This gives you a single iterable you can easily add  to:For cases where you can't use , you'd put the multiple loops in a generator expression, then add  to that. Say you needed to filter some values of :Another option is to add a separate counter;  gives you a counter object that produces a new value with :After all, in essence  is the equivalent of .
You can use enumerate on the sequence of pairs.
For a simple nested loop, use  as @Martijn suggested.If the expression is more complex you can use :


Answer URL
https://docs.python.org/3/library/itertools.html#itertools.product
https://docs.python.org/3/library/itertools.html#itertools.count
