Natural Text
I have two characters:  and , which from now on I'll define as  and  from now on. In the python console, when I run , I get  as a result. When I run , I still get  as a result.I tried to find the encoding with it, because I think it might be returning  because the encoding is different, but when I do  and same with the  command, it returns .How do I get it to recognize that these are the same characters?
⼔ and 匕 aren't the same character ⼔ is a radical for spoon (see wiki). Here you have the chinese word using 匕 radical匕 is a kanji (see wiki)That's why there are different



Answer URL
https://docs.python.org/3/library/unicodedata.html#unicodedata.name
