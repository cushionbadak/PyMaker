Natural Text
I'm trying tokenize some code and would like to keep the delimiter when I split a string.For example, I would like to keep any occurences of , , , , .I have been using re.split:However, my current implementation of re.split currently creates some unnecessary empty strings in the array:How can I fix this to exclude the empty strings?
You may useSee the regex demoThe  will return a list of all matched non-overlapping substrings in the input string, matching the following: - 1 or more (due to  quantifier) chars other than , , ,  and   - or - a single occurrence of , , ,  or .See Python demo online:


Answer URL
https://docs.python.org/3/library/re.html
