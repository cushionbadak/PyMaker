Natural Text
I need help with function attribute in Python.I have the data which look like this:  and this equals for example .I need to get  which must look like this , But when I try to do it I get the error:
You could try to use :This will get the function from the module  that has the name that is currently inside the variable  (which should be a string).This can also be shortened to this:you will have to decide which is clearer and more readable for your case.So, for example, these lines:will yield the same results as:


Answer URL
https://docs.python.org/3/library/functions.html#getattr
