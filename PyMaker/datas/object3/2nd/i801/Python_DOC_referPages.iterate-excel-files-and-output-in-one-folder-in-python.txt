Natural Text
I have a folder and subfolders structure as follows:I want to extract all excel files (xls or xlsx) from source directory and subdirectories, drop duplicates based on excel file names and put all the unique files in D:/dst directory. How can I the following result in Python? Thanks.Expected result:Here is what I have tried:
I think this will do what you want:
Since you've already got , you don't need to also do , and vice-versa. But since glob only matches one pattern at a time and no way to denote an optional extra 'x' in the extension, you'll either need the  loop twice - once for each extension; or use  which could match , etc.For each file matched, use :With , you can do each extension check with  in the same loop:


Answer URL
https://docs.python.org/3/library/shutil.html#shutil.move
https://docs.python.org/3/library/fnmatch.html#fnmatch.fnmatch
https://docs.python.org/3/library/os.html#os.walk
https://docs.python.org/3/library/shutil.html#shutil.copytree
