Natural Text
When running a Python file from the command line, you use , but VSCode Code Runner uses  (by default), so I was wondering:What's the difference (since after testing I see no visibledifference)? What is the  part called?
The  flag, according to Python's  statement:force the binary I/O layers of stdout and stderr to be unbuffered; stdin is always buffered; text I/O layer will be line-buffered; also PYTHONUNBUFFERED=xThis is documented here in the Python docs.These are known as command line options. There are a number of them, which you can read about using .


Answer URL
https://docs.python.org/3/using/cmdline.html#cmdoption-u
