Natural Text
Output:Expected Output:Where I am doing wrong?
You have a couple of options. Either redefine  as an empty list within your outer  loop, so that the same list is not appended to throughout. Or use  to append to a default list value. Here's an example of the second option:Using a  is more efficient and arguably more readable.
Lists in python are mutable objects, which mean that the  variable always points to the same list object. You could easily fix that by doing:That way, a new list object is instantiated at each iteration.NB: you could simplify your code by using list comprehension:


Answer URL
https://docs.python.org/3/library/collections.html#collections.defaultdict
