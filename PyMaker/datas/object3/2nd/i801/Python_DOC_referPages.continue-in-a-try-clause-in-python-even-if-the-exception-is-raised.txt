Natural Text
Is there any way to continue code execution in a try clause, even when the exception is raised?This is the code example.All three values are optional, so the key  may not exist, but  may be present. I would like to reach  even if calling  raises an exception. Is there any way to do this in a single try/except block? Something like:I already found a different solution to the case in the example, but now I am curious to know if there is a way to continue down one try/except block, even if the exception is raised.
You can use  instead of catching the  exception.if  does not exist in  it will be set to .You can also set another default value than  like this.
If you really need the log, refactor the  out of there:If not, then it's as easy as this:


Answer URL
https://docs.python.org/3/library/stdtypes.html#dict.get
