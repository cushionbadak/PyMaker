Natural Text
I have the following python code:Now I want to pass two values  and  to the  function.The  documentation says the  can be a callable that accepts exactly 3 arguments. But i need  and  for calculations. How can this be done?EditThe solution using functools works well. However, my function is in a class as follows:Using this approach,  insists upon assigning  to . Thus the interpreter complains that  is assigned multiple values.I need self for calculating the cost.
You just need a function that wraps . A quick way is to use .
This depends largely on the meaning on the meaning of  and . I would suggest adding a wrapper callable by networkx:And provide it to networkx:Or simply make them global variables, not arguments.


Answer URL
https://docs.python.org/3/library/functools.html#functools.partial
