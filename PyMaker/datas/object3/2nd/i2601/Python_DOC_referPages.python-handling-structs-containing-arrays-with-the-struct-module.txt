Natural Text
While the  module makes handling C-like structures containing scalar values very simple, I don’t see how to sensibly handle structs which contain arrays.For example, if I have the following C struct:and want to unpack its values and use the same variables (, , , , , , , ) in Python, I run into the problem that  just gives me every value in a tuple individually. All information about which values are supposed to be grouped in an array is lost:Instead, I have to slice and pull apart the tuple manually, which is a very tedious and error-prone procedure:Is there any better way of handling arrays with ?
You can use construct library, which is pretty much wraps  module and makes parsing and building binary data more convenient.Here is a basic example:


Answer URL
https://docs.python.org/3/library/struct.html
