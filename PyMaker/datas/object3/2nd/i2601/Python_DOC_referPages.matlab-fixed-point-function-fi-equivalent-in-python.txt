Natural Text
MATLAB has a function "fi" for representing fractions in fixed-point scheme. Its syntax is For example, to represent -0.4 in fixed-point with 8-bit word_length and 7-bit fraction_length, it returns the following:Query: Can we do similar in Python? Is there any function for performing this?
Python's rig library is useful for this purpose. It supports both floating to fixed and fixed to floating point conversion. Example:


Answer URL
https://docs.python.org/3/library/decimal.html
