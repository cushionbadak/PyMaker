Natural Text
Is there any built-in method to get back  array after applying  method, for example,Following two methods don't work:Update 1:Unfortunatelly i have very big data already converted with  method so I connot reconvert it with some other method.
The main issues seem to be separators and newlines characters. You can use  and  to resolve them:Note that without any arguments  behaves like .If your string is given and unavoidable, you can use this hacky method:As per @hpaulj's comment, one benefit of  is ability to specify . For example, consider a string representation of . will return ellipsis, e.g.  will return the complete string
You can do that with :


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.splitlines
