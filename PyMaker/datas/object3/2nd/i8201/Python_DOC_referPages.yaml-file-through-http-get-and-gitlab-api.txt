Natural Text
So I'm try to use the gitlab api to pull my CI job artifacts file. This is the URL:If I run it through the browser it immediately downloads the file and puts it into my downloads folder. Now I want to do this through a python script and parse the YAML for specific data.And now I get this response:Which seems to be a bunch of binary but I don't know what to do to read it. I want to be able to use PYYAML and load the YAML
Downloading artifacts from GitLab gives you a .zip file - See the notes at the start of https://docs.gitlab.com/ce/user/project/pipelines/job_artifacts.html where it says:The start of your data is  which confirms this. If your browser is not giving you a zip file it might be uncompressing the download for you.There's a zipfile module, and to use it with a bytestring in python3, you'll need to use the  module, too:And then use either the  or  methods in the  module to get the data you need from the file archive.If you'd like to download a specific file, then https://docs.gitlab.com/ce/user/project/pipelines/job_artifacts.html#downloading-the-latest-job-artifacts also has notes about that, including how to build the URL for that specific file.


Answer URL
https://docs.python.org/3/library/zipfile.html%20zipfile
