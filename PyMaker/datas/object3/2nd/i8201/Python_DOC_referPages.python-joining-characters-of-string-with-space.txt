Natural Text
I'm trying to handle an string input. At first I joined the input with  so I could have each word in one line (which is what I need):and transform it into something like this:But for some reason I'm getting random spaces at the start of a line but not every line. There are zero spaces in the input, I checked specifically. I'm not sure where those extra spaces are coming from.Here's my code:This gives me something like this:Anyone might have a clue why?
I would not use regular expressions for this.... will filter out words of less than 4 character.... will turn that into a list of "words" with each word "spaced out."So you can do it all with:It's often best to build up your functional code snippets using an iterative "bottom up" approach such as I've shown here.  Also regular expressions tend to be slow and somewhat dangerous.  You're relying on a sophisticated and complex set of parsers for interpreting and applying your regular expressions.  When you can avoid them, it's usually good to do so.  the code is likely to run faster and be more robust.
you could do that with 1 generator and without regex:started the same way as this answer; mine is very similar but as i got a solution that is a little shorter i still posted it...and  is a built-in; avoid those as variable names.
Try this:First, find all words greater or equal to four letters.Next join those words by space. Since  is iterable it will put a space inbetween each of the letter.Then join it by  and you’re done!The reason your solution does not work is because it is putting a space in between the newlines and the new character.  puts the space in between every single character.
You can use a list comprehension instead of a , i.e.:If you really need a , use:output:


Answer URL
https://docs.python.org/3/library/functions.html#input
