Natural Text
I have a code that calculates a given datetime as followsI then use  to set a field in an  object to be saved in the database. The  is a  model instance; it is configured to be:It happens, however, that offer.time is updated when calling . When setting , I get . After calling ,  is . Why isn't  preserving the original time?Any ideas?Thanks in advance.
https://docs.mongodb.com/manual/reference/method/Date/Internally, Date objects are stored as a 64 bit integer representing  the number of milliseconds since the Unix epoch (Jan 1, 1970), which  results in a representable date range of about 290 millions years into  the past and future.This explains why  becomes  will be stored as  leaving only 3 more decimals to store milliseconds. When adding milliseconds the value will be . When this is read back into the , the value will be .


Answer URL
https://docs.python.org/3/library/datetime.html
