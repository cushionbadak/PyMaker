Natural Text
I have Python 2.7 as root. I need to install the package "statistics" in Python 3.6, and it is not in the environments of anaconda navigator. How can install "statistics" with  or  for a secondary Python environment?
Create a new Python 3 environment by running:If you want all the standard anaconda packages installed by default, do:Whenever you need to use  run:Then use the command line as normal. So, if you want to install something into your  environment, make sure you  first.Note that python 3 has it's own statistics module that you may find useful, and this module has been ported to python 2 if you would prefer.


Answer URL
https://docs.python.org/3/library/statistics.html
