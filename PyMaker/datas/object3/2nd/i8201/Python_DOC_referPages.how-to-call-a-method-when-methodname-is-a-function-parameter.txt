Natural Text
In this case  is a Class which is initiating in API2. Inside the Class  there is a function called . I want to call  via . So  should result in .Is something like this possible? The only why I could think of is if/elif statements.Finlay I would like it to work when I make a POST to '.../API/register' it will return .
You can write:which uses the built-in  function. In this case you should provide  as text, so:If you want to return the result you should use :


Answer URL
https://docs.python.org/3/library/functions.html#getattr
