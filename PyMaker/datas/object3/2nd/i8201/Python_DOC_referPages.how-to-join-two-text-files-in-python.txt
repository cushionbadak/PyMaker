Natural Text
i have two text files like and then a numbers file 1, 2, 3, 3, 2, 1 now i need to try and join the two files to each otherthis is what i have so far 
The format of the input files looks like it's Python literals (list of strings, tuple of integers), so you can use  to parse it. And then you can use the built-in  function to loop through them together and print them out. Something like this:Here's the output:


Answer URL
https://docs.python.org/3/library/ast.html#ast.literal_eval
https://docs.python.org/3/library/functions.html#zip
