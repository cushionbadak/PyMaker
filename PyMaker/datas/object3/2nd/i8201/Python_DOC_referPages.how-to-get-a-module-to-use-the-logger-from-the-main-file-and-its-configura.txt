Natural Text
How to have get the logger in  to log to the logger in ?The below example is an adaptation of Python's documentation.Assuming a model structure such as:The file  looks like this:Whereas  looks like this:This is how the log looks like after running  (which creates a class instance and calls a method):How to configure  to use the logger in ? That is, if the logger in  is configures to use a certail format and handlers, how to get  to use the same configuration? 
You can simply give a name to  that is used by both modules, e.g. 


Answer URL
https://docs.python.org/3/howto/logging-cookbook.html#using-logging-in-multiple-modules
