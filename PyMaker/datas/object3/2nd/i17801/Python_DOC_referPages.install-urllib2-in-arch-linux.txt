Natural Text
I am trying to install the urllib2 module in Arch Linux as I need to run a Python code.The error that the Python code outpus is:Now, when I runt his code , I get What can I do in this situation?
In ArchLinux, the command  defaults to .  If you neeed to use 2.x, you should run the script with .
As aelfric5578 said,  defaults to But if you wish to run it with python3 then you can search the Arch repos with  which will a return a list of packages you can As a naming scheme, Arch sticks to  for python 3 or  for python2, so it's usually a safe bet to 


Answer URL
