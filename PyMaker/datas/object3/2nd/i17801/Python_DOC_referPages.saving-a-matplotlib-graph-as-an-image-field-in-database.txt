Natural Text
I just started learning  and I want to use it in one of my django apps. So I wanted to know how I can save the graph generated in an image field of my models, so that I can retrive whenever needed. 
 accepts file-like object as the first parameter. You can pass / (according to your python version).Then, use  to convert the string to  (because  accepts only accept an instance of ).


Answer URL
