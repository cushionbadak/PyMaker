Natural Text
I have a function:And some  and  instances:How can I zip over func of pars without knowing the length of pars? Like this:I imagine some combination of map, zip, lambda, ?
You are looking for :Here  applies an arbitrary length  as arguments to , which you can then pass to  as separate arguments. You can't use  here, as it only supports callables with one argument.This presumes you are using Python 3 ; on Python 2 you'd obviously need to use  to handle the infinite sequence of tuples produced by the  generators as an iterable:
Although this probably reads better as two lines:


Answer URL
