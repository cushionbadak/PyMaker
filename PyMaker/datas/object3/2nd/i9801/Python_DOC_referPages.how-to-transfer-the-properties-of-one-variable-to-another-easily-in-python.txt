Natural Text
Here I have 2  and , has a function of , which can return a empty instance of (but I don't know what it exactly did since they are from vendor).  is an interface class, which let  knowing some values for further calculations. here I need to setup  of its every property. I can do like this directly:or I can setup by doingI hope to setup  in a easy way, so that I can keep its address unchanged, I suppose the  method will totally replace the address, which I don't want to.Do you have any suggestions?Thanks in advance!
To copy/transfer the value for all attributes from  to .Probably should filter out all of 's methods and double-underscore attributesThen iterate through the remaining attributes and use getattr and setattr:Similarly, if you have a container with attribute/value pairs:


Answer URL
https://docs.python.org/3/library/functions.html#getattr
https://docs.python.org/3/library/functions.html#setattr
