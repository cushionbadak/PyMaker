Natural Text
When ran, this returns invalid syntax pointing to "":
Only (s) can follow the global statement:where identifiers are plain names such as:when you break that requirement and python isn't able to find an identifier after , a  is going to get raised.What you've entered after the  keyword isn't an identifier; it's a function call which is syntactically recognized by the  after the identifier.


Answer URL
https://docs.python.org/3/reference/simple_stmts.html#the-global-statement
https://docs.python.org/3/reference/expressions.html#calls
