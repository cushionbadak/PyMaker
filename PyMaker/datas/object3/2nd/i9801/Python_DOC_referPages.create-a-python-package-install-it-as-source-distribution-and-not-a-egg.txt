Natural Text
I have a python package which has several data files in a  folder which have to be read when the package is used; these are not python scripts. However when I install my package to test it with  only an  is installed and so the code can't read the  directory.I see other packages like  install with a  and a  folder.How do I make my package install like that?
 creates a Source Archine as a tar.gz inside .  then installs it to the users site-packages directory.More info here.


Answer URL
https://docs.python.org/3/tutorial/modules.html#packages
