Natural Text
I am learning python. Suppose I have my Class named , and two instances named  and . I want to change an attribute of second instance by overloading  operator with  so when I type the  attribute will be set. How is accessing from one instance to another instance's attributes deployed in python?
I think you are interested in how the  special method works.  In essence it works as  where  and  can be instance objects.  See an example of it's implementation here.You would need to overload  in  to return something that updates the  attribute.  Example:


Answer URL
https://docs.python.org/3/reference/datamodel.html#object.__add__
