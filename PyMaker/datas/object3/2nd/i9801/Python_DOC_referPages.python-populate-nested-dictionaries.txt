Natural Text
Starting form a list:I can populate a  with the number of appearences of each item. and get: likewise, for:the same:and get: , and so on.but lets say I want to populate this  in the same fashion:how can I do it?
Let's say you have users and lists already stored to a dictionary. Then you could iterate over all key value pairs within dictionary comprehension and use  to convert the list to  with counts:Output:To break above a bit  returns an iterable of  tuples:Then dict comprehension turns the list to  containing the counts:Finally for each the user name and resulting  are added the to the result .
 is a good option. But according to this question we can use more simple option. Check this out.Output: 


Answer URL
https://docs.python.org/3/library/collections.html#collections.Counter
