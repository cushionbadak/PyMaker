Natural Text
in django models  django rest codeoutput:But I need the name of friends.I want see .I want to see also  from django model.How can I do it?
First of all, in your  in , you would need to actually pass a string like:I'm assuming you have a  field in the  model.To display the friend username, you would do:Ref: Specifying read only fieldsThe output will now be something like:The  is a string representation of the object.To see it, you would do something like: .This would give an output like Ref: object.str(self)


Answer URL
https://docs.python.org/3/reference/datamodel.html#object.__str__
