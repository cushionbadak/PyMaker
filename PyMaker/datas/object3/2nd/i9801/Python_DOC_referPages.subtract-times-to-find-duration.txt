Natural Text
that returns value error: the value types for both are object.what is the simplest way of getting the duration between commencement Time and finished?
You can use  to parse time formats:Note:  returns a float, so you'll want to convert it to an integer if that is not acceptable for your purposes. 
You need to convert these to a  of  since they currently are  (use  to check this).You can do it explicitly:or using apply:Now the difference works:


Answer URL
https://docs.python.org/3/library/datetime.html#datetime.datetime.strptime
