Natural Text
My code is as follows:Whenever I run the program and input "M" or "D" I get the following errors:
 expects a single string as its input. Currently, you are passing it 5 strings. You should do something like the following:
input function takes only one argument. Correct way is something like this:


Answer URL
https://docs.python.org/3/library/functions.html#input
