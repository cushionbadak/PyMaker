Natural Text
I'm trying to create a python script that loops through an array of entries and adds a new day object with a date if that date is not already in the date list.according to this: https://docs.python.org/3/tutorial/controlflow.html it should work, but I'm missing something.thanks--
So you want to append to  the entries in  but only the first one for a given date?I think this is a case for .reads as: there isn't a day in history with this entry's date.If the history is permitted to be a dictionary, where the keys are the entries' dates, rather than a list:Another option is to use a set along with the two lists:
I think your code should work as is:


Answer URL
https://docs.python.org/3/tutorial/controlflow.html
