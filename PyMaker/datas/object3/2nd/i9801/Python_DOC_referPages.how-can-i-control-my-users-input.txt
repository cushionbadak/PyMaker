Natural Text
So I have this basic program that asks the user for his name and SS# and I wanted to do so the user can't input a str or a float where he is supposed to input an int. I tried this but it'll just loop forever because the If statement is checking to see if the input is the data type str or int, How can I do it to check if the variable is an int or a str?
 does not do what you think it does. The result of input() is always a string. If you only want to get certain types of strings, then you have to validate them yourself. For checking if something only contains letters or only contains numbers you can use  and .If you need something more complicated than what these or othe other builtin string methods provide, then you'll probably need to write a regular expression or other custom validation code.
You're trying to compare the value with a . You should instead store the name and check if has only letters () and try a type casting on the security number. If it fails, it's not a valid input. Using  for the SSN is also an option. Something like:or


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.isalpha
https://docs.python.org/3/library/stdtypes.html#str.isnumeric
