Natural Text
How will I be able to count the number of time a list of items are repeated in a string. For example how can I search through string and check the number of times this list has been repeated in string?
Firstly, don't use a name like , it masks the built-in name  [See: Built-in Methods] and that can lead to some subtle bugs.After that, there's ways to do this. The crucial thing to have in mind is that objects like strings come along with a plethora of methods [See: String Methods] that act on them. One of these methods is  which counts the number of times  occurs in your string.So, creating a count for every element in  could be done by using a  loop: transforms the integer result of  to an  object so it can be printed by .This prints out:After getting more accustomed to Python you could use a comprehension to create a list of the results and supply them to :which prints a similar result.Finally, make sure you read the Tutorial available on the Python documentation website, it'll help you get acquainted with the language.


Answer URL
https://docs.python.org/3/library/functions.html
https://docs.python.org/3/library/stdtypes.html?highlight=string%20methods#string-methods
https://docs.python.org/3/library/stdtypes.html?highlight=string%20methods#str.count
https://docs.python.org/3/tutorial/index.html
