Natural Text
In a few  of different classes I have to use several times the constructor as suggested by Moses KoledoyeI would like to have a method that inputs, say, the string  and  and that the corresponding initialization gets produced. For example, if one inputs  and  the resulting code is In this way I can replace a long sequence of these initializations by a loop along a list of all the required members and their default values.This technique emulate a switch statement is not the same thing but kind of has a similar flavor.I am not sure how to approach what I want to do.Assuming that  is the construction that gets replaced by I would then used it by having a lists  and  and callingThis would replace long list of try's and also make the code a bit more readable.
If your  was meant to handle , then you can use the  method of the kwargs dict which allows you to supply a default value:Which can be extended to your list of attribute names using :


Answer URL
https://docs.python.org/3/library/functions.html#setattr
