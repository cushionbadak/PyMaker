Natural Text
I have something like this:It produces:And I need the same but in flat structure.For now I use something like:But is there something less obsucre to achieve this 'unpacking' of arbitrary length list inside comprehension?
The best way to flatten any iterable in a generic situation is :This said, it's preferable to avoid the extra work in this case by just making it flat to begin with.
Use this list comprehension:The above list comprehension is equivalent to this(but LC are much faster):
You can use numpy flatten():
and then  gives:the use of the  in chain(*l) is inspired from this question join list of lists in python . 


Answer URL
