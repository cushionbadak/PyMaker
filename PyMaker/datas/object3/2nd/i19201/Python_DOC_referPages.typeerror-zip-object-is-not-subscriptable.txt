Natural Text
When I run it, it gives me the error:TypeError: 'zip' object is not subscriptableWhat should I do?
In Python 3,  returns an iterator, not a list. You can either cast it to a list:use  to select everything but the first element, or skip the first element with :Note that  returns an iterator as well, not a list. That may be fine for your purposes.If you wanted to find the max of each column, for example, you can combine that:
You are using Python 3, in which zip returns an iterable, not a list.So, by converting it into a list, you can solve your problem.Also, You can skip the first column in the below manner, in which case, you can remove the slice.


Answer URL
