Natural Text
I get log messages with the same date when I print them to the console (or logfile). But the time-out between messages is two seconds.  Here is my codeHere is output:
have a look at the documentation of . the second argument you need to pass is a  ( in your case). the logger will do the  for you.you are passing a string that represents  in this format. as this is a  (e.g. ) the formatter does not complain but always prints this exact date:  will result in  - there are no format specifiers to fill in.what you should do is this:(btw:  is a built-in; renamed your formatter to  such that the built-in is not overwritten).


Answer URL
https://docs.python.org/3/library/logging.html#logging.Formatter
https://docs.python.org/3/library/functions.html#format
