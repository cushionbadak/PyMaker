Natural Text
 is a built-in constant, which, according to the docs on constants, doesn't seem like an implementation detail. When trying to assign to it (which isn't supported as the docs state) the following error message is displayed:Apparently, according to the message, it's a keyword, but:Which seems odd. ,  and , also listed as constants, are contained in the . Why isn't  in the keyword list?
It's technically not a keyword in the sense of the Python grammar. There's a specific check in the compiler that prevents assignment to , but as far as the grammar is concerned,  isn't a keyword, and  is generated from the grammar.


Answer URL
https://docs.python.org/3/library/constants.html#__debug__
