Natural Text
where url is when i run this thing i am getting as output instead of entire file.
You're giving it the response object itself.Try giving it the content of the response like so .
When you make the request in python you're getting back a response object. You need to parse the body of that response instead. So
Read and try the example fromThe ElementTree XML API: 20.5.1.2. Parsing XML As an Element, root has a tag and a dictionary of attributes:It also has children nodes over which we can iterate:Children are nested, and we can access specific child nodes by index:


Answer URL
https://docs.python.org/3/library/xml.etree.elementtree.html#parsing-xml
