Natural Text
Python I have a list of lists. like I want to count how many times each list occurred in the main list. My output should be like
Just use  from :
You can use  - a dict subclass - to take the counts. First, convert the sublists to tuples to make them usable (i.e. hashable) as dictionary keys, then count:
Depending on your output, you can  through a  of your  and print each item with its  from the original :


Answer URL
https://docs.python.org/3/library/collections.html#collections.Counter
