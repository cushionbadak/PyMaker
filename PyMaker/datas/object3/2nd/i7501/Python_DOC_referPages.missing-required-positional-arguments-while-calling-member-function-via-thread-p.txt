Natural Text
I wrote the following two methods, trying to benefit from multithreading:Unfortunately, it doesn't work with error message:Why?
 (and consequently ) takes an iterable and sends it over the enclosed pool one item per worker. This means that your  method gets called as:  i.e. gets a  of all the arguments as the first argument (after the implied ) so it complains that it didn't receive the last two positional arguments.You can remedy that by simply changing your  signature to:  and then read from the  tuple  as ,  as  and  as .If you want to keep the signature as it is, you can write a proxy expander as:And call the  method from your  instead (i.e. )Keep in mind that if you're going to perform some serious processing, you're much better off with the  module,  is still somewhat limited by the dreaded GIL.


Answer URL
https://docs.python.org/3/library/concurrent.futures.html#concurrent.futures.Executor.map
