Natural Text
I have a : keys are strings, values are float.Example: I'd like to get  or  as answer with equal probability. I found a way to get said behaviour. However I'm not convinced this is best practice.Is there a better (or even more elegant) approach?
Try this:First, we find the maximum value and then we randomly pick one of the keys that match that value. We're using , which guarantees a random selection with uniform distribution.


Answer URL
https://docs.python.org/3/library/random.html#random.choice
