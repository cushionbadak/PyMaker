Natural Text
I would like to check the first few characters (the numbers are variable) in a string. E.g.and I would like to check if the first few characters are inI would like to do it programatically, without separating manually based on the number of characters:Thank you!
str.startswith(prefix[, start[, end]])Return True if string starts with the prefix, otherwise return False.  prefix can also be a tuple of prefixes to look for.docsTry this,.Full code,
You can try this:Output:


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.startswith
