Natural Text
I'm trying to make a set which will have all the words the user queried from a string.Here's my questionSuppose the user queries for terms like  , or .I want to be able to add to my  every word that start's with  or . From a quick pass, I see that there are words like , . So these 2 should be added to the set.                                                             
You could just replace the  with e.g.  and use a pattern like  itself as a regular expression to search for.You might also enclose the result of  in  or  (when matching individual words in the split sentence) to account for word boundaries.
You don't need a regex for this, you can use a list comprehension with :Or since your prefixes are always the same length, a simple  check:


Answer URL
https://docs.python.org/3/library/re.html
