Natural Text
Here's my program:Here's my error: Any suggestions?
You named your initializer method ; the correct name is . The double underscores are how Python indicates names reserved for Python "special" use. By not using the correct name, the superclass ('s)  was invoked, but it takes no arguments, so you get the error.Sidenotes: You've got another error in ; the final  and  calls should almost certainly be dedented, so you write all the data, not just a single item.In addition:should almost certainly become (importing  at the top of the file):which handles newlines and splitting on commas for you, and doesn't involve the anti-pattern of an infinite loop calling  over and over (even if this wasn't CSV, the correct way to iterate by line is , which avoids the ,  and ;  avoids the need to explicitly slice off the trailing newline too).
You're missing __. Yourhas  to be


Answer URL
https://docs.python.org/3/reference/datamodel.html?highlight=__init__#object.__init__
