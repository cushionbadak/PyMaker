Natural Text
I'm somewhat new to regex and Python and am in the following situation. I'd like to take an equation string, like , and place the number 1 in front of all variables that have no number in front of them. So something like:After some research and asking, I came up with However, it looks like this only works for the first two variables:Anything really obvious I'm missing? Thanks!
Looks like the  (ignore case flag) is the culprit:This is because the next positional argument to re.sub is , not . You'll need:Unfortunately, the  flag happens to be :


Answer URL
https://docs.python.org/3/library/re.html#re.sub
