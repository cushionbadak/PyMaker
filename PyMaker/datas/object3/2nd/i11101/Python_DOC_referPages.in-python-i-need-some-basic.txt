Natural Text
This question already has an answer here:How can I read inputs as numbers?                    19 answers                when I get results the list comes out like thishow can I remove the '' between each entry  its making me have trouble useing the Mean function to pull a average from the ages list help !!!
The problem is, that  always returns a string. Just use  to turn it into an integer:But be aware that this will throw a , if you don't enter a number. You can handle the error using a try/except, if you want to. You could cancel the input for the current person and print an error message and then start with a new person:


Answer URL
https://docs.python.org/3/
