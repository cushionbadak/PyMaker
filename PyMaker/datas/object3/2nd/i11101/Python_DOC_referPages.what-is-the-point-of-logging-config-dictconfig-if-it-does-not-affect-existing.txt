Natural Text
Configuring a logger using the APIs provided by  as shown in  configures existing loggers. Configuring logging with  does not configure existing loggers as shown by . filesI/OIf a library creates a logger before  is set up, the logger will not be configured properly. How do libraries handle this? Do they expect a logging configuration be passed to them? Do they simply avoid creating loggers during import?
 does affect the configuration of existing loggers. However,  will disable existing loggers by default unless  is defined in the dictionary passed to  according to .


Answer URL
https://docs.python.org/3/library/logging.config.html#dictionary-schema-details
