Natural Text
If I have a nested list like this:Is there a way I split this nested list at every ?So that it looks like this:Any sort of help would be appreciated :)
Try this,
You can use  to group at every occurrence of your split value (here ).  ensures that we leave out the split value itself. 
Seems like a usecase for groupbyitertools.groupby docs
A good old fashioned loop should do it:


Answer URL
https://docs.python.org/3/library/itertools.html#itertools.groupby
