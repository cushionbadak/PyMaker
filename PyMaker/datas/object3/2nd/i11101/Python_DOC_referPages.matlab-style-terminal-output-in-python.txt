Natural Text
I wasn't able to find this question in a few searches. Consider the following MATLAB struct:In a python dictionary (ignore the +1 differences), it might be:Is it possible to get python to print out something like:I wouldn't necessarily want this to be limited to numpy data types but any multi-item element that can get unwieldy. And obviously, I would like to be able to inspect all values if needed, at any time.
Yes, it is possible. All you need to do is to subclass the  class and provide your custom implementation of the  method.


Answer URL
https://docs.python.org/3/library/pprint.html
