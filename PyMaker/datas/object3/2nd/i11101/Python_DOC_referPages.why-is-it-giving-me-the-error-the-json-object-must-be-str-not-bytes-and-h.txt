Natural Text
I was following a tutorial about how to use JSON objects (link: https://www.youtube.com/watch?v=Y5dU2aGHTZg). When they ran the code, they got no errors, but I did. Is it something to do with different Python versions or something?
The  object returned from  reads  data (raw binary data), not  data (textual data), while the  module works with . You need to know (or inspect the headers to determine) the encoding used for the data received, and  it appropriately before using .Assuming it's UTF-8 (most websites are), you can just change:to:and it should fix your problem.
I think they were using a different version of the urllibTry with urllib3 and do the import like this:Hope this is the fix to your problem


Answer URL
https://docs.python.org/3/library/http.client.html#httpresponse-objects
https://docs.python.org/3/library/urllib.request.html#urllib.request.urlopen
