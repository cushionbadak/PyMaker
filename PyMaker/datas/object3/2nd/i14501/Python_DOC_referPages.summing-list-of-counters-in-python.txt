Natural Text
I am looking to sum a list of counters in python. For example to sum:to give I can get the following code to do the summation:But I am confused as to why  results in the error  ? Given it is possible to add counters together, why is it not possible to sum them?
The  function has the optional start argument which defaults to 0. Quoting the linked page:Sums start and the items of an iterable from left to right and returns  the totalSet start to (empty)  object to avoid the :


Answer URL
https://docs.python.org/3/library/functions.html#sum
