Natural Text
I have this functionThis function is present in a class, and I can not understand way it does not work. When I invoke it, I get an empty sequence.Can someone help to make the modification in it in order to return all sub-sequences in list that are successive, as in the example?
In Python 3.x,  doesn't return a list, but a range object.So, you need to explicitly convert that to a list, and then compare, like thisHere, we create a new list, by converting the range object to a list, withDemo


Answer URL
https://docs.python.org/3/library/functions.html#func-range
