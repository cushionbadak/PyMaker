Natural Text
How can I iterate through a list or tuple whose elements as lists with variable length in Python? For example I want to do and then have the elements of  that are short to be completed with, say, . I have found a workaround with the following code but I believe there must be a better way.Where  is actually the length of the "longest" element. Is there a better way?
To match your own output you can use  to fill with 's and transpose again to get back to the original order:If an ,  etc.. would be better then you can specify a different  argument to the  function:Judging by your  statement you are likely to be using python2 but for anyone using python3 as @TimHenigan commented below it is  .If you don't need a list you can use itertools.izip which returns an iterator:
In Python 3 we can do like this:


Answer URL
https://docs.python.org/3/library/itertools.html#itertools.zip_longest
https://docs.python.org/3/library/itertools.html#itertools.zip_longest
