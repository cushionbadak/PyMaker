Natural Text
I have a problem with Python's  bitwise operation:The problem is the conversion from decimal to hex.  is , however Python says that it is . Why?
The value that you already have is exactly the value you want. From a comment:I was using bytes function because I want to concat the result of base64.b64decode(coded_string) with one more byte at the end. creates a  object with a single byte with the numeric value . What that number means depends on how you interpret the bytes. The fact that its  happens to be  isn't relevant. What the value actually is, is the one byte you want. But the  of a  object, as the docs explain, uses the  literal format for convenience. Any byte that matches a printable ASCII character gets represented as that character, a few common values get represented with backslash escapes like , and anything else as a hex escape, all within So,  is , because byte  is the printable ASCII character .If you want to get a string with the hexadecimal representation of the number , you can use the  function—or, if you want more control over the formatting, the  function:But that's not what you want here. You want the value , and you already have that.
65 is not A in hex, it's A in ASCII code;  and  outputs  and , respectively (ASCII representations). Hexadecimal is merely a numeral system with 16 as its base. 0x41 is therefore .


Answer URL
https://docs.python.org/3/library/stdtypes.html#bytes
https://docs.python.org/3/library/functions.html#hex
https://docs.python.org/3/library/functions.html#format
