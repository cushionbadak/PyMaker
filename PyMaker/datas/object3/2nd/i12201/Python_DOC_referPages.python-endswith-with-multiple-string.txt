Natural Text
I have a string:I also have a list:Using the endswith() method, I want to  write an if statement that checks to see if the myString has ends with either of the strings in the myList. I have the basic if statement, but I am confused on what I should put in the parentheses to check this.
 accepts a tuple of suffixes. You can either convert your list to a tuple or just use a tuple at the first place:str.endswith(suffix[, start[, end]])Return True if the string ends with the specified suffix, otherwise return False. suffix can also be a tuple of suffixes to look  for. With optional start, test beginning at that position. With  optional end, stop comparing at that position.
You could use the keyword :
You may do it like this :)


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.endswith
