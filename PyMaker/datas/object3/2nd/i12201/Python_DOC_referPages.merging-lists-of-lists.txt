Natural Text
I have two lists of lists that have equivalent numbers of items. The two lists look like this:I am looking to create one list that looks like this:I was attempting to use  : but the output produces a flat list.What is the best way to combine two lists of lists? Thanks in advance.
You can  the lists and then  the resulting tuples again...If you need lists all the way down, combine with `map:
You were on the right track with the .Here's a shorter alternative to the first version by tobias_k,  together corresponding elements from both lists:As noted in the comments, in Python 2, the brief  is enough. will work for you in Python 3 too, given that you iterate over it just once and that you don't need sequence access by index. And if you need to iterate many times, you may be interested in .A shorter alternative to the second version:Lastly, there's also this:


Answer URL
https://docs.python.org/3/library/itertools.html#itertools.tee
