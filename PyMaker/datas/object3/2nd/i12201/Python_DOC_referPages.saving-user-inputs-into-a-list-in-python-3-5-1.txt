Natural Text
I want to prompt the user for a list of phrases over and over again until they input  and save all of the user's inputs into a list. How can I do that?My code so far:
One way to do is with a :Result:
You can simply use , which returns a . Then we can use  to get a real list:About how does it work, if you take a look at :You can also use a  loop if you think it's more simple and clear anyway:Demo:


Answer URL
https://docs.python.org/3/library/functions.html#iter
