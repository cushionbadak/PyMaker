Natural Text
I would like to write random prime numbers in new lines to a text file, but when I run my script it writes all numbers in one line. It's very likely that I don't understand something correctly.This is my original code:According to the answers, I just have to use the  method with newline character () instead of the  method.
You can just add a newline when writing each number to the file. Also use  instead of . Just use this as your code:
 doesn't add line separators (which seems weird considering  removes them).  You'll have to add them manually.
You need to pass a sequence to writelines, you can use a generator expression and also use  to open your files replacing your while loop with a range:


Answer URL
https://docs.python.org/3/library/io.html#io.IOBase.writelines
