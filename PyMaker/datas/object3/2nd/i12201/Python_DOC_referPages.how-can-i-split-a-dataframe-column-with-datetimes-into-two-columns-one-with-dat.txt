Natural Text
I have a data frame called , which has a column  like this,I know how to add a column to data frame, but how to divide  to 
If your series is , then this will create such a DataFrame:as once you convert the series using , then the  member can be used to extract the parts.Example
If your  column is a string:If it is a timestamp:
If type of column  is string, convert it by . Then you can use ,  and last  original column :
 +  + You can use  with  to add an arbitrary number of  attributes to your dataframe as separate series:


Answer URL
https://docs.python.org/3/library/operator.html#operator.attrgetter
