Natural Text
I have two dicts like this:I want to convert them to JSONs and then merge them like this:I've converted them to JSON by  and now I can't figure out how to merge them the way I want.My question is somehow similar to this one, but the results are different.I tried this:But I get:I tried some other ways but none of them worked how I wanted
You can use the  library for that. converts the json string to a dict.  recovers your json from the dict. The output will be 


Answer URL
https://docs.python.org/3/library/json.html
