Natural Text
This question already has an answer here:Getting a map() to return a list in Python 3.x                    9 answers                Why do I get in Python2and in Python3?What should replace  in Python3 ?I read that one should use list comprehension in Python3 but does not give the desired result and neither do the variations that came to mind.
That's because  returns a  for multiple return values.From Pydoc[2]:If there are multiple arguments, map() returns a list consisting of  tuples containing the corresponding items from all iterables (a kind  of transpose operation). The iterable arguments may be a sequence or  any iterable object; the result is always a list.Whereas  returns an .From Pydoc[3]:Return an iterator that applies function to every item of iterable,  yielding the results.So to get the  in , just do :Basically :


Answer URL
https://docs.python.org/3/library/functions.html#map
