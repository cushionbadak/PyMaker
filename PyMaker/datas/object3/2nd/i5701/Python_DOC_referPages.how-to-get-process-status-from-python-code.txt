Natural Text
I'd like to operate a process using  from python code, in order to get the status and make conditional branches like this: But this doesn't work.  returns . The response doesn't have what the status is.Is there any way to get the status?
I think you need to use  to get the output returned to you rather than just the return code. Run command with arguments and return its output as a byte string.
The python  documentation describes how  returns a  object.From there you can retrieve things like status:


Answer URL
https://docs.python.org/3/library/subprocess.html#subprocess.CompletedProcess
