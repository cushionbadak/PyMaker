Natural Text
Today stumbled upon Literal String Interpolation, the above code gives the output 9,I was trying to put the operation in variable too.Now how can Literal String Interpolation generate the expected output, saw the documentation but could not make much out of it.
You can use :However, use of  is usually not recommended, since it will evaluate any string you feed to it. That includes arbitrary code a malicious user might have put in there somehow. 


Answer URL
https://docs.python.org/3/library/functions.html#eval
