Natural Text
I'm new to python and self learning wherever possible. I'm trying to read data from a  file containing integers into a one dimensional array (python list).The  file looks like this:I tried like this:I get a 'list of lists' (2D array) instead of the linear list I intended.Also, I would like the list to contain integers and not strings.
Actually, you are not getting a list of lists, but a list of strings (the lines in your file), with each string looking like a list. First, you need to split the lines. You could do this using , but since you are having a CSV file, you should instead just use the  module. This will also take care of removing the  for you.Now, we have an actual list of lists: Next, if you want a flat list with all the numbers from all the lines, you should use  instead of , and you can also cast those  to :This gives you . You can also do all this in a single line using a list comprehension:
Try thisIt might help you.


Answer URL
https://docs.python.org/3/library/csv.html
