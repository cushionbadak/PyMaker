Natural Text
I would like to connect two variables totwo different classes but I don't know if what I'm trying to do is possible or not.for instance, if I have those two classes:In the class  I would like connect the variable  with the variable  of class , so I don't have to explicitly call  each time I execute The code in example give me this:Which is not the result I expect.Somebody know if I can do that in python?updateFrom the example below is it possible to use the answer of deceze and replace the commented line with a property ? Like that classes  and  are linked in the class .answer from deceze
Define  as a :


Answer URL
https://docs.python.org/3/library/functions.html#property
