Natural Text
UPDATE:I went with this:   Thanks guys, your ideas helped me.I am working with a list of tuples:Using the following line of code as an example. My list can be of any lenght. However, I will always be looking for a given index of my tuples: In this scenario, I am looking to grab the unique second elements.
It as simple as follow:
You can use a list comprehension.By the way,  is not a good variable name because it is a builtin.
By zipping the tuples together you get a series of tuples at each index. Combine this with  to eliminate duplicates then;will generate a list containing unique elements at each index. 


Answer URL
https://docs.python.org/3/tutorial/datastructures.html#sets
