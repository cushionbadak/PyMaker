Natural Text
I have the following file names that exhibit this pattern:I want to extract the middle two time stamp parts after the second underscore  and before . So I used the following Python regex string split:But this gives me two extra empty strings in the returned list:How do I get only the two time stamp information? i.e. I want:
Don't use , use the  method of regex / objects.You can even name the capturing groups and retrieve them in a dict, though you use  rather than  for that. (The regex pattern for such a case would be something like )
I'm no Python expert but maybe you could just remove the empty strings from your list?
If the timestamps are always after the second  then you can use  and :
or, somewhat more general: 


Answer URL
