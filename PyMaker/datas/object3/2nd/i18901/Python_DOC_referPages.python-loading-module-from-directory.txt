Natural Text
Why python load my module from directory, even if the directory shouldn't be treated as a package (doesn't contains )? this works, even if  doesn't contains If i would create regular  with  and  file somewhere in , will be  loaded from this regular  package?I am using pycharm and python3.3
It looks like the  is optional as of Python 3.3. See also PEP420.


Answer URL
