Natural Text
If I'm given a path as a string, such as "~/pythoncode/*.py" what is the best way to glob it in ?Using pathlib, there is a way of appending to a path using a glob:but this, for example, does not work because the user isn't expanded:and this is generates an exception because I'm providing no arguments to :Is there a way to do this in , or must I parse the string first?
If you're starting from the string  and you'd like to expand and glob, you will need to split the path first. Luckily pathlib provides  and  to help you out:Note this simple solution will only work when only the  includes a glob, it will not work with globs in other parts of the path, like: . A more general solution that is slightly more complex:
I found that I really wanted the inline expansion.  It wasn't as easy as I thought it'd be.  Anyhow, here's what I've got.  Only trivialy tested, but let me know where it falls down for you and I'll edit it.


Answer URL
https://docs.python.org/3/library/pathlib.html#correspondence-to-tools-in-the-os-module
