Natural Text
I am using Popen to remote call scriptstdout = s.communicate()print stdoutThe script is not exiting with the status mentioned under shell script , instead it only prints success or failure status only ..i want to exit with the status codes as per shell script.here is shell scriptHow to get the status code returned ?
From 's documentation:If the return code was zero then return, otherwise raise  . The  object will have the return  code in the  attribute.Therefore, to get the status code, you need to catch the exception and retrieve the  attribute.
Adding a new answer since you changed your code in question from using  to using  and .From 's documentation:Note that if you want to send data to the process’s , you need to  create the  object with . Similarly, to get anything  other than  in the result tuple, you need to give   and/or  too.In other words, you need to add  to your call to 's constructor in order to use .
Found Issue :The tail statement in shell script was holding the SSH session which is why Popen couldn't able to exit the subshell , Adding kill -9  resolved the issue.


Answer URL
https://docs.python.org/3/library/subprocess.html#subprocess.Popen.communicate
