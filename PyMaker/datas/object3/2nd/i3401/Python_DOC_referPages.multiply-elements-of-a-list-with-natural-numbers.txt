Natural Text
I have a list that look like this :I want to multiply each of these numbers by consecutive natural numbers and form a list out of it.for instance I have the following code:Just wondering if there is a method, inbuilt or otherwise, in which I don't have to define s.TThank you.
Perhaps the most "native" way this can be done is by making sure  is a numpy array, then:
You can use ,  and :Here you have a live example
I think a simple python list comprehension can be efficient for your problem.
There are probably many ways to do this:


Answer URL
https://docs.python.org/3/library/functions.html#map
https://docs.python.org/3/library/itertools.html#itertools.count
https://docs.python.org/3/library/operator.html#operator.mul
