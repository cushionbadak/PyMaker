Natural Text
I want to test appendRole which is called getFileAsJson to read file with open.My problem is that I don't know which open will be next. There are many if/elif.
Even you can find an answer to your question at Python mock builtin 'open' in a class using two different files I would like encourage you to change your approach to write tests for  and then trust it. To test  use  to patch , then by  attribute you can instruct the mock to return exactly what you need for your test.  So, after some test on  where you can use  to mock  builtin (maybe you need to patch  too). Your 's test looks like something like this:


Answer URL
https://docs.python.org/3/library/unittest.mock.html#patch
https://docs.python.org/3/library/unittest.mock.html#mock-open
