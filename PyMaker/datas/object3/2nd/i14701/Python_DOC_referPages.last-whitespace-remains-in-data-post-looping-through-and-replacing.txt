Natural Text
I've ran into a bit of a mind boggling issue, I'm trying to remove all whitespace for data keyword.txt and keeping only the string of letters. But after looping through and replacing each whitespace to an empty field a new line is still seen in the output, and therefore messing up another output. I'm not sure what to make of this.Script:A sample of the output this produces:You can see that the  appears in lines due to it recognising an empty space as a keyword. That's the problem...keyword.txt
It seems like the whitespace isn't being grabbed as part of each word but rather as an individual word.Try reading in the files like thisThen loop through the list like you are doing.Other variations include as mentioned in the comments
Your KeywordList may contain duplicate keywords. Try using a  instead:I tried it with this data and it worked...


Answer URL
https://docs.python.org/3/library/stdtypes.html
https://docs.python.org/3/library/enum.html#comparisons
