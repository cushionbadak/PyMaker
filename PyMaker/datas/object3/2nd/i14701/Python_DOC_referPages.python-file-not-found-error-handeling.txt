Natural Text
the error I get 
You need to place the  block at the same level as the  block, and add a colon, and use the right exception:If you only wanted to catch the  call and not include the rest of the  block in the  handler, you need to open the file separately, then use it in the  block:Note that  is not a valid exception to catch, it is only a constant. I used the  exception instead; it is a subclass of  that is thrown when the file you tried to open was not found.


Answer URL
https://docs.python.org/3/library/exceptions.html#FileNotFoundError
