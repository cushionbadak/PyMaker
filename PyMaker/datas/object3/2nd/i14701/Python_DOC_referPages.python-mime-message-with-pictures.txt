Natural Text
im trying to send png images using my mail and python.Here is a script i found :source: https://docs.python.org/3/library/email-examples.htmlMy problem there is that when i want to precise the pngfiles path i write something like :only returning an error message such Its really a silly problem but don't know how to write it properly... Any helps please ? :)Thanks !
You file path:and your python path:are conflicting (1st in Unix, 2nd is Windows). First thing would be to use a real windows path. But you will also need a way to identify which files to attach (png in this case). To do so, you can use the glob package to create a list of files to attach:


Answer URL
https://docs.python.org/3/library/email-examples.html
