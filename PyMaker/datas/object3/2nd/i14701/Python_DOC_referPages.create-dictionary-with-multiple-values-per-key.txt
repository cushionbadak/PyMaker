Natural Text
How can I create a dictionary with multiple values per key from 2 lists?For example, I have:And, I want something to the effect of:I have tried the following so far:
You can use  and a simple for-loop:From the docs:If  is in the dictionary, return its value. If not, insert  with a value of  and return .  defaults to .
You can use collections.defaultdict for such tasks :
This is a bit different from the other answers. It is a bit simpler for beginners.It will returnThis code runs through  and makes each item in it a key for an empty list in . It then goes from 0-2 and appends each item in  to its appropriate category, so that index 0 in each match up, index 1 in each match up, and index 2 in each match up.


Answer URL
https://docs.python.org/3/library/stdtypes.html#dict.setdefault
