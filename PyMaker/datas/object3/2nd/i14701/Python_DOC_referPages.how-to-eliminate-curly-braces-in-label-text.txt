Natural Text
How to eliminate curly braces around "There are total" and "entries in dictionary"
The problem is that you are currently passing a tuple for the  parameter.  Instead, you should be passing a string.  You can use  to insert the dictionary length into this string:


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.format
