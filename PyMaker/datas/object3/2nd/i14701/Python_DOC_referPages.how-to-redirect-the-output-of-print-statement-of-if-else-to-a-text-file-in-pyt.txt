Natural Text
I have this piece of code, and print the output to a txt file. However whenever I open a file, f=open("out.txt",'w') it shows unexpected indent. I guess I am placing the line of code to a wrong position.Can anyone help. 
In Python3 the output redirection is as simple asRefer the docsIn your particular case you can even use the  syntax as inNote - It is always better to use  (append) instead of  incase you are re-executing the program. 


Answer URL
https://docs.python.org/3/library/functions.html#print
