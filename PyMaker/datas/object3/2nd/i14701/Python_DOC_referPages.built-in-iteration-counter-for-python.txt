Natural Text
This question already has an answer here:Get loop count inside a Python FOR loop                    4 answers                I would like to iterate through a list and reference the iteration number that I'm on. I can do it with a simple counter, but is there a built in function for this?
Its what that  is for !enumerate(sequence, start=0)Return an enumerate object. sequence must be a sequence, an iterator, or >some other object which supports iteration. And in iteration you can do :
You should use the built-in function .


Answer URL
https://docs.python.org/3/library/functions.html#enumerate
