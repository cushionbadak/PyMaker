Natural Text
I have huge list of tuples that looks like:I need to remove the ' ' and convert them all to floats, but I am not sure how to do it. I tried this:It however will tell me that a  cannot be converted to type  and I suspect it may be the  part?
You can convert using a try/except block like:Code:Test Code:Results:
If empty strings are the only non-numeric values in your floats, then simply filter them out:Or simply  for identity function:
A simple list comprehension will do the job along with  and .  will be used to filter out  values and  will be used to cast the filtered string to 


Answer URL
https://docs.python.org/3/library/functions.html#filter
https://docs.python.org/3/library/functions.html#map
