Natural Text
I am building an application using Django 2.0 and Python 3, but I am having some issues with parsing a file using a custom classI have created this class:The purpose of this class is to process a  into a flat structure  where each subsequent level would be seperated by a dot, similar to what the  function does in Laravel PHP.This is done, so that I can easily provide a readable implementation of a  file outside of , so that sensitive information such as database credentials are not stored in version control, but still have the values assigned to variables in My example configuration file looks like this:It all runs fine during , but as soon as we are finished processing the  property of the  data, it crashes with  and does not process the rest of the file.How can I resolve this issue without changing the structure of the file
Reiterating my earlier comment:You receive the error  because you assign the output of a void method -  - to the dictionary. The  method reference states:  Update the dictionary with the key/value pairs from other, overwriting existing keys. Return .Your code that does this is The marked line can simply be


Answer URL
https://docs.python.org/3/library/stdtypes.html#dict.update
