Natural Text
I have a base list  which needs to be converted to a list having consecutive elements of each element in the base list in an efficient wayExamples:If I need 2 consecutive numbers then  will be .If 3 consecutive numbers then  will be .

Here's a one liner, assuming the list is x and the number of 'consecutives' is c:

Here is one way.  removes the need for explicit nested loops.


Answer URL
https://docs.python.org/3/library/itertools.html#itertools.chain.from_iterable
