Natural Text
Have the list of lists "d" like that:I would like to save the output using csv module but without id line (so with columns Name, Email and their values). Performing like this:So it saves me the whole line as a value. How is it possible to get deeper to the level of values of the list and save them properly in csv?
You are almost there:creates the file :It is important to  the file with  so you do not get additional newlines into it -  will handle those on its own - you also should specifiy the  - just to make sure.I fixed some other indentation and minor parenthesis errors on your example data on the way and provided the needed imports to get a working example.csv-writer
An alternative can be to use :


Answer URL
https://docs.python.org/3/library/csv.html#csv.writer
