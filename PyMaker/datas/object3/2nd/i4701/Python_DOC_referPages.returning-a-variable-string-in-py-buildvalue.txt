Natural Text
I'm writing the following as a test case in C++:The code compiles and imports, but produces what looks like a reference to a memory location.I was expecting  as a return valueWhat am I missing? Thanks
[Python]: PyObject* Py_BuildValue(const char *format, ...) (or any other Python/C API function) works with C types not C++.In order to fix the problem, use [cplusplus]: std::string::c_str:


Answer URL
https://docs.python.org/3/c-api/arg.html#c.Py_BuildValue
