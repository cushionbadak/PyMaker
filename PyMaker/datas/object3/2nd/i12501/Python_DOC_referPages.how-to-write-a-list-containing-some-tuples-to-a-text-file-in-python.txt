Natural Text
I have a list containing some tuples as well as normal strings like below :       I want to write it in a space separated text file in a format like:
Something like this? convert all elements in  to string object, then  join them by space and returns a string object.You can also use a list comprehension instead of :
Try thisIn the directory where you run this code you will get a file named 'outfile.txt' containing this:
You can join each element of the list by a space via ' '.join(). The caveat is you need to convert everything to string, including the tuples, which you can do using .One possible way of coding is as simple as:


Answer URL
https://docs.python.org/3/library/functions.html#map
https://docs.python.org/3/library/stdtypes.html#str.join
