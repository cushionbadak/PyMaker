Natural Text
So, having the unittest.mock and the code... one can assert .But assertion  fails. How do I assert that?
I'm assuming that you would like to   reference.Here is an example of how you can do your test:Because  object is a  you can use it directly. As you can see in the log  is exactly the same of ->  -> 


Answer URL
https://docs.python.org/3/library/unittest.mock.html#patch
