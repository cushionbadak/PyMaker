Natural Text
I have a list:I would like to use x as a variable for the first element and the result will be the second element of that list.Then print the second element. Can anyone help me on this?
That list is easily convertible to . The simplest approach I can think of:If you don't want to convert  to dictionary, a simple loop would look likeIt can also be written as a generator expression:Dictionary lookups are usually faster than this and you should use the first solution if you can.Also, try not to use names of built-in types (e.g. ) and functions as variable names.
If that's what you wanted:Input:JohnOutput:Barnes
IIUC you could do it with  module and :Or you could just compare it with x:
A simplistic way to do this would be :That's about it .


Answer URL
https://docs.python.org/3/library/functions.html#func-dict
https://docs.python.org/3/glossary.html#term-generator-expression
