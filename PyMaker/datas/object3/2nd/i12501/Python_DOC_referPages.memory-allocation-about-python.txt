Natural Text
I run the following python code.The result is:I don't know why a.k and b.k have the same memory address.
Because that empty list you're assigning to  is created once when the function is defined, not each time the function is called.Hence  for both classes will point to the same list and therefore changing one will change the other (or, more correctly, changing one will change the only one).If you want to ensure each object of the class gets its own list, simply use the slicing method:


Answer URL
https://docs.python.org/3/tutorial/controlflow.html#default-argument-values
