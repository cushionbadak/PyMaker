Natural Text
This question already has an answer here:How can I read inputs as numbers?                    19 answers                t ask the user for input and when they type letters it comes up with an error message (as i want) but even when user enters number it comes up with error. I want it to come out of this loop and on to next for name if correct data is inputed. 
 always returns text - even there are only digits.Use  to convert to integer or  to check if there are only digits in text.BTW: both methods are useless if user use spaces or  in phone number ;)   Maybe you will have to remove spaces and  using  before you use  or 
You need to convert to an int:


Answer URL
https://docs.python.org/3/library/functions.html#input
