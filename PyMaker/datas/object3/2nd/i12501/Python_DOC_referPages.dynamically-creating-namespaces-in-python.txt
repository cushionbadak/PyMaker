Natural Text
I want to create an object (say ) with dynamically created attributes. I can do something like this:However, when I create the attribute, I don't know the name of the attribute yet. What I need is something like this:How can I achieve this?
setattr:Or, as I originally answered before shmee suggested setattr:
You could use a dictionarythen create a get function to easily access it


Answer URL
https://docs.python.org/3/library/functions.html#setattr
