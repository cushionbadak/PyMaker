Natural Text
Quote from here:The type of coroutine objects, created by async def functions.Quote from here:Functions defined with async def syntax are always coroutine functions, even if they do not contain await or async keywords.Python console session:Why doesn't  evaluate to ?
There's a difference between coroutine and coroutine function. The same way as there is a difference between generator and generator function:Calling the function  returns a coroutine, e.g.:If you need to tell if  is a coroutine function (i.e. would return a coroutine) you can check with:
 by itself is not a valid Coroutine function when used that way:This is similar to the difference between a generator and a generator function. Instead, use  to compare:You could also try  instead, much more shorter and neater:Read more here: https://docs.python.org/3/library/asyncio-task.html


Answer URL
https://docs.python.org/3/library/types.html
https://docs.python.org/3/reference/compound_stmts.html#async-def
https://docs.python.org/3/library/asyncio-task.html
