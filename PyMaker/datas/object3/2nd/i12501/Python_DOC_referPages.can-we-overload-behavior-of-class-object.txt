Natural Text
This question already has an answer here:What are metaclasses in Python?                    15 answers                I know we can overload behavior of instances of a class, e.g. - We can change the  result of :Can we change the result of ?
You can use a metaclass:Python 3:Python 2:Output:A metaclass is the class of class. Its relationship to a class is analogous to that of a class to an instance. The same statement is used. Inheriting form  instead from  makes it a metaclass. By convention  is replaced by .


Answer URL
https://docs.python.org/3/reference/datamodel.html#customizing-class-creation
