Natural Text
I'm pretty new to python, and I wonder how I can print objects of my class fracture. The str funcion is set properly, I guess  because ofNow I'd like to print it properly as a sorted array, and I hoped to get something likeBut this didn't work for my fracture or even for numbers (like )  All I got was:Is this really the best solution? That's quite messing up the code, compared on how easy this works with strings...
If you want to print "1 < 2 < 3" all you need to do is change the type from an int to a string as such:
You have to convert the ints to strings first:
You can convert your array using :
You can convert integers to string.


Answer URL
https://docs.python.org/3/library/fractions.html
