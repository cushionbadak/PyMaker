Natural Text
I have Anaconda 3 installed in  and the Gurobi optimising software installed in . I want to be able to load the Gurobi libraries after having started  (i.e. the Anaconda python - not the system python) with the  command. Currently, anaconda python does not search there as the codesuggests - it returns a list of libraries in the anaconda directory. The command  works with  however - as this uses the PATH environment variable.I realise that I could append the Gurobi directory to , but I want to alter the root  of Anaconda because the library should be accessible by default.In the normal system environment, this is a simple matter of altering the PATH and LD_LIBRARY_PATH environment variables with files in . What would be the analogous solution for using Anaconda python?EDIT: To import gurobipy, it's actually  that needs to be added to , not anything in the  directory.EDIT: This is on Linux (Ubuntu 14.04).
You can always add it to the environment variable , which will be prepended to :It's a hack to add  to your  when using Anaconda's Python. The Anaconda distribution is different from the OS version, possibly in ways that can cause you some headaches.


Answer URL
https://docs.python.org/3/tutorial/modules.html#the-module-search-path
