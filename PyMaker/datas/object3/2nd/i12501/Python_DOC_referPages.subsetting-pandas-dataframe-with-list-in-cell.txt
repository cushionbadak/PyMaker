Natural Text
Suppose I have the following dataframeI know I can subset with:How do I subset so that it matches a string INSIDE of a list? in this example, subset the rows that don't contain 'nodata' in col3?doesn't seem to work and I can't properly seem to access the 'right' item inside of the list.
Rather than converting data types you can use  with a  function which will be a bit faster. Testing it on a larger dataset:My solution:Previous solution:Arguably the first answer may be more readable though. 
Your code should work if you convert the column's datatype to string:


Answer URL
https://docs.python.org/3/reference/expressions.html#lambda
