Natural Text
This is the method I have used tried to find some mistakes with the code all resulted in this error
Usage of  is okay.But,  does not match  because the middle part of the string contains only 2 digits. To prevent the error, you need to guard the last statement:UPDATEIf you don't want to match , you need to use word boundary ():
It works -- group is the right method. However, you need a capturing group in you regex, and the regex is also a bit wrong. Use this code instead:


Answer URL
https://docs.python.org/3/library/re.html#re.match.group
