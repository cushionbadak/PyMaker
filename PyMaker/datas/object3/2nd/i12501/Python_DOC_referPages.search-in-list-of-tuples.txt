Natural Text
I extract a dataset row:I would like to find 'content-type'.I tried dict, list, enumerate, str, ...But nothing works: (
The string in your tuple looks like a list, I'd suggest use  to convert it to a real list, and then convert it to a dict (simply run  function), and use  as the key to access the  (value):Output:


Answer URL
https://docs.python.org/3/library/ast.html#ast.literal_eval
