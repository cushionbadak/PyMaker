Natural Text
I am creating a python movie player/maker, and I want to find the number of lines in a multiple line string. I was wondering if there was any built in function or function I could code to do this:
If newline is  then .The advantage is that you don't need to create an unnecessary list as  does (the result may differ depending on ). accepts more characters as newlines: .
You can  it and find the length of the resulting :Or you can  the number of newline characters:Or you can use  and find the length of the resulting :
You can do:


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.splitlines
