Natural Text
I'm having trouble importing my own Python file. My file structure looks as such:In the  file there is a method,  that uses  to parse the  file to gather values for settings that are stored in a dict.The contents of  are simple: contains:And  just contains the method . The method works fine; it was originally in the  file, but for cleanliness, I wanted to move it to its own file.However, when I run the caboose.py file, I get this error:I'm sure there's some fundamental part of importing modules/files in Python that I'm not understanding, and I've decided to ask for help. Thank you for your time and any possible answers!
You are facing the issue because your first item  still remains at  directory when the bot's  is run. is used for doing initialization logic for the module, you do not need to import  into your  file.Leave the  file empty and do the following in your  - If you really need to import  in your  file, try -
EDITED First read this - https://docs.python.org/3/tutorial/modules.html#packagesThe  files are required to make Python treat the  directories as containing packages; this is done to prevent  directories with a common name, such as string, from unintentionally  hiding valid modules that occur later on the module search path. In  the simplest case,  can just be an empty file, but it can  also execute initialization code for the package or set the   variable, described later.There are 2 options - Eeither use the module export If you wish to use the  then do this - First add and  to export the method from package - File: :File: :OR Ignore the  completely and remove everything from inside it. Then the code in  will work automatically.File: :


Answer URL
https://docs.python.org/3/tutorial/modules.html#packages
