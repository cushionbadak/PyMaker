Natural Text
I created a dictionary for peoples' names as such:What I want to do is call each name and combine them as a string, being separated by a plus sign. Below is a tedious way of doing it:This line is from a larger module I am attempting to create. I tried to following for simplicity, but it resulted in a syntax error:Is there a way of simplifying the above so that it prints:firstname+secondname+thirdname
In python 2 you have to use the slightly hackyHowever, as GingerPlusPlus points out, in python 3 you can get away from this  chicanery and instead use 
If you can use  instead of  to store names, I'd suggest:


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.format_map
