Natural Text
Here is the problem:I want to get something like this:'.-   -... --- -.--'Yet I can only get :'-.---....- ---'The result is the same even though I type "yob a"
You can use  and a generator expressionThis will replace all of the letters, but will preserve the space characters.Details takes a sequence of  and concatenates them into a single string.The syntax  is called a "generator expression". It is a compact 1-line equivalent to a  loop. You should also research "list comprehension" as this is a very similar technique, but creates a list instead of a generator. is a safer alternative to lookup a key from a  than , because it will not throw an exception if that key doesn't exist. It will either return , or the second argument you pass in.
You need to iterarte over the input string and then append the corresponding value for the key (char) to an  variable ( in the below example).
The issue is that you are iterating over the dictionary first, so you are adding conversions for whatever you are finding inside the string into the output variable , which is not in the correct order.You can try -
Try this


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.join
https://docs.python.org/3/reference/expressions.html#generator-expressions
https://docs.python.org/3/library/stdtypes.html#dict.get
