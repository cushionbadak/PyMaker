Natural Text
I've already browsed through the mock documentations, and i've seen some examples where mock has been used. But, being a newbie, i find it hard to use mock in my tests.  - I am creating a test to make sure that a function DOES NOT get called whenever i create an item. The function  that i'd like to mock is part of a class . What i got so far is this : So, how do I mock the method? I'm quite confused on how to use it, because there are different ways in the resources i found. I'd really appreciate your help.  
When using the patch method as a decorator, you need to specify a second parameter to your test function:Note that the mocks should be used only when you've tested the patched method or object in another test.Where to patch? -> https://docs.python.org/3/library/unittest.mock.html#where-to-patchEditThis patch is gonna to avoid a real call to . However, after reread your post I can see you want to assert your function doesn't get called.Hopefully, the  attribute is present in every mock you make, so for that you can use:


Answer URL
https://docs.python.org/3/library/unittest.mock.html#where-to-patch
