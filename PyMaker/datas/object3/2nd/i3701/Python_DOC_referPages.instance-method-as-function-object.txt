Natural Text
I need to pass an instance method as an argument to a function:is there a better way than the ?It appears that  works too, but is this an accident or an official feature?
 returns a callable that will call a method on a given instance passed to it.
You can do this:Here,  is the method without an instance.  It does the same thing as your lambda.  It is definitely an official and supported feature.


Answer URL
https://docs.python.org/3/library/operator.html#operator.methodcaller
