Natural Text
I have multiple str-variables and I want to concatenate them to single line but with separator:As you can see, it looks disgusting and unefficient.So, is there any way to make it simpler by adding separator automatically between separate strings?P.S.I like the way it works in print function:
Use  function:   Update:Also you can use your custom method:In this way you can add separator before every words.Like:
You can use :Output:
I would use , like:Does that answer your question?
Yes, you can use :
Use :If your variables are strings, you can simply do:If they aren't, you have to convert them to strings:


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.join
