Natural Text
I came across a code that looked like this:and I came to the conclusion that this State class could inherit the  class in the same way.What does inheriting from  gives me that inheriting from the regular class  won't? What is the difference between them?
From the python Docs:Enum: Base class for creating enumerated constants. and:IntEnum: Base class for creating enumerated constants that are also subclasses of int.it says that members of an  can be compared to integers; by extension, integer enumerations of different types can also be compared to each other.look at the below example:and they will behave same as an integer:
IntEnum is used to insure that members must be integer i.e.This will raise an exception: 


Answer URL
https://docs.python.org/3/library/enum.html
