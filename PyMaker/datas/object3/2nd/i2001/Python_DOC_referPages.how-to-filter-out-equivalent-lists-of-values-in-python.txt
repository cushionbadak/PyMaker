Natural Text
When given a list of lists like: I want to reduce this list to this:  or I've already tried the following: But the problem with this method is that it turns my lists in sets and while this works - it filters out all the equivalent lists - it also eats all of my duplicate values; When given  it returns , due to the nature of sets in python. Is there another way of doing this?Any help would be grately appreciated.
You could do :OutputAs @soon mentioned you could use a frozen Counter, for example:OutputThe above approach also uses a  for removing duplicates instead of a .
A more straightforward approach would be,


Answer URL
https://docs.python.org/3/library/collections.html#collections.Counter
