Natural Text
What is the proper type annotation for an object that should be unpacked with the  operator, i.e.  in this (random) example:I know that  can unpack tuples and lists, but is it limited to these two classes or does it have any kind of interface that would make it usable for other collections?
Python unpacks any iterable (so a , ing, , ionary, etc.). You thus can use an  for that, like:In case the items that are unpacked should be of a specific type, you can specify this between square brackets, like:


Answer URL
https://docs.python.org/3/library/typing.html#generics
