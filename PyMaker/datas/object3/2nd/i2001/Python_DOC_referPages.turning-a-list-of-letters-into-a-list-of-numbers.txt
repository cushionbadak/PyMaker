Natural Text
I need to turn a list of numbers into a list of letters.More specifically, I need to read  and turn it into , such that the first distinct letter appearing in  assumes in  the number , the second distinct letter assumes the number , the third distinct letter assumes the number , and the fourth distinct letter assumed the number . Example:I need to write a function that turns any given  into  according to this rule.
You could use a dictionary and setdefault, from the documentation:If key is in the dictionary, return its value. If not, insert key with  a value of default and return default. default defaults to None.Code:Output
I think you can use list index and have a simple loop like this

This could be done easily with a dictionary. Try something like this.now  should equal 
Another way to do it by using pandas and list comprehension:


Answer URL
https://docs.python.org/3/library/stdtypes.html#dict.setdefault
