Natural Text
With the  module you can write where  is a class. My question is: what are the brackets following the class name. Which syntactic aspect of python allows that syntax? How could I do something similar in my own classes?
Thanks to  for pointing me to the right direction. Turns out that it does use . However, since  refers to an instance, you must define it in something that the current class is an instance of. So, you have to use a metaclass. EgNow, withyou get


Answer URL
https://docs.python.org/3/reference/datamodel.html#special-lookup
