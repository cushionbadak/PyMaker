Natural Text
In my test case, I assume that if two values are  then they are equal. What is the way to express it using  assertions? The two common functions presented below are not handling this case.A solution that is working for me right now is using a boolean expression inside :
It does not make sense to check two NaNs for equality. The best you could do is to check it in a different way, for example:
You could check if each of them is NaN separately. To do that, I suggest using the following class:It would be as easy as:
You can use math.isnan


Answer URL
https://docs.python.org/3/library/math.html#math.isnan
