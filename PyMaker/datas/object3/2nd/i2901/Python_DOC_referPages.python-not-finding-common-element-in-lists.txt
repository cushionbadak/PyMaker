Natural Text
This question already has an answer here:Why does comparing strings using either '==' or 'is' sometimes produce a different result?                    14 answers                I'm building a program in Python 3 that needs to go through two lists and count how many times the elements of the first list appear in the second. However, even if I plug in two lists that are hard-coded to have common elements, Python says the list doesn't have any common elements.Here's a minimal, runnable version of my program:Output:
Your approach is correct. The only problem is within your , where you compare the two strings using . You should be comparing them using  because you are doing equality comparison. 
 was made for this!
You can make it very short and clear, there are multiple ways but here is one:Or you could simply use your code because it seems to be working for me. 
If you just want to get common elements, then try sets:


Answer URL
https://docs.python.org/3/library/collections.html#collections.Counter
