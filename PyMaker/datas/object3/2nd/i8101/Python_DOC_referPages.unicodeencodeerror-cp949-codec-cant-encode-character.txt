Natural Text
How do I handle this?wfile.write(data['text']+'\n') UnicodeEncodeError: 'cp949' codec can't encode characterI get this error by modifying the Internet.TypeError: write() takes no keyword argumentsTypeError: write() takes exactly one argument (2 given)
 is the default locale for your Windows system, and that's what  defaults to. From the  documentation:encoding is the name of the encoding used to decode or encode the file. This should only be used in text mode. The default encoding is platform dependent (whatever  returns), but any text encoding supported by Python can be used.Specify a different codec when opening the file:Note that you don't need to pre-pend  when opening a file without a path, the default is to use the working directory for relative paths:You'd be better off using  to build paths for everything else.Your code can otherwise be simplified further with  and a context manager. The  dictionary is not really useful here, just reference  everywhere:


Answer URL
https://docs.python.org/3/library/functions.html#open
