Natural Text
I wrote a Python script to run several NS-3 simulations with different seeds and different node counts.My script calls  then executes 10 seeds, change node count and execute 10 more seeds, and so on.The problem is that after I call my script, I ask the user for an input (which scenario to run). Because of that  call, I couldn't use . I also tried , , and . But that didn't work either.Here's my script:Any help will be much appreciated.
Use  instead of  Avoid to use   if  don't need it.Python 3.6 » Documentation » 17.5. subprocess — Subprocess  Execute a child program in a new process.


Answer URL
https://docs.python.org/3/library/subprocess.html#subprocess.Popen
