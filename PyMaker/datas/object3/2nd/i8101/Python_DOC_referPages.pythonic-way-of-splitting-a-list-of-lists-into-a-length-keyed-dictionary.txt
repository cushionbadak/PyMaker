Natural Text
I can do this, for example, by But I can't help feeling there should be a more pythonic way to achieve this.What is it?
As discussed in the comments below, the necessary sorting of the input is a step which is unnecessary costly.  For large input this will slow down this algorithm way more than necessary.  Consider using @hiroprotagonist's solution instead then, or replace the  by a  which can handle unsorted input.
you could use a :python s also have a nice method called  (that way you do not need to import anything):
1 liner:
Gives me


Answer URL
https://docs.python.org/3/library/collections.html#collections.defaultdict
https://docs.python.org/3/library/stdtypes.html?highlight=setdefault#dict.setdefault
