Natural Text
I am very confused that this is not working as I expected:The output is:While I was expecting:What is going on? Why isn't  true?
If you take a look at the Python documentation:6.10.3. Identity comparisonsThe operators  and  test for object identity:  is true if and only if x and y are the same object. Object identity is determined using the  function.  yields the inverse truth value.Thus,  does not check if an object is an instance of a class but if two objects have the same identity. For that you use the builtin function :


Answer URL
https://docs.python.org/3/library/functions.html#isinstance
