Natural Text
If I have a structure like such:Why is that when I call  I get the file path of where the Python script is located plus the ? Looks like:  Needs to be: This is the code I have for a function where the problem is resulting:Shouldn't  give me the full path of each file that was found in the ?
Why does os.path.abspath() return the path of cwd+file?Because that's quite literally what  is supposed to do:os.path.abspath(path)Return a normalized absolutized version of the pathname path. On most  platforms, this is equivalent to calling the function normpath() as  follows: .(Emphasis mine)
 has no idea where the name  came from -- it doesn't know it came from  of some directory. So it doesn't know that's the correct directory to use as the prefix. A relative pathname is always interpreted relative to the current directory.If you want to create the desired absolute pathname, use :


Answer URL
https://docs.python.org/3/library/os.path.html#os.path.abspath
