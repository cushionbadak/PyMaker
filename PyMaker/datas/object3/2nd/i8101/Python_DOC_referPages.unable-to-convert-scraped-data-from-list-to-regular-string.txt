Natural Text
When I run my crawler it fetches the results as list. However, I expected to have that in regular string being displayed in two columns. Thanks for any suggestion.Having results like this:But, I expected the output like:
Try iterating over both list sequentially, like this:
Your  and  actually don't contain a single element, but both contain lists of all the titles and links respectively (those XPath expressions match multiple elements).So in order to get a list of  pairs, you need to  them together:Once you got that, you can iterate over those pairs using a  loop, and print the items left justified so you get your columns:(See this answer for details on how to print left aligned items).Everything together:
You can loop over each link and print the title and url.


Answer URL
https://docs.python.org/3/library/functions.html#zip
