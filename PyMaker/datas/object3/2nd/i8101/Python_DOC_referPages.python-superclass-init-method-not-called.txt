Natural Text
When I run the following program in Python 3.6:I get the following stack trace:As you can see from the  class source code I'm calling its  method with the correct arguments and 's  attribute is set within it.What is happening is that the  method doesn't get called. What am I doing wrong? It must be something silly, but I just can't see it.
The C accelerator that's imported for  doesn't have the  attribute set. A somewhat viable option is to disable it's importing by setting it's value as  in :Of course, in order for this to work, it needs to be done before any imports of  occur and, obviously, you'll get a performance hit.As for why this is, I'm guessing the  attribute is considered part of the private API for , no documentation for it exists (and it doesn't look like an omission). As such, it should not be relied upon. 
For python 2.X your code is fine but for python 3.x you will have to change this to 
If you just want to print starting element's tag name, you can use  which does not require patching the parser object.filepath can be a xml file path or a file-like object.


Answer URL
https://docs.python.org/3/library/xml.etree.elementtree.html#xml.etree.ElementTree.iterparse
