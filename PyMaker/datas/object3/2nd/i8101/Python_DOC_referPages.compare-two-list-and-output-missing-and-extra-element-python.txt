Natural Text
I've 2 lists(sorted) of prefix and would like to compare it in Python so that I can output which element in the original list was missing and which was added.Eg.I want to compare the 2 lists and output the add/remove element in list1_original. ie:
If there is no duplicates in given lists you may use sets and their "-" operator:this prints
I know you mentioned Python, but FWIW, if this is just for a utility script, this can be accomplished with  or .If your lists are already sorted,  will output three columns, one for containing the things common to both lists, one with the things found only in  and one with the things found only in . You can supress output of certain columns with the , , and  parameters (respectively).Of course, you could always jusg .If you really want to do this in Python, you might have good luck with . I've never actually used it, though, so I'm speculating.
You can get the result using a loop and some conditional statements. Same thing can also be framed like so,


Answer URL
https://docs.python.org/3/library/stdtypes.html#set
https://docs.python.org/3/library/stdtypes.html#set.difference
https://docs.python.org/3/library/stdtypes.html#set.difference
