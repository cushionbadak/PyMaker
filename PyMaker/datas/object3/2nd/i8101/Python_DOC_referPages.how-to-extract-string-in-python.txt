Natural Text
I want to extract specific string from a string, but it shows error. Why can't I use the find as index to extract string ? Here is my code
You should change this line:withYou don't need to put anything rather than the index of the letter to get  and you found the index with  already.If you mean substracting the  letter from  then use:
I'm pretty sure you slice strings like this  
 isn't a valid syntax in python, you should rather do:Since an omitted first index defaults to zero, this returns the same result:I'd recommend reading this page for reference on Python string's slicing and it's syntax in general.
maybe you can try it like this two 


Answer URL
https://docs.python.org/3/tutorial/introduction.html#strings
