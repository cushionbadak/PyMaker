Natural Text
I have a txt file with .  There is a space separating the first two columns and then semicolons serve as the delimiter thereafter.Here is what I tried before realizing the file wasn't uniform:How can I read this file in and then be able to manipulate the data and/or append identical files to this one?
To parse one such line from your file, split on the first space with , then split again on semicolons:To concatenate multiple of these files, use a loop:In this case it's not even necessary to split each row into a list. We can just replace the first space in the line with a semicolon and write it back. To do this, I used .
This can be done with a single split:and the reverse operation:



Answer URL
https://docs.python.org/3/library/stdtypes.html#str.split
https://docs.python.org/3/library/stdtypes.html#str.replace
