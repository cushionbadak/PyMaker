Natural Text
I can write  and get  in return or I can type  and get  in return. Also,  returns  as the response. Is  a function (which converted a numeric  to ) or a class, which has methods such as , etc.?
It is a class, whose constructor can get any Python object, and attempts to convert that to a string according to rules built-in the language.The first thing it tries to do is to call the  method in the passed in object. If that does not exist, it tries to call the  method. Whatever is returned is used as the new built string.However, str is trully the  string class in Python, were all string methods are defined. 
 is a class.  As seen in the documentationIs creating an object of type .  It calls 's constructor.  Which in turn calls the objects  function.As suggested in the comments, you can take a closer look at what's happens using the  keyword:


Answer URL
https://docs.python.org/3/library/stdtypes.html#str
https://docs.python.org/3/library/functions.html#func-str
https://docs.python.org/3/library/stdtypes.html#str
