Natural Text
I have a working selenium program that contains this code:(see Scraping Duckduckgo with Python 3.6)When the program runs, the two print statement showQuestion: If you use an object for the [if conditional], likeis python simply substituting  for the conditional? Is it always the case that  is False if ? I wouldn't think that a programmer could tamper with that. You could then equivalently writeBefore posting this I just found Boolean value of objects in Python, and that is certainly helpful.
The documentation is very clear on how values are evaluated in a boolean context:In the context of Boolean operations, and also when expressions are used by control flow statements, the following values are interpreted as false: , , numeric zero of all types, and empty strings and containers (including strings, tuples, lists, dictionaries, sets and frozensets). All other values are interpreted as true. User-defined objects can customize their truth value by providing a  method.


Answer URL
https://docs.python.org/3/reference/expressions.html#booleans
https://docs.python.org/3/reference/datamodel.html#object.__bool__
