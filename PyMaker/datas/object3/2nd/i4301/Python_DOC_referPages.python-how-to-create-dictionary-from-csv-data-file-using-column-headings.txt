Natural Text
I am trying to create a function that accepts the name of a .csv data file and a list of strings representing column headings in that file and return a dict object with each key being a column heading and the corresponding value being a numpy array of the values in that column of the data file.My code right now:The sample CSV looks like:The columns file looks like:The end result should be a dictionary like this:
You can use a DictReader which parse the CSV data into a dict:
A simple pandas solution:If you want to stick with regular python:Or if you're a master of being pythonic like Adam Smith:


Answer URL
https://docs.python.org/3/library/csv.html#csv.DictReader
