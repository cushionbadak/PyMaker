Natural Text
A text file has information about after class activities (name, price per month, days and time) that looks like so:To represent the course catalogue, I've created a nested dictionary in a format like this:And the code looks like this:My question is : is there a simpler way to implement such dictionary? Or maybe another data structure should have been used to represent catalogue rather than this one? 
This is one way using a nested dictionary structure via .
I would simply write a class.


Answer URL
https://docs.python.org/3/library/collections.html#collections.defaultdict
