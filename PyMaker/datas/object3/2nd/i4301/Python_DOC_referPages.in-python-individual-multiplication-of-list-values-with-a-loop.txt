Natural Text
This question already has an answer here:How to perform element-wise multiplication of two lists in Python?                    14 answers                If I have a list , how do I multiply each element of  with each element of another list  and sum the values?The answer should be . I am looking for some kind of  loop.
Try this (using a for loop, as requested by the OP):Another (quicker) way (using  comprehensions):You could also try this (if you have  installed):
Use  to perform index wise operations between two lists:Or a  solution using  and :
Use this creates a zip object of tuples where each tuple  corresponds to the values  and That is if Then, 
You can use  to do this.


Answer URL
https://docs.python.org/3/library/functions.html#zip
https://docs.python.org/3/library/functions.html#zip
