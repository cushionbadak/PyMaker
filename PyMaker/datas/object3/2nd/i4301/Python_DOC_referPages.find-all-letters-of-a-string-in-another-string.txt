Natural Text
I'm working on a project that find all anagrams of a word with many others words.For that I need a function that takes 2 strings and return True if all letters of str1 are in str2.I made this code:For exemple:But is there a better/faster way to do that ?
i prefer using count() function and by the way you don't need to convert the string into list:output:
You can use , which essentially represents a multi-set:The code could be simplified to  if  supported subset-testing with  like sets do, but unfortunately it doesn't.Output:
With  object to get letter counts at once:Test:
Using a  with  should be faster. Ex:Output:


Answer URL
https://docs.python.org/3/library/collections.html#collections.Counter
