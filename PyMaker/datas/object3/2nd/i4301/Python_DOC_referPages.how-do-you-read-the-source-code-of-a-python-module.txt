Natural Text
I'm trying to understand how some built-in Python modules work under the hood.For example, with the code:Where is the definition of the function ?I've been searching through the tkinter source code but cannot find it.The code several time calls  which is also strange because this is , so why is it importing itself?Hope someone can help resolve my confusion
 is in:Py 2.7: Py 3.6: .  In general, when you , if  is a module implemented using Python code, and there is a  in Python's search path, that file runs.  Some more information here, plus official docs on modules for Python 2 or Python 3.  That may not be the case for modules built in to the Python interpreter, such as 2.7's . specificsIn Python 2,  is a Python module and  (lowercase) is a C module that  uses (source).In Python 3,  (lowercase) is a Python module and  (with underscore) is the C module that  uses (source).
Although cxw's answer is accurate and correct, I would suggest installing IPython, if you find yourself looking through the code of Python modules often. It's a better version of the python console:Then, to see the code of a class / function / module, type its name in and add :


Answer URL
https://docs.python.org/3/tutorial/modules.html#packages
https://docs.python.org/3/library/tkinter.html#how-tk-and-tkinter-are-related
https://docs.python.org/3/library/inspect.html#inspect.getsource
