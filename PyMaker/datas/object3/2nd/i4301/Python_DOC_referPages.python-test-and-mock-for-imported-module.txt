Natural Text
I have a module that imports another module like this:now I want to test this file like this:this part works well and does what I want.BUTthis affects other tests file...I mean I have other tests that use the "urls.fetch"and now instead of getting the proper flowthey get the above mocked response.Any idea?quite sure its not related but Im using  to run my tests
Use  in a context to define the scope where the mocked  should be used. In the example below, outside the  block the  is reverted to the original value:


Answer URL
https://docs.python.org/3/library/unittest.mock.html#unittest.mock.patch
