Natural Text
I need to list all the attribute of a class that are properties and have a setter. For example with this class:I need to get the attribute c but not a and b.Using this answers: https://stackoverflow.com/a/5876258/7529716i can get the property object b and c. But is their a way to know if those properties have a setter other than trying:Thanks in advance.  
 objects store their getter, setter and deleter in the ,  and  attributes, respectively. If a property doesn't have a setter or deleter, the attribute is set to .This means you can simply filter out those properties whose  attribute is set to :


Answer URL
https://docs.python.org/3/library/functions.html#property
