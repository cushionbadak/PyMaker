Natural Text
I would like to check if a value exists in every list. The following returns  as expected, but seems un-pythonic. What is the correct/more elegant way to do this?
You can use  and a generator expression:Demo:In addition to being more readable, this solution is more efficient since it uses lazy-evaluation.  It will only check as many items as is necessary to determine the result.Also, there is never a good reason to do:or something similar since  already returns a boolean result.  All you need is:For the negated version, use:


Answer URL
https://docs.python.org/3/library/functions.html#all
https://docs.python.org/3/reference/expressions.html#grammar-token-generator_expression
