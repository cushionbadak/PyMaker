Natural Text
For example, we have a class:what is the function call to replace:I want to apply it with  function.
The functional equivalent of doing   is to use :From the documentation:Return the value of the named attribute of .  must be a string. If the string is the name of one of the object’s  attributes, the result is the value of that attribute. For example,   is equivalent to . If the named  attribute does not exist,  is returned if provided, otherwise   is raised.
Your question is not very clear but if you want to change the value of  in python it is as simple asFrom what I've read from the python documentation, it seems that you can do that without the need of setter() and getter() function like other languages. I took this example from the link hyperlinked above.


Answer URL
https://docs.python.org/3/library/functions.html#getattr
