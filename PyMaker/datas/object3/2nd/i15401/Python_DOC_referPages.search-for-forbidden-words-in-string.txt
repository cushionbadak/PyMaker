Natural Text
I would like to detect a vulgar word in a string. I have a database of indecent words stored in  file. This file I put into my  file.Example bad word:input data string are for example:and I want, when input contains the word in word return is False
You actually do not need Regex for this.  You can use  and a generator expression:To make your string of words into a list, use :You might also want to call  on the list to improve lookup times.  Sets have  (constant) complexity with the  and  operators while lists have  (linear).  So, it is faster to find things in a set than it is with a list.


Answer URL
https://docs.python.org/3/library/functions.html#all
https://docs.python.org/3/reference/expressions.html#grammar-token-generator_expression
https://docs.python.org/3/library/stdtypes.html#str.split
