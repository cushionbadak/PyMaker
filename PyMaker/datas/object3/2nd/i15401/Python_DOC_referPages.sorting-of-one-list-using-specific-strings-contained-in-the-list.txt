Natural Text
I have to sort my list of strings according to the months name:I have to sort by january, february, martch, .....My trial:But no success, how would you do it?The expected answers is:
,  accept optional  parameter. Pass a key function that returns corresponding number to monthes. is mapping between month names and numbers using :
You could make a dictionary of months and their corresponding position in the calendar:And then sort the list by looking up the month in the dictionary. First split each element of the list on the underscore (which gives a list such as ) and then use  to look up the last element of this list in the dictionary:Edit:A lazier way if you have  installed...No need for a dictionary!  parses the month string to a  object.

You can sort your list based on  or  :


Answer URL
https://docs.python.org/3/library/stdtypes.html#list.sort
https://docs.python.org/3/library/functions.html#sorted
https://docs.python.org/3/library/calendar.html#calendar.month_name
