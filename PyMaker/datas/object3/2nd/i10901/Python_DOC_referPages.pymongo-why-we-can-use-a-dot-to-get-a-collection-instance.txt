Natural Text
Just as the snippet above, we can use just a  instead of  and  to get a database instance or a collection instance. Despite the convenience, I wonder what makes this syntactic sugar happen? 
There is a  magic method making dot notation/attribute lookup happen.Let's look into the source code.  class defines  method and instantiates a  class by name:And the same goes for the  class.


Answer URL
https://docs.python.org/3/reference/datamodel.html#object.__getattr__
