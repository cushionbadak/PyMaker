Natural Text
If you inspect the signature of Python's built-in  function like this:The signature is: .Based on my understanding of positional and optional arguments acquired here, it would seem like you could provide an  argument and then a  argument without needing a keyword  for the  argument.  But you always need to specify  when passing a  argument.  Why is that?I understand that if you wanted to specify , but without any  argument, you'd need a keyword for that, but I don't understand why you'd need to specify  when you're providing a .  
This is Python issue 26729, an error in , which is missing the  and  required to indicate that  is positional-only and  and  are keyword-only. The patch is in patch review, assigned to Raymond Hettinger. Once the patched version is released, the signature should display as


Answer URL
https://docs.python.org/3/tutorial/controlflow.html#keyword-arguments
