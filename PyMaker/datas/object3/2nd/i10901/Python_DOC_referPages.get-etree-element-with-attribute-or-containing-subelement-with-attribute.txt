Natural Text
I have an XML file to parse, and I need to find elements by id.In the example code, I need to find the name of the , but I don't know whether my id is for the , , or . I would like a solution which would work with arbitrary xml inside of  (but existence of  is guaranteed).What have I triedI was trying to get the elements by their id, and then, if they weren't  tags, navigate up the tree to find it, but it seems python's  returns None if the result is outside .Looking at the docs, they have this example:But I don't see how to make that work for any descendant, as opposed to a decendant on a specific level.
Note: All the snippets below use  library. To install, run: .You should use  not .If you want to extract driver's name from a given ID, you'd do:UPDATE: To get the driver's name for a given  nested at any level deeper, you'd do:
If you know the , but don't know if this  is from vehicle, engine or block, you can approach it with an XPath expression, but you would have to use  instead of  (it has very limited XPath support). Use the  axis:This would print: if  would be  or  or  if  would be  or  or Complete working example:Prints:


Answer URL
https://docs.python.org/3/library/xml.etree.elementtree.html#xml.etree.ElementTree.Element%22the%20docs%22
