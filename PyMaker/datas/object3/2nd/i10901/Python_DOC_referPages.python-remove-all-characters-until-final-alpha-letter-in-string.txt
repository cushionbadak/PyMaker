Natural Text
I have several strings where I need to remove all characters until the final alphabetical letter. I'm trying to keep the final numerical tag and store it into a variable. If there is no numerical tag at the end (like in the case of  and ), then the variable should be empty. The strings that need editing look like so:I can't strip everything until the first number because there are some strings that begin with numbers, like .Any ideas?
To follow Lawrence's comment, you can use a combination of reversing a string,  and :Prints:Another option would be to use regular expressions and capture in a group digits and slashes at the end of a string ( in the expression):Prints:


Answer URL
https://docs.python.org/3/howto/regex.html#grouping
