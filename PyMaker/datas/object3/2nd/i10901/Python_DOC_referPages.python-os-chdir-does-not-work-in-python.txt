Natural Text
I have the below code snippet and the function  doesn't seem to work.. The below is the output:
As you can read here os.chdir returns None in all the cases.
 returns . In that case, you should set  as a flag that checks if the current directory is the same as the last one:
 doesn't return anything back to you. When you want to see what directory you are in you want . E.g.Outputs something like;
os.chdir does not have a output, you have to make a os.chdir() then make:that way you get the new directory


Answer URL
https://docs.python.org/3/library/os.html#os.chdir
