Natural Text
I have a class A() include some method:How can I make X3 become an attribute which I can access by "self.X3" to use it for other methodsI am a newbie, thanks in advance!
One way would be to simply make  an attribute by prepending .  For example:I'm not sure it's the best approach, but it's hard to tell based on your question.
simply Change  to: 
Give this a try.  I got some issues compiling when I tried adding (self) after class A.
For this example, using a property is probably most appropriate. You can read more about the descriptor protocol (for which properties are syntactic sugar) here.


Answer URL
https://docs.python.org/3/howto/descriptor.html
