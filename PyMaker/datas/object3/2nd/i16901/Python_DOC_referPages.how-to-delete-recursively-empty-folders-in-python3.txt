Natural Text
I am trying to remove the empty folders of a directory.I have also tried with:But that removes everything, even those folders with contents. The problem is that I need to do it from inside to outside, this way if I have:The program should delete grandchild_folder1.1, child_folder1 and child_folder2.2, but not the rest.
 accepts optional  parameter (default: True).By providing , you can iterative from child directories first.


Answer URL
https://docs.python.org/3/library/os.html#os.walk
