Natural Text
I have a dictionary of coordinate pairs and integers that go with those pairs. The first problem is that some of the points are negative. I believe I have to adjust all the pairs so they are greater than zero so I can use them as row, column pairs for a matrix. Then the insertion is the next. I believe that python only has nested lists not matrices so I would want a numpy array. 
Using pure Python:Output:
Your test case:Result:Example with different rows and columns count:Result:
the resut:


Answer URL
https://docs.python.org/3/library/functions.html#func-range
