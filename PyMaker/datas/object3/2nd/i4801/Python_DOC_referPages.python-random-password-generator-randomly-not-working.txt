Natural Text
I am using the following code for generating some passwords with symbols, numbers and digits.Sometimes the code works and sometimes not, what is the problem?Additional details: if you run the code 10 times, almost half of the times it will fail giving the following error at the line with 
 interprets sequences of  in the replacement as references to groups in the pattern (among other escape sequences). You can use a plain string replacement instead:


Answer URL
https://docs.python.org/3/library/re.html#re.sub
