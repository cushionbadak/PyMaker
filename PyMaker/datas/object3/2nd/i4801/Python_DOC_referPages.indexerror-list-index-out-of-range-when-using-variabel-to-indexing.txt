Natural Text
Can some one help me when I try to execute this its return Sorry for my bad English Grammar
You need to change your loop so that  only iterates up to .Like this:This is because your  array is essentially just each adjacent pair of elements in . Naturally, the number of pairs in a  of  elements is . This means that you need to change your loop to stop one iteration before, as it's throwing the  when  is the  and  is the  ( isn't a valid value in the last iteration of your current code as there's not that many elements).
Here you set , , and , thus your  always greater than  by 1. So, in the last loop (where  and  should equal ), the value of  should be  which is bigger than the length of your array.I am not very sure about your excepted output, but I guess you could fix it like following: 
You can change the  loop for a  loop and use  to return the result when it reaches the end of the iteration.  You can also add in extra  clauses to catch other errors and make use of  and  if needs be .. https://docs.python.org/3/tutorial/errors.html
Here zip is used to create the adjacency pair


Answer URL
https://docs.python.org/3/tutorial/errors.html
