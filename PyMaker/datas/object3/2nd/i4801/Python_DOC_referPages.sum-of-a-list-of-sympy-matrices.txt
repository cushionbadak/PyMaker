Natural Text
My python list contains sympy matrix object, and I need to sum them all.If all list elements are just symbols, then using built-in sum function in python works fine.But for the elements of matrix type, sum function looks not working.How can I resolve this problem?
This is why Python's  function has an optional "start" argument: so you can initialize it with a "zero object" of the kind you are adding. In this case, with a zero matrix. 
I don't really know how the built-in function  works, perhaps it kinda looks like this. Now consider the following lines of code.What we can conclude is you add any (btw there are more such as Sum and Pow) to integer but not 


Answer URL
https://docs.python.org/3/library/functions.html#sum
