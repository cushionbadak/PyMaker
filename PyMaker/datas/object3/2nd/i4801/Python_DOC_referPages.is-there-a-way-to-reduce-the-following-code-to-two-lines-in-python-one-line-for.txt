Natural Text
The best I could do to get  in one-line python code is to define a 2d array and then flatten itIs there better way to define ?
You can use  to flatten the 2d list.Now  looks likeand  (when iterated or converted to list) looks likeThe  function is really just undoing the . So why not leave it out? 


Answer URL
https://docs.python.org/3/library/itertools.html#itertools.chain.from_iterable
