Natural Text
This question already has an answer here:`os.symlink` vs `ln -s`                    3 answers                I'm translating Linux commands to a python script. How would I perform the Linux command 'ln -s' CMD:I want to link 'hello' to 'link_file' in pythonThanks!
 is the provided function for creating symlinks. They are created as such:Where  and  are the paths.See the documentation here for more info.


Answer URL
https://docs.python.org/3/library/os.html#os.symlink
