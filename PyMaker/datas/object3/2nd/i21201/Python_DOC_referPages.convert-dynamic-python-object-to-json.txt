Natural Text
Possible Duplicate:Python serializable objects json I need to know how to convert a dynamic python object into JSON.  The object must be able to have multiple levels object child objects.  For example:Using python 2.6 the following code:yields the following error:I don't know what types of subobjects a user might put into .  Is there a solution that is smart enough to detect if an attribute is an object and parse it's  automatically?UPDATED to include subobjects on .
Specify the  parameter (doc):
That will give you a generic JSON object, if that's what you're going for.
Try using this package python-jsonpicklePython library for serializing any arbitrary object graph into JSON. It can take almost any Python object and turn the object into JSON. Additionally, it can reconstitute the object back into Python.
 expects a dictonary as a parameter. For an instance , the attribute  is a dictionary mapping  attribute  names to the corresponding objects.


Answer URL
https://docs.python.org/3/library/json.html#json.dump
