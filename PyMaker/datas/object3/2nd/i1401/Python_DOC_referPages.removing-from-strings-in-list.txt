Natural Text
This question already has an answer here:Replace part of a string in Python? [duplicate]                    2 answers                I'm trying to remove or ignore the  symbol (Apostrophe) within a list of strings. I don't know if my for loop is just plain wrong or what: 
 won't work here use ,
Two problems here. First,  doesn't work in the middle of the string, you have to use `replace("'", "")Second, and more important, strings are immutable. Even if  did what you want, it would not change . It would just produce a new string. So, you have to store that string. Sum up, try something like


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.strip
