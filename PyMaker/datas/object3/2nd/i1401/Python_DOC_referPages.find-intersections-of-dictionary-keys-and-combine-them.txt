Natural Text
So I am on a problem.I have this function where I get a list containing dictionaries as an input and the output should be a new dictionary with keys that are all in the input dictionaries and as value a list which contains all the values the keys had.E.G.
If you find the  intersection first, you can make the dictionary with a comprehension.You can reduce() (thanks UltraInstinct) over the dictionaries with  to build the intersection of the keys.
You can do this in a single for loop. Any key that is not present in every input dictionary will raise a KeyError 
You can use and append the values to the keys. After that just look for values with length 3 and you have the key intersections.d outputs:


Answer URL
https://docs.python.org/3/library/collections.html#collections.defaultdict
