Natural Text
This question already has an answer here:Why does “return list.sort()” return None, not the list?                    6 answers                Newbie here with the python interpreter.Why am I not able to sort this list of strings?Why isn't python able to sort this list of string for me?
 mutates the list in place, instead of returning a new list.
Firstly,  manipulate the list in place, which means when you call it, the list is already sorted, and the default return value is Secondly, above is sort by string value, you want to sort by the numerical value, you can use . Remember you can replace int with any custom methods:Last but not least, you should not use  as a variable name, as it's builtin function name already.
 sorts the  in place. Some Python programmers would prefer  which will return a sorted  and leave the initial  untouched


Answer URL
https://docs.python.org/3/library/stdtypes.html#list.sort
