Natural Text
I want to avoid doing ugly int to each element in a list.The list l has 4 elements, 0 to 3 indexes. I need to apply int starting with the 1st-element, not 0th element.I'm trying to write something like:Do I need lambda here, since map can already do the job?
 calls a function on each element of a list. Since  is a function you can usedirectly.Lambda functions just give you more flexibility, say if you wanted to do:A more Pythonic way to do the above would be to use list comprehensionsIn Python3  returns a generator. If you wish to have a list, you'd want to do:


Answer URL
https://docs.python.org/3/reference/expressions.html#calls
