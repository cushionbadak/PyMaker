Natural Text
I have this list in Python:How can I get a list like this?, sum third elements with the same second element and firstI need to sum the  element by grouping the  and  element. For example, all the  with ...the sum is 
Assuming that the values are intended to interpret commas as decimal points, and that all values to be grouped are consecutive, you can use  to good effect here:Using  is the best solution if the data is already consecutive by key (and preserves order without difficulty), but if the values to be grouped are not consecutive, you can either sort (by the same key used for ), e.g. running  before computing  with  as above, or to avoid  sort costs, you can use a  (or for simplicity, ) to group non-consecutive groups:
You can use pandas, It's a great python module for data analysis.Or:


Answer URL
https://docs.python.org/3/library/math.html#math.fsum
