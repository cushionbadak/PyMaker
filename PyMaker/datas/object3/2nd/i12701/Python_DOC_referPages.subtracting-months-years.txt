Natural Text
I'm trying to subtract dates, both in the format of Month, Year. I wasn't sure if I should use datetime? I was reading over the docs and wasn't sure what would be the best way to calculate something like subtracting January 2005 from July 2008.
If you're trying to get the length of time between two points, subtracting  objects is definitely the approach to take.This generates a   object
A good idea is to use  objects. Subtracting  objects generates a  object, as exemplified below


Answer URL
https://docs.python.org/3/library/datetime.html#datetime-objects
https://docs.python.org/3/library/datetime.html#timedelta-objects
