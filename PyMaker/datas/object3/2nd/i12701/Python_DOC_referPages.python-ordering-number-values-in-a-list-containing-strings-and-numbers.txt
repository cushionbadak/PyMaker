Natural Text
I have Created a list which contains all of the information from a scores file in python.The scores .txt file:I did this to get the information from the .txt file into a 2d list:Now I have this list:From this list I would like to sort the numbers in the list so that they are ordered from highest to lowest so i get a list that looks like this:Finally I want to be able to print the list ordered highest to lowest.
Using  with  as a key function:: separate number values and sort.: name as a list, alternatively you can use . Should be a list to be concatenated to a list of number strings.
Using  and  to cast strings to ints:
You can approach the problem this way as well. I first convert all grades into integer type so that I can keep my lambda function clean. I could have done the conversion in lambda function but it does not fancy me much. You can see splitting the each problem into different module, gives re-usability.     You can even use the sum method of list in lambda function. Like this:


Answer URL
https://docs.python.org/3/library/functions.html#sorted
https://docs.python.org/3/library/functions.html#int
https://docs.python.org/3/library/functions.html#sorted
https://docs.python.org/3/library/functions.html#map
