Natural Text
I have a list:And a list of tuples:I need sorted the:and when the  is equal the same 's sorted by the :
 accepts an optional parameter . The return value of the function (each item is passed to the function) is used instead of the items themselves.Given the above key function order will be by the number first, then position in the .
Use a  mapping  each string in test1 to its index so for ties you sort by the index and the lookup is :Output:If you actually wanted the strings to be in sorted order you could just use the string itself, using  instead of a lambda:


Answer URL
https://docs.python.org/3/library/functions.html#sorted
