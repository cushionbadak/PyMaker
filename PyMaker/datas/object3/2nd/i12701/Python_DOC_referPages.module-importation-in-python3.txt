Natural Text
I am using Python 3.4I have a directory structure that looks this:From g.py I can import things from both B and C modules.I need, in e.py, to import something from c.pyI tried:andFor both I get the error:I also tried something like:I am further confused by the fact with an identical directory structure, I can make the imports I need with Python 2.7.Can you help me figure out what's going on? Solution:Taken from here.
When importing it looks at the the python folder for imports and the files in the local directory. If you want to import a file that is in neither of those, then I suggest using the  moduleIf you don't want to set the full file path then you can also just backtrack to the previous directory with this for the same effect.
You need to do eitherorReference:What's New in Python 2.5PEP 328 - Imports: Multi-Line and Absolute/Relative


Answer URL
https://docs.python.org/3/whatsnew/2.5.html#pep-328-absolute-and-relative-imports
