Natural Text
I have this string  and I want to convert it to .I need to remove the  and the front My site won't always have  at the end so I need something that will still work even if the  is not there.Thanks and hopefully this isn't too confusing to get some help :)EDIT:I know I could slice at 0 and 8 or whatever, but the  could be longer or shorter. For example it could be  or 
Using  (to remove leading ) and  (to remove optinal part after ):or using :
You can use  and :This only works as long as you don't have the characters , , , ,  in the part that you want to extract.
You can use regular expressions:
What about regular expressions?In this case it matches everything that is between  and , but you can change it to your specific requirements
Split will simply return a single element list containing the original string if the separator is not found.


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.lstrp
https://docs.python.org/3/library/stdtypes.html#str.split
https://docs.python.org/3/library/urllib.parse.html#urllib.parse.urlparse
