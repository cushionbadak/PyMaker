Natural Text
I want to process quite big ARFF files in scikit-learn. The files are in a zip archive and I do not want to unpack the archive to a folder before processing. Hence, I use the zipfile module of Python 3.6:However, this yields the following error:According to loadarff documentation,  requires a file-like object.According to zipfile documentation,  returns a file-like .Hence, my question is how to use what  returns as the ARFF input to .Note: If I unzip manually and load the ARFF directly with , all is fine.
Read  into a in-memory  object. Use  with . Use this in-memory buffered text object in .Edit: Turnsout  returns a file-like object so the above can be accomplished by :Thanks @Bernhard


Answer URL
https://docs.python.org/3/library/zipfile.html
