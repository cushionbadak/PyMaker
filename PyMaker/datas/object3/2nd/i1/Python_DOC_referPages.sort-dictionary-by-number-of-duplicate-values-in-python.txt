Natural Text
This is my dictionary :I am expecting output like this :When I run this program I am getting different output than expected :
You can start by counting the amount of times each value appears with :And now sort the dictionary looking up how many times each value appears using a  in :Note however that if you obtain a dictionary from the result, the order will not be mantained, as dictionaries have no order.So one thing you can so is use  to keep the order, simply call  on the resulting list of tuples.
First, we count occurences of each value.  can be used instead of . Then sort them according to  lookup table we just created.Update : You can create  with  like:
I'm not sure if you want to keep the order of months. I assume you want, so here is the solution.And the result is:


Answer URL
https://docs.python.org/3/library/collections.html#collections.OrderedDict
