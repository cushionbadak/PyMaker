Natural Text
I run this python code which reads a file and uploads data to the engine.It runs fine, however out of a sudden it fails and throws the error in the middle.I did some research but wasn't able to find a solution that works.below is the errorbelow is the code:
You are on windows that uses backslash as path separator . However for python this is escape char, so you need to use forward slash or raw string or escape the backslash. change these types of addresses:to this one:or
After doing much research, I found a resolution that worked out for me,instead of usingi used


Answer URL
https://docs.python.org/3/library/sys.html#sys.stdout
