Natural Text
I can get the hollow square, but I can't seem to figure out how to fill in the right half of the square. Here is what I have...
Add an explicit check:Output:If you are not familiar with the  ternary operator, you may use a simple  or refer to the Python documentations
You can calculate the width of the white spaces on the left by floor-dividing the total width sans the border by 2, and calculate the width of the hashes on the right by subtracting that width from the total width sans the border (I've replaced your helper functions for better reproducibility):Note that instead of a loop to print a character repeatedly you can use the repeat operator for strings, so the code below will suffice and is more readable:


Answer URL
https://docs.python.org/3/reference/expressions.html#conditional-expressions
