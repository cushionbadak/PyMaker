Natural Text
I have a program which writes to certain files, where file name is generated on the fly and can contain special characters at random.Whenever there is a  in these special characters, code considers it to be a OS path separator for linux.How can I escape this dynamically?Demonstration of what I am running into:Runs very well when it is any other character than , but when you set selected as :Error:python 2.7 only please.
As the @Nullman said,  is not a valid character for filenames in Linux:You should never use  and  symbols in filenames because it has many unexpected results. For example:Linux terminal:IPython 3:


Answer URL
https://docs.python.org/3/library/stdtypes.html?highlight=str#str.replace
