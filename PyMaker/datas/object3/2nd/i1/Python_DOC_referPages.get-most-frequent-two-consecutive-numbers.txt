Natural Text
get most frequent numbers:How can I get most frequent two consecutive numbers?Answer is [2,3].But how to get it by program?
you could use  and iterate over  in consecutive pairs:telling you that  occurred 3 times.a bit more detail:is your  without its first element. is then used to generate the consecutive pairs (as )the  then just counts how many times the appear and stores them -like:update: if you do not want pairs from different list, you can do this:or directly:
You can use  as suggested by @hiro protagonist, but since you want to treat a one row at a time, you have to apply it along rows.Apply along rows using numpy:Or, if using pandas:Now, apply Counter along rows:


Answer URL
https://docs.python.org/3/library/collections.html#collections.Counter
https://docs.python.org/3/library/functions.html#zip
