Natural Text
Let's say I have the following series:How can I merge them to create an empty dataframe with every possible combination of values, like this:
Assuming the 2 series are  and , use  which gives a cartesian product of input iterables :
If you have 2   and .you can do this:It will give you the follow


Answer URL
https://docs.python.org/3/library/itertools.html#itertools.product
