Natural Text
how to check if this list is empty?I tried solutions from how to find if nested lists are empty. but none of them worked.
You should check if the list is falsy/empty first before recursively checking the list items. You can also avoid explicitly returning  or  by using the  and  operators:Demo: https://repl.it/repls/AccurateSmallOutcome
For lists that actually are empty, the function should simply return True.
 is not empty in fact. But in this case this code should work:
You can use a simple recursive approach with . Using  would make sure that the recursively search ends as soon as a non empty item is found   
Try ThisIf you face any problems comment below


Answer URL
https://docs.python.org/3/library/functions.html#len
