Natural Text
I have to following class that will make an object with chainable methods that derive from class variables. Since this code is quite repetitive, my challenge is to make a decorator that can apply over method ,  and . The problem I am facing is that I cannot seem to find a way to construct a wrapper that will return the instance (). Is there a better way to construct this?It works chained like this:
The trick is to store the arguments to the function as part of the call chain.  The easiest way is to use  objects.


Answer URL
https://docs.python.org/3/library/functools.html#functools.partial
