Natural Text
This question already has an answer here:Determining application path in a Python EXE generated by pyInstaller                    6 answers                I made a program with python that runs correctly when run with python interpreter. It reads some files from the same directory. In order to run the script from other paths, the script changes its working directory to its own location.But this does not work when I package it to .exe. Because when running .exe the  variable is .I know that it can be fixed by explicitly setting a fixed path:But is there an elegant solution?
So the answer here is actually in two parts. To get the executable's location, you can useTo then chdir to the directory of the executable, you should try something like or simply


Answer URL
https://docs.python.org/3/library/sys.html#sys.executable
