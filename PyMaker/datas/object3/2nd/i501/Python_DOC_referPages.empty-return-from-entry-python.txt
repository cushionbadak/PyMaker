Natural Text
I've written a code, which saves the background color of the window in the file, and after closing and running the program again it remembers the window colour (it saves in the file called 'data.txt').When i run it, it works, but when i click 'SAVE!' button in settings window, the file contents don't change. Could you pelease eplain, how can I fix it?
The answer is to make entries global, using


Answer URL
https://docs.python.org/3/tutorial/classes.html#python-scopes-and-namespaces
