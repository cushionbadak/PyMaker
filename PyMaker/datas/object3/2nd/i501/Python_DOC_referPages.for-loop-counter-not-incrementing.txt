Natural Text
I have a double for loop to go through a matrix and compare numbers. The  is not incremented in my loop and I can't see why. I also recreated the spaces in front of every line, in order to have nothing "broken" by this part. In Notepad++ I get therefore that "plus-sign" in front of the first -loop, so that I can close the whole loops and what is inside. Basically my 2 -loops do this:I really do not understand why the  is not raised to 1.
[...]For a positive step, the contents of a range r are determined by the formula r[i] = start + step*i where i >= 0 and r[i] < stop.Python3 documentationFrom an usage perspective,  could be explained as following:Your stop boundary is therefore wrong.


Answer URL
https://docs.python.org/3/library/stdtypes.html#range
