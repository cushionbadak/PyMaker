Natural Text
I have code below :This works fine, I need the IF to check for blank result values before loading to a list.I originally wrote this as :But I get an syntax error on the IF line.  Any ideas why the first code block works but the simpler second block fails?
In python, you simply use the word  for negation.  is not a valid python syntax in python 3. (Updated) Your code:Can be correctly written as Or the much nicer:Which relies on the truthiness of non empty strings. (Empty strings are Falsey, non emptry strings are True)Note that you can also just use the  keyword instead of applying both  and 


Answer URL
https://docs.python.org/3/library/stdtypes.html#boolean-operations-and-or-not
