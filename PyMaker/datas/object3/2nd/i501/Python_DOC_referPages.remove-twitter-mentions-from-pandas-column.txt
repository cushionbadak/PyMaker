Natural Text
I have a dataset that includes Tweets from Twitter. Some of them also have user mentions such as . I try to remove that text at the same time I do other cleaning processes.However, when I run the above code, all the Twitter mentions are still on the text. I verified with a Regex online tool that my Regex is working correctly, so the problem should be on the Pandas's code.
You are misusing  method on a string because it does not accept regular expressions, only fixed strings (see docs at https://docs.python.org/2/library/stdtypes.html#str.replace for more).The right way of achieving your needs is using  module like:
the problem is with the way you used replace method & not pandassee output from the REPLreplace doesn't support regex. Instead do use regular expressions library in python as mentioned in the answer


Answer URL
https://docs.python.org/3/library/re.html
