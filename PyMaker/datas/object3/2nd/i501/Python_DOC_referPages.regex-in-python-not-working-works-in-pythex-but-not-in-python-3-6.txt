Natural Text
This question already has an answer here:What is the difference between re.search and re.match?                    8 answers                This regex works in pythex, but not in python 3.6.  I am not sure why:Pythex link (click)Code in python:I haven't been able to figure out why it works in pythex but not in python interpreter.
You might be looking for  not . The latter only matches at the start of the string (implies an anchor , that is):See a demo on regex101.com.
I suspect your problems comes from calling  instead of . The  function tries to find the regex in the given string, while  requires the regex to match at the beginning of the string.Change this:to this:
The problem is that match() is used for matching the beginning of a string, not anywehere.from python docs: (Python docs for match())"If zero or more characters at the beginning of string match this regular expression, return a corresponding match object."You should use search() instead:"If you want to locate a match anywhere in string, use search() instead."see also search() vs. match()this part:has to be:


Answer URL
https://docs.python.org/3/library/re.html#re.Pattern.match
https://docs.python.org/3/library/re.html#search-vs-match
