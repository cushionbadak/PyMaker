Natural Text
I know the URL of an image on Internet.e.g. http://www.digimouth.com/news/media/2011/09/google-logo.jpg, which contains the logo of Google.Now, how can I download this image using Python without actually opening the URL in a browser and saving the file manually.
Python 2Here is a more straightforward way if all you want to do is save it as a file:The second argument is the local path where the file should be saved.Python 3As SergO suggested the  code below should work with Python 3.
 will contain your image. 
I wrote a script that does just this, and it is available on my github for your use. I utilized BeautifulSoup to allow me to parse any website for images. If you will be doing much web scraping (or intend to use my tool) I suggest you . Information on BeautifulSoup is available here.For convenience here is my code:
A solution which works with Python 2 and Python 3:or, if the additional requirement of  is acceptable and if it is a http(s) URL:
I made a script expanding on Yup.'s script. I fixed some things. It will now bypass 403:Forbidden problems. It wont crash when an image fails to be retrieved. It tries to avoid corrupted previews. It gets the right absolute urls. It gives out more information. It can be run with an argument from the command line. 
Python 3urllib.request — Extensible library for opening URLs
This can be done with requests. Load the page and dump the binary content to a file.
This is very short answer.
Version for Python 3I adjusted the code of @madprops for Python 3



Answer URL
https://docs.python.org/3/library/urllib.request.html?highlight=urlretrieve#urllib.request.urlretrieve
https://docs.python.org/3/library/urllib.request.html?highlight=urlretrieve#urllib.request.urlretrieve
