Natural Text
How to get  like behavior with , as  s not available below python 3.6 . Basically what I want is checking if the method gets called exactly once, no matter with which argument or how many argument. I tried , but seems it isn't what I want.   
All that  does is assert that the  attribute is 1; you can trivially do the same test:If you tried to use  with the  object, take into account that that object stands for the value of one of the arguments.  only matches if the object has been called with exactly one argument, and the value of that argument doesn't matter:You can't use the  object together with the  assertions to mean 'any number of arguments'. Only in an assertion that takes  objects would  be interpreted as any number of arguments; so you could also use  here, passing in a list with a single  element:


Answer URL
https://docs.python.org/3/library/unittest.mock.html#unittest.mock.Mock.call_count
https://docs.python.org/3/library/unittest.mock.html#unittest.mock.call
https://docs.python.org/3/library/unittest.mock.html#unittest.mock.Mock.assert_has_calls
