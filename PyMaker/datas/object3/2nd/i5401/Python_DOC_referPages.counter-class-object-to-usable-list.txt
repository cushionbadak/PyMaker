Natural Text
I have used Counter class to get the iterations number, and now I want to format it as like that :
You should build the  only once.  There's no guarantee that the two separate  objects will iterate over their contents in the same order (Plus for large inputs it's just inefficent).Here  is a sorted list containing tuples :
You can convert them to lists and then  them:Output:


Answer URL
https://docs.python.org/3/library/stdtypes.html#dictionary-view-objects
