Natural Text
I have a file which contains something similar to the following lines:I am looking to split each line into two. The first part being between either the '<' and '>' the '[' and ']' or  " and ".So the output from the desired split would be something like:One snag is that the data values are of an unknown length, so some lines could read:and others could be:Any ideas how?
What you are looking for is :Code:Results:
It looks like you could just split on a space .
There is also a general approach to finding stuff in strings and breaking them apart.Results:


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.rsplit
