Natural Text
This question already has an answer here:csv.writer writing each character of word in separate column/cell                    3 answers                I extract some data from a url but I can't create a csv with that. When I try to save it into a csv each character is in a row and not in a cell...Data:This is my saved csv:
Use This will create a list from your string, split at  -  takes an iterable and writes it into the file -  a string is also iterable, thats why you gett each character seperately.Read up on it here: https://docs.python.org/3/library/csv.html#writer-objects
try this:ExplanationYou need to pass a 2 dimensional array to write rows, in this case you only have one row so we pass [zz] to writer.writerows. zz itself should be an array where each element is a new column, which we achieve by doing zz.split('.')


Answer URL
https://docs.python.org/3/library/csv.html#writer-objects
