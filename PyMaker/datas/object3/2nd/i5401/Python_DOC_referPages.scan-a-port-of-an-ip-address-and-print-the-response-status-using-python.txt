Natural Text
I am trying to hit a port of an IP address and print the  (i.e  etc). Suppose, let's say  is open at ip address  . So,  should give us . In my code, I am using port  , which is definitely not opened and no application is running there. But it is not showing any error.What is the recommended way to achieve it. 
First of all, 811450 isn't a valid port. The largest valid TCP port range is 65535.Second, you're mixing up the TCP protocol and the HTTP protocol. If you want an HTTP response from the socket, you need to send a valid HTTP request. You can easily do this with the  package:


Answer URL
https://docs.python.org/3/tutorial/errors.html
