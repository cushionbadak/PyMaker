Natural Text
I am tying to concatenate all mpeg files together in one new file in windows 7, I adjusted the environment variables and running the code from python shell but it gives invalid syntax. Any help as I am new to Python and ffmpeg library?My code:Thanks
Your example code is mix or  code and  code so it can't run in  nor in  :)On Linux it works in  as two commands:(Windows probably doesn't have  command) version which doesn't need :And you can run it with or without argument ie. (tested only on Linux) (and other  commands) for Windows: GnuWin32More on GnuWin32


Answer URL
https://docs.python.org/3/library/subprocess.html#call-function-trio
