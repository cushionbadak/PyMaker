Natural Text
How can I implement a circular range object in Python?e.g. Let S is a circular space modulo 2^3 (range [0, 2^3)).I want to generate a range object like this:I tryed this:But I can't input bigint to crange e.g. .
Try this:If you know that your list can be too long, you can use a generator instead that generates the necessage sequence:
You can avoid the use of range and the storage of a huge list in memory by creating your own generator:This code outputs:
Thank you very much, everyone.I implemented  which I want (in reference to @Ni and @J.F.Sebastian).I got the following output:


Answer URL
https://docs.python.org/3/reference/datamodel.html#object.__bool__
