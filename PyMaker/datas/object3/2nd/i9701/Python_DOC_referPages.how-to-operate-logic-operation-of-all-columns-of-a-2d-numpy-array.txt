Natural Text
Let's say I have the following   array consisting of four rows and three columns:What would be an efficient way to generate a  array that contains the logic or of all columns (like )?I searched the web and found someone referring to  to calculate the . I wonder if there is some similar way for logic operation?
Yes, there is. Use :Note what happens when you change the argument  to :If you want logical-and use :Also note that if you leave out the  keyword argument, it works across every element:
NumPy has also a  function which is similar to Python's . It's possible to use it with NumPy's logical operations. For example:  It also has the  parameter:The idea of  is that it cumulatively applies a function (in our case  or ) to each row or column.


Answer URL
https://docs.python.org/3/library/functools.html#functools.reduce
