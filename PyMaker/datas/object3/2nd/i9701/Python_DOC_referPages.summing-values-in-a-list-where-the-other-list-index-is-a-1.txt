Natural Text
This question already has an answer here:Filtering a list based on a list of booleans                    5 answers                I have two lists:I want to sum only the values in  where the index in  matches the value of 1 for the same index in .Example here would be How can I do this in Python in a Pythonic way?
Just use  from  and feed it to ; it's built for this purpose:
Zip the lists, then sum only values where the first element is :Another zip approach would be an element-wise multiplication between the lists (thus eliminating elements whose parallel is ):
Probably less pythonic, but also works for lists of similar size:


Answer URL
https://docs.python.org/3/library/itertools.html#itertools.compress
