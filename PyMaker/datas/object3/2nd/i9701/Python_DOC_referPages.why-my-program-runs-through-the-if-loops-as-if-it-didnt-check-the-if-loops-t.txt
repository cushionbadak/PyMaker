Natural Text
The program should add all multiples of 3 and 5, up to the value passed. But instead it seems to add every number regardless of the multiple. It should return 23, but instead returns 45. Why?
In such cases adding a debug statement often helps to identify the problem:and this prints:Note that for simple cases a  (if you just want to inspect variables  is very handy!) is sufficient but there are also other debugging tools like .The reason why this is failing is that you compared the modulo of  instead of .  so  is always . See also the comment of @JohnColeman.Change it to :which prints:and returns 23.


Answer URL
https://docs.python.org/3/library/pdb.html
