Natural Text
Through a Django queryset call, I'm generating a list of integer tuples:I instead need to generate a list which sums the two elements inside the tuple together, i.e.How can I accomplish that within the Django queryset query?The query I've written so far is:
I don't know about Django, but when you have this:you can compute the sum of each element in one line using list comprehension:result:applied to your problem:
Once you have that list of tuples, you can use a list comprehension to generate your second list like below:In our list comprehension, we return for tuple  in  the sum  and the result is sorted in .


Answer URL
https://docs.python.org/3/library/functions.html#map
