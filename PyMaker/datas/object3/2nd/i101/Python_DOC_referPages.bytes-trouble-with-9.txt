Natural Text
I have data stored in a byte array. How can I convert this data into a hex string?Example of my byte array:
Using :or using Note: In the format statements, the  means it will pad with up to 2 leading s if necessary. This is important since  would be formatted to  instead of  or using  with :Here is a benchmark of above methods in Python 3.6.1:Result:Methods using  do provide additional formatting options, as example separating numbers with spaces , commas , upper-case printing /, etc., but at a cost of great performance impact.
Consider the hex() method of the  type on Python 3.5 and up:EDIT: it's also much faster than  (modified @falsetru's benchmarks above)Result:

If you have a numpy array, you can do the following:
Or, if you are a fan of functional programming:


Answer URL
https://docs.python.org/3/library/stdtypes.html#bytes.hex
