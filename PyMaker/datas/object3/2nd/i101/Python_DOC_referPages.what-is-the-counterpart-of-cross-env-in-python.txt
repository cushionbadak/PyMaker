Natural Text
In Node.js, we use "cross-env" to set env variables. It can run on Windows, Linux and Mac OS.Is there a counterpart in python?
In python ecosystem it's more common to rely on a custom config.py file which handles to get things from system variables or set reasonable defaults. This article can detail more on this.
Have you tried ? Read and write value works the same way as with dictionary type. Since it is an environment variable, value can only be string type though.Update:If you only want to set environment variable when running  mode, you can use the python  library to check if the run command has  argument. If so then you can set the environment variable  to  by using the assignment expression .  should work on all operating systems you memntioned. After that you can run the command  on Python using  function from Python's module. Remember to set   argument to  in order to load the environment variables you set into .Here is an example file:So when you want to run  mode, you can enter the command:


Answer URL
https://docs.python.org/3/library/os.html#os.environ
