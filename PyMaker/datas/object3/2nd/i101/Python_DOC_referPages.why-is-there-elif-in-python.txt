Natural Text
So I am slightly confused as to the purpose of having  in Python. Is  a way to shorten ? Which is more Pythonic  or another  statement? Why is there elif in Python but not in something like Java?Thanks for helping me to understand the gem of a language that is Python better.
Python has  to make parsing easier. One of the design goals of the syntax was to keep the parser simpler than other languages which require context free grammars. Faster parsing means a faster interpreter.If I have , I don't know what the else means until I reach the if. To write this in python, I actually need to sayWith , I have a single symbol which is unambiguous.Functionally, these two statements are the same:


Answer URL
https://docs.python.org/3/reference/compound_stmts.html#elif
