Natural Text
I have two dictionaries which consist same keysWhen i merge them the keys of dictionary b replaces the keys of a because of the same name. Here's the merge code I am usingIs there somehow I can keep all the keys, I know dictionaries can't have same key name but I can work with something like this:
You can use a generator expression inside the method :
Do something like this perhaps:       Output:
While I think Usman's answer is probably the "right" solution, technically you asked for this:
Check if key of b present in a then add in a with key_1 value of b for key other wise add in key in a the value of b for key.


Answer URL
https://docs.python.org/3/library/itertools.html
