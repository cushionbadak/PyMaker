Natural Text
I am trying to import data to python numpy.array from data file like this:My expected result is:I've tried something likebut as a result I received an array of data not divided into separate zones. Thanks for the help!Update:It turned out that it is essential that the zones have different sizes. Otherwise there is no problem.
Iterate over the file; for each line strip whitespace from end; split on whitespace; check for numbers; keep line if it only contains numbers after turning them into floats (or mayb ints?); make an array of the result.


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.isdigit
