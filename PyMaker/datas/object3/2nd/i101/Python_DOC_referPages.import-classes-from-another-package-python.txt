Natural Text
My source code directory (named ) is organized as follow:In , I import  by But when I import  in  byAn error is raised as the statement  in  acts as if my working directory is  instead of .How can it fix this ?
in  import it as  and in  import module_1 as 
This is the cause of the misunderstanding:An error is raised as the statement  in  acts as if my working directory is  instead of .First of all, when you import a package, the code in that package does not execute in a different working directory.The second mistake is that the working directory is not relevant at all, even if it did change.Python code is organised into packages and modules. The interpreter searches for packages and modules within the python path.In the structure shown,  will be in python path if you run . Then  is a package whereas  and  are modules. The easiest way to import  is to use an absolute import:Alternatively, from within another module in  you can use a relative import:
Try this:


Answer URL
https://docs.python.org/3/tutorial/modules.html#intra-package-references
