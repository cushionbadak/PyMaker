Natural Text
I have a flask restful project with the following layout (file names changed for convenience) is just a folder containing code for one of the API endpoints, I'm planning to add others in the future, for this reason I have  file which contains reusable functions that I will use with other API endpoints.in  I have classes with methods that get some data, I import those classes to  to return JSON responseclasses in  also uses some functions from  but when I try to import something from  to  I get What could be causing this? I tried importing various ways:but none worked.This is  in case it matters:I'm doing all of this in activated virtualenv if it matters
from common.util import my_funcIn Python 3 this is an absolute import, that is, the directory with  subdirectory must be in . In your situation it's certainly a wrong approach.from .common.util import my_funcThis import expects  to be a subdirectory of  which is also not the case.from myproject.common.util import my_funcThis is finally the best approach but for it to work the parent directory of  subdirectory must be in . Either you install the entire  or add the parent directory to  environment variable or add the directory to  in . Something like:or is the directory where  is.After installing  or adding its parent directory to  you can also use relative import. You need to remember that  is a sibling package comparing to  so the import must be not from  but from :


Answer URL
https://docs.python.org/3/installing/index.html
https://docs.python.org/3/distributing/index.html
