Natural Text
I need to generate an export (csv) of a series of simple objects like this:What is the simplest way to list the attribute/value pairs in order by attribute?  changes order every time.
You could use  on , then sort the items:This then gives you a sorted list of  pairs.However, if your class is little more than a container for 4 related datapoints, then consider using a  class:This lists the attributes in named order, always:Because the instances are sequences (a subclass of  in fact) you can pass these directly to a  call; at the same time you can use  or  to access the fields.Another alternative is to use dictionaries, and use  to handle the order (the  argument determines the order).


Answer URL
https://docs.python.org/3/library/collections.html#collections.namedtuple
https://docs.python.org/3/library/csv.html#csv.csvwriter.writerow
https://docs.python.org/3/library/csv.html#csv.DictWriter
