Natural Text
Before I go reinventing the wheel, can anyone tell me if there's a drop-in (or semi-drop-in) replacement for the single-line statement:
One line? No. But you can just do:
You can use :This is not an identical replacement.  The warning that Dschoni issues in the comment on viraptors answer applies here too: the directory is not re-read when the modification date changes, so if files get added or removed from the directory, later calls will be incorrect.


Answer URL
https://docs.python.org/3/library/functools.html#functools.lru_cache
