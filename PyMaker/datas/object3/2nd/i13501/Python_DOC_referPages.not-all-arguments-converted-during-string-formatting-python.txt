Natural Text
I am writing a script which prints the list items that are divisable by 3 XOR the last character ==4 but I get an error stating:  I am new to Python and may have missed something obvious. Code below:
First of all you need to change  to  because your items are string and convert the  to int in  also you need to parenthesis your comparison expression because the precedence of  is higher than  and it will raise a  also if you want to preserve your result you can use a list comprehension : If you just want to print the result you can use a usual loop with print function.


Answer URL
https://docs.python.org/3/reference/expressions.html#operator-precedence
