Natural Text
I am using curses to print a pretty nice console UI, and I need for that something to be dependent of the terminal size. For this, I read here, that I could use .So I'm doing this code : But the fast is that, it is working perfectly the first time I call the function, but if I use my mouse to change the terminal size and recall the function, the result of  will always stay the same. (120 30).Do you have any idea of where it could come from ? (I'm running Windows actually, and I'd like it to work under all common OS)Thanks a lot everyone ! 
Basically, that's because the application using  is (in this case) using the Windows console api to create a buffer which has a fixed-size.  In more conventional Unix-like applications (rather than  attempt at high-level and portable), one would make a  handler that notifies the application of the size-change. With Windows, you'd have to get that from the event-loop — which is completely obscured by the  interface.It happens to "work" with Unix because  doesn't have to actually pay attention to those notifications.  The operating system's terminal driver (usually) can return the updated information.You might file a bug-report against , to get its developers to take that into account in their design.


Answer URL
https://docs.python.org/3/library/shutil.html#querying-the-size-of-the-output-terminal
