Natural Text
I would like to use a  loop to go through a list of numbers and stop when it reaches a specific number like , returning the sum of the numbers that came before it.So if the list is , the function would return  after summation.
Here are two ways (with and without function) using  loop. The  condition will break out of the  loop as soon as element will be , otherwise it will keep summing. Coming out of the  loop, you can print the sum Without functionWith function
You can use :The docs for  give the equivalent generator which can be used to implement the logic directly:


Answer URL
https://docs.python.org/3/library/itertools.html#itertools.takewhile
