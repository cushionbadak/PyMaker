Natural Text
Is there any way to save into a variable what that was print on the screen, so I will be able to print it back after erasing the screen?
Don't know why this question is being downvoted. Anyway, an answer from Raymond Hettinger on Twitter, utilises :Although, in this case the output is being redirected into a file, rather than a variable and is Python3, not 2.7.From the docs, to capture that in a variable:For example, the output of help() normally is sent to sys.stdout. You can capture that output in a string by redirecting the output to an  object:This answer on SO shows another way to do it, and in Python2: 


Answer URL
https://docs.python.org/3/library/contextlib.html#contextlib.redirect_stdout
