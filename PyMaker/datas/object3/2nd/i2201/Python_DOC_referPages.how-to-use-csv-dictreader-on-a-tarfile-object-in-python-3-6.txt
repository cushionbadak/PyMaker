Natural Text
Here's the issue I'm running into:The code that's causing this looks something like:The trouble seems to be that the  method produces a io.BufferedReader that is a very basic file-like object and has no high-level text interface.What would be a good way to handle this?I'm thinking of looking at decoding the bytes from the reader into text but I need to retain streaming because these files are very large. The codebase is Python 3.6 running on Docker/Linux.
Thanks to both @Aran-Fey and @zwer who led me to another StackOverflow question that answered it. Here's how:This seems to work so far.


Answer URL
https://docs.python.org/3/library/io.html#io.TextIOWrapper
https://docs.python.org/3/library/codecs.html
