Natural Text
I want to make loop generate from random generator with no duplicate values like below:What I mean is, that there are no duplicated value on the second value for each first value. And the value range for second value is from (1 to 10).My code below still gives duplicated values:any ideas on how to address this problems?Any response really appreciated.
Just use  which is the built-in way of generating non-repeating random elements from a population:For sampling 14 random elements from the entire pool of possible pairs, you can combine it with :
generate the product list and then shuffle it. It will contain unique values in tupleCorrecting your code to check for duplicates. Although with expensive in operator

I suggest to make an empty array then append the number if not in the array else passLike this :


Answer URL
https://docs.python.org/3/library/random.html#random.sample
https://docs.python.org/3/library/itertools.html#itertools.product
