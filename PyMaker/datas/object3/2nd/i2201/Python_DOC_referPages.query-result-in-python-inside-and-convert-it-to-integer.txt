Natural Text
my query is like this getting all rows of a table, this is for pagination:and it prints result like this, it looks like an array in js? but inside ():what is that ()? and how do i convert the result which is 1 inside of it (1,) into integer? also, how can i make a query inside of flask to just return the result 1 and not inside ()?
 is a .  returns one record as a tuple. If no records are fetched, it returns . You can index the tuple in order to get the first element as below: And the result should be 


Answer URL
https://docs.python.org/3/reference/expressions.html?highlight=subscription#subscriptions
