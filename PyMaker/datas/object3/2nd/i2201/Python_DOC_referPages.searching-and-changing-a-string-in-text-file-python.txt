Natural Text
I searched a lot in the internet and I didn't find an appropriate code that searches for a string in a certain text file and replaces it with a different word?Would appreciate help.. And thanks for the helpers!!Let's say that the name of the text file is and it has the following contents:And I want to replace the string  in the string 
First open the file for reading and writing using the built-in open function.Then read all its contents and replace  with  using str.replace.Now set the offset to  with the seek-method because you want to override the contents. After that write the result to the file.Example usage:


Answer URL
https://docs.python.org/3/library/functions.html#open
https://docs.python.org/3/library/stdtypes.html#str.replace
https://docs.python.org/3/library/functions.html#open
