Natural Text
So I have a list of tuples, for example:And if the first index of one tuple is the same as another it will make a new list like this:
Use dictionary instead of list then convert dictionary to desired format:
a simple way would be to use  and then convert back to a list:but considering that data structure you might be better off just using the dictionary  instead of the list .


Answer URL
https://docs.python.org/3/library/collections.html?collections.defaultdict#collections.defaultdict
