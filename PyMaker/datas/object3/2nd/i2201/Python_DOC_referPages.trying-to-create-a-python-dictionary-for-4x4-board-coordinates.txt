Natural Text
I want the desired output as-usinggetting the output asnot able to build the logic to get desired output. 
The most straightforward way is using the nested for loop.or, if you prefer oneliners, double comprehensionTo have one simple for loop or comprehension we can use integer arithmetic operators // (integer division) and % remainder This probably more elegant and fast
We can enumerate a Cartesian product of integers with .


Answer URL
https://docs.python.org/3/library/itertools.html#itertools.product
https://docs.python.org/3/library/itertools.html
