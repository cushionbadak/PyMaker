Natural Text
config.inimyscript.pyException:Expected Output:If I pass as a string(without '{/}'), it's working. For eg: But my requirement is I need to populate the values into the json at runtime.Anyone, Could help me? Could anyone suggest the best way for my requirement?
You should really be using JSON for your configs if you need JSON data - INI format is quite limited in that regard and there can be various artifacts if you try to cram JSON in it.That being said, if your only problem is field interpolation while using  you should escape your curly braces using double braces (i.e.  and ) to achieve the desired effect, i.e.:Or if you want it formatted:Yielding:


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.format
