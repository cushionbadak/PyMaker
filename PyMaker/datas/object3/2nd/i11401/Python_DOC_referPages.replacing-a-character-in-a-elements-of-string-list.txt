Natural Text
I have a list in which some strings end with a certain character .Now I need to remove this character from  these strings. say : result should be What I am trying is: But it removes the last character from other strings as well .
You are trying to match to  with , your character followed by a whitespace character. There are no whitespaces in your strings, hence replace does nothing and then you just slice all of your strings' last character off. Furthermore, there is no point in adding a space and then slicing it off, and your current method wouldn't account for your character elsewhere in your string, for instance . Just remove the slice and use  to remove the trailing character you don't want. You can use a list comprehension to create a new list then just overwrite your original's contents. Note that with this method, if your strings end with several of the character you don't want, they will all be removed, for example.  will become . If you want to handle trailing whitespace as well, you can supplement a space along with the character you do not want to , with .
You can use a list comprehension which considers every item in your list and for each item, takes all but the last character if the last character is a dash:


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.rstrip
