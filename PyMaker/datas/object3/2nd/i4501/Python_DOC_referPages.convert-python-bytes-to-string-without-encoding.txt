Natural Text
I am using Python 3.6 and I have an image as bytes:I need to convert the bytes into a string without encoding so it looks like:The goal is to incorporate this into an html image tag:
Why not just call str and remove the b after?In:Out:
You can decode the variable with the above.  Then convert it to base64.  UPDATED:What you really want is this:
Since you just need to convert the image to  string why not just use  function? is in bytes, but when you use  it is converted to type string.  An encoding can also be specified https://docs.python.org/3/library/stdtypes.html#str, which would be a more natural way to do things: As suggested in these answers 
I'm pretty sure  is the byte string that you want to pass to :If you want to incorporate that into an HTML string, use


Answer URL
https://docs.python.org/3/library/stdtypes.html#str
