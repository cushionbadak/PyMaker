Natural Text
The function:What im running isTest.txt contents:What I expect:But when I run the script nothing changes.
You have lines with whitespace indentation, such asThat line has spaces at the start; if you looked at the string representation you'd see:That line does not start with , it starts with : does not ignore spaces.Either strip off the whitespace, or include whitespace in the  variable:You could also use regular expressions to match lines, so using the pattern .
Your mistake is that line does not start with the text you are looking for. It starts with  not . So you either need to look for  or you need to strip the whitespace from the line, i.e. .BTW. instead of the loop you use to read in the file you could just say .
As there is lots of whitespace being created by the IDEyou need to strip it before you can use  so change it to  so that the leading whitespaces get removednext for the writing you can use an Regex to add the whitespace to your new line like thisFixed code:


Answer URL
https://docs.python.org/3/howto/regex.html
