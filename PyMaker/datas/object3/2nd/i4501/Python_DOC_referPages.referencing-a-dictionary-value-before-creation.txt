Natural Text
I want to create a dictionary where one value is dependent on a different value in the same dictionary.The usual way to do this is something along the lines of:Is there a way to do this with one statement?Here's what I thought might work, but didn't:
You can create your own dictionary:Then this will work as you want:Of course, anyone will be able to change  to whatever they want, independently of . You could override  and  to prevent direct assignments of  (and automatically update it when  is changed), or you may want to look into .If you want to pass the function to calculate  when you create the dictionary, as in your example, you can have a parameter for that:
It works if you have/save the value in a variable beforehand and use that:


Answer URL
https://docs.python.org/3/library/functions.html#property
