Natural Text
I have the formatAnd I have a task to define a function that takes a  and returns the date with 1 day increasedExample: returns  returns How can I do that, I don't know how to do this when the function takes  not , , .
This is one way using the 3rd party  library and  from the standard library.Trying to perform the same logic with  will be more restrictive, which is probably not what you want since it's not obvious you can guarantee the format of your input dates.ExplanationTry parsing sequentially with month first (default), then day first.Add a day using .Use string formatting to remove leading zeros.Pure  solution
You can try this function to return the current date at least.extension Date {Then use the extension..((Date().withWeekDayMonthDayAndYear))It's a start..


Answer URL
https://docs.python.org/3/library/datetime.html
