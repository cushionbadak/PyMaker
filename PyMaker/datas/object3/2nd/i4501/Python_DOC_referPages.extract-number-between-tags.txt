Natural Text
I want to get the number  from the string . Note that  is not fixed. It is a string and may be  next time.How to get the number?
I want to get the number  from the string .You're not being specific enough.gives you . This works if  is , too.Do you expect that the numbers are always prefixed with ? (I'm guessing  is an option, too). Are they always inside  tags? Are  tags used for anything other than keeping these numbers? Can the  that surrounds a number also contain other things than the number? Could any  contain user-defined text that looks like these numbers in the source code? Before you can define your problem more concretely, you will risk false positives.
Here with beautiful soap :


Answer URL
https://docs.python.org/3/library/html.parser.html
