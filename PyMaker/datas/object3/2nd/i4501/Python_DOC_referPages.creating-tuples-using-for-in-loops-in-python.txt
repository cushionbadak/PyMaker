Natural Text
This question already has an answer here:Why is there no tuple comprehension in Python?                    10 answers                How can I create a tuple using a for in loop in python? I tried running the following:But this gives a generator object instead of a tuple. Any ideas on how to get this? Not just needed to get the sequence of ints from 0 to 14 but other operations as well. 
Use the  constructor:
You don't actually need the generator here at all;  will work.


Answer URL
https://docs.python.org/3/library/functions.html#func-tuple
