Natural Text
This question already has an answer here:How to pass predefined arguments when storing a function                    2 answers                I need to predefine functions' parameters once, then use that functions somewhere else without passing arguments to them.Like so: Everything working as I wanted. But decorator looks tricky.I feel that there is simpler way to do that. Is it?
For your specific example:


Answer URL
https://docs.python.org/3/library/functools.html#functools.partial
