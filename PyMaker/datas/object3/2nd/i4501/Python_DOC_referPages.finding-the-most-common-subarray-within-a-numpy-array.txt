Natural Text
Example data:with a desired result of ie) The most common pair.Approaches that don't seem to work:Using  as numpy arrays are unhashable.Using  as this returns the mode over each axis, eg) for our example it gives
You can do this efficiently with  using the  function:Returns: 
One way via the standard library is to use .This gives you both the most common pair and the count. Use  index on  to retrieve the highest count.The only complication is you need to convert to  as  only accepts hashable objects.


Answer URL
https://docs.python.org/3/library/statistics.html#statistics.mode
