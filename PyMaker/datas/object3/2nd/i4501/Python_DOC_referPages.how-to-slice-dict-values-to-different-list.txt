Natural Text
The function  returns a list with two values for each index, first is , second is .Its possible to slice this list into two lists, one with  and another with ?or put this into a nested dict key, for me to use later?Instead of returning a list it will assign the two values of each index on the list to a nested dictLike  for the first numberAnd  for the second number
@ekhumoro is correct.You can unzip these pairs by passing  the iterator it contains preceded by the  operator.


Answer URL
https://docs.python.org/3/library/functions.html#zip
