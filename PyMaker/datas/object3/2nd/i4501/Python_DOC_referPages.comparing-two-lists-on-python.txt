Natural Text
I need help comparing two lists and returning the indices that they don't match.indices 4 and 8 don't match and i need to return that as a list I've tried a few methods but they haven't worked for me.
Use  to iterate over both lists at the same time and  to get the indices during iteration, and write a list comprehension that filters out the indices where the list values  don't match:
You could also just use a simple loop which scans the lists, item by item:A list comprehension could also do the same thing:
If you are happy to use a 3rd party library,  provides one way:Relevant: Why NumPy instead of Python lists?
You can use zip :output:


Answer URL
https://docs.python.org/3/library/functions.html#zip
https://docs.python.org/3/library/functions.html#enumerate
https://docs.python.org/3/tutorial/datastructures.html#list-comprehensions
