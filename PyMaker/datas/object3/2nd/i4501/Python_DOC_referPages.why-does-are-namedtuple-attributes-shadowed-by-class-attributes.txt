Natural Text
With a boring class, object instance attribute shadow class attributes:But if my class attributes are declared in a named_tuple base:... so, declaring my instance attribute through the name tuple causes that attribute to be shadowed by the class attribute ... which not what you'd expect. Why is this?
 "attributes" are implemented as descriptors (specifically, s) on the class itself, not attributes in the traditional sense (all the actual data is stored in unnamed indices of the ). In this case, the  (roughly) defines:Since the property is a class level attribute, when you define  on the subclass, it shadows equivalent definitions in the parent class.


Answer URL
https://docs.python.org/3/howto/descriptor.html
https://docs.python.org/3/library/functions.html#property
https://docs.python.org/3/library/operator.html#operator.itemgetter
