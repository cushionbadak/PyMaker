Natural Text
I'm looking for the best way to represent a char in its bits representation by a given number of length(a variable)For example:in bytes representation, the number '5' is: 0101 if the given length is 4. 
In the string '{:04b}', the "4" means that we want the result to include 4 characters even if fewer are required and the "0" means that we want it to pad the result with zeros.If you want the size under program control:Or:Or:
You can use  or  with  as format specification:You can also specify the length 4 as argument if you use :


Answer URL
