Natural Text
This question already has an answer here:Concatenating two range function results                    7 answers                I have the following code:But it gives me such an error:I am using Python 3.3.2. Any suggestions?
 objects aren't lists, so you can't add them together. You can, however, chain the two iterables:
Use :or something similar.Found from https://stackoverflow.com/a/14099894/3093524
Python 3.x's  doesn't return a list, but a  object. So, we have to create a list out of that object like this
Maybe range.extend (python2 only)


Answer URL
