Natural Text
I'm trying to use some C++ method in my python code and I'm using  library. I have wrote a simple C++ code using a simple  with no arguments and another simple method named  which takes an argument of type . I also have wrote a simple python code to import these two methods. I made two linked libraries(one  and one  because I'm using Debian) with this command:And then used .There is no problem in getting the  method, bu when I'm trying to get  I get . Here's the C++ code:Here's the Python code:I get this output:I also tried  and/or ; got the same error in both conditions. Any idea?I'm using Debian 32 bit on a VMWare Workstation and Python 2.6. 
Declare the  using :BTW, you should pass a string (c_char_p), not :About , see Specifying the required argument types (function prototypes).


Answer URL
