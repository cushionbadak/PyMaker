Natural Text
How to write a background thread in Python which will keep on calling a particular method every few minutes. Let's say, if I am starting my program for the first time, then it should call that method right away, and after that, it should keep on calling that method every X minutes?Is it possible to do in Python?I don't have that much experience with Python threading. In Java I can use  or  for this problem but not sure how to do it with Python?What's the best way to do this in Python?
Use .For example:
I think this is a lot easier to do without trying to use .  Do it directly:
I've had good luck with this class.  You would probably want to move the self.func() call before the time.sleep().


Answer URL
