Natural Text
I'm looking for a simple way of taking input from the user and placing it into a list of single character elements. For example:  Input: word2
Use  to take input:Use  to conver a string into a list containg each character.This works because  accepts any iterable and string objects is iterable; Iterating a string yield each character.


Answer URL
