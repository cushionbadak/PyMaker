Natural Text
This question already has an answer here:Python's in (__contains__) operator returns a bool whose value is neither True nor False                    1 answer                What is Python doing in its godforsaken hidden logics that makes that first statement , while the rest are ?
This is a chained comparison (see here in the docs), the same way thatisIn this case, we havebecausebecause


Answer URL
