Natural Text
I use the official python http server code, see here:The client side code is the C++ boost asio code:When I start the server and then run the C++ boost client side code using , the output is:What's the reason of it?
Your Python script is only able to handle a backlog of 5 connections. Your client is initiating 10 connections in rapid succession, too fast for the server to handle them, so the connections are being dropped. I got:Change  to something larger. I used , and there was no error.As for why the error message is displaying question marks, that may be an issue of the encoding used by your C++ program vs. the encoding expected by your terminal. The error messages displayed on my system (Mac OS X 10.9) were as shown above.


Answer URL
