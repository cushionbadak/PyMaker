Natural Text
I have the following string of data:and I need to pull the "id":38 out of it and format it a "id":"38"
since you're using the  library (based on your tag), when calling your repsonse, instead of returning , you can return :
You could use the  module:Or in this case it is a valid dictionary so you could use :If you want to convert the dictionary in  and  back to a string then just use  and  respectively.See the documentation for the use of .Otherwise to keep away from  use the  module.  As described here:Convert a String representation of a Dictionary to a dictionary?


Answer URL
https://docs.python.org/3/library/functions.html#eval
