Natural Text
I am using Watchdog to monitor a directory. If any new directories are added, I want to start subprocesses on those "Source" directories and call a program  on the directories which will output some files.My question is: What would be an elegant way of deleting the directories and their content after my subprocesses are done with that directory?
This can be done with 's  function. So long as  is the directory you want deleted, just check to make sure your subprocess has finished, and then just run If you need to wait until your subprocess is done, you can accomplish this by calling  on your  and waiting for it to return . For example:


Answer URL
https://docs.python.org/3/library/tempfile.html
