Natural Text
I have a HTML formatted text string from an earlier text processing step. It goes like this, and you see that any  character has been replaced by a  sequence:Now, I want to replace some of the  sequences within the text with a  character. The rules would be:replace any single  sequence with a single  characterreplace the first  sequence within a series of  sequences with a single  characterThe resulting string should then look like this:Any ideas for a programmatical approach using Python?
The problem can be shortened to replace every  which does not come immediately after another .To implement this strategy, use  with a negative lookbehind like so:


Answer URL
https://docs.python.org/3/library/re.html#re.sub
