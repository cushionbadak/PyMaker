Natural Text
How change a parameter's value of url? Without regexps. Now I try this, but it's long:Also, I tried by urlsplit:But on  it rise error .
Tuples are immutable.So you have to replace it .Here _ is meant to avoid conflict with fieldnames ._replace


Answer URL
https://docs.python.org/3/library/collections.html#collections.somenamedtuple._replace
