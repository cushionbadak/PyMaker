Natural Text
Example: I'm writing this data to a csv file:I've write this code to do this:When this code runs it gives the output as follows:Here's the imageThis program is writing each letter separately but i want to write the whole text variable into single cell and then move to the next row for writing next variable's data into the new cell. thanks
 accepts an iterable as the row. In this case it interprets the string as an iterable and its characters as respective items for each field. If you want to write the string in one field you can put it in an iterable like tuple.Or you can split the data with the intended delimiter:


Answer URL
https://docs.python.org/3/library/csv.html#csv.csvwriter.writerow
