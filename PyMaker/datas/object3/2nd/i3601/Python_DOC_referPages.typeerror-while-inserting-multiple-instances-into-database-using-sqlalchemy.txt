Natural Text
So I get an a list of list wich contain employees info and I want to write it to database. SQLAlchemy documuntation says its correct by, now but DB is still empty. Database was created properly, how I can now insert data from employees_list to database ?My code causes
You current problem is that you are using  method which has nothing to do with SQLAlchemy.If you need to properly insert objects into database using SQLAlchemy you can open database , create  instances, add them to session and commit changes.To do so we can define  initializer as followsOpen Create  instancesAdd  instances & commit changesTestSince I don't have access to  service I'll imitate responseFollowing steps above I got  SQLite database in my working directory. Let's open it using SQLite CLI (from SQLite tools)Further readingSession basics.Session API.


Answer URL
https://docs.python.org/3/tutorial/datastructures.html#more-on-lists
