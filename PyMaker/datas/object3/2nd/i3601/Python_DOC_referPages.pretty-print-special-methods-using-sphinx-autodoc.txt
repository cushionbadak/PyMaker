Natural Text
In the Python documentation, special methods are documented differently. For instance, instead of , the documentation reads . How can I get Sphinx to do the same?It seems like all I have to do is override the logic producing the method name, but I'm not sure how to do that. The existing autodocs events don't seem to allow it.
As DeepSpace points out, this isn't built-in functionality; the Python docs do this manually.Instead, I wrote a Sphinx transform:I haven't thoroughly tested this, and it's missing some features, but it should be a start (and hopefully helpful to someone).


Answer URL
https://docs.python.org/3/library/stdtypes.html#dict
