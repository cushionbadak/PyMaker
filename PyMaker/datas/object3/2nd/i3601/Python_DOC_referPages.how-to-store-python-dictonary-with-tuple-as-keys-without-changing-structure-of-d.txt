Natural Text
Problem:I'm trying to store a python dictionary which has tuples as keys, I didn't tried to change structure of dictionary because a value in this dictionary can be another dictionary. I tried using Json but since it only supports string type keys, it failed.Structure of the dictionary:where s1,s2 -> strings, f->floatHow do I store this dictionary as a file.
If you just need to save the object for later, then you could use pickle. Try this:


Answer URL
https://docs.python.org/3/library/ast.html#ast.literal_eval
