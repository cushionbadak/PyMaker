Natural Text
I have a txt file like this:I want to read integers  to a list.Here is my code:The output is But when i add , it shows error  Is that means the list I got is still not integer?How can I use the number as int in this list?Thx all
It is still a string.  Test this by .  You cannot add an integer to a string. What you need to do is extract each value from .  This can be done easily using  and running through this list to get each value, next you need to pass it to  to turn each value into an integer, append it to your list with integers, then you can use it as expected:Or as a single liner:
You can not att a  and an  inside a You can:
P.S. your original import is a terrible workaround, please learn to use https://docs.python.org/3/library/csv.html



Answer URL
https://docs.python.org/3/library/csv.html
