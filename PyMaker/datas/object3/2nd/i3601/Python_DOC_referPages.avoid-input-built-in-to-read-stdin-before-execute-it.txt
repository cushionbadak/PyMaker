Natural Text
I detect that input builtin capture everything from stdin even before execute it. For example:Trying to flush stdin with sys module before capture input don't work, because if you press "ENTER", for example, this is captured anyway:How I can avoid this behaviour and only start to capture the stdin when "input" statement is executed?
One way is the termios module (POSIX only):


Answer URL
https://docs.python.org/3/library/termios.html
