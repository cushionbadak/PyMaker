Natural Text
I am trying to filter out all rows in a DataFrame that contain the substring .I have tried doing this with However I keep getting an error error: nothing to repeat at position 0and can't figure out why. 
You have to escape the  character using , as it is being read as the special regex character  (meaning zero or more). In your case:Example:
By default  uses , which considers  to be a special character (matching 0 or more characters). You want to call with  to avoid using  - and instead use the Python  operator. 


Answer URL
https://docs.python.org/3/library/re.html#re.search
