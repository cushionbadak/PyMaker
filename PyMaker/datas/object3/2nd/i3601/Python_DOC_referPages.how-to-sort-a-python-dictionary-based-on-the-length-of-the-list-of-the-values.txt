Natural Text
This question already has an answer here:Python sort a List by length of value in tuple                    1 answer                As a contrived example, I have a dictionary that's set up something like this:I would like to sort the dictionary by the length of the list (which is the value of each entry) in descending order, so the result should be this:I've tried to do something like this:However, I get this error:In the REPL, I tried the following:...so I'm able to get the length of the list, but in the  function, it doesn't work.What do I need to do differently to get the  function to sort the way I want it?
Use  with .Ex:Output:If want to maintain the order.
Use an If you want you  to be ordered, you should use an . You can sort the items from the first  with a .CodeOutputPython 3.6+  are orderedAlthough, if using Python 3.6+, insertion ordered is preserved for . This is specific to the CPython implementation and will only be an official language feature starting at version 3.7.CodeOutput
You can use . Like : Or you can make it without using dict comprehension as Aran-Fey said: 


Answer URL
https://docs.python.org/3/library/collections.html#collections.OrderedDict
https://docs.python.org/3/library/collections.html#collections.OrderedDict
