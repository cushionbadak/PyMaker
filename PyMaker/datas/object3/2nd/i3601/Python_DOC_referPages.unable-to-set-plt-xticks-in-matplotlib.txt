Natural Text
I have a pandas dataframe () I am trying to plot the timeseries plot of I converted the dates to datetime object using  The time series plot is easily done using: But if I try to change the xlabels to anything else, say month names,  I get an error:Although, I can change the ticks by . What am I missing here?
As @ImportanceOfBeingErnest suggested, you should use locators to reformat the xtick labels. I have implemented them below:Here are some helpful resources:The matplotlib.dates api directivesThis example, which I drew from heavily


Answer URL
https://docs.python.org/3/library/datetime.html#strftime-and-strptime-behavior
