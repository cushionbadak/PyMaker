Natural Text
The word  produces the following sha1 hash:In python I'm simply trying to get this same output by doing the following: just produces the following byte object:I've tried  and none of the methods there seem to be able to produce the above output.How can I produce the resulting ascii hash from here?
You have a hexadecimal representation of a digest. You can use the  method to produce the same in Python:You could also convert the binary digest to hexadecimal with the  function:However, that's just a more verbose way of achieving the same thing.


Answer URL
https://docs.python.org/3/library/hashlib.html#hashlib.hash.hexdigest
https://docs.python.org/3/library/binascii.html#binascii.hexlify
