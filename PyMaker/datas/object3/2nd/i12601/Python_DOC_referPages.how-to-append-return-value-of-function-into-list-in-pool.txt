Natural Text
I'm curious if it is possible to append a return value from function f(x,y) into, for example, list. So I have this:Parallel:This is the way how I run the method  parallel but the problem is, that it returns a value which I have to append to results list.How to do that?Sequential:
The  call can be passed a function. Thecallback function will be called when  function completes and will bepassed the value returned by . Note that the return value must be picklable, as the communication between processes is done with a Queue.
If the results should end up in a list use the  method of the : is from the  module.


Answer URL
https://docs.python.org/3/library/multiprocessing.html#multiprocessing.pool.Pool.apply_async
https://docs.python.org/3/library/pickle.html#what-can-be-pickled-and-unpickled
