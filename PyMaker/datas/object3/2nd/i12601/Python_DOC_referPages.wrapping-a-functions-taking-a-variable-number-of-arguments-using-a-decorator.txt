Natural Text
I want to create a decorator which can wrap around functions with different numbers of variables. For example, below I have two functions  and , taking 2 and 3 arguments respectively. I want the wrapper to take a variable number of arguments in order to deal with this. However, when I run the code, I get , as though the  was ignored.Why is this, and how do I fix this?
Python has two variable argument variables:  and .  is a list of arguments specified without names (e.g. ), while  is a dictionary of arguments with names (e.g. ).Your code is only accepting the kwargs, so you also need to add the unnamed args:


Answer URL
https://docs.python.org/3/library/functools.html#functools.wraps
