Natural Text
If list stored in csv file below the example, does each row stored in the array?This example alert errors code.. Why ..?Additionally update the this code & errorTo show what's in the CSV, here's some simple code and the result:result:=============================================================================I edited this script..... Is it best practice ?Result
If your input data is formatted like what you added at the very end of your question, your could get your approach to work like this:However you could simplify things slightly by using a  to read the file which will automatically read the header line to obtain the fieldnames and then return a dictionary using them as keys for each row read:


Answer URL
https://docs.python.org/3/library/csv.html#csv
