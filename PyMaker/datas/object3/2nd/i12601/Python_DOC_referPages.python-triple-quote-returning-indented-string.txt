Natural Text
I have the following code:And it returns the following:Can anyone advise how I can get rid of the indentation on lines 2, 3, and 4?
Please check this answer: https://stackoverflow.com/a/2504457/1869597In general, one of your options is to use:Which is called implicit concatenation.
If you can't modify that string literal (for example, it's defined outside of your code), use  or equivalent:If you can modify it, it's much easier to remove leading spaces manually:or use implicit string literal concatenation:This option will produce expected results if you end strings with newline characters ().
One can use the dedent function. The following solution has the 3 advantages one does not have to add  character at the end of each line, the text can directly be copied from extern source,one can keep the indentation of our function,no extra line are inserted before and after of the textThe function looks like this:The result is :
In your original code, after the first line, there is indentation - the indentation inside the triple quoted string is whitespace. So you need to remove those. The following works:


Answer URL
https://docs.python.org/3/library/inspect.html#inspect.cleandoc
https://docs.python.org/3/reference/lexical_analysis.html#string-literal-concatenation
