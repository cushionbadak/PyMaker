Natural Text
In Python 2.7,  accepts non-string arguments and calls the  method of the value before formatting output:But when I pass a  object, I get  as output in both Python 2.7 and Python 3:Passing a  object to  should either raise a  or format , instead it returns the formatting directive. Why is that?
 results in call to , which returns  for the  format specifier:This happens because  attempts to format  using  and  doesn't understand the formatting directive.The  conversion flag will tell  to call  on the  instance before rendering the result - it will call :
 objects support the  options when formatting:That format includes support for literal text:The  format is treated as literal text. You can fit your time into a 5-character column using the following format:


Answer URL
https://docs.python.org/3/library/datetime.html#datetime.time.__format__
https://docs.python.org/3/library/datetime.html#datetime.time.strftime
