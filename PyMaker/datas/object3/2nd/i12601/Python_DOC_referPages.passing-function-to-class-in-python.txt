Natural Text
So I'm working on a class structure for a text-based menu. I need to create the menus and set the actions before actually running the code. So to avoid the function being called to early, I have created a class called Run which takes the given function and runs it. My problem is that I don't know how I should handle the arguments for the function. What I have so far, and which doesn't work is:Just to be clear I first want the function to run when I run 
You are calling the function before you pass it to , so  is actually the result of calling it (, as it doesn't return anything). The minimal fix is:However, you can just use  to do this, removing the need to maintain your own class and tack  onto the eventual call (which you could also eliminate by renaming  to :


Answer URL
https://docs.python.org/3/library/functools.html#functools.partial
https://docs.python.org/3/reference/datamodel.html#object.__call__
https://docs.python.org/3/library/functools.html#functools.partial
