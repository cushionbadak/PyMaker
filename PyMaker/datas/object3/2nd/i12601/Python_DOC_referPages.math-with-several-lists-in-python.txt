Natural Text
Im trying to obtain all possible combinations of three different lists in pythonsay I have three listsI want to return all combinations for which is true A[i]+B[j]-C[k] == 3which gives me all combinations in a list format, but how to go on from here?
You can use a list comprehension with  to check each of the combinations to see if it matches your criteria.
Just add . You don't want , etc., as that indexes each , while , , and  are already the elements themselves.You should also look into  to avoid nesting like this:
Just add the condition at the end:


Answer URL
https://docs.python.org/3/library/itertools.html#itertools.product
