Natural Text
I have a large file from which I need to load into a list of strings. each element will contain text until a ',' that immediately follows numbersfor eg:this should become:I currently doing  and then splitting on '~' (since my file doesnt contain ~) but this throws out the numbers before the commas.. any thoughts?
You can use  with positive look-behind assertion:
If you want it to deal with spaces as well, do this:


Answer URL
https://docs.python.org/3/library/re.html#re.split
