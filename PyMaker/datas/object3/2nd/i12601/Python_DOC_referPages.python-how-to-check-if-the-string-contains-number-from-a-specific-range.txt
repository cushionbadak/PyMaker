Natural Text
I've a long list of strings; they are basically filenames. The format is like:and so on.What I need is to filter out the names containing numbers greater than  at the end. So with the above input, the desired outout would be:This number (15) is not fixed and provided by user as input.
Like below? Just use  to search numbers?Demo:You can also search for  (use  to catch the numbers like):To remove something like  in your file (if you don't want it).If you only need the last two numbers in the filenames, for example get  from :
I would use Replace  in the above list comprehension with the variable which actually contains the number you want to check with.. Note that the variable must be of type interger or convert it to  during  condition checking.


Answer URL
https://docs.python.org/3/library/re.html#re.search
