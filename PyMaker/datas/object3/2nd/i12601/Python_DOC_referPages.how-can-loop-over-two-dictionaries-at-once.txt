Natural Text
How can I iterate through items of two dictionaries in a single loop? This is not working:
Use :
Since in Python2,  will generate a list of  tuples, you can concatenate the two lists, whereas in Python3, it will return a viewing object, hence we need to convert it to a , so the following is also one way to do:


Answer URL
https://docs.python.org/3/library/stdtypes.html#dictionary-view-objects
https://docs.python.org/3/library/stdtypes.html#dictionary-view-objects
