Natural Text
I'm trying to replace only one instance of a specific word in a string using regex, where the word that needs to be substituted appears at least twice. For instance I have the following: so far I have:The problem that I'm having is that when using  it is replacing both instances of  in . I would like to only replace the first and leave the second instance of  unaffected so that it could be substituted by . 
Output:
For simple expressions like that, you can just use  with a specified :


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.replace
