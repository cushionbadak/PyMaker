Natural Text
I want a class "Library" which has a class variable "books" which is a list of all the books in the library.  So may class beginsNow I want to add a book to my collection, but I can find no syntaxto do so.  The model I have in my head is something likewhich I would expect to call with something like from the main routine with something likeHowever, I've not been able to find any way to do this.  I always get an error with the "append" where I try to add the book to the list.
You should declare  as an instance variable, not a class variable:so you can create an instance of :Notes: For further information about  , you can read this post.This assumes your  class is implemented correctly.

look at this example for the initialization and the setter:


Answer URL
