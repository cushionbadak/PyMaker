Natural Text
I'm trying to create a decorator class that counts how many times a function is called, but I'm getting an error message that says:and I really don't know how I'm giving it four arguments.  My code looks like this:Are there some other parameters I should (or shouldn't) be passing into the def exit function?  Any tips or ideas would be appreciated.As an aside, my line of code that says "print 'f count =',f.count" appears to be outputting the memory address rather than the value, but that's a whole different problem.
the  method should accept information about exceptions that come up in the  block. See here.The following modification of your code works:Then you can try raising an exception in one of your  blocks and it'll be caught in .


Answer URL
https://docs.python.org/3/library/traceback.html
