Natural Text
Within the loop, there's tons of code, so I don't want to just duplicate all of that code again just to be able to incorporate the '_' case.So it would go through A, B, C, .. Y, Z, _without any code duplication
Construct a string containing those characters and tack on a , then loop through it:
Use  and simply concatenate the :
You can iterate through each character of a string:
I would use  (along with  as has been suggested by other answers):


Answer URL
