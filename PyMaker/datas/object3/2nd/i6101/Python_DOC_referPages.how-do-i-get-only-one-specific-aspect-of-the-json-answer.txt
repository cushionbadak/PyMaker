Natural Text
so I have the following block of JSON:I need only a String with the content of the first label field.
Following snippet returns the output as If you have more of such data, you could iterate over the  variable and get the desired results by substituting the  indices with the iterateor at both the places.For example, if the json is somewhat as followsYou could use the following  script to get similar output.

If you can also try a regex approach:


Answer URL
https://docs.python.org/3/library/json.html#json.loads
