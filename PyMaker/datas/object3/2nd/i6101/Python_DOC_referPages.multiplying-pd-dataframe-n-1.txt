Natural Text
I have a pandas  that looks like the following:I'd like to cumulatively multiply the values down the columns. Using column  as an example, the second row becomes , the third row becomes , and the last row becomes . Same for column B. So, the desired result is:There must be some built-in way to do this, but I'm having issues even doing it with for loops due to chained assignment issues.
Use You can also use  on the values:Finally, using  (just for fun):
Use :Alternative :Timings:
data frame has a method named . you can use it as follows


Answer URL
https://docs.python.org/3/library/itertools.html#itertools.accumulate
