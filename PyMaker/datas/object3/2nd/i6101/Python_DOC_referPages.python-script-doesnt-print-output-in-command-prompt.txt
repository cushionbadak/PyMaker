Natural Text
I need some advice with a Python script. I'm still new and learned it by myself. I found the script on Google. After I retype it, it doesn't print the result in the console. How can the result of the script be shown in the console? Details as below:C:\Python27>test1.py af8978b1797b72acfff9595a5a2a373ec3d9106dC:\Python27>After I press enter, nothing happens. Should the result be shown or not?Here's the code that I retyped:
The first line in your script is called a .A Shebang line tells the script to run the Python interpreter from that location.The shebang line you provided is a Linux system path, but it looks from the path you are executing Python from, that you are running on Windows.You can do one of two things here to fix that:Remove the Shebange Line.Remove the first line from your script.Execute the script using Modify Your Shebang line.Change the first line of your script from  to   (This is assuming that python is in your systems  variable.)`Execute the script using Also, you are trying to import the  module that is not installed in the standard library. If you haven't installed this yet, you can do so by going to your Python install directory and go to the  folder.Hold  and right click and go Type  and hit enter.After that you should be good to go, execute the script by navigating to it and type If a Python script doesn't have the :
you need to run your script using python. try:


Answer URL
https://docs.python.org/3/using/windows.html#python-launcher-for-windows
