Natural Text
I have a 2 dimensional list. Example:I need to make a new list whose elements are the elements of each sub-element preserving the order.Example:
Q: The new list must take the first element of each list first, then the  second, and so on..A:You can use  and a list comprehension.For your new list, you could do the same, but first unpack the list with a 
first transpose the list matrix with zip then chaining all the inner list together with itertoolsalso rename variable list to lst, not a good practice to use builtin  as variable name


Answer URL
https://docs.python.org/3/library/itertools.html
