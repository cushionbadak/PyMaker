Natural Text
How to concatenate strings in python which has single quotes and double quotesis giving a "\" before the single quoteI need an output like this 
This has nothing to do with how the string actually looks like; try printing it:It is simply a matter of how Python represents the string in an interactive session (or when d).See also: Built-in Functions:  in the Python docs.


Answer URL
https://docs.python.org/3/library/functions.html#repr
