Natural Text
I try to check if a variable is an instance of a number of any type (, , , , etc.).I cam accross this question and its answer: How to properly use python's isinstance() to check if a variable is a number?However, I would like to exclude complex numbers such as .The class  looked perfect but it returns  for  numbers...In contradiction, it works fine with  for example.The documentation describes some operations which should work with the number, I tested them without any error on a decimal instance.Decimal objects cannot contains complex numbers moreover.So, why  would return ? 
So, I found the answer directly in the source code of :Indeed, adding  to  would raise a .In my point of view, it violates the principle of least astonishment, but it does not matter much.As a workaround, I use:


Answer URL
https://docs.python.org/3/library/decimal.html#decimal.Decimal
https://docs.python.org/3/library/fractions.html#fractions.Fraction
