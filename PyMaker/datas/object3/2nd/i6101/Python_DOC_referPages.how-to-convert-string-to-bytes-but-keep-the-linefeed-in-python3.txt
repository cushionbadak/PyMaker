Natural Text
Originally I have python 2.7 code, which can read a file and deliver it to perl code:Now I would like to convert it to Python3 code, but in Python3  reads bytes-like object rather than 'str'. So I use , but it turns out that all the line feeds in will file be converted to , for example, in the file I have But unfortunately the encode will make it:How could I correct this error? I want to send the byte format stuff to pearl without those 
Use : It returns a string containing a printable representation of an object. output:


Answer URL
https://docs.python.org/3/library/functions.html#open
