Natural Text
I am trying to parse some information thats in a var meta window, and I am just a little confused how to grab just the value for the "id". My code is belowAnd the output of this prints Again, I just want to print just the value of the ID ("463448473639").
you can retrieve all the attributes using following sytax. and if you looking some specific, it's dictionary. the full code is as below However if you want to get value of ID from innerText        the only way to do is, as below. 
It looks like you are going to want to do something like:I haven't tested that but if you want to get what is in between the script tags I think that is they way you will do it. get_text doc


Answer URL
https://docs.python.org/3/library/json.html#json.loads
