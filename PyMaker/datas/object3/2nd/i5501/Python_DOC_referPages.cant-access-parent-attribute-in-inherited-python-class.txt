Natural Text
This is my parent class,This is my inherited object,This throws up the following error,What am I doing wrong here.
It should be , not .Also, if this is Python 3, you can simplify it to just ; if Python 2, you also need to change the declaration of  toto get a new-style class.
If it is python 3 you have to use:  instead of  and instead of If it is python 2 you have to use: instead of  and instead of 
In super you should put your class type


Answer URL
https://docs.python.org/3/library/logging.html#logging.Logger.log
