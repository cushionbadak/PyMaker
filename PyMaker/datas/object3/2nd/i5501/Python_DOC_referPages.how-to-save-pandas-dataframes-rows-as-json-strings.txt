Natural Text
I have a pandas DataFrame  and I convert each row to JSON string as follows:Then  I want to iterate over the JSON strings (rows) of  and make further processing as follows:The problem is that  does not really split  into separate JSON strings.How can I do what I need?
To get each row of the dataframe as a , you can use :Code:Results:
Starting from v0.19, you can use  with  parameter to save your data as a JSON lines file.This eliminates the need for a loop to save each record, as a solution with  would involve.The first 5 lines of  look like this - 


Answer URL
https://docs.python.org/3/tutorial/datastructures.html#dictionaries
