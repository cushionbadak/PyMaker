Natural Text
I'm trying to build a method where if an item is not in a dictionary then it uses the last member of a list and updates the dictionary accordingly.  Sort of like a combination of the pop and setdefault method.  What I tried was the following:So I would like the output to be where  gets updated to dict1, that is to say, if b is not in the keys of dict1 then the code updates dict1 with an item using b and the last item of c. 
It might be possible for you to abuse a :This will pop an item from  and make it a value of  whenever a key missing from  is accessed. (That means the expression  on its own has side-effects.) There are many situations where that behaviour is more confusing than helpful, though, in which case you can opt for explicitness:which can of course be wrapped up in a function:


Answer URL
https://docs.python.org/3/library/collections.html#collections.defaultdict
