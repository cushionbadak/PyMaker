Natural Text
I would like to know how to concatenate a string to the end of all elements in a list.For example:
rebuild the list in a list comprehension and use  on both parameters
output['1a', '2a', '3a']
In one line:You need to convert the integer into strings and create a new strings for each element. A list comprehension works well for this.
You can use :Output:

Convert each element in the list to string and then add the required string to each element of the list.
A 1 liner:


Answer URL
https://docs.python.org/3/tutorial/datastructures.html#list-comprehensions
