Natural Text
Using the Python Launcher works fine: This also works fine to run the corresponding  (which is also not in ): .Is there a way to make this also work for installed scripts? Background: This allows installing multiple Python versions in parallel, and using them easily even when they are not in . But for example I can't run pyinstaller. works. works, but still requires me to know the installation path. does not work. It just prints . does not work. It just prints .Is there a way to do this anyway?
I believe the module name is incorrect.  This works for me:You can see the module names via pip.  For exampleon my workstation shows:Note the case of PyInstaller in the list.


Answer URL
https://docs.python.org/3/using/windows.html#launcher
