Natural Text
I am trying to test my implementation of a simple service that uses a TCP socket. I would like to test my server without creating a connection for every test case. I would like to make a single connection for the entire test suite. Currently I have something that looks like this..What is a good way to keep a single socket connection open and have multiple test cases use that connection?
If you look at the docs you'll find that can put  and  methods in your test class that will get executed once. So you can do something like:


Answer URL
https://docs.python.org/3/library/unittest.html#organizing-tests
