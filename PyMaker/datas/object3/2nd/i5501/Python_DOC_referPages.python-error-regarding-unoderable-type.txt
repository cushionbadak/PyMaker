Natural Text
Error message: Just wondering why its having this sort of error but it works fine without the "and lvl >= list"
In Python,  is a datatype. What you're doing wrong is you're essentially trying to see if 5 is greater than books on a bookshelf. You'll want to do:
I think your code should actually be written like this:Note: Specifically, regarding why your code seems to "work" when you remove the  is actually because  is a reserved keyword for the type :And so your code naturally errors when you are trying to compare the integer  and the type .


Answer URL
https://docs.python.org/3/library/functions.html#func-range
