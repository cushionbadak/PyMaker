Natural Text
I am trying to figure out how to fetch a single value from a UserID and print it in the console. My current piece of code keeps returning "None" even though there is a value for Currency for the UserID I am searching.
When you call , the results are computed and fetched, and you use // to retrieve them. In your case, your query returns a single row as the result, so calling  in the  causes the result to be fetched and subsequently thrown away, so another call to  will yield  as the pointer has already advanced past the only row in the result set.The idiomatic sqlite way of checking if data exists, is to query a row, and test its truthiness - 


Answer URL
https://docs.python.org/3/library/sqlite3.html#sqlite3.Cursor.execute
