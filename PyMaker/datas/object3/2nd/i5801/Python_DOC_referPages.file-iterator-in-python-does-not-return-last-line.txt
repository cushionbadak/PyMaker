Natural Text
I'm new to Python and I have simple script to read file and print it to output:If file ends with new line symbol it prints all lines from file. But if last line does not contain new line symbol it ends with one before last. I run script in Windows environment and use python 3.6. For example file for first case:And script's output is:But when last line contains new line symbol output is:What I'm doing wrong?
To ensure that the buffer is empty, you could add  after your loop completes to make sure it's empty (as  is line buffered).


Answer URL
https://docs.python.org/3/tutorial/inputoutput.html
