Natural Text
Is there a way to copy a field, from a .gz file into a txt file without unzipping in either Windows commands or Python?The .gz file is over 15000 lines long If field 1="SVR" i need to output fields 2, 3, 4, 6, 7, 11, 12, 13 and 21. (if $1 = other, ignore)Can be written to any other file type, does not have to be txt. 
This can be done using Python's gzip library to read the  file, the  library to parse the CSV file inside and the use of  to extract the required items from each row:So if you had a CSV containing something like:You would get an output CSV file containing:Tested on Python 2.x. For Python 3.x, the files would need to be opened as:


Answer URL
https://docs.python.org/3/library/gzip.html#module-gzip
