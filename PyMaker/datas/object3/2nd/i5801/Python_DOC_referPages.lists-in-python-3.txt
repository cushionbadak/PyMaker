Natural Text
So the problem is that I have a list which is made of pairs of numbers [ (0,0),(0,1)(0,2) ...etc  and I would like to know how to delete from this list all pairs with the same numbers . List was created by this function. 
You can avoid duplicate tuple elements while generating the list. Just add an :This could also be written more succinctly as a list comprehension:Yet another option is to use :Removing items afterwards is also possible with a list comprehension:which creates a new list without the duplicate items then rebinds it to . You can overwrite the list in place with:
Method 1: Using list comprehnsion:Method 2: Building the list manually:


Answer URL
https://docs.python.org/3/library/itertools.html#itertools.product
