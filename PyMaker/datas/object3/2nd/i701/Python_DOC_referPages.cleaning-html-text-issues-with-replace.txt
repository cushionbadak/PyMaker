Natural Text
I have an editor that ads:I use:The problem is that sometimes remove everything, an in all cases for the first paragraph I always get: ﻿(removes the first chracter in tag).
Your error is in how you use the strip method, which removes any leading or trailing  sequence of the  characters. So  would be stripped to , for example.If you want to remove any  in the beginning and in the end of the  string, you can do it like so:
Base on you solution, why not just do ?Shouldn't that be enough ?All  and  would get replaced and the rest of string will remain untouched.For  you will get .For  you will get .For  you will get .


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.strip
