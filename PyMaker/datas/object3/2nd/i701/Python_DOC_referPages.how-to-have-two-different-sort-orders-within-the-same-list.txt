Natural Text
This question already has an answer here:Sort a list by multiple attributes?                    4 answers                I have a sorted list such as which is the output of . However, if there is a tie with the 0th element of each sub-list, as is the case with the middle two elements, then I need the sort to have the lists with  come first. So the desired output would be
You can do it sorting by another element, a , indicating whether or not the second element is :This will be using the following list of tuples in order to sort the list:Where the second element will be  if it is not  in . Now sorting this array will yield the expected result, given that  i.e.  will come before   when the first elements match:As @jpp points out you could simply sort by the second element lexicographically in this case:
try this


Answer URL
https://docs.python.org/3/howto/sorting.html
