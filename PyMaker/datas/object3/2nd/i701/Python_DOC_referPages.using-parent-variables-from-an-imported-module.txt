Natural Text
I have some script, i.e.  It uses a module  with some variables:functions integers and so onSo, I need to extend the functionality of  by a nice brand-new submodule named  that should use variables of  to deliver me new features.Here's some code of :So I'm using  (I know there are no overriding names) in the  code and then using  in That code throws So how I can import submodule to use module's variables in it?EDIT: Just explaining why I need this. I'm writing an API wrapper with a bunch of features, but these features should be optional to not overweight main file so users can just use what they need.
So, I need to extend the functionality of module.py by a nice brand-new submodule named submodule.py that should use variables of module.py to deliver me new features.What are you trying to achieve is execfile from Python 2.xNote that you can pass  and  in -call.For Python 3.x you can use ideasman42 recipe from What is an alternative to execfile in Python 3?


Answer URL
https://docs.python.org/3/library/functions.html#exec
