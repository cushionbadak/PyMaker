Natural Text
Python 3.6.6 using miniconda.I followed the instructions from the conda docs. I created the  file and added my variables in there:When I activate the variable, the command prompt lists the environments I saved but Python doesn't recognize them:Am I missing a step?
In Python, one way to handle env vars is via [Python 3]: os.environ. This is how you would do it for %OAUTH_TOKEN%:Notice the quotes surrounding OAUTH_TOKEN, which based on your error, you seem to have forgotten.


Answer URL
https://docs.python.org/3/library/os.html#os.environ
