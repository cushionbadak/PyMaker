Natural Text
Using an incoming string like list:With a lenght N and each element with the following format:What I want?Iterate the list and process each JSON elem individually How could I split to take each JSON element?What I have tried:Option 1:Output:Option 2:Output:Option 3:Simple for to iterate over the listOutput:Possible solution:Is this the correct way?
Consider using the json package for this:
This both functions solved my use case:The inconvenient of the following function is that you will need to know the position where the "," is separating each element of the incoming list, so for real data where the number of fields of the json could be different in each element of the list you can not use: 


Answer URL
https://docs.python.org/3/library/json.html
