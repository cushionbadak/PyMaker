Natural Text
I am cleaning up a column in a pandas dataframe getting rid of garbage usernames that contain certain characters.  Here is the example:I am using the following and it is working as expected:But I would like to add two more characters: and When I try to add these like  for  and  for  but I receive errors. How should I go about this?
Try adding a  in front of the string so Python will interpret your string as raw:
You can avoid manually specifying  conditions via :This use case is specifically mentioned in the docs, which also advise: as of Python 3.7, only characters that can have special meaning in a regular expression are escaped.


Answer URL
https://docs.python.org/3/library/re.html#re.escape
https://docs.python.org/3/library/re.html#re.escape
