Natural Text
This question already has an answer here:How to resolve “ValueError: attempted relative import beyond top-level package”                    1 answer                I have a project with this directory structureI want to import a def from  from .I followed the documentation of relative path import here and studied the answers in this stack overflow post.I tried to write , (With one dot before the  folder) I got the following error.And I tried to add two or three dots before the  folder  then I got the errorI have added the  files at each level of the directory structure as suggested by the documentations]2 as you see, but no luck at all!What am I doing wrong?
I'll be honest: relative import could be hell. That's why Python 3 does absolute imports by default (https://docs.python.org/2.5/whatsnew/pep-328.html). If you use Python 3 - make all your packages absolute.If you still use Python 2 you can switch default behavior by adding the following line at the top:


Answer URL
https://docs.python.org/3/tutorial/modules.html
https://docs.python.org/3/tutorial/modules.html
