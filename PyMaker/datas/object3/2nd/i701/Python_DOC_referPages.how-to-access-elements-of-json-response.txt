Natural Text
I have two types of answers that JSON format is for me since then, and I want to parse this information.when I try this code I have this output:when 'play_voice' is True, then my code works fine but I have a 'play_voice' is False then this code does not run at all.
I think you want the value of the  key for the first dictionary containing such key in the  list, in which case both sides of your list comprehension are wrong and you want something like:A more efficient implementation would be to only find the first such dictionary, using  on the generator expression, rather than run through the whole  list:


Answer URL
https://docs.python.org/3/library/functions.html#next
