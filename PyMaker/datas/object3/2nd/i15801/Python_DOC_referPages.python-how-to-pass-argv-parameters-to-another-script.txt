Natural Text
I need to create a wrapper script that gets parameters from the shell and that passes all of them as they are, to another script.In Perl, I would do:Is there a way to do the same thing in Python?
Call  with  minus the first element, which is the name of your Python script.Example with  as the subprocess:


Answer URL
https://docs.python.org/3/library/subprocess.html#subprocess.call
https://docs.python.org/3/library/sys.html#sys.argv
