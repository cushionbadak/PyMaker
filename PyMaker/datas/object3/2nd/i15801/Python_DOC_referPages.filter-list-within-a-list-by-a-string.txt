Natural Text
I have an array of basketball players that has an array for each player containing data like this:I would like to filter this array to only show players who have the year  in their array, so I can get a list of all active players. Here is the code I'm trying now:And the error in the console is:Can anyone help me out with this? I would really appreciate it!
 is a set but  is a list.  Using  only works with two set objects, not a list and a set.However, you should not be using sets for this in the first place because you only want to see if a string is inside a list.  For that, you should use a simple  membership test : will return  if  is in the list .  Below is a demonstration:


Answer URL
https://docs.python.org/3/reference/expressions.html#in
