Natural Text
In the official python documentation in the Data model section, the  method is defined as:Then, the documentation explains that These methods are called to implement the augmented arithmetic assignments ( for )But what is the syntax of  that allows to use the  argument ?
The third argument is there just for symmetry with .The argument was included in the original 'add in-place operator equivalents' commit but there is no support to use it from Python code, other than calling the  method directly.For example, the  opcode handling passes in  as the third argument:Most likely it is there to make implementing  as an alias for  trivial even from C code.


Answer URL
https://docs.python.org/3/reference/datamodel.html#object.__ipow__
