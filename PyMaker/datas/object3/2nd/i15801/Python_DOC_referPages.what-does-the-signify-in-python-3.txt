Natural Text
This question already has an answer here:What exactly does += do in python?                    11 answers                For example, when adding lists together:
 means add the contents of  variable with the contents of variable  and again store the result to  variable.Code explanation:
It's shorthand for  will loop once through every element in , assigning the value to a temporary variable Check out these duplicates:Duplicate 1and not exactly a duplicate but another example of how it works
It is shortened operation used in any language There can also be "-=", "*=" and "/=" respectively.


Answer URL
https://docs.python.org/3/genindex-Symbols.html
