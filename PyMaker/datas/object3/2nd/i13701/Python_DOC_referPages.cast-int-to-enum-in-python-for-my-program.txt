Natural Text
How can a  be cast to an  in python?
If you want the flexibility to convert between  and an enum, you can use Note: If you are using python<3.4,  has been backported, but you will need to install it, e.g. via More on enums in python - https://docs.python.org/3/library/enum.html
You can use the built-in  (Python 3.4+), the   backport, or, for more advanced needs (which this is not), the new  library.If you use :If you have an  and want the matching  member:Once you have an  member, it is already an :The downside to  is that every  member will compare equal to every other  member that has the same value:If you want to keep your s separate, but don't want to lose the ability of an  you could do:Lookup works the same:But members are no longer s:So you do have to cast it (or access the  attribute):And we no longer have the problem if  being equal to :If you are using Python 3+ there are some cool things you can do with , such as:which results in the same class as the last  example.


Answer URL
https://docs.python.org/3/library/enum.html#intenum
https://docs.python.org/3/library/enum.html
