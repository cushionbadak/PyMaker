Natural Text
I am using a class member function that has a different effect when called within the class in which it is defined than when it is called externally.here is its internal call:When I call getLexname externally:I get:The result gets padded with spaces and .strip() does not seem to have an effect.However when I call getLexid externally:I get, as expected:Thanks for any help!
Actually you are seeing the behavior of print:Print objects to the text stream file, separated by sep and followed by end. sep, end and file, if present, must be given as keyword argumentsBy default the separator (sep) is one space single space.In your example you are comparing the output of two different print statements  and .From your example out bit for the external call I gather that you are using Python 3. For Python 3 you get:You can change the behavior by specifying an empty separator:


Answer URL
https://docs.python.org/3/library/functions.html?highlight=print#print
