Natural Text
This is a simple question:In Python, if a function does not contain a  statement, does it automatically return ?
Yes. From the Python tutorial:In fact, even functions without a return statement do return a value, albeit a rather boring one. This value is called None (it’s a built-in name). 


Answer URL
https://docs.python.org/3/tutorial/controlflow.html#defining-functions
