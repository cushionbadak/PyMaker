Natural Text
This question already has an answer here:Finding in elements in a tuple and filtering them                    4 answers                I have the following tuple containing molecule number () and corresponding  from certain reference point. The molecules are arranged in ascending order according to . I can extract  and  as two individual lists. However, I would like to get the elements of  by satisfying the following condition,  ; so I will get . How I can get this ? After satisfying the condition; 

You can use the builtin  function for this, giving the condition as a lambda expression in first argument and the list to filter in second argument -For Python 2.7 , you do not need the  as filter returns a list.In Python 3.x,  function returns an iterator which yields elements where the condition is satisfied (that is the condition returns  .In Python 2.7 ,   function returns a list of elements where the condition is satisfied (that is the condition returns  .Example/Demo -
You can try like thisOr you may think about @Anand S Kumar is answer of using , which is a more pythonic way.Hope it helps
You might want to look at Pandas, it's a very commonly used package for tabular data analysis of this type:


Answer URL
https://docs.python.org/3/library/functions.html#filter
