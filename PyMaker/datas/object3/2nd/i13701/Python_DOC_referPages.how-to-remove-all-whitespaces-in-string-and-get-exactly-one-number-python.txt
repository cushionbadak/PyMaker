Natural Text
I have a string. For example:orIn this case I would like get number: . But any number can be there. It would be , for example.I had tried this solution:Could you help me?
Do split and replace.or
It looks like you want to remove all whitespace from the string, then parse the longest initial prefix of all digits, stopping if there is a non-digit (such as a hyphen). Here is my code:Btw, don't name your variable , because it confuses with the built-in type .


Answer URL
https://docs.python.org/3/library/stdtypes.html#text-sequence-type-str
