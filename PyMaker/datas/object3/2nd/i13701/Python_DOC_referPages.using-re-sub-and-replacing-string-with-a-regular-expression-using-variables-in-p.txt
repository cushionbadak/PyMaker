Natural Text
Suppose I have a stringAnd I wan't to replace the spaces with a number of characters, for example, 5 dash marksbut this will be a variable, so i cant just do:I need something like the following:Is something like this possible?
Yes, you can do this:
You can define a function in 


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.replace
