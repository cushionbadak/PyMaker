Natural Text
The origin code is(simplify for example):now I only called func_a, or maybe func_b, it depends on configuration.so I want to dynamic do from ... import ..., such as:but I don't know to how to implement it?What I have thought is do import in func_X():but this way will do import every time when call function.Supplement:I have tried  / , but can't implement this condition
 is a builtin function which takes the module name as a string and returns the module as an object. See the documentation:The function imports the module , potentially using the given  and  to determine how to interpret the name in a package context. The  gives the names of objects or submodules that should be imported from the module given by .
before I ask the question, I have tried  and , but can't write out the code.thanks for @machine-yearning, I read the entire doc of .this is my solution:


Answer URL
https://docs.python.org/3/reference/import.html
https://docs.python.org/3/library/importlib.html#module-importlib
