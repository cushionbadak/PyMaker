Natural Text
suppose I have some data that looks like this:and it goes to 20,000, how would I get the average and stdev of this data?I think this works for calculating averages, but I do not know how to get the stdv from here. 
In Python 3 you can use the  module, but first you need to arrange the values from your data into a sequence. Your data comes from a CSV file, so open the file, read in the lines, and convert the value to a float:OutputUsing the 4 values from your sample data:It looks like you are using Python 2. There is a back port of the Python 3 statistics module for Python 2, and the above code will run without change. See https://pypi.python.org/pypi/statistics for info and install with .


Answer URL
https://docs.python.org/3/library/statistics.html#module-statistics
