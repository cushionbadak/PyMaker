Natural Text
How do you get Python 3 to output raw hexadecimal byte? I want to output the hex .If I use , I get the ASCII '170'.
 takes unicode text and encodes that to an encoding suitable for your terminal.If you want to write raw bytes, you'll have to write to  to bypass the  class and avoid the encoding step, and use a  object to produce bytes from integers:This won't include a newline (which is normally added when using ).
The solution was to first create a  object:And then output this to  using the buffered writer
You can also use the hex-byte escape sequence ("\x") in a bytestring, specifying the  method:will output on your terminal:and inspecting with :


Answer URL
https://docs.python.org/3/library/io.html#io.TextIOBase
