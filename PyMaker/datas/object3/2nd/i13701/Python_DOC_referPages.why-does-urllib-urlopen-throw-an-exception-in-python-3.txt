Natural Text
Why is the following python code throwing an exception?Traceback
Because the functionality was moved to the  module:A whole lot of things were moved in Python 3 from their former Python 2 locations. The Python 2 documentation usually names the new location; the  documentation page is no exception:Note: The  module has been split into parts and renamed in Python 3 to , , and . The 2to3 tool will automatically adapt imports when converting your sources to Python 3. Also note that the  function in Python 3 is equivalent to  and that  has been removed.The Python 3 documentation page for  names all new modules under the namespace.


Answer URL
https://docs.python.org/3/library/urllib.request.html
https://docs.python.org/3/library/urllib.html
