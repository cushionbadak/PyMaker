Natural Text
This is the function call below and I'm trying to construct the function , I need to map the tuple using functional prog. so that it turns into , and the last one is . Then I will sum these and return this value in .
You can play with python built-in functions :Demo :You can use  function to apply the  function on pairs and sum the result :Instead of  as a more efficient way you can use  and a generator expression within  :
You can do this with a combination of , , and :Explanation: combines  into a list of 2-tuple pairs. performs the mathematical operation on each 2-tuple element. sums the results of #2 together.
This should do it:


Answer URL
https://docs.python.org/3/library/functions.html
