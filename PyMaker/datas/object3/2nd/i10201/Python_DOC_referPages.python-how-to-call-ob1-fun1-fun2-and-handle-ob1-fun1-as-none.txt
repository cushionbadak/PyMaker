Natural Text
I tried to use the following code, but looks like uglyIs there a better way to do this?
If you just want  to be  if  is  or if  doesn't exist as an attribute, you could use  and use  as a default. Note that  will return .
Use the EAFP (Easier to ask for forgiveness than permission) approach. Wrap it in a try/except and handle your exception accordingly: You could also use hasattr to check if  is in 
How about:


Answer URL
https://docs.python.org/3/library/functions.html#getattr
https://docs.python.org/3/library/functions.html#hasattr
