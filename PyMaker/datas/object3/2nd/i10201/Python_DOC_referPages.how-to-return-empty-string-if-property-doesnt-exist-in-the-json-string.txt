Natural Text
This question already has an answer here:Check if a given key already exists in a dictionary                    21 answers                I have a JSON string from which I am trying to extract a property value using Python as shown below:Now it is possible, the property value that I am trying to extract doesn't exist in that JSON string so it will fail. How can I return empty string if property doesn't exist at all in the JSON string.This line can fail if property doesn't exist.
Simply use , i.e.:See https://docs.python.org/3/library/stdtypes.html#dict.get for more details.UPD:Thanks to @jez for pointing out, that  is not guaranteed to be a dictionary. However, the result for JSON parsing is known: if it's not a dictionary, then it's a list, number or a string. In this case, wrap it into a type checking routine, e.g.:
Like Zaur, I would also have suggested  but I presume the OP's objection to this is that  might or might not be a   (if it is a , then  will work in Python 2 or 3).  If that's the problem, then the following might cover a broader range of cases: 


Answer URL
https://docs.python.org/3/library/stdtypes.html#dict.get
