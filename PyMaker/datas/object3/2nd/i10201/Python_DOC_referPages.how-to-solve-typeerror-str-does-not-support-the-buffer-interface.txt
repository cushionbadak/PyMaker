Natural Text
My code:I want to convert a long int to bytes. But I am getting . 
In Python 3, strings are implicitly Unicode and are therefore detached from a specific binary representation (which depends on the encoding that is used).  Therefore, the string  cannot be written to a file that is opened in binary mode.Instead, you can just write  directly to the file:Moreover, your code () in fact creates a Unicode string containing the string representation of  (i.e., ), which isn't what you think it is.  For example:
In addition to @Will answer, it is better to use  statement in to open your files. Also, if you using python 3.2 and later, you can use  or  to reverse the process.Sample of what I said:The last  is to show you how to reverse it.


Answer URL
https://docs.python.org/3/library/stdtypes.html#int.to_bytes
https://docs.python.org/3/library/stdtypes.html#int.from_bytes
