Natural Text
While using data descriptors in building classes, I came across a strange behavior of getattr function on a class.LHS return an empty string, while the RHS returns an instance of . I thought  on an object was to get the value for the key inside the . How does  function work on a class object?
 triggers the descriptor protocol, even on classes, so  is called.That returns  because your  method has no explicit  statement.From the Descriptor Howto:For classes, the machinery is in  which transforms  into . is just a dynamic from of  here, so  is executed, which is why you don't get the same thing as .If you expected it to return the descriptor object itself, you'll have to do so explicitly;  will be set to  in that case:This is what the  descriptor object does.Note that the  argument to  is optional; if not set you are supposed to use  instead.
 is the same as . This calls the respective descriptor, if present. So it provides the value presented by the descriptor, which is . 


Answer URL
https://docs.python.org/3/howto/descriptor.html#invoking-descriptors
