Natural Text
In Python 3, there's a useful  feature to re-raise an exception. That said, how do you find the original (/ re-raised) exception from the raised exception? Here's a (silly) example with comments to demonstrate what I mean--
It's in the  attribute of the raised exception. Taken from the docs on the  statement it says regarding :The  clause is used for exception chaining: if given, the second expression must be another exception class or instance, which will then be attached to the raised exception as the  attribute (which is writable). If the raised exception is not handled, both exceptions will be printed.So, in your given scenario, ing the  attribute:Will print out:
Whenever an exception is raised from an exception handler (the  clause), the original exception will bestored in new exception's .Whenever an exception is raised using  syntax, the exception specified in  will be saved in the  attribute of the new exception.In the usual use case, that amounts to both  and  containing the original exception:Here is also an example of when  is set:and an example of when  is set:


Answer URL
https://docs.python.org/3/reference/simple_stmts.html#the-raise-statement
