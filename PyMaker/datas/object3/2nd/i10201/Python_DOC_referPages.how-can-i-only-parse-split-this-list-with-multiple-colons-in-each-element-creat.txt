Natural Text
I have the following Python list:I would like to take the entries of this list and create a dictionary of key-values pairs that looks likeHow does one parse/split the list above  to do this? My first instinct was to try , but then I think it is impossible to retrieve the "key" for this list, as there are multiple colons What is the most pythonic way to do this? 
You can specify a maximum number of times to split with the second argument to .Result:If you'd like to keep track of values for non-unique keys, like  and , you could add keys to a :Result:
You can also use Output:


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.partition
