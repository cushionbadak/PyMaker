Natural Text
I have date in string:and I use (according to https://docs.python.org/2/library/datetime.html#strftime-strptime-behavior):but I get error:How to do it correctly?
python datetime can't parse the  part (You might want to specify it manually in your format). You can use  instead:
%z is the ,  is . Therefore:I also replaced your  with ;  is the month, numerically, so in your case  would be parsed as April.
Simpler way to achieve this without taking care of  formatting identifiers will be the usage of . For example:


Answer URL
https://docs.python.org/3/library/datetime.html#available-types
