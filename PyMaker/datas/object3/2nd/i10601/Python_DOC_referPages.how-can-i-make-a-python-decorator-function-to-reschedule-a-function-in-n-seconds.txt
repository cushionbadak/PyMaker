Natural Text
This is my current code:In this example I'm trying to schedule the send_command function to run in 5 seconds and every 10 seconds afterwards. It seems to work but I'm a bit confused with the  call. Any comments will be appreciated.
Your code is fine (except as noted you should return func's result inside decorator), I improved it a bit.It's easier to see and test with synthetic example:Output:


Answer URL
https://docs.python.org/3/library/asyncio-eventloop.html#asyncio.AbstractEventLoop.call_at
