Natural Text
I have a python module with lots of python scripts in, I want to read these scripts and check if a variable is set.I'm currently reading the files line by line, but as the scripts i'm reading are in python i'm guessing there is a better way?EditI have a directory which has lots of classes, some have variables which affect how the script runs.e.g.From this i want to achieve
Here's a working example for your question:Some comments, to make it work, create a folder in the same script's folder called  containing an empty  file and one (or more) python files with the classes you've mentioned in your question.The important bits of the code are the usage of import_module and inspectIt's a little example but it should be a good starting point for you. Hope it helps.


Answer URL
https://docs.python.org/3/library/importlib.html
