Natural Text
All of the examples I've seen have used  with something like .  I'd prefer to use a solution with  if possible, and I was thinking that it might be nice to write it out to a string buffer type object which processes the data and then writes out a compressed  file.How can I do this?
The documentation for psycopg says that  accepts any file-like object. Thus you could simply use the  to open a writable gzip file-like object:Alternatively, if you prefer to write text in certain encoding, and on Python 3.3+, you can use mode  and add  or similar to the .


Answer URL
https://docs.python.org/3/library/gzip.html
