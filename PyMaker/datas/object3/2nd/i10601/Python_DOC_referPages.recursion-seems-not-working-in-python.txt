Natural Text
I am writing a piece of code to recursively processing *.py files. The code block is as the following:When I run the script with a directory path as argument, it only runs the first layer of the directory, the line:seems never get called. I'm using Python 3.5.
The recursion is happening fine, but  is not a directory so it passes control to your stub  block. You can see this if you put  outside your  block. is the name of the next directory, not its absolute path.  becomes just . Use  to get thatAlthough the canonical way to do this (as mentioned in my comment on the question) is to use .
As  has the filename only without parent path, you should changeto


Answer URL
https://docs.python.org/3/library/os.html#os.walk
