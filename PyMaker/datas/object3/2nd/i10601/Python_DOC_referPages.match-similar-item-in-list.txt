Natural Text
I have 2 lists of hostnamesI would expect output to be like...I want to find entries in  that are not in . However some names in  have  or some other string appended that don't occur in . The length and number of dashes per list item vary greatly, so I'm not sure how successful using a regex would be. I'm new to programming, so please provide some explanation if possible.
You may achieve it by using  as:I am using  to check whether any element of  starts with any element of 
You could do this with a list comprehension and :Meaning, you select every element  in  if, for every element  in ,  is not contained in .
You can use  to see if a string starts with another string. So something like:
There is some problems with the solutions provided by @Jim and @bbkglb when the elements are repeated in bar. Those solutions should be converted to sets. I tested the solutions and their response times:Using filter - lambda:Using all:Using any:


Answer URL
https://docs.python.org/3/library/functions.html#all
https://docs.python.org/3/library/functions.html#any
