Natural Text
I have one list and one dictionary. I want to compare the list values with the keys of the dictionary. If I have:and:I want the output to be:Thanks!I tried  and I get an error instead of None. I then tried using nested for loops (I deleted that bit) but I decided that was to inefficient.
Use a list comprehension: Note that dict.get returns  if the key is not in the dict.
Use , which defaults to  if key does not exist:


Answer URL
https://docs.python.org/3/tutorial/datastructures.html#dictionaries
