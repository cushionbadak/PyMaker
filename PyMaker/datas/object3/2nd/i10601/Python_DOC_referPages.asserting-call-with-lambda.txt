Natural Text
I have this piece of code:In my unit tests, I want to mock it so check that right  is passed:What can I replace  with to check that there is a lambda there?
I would do this with the  and  rather than directly in , I don't think  has anything like e.g. :Note that it's not relevant that the argument is a  specifically, just that it is callable. 


Answer URL
https://docs.python.org/3/library/unittest.mock.html#unittest.mock.Mock.call_args
https://docs.python.org/3/library/unittest.mock.html#unittest.mock.Mock.call_count
