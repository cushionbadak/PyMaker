Natural Text
I have the following SQLAlchemy class:When I use the property I get the error .Why doesn't this work?  How do I implement a property for setting the password?
You defined the  for the property as the .  Either remove the  because this is a function, or define the property correctly.If you're going the property route, rename the  attribute and access it through the property for the getter.  Use a  so that the column is still queryable directly.


Answer URL
https://docs.python.org/3/library/functions.html#property
