Natural Text
I'm using PyCharm community edition 2016.2.1 on OS X El Capitan. I can install the packages  and , but I'm still getting an ImportError. How can I solve this?This is the code that's producing the error:
In your case it is helpful to create a virtual environment, since it's not really recommended to touch the default Python installation that comes with macOS (virtual environments are almost always preferred to installing the package globally). Although it would be possible to install packages using sudo, as the error message suggests.For users that are coming here because of the error that PyCharm IDE shows when importing the  hash functions, then try importing from :This will help your confused IDE make sense of the code.


Answer URL
https://docs.python.org/3/tutorial/venv.html
