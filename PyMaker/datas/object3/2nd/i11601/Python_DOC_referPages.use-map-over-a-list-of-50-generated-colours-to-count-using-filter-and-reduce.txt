Natural Text
Given:Generate and print a list of 50 random colours. You will need to  use the  module to get  numbers. Use  and  to generate the  required amount of numbers. Then use  to translate numbers to colours.  Then use  over the colours to count (using , and  or ) how often  each colour occurs. Print the result.This is what I've got so far:Which when printed, gives me the required set of 50 random colours from the given list. I'm sort of confused as to where to move from here.
This one also uses zip so you have a reference of the color being counted:The way to use reduce to count the elements was giving me some thought, the only way I found of doing it was this:


Answer URL
https://docs.python.org/3/library/collections.html#collections.Counter
