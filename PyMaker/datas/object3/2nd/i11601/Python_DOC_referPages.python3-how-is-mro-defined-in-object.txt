Natural Text
Update------------------ is a instance of , but at the same time,  is also the base class of . And  is a instance of . I'm really confused about this. it's totally unlogical. Why python do this?Original--------------------------------I know  has . but I can't figure out why  can be called from . How python implement this?
Descriptors are involved.When resolving , the interpreter first looks for  in .Since  is not in , the interpreter looks in .This object is a descriptor since it has a  method.  So the interpreter invokes the  method to obtain the actual value of .Based on the documentation you might think that since  is a class it would call  but in this case  is being considered as an instance of class  so the interpreter calls  to get the result.To confirm that the result is what we're looking for:


Answer URL
https://docs.python.org/3/reference/datamodel.html#invoking-descriptors
