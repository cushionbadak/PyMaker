Natural Text
Trying to copy text next to variables like type, number, name from text file to csv with following form:I Have lots of text file in this form:I am unable to copy text next to search string from text file and write it into csv file. 
You can convert the contents of one file to a dictionary like this:Then write the dictionary to a CSV file using :This will copy all of the data from the input files to a single CSV file. If you want to exclude some fields adjust the  and remove the unwanted fields from the dictionary prior to calling . You can also rename the fields by changing the field name in both  and the dictionary.


Answer URL
https://docs.python.org/3/library/csv.html#csv.DictWriter
