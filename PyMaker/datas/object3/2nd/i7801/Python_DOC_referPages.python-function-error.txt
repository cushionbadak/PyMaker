Natural Text
I wrote a simple function program. The program works by itself but when converted to a function, it gives the character count of 1. I am using Python version 3.5.
you are overwriting values of ,  at the beginning, tryalso we can improve your function by returning value from it and printing outside and using augmented arithmetic assignment finally we can use  and  methods:
Python has a  object that does this, and  returns  for a value not seen already, so is simply:


Answer URL
https://docs.python.org/3/reference/simple_stmts.html#the-return-statement
https://docs.python.org/3/library/stdtypes.html#str.join
https://docs.python.org/3/library/stdtypes.html#str.count
