Natural Text
I have  in a folder called . in the   i have below code. however when i try to execute from parent folder of  with following command  i get following error . is this not possible? or will i have to run ? 
The  module is executed when the package is imported. The purpose of  files per the documentation is as follows:The  files are required to make Python treat the directories as containing packages; this is done to prevent directories with a common name, such as string, from unintentionally hiding valid modules that occur later on the module search path. In the simplest case,  can just be an empty file, but it can also execute initialization code for the package or set the  variable, described later.In order for a Python package to be directly executed, it needs to have an entry point, designated by a module  within the package named . Thus the error : You have attempted to directly execute the package, but Python cannot locate an entry point to begin executing top-level code.Consider the following package structure:Where  contains the following:Where  contains the following:When we execute the  package with the command , we get the following output:However, if we enter the Python shell and , the output is as follows:Thus in order to get the behavior you want when attempting to directly execute , simply create a new  file within  and transfer the code from  to the new .


Answer URL
https://docs.python.org/3/tutorial/modules.html
