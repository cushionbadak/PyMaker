Natural Text
I have a function that is used by multiple threads. Because of its nature, this function should only ever called once at a time. Multiple threads calling the function at the same time could be bad.If the function is in use by a thread, other threads should have to wait for it to be free.My background isn't coding so I'm not sure, but I believe this is called "locking" in the jargon? I tried Googling it up but did not find a simple example for Python3.A simplified case:



Answer URL
https://docs.python.org/3/library/asyncio-sync.html
