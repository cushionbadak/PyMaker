Natural Text
I have a dictionary.I want to find the combinations of two elements of which each element must be from different keys of dict. for example:  is one such combination whereas  is not.I have tried this but it's giving  and  as two different combination but I want only one of them.
Instead of a nested loop over the keys, pass all values to ; it'll pick unique combinations of a given length:For your given dictionary, the following combinations are created:The exact order of the pairing differs based on dictionary ordering.Full demo:


Answer URL
https://docs.python.org/3/library/itertools.html#itertools.combinations
