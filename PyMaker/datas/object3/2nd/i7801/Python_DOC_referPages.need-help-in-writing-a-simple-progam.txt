Natural Text
I am not a programmer. This is my first program. I am trying to:copy a web page to a variable search the variable for a stringI got #1 to work but when i search for the string "June 5, 2017", I get the error listed below.Error MessageHere's the code:Any advice would be appreciated.
Because   is a str object, and  is a bytes-like object.You need to  convert  to str type.Or make "June 5, 2017" just be a byte-like object when defined as Wasi Ahmad mentioned.
Just do this.In your example, all the data that you got from  is bytes objects, not string. You can see what is the type of an object in python as follows.So, adding a prefix () prefix before the target string solves your problem.Other alternatives:or,
 is a  object, not a string, so in order to use the  operator with it, you'd have to supply another  (or bytes-like) object. Luckily, this is quite easy in Python - just use the  prefix when you denote the literal:


Answer URL
https://docs.python.org/3/library/stdtypes.html#bytes
