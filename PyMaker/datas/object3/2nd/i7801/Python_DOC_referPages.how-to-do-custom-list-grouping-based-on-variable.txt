Natural Text
Say I have a list that gets generated such as:Then I divide it up like this:with this list comprehension:How can I make this code so I can group it into a grouping based on `grp_count'for example if :I know I have to insert multiple  for each addition of grouping size somehow.
You can use  to do the trick like this example:Output:
This looks a lot like the itertools grouper recipe from https://docs.python.org/3/library/itertools.html thengives
As I said in a comment, there's no way to create a list of integers in Python that would display like  because Python doesn't show the value of integers with a leading zero like that. The closest you could get is .However you can create a list of strings with those digits in them like this:


Answer URL
https://docs.python.org/3/library/itertools.html
https://docs.python.org/3/library/itertools.html?highlight=grouper#itertools-recipes
https://docs.python.org/3/tutorial/controlflow.html#unpacking-argument-lists
