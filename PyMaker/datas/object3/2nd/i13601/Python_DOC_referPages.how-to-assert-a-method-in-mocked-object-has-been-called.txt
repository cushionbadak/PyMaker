Natural Text
I a learning python mock, and having problem to check if a method on a mocked object has been called.The code I want to test is this:My test code is:And when I run it, I get this exception:
 does the assertion directly, and returns . You do not need to use  here at all.In other words, the following is enough:If this assertion fails (i.e.  was not called or called with different arguments) then that  raises an  exception, which  catches for you and records as a test failure:


Answer URL
https://docs.python.org/3/library/exceptions.html#AssertionError
