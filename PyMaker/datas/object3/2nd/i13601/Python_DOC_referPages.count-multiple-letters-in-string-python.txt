Natural Text
I'm trying to count the letter's 'l' and 'o' in the string below. It seems to work if i count one letter, but as soon as i count the next letter 'o' the string does not add to the total count. What am I missing?Output: 5
You probably mean .The code you've pasted is equal to : the  operator checks if its first operand (in this case ) is false. If it is false, it returns its first operand (), but it isn't, so it returns the second operand ().Official documentation
Alternatively, and since you are counting appearances of multiple letters in a given string, use :Note that  that you are currently using would evaluate as :The expression  first evaluates : if  is false, its value is  returned; otherwise,  is evaluated and the resulting value is  returned.In other words:
Use regular expression:or :


Answer URL
https://docs.python.org/3/reference/expressions.html#and
