Natural Text
In Python 3, suppose I have Using  givesMy question: how can I get  to return a  sequence using  instead of ?  And how can I  them back to a Python 3  type?I tried using the  builtin, which givesBut this doesn't seem quite right, as I can't decode it back to obtain .Python documentation tells me that  escapes the character with the hex value  while  escapes the character with the 16-bit hex value The documentation says that  is only used in string literals, but I'm not sure what that means.  Is this a hint that my question has a flawed premise?
You can use :Note that  will always return a byte string (bytes) and the  encoding is intended to:Produce a string that is suitable as Unicode literal in Python source code


Answer URL
https://docs.python.org/3/reference/lexical_analysis.html
