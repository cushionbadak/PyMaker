Natural Text
When i try to run this on Shell using  I only get output (3, 4, 5).What might be the problem all the triplets are not generating.
As many others pointed out, when the return statement executes, your function will finish, and will not continue looping. However, you have options for returning multiple values from a single function. For example aggregating your values in a list and return that, or to use the  keyword instead of . The  keyword will make your function a generator function, which returns an iterable generator object with all your expected elements.I advise you to split your code into separate functions, so you have the original formula as a function of i, and a generator function which will return the elements for . Note that you can collect the elements of a generator to a list by supplying it to the  constructor.
The  sentence. Change it for a . If you want to test it, you could:
Your function produces only one pair, because  interrupts function execution. You could use  keyword, as suggested by @jgomo3, or  function:


Answer URL
https://docs.python.org/3/library/functions.html#map
