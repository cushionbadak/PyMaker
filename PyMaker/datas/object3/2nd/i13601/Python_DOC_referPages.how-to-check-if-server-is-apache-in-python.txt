Natural Text
I need some idea to test the server from a link. I do not know where to startWould be:
I prefer using requests since it's simple and well documented. And it doesn't return an error like urllibAlso see : http://www.python-requests.org/en/latest/ for more information
In python 3:would be the simplest way to get the server header in some cases.Some sites (like stackoverflow), however, will return 403 error code.


Answer URL
https://docs.python.org/3/library/http.client.html
