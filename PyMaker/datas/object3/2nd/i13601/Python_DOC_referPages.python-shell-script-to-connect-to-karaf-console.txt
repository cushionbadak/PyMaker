Natural Text
I am writing a script in python which can connect to karaf console like sudo ./client and after logging I want to see what and all bundles are active by using commands like list| grep -i active.I want 1 single script for that.So these two commands I want to use in my script.
So you need:run a command and get its outputparse the output and look for thingsCheck out the Python docs here for how to do this.It should be something like this (I'm not at all familiar with Karaf...):


Answer URL
https://docs.python.org/3/library/subprocess.html#subprocess-replacements
