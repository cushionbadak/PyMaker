Natural Text
How do you enumerate a JSON return? The goal is to get a total count variable to add to my print statment...  I have and have tried:but get:
This is NOT how  works:It works like this:Notice how enumerate returns a tuple, ( and ) not just the count?You may have other issues (in your JSON) but that part might be throwing you off.The error you are seeing is because  is actually the tuple  and then you're trying to do , so it's saying you can't use a string as an index to a tuple.
Hard to say for sure without seeing the structure of the JSON, but going by the key names, you probably want


Answer URL
https://docs.python.org/3/library/functions.html#enumerate
