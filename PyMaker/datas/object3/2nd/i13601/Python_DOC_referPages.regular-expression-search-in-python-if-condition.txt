Natural Text
I am trying to search whole word pid in the link but somewhat this is also searching for id in this code
If you mean that you want it to match a string like  but not , then you need to exclude word characters on either side. Something like:If 'pid' might be at the start or end of the string, you'll need to add extra conditions: check for either the start/end of line or a non-word character:See the docs for more information on the special characters.You could use it like this:


Answer URL
https://docs.python.org/3/library/urllib.parse.html
