Natural Text
I've a class in python that contains a static method. I want to  it in order to see if it was called. When trying to do it I get an error:My setup can be simplified to:Now the test code that fails with error:
You can always use  as a decorator, my preferred way of patching things:EDIT: Your error seems to hint that you have a problem elsewhere in your code. Possibly some signal or trigger that requires this method that is failing?
I was getting that same error message when trying to patch a method using the  decorator. Here is the full error I got.What I ended up doing to fix this was changing the patch decorator I used from to I really wish I could explain further why that was the issue but I'm still pretty new to Python myself. The patch documentation was especially helpful in figuring out what was available to work with. I should not that  uses the  decorator which may be changing things. Hope it helps though.


Answer URL
https://docs.python.org/3/library/unittest.mock-examples.html#patch-decorators
