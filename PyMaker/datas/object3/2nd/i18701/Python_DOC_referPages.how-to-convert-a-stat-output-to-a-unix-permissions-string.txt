Natural Text
If you run  on a file and then take its  parameter, how do you get from there to a string like this:  as known from the Unix world?
Since Python 3.3 you could use :
Something like this:
The following function will achieve this, given some usual circumstances (i.e. I haven't tested it under Windows or with SELinux).This produces a basic string as asked. However, this could also be improved to display further data, e.g. whether it is a directory () or a symlink (). Feel free to improve it.


Answer URL
