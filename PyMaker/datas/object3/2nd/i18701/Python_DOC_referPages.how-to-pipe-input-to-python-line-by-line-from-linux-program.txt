Natural Text
I want to pipe the output of  to python line by line. The script I am using is this (first.py) -  Unfortunately, the "line" is split into words separated by whitespace. So, for example, if I do the output I get is How to write the script such that the output is ?
I do not quite understand why you want to use commandline arguments instead of simply reading from standard input. Python has a simple idiom for iterating over lines at stdin:My usage example:Your usage example:
What you want is , which makes it possible to directly read the output of a command like you would read a file:Note that, if you want more complex parsing, you'll have to dig into the documentation of .


Answer URL
https://docs.python.org/3/library/os.html#os.popen
https://docs.python.org/3/library/subprocess.html#subprocess.Popen
