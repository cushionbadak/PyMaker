Natural Text
I'am learning about the modules of  and . I have a doubt in few lines of the next code. I don't know the use of  and what's inside of brackets in 
 returns a list of items from a tag. For example: is used to access the first element of the list  returns. In the example above, it will return  returns the second (indexing starts at 0, remember) matched value from a regular expression. Looking at your regular expression, it returns the second digit from something that looks like .


Answer URL
