Natural Text
i am passing args likeI want to know if there is a way to treat these args as objects not strings for the following code:since one two three would be strings. how can they be parsed as objects?UPDATE:gives
You can use importlib for Python 2 or importlib for Python 3.
I think what you want is .
You can use the built-in  or the  module to import object programmatically:Whenever I've wanted to programmatically import modules, this is how I do it, but  returns a 3-tuple:


Answer URL
