Natural Text
I have array like this:How can I make the script to go through rows and make one row with 'page1' and sum two values '65' and '40' together.
Here's a solution using :
Using pandas (you say you're open to using this in the comments), this becomes pretty straightforward:This produces the following Pandas Series:Which you can easily index using the page name:
At its core, this is a grouping problem. Grouping is easy with a :If you want the result to be in the same format as you input (a list of lists), convert the dict to a list with a :
You need to sort it, afterwards you could use :This would yield


Answer URL
https://docs.python.org/3/library/collections.html#collections.defaultdict
https://docs.python.org/3/tutorial/datastructures.html#list-comprehensions
