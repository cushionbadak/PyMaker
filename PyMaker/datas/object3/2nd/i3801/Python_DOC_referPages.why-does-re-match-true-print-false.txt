Natural Text
it is printing  but the string given is fullfilling all the conditions of the regular expression.
Neither a match object nor , the two types of objects that  can return, is ever equal to True:Don't use . If you need to see if the match succeeded, test for :You should almost never use  or . In a boolean context like an  statement, just drop ; it is the job of the statement to test for the boolean truth. For , put  in front instead. Moreover, as seen above, most objects are not equal to  or ; to test for the truth value, just pass in the object itself, or convert the truth value too a boolean using the  function.


Answer URL
https://docs.python.org/3/library/stdtypes.html#truth-value-testing
https://docs.python.org/3/library/stdtypes.html#truth-value-testing
