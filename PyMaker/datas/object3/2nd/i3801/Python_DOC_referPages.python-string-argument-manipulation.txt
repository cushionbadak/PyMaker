Natural Text
I have a function that has a  value obtained through user input via  that dictates which parameter within  to use. As someone relatively new to python I was wondering what the proper way to go about doing this was, so that I would be given access to the desired data properly.when completed I'd like the argument to function as :  or 
You can achieve this by using :Please note that  is case sensitive, this means that while  will yield the correct value,  will raise an If you want to handle invalid values, you can use :If instead of raising a  you want to use a default value, you can use the  keyword argument for :


Answer URL
https://docs.python.org/3/library/functions.html#getattr
