Natural Text
Given the following code  Expected output is, since assume the lock part only one process can be in:but actual output is:What's the problem and how to resolve this?
You cannot lock processes like this, you have tu use :Managers provide a way to create data which can be shared between  different processes, including sharing over a network between  processes running on different machines. A manager object controls a  server process which manages shared objects. Other processes can  access the shared objects by using proxies.Instead of


Answer URL
https://docs.python.org/3/library/multiprocessing.html#managers
