Natural Text
I need to convert XML to json without root in python. Here is an example of XMLI need to convert into JSON in the following formatI am trying to use the parker convention from xmljson library but all the examples I'm finding are using string as input. I can't seem to figure out how to pass the actual .xml file instead of a stringFor example:
You can use standart xml library to parse it to dict and then dump dict to json if it needed:OUTPUT:


Answer URL
https://docs.python.org/3/library/xml.etree.elementtree.html
