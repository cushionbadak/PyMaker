Natural Text
I want to select full  groups that contain  and .Output should be:I've tried:
You can use  +  to create a flag for the groups that satisfy that condition. And then you can mask the original  by that flag. If you don't want to modify the original  you can create a separate  named , otherwise you can also just assign it to a column in your original Then you can filter the 
create an array of dates as the intersection of dates that contain  & dates that contain :then use the dates array to filter the dataframeTimings:Comparing the solutions provided by jpp, ALollz and mine above:The numpy based solution is several times more efficient than the others.However, the solution proposed by ALollz can be sped up 2X by omitting some extra unneeded operations and going down to numpy arrays for comparison.
Create a series mapping each date to a  of names. Then use  via syntactic sugar :


Answer URL
https://docs.python.org/3/library/stdtypes.html#frozenset.issuperset
