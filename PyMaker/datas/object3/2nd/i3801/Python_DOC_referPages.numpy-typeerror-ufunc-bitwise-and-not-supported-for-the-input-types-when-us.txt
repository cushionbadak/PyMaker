Natural Text
In numpy, if I have an array of floats, dynamically create a boolean mask of where this array equals a particular value and do a bitwise AND with a boolean array, I get an error:If I save the result of the comparison to a variable and carry out the bitwise AND however, it works:The objects created seem the same in each case though:Why the difference?
 has higher precedence than , so the expressionis the same asYou get the error because bitwise  is not defined for a floating point scalar and a boolean array.Add parentheses to get what you expected:


Answer URL
https://docs.python.org/3/reference/expressions.html#operator-precedence
