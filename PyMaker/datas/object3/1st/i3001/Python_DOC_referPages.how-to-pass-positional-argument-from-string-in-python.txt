Natural Text
This question already has an answer here:Most pythonic way of assigning keyword arguments using a variable as keyword?                    2 answers                I spent some time to figure that out, so I post this Q&A.Let's say you have function def my_func(group) which takes an argument group which will be used as positional argument in next function: def set_permission(group_user=None, group_admin=None). So:but what if group can be 20 combinations?And more 'real' case from Django:
As found in official python docs:and 'real life'


Answer URL
https://docs.python.org/3/glossary.html#term-argument
