Natural Text
I have a project laid out like this:In create_file.py, I want to create a file, train.txt, and put it in proj/main/written_files/I was wondering if I could use the relative package structure. I was thinking of putting something like this in create_file.py:Is there some way to do something like this?
Modules loaded from a file will generally have a __file__ attribute. You can use this as a starting point for getting around your package. You could use the utilties in os.path or pathlib to navigate around the path. Either of the following should work:ORIn both cases, use a context manager to make sure your file gets closed no matter what:Neither of these solutions is super elegant because they involve walking up the directory tree. Instead, you could start with the project root, and walk down:ORIn both cases, proj.__file__ should point to the root level __init__.py.


Answer URL
https://docs.python.org/3/reference/import.html#__file__
https://docs.python.org/3/library/os.path.html
https://docs.python.org/3/library/pathlib.html
