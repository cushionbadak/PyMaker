Natural Text
When running this in Python 3.7 I get no errors but nothing happens, I'm using it for work to encrypt a sensitive user directory when the user logs out, all other parts of my program work. Could someone explain what's wrong with it?
From the doc for glob:If recursive is true, the pattern “**” will match any files and zero  or more directories and subdirectories. If the pattern is followed by  an os.sep, only directories and subdirectories match.So, if you want to use glob here, you need to include this pattern:


Answer URL
https://docs.python.org/3/library/glob.html#glob.glob
