Natural Text
I want to read a file using pythonthe result must be like (a list of tuples):the file contains:my code :the result How do I convert the items into each type(int, float). I got a list of lists, not a list of tuples
You can convert the elements into the proper type immediately after the line they're in has been read:Output:This could be done even more succinctly using a list comprehension:Also note you don't need to use readlines() to read all the lines of a file into memory at once because files are iterable, line-by-line. This is described in the Tutorial in Python's online documentation.
Adding to wpercy's answer, I would imagine the simplest way to achieve this would be the following.
You can use something like:


Answer URL
https://docs.python.org/3/tutorial/datastructures.html#list-comprehensions
https://docs.python.org/3/tutorial/inputoutput.html#methods-of-file-objects
