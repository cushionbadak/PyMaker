Natural Text
I want to find the prime factors for 13195.  I want x to only store integers. I've tried is.integer and but keep getting syntax errors.  
You could use the modulo operator to check if a number is divided evenly, and only set x equal to it then. E.g.:which gives:
I've tried is.integer and but keep getting syntax errors.The method for checking if a float is an integer is called is_integer() not is.integer(), so alternatively, you can do this:Or by adding the brackets:


Answer URL
https://docs.python.org/3/library/functions.html#divmod
