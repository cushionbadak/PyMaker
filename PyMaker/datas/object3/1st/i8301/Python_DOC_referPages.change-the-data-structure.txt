Natural Text
This question already has an answer here:Combine two lists into one multidimensional list                    3 answers                I have 2 lists with the same size. I want to make a new list which the combination of both of them (list of lists) in a way that elements with the same indexes would be in a list and this list would have the same index.input example:do you know how to do that?
Using the built-in zip:
This gives you the list.
this will give you list of tuplesif you really need your elements to be lists then we can write
Use zipOr you want list instead of tuples :
You can zip them:list(zip(a, b))
Use zip command to combine both the list.


Answer URL
https://docs.python.org/3/library/functions.html#zip
