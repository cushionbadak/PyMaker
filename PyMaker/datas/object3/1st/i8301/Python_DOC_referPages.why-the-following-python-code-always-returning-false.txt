Natural Text
Input file Test.ini:Code:The above code is always returning 300. Looks like isdigit() is not working or is there anything wrong in my code ?
Your lines end in a line separator, which you did not remove. Strip the line to remove whitespace from the start and end:A line separator (\n) is not a digit:Rather than build your own parser, consider using the configparser module from the standard library. It fully supports the INI format (including using ; as a comment).


Answer URL
https://docs.python.org/3/library/configparser.html
