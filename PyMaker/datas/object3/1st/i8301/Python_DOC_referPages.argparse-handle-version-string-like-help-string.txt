Natural Text
I am using Python 3.6 with argparse 1.1. Besides -h/--help I want to have an option -v/--version that prints a string with version information and quits the program (like with the help string). However when I also have positional arguments, executing results in Work around solutionI have a work around solution, which I don't like as I need quite some lines of code to do something that argparse could do much better.
argparse already has a version action for exactly this:'version' - This expects a version= keyword argument in the add_argument() call, and prints version information and exits when invoked:


Answer URL
https://docs.python.org/3/library/argparse.html#action
