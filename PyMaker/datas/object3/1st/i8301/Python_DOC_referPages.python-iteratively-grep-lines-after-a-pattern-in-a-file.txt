Natural Text
I have a file that looks like:I would like to extract the three lines after "Start" and two lines after "print out". Basically, it should be:This is what I used:However, I got the error:
Try using next(f) instead of f.readline().Also consider using regular expressions for this task:


Answer URL
https://docs.python.org/3/library/re.html#module-re
