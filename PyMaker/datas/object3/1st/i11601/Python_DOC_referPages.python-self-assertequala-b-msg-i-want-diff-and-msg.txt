Natural Text
If I call it like this, I see a nice diff:If I call it like this, I see the msg only:Is there an easy way to show the diff AND the msg?Implementing assertEqual() myself would work, but I ask myself if this is really the best way.Plattform: Python2.7 and pytest 2.6.2.
If you set longMessage attribute True, you will see both message.Example:output:
Since you're using pytest, you could also use pytest's plain assertions instead of the unittest compatibility:Output:


Answer URL
https://docs.python.org/3/library/unittest.html#unittest.TestCase.longMessage
