Natural Text
This question already has an answer here:How to transform a tuple to a string of values without comma and parentheses                    5 answers                I have this functionthe output of this funciotn is in format tuple, like thisWhat can I do if I need the output not in tuple, so in the following formatCan someone help? Thanks
Use str.join():Or in Python 3 you can simply use print() like this:which also works in Python 2 if you import print_function like this at the top of your file:
You can unpack it and specify a custom separator:
Here you go:


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.join
