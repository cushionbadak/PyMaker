Natural Text
I m using Argparser in Python to parse the command line arguments. Here is my code:If I specify python argparser.py input.txt hello_world output.txt, it works fine. I want to support something like, python argparser.py -i input.txt -m hello_world -o output.txt and python argparser.py --input input.txt --method hello_world --output output.txtI tried this, parser.add_argument('-i', 'input', type=str) but this does not seem to work. It keeps throwing error. How can I fix this.
Try this code(it is fully working on my side):Usage example: python3 parse.py -i someResult:Namespace(i='some', m=None, o=None)
Something like thisYou can check out the different options in the documentationEDIT: I never tried with -i=input but the option without using = works for me



Answer URL
https://docs.python.org/3/library/argparse.html
