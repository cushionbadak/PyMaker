Natural Text
Sometimes it would be handy to test a script that reads data from a file using inline data (so that both the data and the code would be in the same file). In bash this can be done with a heredoc:In some real code there would of course happen something more apart from writing out the lines. Suppose I would do something similar in python:What would be the best way to provide the content of input.txt to read_file() inline?
You can use StringIO:The above code works with Python3. In Python2 use:


Answer URL
https://docs.python.org/3/library/io.html
