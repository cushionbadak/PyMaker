Natural Text
I have an argparser that has a verbose flag and I'm trying to minimize the amount I have to write to get the verbose output.This works as expected:outputs:butthrows an error:I thought the python had the statement if condition else condition syntax? Have I made some mistake?
The Python if ternary operator syntax requires an else, like so:
For verbose output like this you're best off using the Logging module:You can update the format of the string it uses, links are in the documentation.
Define an alias for print that either prints its argument or does nothing, depending on the value of verbose:


Answer URL
https://docs.python.org/3/faq/programming.html#is-there-an-equivalent-of-c-s-ternary-operator
https://docs.python.org/3/library/logging.html
