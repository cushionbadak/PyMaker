Natural Text
To implement a subscriptable object is easy, just implement __getitem__ in this object's class definition.But now I want to implement a subscriptable class. For example, I want to implement this code:I know getattr can do the same thing, but I feel subscript accessing is more elegant.
Seems to work by changing the metaclass. For Python 2:On Python 3, you should use Enum directly:
Add something like this to your class:


Answer URL
https://docs.python.org/3/library/enum.html
