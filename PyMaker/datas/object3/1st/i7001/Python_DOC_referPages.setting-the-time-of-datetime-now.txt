Natural Text
I am trying to get the current datetime and set the time to 00:00:00.To do this, I call:The output is:That is not what I expect. However, if I do:Everything is as I expect and I get the result:Why is this? What is going on?? Why does replace not work?
replace returns a new datetime instance, so you should do:You should also replace microsecond=0 in order to make it exactly 00:00:00.
replace method always return a new  value after replacing  so you need to store that value.Remember it returns a new value after replacinglike in your case.It returns a new datetime instance, so you should do:


Answer URL
https://docs.python.org/3/library/datetime.html#datetime.datetime.replace
https://docs.python.org/3/library/datetime.html#datetime.datetime.replace
