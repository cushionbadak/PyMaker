Natural Text
The ampersand is being treated as code but I want to treat it as text; I get a bad request exception:I tried to escape it (using a \) but this did not work either.How can I treat this ampersand as text?
The library is rather naive in that it just appends the symbols to a URL without proper encoding, see the source code:You can work around this by manually quoting up front, using the urllib.parse.quote() function:Demo:


Answer URL
https://docs.python.org/3/library/urllib.parse.html#urllib.parse.quote
