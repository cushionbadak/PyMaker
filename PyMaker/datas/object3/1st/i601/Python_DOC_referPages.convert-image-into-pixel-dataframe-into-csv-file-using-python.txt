Natural Text
i have folder with pictures ("C:\Users\Admin\Downloads\mypicture")here example of it8and2i want convert it to pixel dataframe like thisEvery image is represented as a single row . The greyscale of each image falls in the range [0, 255].i do sobut i get this errorHow can i get desired data frame in csv file?
You could Use PIL library to convert the image to a pixel dataframe, by getting the pixel's list from an image like thisthis will return a list of pixels with the (r,g,b) values, so if you just want to get the grayscale of each pixel, just iterate the list in the second element of each value, like thisOutput:


Answer URL
https://docs.python.org/3/library/glob.html
