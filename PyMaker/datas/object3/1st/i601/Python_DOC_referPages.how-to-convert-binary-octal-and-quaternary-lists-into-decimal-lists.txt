Natural Text
I need to convert three lists into decimal lists to check maximum and minimum value in them. How can I do it? example Lists:
If I understand your question correctly, you could do something like this (try it online):It works using a function list_to_decimal that takes a list lst and a base base, which then uses a list comprehension to interpret every element of lst as a number of base.Does that answer your question?
You can do it like this.The output looks like:


Answer URL
https://docs.python.org/3/tutorial/datastructures.html#list-comprehensions
