Natural Text
The df looks likeThe output should looks likeI have used df.type.str.cat(sep=',') to add , in between but how to add quotation mark for each element?
Use join or Series.str.cat:Or:
One other alternative may be to use format with apply and sum:


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.join
