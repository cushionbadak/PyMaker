Natural Text
Here is the relevant part of my program:And here is the error message:So if imaplib has no attribute called abort, then how is self.abort() being raised in line 929 of _check_bye ?How do I catch it?
The "imaplib.abort" line in misleading. Actually, you need to catch imaplib.IMAP4.abort.


Answer URL
https://docs.python.org/3/library/imaplib.html?highlight=imaplib#imaplib.IMAP4.abort
