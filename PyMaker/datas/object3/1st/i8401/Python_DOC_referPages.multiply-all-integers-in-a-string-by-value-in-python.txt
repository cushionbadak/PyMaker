Natural Text
I need to multiply every integer in a string by another number, such that With a string like that, it isn't so bad to use regex reassemble the desired output but I would like to be able to handle this situation:
You can use re.sub to replace all the integers.
You can use itertools.groupby:


Answer URL
https://docs.python.org/3/library/re.html#re.sub
