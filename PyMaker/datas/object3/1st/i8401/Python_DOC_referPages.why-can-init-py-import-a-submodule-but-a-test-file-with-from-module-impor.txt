Natural Text
I have the following folder structure.__init__.py has the following:This works fine. sandbox.py looks like this:On the command line, while in the project directory:I think this might have to do with relative imports, and tried changing from klass import Klass to from .klass import Klass, but that didn't work either.My goal is, from files outside the module, to be able to use from module import * or from module import Klass. What do I need to do?In case it matters, this is with Python 3.5.2 under macOS within a virtual environment.
Use absolute path and __main__.py file for this:In __init__.py:Now you can execute it as follows:


Answer URL
https://docs.python.org/3/library/__main__.html
