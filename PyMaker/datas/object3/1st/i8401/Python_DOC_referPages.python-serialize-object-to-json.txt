Natural Text
So i have the following objects:I want to make a json which reprezents the attributes of object p but i receive the following error:Can someone please help me?
You are calling json.dumps with the default encoder again, which surely don't know how to deal with P objects. If you do want to call json.dumps, use:However! If the print the result, we see the result is double-encoded as a string:If you check the ComplexEncoder example, and also the documentation of default(), we will note that it should return a "serializable object", not the serialized string. So you should really write:Output:


Answer URL
https://docs.python.org/3/library/json.html#json.JSONEncoder.default
