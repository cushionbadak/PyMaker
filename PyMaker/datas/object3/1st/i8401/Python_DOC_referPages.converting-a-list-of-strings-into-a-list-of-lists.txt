Natural Text
I have a list of strings, that I'm trying to convert into a list of lists. My list of strings looks like this. My desired output would look like this:My output currently looks like this:Here is the text file's contents:Here's the code I have written thus far:I know it's not very elegant, I haven't been coding for very long
I think this is what you're trying to doresult:credit to @AdamSmith for pointing out the following change to make things more readable and simple:
I think converting your list into a string makes things harder, not easier.I'd probably do something like:Result (newlines added by me for added readability):
If you want to get a list of all your words excluding any spaces and special characters, you can use the regular expression \w+ (at least one word-character) in combination with findall():
A quick answer using regex:And the result is:


Answer URL
https://docs.python.org/3/library/ast.html#ast.literal_eval
