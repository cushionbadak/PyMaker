Natural Text
I am quite new at python.So I am currently working on a highscore list for a game that I made using tkinter and python. So far I have the code:The problem is, it puts this into the file:€]q (X   lukeqK†qX   LukeqK †qX   DalipqK †qX   AndrewqK †qe.(and yes this is exactly what it looks like)I don't know what is wrong with it can anyone help, please?
pickle produces a binary representation of your data - so it's not supposed to be human readable. When you load your pickled file, you get your data back, so everything works fine. If you want a human readable file, a common solution is to use json. See http://docs.python.org/3/library/pickle.html#comparison-with-json for a comparison. In particular:JSON, by default, can only represent a subset of the Python built-in  types, and no custom classes; pickle can represent an extremely large  number of Python types (many of them automatically, by clever usage of  Python’s introspection facilities; complex cases can be tackled by  implementing specific object APIs).You just have to use json instead of pickle in your code:The content of highscore.txt will look like:


Answer URL
https://docs.python.org/3/library/pickle.html#comparison-with-json
