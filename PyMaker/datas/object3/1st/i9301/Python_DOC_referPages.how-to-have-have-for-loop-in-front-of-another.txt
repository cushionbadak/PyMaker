Natural Text
Lets say you're cycling through a string like "hello" and need a nested for loop.So you say something like:But you want the j loop to start one position in front of the i loop.So the i loop will start at "h" and the j loop will start at "e"I thought it would be something like:
Just slice the string where you want to start off with, now j starts at 'e' and i starts at h
Use enumerate with slices.
Iterate over a range:


Answer URL
https://docs.python.org/3/library/functions.html#enumerate
