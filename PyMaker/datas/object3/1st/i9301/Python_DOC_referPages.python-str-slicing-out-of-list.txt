Natural Text
I would write a little function to check if a string in a list, and when yes, the string should remove out of the List.this is my codebut it make nothing with the List.if i make it so that it make a new List with all int or float, he make same nothing.
Python built-in function, isinstance(), can be used here.The following approach is similar to yours.This can be much shorter, though.
Type checking can be done with isinstance. This could actually be done pretty elegantly in a list comprehension:


Answer URL
https://docs.python.org/3/library/functions.html#isinstance
