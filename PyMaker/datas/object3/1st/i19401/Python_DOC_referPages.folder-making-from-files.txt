Natural Text
This is how my files look like..I've like thousands of files like this. Can we make folders for each set of files like a-bCan I grep it? how can I do it?So, what's the error there?
Make a list of all files with glob.glob('*.png').You can then parse every filename using regex (import re).Make dirs using os.mkdir(path).Move files using os.rename(src, dst).
This code will work for what you want to do:You have to specify the name for each folder you want to create, and to move files into, (instead of "something...") eg:
Something to get you stared, adapt to your own needs:Lets create some files:Some pythonLets run it


Answer URL
