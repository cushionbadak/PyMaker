Natural Text
In order to test if a directory path if a python module path (i.e. contains a file named __init__.py) one can do something likeI wonder if there is a special function call to do that?
You can use the  imp.find_module(name[, path]) to test if a particular module is present in a certain pathHow to useForm the documentationIf search is successful, the return value is a 3-element tuple (file,  pathname, description): ..... If the module does not live in a file,  the returned file is None, pathname is the empty string, and the  description tuple contains empty strings for its suffix and mode


Answer URL
