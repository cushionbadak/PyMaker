Natural Text
I have a list that looks like belowThis is actually a custom sorted list, i want to change the order of this by fixing the first element in the list i.e if i give Hetmyer as the input the new list should like this if i give Adan as the input the new list should look like thisbasically whatever input i give that should become the first member of the list and the rest of the order should be followed as it is, i.e it should start with the input and end with the member of the list before the input member, do we have any built in function for this
Find the position:Build the new list:Result:

You could proceed using zip to find the element using index then proceed by slicing:Result:


Answer URL
https://docs.python.org/3/tutorial/datastructures.html
