Natural Text
I have a list of tuples with the pattern "id", "text", "language" like this:
Basic solution with yeild and zip:This should give you all 'en' words,And other one 'it' words,
You can group the items using a defaultdict:This can then be converted to a list of tuples like this:However, you may find the defaultdict useful in its own right, since it allows you to easily look up things by id.


Answer URL
https://docs.python.org/3/library/itertools.html#itertools.groupby
