Natural Text
I have a directory which contains multiple files (some shell ending with .sh, some text files and some python files ending with .py extension).I want to add execute permission to all shell files (ending with .sh) using os.chmod command. Basically I want to do this:I tried checking for permissions first by doing this:But it won't work.How can I do this in pythonic way?
Use the glob module to get a list of files, and then loop over them:And you can use os.chmod() as per this question to actually add the executable mode bit:


Answer URL
https://docs.python.org/3/library/glob.html
