Natural Text
My code so far:I want output of: I want that output by only using the split(' ', int) method. How can I do it?
You could get that output by using rsplit and performing only one split, i.e:Which returns a list:That you can unpack into firstname and lastname:For input that might be short (i.e user only enters first name) it is better to use rpartition if you also want to unpack; the unpacking simply must handle the 3-element tuple returned:


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.rsplit
