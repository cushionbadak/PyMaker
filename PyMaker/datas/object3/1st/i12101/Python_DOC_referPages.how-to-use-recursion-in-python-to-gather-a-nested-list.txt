Natural Text
This question already has an answer here:Flatten an irregular list of lists                    42 answers                How to make a flat list out of list of lists?                    38 answers                Here is the codeWhen I tried print(list_all([[2,3],[4,5]])), it prints out the exactly same input, meaning the code does nothing at all. I think the problem is the [] bracket but I can't think of a way to eliminate. Could someone help?
This should help:Output:
Rather than constructing a bunch of intermediate lists, consider using a generator function. Particularly in Python 3.3+, the code is very simple:If you need the ultimate result to be a list, either you let callers wrap in list() themselves, or you define a wrapper to call the generator and convert it to a list for them:It dramatically reduces the allocator churn involved in creating and disposing of all the intermediate lists compared to non-generator based solutions.Example:
Something like this, sorry not tested but is trivial ....


Answer URL
https://docs.python.org/3/library/itertools.html#itertools-recipes
