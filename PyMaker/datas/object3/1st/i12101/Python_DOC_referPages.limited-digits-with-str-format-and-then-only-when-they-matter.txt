Natural Text
If we're printing a dollar amount, we usually want to always display two decimal digits.showsBut on other occasions we'd like to print the fractions only if they matter. If the two numbers above were volume, for instance, we may prefer to displayCan this be obtained directly with format?
In String Formatting Operations Python Docs describe the %g format which truncates trailing zeros.Or using Python 3 string formatting:The g formatter is unintuitive but you can get some interesting results by setting the precision:Note that in this case setting precision to .5 achieves the desired result of 2 decimal places.Of course you could combine this with f floating point formatter first to get whatever you wanted.


Answer URL
https://docs.python.org/3/tutorial/inputoutput.html
https://docs.python.org/3/library/stdtypes.html#printf-style-string-formatting
