Natural Text
Consider the following (non-working) example code:This will not work, because test_gen has no yield and is no longer a generator function. In this small example I could just return the values from foo and bar and put the yield into test_gen, however I have a case where that's not possible. How can I turn test_gen into a generator function again?
You need to loop over the results of the delegated generators and yield those:If you are using Python 3.3 or up, you'd use the yield from expression to do proper generator delegation:Both re-introduce yield into the function, once again making it a generator function.
why not just:


Answer URL
https://docs.python.org/3/reference/expressions.html#yield-expressions
