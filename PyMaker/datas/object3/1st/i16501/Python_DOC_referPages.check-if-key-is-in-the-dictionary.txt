Natural Text
This question already has an answer here:Check if a given key already exists in a dictionary                    21 answers                How to check if there a key in the dictionary?Here is my dictionary:I have tried to do this:But I get an error:What is the right way to do it?
Just use the in operator (which is accompanied by the not in operator):Below is an excerpt from the documentation of Python's dictionary type, where d is a dictionary:key in dReturn True if d has a key key, else False.key not in dEquivalent to not key in d.


Answer URL
https://docs.python.org/3/library/stdtypes.html#typesmapping
