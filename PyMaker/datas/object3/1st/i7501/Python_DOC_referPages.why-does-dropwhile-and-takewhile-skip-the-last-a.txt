Natural Text
Why does dropwhile and takewhile skip the last a in the following exmaple?Before test in console, I think the result should be ['r', 'd', 'v', 'r', 'k'] and ['A', 'a', 'a'].The documentation said the roughly implementation of dropwhile and takewhile as follows:
You are not filtering the string, you are dropping or taking as a certain condition is maintained.The drop-while would stop dropping when reaching "r" which is not a vowel, and take the rest, and the take-while would stop taking when reaching "r" which is not a vowel, and drop the rest.The behavior you are describing as desired can be gained with filter:
Starting at the beginning, it takes items While they match a condition, and stops when they don’t. You’re looking for filter (and itertools.filterfalse):or a list comprehension:


Answer URL
https://docs.python.org/3/library/functions.html#filter
https://docs.python.org/3/library/itertools.html#itertools.filterfalse
