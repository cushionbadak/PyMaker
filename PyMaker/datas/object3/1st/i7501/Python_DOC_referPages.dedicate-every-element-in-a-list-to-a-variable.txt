Natural Text
This question already has an answer here:How do I create a variable number of variables?                    16 answers                So let's say we have the lists a = [1,2,3,4] and s = ["q", "w", "e", "r"].I want to dedicate every element in s a value in a, so that print(w) prints 2 and so on. You could writeBut here print(w) makes no sense and we get an error.
A dict does exactly this:To get the value associated with e, simply use dict indexing:
The following solution does exactly what you have specified, using the built-in exec() function. However, you should never ever ever use it! It is hideously unsafe, and I am only posting it for completeness' sake. Use the solution after it instead.What you should do is use a dictionary:What this does is it matches each element of the lists s and a into key-value pairs in a dictionary by their index (meaning that they are matched based on their order).And then you can do:


Answer URL
https://docs.python.org/3/library/functions.html#exec
