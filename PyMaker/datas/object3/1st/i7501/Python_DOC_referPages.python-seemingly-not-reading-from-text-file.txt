Natural Text
I have no clue. It prints nothing. The array is empty. I've tried grabbing every null character and removing them in case it's UTF16 -> open as UTF8, didn't work.
You are passing the incorrect second argument to the open call to read the file in this way:According to the Python docs for open, 'r' if the default flag for reading while 'a+' is for reading and writing but you will have to do so in a different manner:
Try this


Answer URL
https://docs.python.org/3/tutorial/inputoutput.html#reading-and-writing-files
