Natural Text
Thank you so much for reading my first post. Started learning Python 3.6.1 - got stuck at the starter - what's wrong with following code:
Your input is a string, and you need to convert it to an int to use comparison operator.Instead of :Try this:
Input returns a string object in python 3. You are trying to see if a string is smaller or greater than an integer. This does not work.from the python 3.6 documentationhttps://docs.python.org/3/library/functions.html#inputinput([prompt])If the prompt argument is present, it is written to standard output without a trailing newline. The function then reads a line from input, converts it to a string (stripping a trailing newline), and returns that.try:Do however note that when when you give any non numerical character as input in the myAge = input()this will raise an other error. Since you can't force for non numerical characters into an integer.
The problem is that you need an integer but the input() returns a string.You can convert the input to int using somrthing like the following:For Python 3.x:For Python 2.x:


Answer URL
https://docs.python.org/3/library/functions.html#input
