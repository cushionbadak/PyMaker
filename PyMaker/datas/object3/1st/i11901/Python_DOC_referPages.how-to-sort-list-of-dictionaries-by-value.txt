Natural Text
This is an extract of my list of dictionariesHow can i sort that by document and then by startPos ?i tried something like this but does not worksorted_dict = sorted(dictionary, key=itemgetter(pattern_key[document]))script.py
You can get the entries in the inner dictionary as keys for sorted:A tuple key will be sorted first by the 0th element, then 1st, and so on.Note that this produces a list of tuples, where each tuple is (str, dict).EDIT:In your context, the correct implementation is the following:
Make your sorting based on your desired criteria then create a new OrderedDict from the sorted list, since dict cannot keep the sorting by it's nature:


Answer URL
https://docs.python.org/3/library/collections.html#collections.OrderedDict
