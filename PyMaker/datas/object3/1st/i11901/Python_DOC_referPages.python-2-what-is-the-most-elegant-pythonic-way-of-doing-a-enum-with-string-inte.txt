Natural Text
I want a enum with predefined single character constants (good for storing in database) and string interpretation. Here is what I am thinking of:But gives And besides there must be a more elegant way of doing itHow to do it better?
A couple observations:You shouldn't call __dunder__ methods directly; instead use the matching command: unicode instead of __unicode__I am unable to duplicate your problemUsing the stdlib Enum (3.4+) or the enum34 backport (Python 2.x) you will have to do it the hard way -- make your own base Enum class:and in use:If you can use the aenum library1 you will have an easier time of it:and in use:Note that since unicode is the default in Python 3 I changed the name to describe.1 Disclosure:  I am the author of the Python stdlib Enum, the enum34 backport, and the Advanced Enumeration (aenum)  library.
The enum34 module has what you want. Possibly even better is using integersAnd recreate the object if you get it's value (from a database for instance) using:


Answer URL
https://docs.python.org/3/library/enum.html
https://docs.python.org/3/library/enum.html
