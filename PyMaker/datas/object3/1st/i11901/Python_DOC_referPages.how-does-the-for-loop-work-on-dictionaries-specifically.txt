Natural Text
I was wondering how exactly the for loop accesses keys in a dictionary ?does it call dict.keys() and iterate through that list ?The reason I'm asking is that I want to query a dictionary's key, any key, and I was wondering if there is a difference in performance (aside from visuals and readability) between calling:andWhich brought me to the above question - what does python do during a for loop on dicts, specifically, under the hood?
Iterating dict does not call dict.key(). The dictionary itself support iteration.BTW, dict.keys in Python 3.x returns dictionary key view which does not support indexing.If you want to get any key, you can use next, iter:


Answer URL
https://docs.python.org/3/library/stdtypes.html#dict.keys
