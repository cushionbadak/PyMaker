Natural Text
Folks,  What is the proper way in python not to execute a statement until necessary.Lets say I have a function that does exponential api backoff:In this case, conn.get_all_instances() gets executed when a function is called, instead of being exercised inside the exponential_backup function Thanks!
Well don't call it when passing it in, and only call it when you need it:EDIT:To predefine some arguments in your function you can use partial, that can take in a function apply the arguments to it and return a new function with those arguments already applied, here's an example:This prints


Answer URL
https://docs.python.org/3/library/functools.html#functools.partial
