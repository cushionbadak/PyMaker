Natural Text
I have two lists that contain strings. The first list contains a list of files and directories:The second list contains dirs that I want to check against list1 for existence.  The only results I want is path/to/my/*, but when I use str.find() it is returning any string that contains path or to or my regardless of where it occurs in the string.So instead of just getting:I get everything in list1My code is like so:
All non-zero numbers are Truthy.  When your string is not found, .find() returns -1 which is still True.  You need to make sure the result is not -1:As @PadraicCunningham mentioned in a comment, that isn't the easiest way.  Just use the in operator:
I think you what you need is str.startswith()


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.startswith
