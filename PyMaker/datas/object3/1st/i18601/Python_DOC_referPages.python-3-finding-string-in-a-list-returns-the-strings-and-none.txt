Natural Text
This question already has an answer here:Function returns None without return statement                    6 answers                Code:Returns:
You're not returning anything, so the function returns None by default. Also, you can do this in a more Pythonic way:This is called a list comprehension, and you can read more about them here.
This is the solution


Answer URL
