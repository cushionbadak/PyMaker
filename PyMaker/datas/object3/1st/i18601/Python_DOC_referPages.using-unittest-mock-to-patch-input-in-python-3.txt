Natural Text
How do you use the @patch decorator to patch the built-in input() function?For example, here's a function in question.py that I'd like to test, which contains a call to input():Here's my test, which gives me the error "ImportError: No module named 'builtins'":
__builtin__ module is renamed to builtins in Python 3. Replace as follow:UPDATEinput has an optional parameter. updated the code to accept the optional parameter.
Or use Mock's return_value attribute. I couldn't get it to work as a decorator, but here's how to do it with a context manager:
For Python 2.x:worked for me.


Answer URL
https://docs.python.org/3/library/functions.html#input
