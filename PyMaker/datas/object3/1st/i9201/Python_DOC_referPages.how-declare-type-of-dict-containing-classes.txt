Natural Text
I'm tring to declare type of following function parameter with typing module:p parameter of my_func must be dict with childclass of A as key and str as value. Actual notation fail with mypy check:example.py:17: error: List item 0 has incompatible type "Tuple[C, str]"How to declare type of p with typing ?
The code is using the class C itself as a key, not an instance of the C.Passing the instance of C class should be fine.If you really need to pass the class itself (I doubt it), you need to use typing.Type:


Answer URL
https://docs.python.org/3/library/typing.html#typing.Type
