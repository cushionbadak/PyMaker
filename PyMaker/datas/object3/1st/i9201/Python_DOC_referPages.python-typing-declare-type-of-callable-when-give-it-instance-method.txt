Natural Text
Consider the following code:My PyCharm IDE indicates a typing error for the last line:Is it a typing/type declaration error? Is the PyCharm type checker failing? If it's a typing error, what should it be?
This is a PyCharm typechecker bug. The mypy typechecker accepts your example without warnings or errors:Because F().b is a bound method, it inherits the signature of the underlying function without the self argument (as it is the job of the bound method to pass in the bound instance).For example, the typing.get_type_hints() function, applied to the bound method, correctly omits self:


Answer URL
https://docs.python.org/3/library/typing.html#typing.get_type_hints
