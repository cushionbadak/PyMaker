Natural Text
I have a file "test.txt". Its data is in the following style:This example only shows the first row in the file, the file has actually 20 rows.In each row, it starts with "[" and ends with "]" (note that the "\n" is just a new line symbol.).As you can see, the pattern in each row is "[(  ((  )  ) ), (  ((  )  ) ), ...]". In real case, there are 10000 "(  ((  )  ) )" within one "[ ]".Do you know how to read this information using python3?The result I want isThanks.
Use ast.literal_eval:Safely evaluate an expression node or a string containing a Python  literal or container display. The string or node provided may only  consist of the following Python literal structures: strings, bytes,  numbers, tuples, lists, dicts, sets, booleans, and None.For your specific problem:If you really need the tuples to become lists do instead:Alternatively you can have lists from the beginning if you create all_rows as:


Answer URL
https://docs.python.org/3/library/ast.html#ast.literal_eval
