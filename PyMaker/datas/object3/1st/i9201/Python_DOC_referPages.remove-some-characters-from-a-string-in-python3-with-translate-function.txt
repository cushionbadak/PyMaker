Natural Text
Why does 'a'.translate({'a':'b'}) return 'a' instead of 'b'? I'm using Python 3.
The keys used are the ordinals of the characters, not the characters themselves:It's easier to use str.maketrans


Answer URL
