Natural Text
I'm trying to write a program that will connect to a specified url:and use this query:I would then like to store the result into a csv file.This is the code I have so far:
You should not define the query as a string; leave it a Python object:You'll need to set a application/json request header to indicate what the contents are. You can do so with a urllib.request.Request() object; do encode the JSON data to UTF-8 first:Now you can save the CSV to a file; using shutil.copyfileobj() would make this efficient even for large responses:


Answer URL
https://docs.python.org/3/library/shutil.html#shutil.copyfileobj
