Natural Text
My previous question asked about Enum in pythonLets say I am creating State classes:And then I create different actual Stateswhile this works, I would like to automate the generation of the enum values, and it seems like it can be done by using Meta classes, the question is how?
While I don't think defining a separate class for each enum value is a very robust approach for the reasons stated in my comments, here's one way it could be done which will support the creation of multiple independent base state classes.
Python 3.4 has an Enum data type, which has been backported.or
I have also come up a solution (needs fixing) for my answer:Running it:


Answer URL
https://docs.python.org/3/library/enum.html#module-enum
