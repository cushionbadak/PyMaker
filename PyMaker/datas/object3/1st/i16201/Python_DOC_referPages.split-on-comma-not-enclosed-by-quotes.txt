Natural Text
This question already has an answer here:Split string on commas but ignore commas within double-quotes?                    1 answer                I am trying to split a string using a regular expression (re.split), but it's been a while since I've been using regular expressions.The string looks like:I would like to split the string on each comma unless a substring is enclose by quotes. The output should look like this:
You could try the below code,Regex stolen from here :-) 
You want to use the csv module instead of reinventing it.


Answer URL
https://docs.python.org/3/library/csv.html
