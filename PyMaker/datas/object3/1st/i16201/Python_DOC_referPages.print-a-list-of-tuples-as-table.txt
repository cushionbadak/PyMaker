Natural Text
I have a list o tuples:I want to print it as a table (so the distance between elements should be the same):I was trying to use some examples which I found, but it was used to print a list of lists, and I have a list of tuples.
Using Format Specification Mini-Language 
You can use ljust for left-aligning the element within a fixed widthoutput
Assume that, all tuples in the list is of the same length, you can use this:Output:
Python 3 solution:output:


Answer URL
https://docs.python.org/3/library/string.html#format-specification-mini-language
