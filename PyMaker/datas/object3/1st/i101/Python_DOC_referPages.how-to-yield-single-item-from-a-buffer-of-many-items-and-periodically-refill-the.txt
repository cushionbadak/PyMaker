Natural Text
I have a large datasource from which I pull data, chunk by chunk.Instead of returning a chunk of chunksize items, I want to yield a single item, via a generator, and once I have yield all the data from a chunk I want to load the next chunk.The goal is to return one item at a time, without loading the full datasource, but also without pulling the items one by one from the datasource.Here is some pseudo code:I tried to use a queue that I refill once empty, without success.
You have two options:Either use a while True: loop and exit when the next chunk is empty:Use the two-argument form of the iter() function in a for loop:or, using itertools.chain.from_iterable():The latter requires that you know what the 'end value' looks like. In the above, I assumed that that end value is None, but if it is an empty list, you need to replace None with [].Demo:


Answer URL
https://docs.python.org/3/library/functions.html#iter
https://docs.python.org/3/library/itertools.html#itertools.chain.from_iterable
