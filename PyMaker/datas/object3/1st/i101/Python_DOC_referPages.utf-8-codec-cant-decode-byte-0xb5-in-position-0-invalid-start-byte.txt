Natural Text
i'm trying to combine multiple CSV files into one with this Function :but I get This Error:'utf-8' codec can't decode byte 0xb5 in position 0: invalid start byteand Here is The TraceBack:Thank U.
I'd try: 
First you need to know the type of encoding that your CSV files use. You can try using Chardet: The Universal Character Encoding Detector to predict the encoding type used in your CSV file. Chardet can be easily installed using:After installing chardet you can use the command-line to predict your CSV file's encoding using:The output will be something like this:Then check the encoding of your CSV file and then change the following line in your code:to:You can check the available encodings supported by python. Hopefully this should solve your issue.
try specifying this:df = pd.read_csv(filename, index_col=None, header=0, encoding='latin-1')the latin-1 encoding is magical - it never fails. See what you get.If this is good enough - well there you go.If not, you'll have to find out what encoding the CSV files actually use. You could just try lots of different encodings until the answer seems OK.


Answer URL
https://docs.python.org/3/library/codecs.html#standard-encodings
