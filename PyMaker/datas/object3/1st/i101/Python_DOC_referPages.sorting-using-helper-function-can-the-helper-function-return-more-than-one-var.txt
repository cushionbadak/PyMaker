Natural Text
I have two lists: numbers and group. I used a helper function to sort the list. What I want to know, is it possible to return another variable say, 'sorted'= 'True' from the function?
If you're using Python 3, you can achieve this with the nonlocal statement:This allows the sorted variable in the outer scope to be modified by the inner scope of the helper function - so sort_priority will then return "True".If you're using Python 2, the nonlocal statement isn't available, but something similar can be achieved like this:


Answer URL
https://docs.python.org/3/reference/simple_stmts.html#nonlocal
