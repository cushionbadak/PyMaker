Natural Text
I have this array:How could I append a string 'D' to each element and convert every set as a tuple:
Like this, using a list comprehension:By the way, it's a bad idea to call a variable list, that clashes with a built-in function. I renamed it.
alternative solution is use zip_longestthe result wiil be:
(apart from the fact that list is a very bad variable name)
Another option using zip:

You can use the function product():


Answer URL
https://docs.python.org/3/tutorial/datastructures.html#list-comprehensions
https://docs.python.org/3/library/itertools.html#itertools.zip_longest
