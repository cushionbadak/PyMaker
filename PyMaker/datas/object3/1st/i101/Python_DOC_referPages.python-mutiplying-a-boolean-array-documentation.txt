Natural Text
Where can I find documentation on the code below? Why does multiplying an array by 2, inserts another False into the array?
It doubled the list. Basically multiplying the number of references inside the list. That is the expected behavior.
For multiplying the numbers inside a list you need to iterate through it.The way you are multiplying gives the output that you are getting.
Python has a number of operator methods - the multiplication method is called __mul__ (https://docs.python.org/3.7/library/operator.html#operator.mul) Any class/module can define its own __mul__ method, which will be run when it is multiplied (which is what happens when you use *)If you look at a list you can see it has __mul__ defined:You can call this method with a number:


Answer URL
https://docs.python.org/3/reference/expressions.html#binary-arithmetic-operations
