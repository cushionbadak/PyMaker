Natural Text
I have a following code:If I run it I get:How to force exec stop without errors? Probably I should replace return with something? Also I want the interpreter work after exec call.
Here, just do something like this:If your worry is readability, functions are your first line of defense.
This will work, return only works from within a defined function:but if you want to use return, you must do something like:
There is no built-in mechanism that allows you to abort execution of an exec call. The closest thing we have is sys.exit(), but that exits the whole program, not just the exec. Fortunately, this can be worked around with a minor amount of exception handling boilerplate:
Just for fun, here's another way:


Answer URL
https://docs.python.org/3/library/sys.html#sys.exit
