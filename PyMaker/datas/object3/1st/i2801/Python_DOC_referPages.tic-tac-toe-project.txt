Natural Text
I don't know why the elements in m won't change inside the if statements!m=[[1,2,3],[4,5,6],[7,8,9]]def board():board()for k in range(0,9):
In Python 3, input() function returns a string, which first needs to be cast to an integer before numeric comparison. Use this line:Also, no need for global m, you can remove that line and will keep the same behavior.


Answer URL
https://docs.python.org/3/library/functions.html#input
