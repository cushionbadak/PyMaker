Natural Text
I wonder if the following can be done better:for example:will returnAddendum: the letters are merely an example, it could be a list of anything. That's why I called the function "label_items". The order of the labels doesn't matter.
If order is not important, you can use numpy.unique:
You can use simple map:Result:So, ord function returns an integer representing the Unicode code point of that character. Than ord(x) - ord('a'), where x - some letter, returns alphabetical order number.


Answer URL
https://docs.python.org/3/library/functions.html#ord
