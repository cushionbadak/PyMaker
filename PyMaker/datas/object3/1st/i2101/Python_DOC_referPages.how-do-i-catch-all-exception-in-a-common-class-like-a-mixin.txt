Natural Text
I have class based views in python django application. Most of them handling same type of exception like:I would like to keep this exception handling in a common class(may be a mixin). Which class need the exception handling will inherit the common class.Keep the repeated exception handling in a common class.I am using python3 and django1.11 - class based views 
You can extract the exception handling to the base class and change implementation in the derived classes:
If you are using django class base views, you can override dispatch and create a mixin. In django class based view dispatch method takes in the request and ultimately returns the response.You can do similar to this - Modify this in you way. For for reference visit documentation.


Answer URL
https://docs.python.org/3/library/abc.html
