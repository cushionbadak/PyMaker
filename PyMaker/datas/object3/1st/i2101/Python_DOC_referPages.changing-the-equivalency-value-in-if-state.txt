Natural Text
Here is my code:I wanna the result is that after change_com=input('changeing_say_to: ') , the change_com becomes something except say, like NAME and then again when I give NAME instead of say to the value of com, it would go to if com=='say' state. How can I do this?Here is the target:
You can compare against the current value of change_com instead of hardcoded say - and move your nested input("> ")-calls up to the beginning of your while loop. Also provide a way to leave the loop (beside Ctrl-C) :You can use a string-format param to adjust your message so it matches whatever curently sits in change_com.See str.format(..)Output:
Here is one solution:


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.format
