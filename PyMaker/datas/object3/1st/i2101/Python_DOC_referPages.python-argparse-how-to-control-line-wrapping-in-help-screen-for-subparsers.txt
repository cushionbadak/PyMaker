Natural Text
I'm using Python 3.6.3 and have this piece of code:The help output looks like:The above output doesn't wrap the help text, as expected (except where i explicitly put in "\n")However, the help output for the subparser looks like:How can i get the "goes here" to be printed on a new line (i specified "\n" but that didn't work)How can i disable the line wrap so that the entire help text for the "zzz" argument is on one line?
You need to give the subparser the same formatter_class you gave to the main parser, which you can do by passing formatter_class to add_parser:


Answer URL
https://docs.python.org/3/library/argparse.html#sub-commands
