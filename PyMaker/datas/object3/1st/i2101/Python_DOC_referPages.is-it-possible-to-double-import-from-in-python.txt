Natural Text
Let's say i have a src folder with Player.py file, that has class PlayVideo in it. Can i directly import that class from main.py file that is in same folder as src?Normally, to use that class i would doIs it possible to directly import the PlayVideo class?
Yes, as long as Player is a Python file (.py) or Python module (directory containing an __init__.py file), you can do:See the docs on importing submodules here: https://docs.python.org/3/tutorial/modules.html#packages
The previous is an extremely common import. I'd guess your answer would be:It works for something like:Hope it helps!


Answer URL
https://docs.python.org/3/tutorial/modules.html#packages
