Natural Text
I have two files .. I use round robin to read a line from the first file and the second line from the second file. Then:a and b are list. How it can be round robin with sorting?.. I tried to use But the error is AttributeError: 'generator' object has no attribute 'sort'I need to sort c based on the elements of the first column (d/M/Y)
As the error indicates, generators do not have a sort method. You can instead exhaust a generator via built-in sorted, which accepts an iterable as an input. Here's a trivial example:


Answer URL
https://docs.python.org/3/library/functions.html#sorted
