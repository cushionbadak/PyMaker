Natural Text
This question already has an answer here:How to split a string into a list?                    9 answers                How can I split and parse a string in Python?                    3 answers                I am trying to have a user input items into a list, and then have all the items they entered go into a list. Example:What I would like to do is, if user inputs "Test Test2 Test3 Test4", each of the items (seperated by the spaces) gets put into the itemList. Sorry if I did not explain it very well, and I am not sure if this is even possible.Thanks in advance
You want to create a list consisting of the input string, split on spaces (calling "split()" defaults to splitting a string on spaces).Output, using your suggested input:


Answer URL
https://docs.python.org/3/tutorial/
