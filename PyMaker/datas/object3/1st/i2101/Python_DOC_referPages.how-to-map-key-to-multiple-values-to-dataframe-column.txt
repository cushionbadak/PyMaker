Natural Text
I have a df column that looks like this: How can I create a dictionary object or mapping layer(open to all suggestions) where I can pass any value if it matches criteria and changes to the key value? For example if the value is Other-501c3 then change it to non-profit. Examples (everything after the equal sign needs to change to the value before equal sign):The solution should be scalable I can add in more 'key value' pairsThank you in advance. 
Create dictionaries from keys, merge them and map:
Similar solution to @jezrael's, but instead of creating a new dictionary you can use collections.ChainMap:


Answer URL
https://docs.python.org/3/library/collections.html#collections.ChainMap
