Natural Text
I am working with Pyramid for Python and there are several files that have the syntax below. I'm very new to Python, and none of the 3 books I have read so far mention this format. My guess is that it's a way of creating a list named logger_sqlalchemy.Is this what the code is doing?
It's not a Python file, but a logging configuration file. See logging config file format.
That syntax appears to be completely off. However, if you appear to have ascertained it correctly, it would not be a list, because that raises a SyntaxError:Instead, it would be a dictionary:


Answer URL
https://docs.python.org/3/library/logging.config.html#logging-config-fileformat
