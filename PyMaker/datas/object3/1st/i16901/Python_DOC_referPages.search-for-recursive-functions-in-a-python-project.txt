Natural Text
I need to find all functions in a Python project which are recursive (i.e. call themselves).Any ideas how to approach this?
It's hard to say whether function recursive or not before it runs. I would personally use this one with inspect.getclosurevars (added in Python 3.3):It will work correctly in most use cases, just remember to use func_X instead of __X__ as function methods on Python 2. It will fail only if a function contain a reference to itself without call:
You can parse the source code with ast:


Answer URL
https://docs.python.org/3/library/inspect.html?highlight=inspect#inspect.getclosurevars
