Natural Text
Python doesn't provide len() on generators. What is most efficient way to check if generator contains at least N elements.Some test cases:My naive implementation:Is there any more pythonic way to do this check?
You can zip the iterator with range(n) and then iterate and count the elements:Keep in mind that this will consume the next n elements of the iterator:There's no way to avoid this, but we can split the iterator into two with itertools.tee and return one of the new iterators along with the result:


Answer URL
https://docs.python.org/3/library/functions.html#zip
https://docs.python.org/3/library/itertools.html#itertools.tee
