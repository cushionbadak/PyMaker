Natural Text
I want to display the result by subtracting two-time slots into a fraction e.g 8.5 hours. At the moment I am getting the below output but I want to get 8 and if it was 8:30 for it to say 8.5. I am using Python Pandasstart time example: 8:00:00finish time example: 16:30:00currentOutput
You can use numpy.timedelta on your current output:EDIT: or, you can replace np.timedelta64 with pd.TimeDelta (so you don't need to import numpy):
If z is a timedelta object (see [1]), then you just need to do:to get the time in hours.


Answer URL
https://docs.python.org/3/library/datetime.html#datetime.timedelta
