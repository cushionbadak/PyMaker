Natural Text
This question already has an answer here:Python: Format output string, right alignment                    6 answers                I need my func to print out the following:all items are aligned to the rightand I have solution but it's kinda workaround, could you please help me to correct my solution? 
You can use the format specification mini language together with str.formatprintsEDIT:You can generate these strings dynamically, if you don't know what size you want the columns when you write the codeformat_str is '{:>8} {:>8} {:>8}', because the longest word, "cherries" is 8 characters long.
Doing this:is wrong about 80% of the time. Try it first with for a in mylist or with for index,a in enumerate(mylist). That is usually a simpler approach.In this case:But that doesn't handle scaling the column widths to take the least amount of space needed. To include that refinement, compute the column widths and include them in the formatting:


Answer URL
https://docs.python.org/3/library/string.html#format-specification-mini-language
