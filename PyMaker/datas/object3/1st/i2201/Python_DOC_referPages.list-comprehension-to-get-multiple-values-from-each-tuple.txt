Natural Text
Lets say I have a list of tuples like this:What I want to do is create a list that has the second and third items (but not the first) of each tuple. Obviously I could do it like this:However, I'd like to know how to do this in a single line with list comprehension.One thing I attempted to do was:This however didn't work, since Python does not like unpacking in list comprehensions.
Really could do it with a list comprehension:Understanding:
You could use  chain.from_iterable with list-comprehension:
Try This:Output:   [2, 3, 'b', 'c', 'j', 9]


Answer URL
https://docs.python.org/3/library/itertools.html#itertools.chain.from_iterable
