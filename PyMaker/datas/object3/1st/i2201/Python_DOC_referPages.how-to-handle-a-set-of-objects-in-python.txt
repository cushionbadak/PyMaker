Natural Text
I am given the following data-file: stars.csvI have been given a class definition (and some methods) as shown:I was asked to a write a function read_stars(csv_filename) that takes as a parameter the name of a CSV file of person data and returns a list of Person objects, one for each line in the input file (and in the same order). The function should only require 7 - 10 lines. Then I need to a create a new function filter_people(people, status) that takes a list of people and a "status" string and returns a new list containing just the people from the original list whose health status equals the status parameter. Here's what I came up with so far:So far, so good. So, where am I stuck? In manipulating the filter_stars GLOBAL function. Is it possible to create a global function filter_stars(people, status) such that the output will be classified based on their bmi and status?Test codeExpected OutputI am finding it impossible to come up with a global function that can output as above.Help appreciated.
Question: ...create a new function filter_people(people, status) that takes a list of people and a "status" string and returns a new list containing just the people from the original list whose health status equals the status parameter.  ... output will be classified based on their bmi and status?Define a function 8.6. Function definitions Describe, your function’s formal parameter list and return valueDefine a result variable using one of the Mutable Sequence TypesImplement a traditional control flow construct with 8. Compound statementsImplement "health status equals the status parameter" with Comparisons.Append matching people to the result variable using a Mutable Sequence Type OperationReturn the result variableEdit your Question and show the code you worked out.Comment if you get stuck with one of the above steps


Answer URL
https://docs.python.org/3/reference/compound_stmts.html#index-19
https://docs.python.org/3/library/stdtypes.html#mutable-sequence-types
https://docs.python.org/3/reference/compound_stmts.html#
https://docs.python.org/3/library/stdtypes.html#index-1
https://docs.python.org/3/library/stdtypes.html#mutable-sequence-types
