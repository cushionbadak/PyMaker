Natural Text
Looking for a pythonic way to sum values from multiple lists:I have got the following list of lists:I am looking for the output:I`ve used:but zip only works when I have 2 elements in my_list.Any idea?
zip works for an arbitrary number of iterables:which is, of course, roughly equivalent to your comprehension which also works:
Numpy has a nice way of doing this, it is also able to handle very large arrays. First we create the my_list as a numpy array as such:To get the sum over the columns, you can do the followingAlternatively, the sum over the rows can be retrieved by
I'd make it a numpy array and then sum along axis 0:Output:


Answer URL
https://docs.python.org/3/library/functions.html#zip
