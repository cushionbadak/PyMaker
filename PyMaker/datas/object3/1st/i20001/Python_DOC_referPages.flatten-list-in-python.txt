Natural Text
I want to flatten the list:to:Right now I am doing this:Even though it works I wanted to know if there's a better way.
How about 

You don't need to convert the itertools.chain object (an iterable) into a list:.You could write a deepness function using recursion:
There isn't a better way, this is the most recommended way. See official recipes 
For fixed level of depth (as in example) you can just sum enough times:


Answer URL
https://docs.python.org/3/library/itertools.html#itertools-recipes
