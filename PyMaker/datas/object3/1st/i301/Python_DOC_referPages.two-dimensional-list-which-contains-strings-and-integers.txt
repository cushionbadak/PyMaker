Natural Text
I have a two-dimensional list; i.e. a nested list, which contains integers and strings.I want to a way to go through the strings only, check if they start with a particular letter; say j, delete it and ignore the rest of the elements that don't meet this condition.The desired result will look like this:
If I understand you correctly, here is a solution using list comprehension:
You can use the function lstrip() to remove leading characters:


Answer URL
https://docs.python.org/3/tutorial/datastructures.html#list-comprehensions
