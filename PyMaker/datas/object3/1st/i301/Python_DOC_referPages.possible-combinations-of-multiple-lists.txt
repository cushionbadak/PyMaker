Natural Text
I have a set of variables in a listI iteratively remove one variable from the list and append this to the original list in a list of lists, stopping when the list only has one item. For example, the output using the list above would be:How would i do for a list of any size?Many thanks, J
Here is a solution using itertools.permutations. It is a generator rather than a massive lists of lists of lists, since the number of such sublists grows hyper-exponentially:Output:


Answer URL
https://docs.python.org/3/library/itertools.html#itertools.combinations
