Natural Text
This question already has an answer here:Converting a string representation of a list into an actual list object [duplicate]                    3 answers                I have a string formatted as a list of lists in a file. How can I get this into a variable in Python as a list?E.g. data-string.txtandare equivalent.How can I get this into a real Python list? I have looked into splitting using multiple delimiters, but if that's the right way I haven't been able to set it up correctly.
You may use ast.literal_eval() to do your bidding:
You can use json for some cases, but @Bharel answer is better at all
How about this:


Answer URL
https://docs.python.org/3/library/ast.html#ast.literal_eval
