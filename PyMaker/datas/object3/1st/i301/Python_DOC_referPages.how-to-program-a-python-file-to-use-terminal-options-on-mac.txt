Natural Text
I am building a python file and instead of asking for user input I want it to get the information on the execution command. For example, I want to be able to type inInstead of having to execute code.py and then tell the code "information". How do I do this? P.S. I am using MacOS mojave as the tags indicate. Thanks. 
You can utilize sys.argv in your Python script to read command-line arguments and store them in a list of strings that you can then parse, as follows:Then, from the terminal:
You can use python's argparse module to use command line arguments.https://docs.python.org/3/library/argparse.html


Answer URL
https://docs.python.org/3/library/argparse.html
