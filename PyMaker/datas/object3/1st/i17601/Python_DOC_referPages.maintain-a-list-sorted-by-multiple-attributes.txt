Natural Text
Using Python, is it possible to insert from ss into pp and maintain pp sorted by two attributes, let's say by the2nd then 3rd position in order to have the following result (both attributes ascending):Or (both attributes descending):I don't want to use the following two statments after the loop which already do the job:Because i am dealing with a very long list and i already have the loop which i want to reuse for sorting as well.
You can use binary search during every insertion.


Answer URL
