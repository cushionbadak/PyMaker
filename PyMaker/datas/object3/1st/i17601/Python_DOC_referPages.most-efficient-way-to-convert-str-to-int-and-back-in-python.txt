Natural Text
This works, but I feel it's not efficient to define a new variable just for a print call. IS there a better method to take an input and print it as a string. Then convert it to an int and possibly back yet again to a str?  I read up on the % command but don't believe I'm incorporating it correctly.Number test for if and block manuvering.
Use string formatting to interpolate integers:You can do much more complex formatting, but in this basic example the value for the first slot ({}) is taken from the first argument (firstNumber), which automatically is converted to a string with the str() built-in function.As a complete program, with a few small optimizations:


Answer URL
