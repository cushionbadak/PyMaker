Natural Text
I have to match this pattern [**] inside a given string but its not giving accurate result.Please Help!!
Use backslash(\) to escape meta characters, not forward-slash (/):BTW, using r'raw string literal', you don't need to escape backslash itself.If you just want to check that the string start with [**], use str.startswith:


Answer URL
