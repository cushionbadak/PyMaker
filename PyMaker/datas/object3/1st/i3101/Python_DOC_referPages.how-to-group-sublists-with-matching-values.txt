Natural Text
I have a list with sublist's in it and want to group them based on a common value.For example:I would like to group these based on the first value (the name). The desired result would be:
Use itertools.groupby.Since you want to only group by the first element of each sublist, you must supply a custom key.Output:
Nice question. I prefer the answer by @user3483203 using itertools.groupby but here is a solution with no imports;Output:


Answer URL
https://docs.python.org/3/library/itertools.html#itertools.groupby
