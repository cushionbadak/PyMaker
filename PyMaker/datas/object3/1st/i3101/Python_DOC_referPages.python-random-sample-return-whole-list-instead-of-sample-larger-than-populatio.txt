Natural Text
I want to take a random sample of k elements from a list, using python's random.sample.The size of the population is not known a-priori, and in some cases it may be less then k: in this case I just want the whole list returned.It looks to me a pretty common and straightforward use case, so I am wondering if there is a way to avoid this code that I find a bit verbose:And instead have something like this:
You could add an if statement:Meaning k will never be greater then the number of elements in whole_population, therefore the condition mentioned below will be met.and in some cases it may be less then k: in this case I just want the whole list returned.Hope this helps!
You could use error handling:


Answer URL
https://docs.python.org/3/library/random.html#random.sample
