Natural Text
This is a list . I want to extract a list having only integersI tried the following code: I gotI couldn't proceed further. 
With regexp (note, don't use list as variable name, it shadows built-in function):Prints:
Output:
Your code was close, though I would probably split on the slashes instead of the .csv, select the last element of the list that split() returns, and just remove the .csv from that.int_list = [path.split('/')[-1].replace('.csv', '') for path in list]
By the way, avoid naming your as list as it contradicts with the builtin
I guess that you want achieveTry with Regular ExpressionsFor example:Maybe it's not perfect but it works.


Answer URL
https://docs.python.org/3/library/re.html
