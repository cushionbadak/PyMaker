Natural Text
I am looking to slice a Pandas dataframe according to values in a couple of pandas series.So I need the rows in-between the values of the pandas series.For example:I need the rows in df that are between the value of beginRows and endRows, in each row of df_info.Technically, I can do this as:  I am not sure how to make that list to send as a parameter to np.r_, from the df_info dataframe.Thank you.
You can pass slice objects:Given your input df_info:Or, for an arbitrary number of slices, you can pass a tuple to np.r_.__getitem__:


Answer URL
https://docs.python.org/3/library/functions.html#slice
