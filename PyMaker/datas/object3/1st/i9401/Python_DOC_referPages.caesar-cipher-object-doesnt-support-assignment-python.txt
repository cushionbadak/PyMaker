Natural Text
Write a method that takes in an integer offset and a string. Produce a new string, where each letter is shifted by offset. You may assume that the string contains only lowercase letters and spaces.print should return def.would like to know how to fix this str assignment error please and thank you!
Strings are immutable...results in this error:A better approach is to build up new a list of 1-character strings out of the substituted letters. In idiomatic Pythonic, that would be a list comprehension, but a for-loop would also work fine.Lastly, join them into a new string like this:


Answer URL
https://docs.python.org/3/tutorial/datastructures.html#list-comprehensions
