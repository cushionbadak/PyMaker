Natural Text
I have this list of solutions from Sympy solver:How can I convert this list to a numpy object with cells which are normal complex value?
You can call the builtin function complex on each element, and then pass the result to np.array().For example,Use a list comprehension and the builtin function complex to create a list of python complex values:Use that same expression as the argument to numpy.array to create a complex numpy array:Alternatively, you can use numpy.fromiter:


Answer URL
https://docs.python.org/3/library/functions.html#complex
