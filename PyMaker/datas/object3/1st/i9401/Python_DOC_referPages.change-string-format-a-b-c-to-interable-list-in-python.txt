Natural Text
I received a JSON with a "code" and "list". I want to iterate for all names in list. But this list is as String, and I tried use split() but it is not working in this case.JSON:I want do something like this:I create JSON in AndroidStudio like this:
You can use list comprehension with split() like this:strip() is used to remove trailing spaces from each item


Answer URL
https://docs.python.org/3/tutorial/datastructures.html#list-comprehensions
https://docs.python.org/3/library/stdtypes.html#str.split
https://docs.python.org/3/library/stdtypes.html#str.strip
