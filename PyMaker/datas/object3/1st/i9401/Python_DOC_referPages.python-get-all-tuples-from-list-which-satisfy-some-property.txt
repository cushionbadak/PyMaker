Natural Text
This question already has an answer here:Finding in elements in a tuple and filtering them                    4 answers                I have the following list.I want to find a subset of this list which satisfies properties like "every tuple has "R" as the second entry". I am very new to programming (literally began studying python syntax a few days ago) so be as clear as you can, thanks.EDIT: this should generalize, since the list above could be with many more tuples, and each tuple could have many more entires.
Use a list comprehension like this:Output:Note that you can achieve the same result with filter(), but you should use the above solution (LC is more pythonic):


Answer URL
https://docs.python.org/3/tutorial/datastructures.html#list-comprehensions
