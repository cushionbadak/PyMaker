Natural Text
This question already has an answer here:Windows path in Python                    3 answers                I'm trying to make a program that will launch programs for me, but some programs python just won't find. I've been using the technique of:And this has worked for all of my other programs but this one just doesn't work.
Backslashes in string literals are special:[String literals] can be enclosed in matching single quotes (') or double quotes (")...The backslash (\) character is used to escape characters that otherwise have a special meaning, such as newline, backslash itself, or the quote character.The \r in your string:is being interpreted as an escape sequence meaning carriage return, not a literal backslash followed by a literal "r".You should use a raw string so backslashes aren't interpreted as escape sequences:


Answer URL
https://docs.python.org/3/reference/lexical_analysis.html#string-and-bytes-literals
