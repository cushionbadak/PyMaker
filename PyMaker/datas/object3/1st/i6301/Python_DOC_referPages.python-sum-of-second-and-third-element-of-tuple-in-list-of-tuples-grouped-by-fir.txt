Natural Text
How do you get the sum of second and third values in a list of tuples grouped by the first value?I.e:list_of_tuples = [(1, 3, 1), (1, 2, 4), (2, 1, 0), (2, 2, 0)]expected_output = [(1, 5, 5), (2, 3, 0)]I found several great answers on StackOverflow doing this found tuples with two values, but couldn't figure out how to adjust them for summing both second and third values.One of the good answers for just the second value was this:
You could also do it like this:
Use a defaultdict as a grouper:Now, you can always get a list of tuples back like this:
You can use itertools.groupby to group based on the first item, and then take the cumulative sums of all the last two items in each group:


Answer URL
https://docs.python.org/3/library/itertools.html#itertools.groupby
