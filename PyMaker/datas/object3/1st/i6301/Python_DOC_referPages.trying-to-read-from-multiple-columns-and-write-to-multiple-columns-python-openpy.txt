Natural Text
I am wanting to read from excel workbook Calavi multiple columns and then write all the data to Shelda.  However it keeps giving me:Is it not entirely possible to use multiple columns?
The error message that you're seeing occurs because of improper use of the range object. If you want to loop through all row numbers, then replace the linewith this:


Answer URL
https://docs.python.org/3/library/stdtypes.html#range
