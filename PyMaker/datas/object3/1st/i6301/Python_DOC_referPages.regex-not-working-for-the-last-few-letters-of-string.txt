Natural Text
This is what my header looks likeI was successfully able to get method by doing this:I'm trying to just retrieve the Status Code '404'. Which returns None has no group error
You can try this:
From the documentation:The final metacharacter in this section is '.'  It matches anything except a newline character, and there’s an alternate mode (re.DOTALL) where it will match even a newline.So, you can fix your code just by specifying re.DOTALL in the arguments:As an alternative to match any character, you can use any of the following constructions:For your example it could be:(note that [0-9] is equivalent to \d)Also, for the instance given in the question, it is sufficient to use only:


Answer URL
https://docs.python.org/3/howto/regex.html
