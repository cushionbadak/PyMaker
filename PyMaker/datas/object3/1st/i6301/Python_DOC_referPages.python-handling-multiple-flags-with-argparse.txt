Natural Text
When I execute python myscript.py -i -i myfile.txtThe output should be:So everytime it comes across a flag a peice of code is executed even if it is outputting the same data.At the moment it just prints:info from file
I mean I guess you could do something like this (that's if I'm understanding your question correctly):An example would be:
Use the 'count' action rather than 'store_true':Without -i:With the default None, you'd need catch the None case with:Setting the default to 0 would simplify that testing:


Answer URL
https://docs.python.org/3/library/argparse.html#action
