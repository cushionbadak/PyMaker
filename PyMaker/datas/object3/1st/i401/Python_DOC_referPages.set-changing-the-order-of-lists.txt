Natural Text
This question already has an answer here:Converting a list to a set changes element order                    7 answers                I have several list with dates written as strings in the format "2017-05-22 04:35:00". All lists are ordered. I want to create a new list with dates that are present in ALL other lists.I have been using the following code:Apparently it is working, but the resulting list is not in chronological order anymore. Any idea why this is happening?
Just convert them back to an ordered list when you're done:


Answer URL
https://docs.python.org/3/library/stdtypes.html#set-types-set-frozenset
