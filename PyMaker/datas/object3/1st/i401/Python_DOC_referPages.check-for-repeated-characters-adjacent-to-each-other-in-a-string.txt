Natural Text
This question already has an answer here:How to use re to find consecutive, repeated chars                    3 answers                I want the user to input a string like this starts and ends with (a), and there are repeated letters in between like this:"abbabbabbabbabba"the repetition can be: (bb), (bbb), (bbbbb) or more like this:"abbbabbbabbba" or "abbbbabbbbabbbba" I managed to check the start and end of the string like this:I tried to check the previous values like this:Iut i just can check the repeated letters for one time.Maybe it is not the right though, but I want to know how to check that a letter in repeated in a specific sequence in a string
You can use a regular expression to check that the input matches the desired pattern.The pattern p matches a string that consists entirely of one or more strings of "a" followed by at least two "b"s, with an "a" at the very end.The match function will return a match object if the string matches the pattern, otherwise it will return None.
@f.wueso should it be like this:


Answer URL
https://docs.python.org/3/library/re.html
