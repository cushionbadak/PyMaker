Natural Text
I'm thinking binascii is the module I'm looking for, but I can't quite seem to get the exact results for which I am looking.Here's what I want to do.  I want to convert:toAny Pythonistas know of a cool (or, frankly, functional) way to do this?
In all versions of Python, you can use the function binascii.a2b_hex() (also known as binascii.unhexlify()):In Python 3.x, you can use bytes.fromhex(s).In Python 2.x, you can use the hex str-to-str codec:The codec internally calls binascii.a2b_hex().
With binascii module:


Answer URL
https://docs.python.org/3/library/stdtypes.html#bytes.fromhex
