Natural Text
given a string of thiswhat is the simplest way to get a list like this in python 2.7?such as:but it will break if the some folder names in the path are not unique, eg 
A one-liner:
from Python 3.2, itertools.accumulate allows us to avoid using indexingproducesOn the other hand, in Python 2 you can do something along the line
Split it on the delimiter of interest and then loop through each slice size:Result:Or if you want strings rather than lists, join() them back on the delimiter:Result:


Answer URL
https://docs.python.org/3/library/itertools.html#itertools.accumulate
