Natural Text
My code is:I want to replace https://icanhazip.com with https://ipinfo.io, but I don't know how to extract the ip returned:
It's not clear which HTTP library you're using in self.bot.get, but for a generic one (where content returns a string), you can parse the JSON response manually with json.loads:If you're using requests, you can read the JSON response directly with json() method on the response:


Answer URL
https://docs.python.org/3/library/json.html#json.loads
