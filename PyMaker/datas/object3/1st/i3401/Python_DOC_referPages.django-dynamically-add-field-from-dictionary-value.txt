Natural Text
I'm trying to refactor some code to be DRYI have an importer that imports from an API that has keys that are different than the destination.Working code looks like this:What I am trying to do is this:But Django throws an exception:Is anyone able to advise on how to assign to a field dynamically like this?
To dynamically assign any Python object's member values using str type values to reference the field name you need to use Python's built-in function setattr. Essentially, this line:Can be rewritten to this, using setattr:Read: Python docs.
You want setattr():


Answer URL
https://docs.python.org/3/library/functions.html#setattr
