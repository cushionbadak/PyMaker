Natural Text
This question already has an answer here:difference between find and index                    3 answers                As said in the official documentation, in Python when calling the function index on a Stringindex raises ValueError when x is not found in sI know we can test the existence of a char in a String simply like this 'c' in String, but it makes the syntax kind of akward. Is there any known implementation of the function index that would return a -1 or such an error code instead of raising an Error
The method you're looking for is str.find - even the documentation for str.index says it does the same except raising an error...
Or use a generator expression with default value as -1:


Answer URL
https://docs.python.org/3/library/stdtypes.html#string-methods
