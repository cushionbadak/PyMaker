Natural Text
What data structure is used to build a concatenation of lists of strings with different size? E.g., desired structure: and then fill it in with 'null' strings to get the same size in every list: 
You could use itertools.zip_longest:Edit: As per your comment that you want to add new lists to your array, it is probably more straightforward to create a list of the lists you want to use, and you can append to that list somewhat dynamically:Then you can add a new list to my_list:
Here's one way using a list comprehension. It involves calculating the maximum length of your lists as an initial step:


Answer URL
https://docs.python.org/3/library/itertools.html#itertools.zip_longest
