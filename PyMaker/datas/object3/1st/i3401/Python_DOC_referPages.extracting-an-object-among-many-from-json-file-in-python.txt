Natural Text
My JSON file (5000 in total) looks like this I need to extract only one object named "1" with its contents and save these to a new JSON file with a new name say,  "New file 1", "New file 2" .... Or if deleting all contents except "1" and its contents, and leaving the file as it is with object "1".
convert Json to Dictionary, using python library jsonThan after you are done performingEdit 1You can access the element in a Dictionary by 
You can use the json library to read the file contents into a python data structure (in this case, a nested dictionary). You can them dump specific parts of the JSON data out into new files.


Answer URL
https://docs.python.org/3/library/json.html
