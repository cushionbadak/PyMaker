Natural Text
I want to remove dangling attributes of html element.I use regex re.sub(r'(<[\S]+.*\s)[^=]+[\s]', r'\1', x) to find attributes without =.But this only removes the first. How to repeatedly remove all?
I choose to use HTMLParser to parse HTML instead of preprocessing HTML and using ElementTree to parse HTML as XML.
Use re.findall to tokenize the parts.This outputs:


Answer URL
https://docs.python.org/3/library/html.parser.html
