Natural Text
My simplified code in python 3.5 is:I run it from a tkinter GUI. It works fine from Sublime Text, but when I make my script executable (via cx_freeze), youtube-dl shows an empty black window for a few seconds.It keeps going perfectly but the window is annoying. Is there a way to hide it?
stdout does not work in a GUI application, you need to set the correct flags through the win32api to hide the windows of the subprocess. The use of call() will not work as it does not support the STARTUPINFO helper, instead you can use Popen()Something like this should do the trick:


Answer URL
https://docs.python.org/3/library/subprocess.html#subprocess.STARTUPINFO
