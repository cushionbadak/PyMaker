Natural Text
Is there a better way to do a toggle in python, like a  nyvalue = not myval ?
Use the not boolean operator:not returns a boolean value (True or False):If you must have an integer, cast it back:However, the python bool type is a subclass of int, so this may not be needed:
In python, not is a boolean operator which gets the opposite of a value:And True == 1 and False == 0 (if you need to convert it to an integer, you can use int())
Use not, for example:


Answer URL
