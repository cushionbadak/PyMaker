Natural Text
I am processing a file line by lineAfter a line containing the text is found, I need to work with the next line. Is there any easy way how to get the next line (e.g., increment an iterator), instead of the less confortable method: Thanks for your help.
Yes, use the next() function on the iterator:Note that this can raise StopIteration if the file has no more lines. You can tell next() to return a default value instead:which will be returned instead of raising the exception.
Martijn's response is good, but you could also use enumerate:


Answer URL
