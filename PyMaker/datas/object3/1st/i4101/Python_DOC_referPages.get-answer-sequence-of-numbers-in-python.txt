Natural Text
how to  add sequence numbers to each item in a list? (['a', 'b', 7]) ---> [(1, 'a'), (2, 'b'), (3, 7)]I can not get exactly answer...
enumerate will numerate iterable list , by default it starts from 0If 1 is desired pass start=1
You can use the enumerate object. Enumerate will pair each item in a collection with its index in that collection. For example:will return an enumerate object that when casted to a list like this:will return the list
Another possible solution is using a zip and range function:


Answer URL
https://docs.python.org/3/library/functions.html#zip
https://docs.python.org/3/library/functions.html#func-range
