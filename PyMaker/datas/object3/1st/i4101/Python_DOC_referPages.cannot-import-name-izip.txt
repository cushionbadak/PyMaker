Natural Text
This question already has an answer here:izip not working in Python 3.x                    3 answers                I´m trying to transpose a matrix from a csv-file with the following code:Unfortunately the following error occurs:I already looked through the forums but couldn´t find the right answer for me.
I guess you use Python 3.Use zip() builtin instead.In Python 3 there is no itertools.izip() as the zip() builtin behaves similarly.


Answer URL
https://docs.python.org/3/library/functions.html
