Natural Text
I have a cli build.py I call it build.py -t -c -fWhen I parse the command line arguments, Is there a built in way to get a list of  Below are the definitions, not sure what to change in the dest (that addition of [..] is not working. Just to show what I try to achieve.
The parse_args method of argument parsers returns a namespace. You can access your values directly as properties there:If you need dictionary access (for whatever reason), you can use vars to convert it:Note, that that dictionary will contain all registered arguments, and not only those four. So if you need a dictionary with exactly those four values, it’s probably better to create it explicitely:


Answer URL
https://docs.python.org/3/library/argparse.html#argparse.Namespace
https://docs.python.org/3/library/functions.html#vars
