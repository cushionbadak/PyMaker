Natural Text
Would it be possible to dynamically add filters to a Peewee select statement given a list? For example instead of:I would like to pass in, say a list, and it would filter by content in that list:
You can use reduce (functools.reduce in Python 3.x):With generator expression:Instead of operator.and_ (operator.__and__), if you need to express more complex expression, you can also use lambda:


Answer URL
https://docs.python.org/3/library/functools.html#reduce
https://docs.python.org/3/reference/expressions.html#lambda
