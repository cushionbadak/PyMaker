Natural Text
This question already has an answer here:functools.partial wants to use a positional argument as a keyword argument                    2 answers                Consider the following piece of code that utilizes functools.partial():When I run it, I got the following error:When I change the keyword argument to a positional argument in the penultimate line, it passes:Why doesn't it pass in the first case? I'm using Python 3.4.
This code will work. Function arguments with default value have to be at the end. So b=10 instead of a=10


Answer URL
https://docs.python.org/3/library/functools.html#functools.partial
