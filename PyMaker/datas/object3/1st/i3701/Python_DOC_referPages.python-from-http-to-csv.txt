Natural Text
I need to write a string from HTTP into CSV file.My columns have to be: LATITUDE,LONGITUDE, OSM_ID, HIGHWAY, UPDATED_ATThis is a sample of the output of HTTP link starting from top:Each row is separated by ','.I wrote this codeThe problem is how to split the string from http into rows. Someone can help me?
Here's a simple implementation using pandas and requests:You can extract the longitude and latitude from the geom column like this:Finally, save the required columns to a csv file like so:
Here is another way to do it : 
A straightforward approach.Fetch the dataOrder the dataExport to CSV
Adding to other answers, here is another way of doing it with csv.writer:Which outputs this csv file:


Answer URL
https://docs.python.org/3/library/csv.html#csv.writer
