Natural Text
I have a list of n elements in decimal where each element is two bytes long.Lets say:The idea is to split each element into MSB and LSB and store it in consecutive list elements.Output should be :Now the basic logic for a signal number operation is clear:And store both values in HexOutput:My question is what is the most efficient way to do this operation in Python3 lists of size n.
With a generator expression and some bit manipulation, you can do that like:Code:Test Code:


Answer URL
https://docs.python.org/3/reference/expressions.html#generator-expressions
