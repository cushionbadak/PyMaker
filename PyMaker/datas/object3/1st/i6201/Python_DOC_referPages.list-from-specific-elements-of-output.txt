Natural Text
I am building a script to get data from server logs.  The data arrive in the following format showing time stamps and frequency of occurrences.I'm trying to create a list of just the numbers after the dash to appear like:I've tried splitting the output and then adding just the desired element but keep hitting errors. trying to split on the dash and new line code then simply add the right position for each number:
You can use re.findall:Output:
To remove trailing newlines you can use rstrip():Output:


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.rstrip
