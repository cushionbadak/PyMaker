Natural Text
This question already has an answer here:How to remove the first Item from a list?                    9 answers                I wanna remove a dictionary from a nested dictionary and I don't know how.From this dictionary: I want to remove:  which has the index 0I tried using delete but i get this error:
First, I wouldn't name the dict "dict", use "d" or something else. 
I would write a comment, but my reputation is too low. It seems like your variable userId might be a string instead of an integer. If this is the case, try converting userId to an int:
See this question for more info on del pop and remove Difference between del, remove and pop on listsIf you do not want to use the dictionary you are removing, I would use del, as below:This [0] is deleting the first index of the list stored as the value of the 'user' keyIf you are repeatedly deleting the first index of your list and your list is long, consider using a deque() instead, which has fast pops from either end:https://docs.python.org/3/library/collections.html#collections.dequeAlso, don't call your variables the same name as their types, aka don't call your dictionary variable dict
You can try this:Output:


Answer URL
https://docs.python.org/3/library/collections.html#collections.deque
