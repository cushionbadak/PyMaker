Natural Text
In the below function, how to use the parameters of pim function within chat and chat1 without passing them as parameters?base.py:I tried using from base import * in to chat.py (where chat() is created), but it is not recognizing. I would like to know if there is a way to access the parameters without passing them as parameters?
I feel like that the reason you avoid to pass the parameters is that there is a bloody long parameter list. You can actually change the function pim to the followings:or you can use optional arguments format as follows:or you can use the global keyword to make the parameters global available, however, this solution is not recommended. 
You can store the input parameters in variables and make the variables global, eg:


Answer URL
https://docs.python.org/3/tutorial/classes.html#tut-classdefinition
