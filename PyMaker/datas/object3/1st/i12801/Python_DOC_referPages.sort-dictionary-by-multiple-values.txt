Natural Text
I have the dictionary {'Bill': 4, 'Alex' : 4, 'Bob' : 3, "Charles": 7}I need to sort this dictionary first numerically, then within that, alphabetically. If 2 items have the same number key, they need to be sorted alphabetically. The output of this should be Bob, Alex, Bill, CharlesI tried using lambda, list comprehension, etc but I can't seem to get them to sort correctly. 
Using sorted with key function (order by value (d[k]) first, then key k):
Sort on the dictionary's items (which are tuples) using sorted(). You can specify the sort key which will be by the dictionary's values, and then its keys:


Answer URL
https://docs.python.org/3/library/functions.html#sorted
