Natural Text
I'm trying to count the number of keywords in another py filehere what's i made:I know it's buggy as even if the keywords is inside a quote or after a # sign, it also counts. So what is the better way to count the orange-highlighted words (IDLE default) in python? Many Thanks<(_ _)>
The correct way to do this is using the tokenize module, which takes care of all the edge cases. 
Should consider using Counter: here a snippet to grep all keywords in a file according a list of keywords:EDIT:for the list of python keywords => 
The below code should do for python 2.7 codes. replace the suitable filename, cheers!


Answer URL
https://docs.python.org/3/library/tokenize.html
