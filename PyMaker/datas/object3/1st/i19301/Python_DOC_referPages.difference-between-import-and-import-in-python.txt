Natural Text
I was taking a look at some commit of a project, and I see the following change in a file:The coder replaced import dataFile by dataFile = __import__(dataFile).What exactly is the difference between them?
translates roughly to Apparently the developer decided that they wanted to use strings to identify the modules they wanted to import.  This is presumably so they could dynamically change what module they wanted to import ...


Answer URL
