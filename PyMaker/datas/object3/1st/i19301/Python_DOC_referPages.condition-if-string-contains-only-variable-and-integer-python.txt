Natural Text
So, I'm working on a script in Python 3, and I need something like thisBasically, I am asking for code that would have condition like this:How would the code look like in Python 3?
What you want is to do a regular expression match. Take a look at the re module. The regular expression ^prompt_[0-9]+$  matches the following:If the number must contain exactly three digits then you could use ^prompt_[0-9]{3}$ or for a maximum of three then try ^prompt_[0-9]{1,3}$.
Without regular expression


Answer URL
