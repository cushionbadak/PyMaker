Natural Text
I have this:the above code return a dictionary like this:I want to loop through that dictionary and get just the age:I tried:No data returned and I got this error:
c.perform() doesn't return anything, you need to configure a file-like object to capture the value. A BytesIO object would do, you can then call .getvalue() on that after the call completes:If you are not married to pycurl, you might find requests to be a lot easier:Even the standard library urllib.request module will be easier than using pycurl:


Answer URL
