Natural Text
I use a Python library that uses the logging module. However, I made my own log function that my script uses internally.Here's the logging function I want to utilize:Let's work assuming that there's a logger named some_logger.Is there a way to, instead of printing to stdout, pass positional arguments (representing log information) to another function instead? (To clarify: A function that will call log with the required args)
I found out you can use something like a Handler object to do this. You'd need to define a subclass that implements emit. See the example below for a basic setup:


Answer URL
https://docs.python.org/3/library/logging.html#logging.Handler
https://docs.python.org/3/library/logging.html#logging.Handler.emit
