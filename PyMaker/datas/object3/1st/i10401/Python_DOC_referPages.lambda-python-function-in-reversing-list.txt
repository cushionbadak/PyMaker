Natural Text
This code can be used to show a txt file looking like this (disordered):To looking like this:How can I modify the code so that it instead prints as:
Use the reverse argument to list.sort():From the documentation:reverse is a boolean value. If set to True, then the list elements are  sorted as if each comparison were reversed.
Just use scores[::-1] instead of scores in the last for-loop. Here's a small illustration of this behaviour:
Use reverse when you sort:  You can use it with sorted:  


Answer URL
https://docs.python.org/3/library/functions.html#sorted
