Natural Text
I have a list of lists and I want to merge them to sum the inner index[1] values where the index[0] values match. My list looks like this:I would therefore like the new structure to be:How do I achieve this in Python?
You can use defaultdict(int) from collections module:
Or, use a Counter from collections. You can initialize it either manually by incrementing the values for every key:Or, by providing a flat list of the expanded contents, Counter will then do the counting for you:In both cases, create the result list by using a list comprehension grabbing the contents of the counter with c.items():Result being:
Use a defaultdict:


Answer URL
https://docs.python.org/3/library/collections.html#collections.defaultdict
https://docs.python.org/3/library/collections.html#collections.Counter
https://docs.python.org/3/library/collections.html
