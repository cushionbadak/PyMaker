Natural Text
If I compile a regexAnd want to pass that regex to a function and use Mypy to type checkI'm running into this problemIt seems that you can import _sre but for some reason SRE_Pattern isn't importable.
mypy is very strict in terms of what it can accept, so you can't just generate the types or use import locations that it doesn't know how to support (otherwise it will just complain about library stubs for the syntax to a standard library import it doesn't understand).  Full solution:Example run:
Yeah, the types the re module uses aren't actually accessible by name. You'll need to use the typing.re types for type annotations instead:


Answer URL
https://docs.python.org/3/library/typing.html#typing.re
