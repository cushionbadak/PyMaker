Natural Text
How do I check if a function b() is being called by a method?I want to use mock in a unit test.
If you are just looking for a way to mock objects this is probably the best way to do it.To answer the first part of your question, there is indeed a way to get information about the caller function. This might help you
Assuming you have a test environment you can in fire up the python(or the Ipython) shell and use the debugger to call the function:if this is your function then you can do this in the shell and debug the code:


Answer URL
https://docs.python.org/3/library/unittest.mock.html
https://docs.python.org/3/library/unittest.mock.html
