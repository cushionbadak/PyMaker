Natural Text
A function accepts *args and **kwargs:calling it as b(5, *[1, 2], **{'a': 'b'}) produces the following output:threading.Thread(target=b, args=[5, [1, 2], {'a': 'b'}]).start() gives:threading.Thread(target=b, kwargs={'num': 5, '*args': [1, 2], '**kwargs': {'a': 'b'}}).start() gives:What's the correct way to pass *args and **kwargs to this function?
gives the expected output. Another option is to use functools. The following code also passes arguments correctly:


Answer URL
https://docs.python.org/3/library/threading.html#threading.Thread
