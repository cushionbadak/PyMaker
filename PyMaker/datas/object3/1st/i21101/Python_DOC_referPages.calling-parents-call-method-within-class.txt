Natural Text
I'd like to call a parent's call method from inherited classCode looks like thisAnd I get, And I can't figure out why, can somebody please help me with this?
The super function takes the derived class as its first parameter, not the base class.If you need to use the base class, you can do it directly (but beware that this will break multiple inheritance, so you shouldn't do it unless you're sure that's what you want):


Answer URL
https://docs.python.org/3/reference/datamodel.html#special-method-lookup
