Natural Text
I have a string. For example:orIn this case I would like get number: 4420700. But any number can be there. It would be 3 333 999 233, for example.I had tried this solution:Could you help me?
Do split and replace.or
It looks like you want to remove all whitespace from the string, then parse the longest initial prefix of all digits, stopping if there is a non-digit (such as a hyphen). Here is my code:Btw, don't name your variable str, because it confuses with the built-in type str.


Answer URL
https://docs.python.org/3/library/stdtypes.html#text-sequence-type-str
