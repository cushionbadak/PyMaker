Natural Text
This question already has an answer here:Python Split String                    7 answers                I have a string like this:I want to get a tuple from it:How to do it?
You can split on commas and use a generator expression to convert each item to float, then create a tuple from that.Similarly map can do the same thing
Your string contains a valid tuple literal. You could give ast.literal_eval a try:or split by ', ' and convert each element of the list to float (or something else):


Answer URL
https://docs.python.org/3/library/ast.html#ast.literal_eval
