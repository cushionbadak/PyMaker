Natural Text
Say I have something like this:where the script is required to have at least one input, and I want it to have the option of accepting multiple inputs such as:I've looked into Python's argparse module so I have something like:But I'm not exactly sure how to make it so it can have multiple inputs and be able to retrieve them. 
You can allow an option to receive any number of arguments using the nargs parameter:


Answer URL
