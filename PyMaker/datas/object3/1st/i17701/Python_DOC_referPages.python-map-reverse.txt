Natural Text
Is there a function that takes a list of functions and an input and outputs a list of the functions operation on the input?So like map, but backwards:
You can in fact use a map to do this:Use the list of functions you want to apply as the iterable, and then apply each of them to your input (in this case 3).
You can use a simple list comprehension:
How about simply:You can wrap it in a function if you like.


Answer URL
