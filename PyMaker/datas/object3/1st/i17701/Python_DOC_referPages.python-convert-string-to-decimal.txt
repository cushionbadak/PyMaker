Natural Text
I have a binary value that is stored in a variable. How do I convert it to decimal value or hex value??So the value of the t would beI need the value of the t converted to decimal or hex.
You can use hex() and int():Note that integers are by default represented in decimal, which is why the last line works as it should.


Answer URL
