Natural Text
here is my request line example:I want to split this string and looking forward to result:thanks:-)
Two step approach using str.partition and str.rpartition (could also be accomplished by a str.split and str.rsplit with a maxsplit=1 parameter each time):
You need to be a bit more precise with specifications like whether it's a string. But given that it is a string it should look like:Of course if it is not a string you can't do that. 


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.split
