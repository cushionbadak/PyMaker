Natural Text
While Scraping data using beautifulSoap In this html code there are two <h2> tag but I want to extract data from second <h2> tag. So how can i do this?and so on if there are multiples of same tag and i want to extract data from any one of the tag how can I do that?Code: I had thought of extracting data using keyword instead of using tag.for example I can use <h2> tag and use keyword Recipe to find the data of second <h2> tag
If you know what h2 you want based on order, you simply need to use that as an index to the return of .findAll method:
for example I can use <h2> tag and use keyword Recipe to find the data of second <h2> tagYes, you can do exactly that. You can use the Python re (Regular Expression) module to match partial text inside a tag.From the documentation:If you pass in a regular expression object, Beautiful Soup will filter against that regular expression using its search() method.Demo:


Answer URL
https://docs.python.org/3/library/re.html
