Natural Text
Running Python 2.7When executing:I get:This is my parser:Does this mean I can't use any unicode characters with python argparse module?
You can tryinstead ofWithout encoding argument unicode() defaults to ascii.
The command-line arguments are encoded using sys.getfilesystemencoding():Note: You don't need it in Python 3 (the arguments are already Unicode). It uses os.fsdecode() in this case because sometimes command-line arguments might be undecodable. See PEP 383 -- Non-decodable Bytes in System Character Interfaces.


Answer URL
https://docs.python.org/3/library/os.html#os.fsdecode
