Natural Text
I have a list of string that look like that :And I want for example only the third and fifth element of each string :how can I do something like this :? 
Use operator.itemgetter to get multiple items from different indices:Cool thing is, it also works with slice objects:
You first perform a mapping, for instance using a genrator:and then you iterate over the generator, and process the intermediate result further, like:generating:


Answer URL
https://docs.python.org/3/library/operator.html#operator.itemgetter
