Natural Text
Hey everyone i have this issue here, i really can't make out what the problem is. I know that range(2) gives 0 and 1 for possible i. len(R) = 2. And lists starts with 0.the error:
It should be != instead of == since he wants to get the numbers that are not identical.
In general, It's much easier to iterate over the items in the list:


Answer URL
https://docs.python.org/3/library/functions.html#filter
