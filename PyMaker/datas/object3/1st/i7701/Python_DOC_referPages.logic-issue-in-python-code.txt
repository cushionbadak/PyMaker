Natural Text
I am new to Python,I am writing a simple program Inputexpected output is as belowBelow is the code I have used ...Actual output is:Missing one more value. 
your join did not join the new itemreturnsyou could also use collections.Counterreturns
You could use a defaultdict, that could make the program much easier:I haven't debugged your algorithm but there are several things that stand out:Python doesn't need ; at the end of lines.str.join is actually called as seperator.join(list_of_words_to_be_joined). You used the seperator as input for the function.
data.join doesn't do what you think. What you want is:


Answer URL
https://docs.python.org/3/library/collections.html#collections.defaultdict
