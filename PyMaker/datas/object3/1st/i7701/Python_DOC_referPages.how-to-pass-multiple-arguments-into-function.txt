Natural Text
This question already has an answer here:What does ** (double star/asterisk) and * (star/asterisk) do for parameters?                    18 answers                I'm trying to parse a json file, Suppose the function that is called takes in the json path from a variable called section1To call the function I'd run:How would I then pass in multiple arguments into the function? e.gA solution which does not predefine the number of arguments will be more suitable as the number of arguments can vary.
You can use *args syntax:UPDATE: Usage
You can pass this kind of arguments as an array or as a dictionary (keyword map).To pass as an array use * operator, to pass as a hash, use ** operator.Example:Read more here and here.
If you know the maximum number of args an un-pythonic way of doing it would beIf four arguments aren't given it just assigns 0 to the remaining variables.However, I would recommend passing the arguments as a list. e.g.Hope this helps


Answer URL
https://docs.python.org/3/tutorial/controlflow.html#arbitrary-argument-lists
