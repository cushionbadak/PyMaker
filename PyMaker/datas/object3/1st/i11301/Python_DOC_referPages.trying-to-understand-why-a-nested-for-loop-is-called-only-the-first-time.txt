Natural Text
I have the following code:When I run this method on python 2.7, it behaves as expected.  The nest for loops runs for each time that the outer for loop runs.When I loaded this same code onto the Kaggle python script engine which runs on Python 3, the nested loop only runs once.  For all other iterations, it is skipped.Here's how the method gets called:
In Python 3, zip() returns an iterator.  You can only iterate through the elements of an iterator once.  To replicate the Python 2 behavior, use list(zip(...)) in place of zip(...).


Answer URL
https://docs.python.org/3/library/functions.html#zip
