Natural Text
This question already has an answer here:iterating quickly through list of tuples                    5 answers                I am trying to get the tweets of a list of users into an object in Python using Tweepy, with the intention of iterating through every tweet and passing it into a text classification algorithm.The array where the tweets are saved in, is of the form:So what I need is to iterate through every row of the array and get the string (the tweet text) of the third column.What is the proper syntax for doing it? 
Based on what you provided:


Answer URL
https://docs.python.org/3/library/array.html
