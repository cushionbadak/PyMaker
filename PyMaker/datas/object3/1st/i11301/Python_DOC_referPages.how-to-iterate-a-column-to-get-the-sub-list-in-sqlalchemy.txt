Natural Text
I would like to acquire the sub list from a table like this using sqlalchemy:  and firstly get and secondly getCan I realize it by executing the query language once? 
You can do it with a single query, but you need to perform the grouping in Python. To do that you could use itertools.groupby to process the query results which you would order by (column1, column2). Here is an example:If your table contains this data:the output would be:To process the data replace the print() in the for loop with your processing code.


Answer URL
https://docs.python.org/3/library/itertools.html#itertools.groupby
