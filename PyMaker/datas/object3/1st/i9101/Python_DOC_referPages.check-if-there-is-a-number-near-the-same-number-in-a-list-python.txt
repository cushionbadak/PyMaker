Natural Text
I want to check if for example the int 2 comes after or before another 2:And if the list is this:thx
Try this:
Run through the list once to find all the pairs and put them in a setThen use it like this:will produce tuples of (a[0], a[1]), (a[1], a[2]), (a[2], a[3]), ...The pairwise Itertools Recipe will do the same thing.


Answer URL
https://docs.python.org/3/library/itertools.html#itertools-recipes
