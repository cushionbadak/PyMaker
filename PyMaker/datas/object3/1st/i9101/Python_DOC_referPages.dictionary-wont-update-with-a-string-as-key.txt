Natural Text
I'm trying to update a dictionary using a function returning a tuple where the first element is a string.However the dict.update function does not quite like it.I can also try dictionary comprehension and get the same unexpected result.The only working way is to first save returned value.How to explain this?
From the docsupdate() accepts either another dictionary object or an iterable of key/value pairs (as tuples or other iterables of length two). If keyword arguments are specified, the dictionary is then updated with those key/value pairs: d.update(red=1, blue=2).For example:
Even more simply, change t() to return a {key: value} as such:Should do the trick for you.
@Jack's answer is the one.In both your failed examples, you're using a pair instead of a sequence of pairs.Also, just to add some sauce, here are two more ways to do it:


Answer URL
https://docs.python.org/3/library/stdtypes.html?#dict.update
https://docs.python.org/3/library/stdtypes.html?#dict.update
