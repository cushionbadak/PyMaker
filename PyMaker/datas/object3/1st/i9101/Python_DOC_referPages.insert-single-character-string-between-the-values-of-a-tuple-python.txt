Natural Text
Given the example code below (modified from here),   I want to be able to insert a single character string (but not hardcode) between the token values in the token_specification and the regex like so:I have considered ascii_uppercase but I cannot work the actual syntax of automatically adding that single character in the token_specification.Could someone provide some guidance?
simply add this just after you initial token_specification declaration:(as well as an import string with your imports)
You could convert the tuple to list and insert the character in the middle and then convert back to tuple
So that you won't run out of letters, start by making an iterator that can produce a letter sequence of letter groupings:Next, use zip() to interlace the letters with the name and pattern from the token_specification:


Answer URL
https://docs.python.org/3/library/re.html#writing-a-tokenizer
