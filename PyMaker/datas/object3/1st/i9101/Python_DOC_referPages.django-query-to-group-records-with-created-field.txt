Natural Text
models.pyI am looking for a single Django query that will return results like:"YYYY-MM-DD" is the date value of created field in Post model
I think you can handle it with groupby from python module.I'll give you an example; My previous test, the values is id/pk from the objects. you also can create a function to get title, slug or else.. an example:By default, if you try with final_titles.append(Post.objects.get(id=id)) should return the string from function of def __unicode__(self): or def __str__(self): inside your models.PostAnd finally, simply way to get meta data from the objects.Result of items;
Have a look at the regroup template tag.Something like this should work:


Answer URL
https://docs.python.org/3/library/itertools.html
