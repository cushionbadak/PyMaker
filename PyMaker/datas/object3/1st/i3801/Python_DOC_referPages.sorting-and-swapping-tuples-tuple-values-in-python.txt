Natural Text
I have a list of tuples in Python and I would like to sort them based on the second value of the tuple (which is a string so I would like to sort them in alphabetical order) and swap the first values of the tuples. For example let's say that we have some list:I would like the final result to be like this:Is this possible to do with a built in method or I have to write my own function?
Split the list into two with the old zip(*lst) transpose trick, then sort the 2nd list and zip them back together:
You can use zip:Output:
Here is a functional solution:Note you should not named variables after built-ins: use lst instead of list.
This is not one line but if you want you can try:


Answer URL
https://docs.python.org/3/library/functions.html#zip
