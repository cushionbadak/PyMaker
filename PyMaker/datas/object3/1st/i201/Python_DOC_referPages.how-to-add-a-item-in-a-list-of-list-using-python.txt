Natural Text
Is there any way to add a item to a list of list using python. As an example there is a list as below:I want to add a item '444' for the position test_list[2][1].At last list should be:
Yes that is definitely something you can do.You simply access the list object that you want to add another item to by its index, and then call .append() on that list object with the new value.

Try using append, append does your job:Or use +=, that adds stuff together, but two lists, so do:append is a builtin python list method, here is the documentation for it, and for +=, that is a builtin addition operator, see the documentation for it.
Above Three are absolutely correct. But I want to add One thing If you want to add an element in a list of list and you don't know the ending index you can do something like,


Answer URL
https://docs.python.org/3/tutorial/datastructures.html#more-on-lists
https://docs.python.org/3/tutorial/datastructures.html#more-on-lists
