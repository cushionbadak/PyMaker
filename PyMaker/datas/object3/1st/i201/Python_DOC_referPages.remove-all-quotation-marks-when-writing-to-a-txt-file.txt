Natural Text
Im writing the list deck into a file, however i need each item in each list to not have ' ' around it when written in.As seen in the code above ive tried to use .strip() but that isnt working as its only removing the first and last quotation. Would there be a way to get the text file to look like this:
Use join to join the different parts of each sublist with ', ':Output (in list.txt):Also, note that it is better to use with open... to make sure that your file gets closed whatever happens.
Simply use .replace("'", "") instead of .strip("'"). Strip only removes the characters from the left and right side of the string and not in the middle, which is why you see the array being printed as ad', 'ah', 'ac', 'as on each line:Repel.it Example
You can work along the lines of this:Producing the output:
What you try to perform is to write in csv fromat. Namely try something like this:


Answer URL
https://docs.python.org/3/library/csv.html
