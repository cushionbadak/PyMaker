Natural Text
I have a 2 dimensional list like this :I want to sum every element of every row with another row and outcome is like this:My code is this:But it does not work.
You can pair the sub-lists by zipping the list with itself but with the items rotated to the right, and map the pairs to the operator.add method in a list comprehension:This returns:
That can be done like:Code:Results:
Use zip() with a list-comprehension:
Try this
Use numpy.roll:
Here's one more way to do it using itertools which should work for any number of lists in list1:


Answer URL
https://docs.python.org/3/library/functions.html#zip
