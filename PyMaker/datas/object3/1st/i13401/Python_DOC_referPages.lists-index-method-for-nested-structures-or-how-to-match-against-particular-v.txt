Natural Text
It is possible to get index of particular element in list using index() method, following way (borrowed from here):Is it possible to apply the same principle to nested structures (if not what is the closest most pythonic way)? The result should looks like this:
Search for the element in a container in your list:-You can read about enumerate here if you haven't seen it before.
adding to Navith's solution index...output[(0, 2), (1, 1)]


Answer URL
https://docs.python.org/3/library/functions.html#enumerate
