Natural Text
I am trying to retrieve a list of columns per tablenameLet's say I have the following tablehow can I return a list grouped by the same tablename?So it returns the following
You can use itertools.groupby for this. Note that this requires the list to be sorted by table name first, but in your case it seems to be, anyway. Otherwise, sort it first.Or use a dictionary (or defaultdict), mapping table names to columns:


Answer URL
https://docs.python.org/3/library/itertools.html#itertools.groupby
https://docs.python.org/3/library/collections.html#collections.defaultdict
