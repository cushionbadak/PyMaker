Natural Text
I know you can do something like this in python:but how would I do something like this?That is, I don't want to substitute the 1 for another number. I want to simply omit it if conditional is false.
Use concatenation:In other words, generate a sublist that either has the optional element in it, or is empty.Demo:This concept works for more elements too, of course:
You can do it with a sliceeg
If you truly want to avoid the else, you could write a generator for the list items:Or, since Python 3.3:And then:


Answer URL
https://docs.python.org/3/whatsnew/3.3.html#pep-380
