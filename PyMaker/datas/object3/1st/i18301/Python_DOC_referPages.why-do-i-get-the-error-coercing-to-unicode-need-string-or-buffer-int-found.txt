Natural Text
Get file from userGet search value from userOpen the file Read the lines in the fileClose the file
From the error I infer that use Python 2.x. In this case, use raw_input, input automatically evaluates the input as a Python statement, which in your case means any number you type will be returned as an int, not a string.Your example should work as is in Python 3.x, as input was changed to return a str and raw_input removed.
First File is a reservedword in python. So you'd better not use it as a variable.Here is a sample as a reference


Answer URL
