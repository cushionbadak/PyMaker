Natural Text
I'm trying to take some data from an online CSV file and make a table from it. I use splitlines() to isolate each bit of data but I keep getting a ValueError:Here is my code:When I uncomment "print(Year)" I get a list of integers. All numbers between 1960-2012 and I can't figure out why it won't accept the conversion from string to integer.Any ideas?
Your first row in the CSV is a header row, not a data row:Skip it with:You could use the io.TextIOWrapper() object to have the webpage decoded from UTF-8 for you:


Answer URL
