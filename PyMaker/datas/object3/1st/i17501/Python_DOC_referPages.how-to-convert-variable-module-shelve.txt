Natural Text
please help convert the variable "fileNameClean" so that you can open the file via the module "shelve" 
Use the os.path module to produce a clean path:Your code also produced a base name minus the extension, but you returned a list by using a slice (from index 0 until but not including the last element). You could have used fileNameClean[0], but using the os.path module makes sure you catch edgecases in path handling too.You probably want to make sure you don't use a relative path here either. To open the shelve file in the same directory as the current script or module, use __file__ to get an absolute path to the parent directory:


Answer URL
