Natural Text
in a python program, I haveplease help with having the stuff commented do what it says
The simplest way is to use slicing and str.zfill function, like thisWhen data is abcdefghijklmnopqrstuvwxyz, the output isNote: If you really meant, appending zeros, you can use str.ljust function, like thisThe advantage of using ljust and rjust is that, we can use arbitrary fill character.
Use str.format:or format:About format specification used:
One simple can be (read comments): output: 
If you want to work with it as a list, as stated, then list comprehension will get you there:output:This also covers the >= 20 characters cases as well.


Answer URL
