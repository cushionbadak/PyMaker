Natural Text
A common operation I perform is joining a list of lists of letters into a list of words (strings). I normally use a list comprehension:Or sometimes a little more functional:I'm wondering if there is a better way to write the function needed for the map call than using a lambda expression. I'm trying to learn the operator and functools to expand my python functional programming chops, but I can't seem to find a clear way to use them in this case.
In this case you can actually just say map(''.join, lists_of_letters):because ''.join is itself a function (a bound method on the empty string) that takes a single argument:That said, here it may not be better per se because it's a bit less readable IMO
You don't need the lambda function here:


Answer URL
