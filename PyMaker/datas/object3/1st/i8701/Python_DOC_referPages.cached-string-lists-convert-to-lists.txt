Natural Text
I have cached lists that contains words. I download the lists from the cache memory as string types. Does anyone know how can i convert them to lists again?1st list as stringand i want it in this formationas list.I already try:Any ideas?
You can use ast.literal_eval to evaluate a string that contains a valid Python literal. To handle your data we need to decode the bytes object to a string. We can use the default UTF-8 encoding.output


Answer URL
https://docs.python.org/3/library/ast.html#ast.literal_eval
