Natural Text
here is my code :Cant figure out why points(paddle) only stays at one when the counter should continue to go up by 1 increments.
add to your class and then use it inside points function.
You'll have to make the count a class variable.If you set count to 0 in the points() method, count is set to 0 again every time the method is called.


Answer URL
https://docs.python.org/3/tutorial/classes.html
