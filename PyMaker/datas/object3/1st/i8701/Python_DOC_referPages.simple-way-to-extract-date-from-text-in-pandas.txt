Natural Text
The excerpt from data:Givent the following example of pandas dataframe:Is there a simple (and pythonic) way to convert free text into a standard date time variable? Something like:
Use pd.to_datetime() to convert from text to date type. You can glean the appropriate date types from this list.
to_datetime handles this fine without any specific format specifier:


Answer URL
https://docs.python.org/3/library/datetime.html#strftime-and-strptime-behavior
