Natural Text
I'm trying to write a program in Python that multiplies two 2x2 matrices. I'm having trouble with the user input where the user is required to enter 2 digits which represents one row of the matrix.I'm getting an error saying:invalid literal for int() with base 10: '3 4'What I'm trying to get is something like:I'm having problem with the user input where I want to first input the first digit such as 3, then hitting the spacebar to enter the second digit which is 4. Wouldn't adding the split(' ') be sufficient? 
You can convert to int after the split using a list comprehension like:
Here's one way:


Answer URL
https://docs.python.org/3/tutorial/datastructures.html#list-comprehensions
