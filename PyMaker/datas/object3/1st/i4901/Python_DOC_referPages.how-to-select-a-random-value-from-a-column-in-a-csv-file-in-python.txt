Natural Text
So I am able to print out a full column from within a CSV easily with this code:Which in turn returns:How would I get it to randomly select just one of those questions as well as selecting the column?
You can use random.choice to get a random row in your csv like:Code:Note that this will return a list corresponds to the csv row.  To get a specific column, you can then select like:Test Code:Results:


Answer URL
https://docs.python.org/3/library/random.html#random.choice
