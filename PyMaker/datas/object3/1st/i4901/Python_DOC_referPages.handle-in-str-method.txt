Natural Text
I have a class that looks like this:Is there a way I can define __str__ on A such that I can pass an index in this fashion:and get __str__ to utilise the index and return something like:
To format strings with parameters, use __format__:Example:
What you're looking for is __getitem__, not __str__.


Answer URL
https://docs.python.org/3/reference/datamodel.html#object.__str__
