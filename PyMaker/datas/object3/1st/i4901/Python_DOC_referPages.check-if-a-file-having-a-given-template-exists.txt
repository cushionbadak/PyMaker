Natural Text
I need to perform a loop in order to check during time if files having a given template are added into a directory.In pseudo-code:The os.path.exists(file_path) function needs the entire filename. How could I used a filename containing the * jolly character?Thanks
Using the glob module you could write something like this:


Answer URL
https://docs.python.org/3/library/glob.html
