Natural Text
I want to create an abstract class such that subclasses will raise an error when instanced if they don't implement an abstract property. An error should be also raised if the attribute is a method rather than a property. My attempt:Unfortunately, this does nothing to prevent instantiating subclasses with an attr method rather than a property.The solution should produce the following results:The manual: docs.
This is not as convenient as a decorator, but you can validate that the attribute is a property in __new__ like:Code:Test Code:Results:


Answer URL
https://docs.python.org/3/library/abc.html#abc.abstractproperty
