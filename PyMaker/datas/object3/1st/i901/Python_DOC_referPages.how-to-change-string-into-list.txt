Natural Text
This question already has an answer here:Convert string representation of list to list                    14 answers                Hi I want change a string into an list like:into
Here’s a simple function to deal with this, using split and replace:Assuming that ‘stringlist’ is your string’d list...
You could use .split() then getattr() and ast.literal_eval()This is probably the safe way of doing this.
Probably the most succinct way is use the re module to split and remove symbols from the string, then recombine the strings back into a list data structure:or a one-liner:To Arya's points in the comment since I don't have those variables defined I can't test it, but you should then be able to verify the result is of type list:


Answer URL
https://docs.python.org/3/library/functions.html#getattr
https://docs.python.org/3/library/ast.html#ast.literal_eval
