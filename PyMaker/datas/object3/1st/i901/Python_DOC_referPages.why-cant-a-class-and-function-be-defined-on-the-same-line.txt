Natural Text
For some reason, it's impossible to define a method like this:but you can define a method and the contents on the same line:Why will the second example work but not the first?
For a one-line compound statement, the body must be a simple statement, or a semicolon-separated list of simple statements:It cannot be a compound statement. That would open the door to too much confusing and ambiguous syntax. For example,


Answer URL
https://docs.python.org/3/reference/compound_stmts.html#grammar-token-suite
