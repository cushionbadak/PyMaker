Natural Text
I'm trying to use values in list to select part of word. Here is working solution:but I wonder if is it possible to shorten it? Something like this comes to my mind:It produces:
You can use the built-in slice (and need to name your list differently to be able to access the built-in):
It's not a python [slice][1], but causes a syntax error, because [..] syntax for getting a slice, not creating it:slice is a python builtin, so don't shadow it's name. Create slice aswhere first argumment is a start value, next is a stop value and the last is the step size:Note that we should use [] with a slice, as it calls a __getitem__ method of a list which accepts a slice ojects (look last link for the __getitem__ and slice). 
Try this:


Answer URL
https://docs.python.org/3/library/functions.html#slice
https://docs.python.org/3/reference/datamodel.html#object.__getitem__
