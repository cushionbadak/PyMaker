Natural Text
I have the following dict:Is there a one-liner I can do to add in the index position of that item, for example so it ends up as:Note: I'd also like to make sure the position of the idx is at the start (dict ordering supported in python3).
You could use a list comprehension with enumerate:Output
A oneliner recreates the dictionary - that is very wasteful - lots of time/memory etc needed to allocate and create the new dict. It is better to simply iterate your list of dictionaries and add the index key-value pair:Output:


Answer URL
https://docs.python.org/3/tutorial/datastructures.html#list-comprehensions
https://docs.python.org/3/library/functions.html#enumerate
