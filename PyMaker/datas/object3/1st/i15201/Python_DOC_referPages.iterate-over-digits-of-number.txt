Natural Text
I have a string input, such as 100124. I want to evaluate each digit of the string as an integer, so I do:Is there a syntactically better way to do this? I tried doing string = int(string) before the loop, but a number isn't iterable.
You could map the string to int:Demo:
You can use a list comprehension that return a generator :or map function :


Answer URL
https://docs.python.org/3/library/functions.html#map
