Natural Text
To start off, I couldn't find an answer anywhere. Im not sure if its something I need to import or if its just poorly done code. In a nut shell, with this project im going to make a better twitch follower notifier with SMS messages, and a lot of other things.Edit: The whole crash log is as follows: Also, I had to repeatedly double click the file to get this, so I'm sorry if its a little bit off. I couldn't get a coded crash log thing to work.
You need to convert to bytes:And decode:


Answer URL
https://docs.python.org/3/library/functions.html#bytes
