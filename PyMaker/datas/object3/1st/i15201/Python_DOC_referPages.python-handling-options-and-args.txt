Natural Text
I have written a python code and I want to handle switches and arguments. So I want to tell script: If you see -u then there is no -d <name> in arguments print "sth":Seems this is wrong method
sys.argv is a list so you'd have to test for membership:However, you'd be better of using the argparse library to handle command line interfaces for you. It won't handle interdependencies directly, but it is easy enough to extend the use and exit with a help message:Demo:
check if u is  in the list sys.argv  and -d is not in the list  sys.argv:


Answer URL
https://docs.python.org/3/library/argparse.html#module-argparse
