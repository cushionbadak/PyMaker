Natural Text
How is it possible thatworks and:does not (AttributeError: 'object' object has no attribute 'a') while?What is object() good for then, when you cannot use it like this?
You cannot add attributes to an instance of object because object does not have a __dict__ attribute (which would store the attributes).  From the docs:class objectReturn a new featureless object. object is a base for all classes. It has the methods that are common to all instances of Python classes.  This function does not accept any arguments.Note:  object does not have a __dict__, so you can’t assign arbitrary attributes to an instance of the object class.And object does have its uses:As stated above, it serves as the base class for all objects in Python.  Everything you see and use ultimately relies on object.You can use object to create sentinel values which are perfectly unique.  Testing them with is and is not will only return True when an exact object instance is given.In Python 2.x, you can (should) inherit from object to create a new-style class.  New-style classes have enhanced functionality and better support.  Note that all classes are automatically new-style in Python 3.x.


Answer URL
https://docs.python.org/3/library/stdtypes.html#object.__dict__
https://docs.python.org/3/library/functions.html#object
