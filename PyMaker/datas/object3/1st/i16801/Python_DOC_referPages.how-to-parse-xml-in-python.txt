Natural Text
I have to extract friendlyName from the XML document.Here's my current solution:I there any better way to do this (maybe any other way which does not involve iteration)? Could I use XPath?XML content:
Using ElementTree, you can either read directly from the file or load it into a string. First , include the following import.If you are using a string:Otherwise, to load it directly:Once you have the tree initialised, you can begin parsing the information.
Pedro, in the comments is right. Finds the first subelement matching match. match may be a tag name or a path. Returns an element instance or None. namespaces is an optional mapping from namespace prefix to full name.The ElemntTree docs are really helpful in these cases.https://docs.python.org/3/library/xml.etree.elementtree.html#xml.etree.ElementTree.Element.findEdit:The link I gave in the comments leads to the following code:The code above uses:If you modify that, along with removing the other code which you don't need... The find should look something like this:You can get the xml file, instead of using the input string with the following (from the ElementTree docs):
The find() function stops when it finds the first match. To get all of the elements that match the XPath, use the findall() function.


Answer URL
https://docs.python.org/3/library/xml.etree.elementtree.html#xml.etree.ElementTree.Element.find
