Natural Text
This question already has an answer here:Round Robin method of mixing of two lists in python                    6 answers                How to add two list up in alternate positions?E.g. I have:and when i add the list up, i get:but the desired output is:I have tried this but is there other way to achieve the desired output?
You can use zip and a list comprehension:zip(x, y) is used to pair up the items in x and y:If you are using Python 2.x though, you may want to replace it with itertools.izip(x, y) so that you do not create a list unnecessarily.


Answer URL
https://docs.python.org/3/library/functions.html#zip
https://docs.python.org/3/tutorial/datastructures.html#list-comprehensions
