Natural Text
After asking this question, I expanded upon the accepted answer to make it work on 2-dimensional lists:And 3-dimensional lists:But how would this class be implemented for n-dimensional lists?
You can return the 'callable list' type if the indexed value is itself a Sequence type:This ensures that anything that can be addressed with the [..] indexing syntax can now be addressed with a (..) call syntax as well.I exempted strings; these are sequences as well but you probably don't want to extend this to those values.Demo:


Answer URL
https://docs.python.org/3/library/collections.abc.html#collections.abc.Sequence
