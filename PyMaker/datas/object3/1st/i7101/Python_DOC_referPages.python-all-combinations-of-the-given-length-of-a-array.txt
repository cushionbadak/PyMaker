Natural Text
If I have:I know I should use: to get all combinations such as However, if I want to set a length subsequence of elements, what should I do?For example, the result I want is (if I set 3 as a length):which are all combinations with length 3.
You can do this by using an outer loop to get all the combinations of 3 sublists in a, and then an inner loop to generate the Cartesian products of those sublists:output
Replace WithAnd take a look at the docs for further details


Answer URL
https://docs.python.org/3/library/itertools.html#itertools.product
