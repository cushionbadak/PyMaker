Natural Text
The following code starts a few threads and prints the result after they are all done:As mentioned in the code, I would like to use the results of the threads which are finished while others are still running. What is the correct way to do that?Should I simply start a new thread which would have a while True: loop and continuously check for a new entry in results or is there a buil-in mechanism for such operations (as part of the threading.Thread call which would point to a callback when the thread is done)?
Since you're using Python 3, concurrent.futures is a better fit than threading:


Answer URL
https://docs.python.org/3/library/concurrent.futures.html
