Natural Text
I have a list of fruits [{'name': 'apple', 'qty': 233}, {'name': 'orange', 'qty': '441'}]When i filter the list for orange using lambda, list(filter(lambda x: x['name']=='orange', fruits)) , i get the right dict but i can not get the index of the dict. Index should be 1 not 0. How do i get the right index of the filtered item ?
You can use a list comprehension and enumerate() instead:Like @ChrisRands posted in the comments, you could also use filter by creating a enumeration object for your fruits list:Here are some timings for the two methods:


Answer URL
https://docs.python.org/3/library/functions.html?highlight=enumerate#enumerate
