Natural Text
Say for example I want to split string "12:30-14:40" and have the result in a matrix like: [["12","30"],["14","40"]].I can do this in JavaScript with:and in Ruby with:What would be the python equivalent for the above?
In Python and using map() you will have something like:
You can use a list comprehension:
From Ruby and Python by exampleRubyPythonIf you'd like python to return a list, just like ruby, instead: list(map(lambda x: x**2, a))


Answer URL
https://docs.python.org/3/library/functions.html#map
