Natural Text
I understand str.join():Is there something which outputs a list? Is there a function that will output:That is, a str.join() for lists? (or any other iterable?)
should do the trick where you are working with a list of chars.A more generic solution, that works for all objects is:I'm not aware of a built-in/std-library version of this function.In action:outputs:
How about this?
For the general case, consider the roundrobin itertools recipe
This simple generator avoids building a list (faster, saves memory):Example:


Answer URL
