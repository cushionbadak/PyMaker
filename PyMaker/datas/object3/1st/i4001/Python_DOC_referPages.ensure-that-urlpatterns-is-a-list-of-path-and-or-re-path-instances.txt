Natural Text
What's wrong with my urlpatterns?ERRORS:  ?: (urls.E004) Your URL pattern [ (None:None) ''>] is invalid. Ensure that urlpatterns is a list of path() and/or re_path() instances.To my best understanding it is equivalent to the example in the docs:BTW:
You have put i18n_patterns inside a patterns list, but this function itself produces a list of urlpatterns, not a single pattern. This is not equivalent to the documentation you found.Use concatenation:or prefix the function call with * to incorporate all elements into the list:This is called iterable unpacking and requires Python 3.5 or newer.The documentation used += augmented assignment to extend the urlpatterns list, which is probably a good pattern for you to just re-use:


Answer URL
https://docs.python.org/3/reference/expressions.html#expression-lists
