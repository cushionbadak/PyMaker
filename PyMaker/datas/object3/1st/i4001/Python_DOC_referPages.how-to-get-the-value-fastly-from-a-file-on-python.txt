Natural Text
I want to store a dict and read it with other script fastly.the data stored in a dict like {1:a,2:b,3:c,...}.I stored the dict on disk  using code：then I want to get the value with the key like [3,7,9...] in the other python script.Does it must read the whole file？it wastes a lot of time. I find mmap,but I don't know how to use it.
If you want to read in single items from an object stored on disk (rather than the whole dictionary), you need a database of some sort.The shelve module is an easy database tool built into Python for dictionaries.  Here's an example from the docs:


Answer URL
https://docs.python.org/3/library/shelve.html#module-shelve
