Natural Text
This question already has an answer here:How do I create a variable number of variables?                    16 answers                If I were to read 10 input files and store them in variable names file1, file2, file3 etc that are dynamically created inside a for loop using string concatenation, how would I store the values in them?For example,doesn't work.What are the alternatives to create variables dynamically to store values that are available dynamically?
One option is to use a dict structure:


Answer URL
https://docs.python.org/3/library/stdtypes.html#dict
