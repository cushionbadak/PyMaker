Natural Text
Using the 2D CNN shown below to extract features from images, how i can store the extracted features in order to train a SVM to classify the features?Model:Extracting features with:Steps:(1) Storing these extracted features from my image dataset in order to train a SVM classififer.(2) Using train_test_split() to split the train and test data(3) Train the classifierI need to know how to do this.Thanks for the support and attention!
You can try saving and loading them as HDF5 file format. It has several advantages over pickle. It's much faster to save and load (especially for large arrays).To do so, you need to install h5py package. Example codes for saving and loading are as follows:For saving:For loading 


Answer URL
https://docs.python.org/3/library/pickle.html
