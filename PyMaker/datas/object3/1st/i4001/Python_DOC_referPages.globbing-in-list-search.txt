Natural Text
Given this list:I'd like a.b.c to match aa[0] and aa[2] using globbing.glob and fnmatch seem to expect filename paths to do this work.  Is there anything that implements globbing for arbitrary string searches?
The fnmatch.fnmatch() function works with any string, not just filenames:The first parameter is named filename because the glob pattern style matching is traditionally used to filter filenames. And as the documentation states, the filename separator (/ on a POSIX system) is not special to fnmatch() and won't alter how a string is matched.If you want to test if there are any patterns that match a given string, use the any() function:


Answer URL
https://docs.python.org/3/library/fnmatch.html
https://docs.python.org/3/library/functions.html#any
