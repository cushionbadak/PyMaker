Natural Text
I imported unittest in python 3.6, and used it like so:the output looks like this:and the second call to print i.e. print("printing after calling unittest"), does not execute. Why is it that I can't do anything after unittest? How can I continue with the code after testing?
The unittest docs explain this: By default main calls sys.exit() with an exit code indicating success or failure of the tests run.sys.exit() exits the script immediately, so your final line is never called. You can avoid this behaviour by passing exit=False:


Answer URL
https://docs.python.org/3/library/unittest.html#unittest.main
