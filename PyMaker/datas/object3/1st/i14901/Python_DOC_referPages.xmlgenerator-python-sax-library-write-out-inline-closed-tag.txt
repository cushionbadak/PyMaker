Natural Text
I write out xml by using the python XMLGenerator class from xml.sax.saxutils.Writing out tags is always done by the startElement and endElement functions. For inline tags (something like XHTML's <br /> or <img />) I would want to write out the tag with end directly. Is there a way to do that with SAX?
If you're using Python 3.2 or later, there's a short_empty_elements parameter you can pass to XMLGenerator (see the Python xml.sax.utils docs).For example:will produce the output:


Answer URL
https://docs.python.org/3/library/xml.sax.utils.html#xml.sax.saxutils.XMLGenerator
