Natural Text
I want to check if a string matches any d["vin"] a list of disctionaries:How is this possible?
Use the any() function and a generator expression:This assumes that all d in mylist have that key; if that's not the case use dict.get():In either case, any() will short-circuit; it'll only test as many d as required to find a match. For your sample, only the first dictionary will be tested.
Use any and a generator expression:

You can use any with in:


Answer URL
https://docs.python.org/3/library/functions.html#any
https://docs.python.org/3/glossary.html#term-generator-expression
