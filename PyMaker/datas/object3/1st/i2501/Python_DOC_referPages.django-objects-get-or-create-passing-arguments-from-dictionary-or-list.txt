Natural Text
I currently have a django management command to import CSV files, which is based on this answer.  The main business is done by the following line in a loop:However, because I have a number of different models to run this on, I wish to put the above code inside a function, and pass the attributes and row keys in from a dictionary; so I'd defnine something like:However I don't know how to run the get_or_create function based on this.  I'm sure this should be simple.
First of all, your dictionaries should look like this:Make sure you use a DictReader so that you're reading the csv file's header and then iterating the rows of the csv file correctly, i.e. the keys of each dict yielded are the column names.Then, the get_or_create call will be a simple unpacking, something like this:


Answer URL
https://docs.python.org/3/library/csv.html#csv.DictReader
