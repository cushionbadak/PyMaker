Natural Text
Consider the following codes and outputsCode1:Output1:Code2:Output2:My intention is to print as followsone is one two is two three is threeWhere I need to modify to print in desired order?
Try this:
Per taoufik's comment, you want to split each sentence before iterating over it. This is because, by default, when you iterate over a string in Python it will go character by character. By calling split, you split the string (by spaces) into a list of words. See below.


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.split
