Natural Text
How can I generate a random string with three integers A, B and C containing exactly A letters 'a', B letters 'b' and C letters 'c'?However, I am capable of achieving such kind of functionality using python random.choice() function.
Complexity is O(a+b+c) but that will work:
Try this:
One way to do this would be to make a string containing those characters, then sample from that string to shuffle it:Based on your comment, a function could look something likeIf you're only worried about those 3 characters, this can be simplified to 
Function:Using:
You have to check for variables A, B and C for int though:


Answer URL
https://docs.python.org/3/library/random.html#random.sample
