Natural Text
This question already has an answer here:Python format throws KeyError                    1 answer                In python 3.5 this results in an error KeyError: 'a'.I cannot understand why. I can use a workaround to overcome the error but I would really appreciate if somebody could explain why am I getting the error. 
the item before the colon is an index into the arguments of the format() function. See https://docs.python.org/3.5/library/stdtypes.html#str.format for more details.So your example should read:Alternatively, if you are running python 3.6 or above, you can use f-strings:See https://docs.python.org/3/reference/lexical_analysis.html#f-strings for details
Dict-like notation:Sequencial notationEither use the dict-like notation:print('just testing {num}'.format(num=a))or the sequential one:print('just testing {0}'.format(a))
here the indexing for the variables start with 0.So if you had another variable Let's say b=3.142the syntax would be :


Answer URL
https://docs.python.org/3/reference/lexical_analysis.html#f-strings
