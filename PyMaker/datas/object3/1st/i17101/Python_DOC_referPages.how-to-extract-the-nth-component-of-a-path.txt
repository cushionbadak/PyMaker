Natural Text
Consider the following file path:How can I extract the nth component of the path? without slicing 
You can use split() as follows:You need to use '\\' instead of '\' since you need to escape the backslash. Otherwise, you will end up escaping the second ' and get an EOL parsing error.
Split the path with str.split:
You can try this ,Output:


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.split
