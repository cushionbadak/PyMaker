Natural Text
Generating different combination of strings while working on some issue, observe the following behaviorI'm aware of the condition where single quoted string can hold double quotes in between, and double quoted string can hold single quote in between.Can someone please explain, why we are observing this behavior, where inside single quoted string two simultaneous single quotes are allowed, but single quote not.
Python implicitly concatenates nearby strings with nothing in between them. Observe:Strings with an odd number of one kind of quote are ambiguous and therefore not allowed.This can be a sort of "gotcha" when dealing with lists if you forget a comma:


Answer URL
https://docs.python.org/3/reference/lexical_analysis.html#string-literal-concatenation
