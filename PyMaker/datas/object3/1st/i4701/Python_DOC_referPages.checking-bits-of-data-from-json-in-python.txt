Natural Text
I want to be able to check if a JSON array has a piece of data that meets criteria. I am learning Python and JSONs are somewhat new to me.This is what I have so far... The idea is that as long as the 'contact_count' < 0 or == to 0 in this case that it wound recursively run that function until its falseI appreciate your help ahead of time!
Note that if your JSON object is not changing, this will result in an infinite loop:


Answer URL
https://docs.python.org/3/library/json.html
