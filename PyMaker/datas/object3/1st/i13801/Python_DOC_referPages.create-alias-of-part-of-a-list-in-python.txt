Natural Text
Is there a way to get an alias for a part of a list in python?Specifically, I want the equivalent of this to happen:But what I get is this:
If numpy is an option:Output:slicing with basic indexing returns a view  object with numpy so any change are reflected in the view objectOr use a memoryview with an array.array:Output:
You could embed each element into its own mutable data structure (like a list).However, I recommend trying to come up with a solution to your original issue (whatever that was) that doesn't create issues of its own.
What you're looking for is a view of the original list, so that any modifications are reflected in the original list. This is doable with the array in the numpy package:


Answer URL
https://docs.python.org/3/library/stdtypes.html#memoryview
https://docs.python.org/3/library/array.html#array.array
