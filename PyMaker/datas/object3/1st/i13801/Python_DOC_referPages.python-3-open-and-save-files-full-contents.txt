Natural Text
I am trying to open a file using 'askopenfilename' and then save the contents of that file to a new file. i could then open that new file for amendment. However I am getting errors and when I attempt to do so. Any help appreciated.The errors provided:
It looks like you're trying to write text (strings) to this file. If so, you'll need to specify a non-binary mode when creating the TemporaryFile by changingtoSee this answer for more details and the tempfile docs for the fact that it defaults to expecting bytes, not strings.


Answer URL
https://docs.python.org/3/library/tempfile.html#tempfile.TemporaryFile
