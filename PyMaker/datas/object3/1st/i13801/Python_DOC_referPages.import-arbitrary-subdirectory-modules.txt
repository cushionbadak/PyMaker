Natural Text
Present situation:I have a script: program.py.I have a directory: /imports. Which contains an arbitrary amount of modules in subdirectories.Python's package directory syntax is being used.Intention:program.py should import every subdirectory in /imports.program.py needs object access to the modules. For example, imports['name'].Service().Remark: I don't feel like illustrating my purpose or questioning the design choice. I'm using the above workaround.The problem is described below. The existing code:Output:Desired output:
Problem identified: The fromlist=[] argument for __import__() is missing. See Why does Python's __import__ require fromlist? for elaboration.Corrected example:Output:Achieved result:Now modules[dir_name].Service() can be used in program.py.


Answer URL
https://docs.python.org/3/library/functions.html#__import__
