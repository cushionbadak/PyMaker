Natural Text
I wonder if there is more Pythonic way to do group by and ordered a list by the order of another list.The lstNeedOrder has couple pairs in random order. I want the output to be ordered as order in lst. The result should have all pairs containing a's then follow by all b's and c's.The lstNeedOrder would only have either format in a/c or c/a. input:output:updateThe lst = ['a','b','c'] is not actual data. it just make logic easy to understand. the actual data are more complex string pairs
Using sorted with customer key function:


Answer URL
https://docs.python.org/3/library/functions.html#sorted
