Natural Text
Expected inputs and outputs:Here's my implementation that feels dirty to me:Because it breaks on Paths without suffixes:So I need to check if the Path has a suffix first:And this is my current use case:Thanks.
You could just .split it:os.path works just as well:It passes all of the tests:
How about a while loop method, where you keep taking .stem until the path has no suffixes remaining , Example -Please note, the while loop exits the loop when example_path.suffixes returns an empty list (As empty list are False like in boolean context) .Example/Demo -For your second input - no_suffix -


Answer URL
https://docs.python.org/3/library/pathlib.html#pathlib.Path
