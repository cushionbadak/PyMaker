Natural Text
I'm trying to create a python script where using the following command in command line I can run the script: For this I am trying to set up the arguements such that if the following command is used the help output can aid the user:I have the following script so far but there are errors that I cannot seem to fix: Ideally the output of python3 myprogramname.py -help should produce the following: Error message:
The usage keyword to ArgumentParser only allows a %(prog)s format specifier.  %(arg.py)s is not a valid format specifier.  Either write usage='arg.py [options]' or write usage='%(prog)s [options]'.


Answer URL
https://docs.python.org/3/library/argparse.html#usage
