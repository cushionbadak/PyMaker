Natural Text
Sorry for the bad title, but I didn't know how to formulate it better ^^.What I'm trying to do is iterate over a list of objects, and then iterate over a list that's a member of each object.So something like this:That's the way I would do it 'traditionally'. I'm interested if there's a way to condensate the two for loops into one?Thanks for your help ^^
You can use itertools.chain.from_iterable if you really don't care from which FooObj the elements come:This has the advantage that it is a generator, so it does not create any (potentially big) intermediate list.
If what you want is to iterate over the list for all objects at once, try this:


Answer URL
https://docs.python.org/3/library/itertools.html#itertools.chain.from_iterable
