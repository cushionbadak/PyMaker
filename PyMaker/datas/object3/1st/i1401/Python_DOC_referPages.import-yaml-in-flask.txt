Natural Text
I have successfully installed the PyYAML:
open is a callable builtin, which is not subscriptable (you can't access items via []). What I think you want is to call it, as in:
I think you need to do something like the following.


Answer URL
https://docs.python.org/3/library/functions.html#open
