Natural Text
And it shows this error
At least one of feet_entry1, feet_entry2, feet_entry3 must be returning '' when you call .get().If you try in the interactive prompt you'll get the same error:You could fix it by filtering out empty values:The if e.get() will convert the value to a boolean. Empty strings evaluate as False so it is ignored.See the documentation for List Comprehensions:


Answer URL
https://docs.python.org/3/tutorial/datastructures.html#list-comprehensions
