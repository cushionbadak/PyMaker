Natural Text
I have this code where I merge 2 files: the output is: How do I remove the "[]" and the "'" from the added fields? I want the output to be:
You're appending a list to a list. What you really want to do is extend the first list with each item of the second list:See the sequence operations
As @JoshSmeaton said, using extend rather than append can make it.I just post another method in case situations where you already get a nested list, and can't change how the list adds new element.Input:Output:


Answer URL
https://docs.python.org/3/library/stdtypes.html#common-sequence-operations
