Natural Text
My code is fairly simple but i don't understand what is going on :while my test code is :So from where I call it I do have sth like :I was expecting the last one to print the dummy value I have given when running the test. Instead I get :what am I missing?
some_func is a instance method, so it is called on an instance of MyDb, not the class itself. An instance is a return value from calling a class. So you need to bear that in mind when you patch.


Answer URL
https://docs.python.org/3/library/unittest.mock.html#where-to-patch
