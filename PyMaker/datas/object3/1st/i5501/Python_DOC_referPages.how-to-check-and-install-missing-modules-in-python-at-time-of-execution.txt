Natural Text
I am working on a Python program which will be distributed to our clients.Their requirement is that the program should take care of everything without their manual intervention. How do I check and install missing modules in Python at the time of executing the code? In R, I can use the code as provided below. How do I replicate something similar in Python?
Use exception handling, then pip to install the modules:Note: the __import__ built-in imports modules by a string name. A probably better way to do this is to use the importlib module, for example importlib.import_module
You can use exception handling:


Answer URL
https://docs.python.org/3/library/importlib.html
