Natural Text
I have to fill excel spreadsheet with prices in column B Since price is the same for article starting "0288" and four numbers after dash does not mean anything so I want to give them one value like this:but when I try to test it, values do not match:What I am doing wrong?
once you create the regex object with re.compile() you need to use it like this:this will return a match object that you can get the matches from using group() like this:you can use the docs page for group() to help you understand it a little better


Answer URL
https://docs.python.org/3/library/re.html#re.regex.match
https://docs.python.org/3/library/re.html#match-objects
https://docs.python.org/3/library/re.html
https://docs.python.org/3/howto/regex.html
