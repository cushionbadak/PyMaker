Natural Text
Trying to write the regularization term for logistic regression in python.In Octive I used the following code:tried the following code in python;getting a SyntaxError: invalid syntax with pointer aimed at the "/"
lambda is a reserved keyword, you can't use it as a general variable name.Rename the variable; adding a _ to the name would be an easy work-around:Note that you have not defined a value for lambda_ anywhere, so no you will have a NameError exception. Also note that Python expressions do not need to end with ;, you'd not normally use a semicolon.


Answer URL
https://docs.python.org/3/reference/lexical_analysis.html#keywords
