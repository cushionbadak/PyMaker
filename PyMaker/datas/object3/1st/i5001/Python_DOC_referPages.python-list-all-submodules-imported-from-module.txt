Natural Text
My code starts with the following:How can I get a list of all submodules imported from this module.For example module has:I want to get after from module import *:(not strings with name of submodules, but a list with all submodules themselves)UPD: I understood that I asked about XY problem.So here's what i'm trying to achieve:I have a folder called modules that will have a bunch of scripts following the same pattern. They will all have a function like main(). In my main script i want to import them all and iterate like that:
Use the pkgutil module. For example:
What about importlib?With 3 differents Python files in ./modules, all like:It gives:


Answer URL
https://docs.python.org/3/library/importlib.html
