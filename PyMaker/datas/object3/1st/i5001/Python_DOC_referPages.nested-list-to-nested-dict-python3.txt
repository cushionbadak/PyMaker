Natural Text
I have a list as below:I would like to convert it into a nested dict as below:I'm not sure how to convert it. Any suggestion? Thank you!
With collections.defaultdict([default_factory[, ...]]) class:The output:collections.defaultdict(dict) - the first argument provides the initial value for the default_factory attribute; it defaults to None. Setting the default_factory to dict makes the defaultdict useful for building a dictionary of dictionaries.
Beginners friendly,Result:
You can use itertools.groupby:Output:


Answer URL
https://docs.python.org/3/library/collections.html#collections.defaultdict
