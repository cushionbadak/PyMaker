Natural Text
Here's the situation:I need to get a value (float) that is different from the selected option in the combobox (a string) and I don't know how to do it.DAP=46MAP=52SPS=21SPT=46combo.get() will fetch me "DAP" (if DAP is selected in the combobox obviously) and I want to get 46 in this case.So when an option is chosen from the combobox I need to get a float (e.g. when someone selects DAP in the combobox I need to get or get returned 46 as a float). With combo.get() I'll get 'DAP' (If DAP is selected for example) and I want to get 46 instead (as a float).I need to relate each gotten string with the corresponding float.I use it on this formula:
Use a dict ([Python]: Mapping Types — dict):and its usage (as current values are ints, convert them to floats):or better (in case the string returned by the combo is not present in combo_dict: [Python]: get(key[, default])):


Answer URL
https://docs.python.org/3/library/stdtypes.html#mapping-types-dict
https://docs.python.org/3/library/stdtypes.html#dict.get
