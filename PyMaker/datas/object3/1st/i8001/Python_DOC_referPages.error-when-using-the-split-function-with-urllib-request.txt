Natural Text
I am trying to split bbc's source into two parts in order to get the top headline:But I am getting this error:
HTTPResponse.read([amt]):Reads and returns the response body, or up to the next amt bytes.returns a bytes object, not str.  So the splitting delimiter needs to also be bytes object. Just add a b in front of the string.


Answer URL
https://docs.python.org/3/library/urllib.parse.html
