Natural Text
I want to compare two lists and get check for the common strings, and if and only if two or more strings are same.The above code works well for one but I want it to check for two keywords. Eg: It should only print(found) if and only if 'name' and 'middle' is found. Not only if 'name' is found. It should check for two strings.
First you can find number of matches using list comprehension and then  len > 2 or not 
You can use sets and intersection if you want to check for the common items (and it's not important which ones).If you want to look for fixed items you could use the issubset method:
If you like one-liners here's my proposition:


Answer URL
https://docs.python.org/3/library/stdtypes.html#set.intersection
https://docs.python.org/3/library/stdtypes.html#set.issubset
