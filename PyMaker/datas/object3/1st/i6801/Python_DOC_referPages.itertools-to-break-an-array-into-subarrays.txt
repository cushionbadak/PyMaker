Natural Text
I have an array of strings that looks something like this I want to break this into something likeHow do I go about doing this ? My Idea was to use groupby in itertoolsto get something like and then probably something likeIn order to do that I would need to access the next element in groupby      How do I do that?
Youd don't need itertools.You can simply use list comprehensions to filter strings with '.' or not, and combine them again in another list comprehension:
Most simple approach:The output:
Here is an interactive demonstration of a general approach.First define input list:Gather common prefix elements:Compute rest of elements:Compute product of prefix with each element of the rest:
Use a list comprehension like this:Output:


Answer URL
https://docs.python.org/3/tutorial/datastructures.html#list-comprehensions
