Natural Text

Right now you pass strings to the search. You iterate over characters in the input string, and check if a character equals second string. Consider this:Convert value to int:Pass integers to the search, not the input:
Danil squared you away with your problem, I just wanted to add another way to do your function. There is a built in method called enumerate which returns the index and item which you can assign to variables like so:Another thing to add is with your split attribute. You should explicitly state how you want to split your strings. My favorite is by commas but you can use spaces too. or


Answer URL
https://docs.python.org/3/library/stdtypes.html#common-sequence-operations
