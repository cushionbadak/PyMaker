Natural Text
I started to learn Python today and I hit to functions. I would like to compare the input if lower equal or higher than for example 5. It does not work correctly or I end up with invalid syntaxOutput:
Although there are many cases where it is a good idea to use set() it is for use in lists.  In the example you gave above it seems you are not actually using lists so you don't need the set().  Here it is rewritten without it:


Answer URL
https://docs.python.org/3/tutorial/datastructures.html
