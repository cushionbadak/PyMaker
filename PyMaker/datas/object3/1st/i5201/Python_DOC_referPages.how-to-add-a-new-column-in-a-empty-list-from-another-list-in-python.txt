Natural Text
I have a list another list and an empty list M = [].col list has the index of columns of L list that has to be copied to M.So M should be [[1,3,0],[4,6,0],[7,9,0]].How can i do this??I want M as a dataframe.

With numpy you can use a list as list index:
You can use operator.itemgetter along with simple list comprehension to fetch your desired elementsTo Convert it to DataFrame you can do
Just to put it out there, the enumerate solution:This would work faster if col was a set:


Answer URL
https://docs.python.org/3/library/operator.html#operator.itemgetter
