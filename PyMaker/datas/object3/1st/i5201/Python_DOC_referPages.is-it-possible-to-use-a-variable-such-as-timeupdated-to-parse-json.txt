Natural Text
Ok, json_tree is a variable that contains something that looks like this:Can you pass it as a variable....like so??My purpose is to pass one of many json_tree values from a .ini into the class that contains the above code. If this is a bad plan, what could work, instead?Thank you!
This is a use case for jsonpath, in which syntax one would use the expression time.updated to refer to the value in question.


Answer URL
https://docs.python.org/3/library/configparser.html
