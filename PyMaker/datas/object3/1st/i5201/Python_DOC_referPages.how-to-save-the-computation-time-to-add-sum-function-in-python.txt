Natural Text
I am trying to use quicksum function in objective functions when building model in Python and using Gurobi to solve it. When I use a large-scale case, it will take a long time. Any way to improve it? (g[i,j,t] is the inputs and Q[(i,j,t)] is the decision variable in my model).  i,j,t is the keys, g[i,j,t] and Q[(i,j,t)] refers to the values:I change it into the following code, but it is wrong:
Instead of having nested loops, use itertools.product:or make use of the more efficient (and readible) tupledict prod functionality:


Answer URL
https://docs.python.org/3/library/itertools.html#itertools.product
