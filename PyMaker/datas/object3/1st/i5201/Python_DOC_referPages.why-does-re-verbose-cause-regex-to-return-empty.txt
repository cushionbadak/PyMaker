Natural Text
I have the following string:This returns the expected result:however this returns empty:The only difference is the re.VERBOSE flag.
Read the docs:Whitespace within the pattern is ignored, except when in a character class, or when preceded by an unescaped backslash, or within tokens like *?, (?: or (?P<...>.The problem is that a class with re.VERBOSE is the same as matching aclass, which is not in your input. You'd need to escape the space (and use a raw string for general correctness):


Answer URL
https://docs.python.org/3/library/re.html#re.VERBOSE
