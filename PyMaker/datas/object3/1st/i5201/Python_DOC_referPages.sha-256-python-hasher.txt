Natural Text
I wanted to create a python program thats asks for an input then hashes(sha-256) the input then prints it. Does this already exist? How would I go about doing so.
using hashlib:Python 3.xPython 2.x
I have programmed a package wich is able to do this.Install the package like this:Use the module to hash strings:


Answer URL
https://docs.python.org/3/library/hashlib.html
