Natural Text
I am reading MacOS data that is in plist (propertly list) format that is not in the XML format, not in the binary format, but in the other ascii format. For example:I am trying to parse this into a Python dictionary. Unfortunately, plistlib.loads returns InvalidFileException() when I try to parse it:Produces:What's the correct way to do this?
You are talking about plist files in the JSON format. Your example is not JSON. Let's start with this sample plist in XMLAnd convert it to JSON format:Now you can read this using the JSON library.Output:


Answer URL
https://docs.python.org/3/library/plistlib.html
