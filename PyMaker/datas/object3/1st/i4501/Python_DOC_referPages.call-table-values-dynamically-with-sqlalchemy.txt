Natural Text
Let's say I have a table named "info_test" mapped like this:If I want to get the names of all items from that table I would do something like this:which would print:Now I want to create a very generall function that prints the value of a key, where the key is given by an argument of that function. Something like this:
In Python getattr() is used for getting attributes based on variables. As an addition, you don't need to query the whole model object and all its attributes, if all you want is just one:
Willdo the job for you?


Answer URL
https://docs.python.org/3/library/functions.html#getattr
