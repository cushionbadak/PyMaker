Natural Text
I hope to get all classes names and fields within each class given a python file say myFile.py. Is there a way to achieve that? I want to pass in myFile.py and get the result.
Have you considered parsing your Python file with the built-in Abstract Syntax Trees (ast) module, and then examining the results to retrieve the data of interest? Something as simple as:fulfills your main requirement. For example, if we had a python source file as myFile.py containing:we could easily parse it using the above:And the output would look like:Check the rest of the ast package to see what sort of a data you can extract from it (hint: most of it).


Answer URL
https://docs.python.org/3/library/ast.html
https://docs.python.org/3/library/ast.html
