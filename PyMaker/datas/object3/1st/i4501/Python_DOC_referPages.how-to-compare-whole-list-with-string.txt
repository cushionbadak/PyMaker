Natural Text

You're doing the exact opposite of what the method description saysWhat you are doing : Checking if every character in the guessed list is in the secret wordWhat you should be doing: Checking if every character in the string is in the listshould be the contents of the function
return leaves the function - you need to ensure "all" letters are in it. Ýou are only checking one letter and then you are leaving your function with True or false.Fix:Check if all are in it:Python has a nice built-in that if applied to a list if all() (or any() ) are True.You reverse the logic kindof - if all letters of your secretWord are in the lettersGuessed list, you are returning True.This will return true if you have "Banana" as secret an guessed ["B","a","n"] - it does not check if the "amounts" fit.Dokumentation:  all()
Since every letter in secretWord should be in lettersGuessed, assuming repetitions don't count (wasn't defined in the question), you can check if the set of letters in secretWord are contained in the set of letters in lettersGuessed the following way:


Answer URL
https://docs.python.org/3/library/functions.html#all
