Natural Text
I have dictionary with large number of keys and each key has 15 values. Below is sample of dictionary There are 800+ cities or keys in it. What I would like to do is basically, add value which is ratio other value so the above dictionary would additional value called "bus ratio" which ratio of pop/bus value. So, new dictionary would look like Thanks.
Just iterate over all dictionary values and add the new field bus_ratio:
To create an entirely new dictionary, you can use a dictionary comprehension:Output:
Have you tried this?
You could also use dict.update here:
You can iterate over the dictionary keys like this:


Answer URL
https://docs.python.org/3/library/stdtypes.html#dict.update
