Natural Text
I want to launch my script like this:where params.conf is a file and 1, 2 are string arguments. I know how to parse file alone:But how to parse following arguments?
An argument prefixed with @ is treated as if its contents were in the command line directly, one argument per line. So if the contents of params.confisAnd you define a parser likeand you call your script asthen your arguments a through d will be set to 1 through 4, respectively.
You just add more argparser.add_argument calls.Like this:


Answer URL
https://docs.python.org/3/library/argparse.html
