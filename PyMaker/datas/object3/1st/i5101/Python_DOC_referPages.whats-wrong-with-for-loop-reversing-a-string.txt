Natural Text
This question already has an answer here:Best way to loop over a python string backwards                    10 answers                reversing a string using for loop
One way to do it in Python2 is:
You probably want to go backwards like:Code:But even better is:Results:
In the loop if you just assign to a, it will only have the first character of the original after the loop. If you need the full string after the loop, you need to keep appending to a. Also your range values are incorrect,Try the following,If you want the shorter approach, try the following,



Answer URL
https://docs.python.org/3/tutorial/index.html
