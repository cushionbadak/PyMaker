Natural Text
I need to read local variables from Python in C/C++. When I try to PyEval_GetLocals, I get a NULL. This happens although Python is initialized. The following is a minimal example.In the manual, it says that it returns NULL if no frame is running, but... there's a frame running!What am I doing wrong?I'm running this in Debian Jessie.
Turns out the right way to access variables in the scope is:


Answer URL
https://docs.python.org/3/c-api/reflection.html#c.PyEval_GetLocals
