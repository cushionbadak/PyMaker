Natural Text
Initially we don't know how many inputs we are going to take and input is given in the following formatso far i have tried using split() and splitlines()It gives only ['sam'] and same result is for splitlines().How can we take all those values as input?
input reads a single line of input. You need to read in a loop.Here I assume an empty input (i.e. two newlines after another) means the end of input:
This will get all lines from standard input. That means it shouldn't be interactive because that never ends, e.g. it should be piped from a file or process.Alternatively:


Answer URL
https://docs.python.org/3/library/functions.html#input
