Natural Text
When running python there is an interpretation question based onSince this was a question about order of operation, I would like to actually see how the python interpreter actually handles the two statements. Is there a method to do so?
The ast module can show an abstract syntax tree for an expression or statement:The dis module can show bytecode:dis can also operate on the the compiled bytecode stored in a function, and also as a standalone utility with python3 -m dis.Passing a string to dis.dis() is new in Python 3; here are some equivalent ways to call this function:
You could use the ast module to parse your expression. The resulting parse tree should provide a clue as to how the expression will be evaluated.


Answer URL
https://docs.python.org/3/library/ast.html
https://docs.python.org/3/library/dis.html
