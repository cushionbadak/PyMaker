Natural Text
This question already has an answer here:How to join two strings from different tuples but in the same index using Python?                    4 answers                How do I concatenate two lists in Python?example:input:output expected:
You have two lists, each with a single element, so you can make a new list by concatenating the [0] element of each list
Using zip and a list comprehension.Ex:Output:
Join is way more readable, and just use + to combine both lists.


Answer URL
https://docs.python.org/3/tutorial/
