Natural Text
I have no idea how to call this operation, so I couldn't really google anything, but here's what I'm trying to do:I have this dataframe:And I want to group it on df.name and apply a max function on df.values but only if the names are in sequence. So my desired result is as follows:Any clue how to do this?
Using pandas, you can groupby when the name changes from row to row, using (df.name!=df.name.shift()).cumsum(), which essentially groups together consecutive names:
Not exactly a pandas solution, but you could use groupby from itertools:Output


Answer URL
https://docs.python.org/3/library/itertools.html#itertools.groupby
