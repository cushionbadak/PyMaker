Natural Text
This question already has an answer here:How do I create a variable number of variables?                    16 answers                I want to make a1=0, a2=0,...  aN=0.I thought using "for"For example N=10but it isn't not zeros(just print).So, I did 'a%d'%i=0. but It didn't workHow can I make that?
For printing use .format() (or f-strings on python 3.6+ :If you want to calculate with those as names, store them into a dictionary and use the values to calculate with them:Output:
You can use a dictionary for that.For accessing them individually,or you can access them all together like this,outputs:
Simple solution, using const value x=0, and counter i:output:
You can also make use of globals and locals built-in functions.


Answer URL
https://docs.python.org/3/library/string.html#string-formatting
https://docs.python.org/3/tutorial/datastructures.html#dictionaries
https://docs.python.org/3/library/functions.html#globals
https://docs.python.org/3/library/functions.html#locals
