Natural Text
This gives me:but I would like to add, minumum, average, and max file size for each type of extension to that, as a list or dictionary doesnt matter. I know how to get each individual file size in my directory using:but not sure how to aggregate based on extension type
Since collections.Counter() will only give you back the counts, perhaps a file grouping approach might be more straightforward. What you can do:1. Group file sizes by extension, perhaps with a collections.defaultdict().2. Perform statistics on each group of file sizes.Demo:


Answer URL
https://docs.python.org/3/library/collections.html#collections.Counter
https://docs.python.org/3/library/collections.html#collections.defaultdict
