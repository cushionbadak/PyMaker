Natural Text
I want to create a new __str__ method for complex type. ReturnsIn this case, raising the instance to the power returns a complex type instance and not a subclass instance.  I would like inherited methods to return new instances of the subclass and not complex.
In order for arithmetic operators to return an instance of your subclass, you'll have to implement the relevant special methods like __add__, __mul__, __pow__, etc.The bad news is that there's a lot of those methods. The good news is that you can write code that automatically generates those functions for you. Here's a class decorator that does exactly that:Slap this onto your Phasor class and you're done:


Answer URL
https://docs.python.org/3/reference/datamodel.html#object.__add__
