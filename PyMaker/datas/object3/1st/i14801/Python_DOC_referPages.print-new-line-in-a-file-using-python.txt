Natural Text
I used the following code. The genetarated_rtl.v file contains the text with \n character. It doesn't come up with a new line. But the new line character is printed.When I give the for_simple1.rgt file as below, \n gets printed. I don't want that. What I want is a new line.
To convert a literal backslash followed by a literal 'n' to a newline, you can decode the string using the string_escape encoding:So your function would be:
You can try the replace function:


Answer URL
https://docs.python.org/3/library/stdtypes.html?highlight=replace#str.replace
