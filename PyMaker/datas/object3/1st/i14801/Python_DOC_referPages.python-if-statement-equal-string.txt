Natural Text
I want jobber() to do the following: if user inputs "talk" it runs chat() and "calc" for calculator().This is my very first project and I am completely new so thanks for any help.
use input() and convert to lowercase
For gathering user input, you'd better use input() function.
You should use input() built in function, for example setting the money variable in the calculate function should simply be:Since you are only using the choice variable to determine which function to perform, you can even consider placing the input in the if statement:The python tutorial is a good guide as to how use if statements and other control flow tools.


Answer URL
https://docs.python.org/3/library/functions.html#input
https://docs.python.org/3/tutorial/controlflow.html
