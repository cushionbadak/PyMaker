Natural Text
In python 2.7:In python 3.4:how to change x1 into x2?    how to change x2 into x1?
In Python 3.x, use str.encode (str -> bytes) and bytes.decode (bytes -> str) with latin1 encoding (or iso-8859-1):
Both x1 and x2 are bytestrings in Python 2. If you compare Unicode and bytes on Python 2; you also get False in this case:x1 is a Unicode string in Python 3. You could add from __future__ import unicode_literals to make the code work the same on both Python 2 and 3:Don't mix bytestrings and Unicode strings.To convert Unicode string to bytes:To convert bytes to Unicode string:


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.encode
https://docs.python.org/3/library/stdtypes.html#bytes.decode
