Natural Text
I'm really, really new to Python and was making a small test program.Here is my code:when I try to run it I get the "Unexpected EOF while parsing" error.
For the EOF error, you can just get rid of that try: like soYou can also just add an except clause, as Fernando said, if you still want to use try:
The try needs an except clause.
You need an except after the try. It's just a small syntax error; the eof comes from the python parser before it executes the code. Here's the link to docs just to save a quick google:https://docs.python.org/3/tutorial/errors.html


Answer URL
https://docs.python.org/3/tutorial/errors.html
