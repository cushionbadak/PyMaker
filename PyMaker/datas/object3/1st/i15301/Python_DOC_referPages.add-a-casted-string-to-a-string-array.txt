Natural Text
I have the following code (simplified):  That gives me the following value for bar:However, I want the following value for bar:  Is there a way to do that?  
When working with lists, the += operator acts like list.extend.  In other words, it will take the characters in the baz string and append them to the bar list one-by-one.To append the baz string as a whole, use list.append:Below is a demonstration:


Answer URL
https://docs.python.org/3/library/stdtypes.html#mutable-sequence-types
