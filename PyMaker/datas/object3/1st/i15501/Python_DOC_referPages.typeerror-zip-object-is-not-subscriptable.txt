Natural Text
I have a tagged file in the format token/tag and I try a function that returns a tuple with words from a (word,tag) list.In python 2.7 it worked well, but in python 3.4 it gives me the following error:Can someone help?
In Python 2, zip returned a list. In Python 3, zip returns an iterable object. But you can make it into a list just by calling list. That would be:That makes a list, like in Python 2, so you can use:etc. But if you only need the first element, then you don't strictly need a list. You could just use next.That would be:


Answer URL
https://docs.python.org/3/library/functions.html#zip
https://docs.python.org/3/library/functions.html#func-list
https://docs.python.org/3/library/functions.html#next
