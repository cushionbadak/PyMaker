Natural Text
I'm in the stage of practising Python and while I was doing an assignment related to Python I was stuck. The above code Snippet uses htmlparser for parsing .msg file to convert into csv format.Could any one explain me the what does [1] [0] signify in these below linesPresently I'm using Python community version for development, when I highlight that particular [1] or [0] its showing up as
As described in special method names, the method name __getitem__() allows a class to override the foo[bar] syntax.  Lists do this to provide both subscripting (e.g. foo[5]) and slicing (foo[1:5]).  Dictionaries do this to provide key lookup.


Answer URL
https://docs.python.org/3/reference/datamodel.html#object.__getitem__
