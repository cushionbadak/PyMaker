Natural Text
With the below code I get the error:BUT if i add r.close() it returns me with the error:The code for the keypad is 2580 and the CSV is in the format of Code:
csv.reader does not have a close method (or any other normal methods for that matter).  Instead, it takes a file object as its argument and iterates over it, yielding the lines one-by-one.You should be calling close() on the file object itself:Of course, a with-statement will do this for you automatically:
You need to close the file object returned by open not the csv.reader.Use a with block to close the file for you:
I think you should have this:


Answer URL
https://docs.python.org/3/reference/compound_stmts.html#the-with-statement
