Natural Text
This is a syntax issue I think; I am trying to add a dynamic element to my traversal (? Might be the wrong word) which adds the text of a number of buttons to a list. This is basically pseudo-code but it is how I am thinking:This is to save me having the following:Is it possible to do this? The button_# is the bit that should be dynamic. Apologies If any of my terms are incorrect here.
To get attributes dynamically, you have to use getattr(obj, 'attr_name').In your case it could be like this:For additional details see the docs.


Answer URL
https://docs.python.org/3/library/functions.html#getattr
