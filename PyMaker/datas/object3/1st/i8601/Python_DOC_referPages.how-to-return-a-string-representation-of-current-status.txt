Natural Text
I have to create a function that returns a string representation of the current status that looks like:I have to match the format exactly (2 decimal places, spacing, etc)
You can use the str.format() method, which uses a string with special replacement fields in it that specify how format the data. You don't have to declare all those variables global—although it doesn't hurt—because that will be assumed in the function automatically since none of them are defined within it and only have their current values referenced.Output:


Answer URL
https://docs.python.org/3/library/string.html#format-string-syntax
