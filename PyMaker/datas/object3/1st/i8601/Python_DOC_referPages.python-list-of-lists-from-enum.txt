Natural Text
here is my exampleI would like to write method that will give me a list of ALL lists from enum. For that example it should return I tried something like this:But as you can guess it didnt work. Thanks for any helpful advices.
From the comments it sounds like you want a method on the class that will return a list of all the values.  Try this:and in use:
Here is a full example of what you want. This method will always return every list in the enumeration and ignore every other variable.


Answer URL
https://docs.python.org/3/library/enum.html#creating-an-enum
