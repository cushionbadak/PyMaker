Natural Text
This question already has an answer here:Cross product of sets using recursion                    2 answers                Is there a recursive solution for the following problem:For a given list of lists all combinations of the members of these lists should be calculated, for example:will produce:The following iterative program generates the above output:but I am looking for a recursive solution which probably will be shorter and more readable ? 
you could try this (not recursive but very compact and probably the most efficient you can do):output:


Answer URL
https://docs.python.org/3/library/itertools.html#itertools.product
