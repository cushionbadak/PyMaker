Natural Text
I'm attempting to mock both bulk_create and save for a dry run. I've created code which in essence does following:The problem is that when I do bulk_create instead of print_bulk it executes print_save. How do I fix that or is there a cleaner way to do that?
The order of argument should be reversed. As you can see at Quick Guide where describe patch the order of mock arguments passed to the function follow patch's decorator from bottom to top.In you case simply change the patch order or the function signature like


Answer URL
https://docs.python.org/3/library/unittest.mock.html#quick-guide
