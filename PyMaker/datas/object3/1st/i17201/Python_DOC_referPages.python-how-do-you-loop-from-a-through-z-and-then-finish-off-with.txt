Natural Text
Within the loop, there's tons of code, so I don't want to just duplicate all of that code again just to be able to incorporate the '_' case.So it would go through A, B, C, .. Y, Z, _without any code duplication
Construct a string containing those characters and tack on a _, then loop through it:
Use string.ascii_uppercase and simply concatenate the _:
You can iterate through each character of a string:
I would use itertools.chain() (along with string.ascii_uppercase as has been suggested by other answers):


Answer URL
