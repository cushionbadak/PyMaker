Natural Text
This question already has an answer here:How to get first element in a list of tuples?                    9 answers                An SQL query gives me a list of tuples, like this:I'd like to have all the first elements of each tuple. Right now I use this:But I think there might be a better syntax to do it. Do you know a better way?
Use a list comprehension:Below is a demonstration:Alternately, you could use unpacking instead of x[0]:Below is a demonstration:Both methods practically do the same thing, so you can choose whichever you like.
If you don't want to use list comprehension by some reasons, you can use map and operator.itemgetter:
The functional way of achieving this is to unzip the list using:
You can use list comprehension:This should make the trick
c.f. http://docs.python.org/3/tutorial/datastructures.html#list-comprehensionsFor a discussion on why to prefer comprehensions over higher-order functions such as map, go to http://www.artima.com/weblogs/viewpost.jsp?thread=98196.


Answer URL
