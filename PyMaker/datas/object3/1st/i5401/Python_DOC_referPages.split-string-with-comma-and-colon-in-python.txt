Natural Text
This question already has an answer here:Split Strings into words with multiple word boundary delimiters                    29 answers                I'm developing a program that has the following input type:So it's a string divided by colon and comma. I have to get each part of <> separated so I was thinking about using the split() function from Python. The thing is, how can I split both colons and commas?
(Assuming that there's a hierarchy and , has a higher priority than : in the syntax of your strings, which is probably the case)



Answer URL
https://docs.python.org/3/library/re.html
