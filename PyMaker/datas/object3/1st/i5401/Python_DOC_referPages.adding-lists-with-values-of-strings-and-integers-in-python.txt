Natural Text
This question already has an answer here:How to merge lists into a list of tuples?                    8 answers                Is it possible to add two lists, with different value types in Python? Or is there an alternative way? For example:I want to combine these so that the output string is either:    finalString = [('a',1),('b',2),('c',3),('d',4)]or   finalString = ['a',1,'b',2,'c',3,'d',4]
Use zip:And itertools.chain or a nested list comprehension for the flattened version:


Answer URL
https://docs.python.org/3/library/itertools.html#itertools.chain
