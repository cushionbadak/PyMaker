Natural Text
This question already has an answer here:Split Strings into words with multiple word boundary delimiters                    29 answers                I’d like to get all the words from a text, including unicode characters, not including hyphens or underscores or any other non-alphanumeric characters.I.e. I want something like this:Withit works for the first example, but not the second, and the other way around with this:
You can use str.isalnum() instead of RegEx in this case:


Answer URL
