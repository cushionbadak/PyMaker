Natural Text
I want to match multiple strings from a file. How do I do this in python ?Objective: match the lines which is having both values of "DC1" & "TIER2" from fileMy code but give matches any string from strings object
Use all for your checking:all(iterable) Return True if all elements of the iterable are true (or  if the iterable is empty). Equivalent to:And if you want to check if at least one of strings is in deltaa use any instead:any(iterable) Return True if any element of the iterable is true. If  the iterable is empty, return False. Equivalent to:


Answer URL
https://docs.python.org/3/library/functions.html?highlight=all#all
https://docs.python.org/3/library/functions.html?highlight=all#any
