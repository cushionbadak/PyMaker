Natural Text
This question already has an answer here:Convert list of strings into list of lists with each element in list of lists as each iterable letter in string. All in one line                    3 answers                How do I lowercase a string in Python?                    6 answers                I have a list m:and I want output as follows:How can this be done?
Use list(zip(*..)) to transpose the nested list, and use list comprehension to create nested list:Output:If want sub-values to be lists:Output:
All you need is a list-comprehension, zip to transpose and map to convert to lower.
Output:


Answer URL
https://docs.python.org/3/library/functions.html#zip
https://docs.python.org/3/library/functions.html#map
