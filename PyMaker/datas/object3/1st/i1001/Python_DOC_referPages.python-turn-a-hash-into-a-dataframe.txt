Natural Text
I have a hash file looks like this and the data is separated line by line:and more ...I want to have a dataframe that look like this: I tried to strip the hash line by line 
Start with an empty DataFrame:Line by line read the hash file into the dataframe:
Here's a solution using ast.literal_eval which doesn't require explicit line-by-line iteration. You should find it considerably more efficient.


Answer URL
https://docs.python.org/3/library/ast.html#ast.literal_eval
