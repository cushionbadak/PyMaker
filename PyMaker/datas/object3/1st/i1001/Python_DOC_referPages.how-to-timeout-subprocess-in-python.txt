Natural Text
I have a simple Python threading example like below:Problem is that sometimes subprocess command is stuck and then the whole process is stopped (next part of the script can't be run).Could you please verify this piece of code and give a hint, how to proceed with force killing thread if time reached limit e.g. 1 minute? 
Thanks, @shmee for your hint. Adding timeout to subprocess.wait method is the simplest solution:


Answer URL
https://docs.python.org/3/library/subprocess.html#subprocess.Popen.wait
