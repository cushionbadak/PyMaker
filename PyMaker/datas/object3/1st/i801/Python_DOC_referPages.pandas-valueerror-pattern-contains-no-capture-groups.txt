Natural Text
When using regular expression, I get:In pandas, I write:How to solve the problem?Thanks.
According to the docs, you need to specify a capture group (i.e., parentheses) for str.extract to, well, extract.Series.str.extract(pat, flags=0, expand=True)  For each subject  string in the Series, extract groups from the first match of regular  expression pat.Each capture group constitutes its own column in the output. Or, if you need a Series,
You need specify column url with () for match groups:
Try this python library, works well for this purpose:Using urllib.parse


Answer URL
https://docs.python.org/3/library/urllib.parse.html
