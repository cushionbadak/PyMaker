Natural Text
Given a list of items [musicians] and a list of dictionaries [instrumets] how would one make the association between them in a pythonic way?So to simplify my real scenario consider the followings:My objective is to replace the N/A is musicians with the instrument in instruments. the code below will do the trick (but the intuition comes from java/c++) and the correct desired result would be:Q: Is there a classic way to do it in python? 
You can use collections.ChainMap to combine your instrument mappings, then use a list comprehension:
If there are no duplicate keys in list of dicts then merge those


Answer URL
https://docs.python.org/3/library/collections.html#collections.ChainMap
