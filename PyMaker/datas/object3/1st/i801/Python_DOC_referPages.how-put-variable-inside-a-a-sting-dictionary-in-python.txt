Natural Text
This question already has an answer here:How do I put a variable inside a string?                    7 answers                I'm trying to interact with the Trakt.tv api, but i'm struggling in create a variable inside a string dictionary.The api OAuth make a call to the server, that provides 2 access codes, one of those code need to be forwarded to a following server call. like this:I have the needed code in variable api_code and would like to put this variable inside the string, like the following example.
IIUC you can use .format() like this:Note that in order to get actual {} characters in the format string, you need to double them as I have in lines 1 and 4 here


Answer URL
https://docs.python.org/3/library/json.html
