Natural Text
This question already has an answer here:What exactly does += do in python?                    11 answers                For example, when adding lists together:
list_sum += x means add the contents of list_sum variable with the contents of variable x and again store the result to list_sum variable.Code explanation:
It's shorthand for list_sum = list_sum + xfor x in list: will loop once through every element in list, assigning the value to a temporary variable xCheck out these duplicates:Duplicate 1and not exactly a duplicate but another example of how it works
It is shortened operation used in any language list_sum += x  =>  list_sum = list_sum + xThere can also be "-=", "*=" and "/=" respectively.


Answer URL
https://docs.python.org/3/genindex-Symbols.html
