Natural Text
In my level editing program, I have the following code, which removes game pieces that have moved out of the playable area:Is there any efficient way I can simplify this to a single if statement? I've considered using list comprehension to generate a list of all valid position tuples, but that seems a little bit bloated.
You could use:This uses chained comparisons to test against two boundaries for both x and y.I'd not create range() objects here; you are creating a new object for each test.Demo:


Answer URL
https://docs.python.org/3/reference/expressions.html#not-in
