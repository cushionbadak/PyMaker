Natural Text
In C-like languages, we can write a loop like that:Is there any syntax in Python to do the same thing?
There is no direct equivalent because assignments are statements in Python, not expressions like in C.Instead, you can do either this:or this:The first solution is less code, but I personally prefer the second because it keeps you from duplicating the a = func(x) line.
No python does not have this because you open yourself up to bugs likeWhere you really meant == and not = 
Python doesn't allow assignments in the place of boolean expressions. The "pythonic" way to do this would be:


Answer URL
https://docs.python.org/3/reference/simple_stmts.html#grammar-token-assignment_stmt
