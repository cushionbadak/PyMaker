Natural Text
When a Python file is open for reading viahow can we find the name of functions from the open python file and print them?
With dir():Without arguments, return the list of names in the current local scope. With an argument, attempt to return a list of valid attributes for that object.
ast to the rescue!Copy/paste-able working demo:Note that we get all the function names (including those in classes, nested inside of other functions, etc).  You could easily skip those by adding def visit_ClassDef(self, node): pass ...


Answer URL
https://docs.python.org/3/library/functions.html#dir
