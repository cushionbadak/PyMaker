Natural Text
Consider the following two modules:andIs the output of the following program well-defined and the same across all Python 3 implementations?
According to the documentation for the builtin function __import__() (which is called behind the scenes for each import statement):... the statement from spam.ham import eggs, sausage as saus results inHere, the spam.ham module is returned from __import__(). From this object, the names to import are retrieved and assigned to their respective names.While this explanation refers to importing specific names, the mechanism is clear: first, the module is imported, and then, the relevant attributes of that module are assigned to names in the local namespace.So, yes, the behaviour is well-defined: any names assigned as a result of from a import * which conflict with names assigned as a result of a subsequent from b import * are overwritten, just as they would be in the case of ordinary reassignment along the lines ofNote that this remains true even if you mess about with the import machinery by replacing builtins.__import__() with a custom-defined import function:


Answer URL
https://docs.python.org/3/library/functions.html#__import__
