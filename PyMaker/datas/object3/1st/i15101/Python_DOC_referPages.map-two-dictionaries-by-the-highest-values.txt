Natural Text
I have two dictionaries with keys of a letter and the values are numbers (occurrences in the message I have)For example ill have:so what i want to do is find a way to replace the letters of the secretWord with the letters in known by the highest, second, and least numbers. So for example secretWord will become 'god'
Order by dictionary keys by values using sorted, then join the keys using str.join:


Answer URL
https://docs.python.org/3/library/functions.html#sorted
https://docs.python.org/3/library/stdtypes.html#str.join
