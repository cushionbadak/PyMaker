Natural Text
I am trying to code a Newton's Method with back stepping code that I wrote in Matlab to Python, but am having some trouble with the Python syntax. Matlab takes about 5 iterations, but my Python code is looping up to the max iteration of 1000 and having a domain error as the back stepping mechanism does not work (tries to calculate a negative log). I have not used Python in a while so I am most likely confusing some syntax of some sort.Here is the Matlab code that works correctly:Here is the Python code:
1/x in df can be 0 in most cases since in python 2.x divison over integers returns integerrange based for is one index too less 


Answer URL
https://docs.python.org/3/library/cmath.html
