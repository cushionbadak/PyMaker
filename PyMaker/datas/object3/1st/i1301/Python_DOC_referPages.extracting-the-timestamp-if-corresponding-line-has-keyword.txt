Natural Text
I want to extract the timestamp in col2 whenever col1 has the keyword.Given excel data is,For example one of the keywords is present in the 7th row, So the corresponding timestamp in col2 should be extracted.The output should be as follows, Thanks in advance.
this should work.either change all to upper or lower case, as it would be case sensitive. be careful, as punctuation might have to be dealt with as welloutput:
Given you can run:any enables you to succinctly test if any of the keywords occur in your strings.Depending on your need, you may want to convert the strings to lowercase before the search or do something similar to the following within the for-loop:


Answer URL
https://docs.python.org/3/library/functions.html#enumerate
