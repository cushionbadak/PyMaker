Natural Text
The following code produces a file with content test\\nstring, but I need the file to contain test\nstring. I can't figure out a way to replace the \\symbol either.How can I make sure that the file contains only \n instead of \\n?
use s = "test\\nstring"I tried with the following code and worked.and the test.txt file contains 
Besides of escaping and raw string, you can encode it (2 or 3) with 'string_escape':
The raw strings may help


Answer URL
https://docs.python.org/3/library/codecs.html#standard-encodings
