Natural Text
I'm trying to, row by row, append any positive numbers to a list until the list has 5 items.  Then, for each row searched, place each list within a meta-list holding all of them.Currently, when I run the code, I get back an error saying:
The isdigit method is supposed to be used with strings, not floats. Try using eitheror


Answer URL
https://docs.python.org/3/library/stdtypes.html
