Natural Text
if heaptify this [(10,'Mike'),(20,'Jack'),(10,'Bob')]And return the min of the heap, will it guaranteed to return (10,'Mike') and not (10,'Bob') ?
no heapq is not stable. as stated in the examples of the documentationThis is similar to sorted(iterable), but unlike sorted(), this  implementation is not stable.


Answer URL
https://docs.python.org/3/library/heapq.html?highlight=heapq#basic-examples
