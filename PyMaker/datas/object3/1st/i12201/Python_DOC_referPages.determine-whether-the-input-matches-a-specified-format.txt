Natural Text
Is there an option in Python 3 to determine whether the input matches a specified format? For example: the format is d-dddd-dddd-d with the d standing for a digit (0-9):
Using a regex would work like this:You can try the code at ideone.com.The regex you suggested in the comment below your question is just the long version of mine. So that should work, too.
Try this:You could also change string to be an input so you can enter input or create a function from this code and run it multiple times.


Answer URL
https://docs.python.org/3/library/re.html
