Natural Text
I want to prompt the user for a list of phrases over and over again until they input END and save all of the user's inputs into a list. How can I do that?My code so far:
One way to do is with a while loop:Result:
You can simply use iter(input, 'END'), which returns a callable_iterator. Then we can use list() to get a real list:About how does it work, if you take a look at help(iter):You can also use a while loop if you think it's more simple and clear anyway:Demo:


Answer URL
https://docs.python.org/3/library/functions.html#iter
