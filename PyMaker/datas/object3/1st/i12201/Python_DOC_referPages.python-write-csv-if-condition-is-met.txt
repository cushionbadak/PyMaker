Natural Text
I have a file named y_and_n.csv, and it looks like this:What's the best way to write a code in Python that saves a new file, y_only.csv, which would look like the text below?
I'd write the following.
Plain Python has a very good module for reading/writing csv files, named csv. The basic structure of a reader is:Therefore,  you shold substitute print(row) for one if structure, selecting only the rows you want to print. Rows are lists, so you should use [] to access columns, as in row[0] for first column.You should only remember that the row is read as a list of strings.
Somewhat similar to GarethPW's answer, but using csv and a generator expression:


Answer URL
https://docs.python.org/3/library/csv.html
