Natural Text
I have a around 20 different modules that all contain a variable with the same name. I am trying to import these variables without having to explicitly type out every import. So instead of doing this:I would like to have some kind of loop that creates these dynamically. Something like:The answer probably lies somewhere in dictionaries but I'm not quite sure how exactly to use them here.
If you have a file a.py:and a file b.py:you can do something like this:importlib.import_module allows to import a module using a string as the name. You can use getattr() to retrieve an attribute again using a string for its name, for example:If you are into one-liners, you can do it in one line:


Answer URL
https://docs.python.org/3/library/importlib.html#importlib.import_module
https://docs.python.org/3/library/functions.html?highlight=getattr#getattr
