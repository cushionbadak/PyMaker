Natural Text
I have two nested lists like this:I want to list the different values from both of the nested lists, into a list like this:Is there any way in Python 3 to do this?This what I have tried:but it returns:
Which prints:
Another approach using set difference:And the same result using reduce:
Later answer, but you can use set(list_1[i]) ^ set(list_2[i]), i.e.:Demo


Answer URL
https://docs.python.org/3/library/functions.html#zip
