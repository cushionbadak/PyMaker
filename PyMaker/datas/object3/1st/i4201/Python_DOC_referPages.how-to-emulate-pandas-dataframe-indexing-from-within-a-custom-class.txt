Natural Text
I want to define a class which does not inherit from pandas.DataFrame but sometimes acts like it. For example, I can do:which gives:and now:gives:How can I now make the following syntax also work?or 
It might not capture all the behavior of a DataFrame, but for starters you could implement __getattr__ or, much more risky, __getattribute__ to pass on requests for attributes to the underlying dataframe:


Answer URL
https://docs.python.org/3/reference/datamodel.html#object.__getattr__
https://docs.python.org/3/reference/datamodel.html#object.__getattribute__
