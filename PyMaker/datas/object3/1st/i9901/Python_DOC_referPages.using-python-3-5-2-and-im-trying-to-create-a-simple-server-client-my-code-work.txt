Natural Text
Here is the server code in Python 2.7:Here is the client code in Python 2.7To reiterate, everything works fine in Python 2.7 but not 3.5.2 which is the version I would like it to work in.
You cannot send a string in python 3. It must be bytes. Your server script will have failed with the error TypeError: a bytes-like object is required, not 'str', not sure why you never saw it.So prefix your string with b to make it a bytes literal and python 3 will be happy.


Answer URL
https://docs.python.org/3/library/socket.html#example
