Natural Text
The two functions below perform an arithmetic operation on two integers and return an integer result.I've heard a lot about eval() being really bad to use because it can cause many problems. But taking a look at the code I've written below, it seems that eval() spares many lines of code, right?Outputs are the same for both functionsIsn't eval best to use in this type of case? If not, is there a better way than eval() to achieve this same type of code-efficiency?Thanks a lot.
You can do this:If you don't want to import another module, you can do this:


Answer URL
https://docs.python.org/3/library/operator.html
