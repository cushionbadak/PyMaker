Natural Text
I have data like this:And I'm trying write it to a file in this pattern:Here is piece of my code:In this case I use csv format, but csv is not necessary needed. I just want to format primary numbers data in column depends on modulo operation (function modulo).My example is modulo 10 case, but I plan extend this into other values. The "a" variable is a simple list of primes.
You can use  itertools.groupby to group the tuples by first element using itemgetter(0) as the key and itertools.zip_longest to transpose the data into columns. out.csv:If you want the columns to align we need to use str.format using the amount of chars that will be in the largest tuple:So for input like:You'll get:


Answer URL
https://docs.python.org/3/library/itertools.html#itertools.groupby
https://docs.python.org/3/library/operator.html#operator.itemgetter
https://docs.python.org/3/library/itertools.html#itertools.zip_longest
https://docs.python.org/3/library/itertools.html
https://docs.python.org/3/library/operator.html
