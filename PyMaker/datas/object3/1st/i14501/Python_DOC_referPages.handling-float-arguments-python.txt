Natural Text
I have a function that takes an optional argument like so:myProgram -n 8I want to add in error handling that will exit the program and print an error message if the argument that the user enters is a float.  How would I test for this if the argument always comes in as a string?
Use str.isdigit():If you want to support negative numbers, strip the sign off first:
It may seem heavy-weight at first, but the argparse module can do exactly what you want. The first example on the page shows an integer-only argument (notice the type=int part):Calling that example with python deleteme.py 3.5 (i.e. 3.5 is the argument that should be an integer) gives the following output:
To parse command line arguments, you should be using argparse.Usage:
You could perhaps do something like this:From the math.modf docs:math.modf(x)¶ Return the fractional and integer parts of x. Both  results carry the sign of x and are floats.Or alternatively (as @dawg mentioned):


Answer URL
https://docs.python.org/3/library/argparse.html
