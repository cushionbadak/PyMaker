Natural Text
I have a pandas dictionary series, that takes the values like I want to create a dictionary out of it based on the key and its occurrence in series, like:
I doubt there is a "built-in" solutiuon for this, so you'd have to manually iterate and count each key in every dictionary.You could also use Counter, however this looks rather "forced" in this situation:
Turn your series in to a series of lists of keys, sum those creating a single list of keys, and use a Counter:Or using generator expressions and flattening:

Maybe it's a bit late but this is another way of doing it by using pandas built-in functions.


Answer URL
https://docs.python.org/3/library/collections.html#collections.Counter
https://docs.python.org/3/library/collections.html#collections.Counter
