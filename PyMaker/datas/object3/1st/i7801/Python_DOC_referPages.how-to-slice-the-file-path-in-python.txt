Natural Text
I am very new to python as well a programming, I like to slice the folder path. For example, if my original path is:I like to get the path like this:please share the methods of doing this in python
If you just want to split on elements, then use this.One drawback of this approach is that it'll fail if you encounter the word elements in your path multiple times. In that case, you can do something like:Assuming you know the depth of the path you need.
you could use the pathlib module:the module has a lot more of very convenient methods for handling paths: your problem could also be solved using parts like this:in python 2.7 you could build your own parts function using os.path:
You can do something like this:str.rsplit() basically returns a list of the words in the string, separated by the delimiter string (starting from right).Please have a look at documentation for more details on this method.


Answer URL
https://docs.python.org/3/library/pathlib.html
https://docs.python.org/3/library/pathlib.html#pathlib.PurePath.parts
