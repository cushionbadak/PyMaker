Natural Text
I have a csv file with non-ascii text (Chinese characters) that is something like this:I'm trying to write this file into a particular format, where the names are group by the category like this:In other words, the output would be like:  I got up to creating a dictionary with the category as key and the names are values but when I try to write this dictionary, the text is not displayed properly.This is my code:  
Update:To sort by column 3:Python is really cool but IMHO there are some things concerning encodings (binary/utf-8/etc) like in os.walk which are not so cool.Since we're in the unix world here is a solution using awk which might just do the job in a oneliner out of the box.Assuming your csv file is called test and the first line in it is the table header:input test:output:
Here we go, Python solution:The output:https://docs.python.org/3.6/library/csv.html?highlight=csvreader#csv.DictReaderhttps://docs.python.org/3/library/collections.html#collections.defaultdict


Answer URL
https://docs.python.org/3/library/collections.html#collections.defaultdict
