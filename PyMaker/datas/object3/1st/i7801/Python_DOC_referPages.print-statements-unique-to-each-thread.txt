Natural Text
I'm wondering if I can print some stuff that is unique to each thread, so like a number in each print statement to tell which thread it is coming form. Here is my thread implementation:add_to_cart method:Would it be possible to print something like:
There are two ways that I can think of, 1:You can simply pass an ID argument to your threads, like so  Or 2, use the thread.get_ident() value if you plan on starting many threads and don't want to ID them all. 


Answer URL
https://docs.python.org/3/library/threading.html#threading.get_ident
