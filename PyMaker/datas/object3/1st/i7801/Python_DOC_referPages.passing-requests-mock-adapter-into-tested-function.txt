Natural Text
I am trying to use requests_mock on a function that I am testing.This returns an error which suggests the mock adapter is not making its way into the tested function.How can I pass the mock adapter into the function?
This won't work because you have to use session.head instead of requests.head.One possibility to do so without messing with the main function's code is to use patch:


Answer URL
https://docs.python.org/3/library/unittest.mock.html#patch
