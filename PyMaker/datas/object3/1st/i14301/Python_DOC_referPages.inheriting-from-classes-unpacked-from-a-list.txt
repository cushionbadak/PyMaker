Natural Text
Say I have a list of class objects in Python (A, B, C) and I want to inherit from all of them when building class D, such as:Unfortunately I get a syntax error when I do this. How else can I accomplish it, other than by writing class D(A, B, C)? (There are more than three classes in my actual scenario)
You can dynamically create classes using type keyword, as in:see 15247075 for more examples.
One way will be to create a decorator that then creates a new class for us using type():


Answer URL
https://docs.python.org/3/library/functions.html#type
