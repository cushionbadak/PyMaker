Natural Text
I have a regular expression to fetch some links in HTML document.What I am intending to match is, if the http part exists match it if not, don't. If up part exists match it if not, don't. If example.com exists match it if not, don't. The same about /uploads/pp2p and the other one, if exists match if not, don't. Finally, if it has one of the following image formats match it if not, don't. I expect to get a list of links likeAnd the elements in the link continue to be filled with different combinations. Anyway, I am getting results as a tuple likeI don't want to get a tuple, I want the match to be represented as a whole. Only a complete link in each list element. How can I avoid getting a tuple as a result of the Regex match?
I am assuming you are getting images off a web page somewhere.Here's a quick way to grab all the image src links using lxml.html:Gives the output:-----[output truncated]-----Then you could do (?:%2F)([\w-]+\.(?:png|jpg)) to get all the image names (of course just an example). I.E. nexus2cee_gamethumb_thumb1.pngUpdated CodeChanged it to search only for androidpolice.com in each link. You can find more on using re module at 6.2. re — Regular expression operations.


Answer URL
https://docs.python.org/3/library/re.html
