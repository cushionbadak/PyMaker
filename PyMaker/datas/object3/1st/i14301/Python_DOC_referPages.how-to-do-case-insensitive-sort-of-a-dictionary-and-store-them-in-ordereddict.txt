Natural Text
I have the following dictionary of set:What I want to do is to perform key sorting using case insensitive and stored them in OrderedDict yielding:I tried this but failed:that gives:
You'll need to provide a key function to sort case insensitively.On Python 3 you'd use the str.casefold() function, on Python 2 sticking to str.lower() is fine:Note the lambda; you are sorting key-value pairs, but set objects are not orderable so you want to return just the key, folded to compare without case.Demo:


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.casefold
