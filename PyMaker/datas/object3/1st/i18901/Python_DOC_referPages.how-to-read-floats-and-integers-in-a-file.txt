Natural Text
I'm trying to write a function that takes a string (as a file), and in that file it contains numbers (in integers and floating points) on separate lines.I then want to print the numbers in the file that are greater than zero. I am pretty new to python, so I am having a little trouble getting the output to work.so far I have:and I keep getting the error Also, in my file i have: and I want it to only print:Could someone please explain to me what I am doing wrong and what I need to do to fix it?
Use the float() function to turn the string from the file into a number:There is no need to read the whole file in memory; process it line by line instead:The with statement makes sure the file object is closed automatically.Output:


Answer URL
