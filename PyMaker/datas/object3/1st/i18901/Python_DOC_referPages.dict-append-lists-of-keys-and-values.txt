Natural Text
Is there an efficient way to append elements in a dict from a list of keys and a list of values, in same order ?For now, i iterate in a for loop on elements in lists, and dofor each pair. Is there a built in function ? I am with 3.3.
Yes, use zip():or use .update() to add items to an existing dictionary:The zip() function pairs up two iterables into a sequence of tuples, which both dict() and dict.update() happily turn into key-value pairs:
You can use dict comprehension:


Answer URL
