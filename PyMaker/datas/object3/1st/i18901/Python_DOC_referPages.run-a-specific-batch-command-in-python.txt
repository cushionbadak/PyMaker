Natural Text
What if I want to include a single batch command that isn't already in a file in python?for instance:Could I put that in a python file somehow?
The "old school" answer was to use os.system. I'm not familiar with Windows but something like that would do the trick:Or (maybe)But now, as noticed by Ashwini Chaudhary, the "recommended" replacement for os.system is subprocess.callIf REN is a Windows shell internal command:If it is an external command:
try this:or
A example use subprocess for execute a command of Linux from Python:
I created a test.py containing this, and it worked....


Answer URL
