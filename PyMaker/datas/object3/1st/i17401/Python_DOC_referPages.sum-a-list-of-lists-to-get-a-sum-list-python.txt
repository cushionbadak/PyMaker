Natural Text
I'm trying to create a define a helper function that sums a list of lists to get a sum list. For example: Would come out as:I found this on another post to work:I need to to translate this into a defined function. This is what I have so far but it's not working.
Just put the list comprehension inside the function. (with return)Why your code does not work?You need a list to hold sums as items.BTW, don't use list as a variable name. It shadows builtin function list.
Since the function is  quite small, you could also use an anonymous definition:


Answer URL
