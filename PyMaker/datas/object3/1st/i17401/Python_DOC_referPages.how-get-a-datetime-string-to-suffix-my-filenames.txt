Natural Text
I need a function to generate datafile names with a suffix wich must be the current date and time.I want for the date Feb, 18 2014 15:02 something like this:But this is that I get: data_2014218152.txtMy code...
You can use time.strftime for this, which handles padding leading zeros e.g. on the month:If you simply turn an integer to a string using str, it will not have the leading zero: str(2) == '2'. You can, however, specify this using the str.format syntax: "{0:02d}".format(2) == '02'.
Looks like you wantThe format string will allow you to control the output of strftime, try something like:"%b-%d-%y"From http://docs.python.org/2/library/datetime.html
Using str.format with datetime.datetime object:


Answer URL
