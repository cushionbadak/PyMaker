Natural Text
I'm doing a simple object marshalling thing that goes between a simple RESTful API to a peewee-managed data store.  However, this pattern keeps on coming up (form being the output of cgi.FieldStorage()):This is, of course, crufty and hard to maintain. What I'd much rather do is have something like this:Obviously it's the SET_FIELD_VALUE that I'm unable to find.  I know how I can do it using eval but I'd rather avoid that, obviously, and I realize that the actual pattern might actually involve some form of field reference or reflection API or the like instead, but I'm unable to find anything about how one would do that either.
Use setattr:BTW, you should use items() or iteritesm(). Otherwise, iterating dictiaonary yields keys only.Example usage of setattr:


Answer URL
