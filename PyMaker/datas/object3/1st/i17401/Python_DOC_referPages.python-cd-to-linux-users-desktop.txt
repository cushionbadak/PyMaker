Natural Text
I need to change directory (CD) to a Linux user's Desktop. Now, the Linux username is a variable, so I tried having the path, but of course it wont work:So I tried the following:But I got the following error:How can I cd to a user's desktop in Linux, using Python?Thanks in advance.
You need to expand ~ as actual path using os.path.expanduser:


Answer URL
