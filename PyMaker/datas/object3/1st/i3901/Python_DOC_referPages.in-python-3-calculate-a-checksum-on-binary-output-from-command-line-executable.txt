Natural Text
I'm using a tool called "exiftool" to extract a binary JPG from a file. I would like to then compute a sha512 sum from that file. What is a good way to do this?My function to extract the binary JPG is as follows:The output is binary. How should I handle this to compute a sha512 sum? I was thinking I could pipe the output to sha512sum in the command line and read the resulting string into Python, but not sure if there is a better way?
Take a look at https://docs.python.org/3/library/hashlib.htmlFor example:So you can just:


Answer URL
https://docs.python.org/3/library/hashlib.html
