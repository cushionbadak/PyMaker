Natural Text
I finally figured how to convert my dictionary with data into a string which gives me the desired output. I was wondering how to make this code more dense. Any suggestions? 
By preprocessing your req input, you could avoid the need for re:So you could limit your code to:But this feels cumbersome to modify the user input, so using argparse might be beneficial. By the look of things, vars and stns could be of varying length so you might benefit from the 'append' action with a specific default case handling:Usage being something likewhich would query the following URL:


Answer URL
https://docs.python.org/3/library/argparse.html
https://docs.python.org/3/library/argparse.html#action
