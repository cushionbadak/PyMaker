Natural Text
Python version 3.5.2So I have file output from mitmproxy called enter code here, and I want to read it using:and returningbut if I change command to ping 127.0.0.1, like:and it works fine!. returning:So what's the solution?
The data is returned in bytes, as indicated by the b in fron of the output.You have to decode it to a string.print ("data:" + line.decode('utf-8') + '\n\n')
I am pretty sure your command isn't being piped as you might expected when calling g.run - the arguments -n -r after mitmdump is somehow being passed to tail, thus running this:Produce that error message you are seeing.Now that you tell us what g.run actually is, looking at the documentation for the function we can see that it takes a shell argument which is passed to Popen, which is the argument to enable the processing of shell commands.  To resolve this issue, you may try the following instead:


Answer URL
https://docs.python.org/3/library/subprocess.html#subprocess.Popen
