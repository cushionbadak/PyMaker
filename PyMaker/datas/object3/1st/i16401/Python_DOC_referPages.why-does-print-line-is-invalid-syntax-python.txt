Natural Text
This question already has an answer here:Syntax error on print with Python 3 [duplicate]                    9 answers                It says print line is invalid syntax. Why? I m using python 3.3.5
In Python 3.x, you have to write print() as follows, because now it's a function:In Python 2.x it was possible to omit the (), but that's no longer the case.


Answer URL
https://docs.python.org/3/library/functions.html#print
