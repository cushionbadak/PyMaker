Natural Text
I have strings containing code that can have whitespace in front of it (tabs & spaces):Since whitespace defines scope in python, this won't compile. So what is a good way to remove whitespace from the start of each line ONLY if it occurs on every line in the string, giving the following result:The code is both generated and compiled in the same python program, so no other tools allowed.
The standard library's textwrap.dedent function does exactly what you want.https://docs.python.org/3/library/textwrap.html#textwrap.dedent
Read first line to string a, then :Now for each line a print :
Try this,


Answer URL
https://docs.python.org/3/library/textwrap.html#textwrap.dedent
