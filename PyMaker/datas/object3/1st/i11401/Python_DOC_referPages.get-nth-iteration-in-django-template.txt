Natural Text
I am trying to get the first or nth iteration only in a Django template.Usually I can iterate through using,I need the first iteration but would also like to know how to get the n-th iteration,{{ pModel.0.post }}` displays nothing and gives no error.I don't want to iterate through every object in pModel. I have tried all combinations i.e. The pModel comes from this view,The following display nothing,In the same template I use the pModel which displays correctly so i know that the pModel is working. The complete template,
Your pModel variable is not a queryset or a list, but a reverse iterator. You cannot access individual elements of an iterator, you can only iterate over the iterator once, exhausting it in the process.To support access of individual elements, you need to convert pModel to a sequence, such as a list:You can then access the index in your template:
You can use the forloop.counter0 template variable. For example, to access the n'th element:You can also use first as a special case:


Answer URL
https://docs.python.org/3/library/functions.html#reversed
https://docs.python.org/3/library/stdtypes.html#sequence-types-list-tuple-range
