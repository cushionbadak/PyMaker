Natural Text
This question already has an answer here:exhausted iterators - what to do about them?                    4 answers                The zip() function in Python 3 [duplicate]                    2 answers                Performing len on list of a zip object clears zip [duplicate]                    2 answers                It seems like we can cast the zip object only once. After z = zip(t1, t2) only the first casting either list(z) or dict(z) works and the other one does not work. Why is it so ?
Another option is to use the itertools.tee() function to create a second version of your generator:


Answer URL
https://docs.python.org/3/library/functions.html#zip
