Natural Text
Suppose there is a list ['Hello','@sdfss','sdfh787s','World'].How to delete all the words which contain non-alphabetic in this list?Using Regex in python.The result should be 
You don't really need regular expressions, just isalpha():Output:
For that you really don't need regex.  You can perform faster than re. But still if you want to do with re following is solution. import res = ['Hello','@sdfss','sdfh787s','World'] # copied from above solution print [i for i in s if re.match(r'[A-Za-z]+$', i) ]


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.isalpha
