Natural Text
trying to wrap struct the reference it's definition as belowfoo.chow to model that, for examplefoo.pyof course python doesn't interpret that, I could use c_void_p instead of pointer(Foo), and cast it's value as followbut, is there a way to model that struct in a python class?
From [Python]: Incomplete Types:... . In ctypes, we can define the cell class and and set the _fields_ attribute later, after the class statement.If we apply that to the current problem, the code would look smth like:


Answer URL
https://docs.python.org/3/library/ctypes.html#incomplete-types
https://docs.python.org/3/library/ctypes.html#module-ctypes
