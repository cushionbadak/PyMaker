Natural Text
I have the following code:When I run it, I get:I want to get:I have tried doing:And:but no luck.What is curious is that when I run it in the shell, it works l, and also that it works in python 3.1 but not 3.4.
Remove the newline on end:  Or do it like so:What stumps though is, that the input() normally removes the return that closes its input. I wonder why you get one ... for me name = f'{input("Filename?")}.txt' works and name comes out as 'tata.txt' if I input 'tata' and hit return.


Answer URL
https://docs.python.org/3/library/functions.html#exec
