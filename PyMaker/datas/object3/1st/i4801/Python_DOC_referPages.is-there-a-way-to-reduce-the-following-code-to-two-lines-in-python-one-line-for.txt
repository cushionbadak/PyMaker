Natural Text
The best I could do to get y in one-line python code is to define a 2d array and then flatten itIs there better way to define y?
You can use itertools.chain.from_iterable to flatten the 2d list.Now x looks likeand y (when iterated or converted to list) looks likeThe chain function is really just undoing the reshape. So why not leave it out? 


Answer URL
https://docs.python.org/3/library/itertools.html#itertools.chain.from_iterable
