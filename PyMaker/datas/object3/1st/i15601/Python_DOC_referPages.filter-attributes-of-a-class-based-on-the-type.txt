Natural Text
I would like to filter the attributes of an object of a class based on their types.The answer will be something around inspect, “list comprehensions”, type(), __dict__ and dict() but I don't get it working.And I would like to print only the attributes of the type A in the class TOutput should be something like:
You can use vars(self) to get a dictionary of the local attributes, then just test the values with isinstance():vars() essentially returns self.__dict__ here but is cleaner.Turning this into a list comprehension for one-liner appeal:


Answer URL
https://docs.python.org/3/library/functions.html#vars
