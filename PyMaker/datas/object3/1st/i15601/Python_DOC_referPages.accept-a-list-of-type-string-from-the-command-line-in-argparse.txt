Natural Text
I've run in to another problem using argparse. I'm trying to accept and store a list of string arguments from the command line and accept them without error. At some point I had a solution which required me to to do something similar to the following: What I would like to do is accept arguments simultaneously without having to specify -d after every input. Example:And have them stored as a list of type string rather than individual characters.
Use nargs='+':For example,yields


Answer URL
https://docs.python.org/3/library/argparse.html#nargs
