Natural Text
I do this type of thing a lot:Is there some neat, small, fairly sensible way to "unpack" the arguments passed to a class method into attributes of the class (while keeping the default values specified explicitly)? Like, I'm thinking maybe locals() could be used somehow around the first line in the method but it's not obvious to me.So we could end up with something a bit like this:And it could be used like this:
Here's a bit of a hacky method. Build a defaults dictionary containing the default values for your allowed parameters. Then update self.__dict__ with **kwargs, after some error checking on the keys:Now you can do:But if you did:


Answer URL
https://docs.python.org/3/library/dataclasses.html
