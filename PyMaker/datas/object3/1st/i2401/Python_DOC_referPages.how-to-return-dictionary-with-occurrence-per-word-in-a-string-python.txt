Natural Text
This question is an exact duplicate of:python dictionary counting how many times a word occurs and linking to the word in a dictionary                    3 answers                How can I make a function (given a string sentece) that returns a dictionary with each word as key and the number of occurrence as the value? (preferably without using functions like .count and .counter, basically as few shortcuts as possible.)What I have so far doesn't seem to work and gives a key error, I have a small clue of why it doesn't work, but I'm not sure what to do to fix it. This is what I have for now:
You have been mixing up variables mySentence and myDict at one point.You can also not use local variables outside of their scope. The following will work:


Answer URL
https://docs.python.org/3/library/collections.html#collections.Counter
