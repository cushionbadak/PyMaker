Natural Text
As per my understanding (but actually didn't test) calculating len of string (for example) with len() func takes less time than simply calculating number of chars in the same string cause len() somehow optimized.  Is it the case and how it works in simple words?The follow up practical question probably would be : " If possible is it a good practice in general to use something like len(string) for early false detection in code in terms of O(code) complexity"ThanksUPDATED.Originally I didn't present any code so now just adding it and hope that would be a bit representative if I haven't mess a lot with it.
Returns:If test = "This is a test string."Returns:


Answer URL
https://docs.python.org/3/c-api/object.html
