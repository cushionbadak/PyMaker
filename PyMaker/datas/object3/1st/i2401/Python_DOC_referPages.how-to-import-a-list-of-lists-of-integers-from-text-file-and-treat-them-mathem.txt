Natural Text
I have data in text.txt fileand so onI want to read each line of the text file and treat them as integers e.g. plot these points in 3d space. When I import the file byit takes it as:So I am confused what to use as separator for split() function. Will appreciate some suggestions.Thanks in advance.
You can use the built in function eval() to interpret python lists from files:If you are not 100% sure about the nature of this file or if it would contain data that another user manually generated, as pointed out by @G_M, you should use ast.literal_eval that will only evaluate a python literal:


Answer URL
https://docs.python.org/3/library/functions.html#eval
https://docs.python.org/3/library/ast.html#ast.literal_eval
https://docs.python.org/3/library/ast.html#ast.literal_eval
