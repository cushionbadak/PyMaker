Natural Text
This question already has an answer here:Splitting on first occurrence                    3 answers                In python3 I have e.g. the following stringwhich I want to split in four parts: the first three elements (the numbers) and the remainder as one string. Of course its possible to do it the following way:but maybe there is a more pythonic way to do that?
You can use split with the maxsplit parameter:


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.split
