Natural Text
Is there a way to inspect source code (or compiled code object) and tell what modules are being imported in Python 3.3?For instance, in:I'd like to be able to tell that modules foo and bar were being imported.  I'm hoping to avoid regex or pattern matching.
You can use the ast module to analyse the code.produces


Answer URL
