Natural Text
I have a text file with the top names of each year male and female and i need to  find the top five for each, i have a little bit so far but not enought, can i get a little help.
Use the csv module to read your format; it is after all comma-separated. Python uses 0-based indexing, so the count is the column at index 2, not 3.A heapq is the most efficient method of tracking the top N values of a given sequence


Answer URL
