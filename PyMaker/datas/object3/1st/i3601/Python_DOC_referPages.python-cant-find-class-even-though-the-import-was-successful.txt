Natural Text
I have some folder: /home/tom/my_module.In my_module directory I have:__init__.py (empty file)my_class.py Now, let's go into terminal terminal:  cd ~ pythonAnd then, in python shell:import my_module succedeed. However, I can't type something like:x= HelloWorldExample()norHelloWorldExample.someMethod()  because python can't find this class.Any ideas?
When doing import my_module you're importing the module and it will be available as my_module. If you want to access something defined in your module you will need to access it on the module ex: my_module.thing_in_my_module.In your example that would translate to:But you could also do:This imports the class directly rather than the module.See https://docs.python.org/3/tutorial/modules.html#packages for more information on packages and imports.


Answer URL
https://docs.python.org/3/tutorial/modules.html#packages
