Natural Text
I m trying to delete all duplicates & original from a nested list based on specific column.ExampleThe key column is the first (abc, def, abc) and based on this I want to remove any item (plus the original) which has the same value with the original.So the new list should contain:I found many similar topics but not for nested lists...Any help please?
Use collections.Counter:Also note that you shouldn't name your list as list as it shadows the built-in list.
You can construct a list of keysand select only those records for which the key occurs exactly once
Using a list comprehension. Demo:Output:
Using collections.defaultdict for an O(n) solution:


Answer URL
https://docs.python.org/3/library/collections.html#collections.Counter
