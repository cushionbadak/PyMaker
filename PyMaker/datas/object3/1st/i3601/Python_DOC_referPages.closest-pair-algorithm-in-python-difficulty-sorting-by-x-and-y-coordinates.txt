Natural Text
I'm trying to define a function that takes an array, consisted of points with x and y coordinates, as input. I need 2 outputs: one array that has the points sorted by their x coordinates and another array that has the points sorted by their y coordinate. I'm doing it in python. Thanks. 
I'm going to assume that your array looks like the following:You can use the sorted method (built-in) to accomplish list sorting:Where the lambda will sort your variable by the first and second indices, respectively
Alternatively, you can use the itemgetter()  function as the key parameter to the sorted() function:Output:


Answer URL
https://docs.python.org/3/library/operator.html#operator.itemgetter
https://docs.python.org/3/howto/sorting.html#key-functions
https://docs.python.org/3/library/functions.html#sorted
https://docs.python.org/3/howto/sorting.html#key-functions
