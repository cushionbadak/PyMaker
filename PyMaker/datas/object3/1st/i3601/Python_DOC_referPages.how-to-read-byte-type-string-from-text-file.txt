Natural Text
I have a txt file. And this file's content is a byte type, like thisHow can I translate it into 
You can use ast.literal_eval() to evaluate the bytes literal:Demo:However, it would be much better if you could just fix the source of the error, which is the code that created that file in the first place. Presumably that code used str() on a bytes value to write the data to a text file, where it should instead have opened a file in binary mode.


Answer URL
https://docs.python.org/3/library/ast.html#ast.literal_eval
