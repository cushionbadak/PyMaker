Natural Text
I need to send commands to shell "MYSHELL>" after it has been initiated.Problem only entering shell is implemented, Other commands (cmnd1; cmnd2;)aren't sent.Result:MYSHELL>
From the docs:Notice, Wait for process to terminate.  I think what you need is pexpect.  It isn't in the standard library, but it'll do what you want.Example:Output:
If you want to send input to your shell then you should pass it as an argument in communicate, like so:test:


Answer URL
https://docs.python.org/3/library/subprocess.html#subprocess.Popen.communicate
