Natural Text
How do I write to a .csv file the following:instead of this:These are the lists that I have:This is the way how I did it. I do not know how use other delimiters.
You can join the elements of ltarg together with a colon:Then continue as you were with the new list:
You can use joinTry:
You need to combine (join) the sub-lists in ltarg into single strings while zipping its contents with the lche list. Contents of the list.csv file afterwards:


Answer URL
https://docs.python.org/3/library/stdtypes.html?highlight=join#str.join
