Natural Text
I am trying to use os.system for opening text file as below.The problem is that gedit opens a file with name ff instead of taking the path value that is stored in ff. Help is needed thanks in advance.
You have ff inside the double-quotes, so it's interpreted as the literal characters ff, not as a reference to the variable named ff.You could build the command like this:But this is dangerous; it can have unexpected results if the value of ff contains spaces, newlines, semicolons, etc. It is recommended to use the subprocess module instead, which lets you pass the arguments in a list instead of having to construct a string representation of the command you're running.
I think you are forgetting the $ sign to escape the env var.try:


Answer URL
https://docs.python.org/3/library/os.html#os.system
https://docs.python.org/3/library/subprocess.html#module-subprocess
