Natural Text
This causes an error:ImportError: No module named 'webbrowser'I want open an URL using webbrowser. How can I do this?
The module may be not installed so , open a shell and start the python shell by typing pythonenter the commmand help('modules')review the list of modules that's returned to see whether webbroswer is includedif not, then install the module


Answer URL
https://docs.python.org/3/library/webbrowser.html
