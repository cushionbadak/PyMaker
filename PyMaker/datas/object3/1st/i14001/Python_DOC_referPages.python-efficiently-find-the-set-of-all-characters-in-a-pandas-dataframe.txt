Natural Text
I want to find the set of all unique characters contained within a pandas DataFrame. One solution that works is given below:However, the solution above takes a long time with large DataFrames. What are more efficient ways of doing this?I am trying to find all unique characters in a pandas DataFrame so I can choose an appropriate delimiter when writing the DataFrame to disk as a csv.
Learned this from Jeff hereThis should be doable using Pandas built-ins: 
I realise this is an old question, but I was looking for the same thing and thought I'd share for anyone else looking.This can be done very quickly with Counter.Use unstack() to get a list of all values in your dataframe. The result  even has a count of each character.Timings:


Answer URL
https://docs.python.org/3/library/collections.html#collections.Counter
