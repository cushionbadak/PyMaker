Natural Text
There is the built-in method unicodedata.name which given a unicode character will return a human readable name, e.g.:Is there anything out there that will provide the inverse? I'm looking for something like:I could easily loop over all the possible values, build up a map, but that seems like it could be inefficient and was hoping something like this already exists. I am using Python 3 so am open to 3-only solutions.
You're looking for unicodedata.lookup:This returns a character, use ord to get integer ordinal:


Answer URL
https://docs.python.org/3/library/unicodedata.html#unicodedata.lookup
