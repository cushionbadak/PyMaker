Natural Text
What I want to do is reference several different ranges from within a list, i.e. I want the 4-6th elements, the 12 - 18th elements, etc. This was my initial attempt:Which I would expect to give do the same thing as:But I got a syntax error. What is the best/easiest way to do this? 
You can add the two lists. 
You can also use itertools module :Note that since islice returns a generator in each iteration it performs better than list slicing in terms of memory use.Also you can use a function for more indices and a general way .Note : if you want to loop over the result you don't need convert the result to list!
You can add the two lists as @Bhargav_Rao stated. More generically, you can also use a list generator syntax:


Answer URL
https://docs.python.org/3/library/itertools.html#module-itertools
