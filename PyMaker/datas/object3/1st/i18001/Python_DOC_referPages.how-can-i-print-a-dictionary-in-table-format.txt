Natural Text
Let's say I have a dictionary like:I want to be able to print it so it looks like:I'm actually working with much larger dictionaries and it would be nice if I can see how they look since they're so hard to read when I just say print(my_dict)
You could use zip() to create columns:Demo:This does assume that the value lists are all of equal length; if not the shortest row will determine the maximum number of rows printed. Use itertools.zip_longest() to print more:Demo:You may want to use sep='\t' to align the columns along tab stops.



Answer URL
