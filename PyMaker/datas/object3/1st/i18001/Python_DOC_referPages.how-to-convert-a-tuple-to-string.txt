Natural Text
How can you convert a tuple into a string in the most simplest way possible? for instance. Lets say I have a list of pairs such as:and I want to convert the first pair to '1hi'
use this:
For the first element only:To do this to all elements:Gives:

Using map:


Answer URL
