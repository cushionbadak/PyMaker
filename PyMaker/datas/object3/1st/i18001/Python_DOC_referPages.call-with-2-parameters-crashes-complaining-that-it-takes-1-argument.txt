Natural Text
Why does the following call to jinja_render() crash?This is the trace:
means that, it accepts only one non-named parameter and all of them should be named parameters . To fix this error, you can call the function like thiswhen we call like this, welcome.html is a non-named parameter, where is test is a named parameter and the name of the parameter is username. There is another way to call this,This way we can unpack the values from the dictionary as named parameters to the function.


Answer URL
