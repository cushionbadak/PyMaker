Natural Text
i have a file 'test.txt' which look likes:then i should somehow summ the values with the same key so the answer is:i managed to write a codebut it gives me:so i can't summ the values together. Thanks in advance
You can't sum the values because they are strings. You will be able to sum them if you convert them to integers:You can shorten your code by using defaultdict:By using defaultdict(int) you don't need to check if the key already exists in the dictionary. If it doesn't exist it will be added and be given value of 0.
You can make directly to a regular dictionary, like this:


Answer URL
https://docs.python.org/3/library/collections.html#collections.defaultdict
