Natural Text
I am trying to store JSON output in a variable. Initially I got the output but it was prepended with character "u" (unicode). Below is the initial output:So then I added encode('utf-8') to my print output statement and after that I got below error:Below is definition and function call.Function call:
dict objects do not have a method encode().  That is a method for str objects.  The text you see is python's "repr" representation of a (unicode) string.  You only have it because you used the wrong approach to convert your dict to a string.You need to convert your dict to JSON.  This is not achieved using print() or repr() or str().  Use the json module for this.Eg:


Answer URL
https://docs.python.org/3/library/json.html#module-json
