Natural Text
I'm trying to create an Enum subclass whose values use their definition order as their natural sort order, like in the example below:Is there a more direct way to get the position of an enum value in its definition order or otherwise a better way to do this?
You could encode the positions as values. Use .name to get the name.On Python 3.6+ you could use enum.auto() to avoid hard-coding the positions.
If this is a pattern you need often, or if the values are important and cannot be replaced by numbers, make a custom Enum you can inherit from:This allows you to keep any other value instead, while still sorting according to definition order.and in use:


Answer URL
https://docs.python.org/3/library/enum.html#orderedenum
