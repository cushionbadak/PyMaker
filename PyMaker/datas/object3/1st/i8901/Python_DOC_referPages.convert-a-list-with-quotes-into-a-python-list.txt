Natural Text
This question already has an answer here:Convert string representation of list to list                    14 answers                I want to convert above into this:I tried following: list_example = list_example.split(',') which resulted to:Which is messy to extract the values from.
You can use ast.literal_eval:You can do it with split & strip too but it's bit more complex:
The simplest (and probably most dangerous) way to do this is to use eval.This code should return a list object full of strings.  Warning:  make sure the user can't input any malicious code inside of list_example!


Answer URL
https://docs.python.org/3/library/ast.html
