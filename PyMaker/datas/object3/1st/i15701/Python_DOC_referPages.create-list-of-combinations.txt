Natural Text
I would like to create a list of numbers with three values and would like to cover every combination from 0 - 3. For example:all the way to 3, 3, 3. Is there a better way to do this than using multiple for loops? Here is the code that I used: 
Usually itertools.product:list(itertools.product(range(4), repeat=3))
You can use the itertools.product() function for that:Demo:
You can use itertools.product:
The itertools.product() is a great solution, but if you should happen to want a list of lists and not tuples, you could use this:or the equivalent list comprehension:


Answer URL
https://docs.python.org/3/library/itertools.html#itertools.product
