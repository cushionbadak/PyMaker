Natural Text
I'm trying to print two strings on the same line using a timer in between. Here's the code:But it seems the program waits for two seconds then prints both strings.
in python 2.7 you could use the print_function from the future packageBut like you said, this waits 2 seconds then prints both strings. In light of Gui Rava's answer you can flush stdout, here is a sample which should get you in the right direction:
The issue is that , by default, the console output is buffered.Since Python 3.3 print() supports the keyword argument flush (see documentation):If you use an prior versionf of python, you can force a flush like this:


Answer URL
