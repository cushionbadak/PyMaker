Natural Text
file1.py:in python3 repl:How to fix test_globals function to actually modify globals()?
The problem with what you did is that from file1 import * didn't import t because it didn't exist. If you repeat the import line, it will work:Another thing that would work:However, if you're only after reloading a module (as you wrote in the comments), consider using importlib.reload.


Answer URL
https://docs.python.org/3/library/importlib.html#importlib.reload
