Natural Text
I have the following statement that I can't seem to find the problem:I keep receiving the following error:I have tried to use %d and %c  instead of %s.  Does numpy have a problem using '_' before using a string?
Python is complaining that you gave four arguments to % but only have three %s in your format string.  I think the $s is meant to be a %s:Note however that in modern Python code, you should be using str.format instead:


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.format
