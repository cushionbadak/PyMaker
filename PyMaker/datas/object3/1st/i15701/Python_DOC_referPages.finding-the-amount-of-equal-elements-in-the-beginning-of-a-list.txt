Natural Text
Given a list in python, I would like to find how many equal elements are in the beginning of the list.Example input:Some magical function (or a one liner) would output:If I do:I just get the number of occurrences of 'a' in x1, not the number of leading values in a row. Would be nice to have a one liner which doesn't need to know the first value.
itertools.groupby can help ...And of course we can turn this into a 1-liner (sans the import):But I wouldn't really recommend it.demo:
You can use takewhile.In a function:
Maybe is better check the first occurrence of something that is not equal to the first value:


Answer URL
https://docs.python.org/3/library/itertools.html#itertools.takewhile
