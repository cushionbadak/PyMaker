Natural Text
I have a list of lists, well a list whose elements can have an attribute which is a listand a listboth lists contain 10 elements,ie to say they will always contain the same number of elements.What I want to do is  ob[i].b = b[i] if ob.has_list == False for all i.If ob has a list assign each element of ob[i].list the corresponding b[i]I have solved it like this(quite C'ish),and was wondering if there is a simpler way.
Since both lists have the same length when a is flattened, you can flatten a, then you can use zip:


Answer URL
https://docs.python.org/3/library/functions.html#zip
