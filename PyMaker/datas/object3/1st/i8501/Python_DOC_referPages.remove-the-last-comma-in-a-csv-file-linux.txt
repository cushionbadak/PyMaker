Natural Text
cdp_data.csvSo i'm making this csv processor. When i compile it on windows there is no "," at the end of the line inside the csv. However when i run this on the server there was an extra "," in the line. They have the same python version though.Windows result:Linux Results:
If you really want to hard-code CSV, use bits = line.strip().split(','). The strip() method removes trailing white spaces and will eliminate the empty string at the end of your list.Otherwise, the recommended approach would be to use the csv module to handle all read, writing and parsing automatically. 


Answer URL
https://docs.python.org/3/library/csv.html
