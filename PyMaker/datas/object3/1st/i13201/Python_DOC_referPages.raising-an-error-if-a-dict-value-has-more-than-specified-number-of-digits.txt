Natural Text
I'm trying to raise an error if a value in a dict has greater than e.g. 6 digits (would be useful to test for a particular number of decimal places too).e.g. for the dict below (and say number digits = 6)(Note if I stick the above example in ipython and do example_dict.values() it pops out [10501.05]  - Is there in built rounding or is this just for display?)What's the best way to test for the number of digits and raise an error on fail?
A simple approach is to convert the value to a string then count its length:


Answer URL
https://docs.python.org/3/library/decimal.html
