Natural Text
This question already has an answer here:Print in one line dynamically                    17 answers                How can I print over the current line in a command line application?                    9 answers                How do I make:so that instead of printing across several lines like:In each loop it will go:Then when it loops again it will overwrite the existing line with:How do I go about that?
In python 3.3, you can do like below
You can use carriage return ("\r") to do the trick:
To print text with a space instead of a newline at the end you can use the end parameter.in python2 this can be achieved with a trailing comma:for Example,


Answer URL
https://docs.python.org/3/howto/curses.html
