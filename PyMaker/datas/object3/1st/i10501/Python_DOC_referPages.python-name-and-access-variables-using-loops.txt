Natural Text
This question already has an answer here:How do I create a variable number of variables?                    16 answers                I am trying to name and access variables using an index in a loop.It's a bit hard to express in code what I want to achieve, but the output is the important thing:I have tried using a list as  suggested in previous posts, however, this is not what I am looking for as in the 'list' solution I cannot really access the variable but only its location in a list. Or, in other words, I cannot call that variable directly, but only its location in the list.

The length of myAr is 3, and this is the output:If you want the output to be EXACTLY like the output you provided in the question, you would change the print line to this: print 'var_{} = {}'.format(i, val) which for this example would output:
This is the output, you can manipulate it as you want.  Output:


Answer URL
https://docs.python.org/3/library/array.html
