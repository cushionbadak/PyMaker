Natural Text
Please consider this snippet:What would be Pythonic way to make this snippet return True even if item in iterable is empty string?Standard restrictions should apply - if item is None or False etc, it should return False as expected.
The only falsy string is the empty string, so if you don't want to test for it, then filter out the strings.
If you want True if there is at least one item that evaluates to True use any().Note that in general any() and all() accept iterable argument and you don't need to loop over them.
This option looks nice to me.
You can use all, for instance:


Answer URL
https://docs.python.org/3/library/functions.html#filter
