Natural Text
This question already has an answer here:Right Justify python                    5 answers                So I have the following lists:Want to format the output as such:The issue that I am having is that I am unable to right align the print statement so that all the values of population (regardless of size) align in the manner I displayed above.Here is the code:Here is the output:The output seems to add a space after the "count" and before the ":", and depending on the number of digits, it is not properly right aligned.Any help would be great!
You can use format in Python to format your strings. Also, count variable is not required in your code because you can get value directly from looping variable.
If you just want to print in the specify format, this is enough, using rjust:OutputHowever if you are planning on doing additional stuff, related to data, I suggest you take a look at pandas.
The most simple solution is probably to use pandas.Result:


Answer URL
https://docs.python.org/3/library/string.html#formatstrings
https://docs.python.org/3/library/stdtypes.html#str.rjust
