Natural Text
I would like to know how could be possible to create a loop for in Python 3 to increment the values of i, j, k ... for a specific application. I need to select different columns but they cannot be select with themselves. Let's suppose that my dataframe has 7 columns. I will put an example bellow.The idea is to create a selection like that:After some replies, I could be able to create the following code:The output is:How could I use those numbers as index of a iloc iterator ?For examples, the numbers generated must substitute the code:Then I will be able to interact among the columns
You can solve this using itertools.combinations and itertools.chain:Or using a list comprehension if you prefer:This produces the following output:


Answer URL
https://docs.python.org/3/library/itertools.html#itertools.combinations
