Natural Text
I have the following string in Python 3:How do I get this to a string with normal characters? That is:I've already tried decoding it using:But when I try to print that value to the console Python gives me the following error:Any help appreciated.SOLUTIONI solved the problem by typing the following in the terminal:After that I was able to print the decoded bytestring to the console.
It seems like you are working with unicode rather than string. See if this helps. You decode using this custom function; first with UTF8 and then with Latin1 then encode to ascii.Result:


Answer URL
https://docs.python.org/3/library/stdtypes.html?highlight=byte%20decode#bytes.decode
