Natural Text
Consider the following Path:How can I extract the exact string 'documents\importantdocuments' from that Path?I know this example looks silly, the real context here is translating a local file to a remote download link.
Use the PurePath.relative_to() method to produce a relative path.You weren't very clear as to how the base path is determined; here are two options:then just use str() on the path.relative_to(secondparent) or path.relative_to(homedir) result.Demo:
You can make use of .parts:Prints:
This works on any OS and every version of Python:This works on python 3:


Answer URL
https://docs.python.org/3/library/pathlib.html#pathlib.PurePath.relative_to
https://docs.python.org/3/library/pathlib.html#pathlib.PurePath.parts
