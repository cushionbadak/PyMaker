Natural Text
I need to add lists in side ways, not in the below. So I think 'append()' is not appropriate. 'concat()' seems to be adding in side ways, but not working for lists. Do I need to use concat() to add list side ways? If so, how can I do that with lists?  I used the append(), so the list was added in the below. 
You can use .extendeg:


Answer URL
https://docs.python.org/3/tutorial/datastructures.html#more-on-lists
