Natural Text
I'm having an issue with my nested loop. I have the list of ipaddress and hostname, looping of address is correct while looping on hostname is not, it always use the index 0 after the break.Output:Thanks
You don't need a nested loop:
You put a break statement at the end of the nhostname-loop, meaning that the nested loop breaks after the first iteration and it goes back to the ipadress loop.Remove the break statement at the end of the nested loop and it should work.
You need something likeOutput:You inner loop is breaking, but in  the second loop of the outer for loop, it is starting all over again. That is why your hostname never changes.I think your code should be -


Answer URL
https://docs.python.org/3/tutorial/datastructures.html#looping-techniques
