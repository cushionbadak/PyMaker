Natural Text
This question already has an answer here:How to format print output or string into fixed width?                    4 answers                What does {:<20} {:>4}  mean in format_string？Beginner
Doc here:format_spec     ::=  [[fill]align][sign][#][0][width][grouping_option][.precision][type]'<': Forces the field to be left-aligned within the available space (this is the default for most objects).'>': Forces the field to be right-aligned within the available space (this is the default for numbers).So {:<20} means left-aligned with width 20. Similarly, {:>4} means right-aligned with width 4.To see how it works:Output:By the way, Python ranked Top 1 in 2017, and stays on top in 2018.


Answer URL
https://docs.python.org/3/library/string.html#format-specification-mini-language
