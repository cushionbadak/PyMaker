Natural Text
Say you have a config.py which inside has so config.settings is a dictionary.in one script foo.py you have:and in another script bar.py you haveHow would you keep the printed value in bar.py up to date with the new value after running foo.py at any time without the obvious reading the file again seeing as the while loop in bar.py needs to be as quick as possible!I currently run these on seperate bash threads i.e:But I could run bar in a thread if that is possible?
I don't know how fast you need this to be. But it would certainly be possible to just reload the config module with importlib.reload. So config.py and foo.py stay the same and your bar.py changes to:UpdateThe example above works for Python >= 3.4, use imp.reload for earlier versions of Python 3 or reload for Python 2.


Answer URL
https://docs.python.org/3/library/importlib.html#importlib.reload
