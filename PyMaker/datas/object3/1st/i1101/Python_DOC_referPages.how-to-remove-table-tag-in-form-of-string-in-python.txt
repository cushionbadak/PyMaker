Natural Text
input is like:In output i want these 2 lines only, want to remove table tag with data in python:Hi Team from the following Server :Please archive the following Project Areas :
Use BeautifulSoup to parse HTMLEx:Output:
You can use HTMLParser as demonstrated below:Output:
If you do not want to use external library, you can use re module to remove tables: printing output give:(and 2 empty lines which are not visible there).Regarding pattern notice that + is followed by ? meaning use non-greedy matching - otherwise it would purge anything between begin of first table and end of last table. re.DOTALL is required, because our substrings contain newlines (\n)


Answer URL
https://docs.python.org/3/library/re.html
