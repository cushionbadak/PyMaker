Natural Text
I'm trying to remove an item from a list , like so:does not work, I also tried this:does anyone know whats the problem?
One possible fix.Passing the todo list (list of dict) as parameter, so it mutates:For your solution to work you need to return:This is because the line list(filter(lambda todoItem:todoItem['ID']!= ID, TodoList)) doesn't mutate the list, it extracts elements based on condition.But it is better to return the list itself if there is no item to delete, instead of returning a string.
A possible solution is to find the index of the item with id and use pop:OutputThe problem with your first attempt is that when you do: TodoList=RemovedList inside of a function, it creates a new local variable inside the function, even if there is a global variable with the same name. In your second attempt, you need to set to global instead of nonlocal:OutputFurtherDocumentation on global


Answer URL
https://docs.python.org/3/tutorial/datastructures.html#more-on-lists
https://docs.python.org/3/reference/simple_stmts.html#the-global-statement
