Natural Text
This question already has an answer here:Python list group and sum with more fields                    4 answers                Python - Group by and sum a list of tuples                    3 answers                Sum numbers by letter in list of tuples                    8 answers                HOW Summarize the second elements in list of tuples,if first elements are the same?for example : in this I would summarize second elements of milk together andsummarize second elements of bread together.and return this list:I could just find same one :(
You can summarize with a collections.defaultdict() of floats:


Answer URL
https://docs.python.org/3/library/collections.html#collections.defaultdict
