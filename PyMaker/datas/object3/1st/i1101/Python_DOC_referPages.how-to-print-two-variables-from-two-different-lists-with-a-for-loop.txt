Natural Text
This question already has an answer here:Printing all the values from multiple lists at the same time                    7 answers                I'm trying to print two variables with a for loop from two different lists with an equal amount of values, but when the lists have more than two values I get this error:Assume both name and surname lists are the same length (eg. name = ['m', 'y', '.', '.'] and surname = ['n', 'a', 'm', 'e'])   My expected result is "my.." and "name" printed next to each other vertically.
Use zip():Result:


Answer URL
https://docs.python.org/3/library/functions.html#zip
