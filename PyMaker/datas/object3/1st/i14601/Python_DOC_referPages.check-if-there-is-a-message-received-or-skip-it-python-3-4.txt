Natural Text
I would like to ask how to skip the receive function.So what I need to do is to check if there is a message coming from the client:If yes, print it and send it.if no, skip the function go to check with the other clients.
You need the polling mechanism. The simplest is select


Answer URL
https://docs.python.org/3/library/select.html#select.select
