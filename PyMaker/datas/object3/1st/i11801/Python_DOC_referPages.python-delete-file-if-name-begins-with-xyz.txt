Natural Text
I have several files in my folder xyz_driver, xyz_tool, xyz_mac, aaa_driver, aaa_tool, aaa_macIf I were to do it works.But how do I delete all files only if the name begins with xyz?
glob() supports wildcard pattern matching:

You can try thisEDIT:As zondo implied, this might give you an error if you use python2.x, the correct syntax is then os.listdir(path) where path is the folder in which you wish to list content.


Answer URL
https://docs.python.org/3/library/glob.html#module-glob
