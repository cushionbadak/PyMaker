Natural Text
I have a list like this:and I want to convert it to a dictionary like this:
Step through the list by twos, zip the results together, giving a list of tuples that will be your key/value pairs and then convert that list of tuples into a dictionary:
You can try something like this:Here's a small time comparison in Python 3.3.2:Reference:Dict Comprehensions (PEP 274)iter()next()
Just some flavor:
I think the shortest way is to use slices:This code really maps all even elements of list to it's odd elements.Update: I've missed previous post, sorry))
Another way to use iter and zip:Or, if you prefer longer programs to shorter ones:


Answer URL
