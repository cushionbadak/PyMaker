Natural Text
This is the error I am getting:
On Python 3, input() always returns a string value. Use the int() type to convert it:A string value and an int are not orderable:Note that int() can throw a ValueError exception, if the input cannot be converted:
age is a string, not an int. To make it an int, use the int() function, so:Note the line:


Answer URL
