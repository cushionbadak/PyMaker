Natural Text
Since Python 3.5 introduced async with the syntax recommended in the docs for aiohttp has changed. Now to get a single url they suggest:How can I modify this to fetch a collection of urls instead of just one url?In the old asyncio examples you would set up a list of tasks such asI tried to combine a list like this with the approach above but failed.
For parallel execution you need an asyncio.TaskI've converted your example to concurrent data fetching from several sources:


Answer URL
https://docs.python.org/3/library/asyncio-task.html#task
