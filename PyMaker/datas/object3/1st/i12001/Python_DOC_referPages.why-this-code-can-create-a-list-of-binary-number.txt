Natural Text
Here is the code:Output: So this code can create a binary number list.My main problem is:I have no idea what this means. Is it a regex or something?
See the python format string documentationThe value before the : specifies the field to format (in this case, 0, so the first param)The value after the colon specifies the format. And it gets fancy here. First the b specifies binary renderingThen you can specify a minimum width, which here is taken from the second parameter:And lastly you can add a 0 before the width to say the padding should be 0s and not spaces:


Answer URL
https://docs.python.org/3/library/string.html#formatstrings
https://docs.python.org/3/library/string.html#format-specification-mini-language
