Natural Text
I'm using the library turtle.It has the onkey command, as specified:However, I need to pass an argument. Was there any way to do this?My code:menuInitial.pymenu.pyAs can be seen, the "MenuInitial" class is heir to the "Menu". I'm learning object orientation.
It looks like you should just do this:Because you're passing itemUp as a bound instance method (self.itemUp) to onkey instead of as an unbound method (menu.Menu.itemUp), self will automatically be passed as the first argument. You can do this because MenuInitial is a child of Menu, so they share the same internal state.If for some reason you did need to pass another argument to itemUp, you could use functools.partial:Then you could have this:


Answer URL
https://docs.python.org/3/library/turtle.html?highlight=logo#turtle.onkey
