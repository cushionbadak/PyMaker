Natural Text
I found an examplehow to move all files recursively, but I would like to keep the same folder structure in the destination folder.How is it possible to modify the above code to keep the same folder structure in the destination folder?
(This answer assumes you're working in Python 2.x)You need to make the sub-directories as you go:Also note that camelCase names are not standard practice in Python. Take a look at PEP8: http://legacy.python.org/dev/peps/pep-0008/
To move all .mp4 files from root_dir directory to dest_dir directory recursively while preserving the directory structure relative to root_dir:It uses pathlib library that is available in stdlib since Python 3.4. To install it on earlier versions: 
Why not just do os.system('your mv command')?


Answer URL
https://docs.python.org/3/library/pathlib.html
