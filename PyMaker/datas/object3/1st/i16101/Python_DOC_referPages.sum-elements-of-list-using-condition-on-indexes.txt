Natural Text
I have a list of length L, with some random values:and another list of the same length, with boolean valuesI want to sum the values of list1 where the corresponding element oflist2 is 1. (in this case 6+8+3)is there an easy way to make it in Python?
Yes, using sum and itertools.compress:Explanation: compress() takes two iterables, and yields elements from the first if the corresponding element of the second is truthy. sum() ... well, you can probably guess.
You can use itertools.compress with sum:
You can also do a conditional comprehension with zip:The if flag checks if the corresponding item is not-zero. If you want an explicit check for 1:


Answer URL
https://docs.python.org/3/library/functions.html#sum
https://docs.python.org/3/library/itertools.html#itertools.compress
