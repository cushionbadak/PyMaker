Natural Text
Example Input:  [5, 9, 2, 4, 1, 3]Expected Output: [9, 2, 1]Function is returning each item as a list instead of a single list with the same items as below.[9][2][1]
The problem is new_list = []. You are re-initializing the list in every iteration. And you have to de-indent the return.A shorter alternative to your code that utilizes a list-comprehension would be this:
Your indentation is wrong. Your return statement is inside the while-loop. It should be outside of it and that means you need to define new_list outside of the loop, too. Try the following:
You can achieve the same result with the following concise solution using a list comprehension:Output:


Answer URL
https://docs.python.org/3/tutorial/datastructures.html#list-comprehensions
