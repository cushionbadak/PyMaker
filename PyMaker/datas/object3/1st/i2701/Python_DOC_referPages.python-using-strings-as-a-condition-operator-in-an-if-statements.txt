Natural Text
In Python is it possible to use a string as an operator in an if statement condition?I would like to have an object with two values and an operator to test them.For Example:Thanks.
Python's operator module provides functions corresponding to all the built-in operators, so you can write:If you want to pass strings instead of the functions, you can use a dict to map them:
You can use eval('{}{}{}'.format(v1,op,v2))
The operator module provides the various comparators as functions, so you could map from string form to a function to perform the comparison:


Answer URL
https://docs.python.org/3/library/operator.html
https://docs.python.org/3/library/operator.html
