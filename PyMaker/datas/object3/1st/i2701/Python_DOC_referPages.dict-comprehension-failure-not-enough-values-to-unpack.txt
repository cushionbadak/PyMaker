Natural Text
I want to create a dict from a list of strings:However, I get d = {int(k):v for k,v in l[0].split(',',1)}     ValueError: not enough values to unpack (expected 2, got 1)I can't understand why, as l[0].split(',',1) returns 2 values, as can be seen from my previous checks (print(len(l[0].split(',',1))) returns 2)My desired output:
It returns two values but the loop expects a list of two values.You have this: [x, y]But the code expects this: [[x, y]]You could do:Note that you'll get all the data and not just one item.You may want to parse the list using ast.literal_eval because currently its a string:


Answer URL
https://docs.python.org/3/library/ast.html#ast.literal_eval
