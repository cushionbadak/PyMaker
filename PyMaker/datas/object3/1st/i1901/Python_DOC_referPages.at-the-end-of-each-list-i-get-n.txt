Natural Text
At the end of each list I get '\n'How can I remove it?Output in console:
Python file.readlines() gives you a list of strings, bu by default it doesn't strip newlines.You either should do it yourself (i.e. with line.strip('\n').split(',')) or use csv module to read file, as it looks like csv format.
use re module to replace \n:


Answer URL
https://docs.python.org/3/library/csv.html
