Natural Text
I've been trying to add a function to my code which allows the user to search for certain keywords within a list of lists. It only works when the input is identical to the element but I want every element that contains the input to show.Example:Part of the list li_to_present is:If the users input is "Birgitta" it won't append to the new list matchesDoes anyone have a nice solution to this problem?
Use the all() function on a generator which yields whether the user input is in each string the sub-lists.E.g.



Answer URL
https://docs.python.org/3/library/functions.html#all
