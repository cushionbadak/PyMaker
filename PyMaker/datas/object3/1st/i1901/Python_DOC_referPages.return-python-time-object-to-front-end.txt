Natural Text
In python, the timedate library contains a datetime (timestamp, adateand atime` class. Is there a time-only (not corresponding to a specific date) concept in javascriptiso 8601so, that I can return the datetime.time object to the front end in a standardized manner? 
Is this what you are looking for ? 
Use .isoformat to return an ISO8601 formatted string from a datetime object.


Answer URL
https://docs.python.org/3/library/datetime.html#datetime.date.isoformat
