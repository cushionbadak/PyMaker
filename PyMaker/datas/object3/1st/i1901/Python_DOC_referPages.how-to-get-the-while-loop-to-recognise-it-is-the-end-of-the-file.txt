Natural Text
I have a text file with data that represents Line 1name, email, phoneLine 2addressLine 3 is an integer that says how many friends are listed.Line 4 ~ nEach line consists of a nameI have been able to get each line withI have a problem when I get to the end of the file.It wants to loop around again as if it does not recognise the end of the filefriends.txt
The readline() method returns an empty string only when EOF is reached per the documentation, so you can add a condition to check if line is empty (not truthy) right after you call readline():Alternatively, you can use the file object as an iterator with a for loop instead:
You can use instead of the while loop.


Answer URL
https://docs.python.org/3/tutorial/inputoutput.html#methods-of-file-objects
