Natural Text
I'd like to split a string = ['O little forests,'] so that the output includes both words and spaces, with the last comma appended to the last words. Here's what the desired output should look like:output = ['O', ' ', 'little', ' ', 'forests,']I was able to split the given string into a list of words with spaces excluded by using line.split(). I welcome your suggestions! 
You may do this work using re like following code.Output:
You could use groupby:Output
You can use re (regex):
You can add a space element between each word like this in a list comprehension:


Answer URL
https://docs.python.org/3/library/re.html
https://docs.python.org/3/library/itertools.html#itertools.groupby
