Natural Text
I will be given a string, and I need to split it every time that it has an "|", "/",  "." or "_"How can I do this fast? I know how to use the command split, but is there any way to give more than 1 split condition to it? For example, if the input given wasI want the output to give:
Use a regex and the regex module:In this case, [/_|.] will split on any of those characters.Or, you can use a list comprehension to insert a single (perhaps multiple character) delimiter and then split on that:With the added example:Method 1:Method 2:
Use groupby:Output


Answer URL
https://docs.python.org/3/library/itertools.html#itertools.groupby
