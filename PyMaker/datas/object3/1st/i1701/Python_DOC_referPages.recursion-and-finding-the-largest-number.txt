Natural Text
So I am trying to use recursion to find the largest number in my program. I have to import one file into the other. Here is the code I have so far:That is the file calling the recursion. This is the main that will import it and establish the list.When I run my code it comes back saying line m=find_largest(numlist[0]) takes 0 positional arguments but 1 was given.Help Please!
There are a few issues with your code, mainly you are missing num_list as a parameter:OutputSecond when checking for the length of a list use len, finally you need to make the recursive call on the rest of the list (num_list[1:]).
You’re missing a parameter in your function:


Answer URL
https://docs.python.org/3/library/functions.html#len
