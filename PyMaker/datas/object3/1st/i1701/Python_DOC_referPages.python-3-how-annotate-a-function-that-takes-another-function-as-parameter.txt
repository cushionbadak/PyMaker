Natural Text
I'm experimenting with type annotations in Python. Most cases are pretty clear, except for those functions that take another function as parameter.Consider the following example:The output simply prints 15.How should I annotate the func parameter in func_b( )? ANSWERThank you @Alex for providing the answer. The typing module offers the Callable annotation (see: python docs). For my example, that gives:As you can see, the Callable annotation gets also annotated itself, following this scheme:
You can use the typing module for Callable annotations.The Callable annotation is supplied a list of argument types and a return type:
Shouldn't it just be function?


Answer URL
https://docs.python.org/3/library/typing.html#callable
https://docs.python.org/3/library/typing.html#callable
