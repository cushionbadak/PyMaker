Natural Text
This question already has an answer here:Convert two lists into a dictionary in Python                    15 answers                I am trying to create a dictionary from two lists, the dictionary should do a one-one mapping of each element of the listA to the corresponding element in the listB at the same index ,I have the current output and expected output below, can anyone suggest how to fix this?Current output:-Expected output:-
You overwrite your dictionary values every time by doing that double iteration in your loop. Use zip instead:If you insist on doing it through a loop, use enumerate to get the index of each destination_milestones:


Answer URL
https://docs.python.org/3/library/collections.html#collections.OrderedDict
