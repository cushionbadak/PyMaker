Natural Text
I am downloading files over FTP using the following Python script. What I wanted is to see the details of the progress while downloading. For that I used ProgressBar but it isn't showing anything.Here's my code:Please help in understanding what's actually wrong here. Thanks :)
You never update the ProgressBar. What you need to do is to:Implement a function (or a class method) that you will pass to FTP.retrbinary as callback instead of file.write. The function should do file.write and also update the progress bar.You also need to know size of the file/transfer for maxval argument of ProgressBar. For that you can use FTP.size.A trivial implementation is like:And now you get the progress bar you want:


Answer URL
https://docs.python.org/3/library/ftplib.html#ftplib.FTP.retrbinary
https://docs.python.org/3/library/ftplib.html#ftplib.FTP.size
