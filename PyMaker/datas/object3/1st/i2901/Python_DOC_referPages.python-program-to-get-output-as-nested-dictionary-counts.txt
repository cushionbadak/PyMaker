Natural Text
I have the file in the format belowDesired OutputFor each Pid, I am building a dictionary with Pid as key and a nested dictionary as value. This nested dictionary has Lid as key and its frequency as value.This is code I am trying but it does not work, please help
You can use a nested collections.defaultdict() to store the counts, and read the .csv file with with csv.reader():Which gives the following:You could also count with a collections.Counter() in the sub dictionary:Which gives the following:Note: defaultdict() and Counter() are just subclasses of dict, meaning that they can be treated as normal dictionaries. 


Answer URL
https://docs.python.org/3/library/collections.html#collections.defaultdict
https://docs.python.org/3/library/csv.html
https://docs.python.org/3/library/collections.html#collections.Counter
