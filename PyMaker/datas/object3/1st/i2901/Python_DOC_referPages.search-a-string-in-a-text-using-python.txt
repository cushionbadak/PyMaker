Natural Text
I have a listand a text file. Inside the text file looks like the following:I want to search the list inside the text file, and at the end I want to get the results as:1,4 and 2 are the numbers extracted from V1, V4 and V2. Here is the code:Question: When I pick an item from the list and search inside the text, and then it does not exist in the text, I want it to return 'NA'. But I couldn't figure it out to do it correctly. As I said I want the result looks like this at the end:Thanks.
You can do it using regular expressions:Please, don't use list keyword as variable name - it's a keyword for python's list function (I replaced it with items).


Answer URL
https://docs.python.org/3/library/re.html#re.Match.group
