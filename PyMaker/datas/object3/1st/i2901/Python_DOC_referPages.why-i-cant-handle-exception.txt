Natural Text
I set proxy usingSometimes my proxy drops down (throwing ProxyError) and in this case I need change variable "proxyError". I use this code:And I cant handle ProxyError because python says what "ProxyError" is uknown. I try this:This dont work too. That I need to catch?
ProxyError is nothing but a class in python and the namespace rule apply to it too, so you need to either import ProxyError or use it where it is located.In requests library exceptions for the requests library are created at requests.exceptionsso if you want to handle exception as below:you need to import exception class as below:otherwise you can handle it as below:


Answer URL
https://docs.python.org/3/library/os.html#os.environ
