Natural Text
I'm trying to read a file, look for a specific word and if a line contains that word, remove the line and send the remaining lines to a new file.Here's what I have but it is only finding one of the lines not all of them;
The problem is that when you do with open('fixed.txt', 'w') as f: you basically overwrite the entire content of the file with that one next line. Either open the file in append mode a ...... or (probably better) open the file in w mode, but just once at the beginning:


Answer URL
https://docs.python.org/3/library/functions.html#open
