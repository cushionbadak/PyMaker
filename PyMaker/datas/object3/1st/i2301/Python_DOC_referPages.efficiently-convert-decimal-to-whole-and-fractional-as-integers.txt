Natural Text
What is the best way to convert Decimal to a pair of integers to get whole and fractional in python?By saying best - I mean efficient, exact and using underlaying structure of the data type.e.g.:I believe there is more efficient way than to just split string representation :)I also think that Decimal is stored in memory as a pair of integers, the question is how to get them? :)
Use divmod
Try this using split:Output:Better math.modf:
If you need it done by using the properties of the Decimal class you can use this function for some_func():This gives the output:
Decimal can be created with tuple (sign, digits, exponent) where a sign (0 for positive or 1 for negative), a tuple of digits, and an integer exponent. decimal.as_tuple() return this representation. using those options we can write the following:


Answer URL
https://docs.python.org/3/library/functions.html#divmod
