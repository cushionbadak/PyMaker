Natural Text
Say I haveThen, I want to create an abstract subclass B of A, which itself is concrete. Should I use multi-inheritance for this purpose? If so, should I import ABC first, as in, or should I import it last, as in
Yes, multiple inheritance is one way to do this. The order of the parent classes doesn't matter, since ABC contains no methods or attributes. The sole "feature" of the ABC class is that its metaclass is ABCMeta, so class B(ABC, A): and class B(A, ABC): are equivalent.Another option would be to directly set B's metaclass to ABCMeta like so:


Answer URL
https://docs.python.org/3/library/abc.html#abc.ABC
https://docs.python.org/3/library/numbers.html#numbers.ABCMeta
