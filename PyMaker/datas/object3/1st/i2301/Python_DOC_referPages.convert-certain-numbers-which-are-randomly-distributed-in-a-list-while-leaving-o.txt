Natural Text
I have list z, been trying to convert all the 1s to 0s only if they were preceded or followed by 0. The 2s and 4s act as breaks in the sequence, and 1s not attached to 0 should remain 1s. The list y is what I would like to have.I can only get all the 1s to be 0s including the ones not attached to 0. Thanks.
One way to do it, using itertools.groupby. The idea is, if there is any zero in a sequence of 0 and 1, all will turn to 0, and the other values are left unchanged:


Answer URL
https://docs.python.org/3/library/itertools.html#itertools.groupby
