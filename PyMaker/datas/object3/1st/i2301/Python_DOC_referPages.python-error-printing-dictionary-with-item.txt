Natural Text
I'm starting with dictionaries and while trying to print a dictionary with .items(), I got this:Here is the code I used:Any ideas?
I asume you are using python 3.x (because of the print() function you use).The dict.items() method returns a view, not a list directly in this version of python.To get a list you need to iterate over the view, maybe converting it to a list (using list()) or tuple (using tuple()). Here is an example:
I found my error to be solved when reinstalling the path for Python, which had 3.7 installed, while my interpreter used 3.0. The problem was not of the writting itself, but of the system's path.


Answer URL
https://docs.python.org/3/library/stdtypes.html?highlight=dict#dict.items
