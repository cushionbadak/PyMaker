Natural Text
I'm using Python and SQLite to manipulate a database. I have an SQLite Table Movies that looks like this:I would like to split the comma delimited values in Country and insert them into another table Countries so that Countries table looks like this How do I do split the values from Country column in Movies table and insert them into Country column in Countries table?According to this post, I can't accomplish this task using pure SQLite. How would I go about it using Python?
Using Python,
You can solve this in pure SQLite using a common table expression.


Answer URL
https://docs.python.org/3/library/sqlite3.html#sqlite3.Cursor.execute
