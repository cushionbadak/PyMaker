Natural Text
We have this assignment and I spend some time on it...Test script wants us to print this:without using modules(we can use module MATH just using this as a hint https://docs.python.org/3/library/string.html#format-specification-mini-languagePlease help. This is what I have now: 
In python3 you can use the format method from string:Notice that ^ means to center, 10 is the total size of the 'cell' and .2f is to print a float with 2 decimal places, change it according to your needs.
First print first 2 lines.Then add this code:Hope this helps!


Answer URL
