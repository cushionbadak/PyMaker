Natural Text
So I have a list like this:How would I create an aggregated version of this list? So I get each date only once, and see how many of each type there are on a given date.Like this:
Assuming that your list is a list of lists with each sublist containing a date string and of of x, y or z, you can first group your list by dates.  Just create a dictionary, or collections.defaultdict, mapping dates to lists of x/z/y.Next, you can create another dictionary, mapping each of those x/y/z lists to a Counter dict:Afterwards, counts is this:


Answer URL
https://docs.python.org/3/library/collections.html#collections.defaultdict
https://docs.python.org/3/library/collections.html#collections.Counter
