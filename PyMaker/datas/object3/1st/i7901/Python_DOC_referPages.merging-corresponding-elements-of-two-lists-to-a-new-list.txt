Natural Text
I have two lists as shown below,car_xy and car_id. I have wrote a sample code to merge the corresponding elements of both lists, but am not getting the desired output.Any suggestions will be great.
Actually you need [list(pair) for pair in zip(car_id, car_xy)]
Assuming that the arrays are same length, you loop through them and combine them like this:
you are searching for the zip function. https://docs.python.org/3/library/functions.html#zipEdit:adding the following makes it a list, in case there is a need for mutation.


Answer URL
https://docs.python.org/3/library/functions.html#zip
