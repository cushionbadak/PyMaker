Natural Text
I have the following codeWhen reading it withIt blocks forever. However, if I'm reading it withIt returns 'hello' without waiting for 10 bytes to be available.Question is: How do I make .read() on os.fdopen()'ed file object behave the same? (aka non-blocking)
There might be a better way, but you can use the fcntl module to set O_NONBLOCK:
This is solved by using io.open() instead of os.fdopen()Note that you must use buffering=0 for this to work:


Answer URL
https://docs.python.org/3/library/io.html#io.BufferedIOBase.read1
