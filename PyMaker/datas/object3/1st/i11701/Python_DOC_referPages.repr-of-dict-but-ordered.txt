Natural Text
I want a simple way to get the repr() like string of a dict sorted by the keys.My solution:... does not work. Here the wrong output:How to implement my_print()?This is not a solution since dicts are not sorted in Python:
Well, you can use JSON.Result:
JSON will work only for simple types. Manually it could be done like that:where od is collections.OrderedDict object.
The standard dictionary in Python 3.7 is going to be ordered, and in CPython 3.6, dict is ordered due to an implementation detail, so the following will work with Python 3.7, and probably with your Python 3.6 too:


Answer URL
https://docs.python.org/3/whatsnew/3.6.html#whatsnew36-compactdict
