Natural Text
I want to writein the simplest way possible. Is as close I can get without a loop (but it doesn't add the exclamations).If you can't do this in python without a loop, I am curious if there is a language that would allow it.  
Use join and a generator expression (avoiding the loop is not possible)If you really want to avoid for keyword 
You won't be able to do this without some sort of loop, as you need to change the number of exclamation points from 1 to infinity, and for that you need a loop.You can do that without writing the loop yourself, though:There's still a loop in there.
You need to enclose your format string like this:


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.join
