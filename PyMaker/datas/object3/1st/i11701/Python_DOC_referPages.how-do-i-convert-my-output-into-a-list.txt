Natural Text
I'm trying to plot the output of my function into a list so I can plot it as a graph. My code so far isThe output I get isbut the output I want isWhat code do I need to add to get this?
You could use a list comprehension or map() to construct a list:You could then return the list from your function or print it.
You could use yield instead of print and do print(list(anninos())) instead of anninos().  You could also construct a list and append to it:Whenever you have a loop that just creates a list of the same things, you can use a list comprehension:
You need use yield or not use funtion. You don't even use the return from a function - why do you use function?:



Answer URL
https://docs.python.org/3/tutorial/datastructures.html#list-comprehensions
https://docs.python.org/3/library/functions.html#map
https://docs.python.org/3/tutorial/datastructures.html#list-comprehensions
