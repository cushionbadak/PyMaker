Natural Text
I want create dictionaries when a new word comes from a files, to store filename and position in that dictionary of that word.E.g:Dictionaries be like:My code for retrieving the words:here 'dict' dictionary is storing the word and occurrences. Any suggestions ?
If you want this structure {word : {file1: count1, file2: count2}}.Update:If you want this structure {word : {file1: [pos1, pos2...], file2: [pos1, pos2...]}}.
I don't believe there is a way to name the actual dictionaries after each word for any input, but this code should provide your desired output (not sorted because it's a dictionary)This should produce:


Answer URL
https://docs.python.org/3/library/collections.html#collections.Counter
