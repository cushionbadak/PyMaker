Natural Text
I want to use lru_cache in my code, however, I get this error:I do have an import functools in my code but that does not helpExample code is here:
If you really just wrote import functools, then that's not enough. You need to either import the lru_cache symbol with from functools import lru_cache, or you need to qualify the name when you attempt to use it, like @functools.lru_cache.There's nothing special about the functools module in this respect. All modules work this way. You've probably noticed when you've imported other modules and used other functions.
You need to import lru_cache before using it:Or alternatively refer to it with full name when importing functools:
The import line is not included in the question, but it should be:Alternatively, the function decorator could be changed to:


Answer URL
