Natural Text
I'm trying to remove whitespace from a list of dictionaries in Python 3.5.I have this:And want this:Other answers have suggested .strip() using a dict comprehension, such as this:But I haven't been able to get it to work for my data structure as I'm a python newbie I'm afraid.
Don't sweat it. Just use a for loop:This solution is pretty specific to your data. For a general solution that works for all string items in sub dictionaries, you might consider iterating over the keys inside a nested loop:
maybe this will do what you want. but strip can't remove space inside string.so change strip_string() like this


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.strip
https://docs.python.org/3/library/stdtypes.html#str.replace
