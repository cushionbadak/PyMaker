Natural Text
This question already has an answer here:Why can't Python's raw string literals end with a single backslash?                    12 answers                This is one of my lines in a script I'm trying to make in Python:As you can see, I'm trying to concatenate a file destination together with folder names stored in a variable and a file name stored in another variable.When debugging, I'm getting an error with the syntax:I'm assuming it's the backslash, but I've escaped it so I'm not too sure what to do. Other than the  backslash, I have no idea what this error is pointing at.Whole code:
A string can't end with a backslash which essentially escapes the closing quote, even if using raw strings:Simply use os.path.join:


Answer URL
https://docs.python.org/3/library/os.path.html#os.path.join
https://docs.python.org/3/library/pathlib.html
