Natural Text
Is there a way to overload the built-in function sum() so that it works on a user defined iterable? Assume the elements in the iterable can be added using +. For instance strings. Specifically I want to be able to do the followingI can obviously achieve this using a simple join statement on elements of S but I was wondering if Python offers support to overload some of the standard built-ins.
The start argument provides an initial value to add to. But:


Answer URL
https://docs.python.org/3/library/functions.html#sum
