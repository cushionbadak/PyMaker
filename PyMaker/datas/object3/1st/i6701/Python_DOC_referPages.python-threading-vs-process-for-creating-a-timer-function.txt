Natural Text
I would like to run a definition in the background, and pass an argument into it (how long it should run for), but the following code isn't working:If I use process, I replace the first chunk of code with:However, both return:My question is, which module should I use, and what is the correct way to set it up?
As @roganjosh pointed out you need to pass a list or a tuple (see Thread). This is working example:For the differences between processes and threads see this answers.BTW: It is maybe better to parse the user input outside the thread as shown in the example.


Answer URL
https://docs.python.org/3/library/threading.html#threading.Thread
