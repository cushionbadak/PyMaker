Natural Text
I am keeping track of a list, and each set has a percent support (percentage value indicating frequency). Each support is tied to one set.x:I would like the array sorted, ignoring the support so that it looks like this:sort the sets in x[0] then the entire first column
Sort the tuples, then sort the list:


Answer URL
https://docs.python.org/3/library/functions.html#sorted
