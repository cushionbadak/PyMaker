Natural Text
Function a() and b() can throw an exception. This code does not handle while exception is throwing during the assignment "UnboundLocalError: Local variable 'x' referenced before assignment"Code:My solution is:Is it possible to do it more tricky, elegant? Now I`m forced to use double try except template.
This is probably one way you could do it:Personally, I think catching both exceptions in separate try/except blocks is more elegant, as it shows the reader that a() did not successfully complete and disallows an UnboundLocalError which masks the real issue:Here is some documentation about locals(): https://docs.python.org/3/library/functions.html#locals
You could use the else clause of [Python]: The try statement:


Answer URL
https://docs.python.org/3/library/functions.html#locals
https://docs.python.org/3/reference/compound_stmts.html#the-try-statement
