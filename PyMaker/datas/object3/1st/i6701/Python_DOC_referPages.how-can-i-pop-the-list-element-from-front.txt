Natural Text
I have a list in bellow:I have a requirement is I want pop from front, but it has no the feature seemingly.
The documentation says you can specify an index 
There are two ways to achieve that:You can use the deque in python, which has a popleft() method:It will shows deque([2, 3, 4, 5]) in the output, and its usage is like the list.And you can also use pop(0) to pop from front:
The pop method accepts an argument, which is the index to pop. The default is -1, which indicates the last element. To pop the firs element, you do:Check the docs.


Answer URL
https://docs.python.org/3/library/stdtypes.html#mutable-sequence-types
