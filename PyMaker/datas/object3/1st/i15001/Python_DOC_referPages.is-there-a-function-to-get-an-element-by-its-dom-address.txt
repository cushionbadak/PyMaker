Natural Text
A function like this:returning the grandchild element.
Yeah, it's called XPath:Of course, lxml is a lot better:XPath Support in ElementTreeXPath supportXML Path Language (XPath)
There is no such function in the DOM standard, no. You probably want to use the lxml library instead and use XPath expressions:lxml builds on top of the ElementTree API rather than the (archaic) DOM APIs. The standard library ElementTree API implementation supports limited XPath expression searching too:


Answer URL
https://docs.python.org/3/library/xml.etree.elementtree.html#xpath-support
