Natural Text
if I have a file (file.txt or file.dat) with two columns (let say x and y):I want to sort each  (x,y) with x as key value in ascending order. How to do this exactly in python? 
Python has the built-in function sorted which you can use to sort a list.Output:Edit: If your input is two lists of integers, do this:Output:


Answer URL
https://docs.python.org/3/library/functions.html#sorted
