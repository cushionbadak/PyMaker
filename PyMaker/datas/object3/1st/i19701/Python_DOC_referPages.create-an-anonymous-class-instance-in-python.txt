Natural Text
Sometimes i need to create an anonymous class instance in python, just like c#:but in python i do it in this way:how can do that in pythonic way in single statement?

typewhile this is not precisely a single statement I think creating a wrapper around the magic of the accepted answer makes it by far more readable.  OutputnamedtupleOutput
If you are using Python 3.3 or later, you can use types.SimpleNamespace:
But it seems like you should probably just use a standard dict.
I prefer the dict answer from mwhite, but here's how I've done it in the past using the "magic" of kwargs (pun intended). 


Answer URL
https://docs.python.org/3/library/types.html#types.SimpleNamespace
