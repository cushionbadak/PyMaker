Natural Text
Consider:What is the % operator between the string literal and the tuple called? Other than in the print statement, where else is it seen in Python?
It's known as the "string formatting" or "interpolation" operator, and it's used anywhere you might want to interpolate formatted data into a string.


Answer URL
