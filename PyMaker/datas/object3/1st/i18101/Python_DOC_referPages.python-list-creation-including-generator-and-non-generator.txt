Natural Text
Seems like there should be an easier way to do this:naively i thought the following should work, but get syntax error on the header_line:
How aboutAs mentioned in the comments, for this case you can just use my_dict.keys() or list(my_dict), but the above example is still useful as an example of how to do this when the list comprehension can't be trivially eliminated.
You can use itertools.chain if you have a more generaty generator:


Answer URL
