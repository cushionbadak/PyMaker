Natural Text
This runs ok:This doesn't:How do I modify the print() statement so that it accesses the dict item with integer key 1 ?
The %(name)x format always uses strings to look up the name in the dictionary.Use the newer str.format() method instead:


Answer URL
