Natural Text
I have date in string:and I use (according to https://docs.python.org/2/library/datetime.html#strftime-strptime-behavior):but I get error:How to do it correctly?
python datetime can't parse the GMT part (You might want to specify it manually in your format). You can use dateutil instead:
%z is the +0200, %Z is CEST. Therefore:I also replaced your %m with %d; %m is the month, numerically, so in your case 04 would be parsed as April.
Simpler way to achieve this without taking care of datetime formatting identifiers will be the usage of dateutil.parser(). For example:


Answer URL
https://docs.python.org/3/library/datetime.html#available-types
