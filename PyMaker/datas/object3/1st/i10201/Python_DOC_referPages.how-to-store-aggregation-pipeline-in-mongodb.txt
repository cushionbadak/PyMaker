Natural Text
I am developing a rule engine using MongoDB and Python. I want to store Rules in MongoDB in a Rule database "myrulesdb" and want to use it to run aggregation pipeline on a different collection. Rule saved in MongoDB isPython Code:I am getting correct string when i print pipe.But when i pass this to aggregate() function, it gives me following error:After retrieving pipeline from database, how can i pass it to aggregate() function?
I don't know why you are doing this but the culprit here is the value of the Rule field which is string. If you try a little debuging with the print() function your will see that type(pipe) yields <class 'str'>You can fix this by loading the value using json.loads like this:


Answer URL
https://docs.python.org/3/library/json.html#json.loads
