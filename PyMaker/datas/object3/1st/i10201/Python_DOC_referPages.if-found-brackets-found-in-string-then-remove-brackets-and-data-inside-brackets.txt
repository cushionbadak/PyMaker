Natural Text
I would like to detect brackets in a string, and if found, remove the brackets and all data in the bracketse.g.Developer (12)would becomeDeveloperEdit: Note that the string will be a different length/text each time, and the brackets will not always be present.I can detect the brackets using something likebut how would I remove the (12)?
You can user regex and replace it:
I believe you want something like this
Since it's always at the end and there is no nested brackets:
For nested brackets and multiple pairs in string this solution would work


Answer URL
https://docs.python.org/3/library/re.html
