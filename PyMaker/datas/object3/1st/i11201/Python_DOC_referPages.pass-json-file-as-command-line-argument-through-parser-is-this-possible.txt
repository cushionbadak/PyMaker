Natural Text
I need to overwrite json file parameters to a python dictionary through command line argument parser. Since, json file is located in the current working directory but its name can be dynamic , so i want something like below :-python  python_script  --infile  json_filepython_script:json_file:I tried to add the following lines, but don't know how to load this json data to the python dictionary :-
Read the JSON file with the name given to --infile and update your profileInfo:


Answer URL
https://docs.python.org/3/tutorial/inputoutput.html
