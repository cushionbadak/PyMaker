Natural Text
I have a string and I want to replace the every time after its second occurrence only.   I would like the replace the word the to hello. But except the first one.Output should be:
Try this:prints: change the string in hello sentence and hello save
I would split the string from right with the word you're going to replace use str.rsplit() function, but only split s.count('the') - 1 times.Then, join the output list with hello:
Try the following one liner solution.
I hope this will do the job.

This should work
Try this:
You can split the string into parts:    Or in one line:


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.rsplit
https://docs.python.org/3/library/stdtypes.html#str.count
