Natural Text
Given,I want obtain a list3 like this:  So I want obtain the list1 size and where the words are not the same I want put a 0.  I try with:  
Use list comprehensions to write your code in one-liner,PS: convert list2 into set to speed up.
Basically you just iterate the second list with each element from the first list. If they match you can break out of the second iteration and append the found element to the result, else append "0". Something like this:
One for loop should do the trick:


Answer URL
https://docs.python.org/3/tutorial/datastructures.html#list-comprehensions
