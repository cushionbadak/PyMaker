Natural Text
I am trying to create a 'Group Chat' class that inherits some of its attributes from the main 'Chat' class. I am getting an error at the "super(chat_id, user1).init()" line. I am unable to fix it!Upon instantiating 'GroupMessage', I get an error!            TypeError: super() argument 1 must be type, not int 
You should do instead of super(chat_id, user1).__init__() you should do:orThis last option is not recommended if exist change that your class hierarchy change in the future. I really don't like it for others motive but still worth it a mention.


Answer URL
https://docs.python.org/3/library/functions.html#super
