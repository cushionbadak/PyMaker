Natural Text
How to index a list using a variable?Using the below example, how can a and b be used to return the equivalent of a[1]?
b is a list, not an integer. If b was set to 1 you can just use that to index:If you were looking to store the operation as a variable (not just the index, but that you are indexing in the first place), use functions or helper objects from the operator module.For example, you can store a operator.itemgetter() object:


Answer URL
https://docs.python.org/3/library/operator.html
https://docs.python.org/3/library/operator.html#operator.itemgetter
