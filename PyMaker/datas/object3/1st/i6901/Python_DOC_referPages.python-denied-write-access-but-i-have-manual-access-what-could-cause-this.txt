Natural Text
I have verified that Py is running as the same user. I am running locally and writing to a share, but the home dir (where Py is running from) shouldn't matter. Same error whether destination file (pull.zip) exists or not. Shouldn't have to run elevated for this. Same result in Py 2 or 3.Returns this: (paths have been anonymized)Efective Permissions on destination folder.
\\ is an escape sequence interpreted as a single backslash.You can use four backslashes to represent two of them (but this will fail if the literal contains another escape sequence)or a raw literal
If your "pull.zip" is local ("pull.zip" is in folder "share" which is in folder "domain" which is in the current directory of your python file) than you should remove \\ from start of string loc and double all other slashes. So your code should look like this:


Answer URL
https://docs.python.org/3/reference/lexical_analysis.html?highlight=string%20literals#string-and-bytes-literals
