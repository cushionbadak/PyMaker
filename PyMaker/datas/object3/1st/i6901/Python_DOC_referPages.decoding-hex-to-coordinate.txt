Natural Text
I have a hex payload:The lat and lng is in little endian and is at the following locations:Im not sure how to get the correct results. I thought first I would have to change the hex to little endian? Then convert it to int?I tried this:output:
You first need to convert the hexdecimal representation into a bytes-object:Then you can use struct to decode into integers or whatever:


Answer URL
https://docs.python.org/3/library/stdtypes.html#bytes.fromhex
