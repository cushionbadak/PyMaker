Natural Text
User providing me with search expression, e.g. "^.*/bin/run.sh"Need to scan a file and replace search expression and replace with value also provided by user.search = "^.*/bin/run.sh"replace = "/path/to/bin/new_run.sh"how do I search and replace in python 3?
Using the re module's re.sub method.Where you are looping through line by line.Look at the fileinput and argparse modules for how to wrap this up into an application if that is what you want.


Answer URL
https://docs.python.org/3/library/re.html#re.sub
https://docs.python.org/3/library/fileinput.html
https://docs.python.org/3/library/argparse.html
