Natural Text
Let's say I have a list:How can I sort it into chronological order such that:
By using natsortnatsort provides a function natsorted that helps sort lists “naturally” (“naturally” is rather ill-defined, but in general it means sorting based on meaning and not computer code point)
These are periods. You'll want to generate a PeriodIndex and then call sort_values.
You can use pandas.to_datetime with sorted:Result:
Output:Or like this if you want it back to strings:


Answer URL
https://docs.python.org/3/howto/sorting.html
