Natural Text
I have a YAML document with sequences like thisI need to get all possible combinations of the elements taken from each list one at a time, only one element at every instance from the list and all list must be used.I need to do this is python.Until now, I can print the YAML file using:
A solution using itertools:Output:


Answer URL
https://docs.python.org/3/library/itertools.html
