Natural Text
This question already has an answer here:String to list in Python                    4 answers                What I want to do is convert a string like:To a list like:I tried the list() function but it does not work.
Use the split() method of strings.
Use .split()So after .split() test_list[0]=77  ,  test_list[1]=65  etc..
Use split()You can do the same thing by specifying the separator, like so:


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.split
