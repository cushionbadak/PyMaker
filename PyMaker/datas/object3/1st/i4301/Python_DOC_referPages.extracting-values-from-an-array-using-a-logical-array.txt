Natural Text
I have the following MATLAB code which I would like to replicate using Python.The MATLAB code creates a logical array for when xDiff == 2 and then uses that logical array to extract corresponding values from the tDiff array to create the resulting array, tTacho.MATLAB code:
You can do boolean indexing with NumPy.For example: gives:If you don't want to use NumPy, then you can use list comprehensions with zip:gives:
You can use list indexing also.If this helps.


Answer URL
https://docs.python.org/3/library/functions.html#zip
