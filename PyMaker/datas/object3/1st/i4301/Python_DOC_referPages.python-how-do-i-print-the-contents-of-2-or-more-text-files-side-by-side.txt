Natural Text
This question already has an answer here:How to iterate through two lists in parallel?                    8 answers                Say I have a couple of text files, fruit.txt and veg.txt which look like,I have a couple of for loops that print out the contents of the .txt files, And the output I get is,I want to try and print it side by side like,
You can use the format method in builtin strings, and zip_longest.zip_longest will take care of the situation where you have unequal number of fruits and vegs.NOTE: The above will work in python 3. For python 2, remember to replace:with:
An easier way would be to use the readlines method like so, and the 'end' parameter is the printed value that prints at the end, by default its value is '\n'  


Answer URL
https://docs.python.org/3/library/functions.html#zip
