Natural Text
Here's a bit more context, in python, how could you create this effect?Is there any way to write a code for this effect? I haven't found anything outside of a load of {if} blocks.Thanks!
You can use the getattr built-in:From the docs:getattr(object, name[, default])Return the value of the named attribute of object. name must be a string. If the   string is the name of one of the object’s attributes,  the result is the value of that attribute. For example, getattr(x,  'foobar') is equivalent to x.foobar. If the named attribute does not  exist, default is returned if provided, otherwise AttributeError is  raised.
just an alternative of using getattr:


Answer URL
