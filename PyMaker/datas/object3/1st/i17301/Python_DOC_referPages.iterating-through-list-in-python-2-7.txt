Natural Text
How will I do the following?I want isfor eg i.e 
Use a list comprehension:This gives you [Q[0][0].get(), Q[0][1].get(), ..., Q[-1][-2].get(), Q[-1][-1].get()]The double loop is just like a nested for loop outside of a list comprehension, the above is the equivalent of:The list comprehension follows the same order of loops, nesting is translated to loops being listed from left to right.
Replicating a list can be done with the * operator in Python (i.e. multiplying a list):


Answer URL
