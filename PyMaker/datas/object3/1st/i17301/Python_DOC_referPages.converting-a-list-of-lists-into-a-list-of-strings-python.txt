Natural Text
In Python how do I convert:list01 = [ ['a', 'b', 'c'], ['i', 'j', 'k'], ['x', 'y', 'z'] ]into list02 = [ 'abc', 'ijk', 'xyz'] 
Using map:Or with a list comprehension:Both output:Note that in Python 3, map returns a map object instead of a list. If you do need a list, you can wrap it in list(map(...)), but at that point a list comprehension is more clear.
Use str.join and a list comprehension:

Hi you can use join to implode the elements in a string and then append. If you dont want to use map.


Answer URL
