Natural Text
I want a Python program which have to ask user to give values as arguments.If user runs simply (without argument) the program it have to printIf user gives arguments as Welcome it have to print Welcome.
sys.argv contains a list of runtime arguments, with the filename of script as first argument:$ python script.py foo bar
Output:
The shortest way:This will print Welcome if there's no arguments, and print the first argument if there's arguments. Demo:If you'd like print Hey there when the arguments are Hey there, simply use:Here's more details about sys.argv.
You may test the size of the argument list, for example:Or use a try: block:


Answer URL
https://docs.python.org/3/library/sys.html#sys.argv
