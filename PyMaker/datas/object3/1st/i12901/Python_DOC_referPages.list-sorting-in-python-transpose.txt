Natural Text
I have arbitrary lists, for instance here are three lists:And I want transpose them together in order to get the output like this:As, you can see, I just converted "columns" to "rows".The issue is a solution has to be independent of the lists length.For example:
You can use zip_longestIf None is a potential valid value in the lists, use a sentinel object instead of the default None:


Answer URL
https://docs.python.org/3/library/itertools.html#itertools.zip_longest
https://docs.python.org/3/library/functions.html#object
