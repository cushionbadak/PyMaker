Natural Text
I'm new to Python and I am having a small problem I want to solveI have this dic and I would like to get the maximum key out of himfor example in the dictionary above I would like to get the key 6 as a value
There are many ways, one way of doing it is:Or a human readable format, so you can understand the logic:
For a simple, functional approach:This applies the int function to each key of a using map to convert the keys as strings to integers, then finds the maximum of these using max.
If you want the original key (as as string, not an integer), you could simply use max with int as key argument:


Answer URL
https://docs.python.org/3/library/functions.html#max
