Natural Text
I have a setup.py file like this (not in pwd, not in Python path, a random file somewhere):And from an external (Python) script I'd like to import config["install_requires"]. I'm looking for the most minimalist way of doing this as it's intended to be run from other scripts that might even not be Python.A Python one-liner would be awesome.
Aside from importing python module from an arbitrary path, you also need to avoid execute setup(), one method is filtering through AST:another method is a bit tricky, to nullify setuptools.setup temporarily:Update:In case you also want bypass import of ext_modules:
In the doc : https://docs.python.org/3/library/importlib.html#importing-a-source-file-directlyIn your case:


Answer URL
https://docs.python.org/3/library/importlib.html#importing-a-source-file-directly
