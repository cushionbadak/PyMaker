Natural Text
I want to:Mention the folder in one spot (I keep user-defined variables in one section)Use that to search all files mentioning a search string ({}.sh)How can I do this?This works:But I want to keep the folder assignment separate.This doesn't work:
This works for me.
Here's a quick example of using a f-stringpath is then equal to 'max/test/*{}*.sh'.  Note that we have to escape the curly braces by doubling up on them.  {{ escapes to { and }} to }


Answer URL
https://docs.python.org/3/reference/lexical_analysis.html#f-strings
