Natural Text
I a learning python mock, and having problem to check if a method on a mocked object has been called.The code I want to test is this:My test code is:And when I run it, I get this exception:
assert_called_with does the assertion directly, and returns None. You do not need to use TestCase.assertTrue() here at all.In other words, the following is enough:If this assertion fails (i.e. directory_service.HasSchema() was not called or called with different arguments) then that assert_called_with raises an AssertError exception, which TestCase catches for you and records as a test failure:


Answer URL
https://docs.python.org/3/library/exceptions.html#AssertionError
