Natural Text
I am wanting to take a list of various variable names, and assign all of them as instance variables to a class.Furthermore I would also like to assign attributes to these instance variables from a database.For Example: I have a dataframe with headers, ('col1', 'col2', 'col3', 'col4'). each row should be a class instance, and each column should be an instance variable of that class. Then the values in each row, should be assigned to each instance variables as attributes for every class instance.how can i accomplish this?here is a list of variables:Here is an example dataframe:
This is a natural fit for namedtuples.Output:
You can use df.to_dict('records') to generate a list of dictionaries,Then you can do something like this to create your list,


Answer URL
https://docs.python.org/3/library/collections.html#collections.namedtuple
