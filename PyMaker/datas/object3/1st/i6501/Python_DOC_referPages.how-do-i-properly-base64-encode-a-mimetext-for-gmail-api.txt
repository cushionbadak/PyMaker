Natural Text
From the Gmail API's developer guide:The following code sample demonstrates creating a MIME message, encoding to a base64url string, and assigning it to the raw field of the Message resource:But if I do something like I get TypeError: a bytes-like object is required, not 'str'If instead I do:I get the following traceback about some binary not being json serializable:
Try:base64.urlsafe_b64encode returns a bytes object (see docs) so you need to convert the output to a string before serialising it as JSON.


Answer URL
https://docs.python.org/3/library/base64.html#base64.urlsafe_b64encode
