Natural Text
I have the following tuple:that I'd need to convert to a dict as follows:I'm trying with but it's not resolving the nesting of the tuple I have. Any suggestions?Another SO question appears to be similar, but is not: its main issue is re transposing the dict elements, while this question focuses on how to join 2 arrays within a tuple into a dict.
You can use zip (to create key-value pairs) and dict (to convert the pairs to a dictionary):


Answer URL
https://docs.python.org/3/library/functions#zip
https://docs.python.org/3/library/functions.html#dict
