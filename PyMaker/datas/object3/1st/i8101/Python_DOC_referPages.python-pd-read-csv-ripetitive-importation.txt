Natural Text
I need to import with a repetitive importation asat first time I import but I need to import # times as N1, N2, ..., N121, N122as the second time:and so on.Other question, as you can see into the code there is a repetitive name (as 'N122')there is a simple method to substitute in the code 'N122' to 'N' asso I keep the same code, like before, for each node and with other code I change only one time the node name (N122, N121,...).There is a simple method to do this?
That can be done using a formatted string and a loop like:


Answer URL
https://docs.python.org/3/library/string.html
