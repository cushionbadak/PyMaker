Natural Text
Say I had a sentence "The cat ate the mouse." I want to split the sentence with size = 2.So the result array becomes: If my size was 3, it should become: My method I have right now uses tons of for loops and I'm not sure if there is a best way.
Using list slice, you can get sub-list.Use str.join to convert the list to a string joined by delimiter:List comprehension provides a conside way to create words list:
you can use nltk library to do all the jobwhat gives us:('The', 'cat', 'ate')('cat', 'ate', 'the')('ate', 'the', 'mouse')('the', 'mouse', '.')


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.join
https://docs.python.org/3/tutorial/datastructures.html#list-comprehensions
