Natural Text
Since python lists aren't implemented as arrays, if for some large N we have:for   0 <= X <= N ,which is faster?Thanks
You can run a timeit test like this:The result is as followed:With list indexing being a slightly faster. Try it online!The result will remain parallel with increase of N, here's 1000 N:With result being:List index is still faster. Try it online!


Answer URL
https://docs.python.org/3/library/timeit.html
