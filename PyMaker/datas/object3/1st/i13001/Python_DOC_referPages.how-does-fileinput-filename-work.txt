Natural Text
I am making a script which takes a file from terminal as an input. In order to do that I call myScript like that:The script is also pretty simple:But the output is:What am I doing wrong? How does the fileinput.filename() works? Why does it output <stdin>?
fileinput.input() iterates over the lines of all files listed in sys.argv[1:], defaulting to sys.stdin if the list is empty (source).<fileInput.txt sends the contents of fileInput.txt to stdin. The filename won't appear in sys.argv, because input redirection is handled by your shell.If the filename contains the < character (or any other "special" characters), you'd quote it:


Answer URL
https://docs.python.org/3/library/fileinput.html
