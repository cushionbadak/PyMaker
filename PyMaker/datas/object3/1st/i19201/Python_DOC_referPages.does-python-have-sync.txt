Natural Text
The sync man page says:sync() causes all buffered modifications to file metadata and data to  be written to the underlying file systems.Does Python have a call to do this?P.S. Not fsync, I see that.
Python 3.3 has os.sync, see the docs. The source confirms it is the same thing.For Python 2 you might have to make an external call to the system.
As said, Python 3.3 has the call - on Python 2.x, since it is a simplesystem call, requiring no data to be passed back and forth, you can use ctypes to make the call:
Combining the two answers, I use the following at the top of my module:


Answer URL
