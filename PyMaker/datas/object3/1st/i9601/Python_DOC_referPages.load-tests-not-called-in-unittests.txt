Natural Text
I am trying to use the load_tests protocol to parameterize some test cases. Here is my MCVE:When I run this I get the following output:It appears that my load_tests() is not even being called. What am I doing wrong?A similar MCVE runs to just fine:Output 2:Apparently I am missing something because these look similar to me.
The problem appears to be that the unittest framework calls __init__() on each subclass of unittest.TestCase for each method which starts with "test". It passes a single parameter which is a string containing the test method's name. This all occurs before calling load_tests(). One solution is to modify __init__() to allow this mechanism to run:


Answer URL
https://docs.python.org/3/library/unittest.html#load-tests-protocol
