Natural Text
The output I got is [('Hello', 0), ('World', 1), ('Hello', 2), ('World', 3)]But I want
You can use a dictionary:
It seems like collections.defaultdict is a perfect fit for this case (see also the answer of @mhawke) :Then convert this to a plain dictionary again:I recently created a package containing a function that could be used as alternative iteration_utilities.groupedby:
Use a collections.defaultdict of lists:
This can also be done using a fairly simple dictionary comprehension:This works by making keys using each unique element in the list s(which set(s) does), and getting the index of each of the unique elements(which the list comprehension does).


Answer URL
https://docs.python.org/3/library/collections.html?highlight=defaultdict#collections.defaultdict
