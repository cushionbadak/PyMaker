Natural Text
Is it possible to mmap just part of a file using Python/mmap?I do it like this:Should mm size be 40?Then when I read data it passes the boundary without errorWhat's the point of length then?
According to the documentation, no.  It's the size of the whole file.


Answer URL
https://docs.python.org/3/library/mmap.html#module-mmap
