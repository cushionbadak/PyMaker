Natural Text
I want to obscure a password in a URL for logging purposes. I was hoping to use urlparse, by parsing, replacing password with dummy password, and unparsing, but this is giving me:So the alternative seems to be this, which seems overkill.Is there an easier way of replacing the password, using the standard library?
urlparse returns a (subclass of) named tuple. Use the namedtuple._replace() method to produce a new copy, and use geturl() on that to 'unparse'.The password is part of the netloc attribute, which can be parsed further:Demo:


Answer URL
https://docs.python.org/3/library/urllib.parse.html
https://docs.python.org/3/library/collections.html#collections.somenamedtuple._replace
