Natural Text
Element3 of each list is a list (in brackets). I want element3 to be a string 'store1.txt' not ['store1.txt']. 
Change the 6th line to:The list comprehension automatically generates a list, so you need to take the first value of that list if you just want the string.
matching is a list, you can use pop() to get your element like this:


Answer URL
https://docs.python.org/3/library/stdtypes.html#set.pop
