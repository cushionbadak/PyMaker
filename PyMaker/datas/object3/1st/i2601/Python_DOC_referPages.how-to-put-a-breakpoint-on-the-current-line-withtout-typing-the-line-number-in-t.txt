Natural Text
I'm used to GDB, where b does that.But in pdb, b just list breakpoints.I can do b 123, but lazy to type 123.Maybe a magic argument like b .?I know PyCharm and __import__('pdb').set_trace(), just checking if there is an usable CLI alternative for those quick debugs.
if you accept adding a new pdb command, it is trivial:use breakcurrent or bc:if you want to put those code into .pdbrc to make it available automatically, need little tweak:


Answer URL
https://docs.python.org/3/library/pdb.html#debugger-commands
