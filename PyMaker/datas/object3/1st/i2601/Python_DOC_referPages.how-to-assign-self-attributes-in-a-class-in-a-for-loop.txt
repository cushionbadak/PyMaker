Natural Text
I have a class in which I read a json into a dictionary and I want to assign in a loop self.variable based on the key-value pairs in the dictionary as in:How can I pass the key value of the dictionary as the self.name_of_the_variable?
You can use setattr:
You can use setattr(self, key, value).However, I don't know your use case, but in most cases it's probably better to just store the actual dict as attribute.
You can update self.__dict__:


Answer URL
https://docs.python.org/3/library/functions.html#setattr
