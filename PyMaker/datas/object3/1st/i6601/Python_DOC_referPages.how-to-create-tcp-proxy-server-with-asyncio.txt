Natural Text
I found these example with TCP client and server on asyncio: tcp server example. But how to connect them to get TCP proxy server which will be receive data and send it to other adress?
You can combine both the TCP client and server examples from the user documentation. You then need to connect the streams together using this kind of helper:Here's a possible client handler:And the server code:


Answer URL
https://docs.python.org/3/library/asyncio-protocol.html#tcp-echo-server-protocol
