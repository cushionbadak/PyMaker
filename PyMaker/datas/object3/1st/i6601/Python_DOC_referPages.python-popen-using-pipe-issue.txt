Natural Text
I am trying to replicate this command using python and Popen:When running this from the command line as it is above, I get the expected:I want to achieve the same from a python script, so I coded it like this:But, I  must be doing this incorrectly as the "result" variable contains the radclient usage info, as if it is called incorrectly:Anybody any idea where my mistake lies?Thanks!
Besides @Rawing catch of the args typo, you can make it much simpler with a single Popen process. Try this:Using communicate to handle all the I/O prevents possible deadlocks that are possible when explicitly writing to stdin when you also need to read from stdout/stderr.


Answer URL
https://docs.python.org/3/library/subprocess.html#subprocess.Popen.communicate
