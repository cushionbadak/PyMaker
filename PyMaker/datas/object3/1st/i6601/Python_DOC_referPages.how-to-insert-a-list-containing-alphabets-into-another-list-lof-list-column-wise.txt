Natural Text
I go two lists of alphabetsand another listand two lists of lists of numbers:I want to get an output of:Basically I need to reference each number with an alphabet.Tuples are possible too but I prefer list methods because I'm more familiar with them.
You can try this:Output:Edit:if you have nested lists, you can try this:Output:
so I did it only for the first line you just need to change some small stuff to do the second line but here it is:
Basically we want to combine two lists for which we can use map function. The command would be the following: So the output would be:


Answer URL
https://docs.python.org/3/library/functions.html#zip
