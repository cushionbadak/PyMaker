Natural Text
Originally I have python 2.7 code, which can read a file and deliver it to perl code:Now I would like to convert it to Python3 code, but in Python3 proc.communicate reads bytes-like object rather than 'str'. So I use stdout, _ = proc.communicate(open(filename).read().encode()), but it turns out that all the line feeds in will file be converted to \n, for example, in the file I have But unfortunately the encode will make it:How could I correct this error? I want to send the byte format stuff to pearl without those \n
Use repr: It returns a string containing a printable representation of an object. output:


Answer URL
https://docs.python.org/3/library/functions.html#open
