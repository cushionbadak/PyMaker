Natural Text
my expected output is,I cannot simply apply strip() to these items, please help.
Here is another solution:output:
Use the str.translate() method  to apply the same translation table to all strings:The str.maketrans() static method is a helpful tool to produce the translation map; the first two arguments are empty strings because you are not replacing characters, only removing. The third string holds all characters you want to remove.Demo:
try this:
Using two for loopsUsing list comprehensionAssuming 3 in on@3 is a typo, the output will be on@3 and not one as expected


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.translate
https://docs.python.org/3/library/stdtypes.html#str.maketrans
