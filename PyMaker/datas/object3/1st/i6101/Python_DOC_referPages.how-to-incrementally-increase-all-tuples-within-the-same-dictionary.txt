Natural Text
I have a dictionary which generates a "map" e.g. tuple (x, y) : mapTileWhat I'm trying to achieve is when an keyboard arrow is pushed all the x/y values within the same dictionary are incremented by +1/-1I have previously run into this error
Instead of "for key, value in dic", you can consider following code


Answer URL
https://docs.python.org/3/library/stdtypes.html#typesseq-tuple
