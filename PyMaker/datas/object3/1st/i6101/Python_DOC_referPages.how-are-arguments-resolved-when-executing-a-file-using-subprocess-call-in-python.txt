Natural Text
I would like to execute an .exe file from a Python script, on Windows, with a bunch of arguments. Part of those arguments are the paths to input files and outputs. My question: can I save the paths in a variable, which I then use as an argument, or does the call take only the name of the argument instead of its value?My idea:instead of 
you can do something like this 
The call function, like every other function and method in Python, takes variables or literals as arguments. See What is the difference between literal and variables in Python? for more details on those. You may also consider reading through the Python tutorial. It can give you a great introduction to programing in Python.


Answer URL
https://docs.python.org/3/tutorial/index.html
