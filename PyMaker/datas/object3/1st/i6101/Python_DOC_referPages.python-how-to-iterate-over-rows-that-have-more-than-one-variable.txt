Natural Text
I have a data frame like this:I used a method to find the most repeated tag (like here the most repeated tag is 'technology'). Now I want to calculate the total number of views for that tag. For example, here for 'technology', I should have the sum of 360+880+690I should find rows in views related to that tag and sum the numbers.How should I do that?
I suggest to use collections.Counter.You need to iterate over rows and over keys inside summing up the corresponding values for each key.Something like this should work:


Answer URL
https://docs.python.org/3/library/collections.html#collections.Counter
