Natural Text
This question already has an answer here:How do I concatenate two lists in Python?                    30 answers                I would like to take the following lists:and combine them into:And I can't seem to figure out a good method. Insert() puts the whole list in, resulting in a list of lists of lists. Any suggestions would be appreciated!
Just ADD them!
Use + to add them:
use extend it extends the list with another rather than inserting it inside.However, this will make the changes in place rather than creating a new list, which might be what you want. If you would rather create a new one, then + is what you need.
Just use the + operator
Generic solutions for many lists:Either:Or:Or with a list of lists:Or:


Answer URL
