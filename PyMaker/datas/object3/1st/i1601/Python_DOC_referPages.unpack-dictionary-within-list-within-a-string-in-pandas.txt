Natural Text
I have the following problem: within a dataframe I have endless dictionaries, which are in a list, which on the other hand is a string:How is it possible to unpack these dictionaries into separate columns of my dataframe?
You can use ast.literal_eval, then the str accessor to retrieve the first (and only) list element. Here's an example:Related: Splitting dictionary/list inside a Pandas Column into Separate Columns


Answer URL
https://docs.python.org/3/library/ast.html#ast.literal_eval
