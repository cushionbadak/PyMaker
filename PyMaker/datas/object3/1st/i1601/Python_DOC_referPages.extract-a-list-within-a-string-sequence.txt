Natural Text
I have a sequence of string which has a python list within it. It looks like thisHow can I can retrieve the string enclosed by [] as the list data type of python?
Use the literal_eval function from the standard library:This is way more safe than using eval directly (source).
Use a regular expression to extract the list:
Use python's eval function to evaluate the string and get a listNOTE: eval is dangerous in case you are exposing the code to open world such as a website or an api. eval executes in global namespace and hence could be dangerous.   Example:eval(os.listdir()) gives all files and folder in working directory.
You can also achieve your desired result using string operations and slicing:Output:


Answer URL
https://docs.python.org/3/library/ast.html#ast.literal_eval
