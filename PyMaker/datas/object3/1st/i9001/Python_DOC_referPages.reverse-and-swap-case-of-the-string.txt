Natural Text
What's the most efficient way to reverse the string and swap the case? For example, if my input string is:I want the content of the string to be:
You may swap the case using str.swapcase() on the reversed string (or vice-versa) as:where output_string will hold:
Something like


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.swapcase
