Natural Text
I have a lot of strings with this pattern [INTEGER]-[URL-SLUG]. Examples:I need to replace the '-' after the integer with a '/' but leave the other '-' intact. So final strings would be:
re.sub function has the optional argument count which is the maximum number of pattern occurrences to be replaced:The output:
This should work using re.sub:\1 in the replacement string refers to the first captured group in the matching pattern: '(\d)'.


Answer URL
https://docs.python.org/3/library/re.html#re.sub
