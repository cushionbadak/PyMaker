Natural Text
I am trying to extend a list of lists in a commulative way like this:Desired result is:So basically the size of the list remains the same after extending commulatively.Edit:Here is what I did initially:But then the result was:Does anyone know how to achieve this in the correct way?
One way to do this without itertools is to use Python's sum function to concatenate lists.  
Use accumulate from itertools:
You want a cumulative sum, just with lists. itertools.accumulate  can do this.
You can also create a simple list comprehension:


Answer URL
https://docs.python.org/3/library/itertools.html#itertools.accumulate
