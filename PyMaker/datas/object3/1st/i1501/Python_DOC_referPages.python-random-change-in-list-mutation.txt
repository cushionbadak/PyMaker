Natural Text
I'm writing a genetic algorithm and want to randomly mutate a number within a list.If I had a list, for example:How could I take the list and change just one of the numbers to a random numbers, for example:or Any help would be appreciated :)
Use random.randint:And now:Is (every time it's different, this is just a first-time-output):
Use random.randrange and random.randint: Edit: if you want to always make a change (i.e. never leave the list unchanged), consider incrementing the item by a non-zero value:
Use random.randint:or you can try like:


Answer URL
https://docs.python.org/3/library/random.html#random.randrange
https://docs.python.org/3/library/random.html#random.randrange
https://docs.python.org/3/library/random.html#random.randrange
