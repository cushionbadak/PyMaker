Natural Text
I have a list as follows:This is the desired output This is what I've tried:NOTE A.json has the list of list as shown above
You could use groupby, assuming you want a list of dicts where the values are list:Output
The best we can do here is a list of dicts with lists as values.result:


Answer URL
https://docs.python.org/3/library/itertools.html#itertools.groupby
