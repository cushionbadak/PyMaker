Natural Text
Is there a way to convert a numpy ndarray (numpy.array) to a standard-library array (array.array) without reallocating the data?For the record it is possible to convert an array.array to a ndarray using the buffer interface, so I hope the way round is possible:
My best guess so far is that it is not possible: memory reallocation cannot be avoided.The fastest way I have found to convert my numpy array to an array.array is to use ndarray.tobytes():Quick benchmark with other methods (using IPython):


Answer URL
https://docs.python.org/3/library/array.html
