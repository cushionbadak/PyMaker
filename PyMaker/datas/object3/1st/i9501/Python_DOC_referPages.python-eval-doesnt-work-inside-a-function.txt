Natural Text
This question already has an answer here:Creating dynamically named variables in a function in python 3 / Understanding exec / eval / locals in python 3                    2 answers                Why does Python's eval not work inside a function? The same eval(compile(cmd)) code works in a global environment, but does not work inside the foo function.Simple example:This is the output and error message:
In your function, the execution does work but x ends up in locals(), and then the print statement tries to find x in globals() and so raises the NameError.Outputs:


Answer URL
https://docs.python.org/3/reference/executionmodel.html#resolution-of-names
