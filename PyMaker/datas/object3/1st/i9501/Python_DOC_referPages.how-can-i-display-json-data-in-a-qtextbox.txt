Natural Text
For example I have this json data which is read in from a file: How can I then display this in an edit text box with formatted indentations
use json library
You can do this by using json.dumps() and putting the results into your text box.A quick example (without pyside):Results: If, instead of printing, the results of the json.dumps() you assign it to a variable: You are now able to set the content of your text box to p using the QTextEdit's setText() slot
It's not the precise format you specify, but pprint.pformat with an appropriate width value produces a nicely-formatted string, which you can then insert into the text box:Result:


Answer URL
https://docs.python.org/3/library/json.html#json.dumps
