Natural Text
Below is mwe:This gives error:However, if I replace forward slash with underscore, then it works fine.Escaping the forward slash by appending backslash doesn't help either. 
According to the documentation, the default regular expression for the placeholder value is [_a-z][_a-z0-9]*, which means that a / character is not allowed. If you want to allow this, you should subclass Template and redefine the idpattern expression:Demo:


Answer URL
https://docs.python.org/3/library/string.html#template-strings
