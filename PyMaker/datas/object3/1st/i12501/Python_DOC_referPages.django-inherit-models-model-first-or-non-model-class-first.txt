Natural Text
Sometimes I see Other times, I seeWhat's the difference between the two and when should I use each over another?
It depends on what you have in NonModelCls. Search order for attributes and methods are from left to right (https://docs.python.org/3/tutorial/classes.html?highlight=inheritence#multiple-inheritance).Let's say you have these classes:Then calling C and D will result in:So in your case, if NonModelCls has a method with same name as one of models.Model methods, like save, then it will override Model.save in Bar class, while it gets ignored in Foo class.


Answer URL
https://docs.python.org/3/tutorial/classes.html?highlight=inheritence#multiple-inheritance
