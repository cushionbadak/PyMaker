Natural Text
I extract a dataset row:I would like to find 'content-type'.I tried dict, list, enumerate, str, ...But nothing works: (
The string in your tuple looks like a list, I'd suggest use ast.literal_eval() to convert it to a real list, and then convert it to a dict (simply run dict() function), and use 'content-type' as the key to access the 'text/html' (value):Output:


Answer URL
https://docs.python.org/3/library/ast.html#ast.literal_eval
