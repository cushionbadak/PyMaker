Natural Text
So I developed python code that sends a quote in the form of a text message. It runs properly when ran in a simple script. However, once I put the code into a function and call the function then it fails to work. It sends a blank text message, instead of the quote.Here's the code working:Here's the nonfunctional code
Maybe sendmail is sensitive to the whitespace at the beginning of each line of this string. Try de-indenting them.


Answer URL
https://docs.python.org/3/library/textwrap.html#textwrap.dedent
