Natural Text
I have a Linux command from here https://github.com/scragg0x/FFXIV-Scraper:That returns data like this:I'm looking to script this in Python and retain the formatting. I've done this:But it does not seem to preserve the formatting:Am I missing anything? How can I bring this into Python and handle it properly (BeautifulSoup/XML)? For example, if I wanted to print title.
It's a json string (byte-string). Decode it using json.loads after decode the byte-string into a string:If you want to just print the xml, decode xml (byte-string object) and print:


Answer URL
https://docs.python.org/3/library/json.html#json.loads
