Natural Text
I've just recently started coding and came across this problem which I can not seem to solve. This is my code: and this is the problem:
Remove the extra paretheses and have something like 
The problem is that you are not using the line separation operator properly, so you are treating the result of print, which is None, as a callable. Look at the following:That will print:and calling print will return None, so following your code you will have:which is generating this error.You can solve this problem by:I strongly encourage you to use f-string if you are using Python 3.6+:Notice how using f-strings improves the readability of your code.
Use the .format() or the even formatted string interpolation:orfor:Doku:pep 498 string interpolationformat()


Answer URL
https://docs.python.org/3/reference/lexical_analysis.html#f-strings
https://docs.python.org/3/library/stdtypes.html#str.format
