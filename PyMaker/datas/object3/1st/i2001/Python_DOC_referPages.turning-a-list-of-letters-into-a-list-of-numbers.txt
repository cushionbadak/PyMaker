Natural Text
I need to turn a list of numbers into a list of letters.More specifically, I need to read PZ and turn it into PZx, such that the first distinct letter appearing in PZ assumes in PZx the number 1, the second distinct letter assumes the number 2, the third distinct letter assumes the number 3, and the fourth distinct letter assumed the number 4. Example:I need to write a function that turns any given PZ into PZx according to this rule.
You could use a dictionary and setdefault, from the documentation:If key is in the dictionary, return its value. If not, insert key with  a value of default and return default. default defaults to None.Code:Output
I think you can use list index and have a simple loop like this

This could be done easily with a dictionary. Try something like this.now PZx should equal [1,2,3,2,1,1,3,4,2,4,3,4]
Another way to do it by using pandas and list comprehension:


Answer URL
https://docs.python.org/3/library/stdtypes.html#dict.setdefault
