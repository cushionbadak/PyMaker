Natural Text
I need to define a function that takes a string and counts the number of letters of the alphabet(only lower case)in the input, for instance if I input "jack" it would return:So I implemented the following :Which only returns the number of 'a' in the string.Since i don't want to do it for all the alphabet I thought about implementing a list comprehension like this :But I get an error :I don't know how to change it since I get the same error. 
Here's one way using collections.Counter:You may wish to add logic to lowercase your string, exclude punctuation, etc.
You can use the Counter object for thisThis transforms all characters in string to lowercase, checks whether they are alphabetic, and then Counts all of the characters.
I think you need this one:
If you only want the chars that are present in the string, then you can use the Counter object:If you want to show the counts for all lowercase letters, you can loop now like this:


Answer URL
https://docs.python.org/3/library/collections.html#collections.Counter
https://docs.python.org/3/library/collections.html#collections.Counter
