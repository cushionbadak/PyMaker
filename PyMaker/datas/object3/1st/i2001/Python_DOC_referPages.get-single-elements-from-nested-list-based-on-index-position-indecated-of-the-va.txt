Natural Text
As last part of a bigger project here is what I am trying to solve:I have a list of lists of which I need to extract exactlty one element based on the value of a second list.The values in b indicate the positions of the elements in the sublists. Also, the index in b is the true for the index of elements in list a. The result I am looking for is:I have tried many versions of this:But as a result I get a list over all emements of all lists looking like this:
By using nested loops, you are combining every element from a with every element from b. What you want is pair-wise iteration, using zip:This is roughly equivalent to:
Or:Example:
Try this:
You can try the following.


Answer URL
https://docs.python.org/3/library/functions.html#zip
