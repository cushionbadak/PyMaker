Natural Text
Im trying to create 2 lists form a list of lists, the following code is a working easy example, but it makes sense to think there is a command that can be used instead of creating the for loop for splitting. The list of lists is:Is there a command that can help aviod using the for loop?
You can use list comprehensions to extract the nth element of each tuple:Functionally, you can use operator.itemgetter for the equivalent:If you don't know the size of your tuples in advance, the recommended solution is zip, which will output a list of tuples, each tuple representing a tuple index:You can even use sequence unpacking here without forming the full list:


Answer URL
https://docs.python.org/3/library/operator.html#operator.itemgetter
