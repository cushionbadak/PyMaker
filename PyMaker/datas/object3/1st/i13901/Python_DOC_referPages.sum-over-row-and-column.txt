Natural Text
Let's have for example the following dataframe: I would like to sum column d and e, and add those numbers together. Results in sum = 45. I couldn't find a simple way to do this.
Pass a list of the columns of interest to sub-select from your df, then call the attribute .values to return a np array, you can then use np.sum to get the answer you desire:Or for a pure pandas way call sum twice:
if you have a dataset like this:thengives youwhich in turn can be summed by index.It's written well in the Python tutorial:https://docs.python.org/3/tutorial/datastructures.html#nested-list-comprehensions


Answer URL
https://docs.python.org/3/tutorial/datastructures.html#nested-list-comprehensions
