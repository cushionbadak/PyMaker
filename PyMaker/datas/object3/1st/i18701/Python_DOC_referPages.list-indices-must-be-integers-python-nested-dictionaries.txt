Natural Text
In python 3, I need a function to dynamically return a value from a nested key.How should nestedvalueget be written?I'm not sure the title is properly phrased, but I'm not sure how else to best describe this. 
If you want to traverse dictionaries, use a loop:or use functools.reduce():then use either version as:Note that either version takes a variable number of arguments to let you pas 0 or more keys as positional arguments.Demos:And to clarify your error message: You passed the expression ['n']['cn'] to your function call, which defines a list with one element (['n']), which you then try to index with 'cn', a string. List indices can only be integers:


Answer URL
