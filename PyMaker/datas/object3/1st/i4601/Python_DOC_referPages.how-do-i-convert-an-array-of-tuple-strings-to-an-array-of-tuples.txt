Natural Text
This question already has an answer here:Parse a tuple from a string?                    4 answers                My situation is that I'm trying to convert an array of tuples in a string format into an array of tuples.i.e. I want to go from:toThe purpose of this is to be able to create a shapely Polygon.  It throws an error when the tuples are a string.  What is the best way to go from strings to actual tuple objects?
They're literals, so you can safely evaluate them:
This is the functional way with ast.literal_eval:


Answer URL
https://docs.python.org/3/reference/expressions.html#literals
