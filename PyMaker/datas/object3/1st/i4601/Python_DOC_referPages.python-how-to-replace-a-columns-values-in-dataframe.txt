Natural Text
If there is a column called date in a pandas.DataFrame, For which the values are:How do I change all the values to integers? For example:
You can use .str.replace() like:Code:or if not using a regex, faster as a list comprehension like:Test Code:Results:


Answer URL
https://docs.python.org/3/tutorial/datastructures.html#list-comprehensions
