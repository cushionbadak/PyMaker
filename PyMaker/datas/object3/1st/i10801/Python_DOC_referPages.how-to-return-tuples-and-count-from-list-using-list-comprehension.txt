Natural Text
I have a function which, using list comprehension, returns the list elements in caps, and counts each element. This returns:but i need it to return paired tuples like this:I tried to use the zip() method with no success. How do I go about doing this?
You can use zip():But, why don't construct the list in a single iteration:


Answer URL
https://docs.python.org/3/library/functions.html#zip
