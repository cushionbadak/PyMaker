Natural Text
This question already has an answer here:How do I pass a variable by reference?                    24 answers                this works fine for fun1([1,2,3])but when i want to do something like this for both the above statements output is [r,q,p] but I want it to remain [p,q,r].
Using input as a name will shadow the builtin input function, so that's a bad idea.To keep input_ intact, you can modify a copy of it, instead of modifying it directly:You can also use the explicit copy function to make a shallow copy:Your input_ will not be modified and changes on the copy will not propagate to the original input_
Assign new local variable for the reversed list and return it:


Answer URL
https://docs.python.org/3/library/stdtypes.html#mutable-sequence-types
