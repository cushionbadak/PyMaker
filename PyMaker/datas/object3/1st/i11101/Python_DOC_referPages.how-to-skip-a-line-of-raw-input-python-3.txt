Natural Text
I am completing the following HackerRank challenge. I am fed the following input: Let's say though I just want the 2nd and 4th lines of input (and assume all input will look like this). At the moment I'm implementing a hackneyed solution: I don't use delete later in the program. Is there a better, more exact and concise way forward?
Loop over the sys.stdin object; it is a regular file and supports iteration. You can use an itertools.islice() object to skip every second line:


Answer URL
https://docs.python.org/3/library/sys.html#sys.stdin
https://docs.python.org/3/library/itertools.html#itertools.islice
