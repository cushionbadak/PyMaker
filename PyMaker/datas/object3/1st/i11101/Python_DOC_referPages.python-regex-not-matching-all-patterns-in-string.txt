Natural Text
I want to match a curl command's header pattern like:-H 'key: value'or-H "key: value"This switch can appear in the middle somewhere or at the end of string.My pattern:My string:Now I attempted to find all instances of this pattern but its matching just the first pattern.
Why don't use argparse module instead of regular expressions:Prints ['Authorization: Bearer xxx', 'Content-Type: text/plain'].(Inspired by uncurl package).
You can use this regex to match all header options:RegEx DemoThere is no real need to assert for whitespace before -H but you need you can use:


Answer URL
https://docs.python.org/3/library/argparse.html
