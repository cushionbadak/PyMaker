Natural Text
Is there any easier way to convert non-html letters to html letters? for example if I do function("a") it would return "&#97;" the Only way I know how to do this is by:So is there a better way to do this or is using replace the only way to achieve this?
Using html.entities.codepoint2name and re.sub:Example:
Try the html.entities(Definitions of HTML general entities) module.although if somebody could give a specific example that would be helpfull


Answer URL
