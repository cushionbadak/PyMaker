Natural Text
Is there any way to check for strict sublist:Given those lists, I want to achieve something like this:Update:I rephrase my question:This is what I wanted to do. I am traversing a graph, where I have several paths (list of nodes). Unfortunately, I find duplicates and sub-lists. Is there any better way to remove sub-lists other than using issubsetIn the above example, I want only 2 paths: [1,3] and [1,2,3,4]

As mentioned, you can use the set.issubset call:Alternatively, for a strict subset match, you can also use the less than operator:


Answer URL
https://docs.python.org/3/library/stdtypes.html#set.issubset
