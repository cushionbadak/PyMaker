Natural Text
My code is :How can I to break out of the for loop inside of join() method if idx >= maxlimit ?
Well, you should use the if condition in the list comprehension instead:This generates a list of 5 items only.But then it is easier to just limit the number of items to iterate with itertools.islice; or if the queryset (which I do not know what it supports), supports slicing, then just slice it with [:maxlimit]: Though for a simple application of one function to each element, I often use map as it requires less typing:


Answer URL
https://docs.python.org/3/library/itertools.html#itertools.islice'
https://docs.python.org/3/library/functions.html#map
