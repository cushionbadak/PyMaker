Natural Text
Is it possible to call a Python function from a C dll function?We consider this C function:On Python, I would like to call foo and set the callback to a Python function: Unfortunately, the ctypes documentation is pretty light on this topic and the above code does not work. 
If you need stdcall calling conventions, use WINFUNCTYPE instead.Note: if foo may store the callback to be called at a later time then make sure that Python callback is alive (it is enough if it is defined at the global level using the decorator as shown in the example -- modules are essentially immortal in Python unless you try to remove them explicitly).
Use CFUNCTYPE to create a callback type:


Answer URL
https://docs.python.org/3/library/ctypes.html#ctypes.WINFUNCTYPE
