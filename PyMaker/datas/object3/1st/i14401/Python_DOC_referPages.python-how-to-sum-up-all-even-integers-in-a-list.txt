Natural Text
I'm completely new to the subject and I want to ask how to sum up all even integers in a list (without using functions (I haven't studied them yet))?For example: I create for loopHow to store these values to calculate the sum?
Using a generator expression:Using filter():Using a manual loop:
Sorry, I just had to golf this. Maybe it'll teach someone the ~ operator.Found another one with the same length:
You need to store the result in a variable and add the even numbers to the variable, like so:
You can filter out all non-even elements like soand then sum the output like so:


Answer URL
https://docs.python.org/3/reference/expressions.html#unary-arithmetic-and-bitwise-operations
