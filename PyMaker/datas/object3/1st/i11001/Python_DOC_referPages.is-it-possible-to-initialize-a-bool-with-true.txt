Natural Text
I am defining a variable through an empty declaration:Is it possible to force an initialization of True?Note: I know I can simply have a = True or a = bool(True) but I will be initializing several variables with specific types, something likeThe defaults for int, float, etc. are OK, I would just prefer to specifically have a bool initialized as True
You can use functools.partial to customize your bool:And you can use it in your code as:You could also use a lambda (credit: @tobias_k):
Maybe a possible solution for you with the flexibility for other special default values
You could map the types you want to change the default values for to your own functions:or using a dictionary comprehension:


Answer URL
https://docs.python.org/3/library/functools.html#functools.partial
