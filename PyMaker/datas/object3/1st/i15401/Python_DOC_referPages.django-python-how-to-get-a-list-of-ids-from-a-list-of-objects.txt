Natural Text
If I have a list of objects acquired by some query (in this case Django models).How can I get the list of ids, so I can use it to search another model, like this:I'm pretty sure the lambda expressions should be able to do it but I can't figure it out...
If the return value of the friends method is a QuerySet object, you can use QuerySet.values_list method:If it's not a QuerySet object, you can use list comprehension (This one can be used for both QuerySet and non-QuerySet one):


Answer URL
https://docs.python.org/3/tutorial/datastructures.html#list-comprehensions
