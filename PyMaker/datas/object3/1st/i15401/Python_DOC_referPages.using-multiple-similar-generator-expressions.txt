Natural Text
In a csv file, I'm trying to replace certain characters with other characters.My current code is similar to this:In this example I am only using three generator expressions, but it could easily be more than that.Does anyone know the right way to do this? My concern is that this structure might not be the fastest (and certainly doesn't look optimal).
str.translate might be appropriate; something along the lines ofand
You can use a loop and parameterise the varying parts:If you are replacing characters, don't use sets but a mapping:
Here is something that combines both answers in some way and works great (multiple times faster than the code in the question):


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.translate
