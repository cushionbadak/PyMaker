Natural Text
This question already has an answer here:Is it possible to turn a list into a nested dict of keys *without* recursion?                    5 answers                I'm making simple use case of my problem, here its is: Now i want to have a method which operates on this dictionary, fetch values based on the key.At different places this generic method will be called to fetch the values.get_value(dic, 'a') will work.Is it be possible to get the value 2 (dic['b']['c']) in more generic way.
Using unbound method dict.get (or dict.__getitem__) and reduce:UPDATEIf you use dict.get and try to access non-existing key, it could hide KeyError by returning None:To prevent that, use dict.__getitem__:


Answer URL
https://docs.python.org/3/library/stdtypes.html#dict.get
https://docs.python.org/3/library/functools.html#functools.reduce
