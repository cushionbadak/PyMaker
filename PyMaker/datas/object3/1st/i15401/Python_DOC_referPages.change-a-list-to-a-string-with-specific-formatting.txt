Natural Text
Let's say that I have a list of names:And need to get a string output like:(Note that the missing quote at the beginning and at the end is on purpose)Is there an easier way to do this than I know something like that will work, however the real names list will be very very (very) big and was wondering if there is a nicer way to do this.
You can simply use str.join:
may be bad way to do it, just wana share it :


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.join
