Natural Text
I simply want to extract the indices in two list where they equal some string.Say:Then I simply want all indices with 'foo' in list a and 'bar' in list b, which should be [3, 4] does not work as it does with e.g numpy arrays. How do I make this work?thanks a lot!! 😊 
Iterate with enumerate function, filter out all the elements which are foo and get only their index, like thisAfter the edit, the question changed drastically. But this solution would work


Answer URL
https://docs.python.org/3/library/functions.html#enumerate
