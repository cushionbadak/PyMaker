Natural Text
so I have a project that is pretty straight forward. It's a simple BMI calculator and should just simply print, "The BMI for (name) is (BMI), (status, like if they're underweight, overweight etc.)I keep getting: I know that means I need to convert the object to a string and I'm sure I'm missing something simple. Thanks for helping if you do so choose to.
You've rebound a to the class. You need to instantiate the class instead.
It looks like you're not actually instantiating the object. You need to instantiate an instance of CalcBMI
Just implement __str__ and __repr__ methods.https://docs.python.org/3/reference/datamodel.html#object.strhttps://docs.python.org/3/reference/datamodel.html#object.repr
You have problem in the following method:Since self.getBMI() is returning float value, you need to convert it to string.Moreover, you need to instantiate the class instead as follows:For example:It outputs:


Answer URL
https://docs.python.org/3/reference/datamodel.html#object.__str__
https://docs.python.org/3/reference/datamodel.html#object.__repr__
