Natural Text
I have a Python dictionary with strings as keys and numpy arrays as values:Now I want to use the itertools product to create the following list:As normally done when the items passed to product are numpy arrays.When I do the following:I get the following output instead:
The itertools.product() function expects the arguments to be unpacked into separate arguments rather than kept in a single mapping view.  Use the * operator to do the unpacking:


Answer URL
https://docs.python.org/3/library/itertools.html#itertools.product
https://docs.python.org/3/tutorial/controlflow.html#unpacking-argument-lists
