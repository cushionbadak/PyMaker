Natural Text
So, I extracted some URL from an SQLite database. I thought it would come as a list but apparently, it came as a tuple.attributeerror : "tuple" object as no attribute "split"Do you know how could I convert my tuple into a list?  
Right, that's the default type of the result of the .fetchall() - these are actually not tuples but Row objects which mimic the tuples. You could unpack them in the loop:


Answer URL
https://docs.python.org/3/library/sqlite3.html#sqlite3.Row
