Natural Text
I have a python object that looks like this. I am trying to parse this object and turn it to a human readable string which I need to put in the logs. How can I recursively loop through this considering the object could be nested dictionaries or nested lists or dictionaries inside lists inside dictionaries etc. I want to possibly serialize the above to look something like thisMy code so far [this works for nested dictionaries but I am not sure how I can alter this to support lists in the above object]:I have looked over possible answers but could not find a solution.
The formatting might be a bit offResult:
Maybe the output of pformat would suit you:
You need an elif-condition in case of type == list and a indentation tracker:That will print out:


Answer URL
https://docs.python.org/3/library/pprint.html#pprint.pformat
https://docs.python.org/3/library/functions.html#isinstance
