Natural Text
I have this formatting expression:The code runs but it is not sorted. I don't see why it is not being sorted. It returns the code below and every time it is in a different order.
You're calling '\n'.join on a set (an unordered type) which defeats the initial purpose of ordering in the first place:You could use a list (a sequence):Or remove those and use the generator expression directly. Here's a quite readable vesion that uses itertools.starmap:


Answer URL
https://docs.python.org/3/library/itertools.html#itertools.starmap
