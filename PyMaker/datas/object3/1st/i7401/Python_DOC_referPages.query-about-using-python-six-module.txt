Natural Text
I'm using the six module in my program, and the code is as follows: The problem with this approach is that, the function do_something_else() would run only if Python version is 3.4+ due to dependencies. (And not on Py 3.3)How do I check for this?Thanks a lot! :)
Since it's a common requirement, six already provides this one:It will be true if the Python version is greater or equal than v3.4.  So you could do this:
You can get the actual version number as a tuple from sys.version_info. So:


Answer URL
https://docs.python.org/3/library/sys.html#sys.version_info
