Natural Text
This question already has an answer here:How to replace multiple substrings of a string?                    19 answers                What is a neat way to remove different characters from a string? For example, I have the following strings that I need to convert to integers:I use the following code which works fine, but is there a less bulkier way to do the same?Edit: I am using Python 2.7.
You could use str.translate, e.g.As stated by @AdamSmith in the comments you could also utilize the (full) three-argument form of str.maketrans:In case you're using python-2.x the str.translate function and string.maketrans function can be used:or with unicodes on Python-2.x you need a unicode-ordinal to unicode-ordinal/string or None:
Well, you can rely on a loop to make it less ugly:
or 


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.maketrans
