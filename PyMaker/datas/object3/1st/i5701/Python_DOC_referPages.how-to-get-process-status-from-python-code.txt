Natural Text
I'd like to operate a process using supervisorctl from python code, in order to get the status and make conditional branches like this: But this doesn't work. subprocess.run returns CompletedProcess(args=['sudo', 'supervisorctl', 'status', 'my_status'], returncode=0). The response doesn't have what the status is.Is there any way to get the status?
I think you need to use subprocess.check_output() to get the output returned to you rather than just the return code. Run command with arguments and return its output as a byte string.
The python subprocess documentation describes how .run() returns a CompletedProcess object.From there you can retrieve things like status:


Answer URL
https://docs.python.org/3/library/subprocess.html#subprocess.CompletedProcess
