Natural Text
Often when I'm working with dictionaries whose values are lists, I happen to code this:Is there a more concise way of doing this?
You can use a collections.defaultdict:
The most concise way is to use setdefault like:


Answer URL
https://docs.python.org/3/library/stdtypes.html#dict.setdefault
