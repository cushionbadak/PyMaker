Natural Text
I need to retrieve only those words from a file whose pos tags are:'NN'or 'NNP' or 'NNS' or 'NNPS'.My sample input is:My expected output is:
Since your input is a list of a list, You could use a nested list comprehension:Edit:
Here's a simple method based on list comprehensions:It basically iterates over all words, takes only those that contain ",NN" and cuts out the part before the comma.
You can try something like this with regular expression in just one line :output:Now if you want without 'NN' then :output:


Answer URL
https://docs.python.org/3/tutorial/datastructures.html#nested-list-comprehensions
