Natural Text
I have a raw SQL to be executed in SQLAlchemy with optional parameters coming from a query string.I did a function to build the sentences to a where clause:The problem is when I need to execute the query, I have to concat this to a SQL string and it's not good for a SQL Injection happening.How can I use optional parameters using prepared statement?Thanks to advance!
Here's a somewhat simplified example of what you could do:Using column() handles quoting reserved words and mixed case, and escaping bad characters in column names, but you should still possibly whitelist though. The comparison produces a binary SQL expression object, with the bound parameter val. You can combine Core constructs with text fragments in order to generate your query:


Answer URL
https://docs.python.org/3/library/re.html#re.regex.match
