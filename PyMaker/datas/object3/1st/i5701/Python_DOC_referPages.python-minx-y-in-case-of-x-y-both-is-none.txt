Natural Text
I would like to perform the following:x/y/both of them may be None.If I filter the None values in this way:I may get this assertion if x&y are both None.Is there an elegant way to get the little number if only one of them is None, and get None if both of them are?
You can do it like so:You can also turn it into a function:or a lambda:Tests:EDIT:Since Python 3.4, min got updated to accept a default argument in case the iterable provided is empty. This allows for an elegant one-liner:From the documentation:The default argument specifies an object to return if the provided iterable is empty. If the iterable is empty and default is not provided, a ValueError is raised.
In the interest of keeping things readable, I'd suggest:

It takes the argument as a list of any number of elements and returns the minimum. I think this should work.
Another option:


Answer URL
https://docs.python.org/3/library/functions.html#min
