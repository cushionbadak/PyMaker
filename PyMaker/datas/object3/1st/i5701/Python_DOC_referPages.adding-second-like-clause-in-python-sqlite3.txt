Natural Text
I 'm trying to get lines from an sqlite database that contain a certain word in 'A1' column and another in 'A2' column. My code looks like this:I 'm getting a TypeError: 'str' object is not callable. I am pretty sure my syntax is wrong but i cannot figure out how the correct one should be. Any help?
The execute() method expects two parameters: a single SQL statement string, and a list containing the SQL parameters:
It's the last line. Should be something like this


Answer URL
https://docs.python.org/3/library/sqlite3.html#sqlite3.Cursor.execute
