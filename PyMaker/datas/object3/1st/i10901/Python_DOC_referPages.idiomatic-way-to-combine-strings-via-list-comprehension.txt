Natural Text
Can you suggest a better way to combine strings from lists?Here is an example:which results in: The actual context is working with files and paths:resulting in:
For working specifically with file paths, use os.path.join:
You can use list comprehension, os.path.join function and itertools module:
How about with str.join.As others mentioned, you should use os.path.join for filepaths.
You could use cartesian product for lists.Then join them however you want.
First one: ['prefix-%s-%s' % (a,b) for a in [1, 2] for b in 'ab']The second one could be the same way but you may want to use os.path.join to normalize for Windows:


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.join
https://docs.python.org/3/library/os.path.html#os.path.join
