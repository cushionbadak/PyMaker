Natural Text
I'm trying to mock file open, and all of the examples show that I need to but I keep getting I know patch needs the full dotted path of open, but I have no idea what it is. As a matter of fact, I'm not even sure that's the problem.
You need to include a module name; if you are testing in a script, the name of the module is __main__:otherwise use the name of the module that contains the code you are testing:so that any code that uses the open() built-in will find the patched global instead.Note that the mock module comes with a mock_open() utility that'll let you build a suitable open() call with file data:
In Python 3 you should use:


Answer URL
https://docs.python.org/3/library/unittest.mock.html#mock-open
https://docs.python.org/3/library/unittest.mock.html#mock-open
