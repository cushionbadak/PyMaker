Natural Text
This question already has an answer here:Syntax error on print with Python 3 [duplicate]                    9 answers                I'm running files obtained from the web in my desktop's Python 3.4 install, and getting syntax errors, such as:skipping a few defs which aren't pre-requisite, there's:for which the interpreter respondsIs this perhaps due to a difference of 2.7 vs 3.4? I'm not sure what version the source file was created in. Source is from IBridgePy github repo
Check the Python 3 docs.Python 3 uses () around the print contents - print ('python')


Answer URL
https://docs.python.org/3/tutorial/inputoutput.html
