Natural Text
I have been attempting to organize and analyze a large amount of data in an array. However, my code does not appear to be functioning the way that I would like.I am attempting to use arrays A (Data Array) and B (Reference Array) to output one reusable array, C.Input arrays:Output (Reusable) array:This is what I have so far:Any help would be greatly appreciated, thanks.
Assuming the unifying characteristic is that the first field is the same for each go around of C, and making up my own data file:We can build the groups as we go, reading the file only once:The output is
Cant you just use filter for this?Given:You can filter by the beginning element of the string:Or, 


Answer URL
https://docs.python.org/3/library/itertools.html#itertools.groupby
