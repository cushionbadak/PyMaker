Natural Text
I'm trying to use argparse in a Python 3 application where there's an explicit list of choices, but a default if none are specified.The code  I have is:However, when I run this I get the following with an option:Or without an option:Am I missing something here? Or can I not have a default with choices specified?
Pass the nargs and const arguments to add_argument:If you want to know if --list was passed without an argument, remove the const argument, and check if args.list is None.
Thanks @ShadowRanger.  Subcommands is exactly what I need, combined with nargs and const.  The following works:list option alone:List option with a parameter:


Answer URL
https://docs.python.org/3/library/argparse.html#sub-commands
