Natural Text
I need to fill in the csv file so that rule.endpoint and url have separate columns.
Your writerows parameter just needs to be a list of lists of the values the row should contain. So for example writer.writerows([[1, 2], [3, 4]]) will add two rows where each row contains two values. As you add only one row you can use writerow:Here is a mock-up. I'm using BytesIO so that I don't need to actually write to a file:



Answer URL
https://docs.python.org/3/library/csv.html#csv.csvwriter.writerows
