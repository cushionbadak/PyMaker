Natural Text
Is there a way to add all the input numbers to a list? I mean like this:and then make all those numbers into a list?I thought maybe like this but it doesn't work, 
Python 2.7 will just work:Python 3:
You can input a list of numbers like this in Python 2:
You can use ast.literal_eval to parse a list of numbers entered by the user:Note that with Python 2, you need to use raw_input() instead of just input().


Answer URL
https://docs.python.org/3/library/ast.html#ast.literal_eval
