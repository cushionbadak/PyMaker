Natural Text
In this example:why does the %d become a 3 but when I try it in other cases it doesn't? And what does the %3 do?
The % after string literal (enclosed in quotes) is for something called formatting. In string occurrence of %d is changed to number in decimal (d stands for decimal, x stands for hexadecimal, f for floating-point, etc.).So your example it is equivalent to:This is probably more useful example:For more information on string formatting look here: Old string formatting


Answer URL
https://docs.python.org/3/library/stdtypes.html#old-string-formatting
