Natural Text
How to have get the logger in auxiliary_module.py to log to the logger in main.py?The below example is an adaptation of Python's documentation.Assuming a model structure such as:The file main.py looks like this:Whereas auxiliary_module.py looks like this:This is how the log looks like after running main.py (which creates a class instance and calls a method):How to configure auxiliary_module.py to use the logger in main.py? That is, if the logger in main.py is configures to use a certail format and handlers, how to get auxiliary_module.py to use the same configuration? 
You can simply give a name to logging.getLogger() that is used by both modules, e.g. logger = logging.getLogger('my-logger')


Answer URL
https://docs.python.org/3/howto/logging-cookbook.html#using-logging-in-multiple-modules
