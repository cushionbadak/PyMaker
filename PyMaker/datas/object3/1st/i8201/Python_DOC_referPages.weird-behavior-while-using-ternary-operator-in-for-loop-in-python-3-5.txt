Natural Text
This question already has an answer here:python ternary operator behaviour                    3 answers                While I was testing ternary operator in python 3, I came across this weird phenomenonwould print a~Z 1 character per line, butwould print nothing.Can somebody give me an explanation?
This is due to operator precedence: + binds tighter than if.In the first snippet, testing evalutes to "" because chr(0) is not in test. So, the loop is over ascii_letters + "", ie just the letters.In the second, the + is evaluated first; the if therefore evaluates the whole thing to "" and the loop is just over the empty string.
Change to:and it will work.Why? Operator precedence. Your current code is actually:


Answer URL
https://docs.python.org/3/reference/expressions.html#operator-precedence
