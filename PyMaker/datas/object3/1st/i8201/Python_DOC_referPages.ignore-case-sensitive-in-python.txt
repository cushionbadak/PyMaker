Natural Text
I want to check if a variable's value is one of 'Val' or 'val'.I can't use var.lower() because the variable can be None.I know I can use var in ['Val','val'] or str(var).lower() but I look for a nicer way...
you can just combine those tests:and short-circuits: if var is None, var.lower() is not executed.or if var can also have other types and you want to be a bit more explicit:


Answer URL
https://docs.python.org/3/reference/expressions.html#and
