Natural Text
I am attempting to turn foo into:This in effect is turning the "rows" into "columns":I have attempted something along these lines:But it leaves me with:Any help would be appreciated!
With zip() and the star operator:Explanationzip(*foo) "unzips" foo, e.g.:So in this case:Then on each of these sub-elements (tuples), we want to join the individual elements into one string.  ''.join(seq) is the standard way to go about this.


Answer URL
https://docs.python.org/3/library/functions.html#zip
