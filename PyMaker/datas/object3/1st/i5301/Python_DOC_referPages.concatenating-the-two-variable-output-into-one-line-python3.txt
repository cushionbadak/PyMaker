Natural Text
I have two variables holding the ouputs from using subprocess output and want the output of the both variable to be printed into one line..below is the details..I need the print output of both the variables into one line, line below..I tried below but not working.. i just started learning python..Its python3 , please guide..
You may want to use zip() built-in function to mix two lists and split() to split the output to lists of strings.Zip will make a list of tuples:Then you'll need to print it out. Here's the full code:
You can iterate over both outputs simultaneously by splitting them into lines and zipping them:I also used str.ljust to align the output.


Answer URL
https://docs.python.org/3/library/functions.html#zip
https://docs.python.org/3/library/stdtypes.html#str.ljust
