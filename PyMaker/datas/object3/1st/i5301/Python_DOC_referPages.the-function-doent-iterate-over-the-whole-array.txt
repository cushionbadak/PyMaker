Natural Text
I have the following code which gets two arrays and finds the location of each item in the second array based on the first array. For example, for 23 from loc1, which is between 20 and 25 in array it should return 20.I expect to receive this array as the result:But I receive only the first item like this:
Just use the bisect module:Output:Finding the index is simpler:
You're returning the matrix_x.append statement, so it only ever appends 1 item, in this case 20. To get the behavior you want, you may need to restructure your function some. 


Answer URL
https://docs.python.org/3/library/bisect.html
