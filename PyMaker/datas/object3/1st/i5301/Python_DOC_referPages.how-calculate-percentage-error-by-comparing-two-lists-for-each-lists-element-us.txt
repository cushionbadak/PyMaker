Natural Text
I have two lists, for example:I want to use this formula - |accepted value(a[i]) - experimental value(b[i])| \ accepted value(a[i]) x 100%, and get this result:How is it possible for two lists without numpy?
This can be accomplished with an ordinary loopIf you have zeros in a, then you can skip these (while keeping the same length) by including an if-else statement:


Answer URL
https://docs.python.org/3/library/functions.html#zip
https://docs.python.org/3/tutorial/index.html
