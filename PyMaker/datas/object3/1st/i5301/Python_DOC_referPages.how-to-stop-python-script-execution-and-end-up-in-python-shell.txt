Natural Text
Octave has a keyboard function that stops the execution of the script and drops you out into an interactive octave shell, so it is possible to inspect variables at that point and carry our other debugging actions. Is there anything similar in python?
Yes. But you must use the -i option when running the script:EditActually, you are looking for the python debugger, pdb. So you must:And this will drop you into the python debugger. It is quite a broad topic, best to start with reading the docs
pdb is the correct answer for debugging, but sometimes it's nice to have the regular REPL:


Answer URL
https://docs.python.org/3/library/pdb.html
