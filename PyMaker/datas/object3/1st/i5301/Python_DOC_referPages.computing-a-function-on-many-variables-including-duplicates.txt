Natural Text
I have a lot of variables. Say for example, I wanted to print each variable everytime it was computed with. How would I do this. I will illustrate below:How would I print so it displays both computations of b, as shown below:Would I have to store the variables in a list and use a for loop? Quite new to python so I am unsure.
If you store both variables in a list, you could use this idiom:zip() takes two lists, and returns a list of 2-tuples:str.format() takes a string, and injects the arguments you supply into the curly braces:


Answer URL
https://docs.python.org/3/tutorial/index.html
