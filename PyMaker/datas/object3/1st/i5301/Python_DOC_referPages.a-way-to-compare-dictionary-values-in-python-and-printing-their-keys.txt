Natural Text
I have written the following code:From this I get the result: dogWhereas I actually want to get:dog;catHow can I fix the code? 
If you want to find all the keys which have the same maximum value, you can do:
One way is to use list comprehension:
You can just filter out the keys that have the max value:Or even with a collections.defaultdict:
d = {}This will do the trick: max(d, key=d.get)Also: max(d.values())
Try this :output:


Answer URL
https://docs.python.org/3/tutorial/datastructures.html#list-comprehensions
