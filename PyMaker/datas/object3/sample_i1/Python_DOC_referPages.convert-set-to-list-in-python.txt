Natural Text
This question already has an answer here:Concatenate item in list to strings                    5 answers                I'm converting a set to a list, and then to a string in Python. I can remove the brackets in the string output, but I also want to remove the quotes around the string.This is what I tried:This is the error I get:If I change the bottom line to this, without the final replace, the script works. But the quotes are still there:How can I remove the quotes from the output?
you can do:


Answer URL
https://docs.python.org/3/library/stdtypes.html#str.join
