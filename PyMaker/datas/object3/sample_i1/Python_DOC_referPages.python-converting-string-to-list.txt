Natural Text
This question already has an answer here:Convert string representation of list to list                    14 answers                Converting a string representation of a list into an actual list object [duplicate]                    3 answers                I am sending data between python socket connection. The data is an array and I am sending it in blocks of each line. When the data comes in it looks like this:I want to convert this back into array form so I can then add it back into my master array that is being sent over. Any idea how I can convert this string back into an array.
Out:


Answer URL
https://docs.python.org/3/library/ast.html?highlight=literal_eval#ast.literal_eval
