link:
library/textwrap.html#textwrap.TextWrapper.subsequent_indent

docs:


¶
(default: ) String that will be prepended to all lines of wrapped
output except the first.  Counts towards the length of each line except
the first.
