link:
library/decimal.html#decimal.Decimal.is_canonical

docs:


()¶
Return  if the argument is canonical and 
otherwise.  Currently, a  instance is always canonical, so
this operation always returns .
