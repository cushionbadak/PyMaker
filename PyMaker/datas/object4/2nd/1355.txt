link:
library/array.html#array.array.pop

docs:


([i])¶
Removes the item with the index i from the array and returns it. The optional
argument defaults to , so that by default the last item is removed and
returned.
