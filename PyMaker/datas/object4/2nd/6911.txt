link:
library/imaplib.html#imaplib.Time2Internaldate

docs:


(date_time)¶
Convert date_time to an IMAP4  representation.
The return value is a string in the form:  (including double-quotes).  The date_time argument can
be a number (int or float) representing seconds since epoch (as
returned by ), a 9-tuple representing local time
an instance of  (as returned by
), an aware instance of
, or a double-quoted string.  In the last
case, it is assumed to already be in the correct format.
