link:
library/urllib.request.html#module-urllib.response

docs:

 — Response classes used by urllib¶
The  module defines functions and classes which define a
minimal file like interface, including  and . The
typical response object is an addinfourl instance, which defines an 
method and that returns headers and a  method that returns the url.
Functions defined by this module are used internally by the
 module.
