link:
library/cgitb.html#cgitb.enable

docs:


(display=1, logdir=None, context=5, format="html")¶
This function causes the  module to take over the interpreter’s
default handling for exceptions by setting the value of .
The optional argument display defaults to  and can be set to  to
suppress sending the traceback to the browser. If the argument logdir is
present, the traceback reports are written to files.  The value of logdir
should be a directory where these files will be placed. The optional argument
context is the number of lines of context to display around the current line
of source code in the traceback; this defaults to . If the optional
argument format is , the output is formatted as HTML.  Any other
value forces plain text output.  The default value is .
