link:
library/bisect.html#bisect.insort_left

docs:


(a, x, lo=0, hi=len(a))¶
Insert x in a in sorted order.  This is equivalent to
 assuming that a is
already sorted.  Keep in mind that the O(log n) search is dominated by
the slow O(n) insertion step.
