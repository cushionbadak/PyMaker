link:
library/shutil.html#querying-the-size-of-the-output-terminal

docs:

Querying the size of the output terminal¶


(fallback=(columns, lines))¶
Get the size of the terminal window.
For each of the two dimensions, the environment variable, 
and  respectively, is checked. If the variable is defined and
the value is a positive integer, it is used.
When  or  is not defined, which is the common case,
the terminal connected to  is queried
by invoking .
If the terminal size cannot be successfully queried, either because
the system doesn’t support querying, or because we are not
connected to a terminal, the value given in  parameter
is used.  defaults to  which is the default
size used by many terminal emulators.
The value returned is a named tuple of type .
See also: The Single UNIX Specification, Version 2,
Other Environment Variables.

New in version 3.3.


