link:
library/re.html#re.Pattern.finditer

docs:


(string[, pos[, endpos]])¶
Similar to the  function, using the compiled pattern, but
also accepts optional pos and endpos parameters that limit the search
region like for .
