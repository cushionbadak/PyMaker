link:
library/atexit.html#atexit.unregister

docs:


(func)¶
Remove func from the list of functions to be run at interpreter
shutdown.  After calling , func is guaranteed not to be
called when the interpreter shuts down, even if it was registered more than
once.   silently does nothing if func was not previously
registered.
