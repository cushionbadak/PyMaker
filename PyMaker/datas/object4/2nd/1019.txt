link:
library/codecs.html#codecs.namereplace_errors

docs:


(exception)¶
Implements the  error handling (for encoding with
text encodings only): the
unencodable character is replaced by a  escape sequence.

New in version 3.5.

