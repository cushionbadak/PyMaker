link:
library/platform.html#platform.python_version

docs:


()¶
Returns the Python version as string .
Note that unlike the Python , the returned value will always
include the patchlevel (it defaults to 0).
