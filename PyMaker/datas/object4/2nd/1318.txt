link:
library/heapq.html#heapq.heappushpop

docs:


(heap, item)¶
Push item on the heap, then pop and return the smallest item from the
heap.  The combined action runs more efficiently than 
followed by a separate call to .
