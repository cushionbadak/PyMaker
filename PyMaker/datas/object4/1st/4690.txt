link:
library/asyncio-sync.html#asyncio.Condition.acquire

docs:


coroutine acquire()¶
Acquire the underlying lock.
This method waits until the underlying lock is unlocked,
sets it to locked and returns True.
