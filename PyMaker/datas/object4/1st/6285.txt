link:
library/xml.dom.minidom.html#xml.dom.minidom.Node.toxml

docs:

<dt id="xml.dom.minidom.Node.toxml">
<code class="descclassname">Node.</code><code class="descname">toxml</code><span class="sig-paren">(</span><em>encoding=None</em><span class="sig-paren">)</span><a class="headerlink" href="#xml.dom.minidom.Node.toxml" title="Permalink to this definition">¶</a></dt>
<dd><p>Return a string or byte string containing the XML represented by
the DOM node.</p>
<p>With an explicit <em>encoding</em> <a class="footnote-reference" href="#id3" id="id1">[1]</a> argument, the result is a byte
string in the specified encoding.
With no <em>encoding</em> argument, the result is a Unicode string, and the
XML declaration in the resulting string does not specify an
encoding. Encoding this string in an encoding other than UTF-8 is
likely incorrect, since UTF-8 is the default encoding of XML.</p>
</dd>