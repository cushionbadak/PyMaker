link:
library/asyncio-queue.html#asyncio.Queue.full

docs:


full()¶
Return True if there are maxsize items in the queue.
If the queue was initialized with maxsize=0 (the default),
then full() never returns True.
